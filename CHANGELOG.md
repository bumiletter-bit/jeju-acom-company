## v5.9.275 (2026-08-23) — app.js v=366
- 🗓️ **#397(대표 8/23): 정산현황 달력 실시간 갱신** — 금액을 입력하면 하단 요약은 즉시 바뀌는데 **달력의 금액·전일대비(+/-)는 새로고침해야 갱신**되던 것 교정. 원인 = 입력 핸들러(`ssInp`)가 데이터·요약만 갱신하고 `ssRenderCalendar()`를 호출하지 않았음(달력은 날짜 선택/추가/삭제 때만 재렌더).
- 교정 = ssInp에 달력 재렌더 1줄 추가. 달력은 별도 컨테이너(#ss-cal-wrap) 순수 재생성이라 입력 폼 포커스 무영향.
- 검증(`scripts/verify-397-live-cal.js` **10/10** — 로컬 실서버·실클릭·실타이핑·쓰기 route 가로채기로 실DB 무변경): 입력 즉시 달력 금액 변경(4,813만→12,907만)·키 입력마다 갱신·리로드 0회·포커스 유지·전일대비 동시 갱신·요약 합계 무회귀·월 이동 무회귀·에러 0.

## v5.9.274 (2026-08-23) — 서버만·app.js 무변경
- 📷🔴 **#396-c: 오염 판정 범위를 응답 전체로 확대** — 273 배포 후 본선(full) 검증에서 실측된 구멍: partner에만 오염 마커가 남고 items는 마커 없이 **깨진 품목 1건("과 즙팡팡" 1박스)으로 줄어든 채** items-한정 오염 검사를 통과(실제 12품목·479박스). 오염 = 그 생성 전체의 탈선 신호이므로 **응답 어디든 오염이면 items가 남았어도 불신 → 평문 폴백 채택**, 폴백도 실패하면 채택 금지(오류).

## v5.9.273 (2026-08-23) — 서버만·app.js 무변경
- 📷🔴 **#396-b: 정산 OCR 실패의 진짜 원인 확정 — 도구 JSON 태그 오염(#9716 계열)** — v5.9.272의 진단 러너로 실패 이미지를 재현한 결과 `stop_reason='tool_use'`(잘림 아님)인데 **`partner="</antml…parameter>"` 태그 오염**이 실측됐고, 오염이 심한 호출에서 items가 통째로 사라져(raw_items=0) "품목 0건" 오류로 위장. 272의 max_tokens 가설은 기각(가드는 무해라 유지).
- 교정: **평문 JSON 폴백**(`settleOcrPlainRead` — maruPlainAnswer #9716과 같은 원리, 도구 없이 평문 재질의라 오염 원천 불가) — 도구 경로가 잘림·품목 0건·items 오염이면 폴백 사용. items 오염인데 폴백도 실패하면 **의심 데이터 채택 금지**(정산=돈 표 — 부분 품목으로 진행 안 함). partner만 오염은 무해(거래처는 지시문·품목 구성으로 자동 인식).
- 진단 러너 mode 확장: tool(도구 1회)·plain(폴백 단독)·full(실제 본선 경로) — 실패 이미지 3장으로 전 경로 검증.

## v5.9.272 (2026-08-23) — 서버만·app.js 무변경
- 📷 **#396(대표 실물): 정산관리 이미지 판독 실패 다발 교정** — "이미지에서 품목을 하나도 읽지 못했습니다"가 8/11부터 간헐 발생, 8/23 아침 6/7건 실패.
- 🔴 원인: 마루 비전 판독 호출의 `max_tokens: 1500` — **sonnet-5는 내부 사고(adaptive thinking)가 같은 예산을 잠식**(#373 실측 계열)해 도구 JSON이 잘리면 `tool_use.input`이 빈 객체로 와 "품목 0건"으로 위장됐다. `stop_reason` 검사가 없어 잘림이 이미지 문제로 보였음. 실패 이미지 전수 확인 = 전부 정상 판독 가능한 표(같은 목록의 재캡처가 성공/실패로 갈림 = 호출마다 확률적).
- 교정: ①max_tokens 1500→**4000** ②`stop_reason='max_tokens'` 또는 품목 0건이면 **1회 자동 재시도** ③재시도 후에도 잘렸으면 **부분 품목 채택 금지**(정산=돈 표라 불완전 판독 차단) + 오류에 out_tokens 명시 ④진단 러너 `settle_ocr_probe_request {ids, max_tokens}` 신설 — 보관 이미지로 stop_reason·토큰 사용량 재현(쓰기 없음·PII 없음).

## v5.9.271 (2026-08-21) — app.js v=365
- 📦 **#395(대표 GO): 「주문 정리기」 새 메뉴 이식** — 현금·단체주문의 두서없는 주소를 자동 정리·검증하고 CJ대한통운 발송 엑셀을 만드는 단일 HTML 도구(v2.4)를 오피스 새 메뉴(송장변환 아래·직원 공용)로 통합.
- 이식 구조: parser.js·extract.js **원형 무수정 이식**(order-parser/extract.js) + 앱 로직(order-organizer.js)은 통신층만 교체. 업무 규칙 7개(애매하면 자동확정 금지·원본 그대로 두기=상태 유지·수취인명에는 수취인만·보내는이 미입력 시 공란·시트 원본 순서·모달 ✕/취소만·헤더 공백 무시) 전부 원형 보존.
- **주소 검증 = 서버 프록시 전환**(`GET /api/agent-office/juso`) — 승인키는 env `JUSO_API_KEY`만(코드·화면·브라우저 노출 0). 같은 검색어 5분 캐시 + 분당 과호출 가드. juso 자가진단 러너(`juso_test_request`) 신설.
- **설정 공유** = 출고지·기본 배송메세지·주문경로만 서버 공용(`order_organizer_settings`·audit 기록). **보내는이 3칸은 건별 입력 — 초기화 시 전부 비움**(대표 조건 3, 원본의 변경주소 미초기화 결함도 함께 교정).
- **OCR·pako = 자체 서버 서빙(/vendor) + lazy-load** — 외부 CDN 무관(대표 조건 1), 첫 화면 로딩 증가 0. tesseract.js 5.1.1 고정, **이미지 전처리(격자선 제거+3배 확대)·bbox 행 재조립·PSM 3 원형 유지**(대표 조건 2). 의존성 5종 추가(pako·tesseract.js·-core·kor/eng 데이터).
- 검증: 파서 단위 **18/18** + UI 회귀 **40/40**(README 8번 6케이스·규칙·엑셀 재독(13컬럼/헤더색/노랑/테두리/맑은고딕)·시트 모달·vendor 6종·XLSX.CFB·타 화면 paste 무간섭·전 페이지 전환·에러 0) + **레퍼런스 완성본과 같은 입력→같은 출력 완전 일치**(자유 텍스트 9필드×4행·TSV) + OCR 실동작(이미지→전화·주소 인식→자동 파싱) + **배포 후 juso 실API 러너 9/9**(연삼로 1066-31 → 우편번호 63326 · UI 검증의 juso 값도 실응답으로 교체 후 40/40 재통과 · 실서버 vendor 200).

## v5.9.270 (2026-08-21) — app.js v=364
- 🖥️ **#394(대표 실물): 정산관리가 가끔 빈 화면으로 뜨던 결함 교정**
- 🔴 원인(로컬 실서버+실클릭으로 재현 확정): 문의 관리의 탭 7개·서브탭 2개가 `.settlement-tab` 클래스를 **스타일로 재사용**하는데, 정산 탭 전환 핸들러가 이 버튼들에도 붙어 있었다. 그 버튼을 클릭하면 **정산 본문(.settlement-tab-content)의 active를 전부 꺼놓고** 자신은 data-tab이 없어 아무것도 다시 켜지 못함 → 다음 정산관리 진입이 빈 화면(#179는 이때 나던 TypeError만 막았고 active 제거 부작용은 남아 있었다). "가끔" = 직전에 문의 관리를 다녀온 경우.
- 교정 ①핸들러 첫머리 `if (!tab.dataset.tab) return;` — 스타일 재사용 버튼은 각자의 onclick이 전담 ②정산 진입 시 active 본문이 0이면 기본 탭 자동 복구(이중 안전).
- 검증: 재현 시나리오(정산 진입→문의관리 탭 2회 클릭→정산 재진입) 교정 전 **빈 화면 재현**(active 0) → 교정 후 **6/6**(본문 렌더·정산 탭 전환·문의관리 탭 전환 무회귀·에러 0).

## v5.9.269 (2026-08-21)
- 🍋 **#392(대표 GO): 청귤 사전예약 종료 — AI 배송 표의 「청귤 등 사전예약 상품은 이 표와 무관」 하드코딩을 동적으로**
- 종전엔 이 줄이 고정이라 청귤이 일반 전환된 뒤에도 봇이 청귤 배송 문의에 정확한 발송·도착 표를 쓰지 않으려 했다. → **지금 예약 판매 중인 품목(판매중 + reserve_ship_start ≥ 오늘 KST — 알림톡 isReserveOrder와 동일 기준)이 실제로 있을 때만, 그 품목 이름으로** 예외 줄을 붙인다. 예약 품목이 없으면 줄 자체가 사라져 모든 품목이 표를 따른다. 날짜 비교는 SQL 안에서(`NOW() AT TIME ZONE 'Asia/Seoul'`)::date — #333 원칙. 조회 실패 시 일반 문구 폴백(예약 시즌에 예외 줄이 빠지는 사고 쪽이 더 위험).

## v5.9.268 (2026-08-13)
- 🔒 **#386-c 핫픽스: reviewed 목록이 게시판 300건 전체(다른 회원 후기 포함)의 주문번호를 내보내고 있었다** — 배포 직후 실응답 검산에서 자체 발견(박인석 님 등 남의 주문번호 23건 노출). 게시글 member_id가 본인인 것만 추리도록 교정. reviewArticles 30초 캐시 재사용이라 추가 호출 0. 🔵 교훈(#310 계열): **새 응답 필드는 배포 직후 실값을 눈으로 검산할 것** — 이번에도 검산이 노출을 몇 분 만에 잡았다.

## v5.9.267 (2026-08-13)
- ✍️ **#386-c(대표 실물): 후기를 쓴 주문에 「작성완료」 표시가 없던 것 — 서버가 알고 있는 작성 여부를 화면에 연결**
- 실측 배경: 대표가 주문 20260811-0000044에 후기 2건을 등록했는데도 주문내역 카드의 [후기 쓰고 500원 받기] 버튼이 그대로였다. **카페24는 후기를 써도 구매후기 버튼을 계속 노출**하고, 같은 주문에 중복 등록도 막지 않는다(실증 — 2건 등록됨).
- 교정: `/api/mall/pub/reviewable` 응답에 **`reviewed`(작성완료 주문번호 목록)** 추가 — reviewQuota의 게시판 대조 결과(done 집합)를 그대로 내보낸다. 30일 창과 무관하게 전체(옛 주문에 쓴 후기도 완료 표시가 맞다). 스킨 주문내역(list.html)이 이 값으로 해당 카드 버튼을 「후기 작성완료 ✓」로 교체(스킨 배포는 별도 — 서버는 이 필드 추가뿐, 기존 필드·판정 무변경).
- 🔎 **#384(대표 GO): 자사몰 홈 「관심고객」이 8/10에 멈춰 있던 원인 교정 — 공개 지표 수집 429 재시도**
- 🔴 **원인(실측 확정)**: 새벽 04:30 수집이 중계서버 브라우저를 [①리뷰 → ②스토어 지표] 연달아 부르는데, 중계서버는 **응답을 먼저 보내고 브라우저를 닫은 뒤에야 동시 실행 잠금(`_npBusy`)을 푼다**. 1GB 서버에서 Chromium 정리가 수 초~수십 초 걸려, ①의 응답 직후 도착한 ②가 **자기 잠금에 걸려 429**. 8/10·8/11·8/12 새벽 3회 연속 실측(note: `공개지표 갱신 실패→직전분 유지: naver_public_error_429`). 낮 수동 실행은 성공 — 리뷰는 첫 호출이라 매일 성공했고 지표만 죽던 패턴과 정확히 일치.
- **교정**: `naver-relay.js callNaverPublic`에 **429 한정 재시도**(15초 간격 최대 3회 — 잠금은 브라우저 정리가 끝나면 풀린다). 그 외 상태·연결 오류는 종전대로 즉시 실패 = 동작 무변경. 호출부 3곳(리뷰·store·products)이 한 함수라 전부 커버.
- ✅ **검증 7/7**(실코드 블록 추출 실행 — 규약 준수): 429×2 후 3번째에 성공·대기 15초×2 · 429 지속 시 4회 시도 후 종전과 같은 오류(무한 루프 없음) · 성공 시 1회 호출(평시 무변경) · 500은 재시도 없이 즉시 실패 · 요청 페이로드 종전 동일.
- ℹ️ **누적 리뷰 229,561(7/30 고정)은 별개 문제로 미조치** — 수집 지점(상품 페이지)의 「99,999+」 상한 때문에 잘린 합계를 일부러 버리는 #310 가드가 작동 중. 실값 갱신은 수집 지점 변경(중계서버 수정 = 대표 SSH 1회) 필요 — B안으로 대기.

## v5.9.265 (2026-08-12)
- 🗓️ **#383(대표 지시): 발송 휴무일 달력 — 「누를 때마다 상태가 바뀌는」 순환 방식 폐지 → 편집 창으로 교체**
- 🔴 **왜 바꿨나 (실사고가 먼저 있었다)**: 종전엔 ⓐ빈 날짜는 **시작→종료 2회 클릭**으로 등록하고 ⓑ지정된 날짜는 **누를 때마다** 발송휴무 → +도착불가 → 도착불가만 → 해제로 **순환**했다. 그런데 **사유(reason)를 고치는 길이 없어서**, 문구를 바꾸려면 삭제 후 재등록해야 했다. 2026-08-12 대표가 달력을 잘못 눌러 그 경로를 타면서 **8/13~8/16 사유가 「택배사 택배 없는날 및 공휴일(광복절) 휴무」 → 「택배사휴무」로 유실**됐다(손님 알림톡에 그대로 나가는 문구). 대표 원문: *"한번씩 누르면 변경 되는식이 아니라! … 체크하여 한번에 입력할수 있게"*.
- **새 동작**: 날짜를 누르면 **편집 창**이 열린다 — [기간 시작~종료] + [☑ 발송 휴무 / ☑ 도착 불가] + [사유] + [저장]·[해제]·[취소]. **클릭만으로 상태가 바뀌거나 삭제되는 경로는 전부 사라졌다**(오조작 원천 차단).
  - **기간 한 번에**: 창 안 [종료일]을 늘리면 8/10~8/15처럼 여러 날이 한 번에 저장된다. 일수와 「이 기간 전체가 아래 설정으로 통일됩니다」를 실시간 표시.
  - **★ 사유만 고쳐 저장 가능**: 기존 지정일을 누르면 **그 날의 사유가 채워진 채** 열린다 → 삭제→재등록이 필요 없어져 이번 유실 사고 경로가 구조적으로 막혔다.
  - **[해제]**는 창에 잡힌 기간 안에서 **실제로 등록된 날짜만** 삭제(없는 날짜는 건너뜀).
  - 저장 전 화면 단 검증: 사유 필수 · 발송휴무/도착불가 중 하나 이상 · 종료일 ≥ 시작일 · 31일 이내(서버와 같은 조건).
- 🔵 **서버 무접촉** — 등록 API가 이미 기간·구분·사유를 모두 받고 `ON CONFLICT (holiday_date) DO UPDATE`라 기간 안에 기존·신규가 섞여 있어도 한 번에 통일 저장된다. **화면(app.js) 한 블록만 교체** = 발송일 계산 엔진·알림톡 문면 경로 전부 무영향.
- **디자인**: 기존 `.modal-overlay`/`.modal`/`.btn-primary`/`.btn-outline`/`.akm-date` 재사용(가이드 §5 — 새 모양 발명 금지). 커스텀 달력(z-index 5000)이 모달(1000) 위에 떠 기간 선택이 가려지지 않는 것도 실측 확인. 모바일 터치 영역 44px+.
- **달력 위 안내 문구 교체**: 「지정된 날짜를 누를 때마다 발송휴무 → +도착불가 → 도착불가만 → 해제」 → 「날짜를 누르면 편집 창이 열립니다 — 기간·구분·사유를 정해 저장하세요」. 칸 툴팁도 함께.
- ✅ **실측 41/41**(실서버·실DB·실클릭 — 쓰기는 전부 가로채 실DB 무변경): 안내 문구 교체·옛 문구 잔존 0 · 빈 날짜/지정일 각각 창 열림 · **클릭만으로 쓰기 0건(순환 폐지 증명)** · 기간 6일 저장 페이로드 정확 · 검증 3종(사유 없음·둘 다 해제·기간 역전) 차단 · **기존 사유 자동 채움** · 사유만 수정 저장 · 8/13(발송휴무만)·8/14(둘 다) 체크 상태가 실DB와 일치 · [해제] 시 기간 내 3일만 DELETE · 열고 닫기 3회 후 창 잔존 0 · 모달 안에서 커스텀 달력 정상 · 모바일 터치 44px+ · pageerror 0.
- ℹ️ 판매현황 탭의 가로 넘침 258px은 **달력을 숨겨도 258px로 동일**함을 실측 — 이번 변경과 무관한 기존 문제(#336에 기록됨·범위 밖).

## v5.9.264 (2026-08-12)
- ✍️ **#381(대표 지시): 후기 물방울 — 동선 교체 · 배송완료 30일 이내 조건 · 조회 300건**
- **배경(대표 확인 과정에서 드러난 것)**: 귤나무 퀘스트의 [+1 받기]는 후기를 아직 안 썼으면 **눌러도 거절 문구만 뜨는 자리**였다(실지급 이력 **0건**). 반면 마이 > 리뷰 관리는 **받을 게 있을 때만 버튼이 뜨고** 후기 쓸 주문 목록까지 보여준다. 대표 지시 = **퀘스트를 누르면 리뷰 관리로 보내라.**
- **① 퀘스트 → 리뷰 관리 이동**(farm.html): 버튼이 그 자리에서 `review-claim`을 부르던 것을 **`index.html#v/review` 이동**으로. 받을 게 있으면 버튼에 「💧N개 받기」, 쓸 주문이 있으면 「후기 쓸 수 있는 주문 N건」으로 알려주되 **가는 곳은 리뷰 관리 하나**.
- **② 지급 대상 = 배송완료 후 30일 이내 주문**(대표 확정). 🔴 **카페24에 「배송완료일」 필드가 없다**(실측) — 있는 것은 `items[].shipped_date`(출고일)뿐이고 상태 N40이면 배송이 끝난 건이다. → **출고일 + 32일**을 경계로 잡았다. 우리 배송이 출고 다음날~이틀 도착이므로 손님 기준 **"받은 날로부터 30일"이 확실히 보장**된다(짧게 잡아 손님이 손해 보는 쪽을 피함).
- **③ 후기 조회 100 → 300건**: 카페24는 **1회 limit 100이 상한**(실측 422 `[Limit] should be between 1 and 100`) → **offset으로 3페이지** 순회. 자사몰 주문이 늘면 회원 후기가 최근 100건 밖으로 밀려날 수 있다는 대표 지적 반영. ⚠️ `member_id` 파라미터는 **카페24가 무시**하므로(실측: 지정해도 전체가 온다) 응답에서 직접 거른다.
- **④ 🔴 판정을 한 곳으로 통일(`reviewQuota`)** — 종전엔 **리뷰 관리는 `order_id` 대조 · 퀘스트는 `member_id` 대조**로 서로 다른 기준이었다. 같은 사실을 두 곳에서 각자 계산하면 반드시 어긋난다(#379 교훈) → `/pub/reviewable`·`/pub/review-claim`·`/pub/wallet`이 **모두 이 함수 하나**를 쓴다.
- **⑤ 문구 정정**: 「**사진** 후기 쓰기」 → 「후기 쓰기」(사진 무관 — #259 대표 확정) · 「확인 후 지급」 **삭제**(직원 검토가 아니라 버튼 누르면 즉시 자동 확인인데, 손님이 "쓰면 알아서 주겠지"로 읽어 **버튼을 안 누르게 만드는** 문구였다) · 대상 조건을 **리뷰 관리 화면에 상시 표시**(목록이 비어 있을 때도 "왜 없는지"가 보이게) · 각 주문에 **「N일 남음」** 표기.
- **성능**: 배송완료 목록에 60초 캐시 추가(지갑·리뷰 관리 공유), 후기 게시판은 30초 캐시로 3페이지 결과를 함께 보관 — 페이지가 3배가 돼도 실호출은 늘지 않는다.
- **무회귀**: 카페24 조회 실패 시 종전과 같은 안전 폴백(주문 수 상한). 기존 지급 이력이 0건이라 정책 변경으로 회수되는 물방울도 없다.

## v5.9.263 (2026-08-11)
- 🍊🔴 **#380 지금 팔지 않는 「타이벡 감귤」로 손님에게 안내하고 있었다 (대표 실물)**
- **대표 지적**: 봇이 *"타이벡 감귤은 지금 시기 당도가 한창 올라있어…"* 라고 답했는데, **지금 판매 중인 건 하우스귤**이다. 대표 원문 — *"지금 상품명만 그거고 하우스귤 판매중인거야. 타이벡감귤이 아닌 감귤이라고 해야 해."*
- 🔴 **원인 = 네이버 상품명이 검색용이라 재배방식이 전부 적혀 있다** — `제주 노지 감귤 타이벡 하우스 …`. 그 상품 페이지에서 문의가 들어오면 **상품 맥락에 '타이벡'이 실리고**, AI가 시나리오 「품목-타이벡·노지감귤」(제철 11~1월)을 재료로 골라 그 이름으로 답했다. **판매현황(`bot_products`)의 감귤은 「고당도 하우스감귤」 6종뿐**이고 타이벡·노지는 없다(실측).
- **교정 = 「[오늘 판매 품목] 감귤 이름 부르는 법」 가상 시나리오 주입**(`citrusNamingToday` — scenario_no 891): 손님께는 **그냥 「감귤」**로 부르고, **지금 판매하지 않는 재배방식(타이벡·노지·비가림)은 답변에 쓰지 말라**고 명시. 상품명·문의에 그 단어가 있어도 마찬가지이며, **그 재배방식 전용 재료도 쓰지 말 것**까지 지시.
- 🔵 **하드코딩하지 않는다** — 판매현황에서 매번 판정하므로 **11월에 타이벡이 다시 판매중이 되면 규칙이 저절로 풀린다.** 감귤을 하나도 안 파는 시기엔 주입 자체를 생략(종전과 동일).
- 주입 = `/api/scenarios`(톡톡·카카오) · `qnaGenerate`(상품문의·자사몰 챗) · `inquiryGenerate`(고객문의) — #379와 같은 4채널.
- ✅ **실측 10/10**(실 server.js 블록 추출 실행): 타이벡·노지·비가림이 금지 목록에 · 하우스는 판매중으로 분류 · 실DB 판매중 목록에 타이벡/노지 **0건** 대조 · 배포 후 실답변에서 「타이벡」 소멸 확인.
- **무회귀**: 재료 1건 추가. 조회 실패·감귤 미판매 시 **빈 배열 = 종전과 완전히 동일**. 시나리오 원본(#32 품목-타이벡·노지감귤)은 **무접촉** — 제철이 오면 그대로 다시 쓰인다.
- 🔵 **교훈: 네이버 상품명은 검색용이라 "지금 파는 것"과 다르다.** 상품명을 그대로 손님 안내에 쓰면 안 되고, **판매현황이 정본**이다.

## v5.9.262 (2026-08-11)
- 🚚🔴 **#379 톡톡봇·AI 답변이 「발송 휴무일 관리」와 전혀 연동돼 있지 않았다 (대표 실물) — 4채널 연동**
- **대표 실물**: 손님이 톡톡으로 *"모레인 8월 13일 주문하면 언제 배송되나요?"* → 봇이 **"8월 13일 오전 8시 이내 주문해주시면 당일 발송되어 다음 날 도착"** 이라고 답했다. **8/13~8/16은 발송 휴무**(택배사 택배 없는날·광복절)라 실제 발송일은 **8/17(월)** 이다.
- 🔴 **원인 = AI가 쓰는 재료에 휴무일이 아예 없었다.** 톡톡봇 재료는 「시나리오 + 판매현황」 둘뿐이고 `shipping_holidays`·`computeShipping` 참조가 **0건**. 그래서 시나리오 「배송 일정 안내」의 고정 문구("오전 8시 이내 주문 시 당일 발송 · 토요일만 배송휴무")를 그대로 안내할 수밖에 없었다. **봇은 오늘 날짜조차 프롬프트에 없다** — 특정 날짜 계산이 구조적으로 불가능했다.
- 🔴 **같은 손님이 채널마다 다른 안내를 받고 있었다**: 주문 **전**(톡톡) = "당일 발송" / 주문 **후**(알림톡) = "8/17 발송". 알림톡·문자는 `computeShipping`을 쓰고 있어 **원래 정확했다**(소급 피해 없음 — 실측 확인).
- **교정 ① 「[오늘 발송 일정]」 가상 시나리오 주입** (`shippingScenarioToday`) — 오늘부터 14일치 **주문일별 발송·도착 예정표**를 재료로 넣는다. **알림톡과 같은 계산기(`computeShipping`)·같은 DB**를 쓰므로 두 안내가 구조적으로 어긋날 수 없다. 방식은 #108(시기 지식)과 동일한 「가상 시나리오」라 **봇 프롬프트 구조 무수정**.
  - 주입 4채널 = `/api/scenarios`(톡톡·카카오) · `qnaGenerate`(상품문의·자사몰 챗) · `inquiryGenerate`(고객문의).
  - 🔵 **상대 표현("내일")을 쓰지 않고 절대 날짜로 적는다** — 표의 각 행은 '그 날짜에 주문했을 때'가 기준이라, 상대 표현을 쓰면 손님(오늘 기준)과 AI가 서로 다른 날을 가리킨다.
  - 표에 **8시 컷오프(#171) 전·후를 함께** 적고, 발송·도착이 같으면 한 줄로 합친다(사유 꼬리는 비교에서 제외 — 같은 일정이 두 줄로 갈려 AI가 다른 안내로 읽는 것 방지).
  - 예약 상품(청귤)·표 범위 밖 날짜는 **이 표를 쓰지 말라고 명시** — 순차발송 안내가 오염되지 않게.
- **교정 ② 톡톡봇 "(예시)" 줄 치환기 교체** (톡톡봇 `2026-08-11.3`) — 봇의 `getDeliveryScheduleText()`는 **"토요일만 발송 휴무"만 아는 자체 계산기**라 휴무 기간에 틀린 날짜를 넣고 있었다. `/api/scenarios` 응답의 **`delivery_today`**(회사 계산값)를 우선 쓰고, 없으면 종전 계산으로 폴백.
  - `delivery_today`는 **시나리오 version 판정에서 제외** — 넣으면 5분마다 version이 올라 정적 프롬프트가 재구성되고 Anthropic 프롬프트 캐시가 깨진다(비용 증가). 프롬프트가 아니라 후처리 전용이므로 version과 무관한 것이 맞다.
- ✅ **실측**: 표 전수 대조 **28/28**(14일 × 8시 전·후 — 실 server.js 블록을 떼어 실행해 알림톡용 `computeShipping` 결과와 1:1) · 톡톡봇 치환 **6/6**(실 `ai-handler.js` 블록 실행 — 회사값 채택·연동 실패 시 폴백 동일·본문 보존·`$` 치환 안전) · AI 실답변 배포 전/후 대조.
- **무회귀**: 재료 1건 추가일 뿐 기존 시나리오·응답 로직 무접촉. 계산·조회 실패 시 **빈 배열 반환 = 종전과 완전히 동일**. 봇은 회사 값이 없으면 기존 자체 계산 그대로.
- 🔵 **교훈: 같은 사실을 두 시스템이 각자 계산하면 반드시 어긋난다.** 발송일은 알림톡·톡톡·상품문의·자사몰이 전부 **하나의 계산기**를 보게 만들어야 한다.

## v5.9.261 (2026-08-11)
- ↩️🔴 **#374-r 원복 (대표 확정): 자사몰 챗 미답변 텔레그램 알림은 넣지 않는다 — 결함이 아니라 설계였다**
- 🚨 **내 잘못**: 대표 발화("자사몰 미답변이 우리에게 오는 게 없는데")를 **결함 신고로 해석해 확인 없이 시공**했다. 실제로는 대표가 의도적으로 그렇게 둔 것이었다 — 대표 원문: *"자사몰챗은 우리가 답장할 수 없는 구조라 텔레그램도 안 받고 한 거야. 지금 니가 수정 전에 나에게 물어보고 수정했어야 했어."* **판단 지점은 먼저 물을 것**(CLAUDE.md 운용 원칙 위반).
- **원복 3건** — ⓐ자사몰 미답변 기록을 종전대로 `bot_response=null`(알림 조건에 안 걸리게) ⓑ알림 필터의 자사몰 전건 조건 제거 ⓒ텔레그램 문구에서 자사몰 줄 제거. **v5.9.258(수정 전)과 실코드 대조 → 알림 관련 변경 0건 = 완전 동일.**
- ✅ **유지 2건(대표 확인·별개 사안)**
  - **손님 안내 문구** — 대표가 실물로 보시고 *"잘 안내한거지!"* 확인. `"답변은 이 창이 아니라 카카오톡 채널 「제주아꼼이네」 또는 전화(010-6687-4031)로 드리니, 둘 중 편하신 곳으로 문의 남겨주세요!"` **대표 확정 원칙 = "자사몰은 AI가 답할 수 있는 건 답하고, 못 답하는 건 고객센터·카카오톡으로 유도한다"** — 이 문구가 그 원칙을 그대로 수행한다.
  - **#375 SKIP 노출 차단**(v5.9.260) — AI 내부 판정 문구가 손님 화면에 나가던 것으로 알림 사안과 무관한 별개 결함이라 유지.
- 🔵 **자사몰 챗 구조(확정 기록 — 다음 세션 주의)**: 직원 응대 화면·답장 경로가 **없다**. 그래서 ①AI 답변 ②미답변 시 고객센터·카카오톡 유도, **이 둘로 완결된다.** 직원 알림·직원 답변 기능을 넣지 말 것 — 대표가 그렇게 설계했다.
- ⚠️ 검증으로 `message_logs`에 3건 잔존(id 1509~1511 `mall:wCLAUDEVERIFY*`) — 그중 2건은 **한글이 깨진 채 저장**됐다(curl 전송 인코딩). 대표 폰에 깨진 알림이 간 원인. 정리는 대표 판단(soft-delete 컬럼이 없어 물리삭제만 가능).
## v5.9.260 (2026-08-11)
- 🚨 **#375 AI 내부 판정 문구("SKIP")가 손님 답변에 섞여 나가던 결함 — 4채널 차단**
- **#374 검증 중 실제로 재현됐다**(자사몰 실호출 — `message_logs` id=1509): 답변 끝에 `잠시만요 — 위 문의는 내용을 파악할 수 없는 경우라 정책상 SKIP 처리해야 합니다.` + `SKIP` 이 **그대로 손님 화면에 출력**됐다.
- 🔴 **원인 = SKIP 판정을 맨 앞에서만 봤다**(`/^SKIP\b/`). AI가 답을 다 쓴 뒤 **뒤늦게 "이건 SKIP 대상"이라고 본문 끝에 덧붙이면** 그 독백이 필터를 통과했다. 톡톡봇·회사 3채널 **전부 같은 구조**.
- **교정**: 본문 **어디든** `SKIP`이 있으면 답변하지 않는다(침묵 → 기존 SKIP 경로). AI가 스스로 "답하지 말라"고 판단한 것이므로 그 판단을 따르는 쪽이 안전하다.
- ✅ **오탐 위험 사전 확인**: 재료(`inquiry_scenarios`·`product_season_knowledge`)에 `SKIP` 문자열 **0건** · 최근 90일 정상 발송 답변 중 `SKIP` 포함 **1건(= 이번 검증 호출뿐)** → **정상 답변을 잘못 막지 않는다.**
- ℹ️ 함께 조사한 `### 시나리오 제목` 노출 5건은 **7/6~7/9 건**으로, 7/27 설계 변경 때 들어간 `###` 필터로 **이미 해결된 옛 결함**(이후 재발 0).
- 🔵 **교훈: 판정 토큰을 「맨 앞」으로만 검사하지 말 것.** 생성형 답변은 결론을 뒤에 붙이는 경우가 있어, 앞만 보면 내부 문구가 그대로 고객에게 노출된다.
## v5.9.259 (2026-08-11)
- 🛒🔴 **#374 자사몰 챗 미답변이 직원에게 한 건도 알려지지 않고 있었다 (대표 실물)**
- 대표: *"자사몰 챗 경우에 미답변 우리에게 오는 게 없는데."* → **실측 결과 사실이었다.**
- 🔴 **원인 = 기록값이 달랐다.** 톡톡·카카오는 미답변을 `bot_response='[SKIP-무응답]'`으로 남기는데 **자사몰만 `null`로 남겼다.** 직원 알림 폴러 조건이 `bot_response IS NOT NULL AND = '[SKIP-무응답]'`이라 **자사몰 미답변은 쿼리 단계에서 통째로 제외**됐다(#298에서 미답변 알림을 넣을 때 자사몰 경로만 빠진 것). 실측 = 최근 30일 자사몰 미답변 **2건 전부 알림 0** — 그중 1건이 **대표가 오늘 08:25 직접 겪은 문의**(`"8월13일이야! 모레!"`).
- **교정 ①** 자사몰 미답변도 **`[SKIP-무응답]`으로 기록** — 알림·통계·문의 관리 화면이 톡톡과 같은 기준으로 잡는다.
- **교정 ② 자사몰은 미답변 전건 알림.** 톡톡·카카오는 직원이 볼 화면이 각각 있지만(판매자센터·[내 채팅]) **자사몰 웹챗엔 직원 응대 화면이 자체가 없다** — 손님이 안내를 보고 카카오·전화로 오지 않으면 그 문의는 아무도 모르는 채 끝난다. 빈도도 낮아(30일 2건) 전건 알려도 폰이 울리지 않는다. **톡톡·카카오는 종전 선별(`NEEDS_STAFF_RE`) 그대로** — 인사·덕담까지 울리는 것 방지(#298-b) 유지.
- **교정 ③ 손님 안내 문구 정직화** — 종전 `"담당자가 직접 확인해서 답변드릴게요"`는 **자사몰 챗에 직원이 답할 화면이 없어 지킬 수 없는 약속**이었다(손님이 그 창에서 기다리면 영영 답이 없다). → `"답변은 이 창이 아니라 카카오톡 채널 「제주아꼼이네」 또는 전화(010-6687-4031)로 드리니, 둘 중 편하신 곳으로 문의 남겨주세요!"` (#307 원칙 — 못 지킬 안내는 하지 않는다).
- **교정 ④** 텔레그램 알림 문구에 자사몰 줄 추가 — `🛒자사몰은 직원이 답할 창구가 없어 손님께 카카오채널·전화로 안내해 둔 상태입니다 — 연락이 오면 응대해주세요`.
- ✅ **실측 8/8**(`verify-374.js` — **실 server.js에서 정규식·필터 조건을 그대로 떼어 실DB 행으로 판정**): 자사몰 미답변 2건 전부 알림 대상 전환(종전 둘 다 누락) · **무회귀 = 톡톡·카카오 800건 판정 변화 0건** · 톡톡 인사·덕담은 여전히 조용("넵"·"안녕하세요^^") · 질문형은 여전히 알림 · 채널 라벨 3종.
- ℹ️ **손님용 상담원 안내는 원래도 즉시 나가고 있었다**(스킨 실코드 확인 — `answered:false`여도 서버 `message`를 그대로 화면에 띄운다). 이번에 바뀐 건 **문구의 정확성**과 **직원 알림**이다.
- ⚠️ 과거 2건은 `null`로 저장돼 있어 소급 알림은 가지 않는다 — **앞으로 들어오는 건부터** 적용.
- 🔵 곁가지 확인: **카카오는 #301 이후 봇 경로 트래픽 0**(마지막 로그 8/8) — 대표 말씀대로 전건 수기 응대 중이며, 이번 변경의 영향을 받지 않는다.

## v5.9.258 (2026-08-11)
- ✂️🔴 **#373 AI 답변이 문장 중간에 잘린 채 발송된 사고 — 4채널 예산 상향 + 절단 방어 (대표 실물)**
- **증상**: 톡톡 답변이 `말씀 주신 내용 확인했습니다! 이미 청귤 50kg 주문해주셨고,` 에서 끊긴 뒤 곧바로 고객센터 문구로 끝났다(대표: "말하다 끊긴 느낌"). 손님 문의 = 이미 주문한 청귤 50kg + 추가 100kg를 **3~4일 간격으로 순차 발송** 요청.
- 🔴 **화면 문제가 아니라 생성 단계 절단** — `message_logs` id=1490에 저장된 값 자체가 **146자**로 그 자리에서 끝나 있었다(발송·기록·표시는 정상 동작). 손님이 받은 것도 이 상태.
- 🔴 **원인 = `max_tokens` 소진**. sonnet-5는 답을 쓰기 전 **내부 사고(adaptive thinking)에 같은 예산**을 쓴다. 이 문의는 ⓐ재료에 없는 조건(50kg+100kg 분할) ⓑ프롬프트 규칙상 "단정 말고 되물어라" 판단까지 겹쳐 **사고가 2048을 거의 다 잠식** → 본문 두 문단 쓰고 상한 도달. **회사프로그램 마루가 v5.9.109에서 겪은 것과 같은 원인**(그때 1200→4000으로 해결).
- 🚨 **절단이 감춰지는 구조였다**: 본문에 고객센터 번호가 없으면 서버가 마지막 줄을 **자동으로 붙인다**(꼬리 문구 안전망 — 톡톡봇 `ai-handler.js`·회사 `QNA_TAIL` 양쪽). 그래서 **끊긴 자리 바로 뒤에 마무리 문구가 붙어 겉보기엔 완결된 답변으로 보인다.** 대표가 실물을 보고 지적하지 않았으면 계속 나갔을 결함.
- **교정 ① 예산 상향 = 손님 응대 4채널 전부 2048 → 4000**
  - 톡톡봇 `ai-handler.js`(네이버 톡톡·카카오) / 회사 `qnaGenerate`(**네이버 상품문의 + 자사몰 AI챗** — 원천 하나 원칙) / `inquiryGenerate`(네이버 고객문의).
  - ℹ️ **AI 요원(글샘·마루·미소·예리)은 무접촉** — 이미 4000~8000.
- **교정 ② 절단 방어(더 중요) — 잘린 답변은 아예 내보내지 않는다**: `stop_reason === 'max_tokens'`면 **발송·게시하지 않고 기존 SKIP 경로로 침묵**. 톡톡은 직원이 판매자센터에서 직접 답하고, 카카오는 기존 error 분기가 [상담 연결] 안내를 보내고, 상품문의·고객문의는 `ai_status='skip'`으로 직원 대기, 자사몰 챗은 사람 연결 안내로 빠진다(**전부 이미 있던 경로 = 무회귀**). 예산을 키워도 언젠가 또 찰 수 있으므로 이 방어가 근본이다.
- **교정 ③ 진단 기록**: `stop_reason`이 정상(`end_turn`)이 아니면 로그에 남긴다 — 종전엔 기록이 없어 원인을 정황으로만 추정해야 했다.
- ✅ **무회귀 실측(대표 질문 "기존 그대로 나가는가·길어지거나 중복되지 않는가")**: 재현 도구(`qna_sim_request` — 생성만·발송 없음)로 **같은 문의 6건을 배포 전/후 대조**. 평소 유형 5건(청귤 배송·가격+배송 조합·파손 클레임·보관법·주소 변경) 답변 길이·구성·재료 **동일 수준 유지, 중복 문단 0, 장황화 0**. 🔵 **`max_tokens`는 상한일 뿐 목표가 아니다** — 답변 길이·구성은 시스템 프롬프트가 정하므로 예산을 키워도 평소 답변은 변하지 않는다. **비용도 실제 생성분만 과금되므로 평소 증가 0원**.
- 📊 빈도: 최근 60일 AI 답변 **176건 중 1건**(답변 길이 최솟값 146자 = 이 건, 평균 396자). 상시 결함은 아니지만 **판단이 복잡한 문의일수록 발생 확률이 높다** = 하필 가장 중요한 문의에서 터진다.
- ℹ️ 해당 손님 대화는 **직원(대표)이 이어받아 전건 처리 완료**(후속 3건 중 1건은 쿨다운 침묵 — 직원 답변 후 30분 규칙대로 정상 동작).

## v5.9.257 (2026-08-11)
- 📮 **#371 「직원 확인 필요 문의」 텔레그램 안내 문구 교정 (대표 실물)**
- 🔴 **문구가 낡아 있었다** — 대표가 받은 알림은 **네이버톡톡** 건인데도 `(‘봇 미답변’ = … 카카오는 챗봇 대화라 카카오 앱에는 알림이 오지 않습니다 …)`가 그대로 붙어 나갔다.
- 그 문장은 **#301(8/9)에서 카카오 챗봇을 끄고 상담 채팅으로 전환하기 전**의 상태를 설명한 것이다. 지금은 카카오도 채널 [내 채팅]에 뜨고 직원이 직접 답할 수 있다.
- 새 문구: `(‘봇 미답변’ = AI가 답하지 못한 건입니다. [문의 관리] > 💬 톡톡 문의에서 3채널을 함께 볼 수 있고, 답변은 네이버톡톡은 판매자센터·카카오는 채널 [내 채팅]에서 해주세요)`
- 🔵 **교훈: 운영 방식을 바꾸면 그 방식을 설명하던 안내 문구도 함께 찾아 고칠 것.** 코드 동작은 바뀌었는데 문구만 과거에 남아 직원을 잘못 안내하고 있었다(같은 계열 주석도 함께 갱신).
- ℹ️ 알림 대상 판정(요청형·미답변 선별)과 발송 로직은 **무접촉** — 문구만 바뀐다.

## v5.9.256 (2026-08-11)
- ☎️ **#357 시즌 대기 접수 — 잘못된 휴대폰 번호 차단 (대표 GO)**
- 종전 검증이 **10~11자리면 통과**라 한 자리를 빠뜨려도 접수됐다(실제로 `010` 10자리가 3건 쌓여 있었다). 이 명단은 시즌 오픈 때 **알림톡·문자 발송용**이라 잘못된 번호는 **그때 조용히 실패**한다 → 접수 단계에서 막는다.
- 규칙: **휴대폰만 허용**(`01[016789]`) · **`010`은 11자리 필수**(011·016~019는 옛 번호라 10자리도 정상). 서버(`/api/public/season-waitlist`)와 스킨(자사몰 상세·캘린더) **양쪽** 적용 — 서버가 최종 방어선.
- ℹ️ 관리자 수기 등록(`/api/agent-office/season-waitlist`)은 **그대로 뒀다** — 직원이 유선번호를 알고 넣는 경우가 있어 재량을 남긴다.

## v5.9.255 (2026-08-11)
- ☎️ **#356 시즌 오픈 대기 명단 — 연락처 전체 표시 (대표 실물)**
- 대표: *"우리 번호가 중간에 가려져 있어. 다 보이도록 해야 그때마다 연락드릴 수 있어 — 알림톡이든 문자든."*
- 🔴 **원인**: 목록 응답에서 서버가 `maskWaitContact`로 가리고 있었다(`server.js:5476`). **이 명단의 존재 이유가 시즌 오픈 시 발송**인데 가려두면 쓸 수가 없었다(손님이 그 목적으로 직접 남긴 번호).
- 교정: 목록 조회만 **원문 반환**. ⚠️ **감사 로그(audit_logs)에는 종전대로 마스킹만** 남긴다 — 로그에 원문을 쌓지 않는 원칙(#177)은 유지.
- 화면: 번호를 **하이픈 포맷**(`010-6687-4031`)으로 표시 + `nowrap`·`tabular-nums`. 캡처에서 메모가 세로로 깨져 있던 것도 함께 정리(메모 열 nowrap).
- ℹ️ 이 화면은 문의 관리 계열이라 **직원도 조회 가능**(#245 전 직원 공용 원칙). 관리자 전용으로 좁히려면 라우트에 `adminOnly`를 붙이면 된다.

## v5.9.254 (2026-08-11)
- 🗂 **#355 마이 > 「시즌 대기 목록」·「리뷰 관리」 실목록 + 「공지사항」 제거 (대표 지시)**
- 종전엔 셋 다 **준비중 모달**만 떴다. 대표: *"내가 시즌대기 누른 게 있으면 보이도록, 없으면 「시즌 대기 목록이 없습니다」. 리뷰는 실제 쓸 수 있는 게 있으면 목록, 없으면 없습니다. 리뷰는 물 1이 지급되는 거라 연동. 공지사항은 세팅한 게 없으니 없애도 된다."*
- 🔴 **시즌 대기 — 회원 연결이 없었다**: `season_waitlist`는 **연락처만** 저장해 누구 신청인지 알 수 없었다. → `member_key` 컬럼 신설 + 신청 시 로그인 회원ID 저장(자사몰 상세·캘린더 양쪽). 조회는 **회원ID 기준만** — 연락처로 조회하면 남의 번호를 넣어 남의 신청을 볼 수 있어 막았고, 응답에도 연락처를 담지 않는다.
  - 🚨 `ADD COLUMN`에 **DEFAULT를 주지 않았다** — 기존 행을 즉시 채우는 함정(#336에서 데인 것). 기존 3건은 NULL(회원 불명)이 정답이고 마이 화면에 뜨지 않는다.
- **리뷰 관리** = 신규 `GET /api/mall/pub/reviewable` — **배송완료 주문 중 아직 후기를 안 쓴 건**만 목록. 카페24 주문(N40)과 **상품후기 게시판(board 4)의 `order_id`를 대조**해 가른다. 물방울은 기존 `/pub/review-claim`과 **같은 계산**(주문 수 상한 · 실제 후기 수 상한 · 이미 받은 수 차감)으로 「받을 물방울 N개」 버튼에 연동.
  - ⚠️ PII 최소: 주문번호·주문일·상품명만 담는다(수취인·연락처·주소 제외). 카페24 주문 조회 3개월 제한 때문에 90일씩 2구간(`countPaidOrders`와 동일 방식).
- **공지사항 메뉴 삭제**(등록된 공지가 없어 준비중만 떴다).
- 신규 라우트 2종은 기존 `pubGate`(DB 스위치·CORS 화이트리스트·레이트리밋)를 그대로 통과한다.

## v5.9.253 (2026-08-11)
- 📨🔴 **#353 알림톡 8~9시 보류를 발송휴무일과 맞춤 (대표 실물)**
- **대표 지적**: 토요일·발송휴무일 08:00~08:59 주문건이 자동 안내를 못 받고 **수기 보류로 남아** 있었다. "토요일은 발송이 없기 때문에 자동으로 나가야 하고, 발송휴무 체크된 날도 나가야 한다."
- 🔴 **보류의 원래 목적**: 08~09시는 발주 작업 시간이라 **당일 출고에 편입될지 사람이 판단**해야 해서 보류(#171). 그런데 **토요일·발송휴무일은 애초에 출고가 없어** 판단할 것이 없는데도 똑같이 보류돼, 계산이 이미 명확한 건까지 수기로 남았다.
- **교정(조건 한 줄)**: `if (payHourKst === 8)` → `if (payHourKst === 8 && shippingSchedule.isShipDay(payDayKst, hinfo.set))`. 주문일이 **실제 출고하는 날일 때만** 보류하고, 아니면 종전처럼 자동 계산으로 흘려보낸다(계산이 다음 출고 가능일로 밀어주므로 결과가 명확). `isShipDay`는 **토요일 + 발송휴무일**을 모두 출고 불가로 본다(#69·#336) — 대표가 말한 두 조건이 한 함수로 커버된다.
- ✅ **대표 예시 실측**: `8/12 15:00 주문` = `8/13 08:05 주문` = **「월요일(8/17) 오전 발송, 화~수 도착 예정」** — 문면·발송일 완전 동일.
- ✅ **무회귀**: 정상 출고일 08:30은 **보류 그대로**(판단이 여전히 필요) · 07:59·09:00은 무변동 · `computeShipping` 계산부는 **무접촉**(보류 조건만 변경). 실측 **10/10**(실 server.js에서 조건식을 추출해 확인 + 실DB 발송휴무 설정으로 판정).
- ⚠️ **이미 쌓인 보류 건은 소급되지 않는다** — 과거에 `hold-0809`로 남은 건은 [알림 발송 이력]의 [오늘/내일 발송으로 안내] 버튼으로 처리해야 한다. 이번 변경은 **앞으로 들어오는 주문**부터 적용.

## v5.9.252 (2026-08-10)
- 🧾🔴 **#350 송장변환 — 자사몰(카페24) 주문이 미매칭되던 문제 (대표 실물)**
- **증상**: 중간발주에 `[미매칭] 제주 감귤 상품 선택=2. (제철)과즙팡팡 황금향 · 황금향 가정용 - 5kg(중소과 27과 전후)`.
- 🔴 **원인**: 자사몰 옵션 **그룹명이 「제주 감귤 상품 선택」** 이라 문자열에 '감귤'이 들어간다. `matchProductRaw`에서 `/비가림|감귤/` 분기(5823행)가 **황금향 분기(5853행)보다 30줄 먼저** 나와 **비가림귤로 오분류 → 카탈로그에 없어 [미매칭]**. 네이버 옵션명엔 '감귤'이 없어 **자사몰 건만** 터졌다.
- 🔴 **황금향만의 문제가 아니었다** — 실측: 카탈로그 60건 중 **27건**(황금향 4·한라봉 7·자몽 6·하귤 2·카라향 등)이 이 접두어 하나로 전부 미매칭.
- **교정(최소·국소)**: 감귤 분기에 **"아래에서 따로 처리하는 품목명이 함께 있으면 양보"** 가드 추가 — `&& !/황금향|한라봉|천혜향|레드향|세미놀|카라향|자몽|하귤|취나물/`. 그 이름이 없으면 조건이 종전과 완전히 같다.
- ✅ **무회귀 증명(대표 "다른 것 지장 가면 안 된다")**: 수정 전(HEAD) 코드를 그대로 떼어 **같은 입력 240건(카탈로그 60 × 형식 4종)으로 전수 대조** → **회귀 0건 · 개선 27건**. 순수·네이버 형식은 **완전 무변동**(개선 0·회귀 0 = 기존 경로 무접촉). 실코드를 파일에서 추출해 실행(하네스 재현 금지 규약 준수).
- ℹ️ 비가림귤은 현재 **카탈로그에 0건**(미판매 시즌)이라 전후 모두 미매칭이 정상 — 이 가드의 영향 자체가 없다.
- ⚠️ **미조치(범위 밖·보고만)**: 같은 오분류가 `app.js:1889`(정산 화면 과일명 추출 `fruit = '비가림귤'`)에도 있다. 송장변환과 별개 경로라 이번엔 손대지 않았다.

## v5.9.246 (2026-08-10)
- 🚚🔴 **#336 「발송 휴무」와 「도착 불가」 분리 (대표 실물 확정 — 어제 v5.9.245의 전제가 틀렸다)**
- **뒤집힌 전제**: 달력 체크의 정체는 **"우리가 출고하지 않는 날"** 이지 "택배가 안 가는 날"이 아니다(대표 원문: *"13일 발송이 없는거! 13일 배송은 되!! 14일이 택배없는날 휴무로 13일 발송 시 다음날 도착 14일을 못해서 그러는거야"*). 종전 코드는 휴무일 하나를 `isShipDay`·`isDeliveryDay` 양쪽에서 똑같이 제외해, **8/12 발송분이 8/13에 정상 도착하는데도 8/17 도착으로 안내**하고 있었다.
  - → v5.9.245 CHANGELOG 마지막 줄 「대표가 기대한 "수~목"은 나올 수 없는 값」은 **오판이었다. 「수~목」이 정답**이다(8/13 배달 가능).
- **스키마**: `shipping_holidays`에 `no_ship`(발송 불가)·`no_arrive`(도착 불가) 신설. 한 날짜가 둘 다일 수도, 하나만일 수도 있다(예: 광복절 대체휴무 = 우리는 발송 O · 택배 배달 X).
  - 🚨 **함정 실측**: `ADD COLUMN ... DEFAULT FALSE`는 **기존 행을 NULL이 아닌 FALSE로 즉시 채운다** → 뒤따르는 "미설정(NULL)만 채우기" 마이그레이션이 **한 건도 안 걸렸고**, 시드 공휴일 17건이 전부 「도착 가능」으로 남아 설날·추석 배달 계산이 뒤집힐 뻔했다(적용 직후 요약 숫자 `arr_off: 4`가 이상해 잡음). → **DEFAULT 없이 ADD COLUMN** 후 값 지정으로 교정.
- **계산**(`shipping-schedule.js`): `isDeliveryDay`가 발송휴무가 아니라 **도착불가 집합**만 본다. 발송안내용 `computeArrival()` 신설.
  - 🔴 **발송안내(E) "내일/모레" 고정 문구 폐지**: 종전엔 `내일 {{내일요일}}요일~모레 {{모레요일}}요일`을 **문자열로 박고 요일만 갈아끼워**, 실제로는 닷새 뒤 도착인 건에도 "내일"이 붙어 나갔다(대표 지적). 실제 간격을 재서 맞을 때만 "내일"이라 쓴다.
- **휴무 사유 = 고객 문구**(대표 "휴무사유 하나만 나가면 되니깐"): 종전 #73의 「안내 문구로 통째 대체(override)」 **폐지** — 발송일이 통째로 사라져 8/13~16 주문 고객은 `▶ 택배사 택배 없는날 및 공휴일(광복절) 휴무`만 받고 **언제 받는지 한 글자도 못 듣는 상태**였다. → 자동 계산 **뒤에 사유를 병기**: `▶ 월요일(8/17) 오전 발송, 화~수 도착 예정 (택배사 택배 없는날 및 공휴일(광복절) 휴무)`. 휴무 지정 입력창도 사유 1개로 축소(안내문구 칸 삭제).
- **달력 3종 표시**(대표 "표시 늘리되 예상해서 표시해주고 수정할 게 있으면 직접 탭 눌러서"): 발송휴무(danger)·도착불가(앰버)·둘 다(반반). **지정된 칸을 누를 때마다 발송휴무 → +도착불가 → 도착불가만 → 해제** 순환(`PATCH /shipping-holidays/:id` 신설 — 삭제 후 재등록 불필요). 폭은 780px 중앙 고정 → **카드 틀 전체**(1368px 실측).
  - 🚨 사유 표시를 6자→18자로 늘리며 grid 칸이 밀려 가로 넘침이 났다 → `minmax(0,1fr)`+`min-width:0`+2줄 말줄임(디자인 가이드 §6).
- **8월 예상값 세팅**(대표 확정 사실 기준·화면에서 수정 가능): 8/13 발송휴무·**도착가능** / 8/14~16 발송휴무·도착불가 / **8/17 발송 O·도착불가**(대체공휴일 — 우리는 발송, 택배사 배달은 없을 것으로 예상) / 시드 공휴일 17건 도착불가(종전 동작 유지).
- 검증: **주문안내 무회귀 = 종전 코드(HEAD)와 2026 전 기간 × 3시각 1,095건 전수 대조 → 설명 안 되는 차이 0건**(변경 6건 전부 8/13~17 관련 = 의도된 변경). 발송안내 365일 대조 차이 125건은 **전수 확인 결과 전부 종전이 "내일/모레"를 잘못 붙이던 케이스**. 실렌더 13/13(실제 탭 클릭·칸 상태·순환 클릭·폭·에러 0) · 실API `/api/public/shipping-eta` = `내일 화요일 오전 발송, 수~목 도착 예정`(대표 확정 사실과 일치).
- ℹ️ 판매현황 탭의 가로 넘침 258px은 **달력을 숨겨도 동일** = 이번 변경과 무관한 기존 문제(범위 밖·미조치).
## v5.9.251 (2026-08-10)
- 🐛 **[지급완료] 2차 오류 교정** — `inconsistent types deduced for parameter $2`. 같은 `$2`를 `status=$2`(컬럼 타입 추론)와 `CASE WHEN $2='granted'`(리터럴 비교)에 함께 써서 PostgreSQL이 타입을 못 정했다 → **`$2::text` 명시 캐스팅**.
- 🚨 **자책(검증 규약 위반)**: 1차 검증에서 **라우트의 실제 SQL이 아니라 내가 새로 쓴 SQL**로 왕복 테스트를 해 이 오류를 못 잡았다. CLAUDE.md 규약대로 **server.js에서 SQL 문자열을 그대로 추출해 실DB에 실행**하도록 교정 — 지급완료·되돌리기·원상복구 **3/3**.

## v5.9.250 (2026-08-10)
- 🛠 **[자사몰 세션] #342·#343(대표): 당첨 지급 탭 재작업 + 룰렛 티켓 기준일 8월 시작**
- 🔴 **[지급완료] 오류 원인 = 내 호출 실수**: `api(url, method, body)`는 **위치 인자**인데 `api(url, {method,body})` 옵션 객체로 넘겨 method가 객체가 됐다. → 위치 인자로 교정. (같은 실수 방지 주석 삽입)
- **화면 전면 재작업 — [톡톡 문의] 탭과 같은 규격으로 통일**: `card-header-row` 제목 + 🔄새로고침 · `inq-subtab-bar` 서브탭(⏳미지급 / 📋전체 이력) · 통계 pill · **기간 select(1·3·6개월·1년·전체) + `akm-date` 날짜 + 검색(회원ID·경품)** · `.data-table` 표 + `table-scroll-wrapper`.
  - 날짜 대조는 **KST 기준**(`aoKstDate`) — `toISOString()`으로 비교하면 하루 밀리는 함정(#179 기록) 회피.
- 🎟 **룰렛 티켓 = 2026-08-01 이후 배송완료건만**(대표 확정 "자사몰 리뉴얼 후부터"). 종전엔 최근 180일을 세어 **리뉴얼 전 주문까지 소급 지급**됐다 — 실측: 회원 `4790625123@k`가 4~7월 배송완료 7건으로 **티켓 7장**을 받아 3회 사용(10% 쿠폰 당첨).
  - 기준일은 코드에 박지 않고 **DB 설정 `mall_ticket_since`**(기본 `2026-08-01`·5분 캐시). 구간 계산 시 기준일 이전 구간은 건너뛰고, 걸친 구간은 기준일로 잘라 조회.
  - **이미 받은 경품은 회수하지 않는다.** 앞으로의 티켓만 새 기준으로 산정.
  - 실행 판정: 오늘 기준 구간0 `2026-08-01~08-10`만 조회 · 구간1(2~5월) 건너뜀 ✅
- 🔎 **#341 동반**: raw 러너에 **GET `/api/v2/admin/orders` 허용**(티켓 근거 추적용·읽기 전용). PUT/POST/DELETE·customers는 차단 — 가드 실행 판정 7/7. 🔴 호출 시 `fields`로 이름·연락처·주소 제외, 조회 결과 행은 즉시 삭제.

## v5.9.248 (2026-08-10)
- 🎁 **[자사몰 세션] #340(대표 실사고): 문의 관리 > 「🎁 당첨 지급」 탭 신설 + 텔레그램 회원ID 마스킹 교정**
- 🔴 **사고 내용**: 룰렛 실물 당첨(쿠폰·귤박스·업그레이드권) 알림이 `🎡 [룰렛 실물 당첨] 회원 ●●●@k — 쿠폰 10%` 로 와서 **대표가 누구에게 쿠폰을 줘야 하는지 알 수 없었다.** 원인 2겹 —
  - ⓐ `tgMask`의 「5자리 이상 숫자는 금액이니 가림」 규칙이 **회원ID(`4790625123@k`)까지 삼켰다.**
  - ⓑ 알림은 "데이터관리 게임 운영 참조"라고 안내하는데 **그 화면에 당첨자 목록이 아예 없었다**(`reward_grants`에 UI 0). 즉 확인할 통로가 하나도 없었다.
- **탭 신설**(고객문의 옆): 미지급 건 목록 — 당첨 일시·상태·경품·**회원ID(+이름)**·지급 처리자 + **[지급완료]/[되돌리기]**. 미지급 건수는 탭 배지로 표시. 직원도 조회 가능(문의 관리 공용 원칙), 되돌리기 허용(오클릭 구제), 물리삭제 없음·감사 로그 기록. `reward_grants`에 `granted_at`·`granted_by` 컬럼 추가.
- **마스킹 교정**: 숫자가 **글자·@·.·_·- 또는 다른 숫자에 붙어 있으면 식별자**로 보고 가리지 않는다. 금액(`19800원`·`1,234,567원`)·전화(`01012345678`)·주문번호는 종전대로 가려진다.
  - ⚠️ 1차 정규식은 숫자열의 **일부만** 잘라 매칭해 `●●●3@k`가 됐다(경계 제외 문자에 숫자를 안 넣어서) → 경계에 `0-9` 포함해 교정.
- **알림 문구 보강**: 경품 · **회원ID(이름)** · 미지급 누적 건수 · 「카페24에서 발급 후 문의 관리 > 🎁 당첨 지급에서 [지급완료]」 처리 안내.
- 검증 **정적 18/18**(마크업 위치·탭 전환 배선·**사용한 공용 함수 실존 확인** — `aoToast`·`pill-warn`이 존재하지 않아 `showToast`·`pill-wait`로 교정. #192 "조용히 죽는 화면" 재발 방지) + **마스킹 실행 판정 8/8**(파일에서 추출한 실코드로 실행).
- ℹ️ 현재 미지급 1건 = 회원 `4790625123@k` · 10% 할인 쿠폰(2026-08-10 02:54) — **대표가 직접 지급하기로 확정**.

## v5.9.247 (2026-08-10)
- 🧮 **[자사몰 세션] #338(대표 GO): 일일 스냅샷에 네이버 옵션까지 수집 → 카페24 기준값(minAdd) 자동 갱신** — 새 타이머·새 시스템 없음. 기존 04:30 수집 / 05:10 반영 파이프라인에 **재료(옵션)만 추가**.
- **왜**: 카페24 기본가 공식은 「네이버 최종가 + min(옵션 증감액)」인데, 그 min값을 **7/30 시점으로 고정 저장**해 쓰고 있었다. 옵션이 바뀌면 낡은 값으로 계산된다 — 2026-08-10 실측: 밤호박 특품 종료로 min이 −11,000 → −5,000이 됐는데 방치했으면 **다음날 05:10 fix가 14,800원을 8,800원으로 내릴 뻔**(±50% 가드에도 안 걸림 −40.5%).
- **수집기**: 목록 API 응답에 옵션이 없어(실측) **상품별 단건 조회 `GET /external/v2/products/origin-products/{no}`** 추가. 20종 = 하루 20콜·400ms 간격(+8초). `usable` 포함 저장, 옵션 없는 단일 상품은 `optsNone` 표식.
- **동기화**: 저장된 minAdd 대신 **그날 스냅샷 옵션에서 계산**(종료된 옵션 `usable:false`는 기준에서 제외). 값이 바뀌면 매핑표도 최신값으로 갱신하고 **텔레그램에 「기준값 자동 갱신 c91 −11000→−5000」** 보고.
- 🔴 **안전장치**: 옵션을 못 받은 상품은 **그 상품의 가격 보정을 통째로 건너뛴다**(`optSkip`) — 낡은 재료로 가격을 건드리는 경로 자체를 제거. 종전에는 재료가 낡아도 그냥 계산했다.
- **보관 정리**: `naver_product_snapshot`에 자동 삭제가 없어 무한 증가하던 것 → **90일 경과분 삭제**(실측 현황 488KB·23행, 옵션 추가분 하루 ~13.6KB).
- 검증 **15/15**(`scratchpad/verify-338-logic.js` — **실 server.js에서 기준값 계산 블록(1,025b)을 추출해 그대로 실행**): 옵션 6종 → −5000 산출·기본가 14,800 일치 · 미수집 시 건너뜀 · 단일 상품 0 · 종료 옵션 기준 제외 · 전멸 폴백 · 변화 없으면 무알림(소음 방지) + 삽입 정규식 7종.
- ℹ️ **카페24 API 한계(테스트 상품으로 실측·삭제 완료)**: 옵션 **이름변경·추가·전면교체 전부 거부**(`Existing option value is missing` / `사용 중인 옵션은 재등록 불가`). 따라서 **옵션 구성 변경은 자동화 불가** — 종료분 미노출·가격·추가금까지만 자동이고, 신규 옵션은 알림 후 관리자에서 사람이 추가해야 한다.

## v5.9.245 (2026-08-10)
- 🚚 **[자사몰 세션] 발송 안내 도착 표기 교정 (대표 실물 "수~월 도착으로 뜬다")** — ⚠️ 번호 주의: 같은 날 병행 세션이 #333·#334를 다른 건(시간대·브리핑)에 썼다. 이 건은 **자사몰 스킨 세션의 발송 문구 건**이다.
- **결함 아님 — 휴무일 등록이 반영된 결과였다.** DB 실조회: `shipping_holidays`에 **8/13~8/16이 「택배사 택배 없는날 및 공휴일(광복절) 휴무」로 등록**돼 있어, 8/11(화) 발송 기준 배달 가능일 두 후보가 **8/12(수)와 8/17(월)** 로 벌어졌다. 즉 `computeShipping`은 이미 회사프로그램 휴무일 관리 기준으로 정상 동작 중이었고, 알림톡과 자사몰이 같은 함수를 쓰므로 안내도 이미 일치 상태였다.
- 🚨 **조회 자책(정정 기록)**: 1차 조회에서 **`WHERE deleted_at IS NULL`을 빠뜨려** 이미 삭제된 8/17·8/27·8/7까지 유효 휴무로 셌다. 그 탓에 계산이 `수~화(8/18)`로 나왔고 **대표에게 "8/17 임시휴무가 걸려 있으니 확인해달라"고 잘못 보고**했다(대표 화면 캡처로 즉시 정정 — 8/17은 이미 삭제 상태·발송 가능). **`shipping_holidays`는 soft-delete 테이블이다 — 조회 시 `deleted_at IS NULL`을 서버(`loadShippingHolidayInfo`)와 동일하게 걸 것.** 아무 것도 삭제하지 않았다.
- **교정한 것 = 표기**: 종전엔 두 후보를 **무조건 범위로** 붙여 `수~화`처럼 벌어졌고, 손님에게 "도착까지 일주일"로 읽혔다.
  - 도착 후보가 **연속일 때만** 범위(`수~목`) — 평상 구간은 종전과 완전 동일(무회귀)
  - 사이에 휴무가 끼면 **가장 빠른 도착일 하나만**(`수요일 도착 예정`)
  - 첫 도착일이 발송 다음날이 아니면(연휴로 밀림) **날짜 병기**(`화(8/18)~수 도착 예정`) — 요일만 보고 오해하지 않게
- 적용 범위: `shipping-schedule.js`의 `computeShipping().text` 한 곳 → **알림톡 주문안내·자사몰 상단 배너·보류 수기 발송 전부 자동 동일 적용**(#185 원칙 — 자사몰이 자체 계산하지 않고 이 API 결과를 그대로 쓴다).
- 검증 **14/14**(`scratchpad/verify-335-eta.js` — **실DB 휴무일 24건·안내문 6건 주입**): 연휴 구간 3케이스에서 벌어진 범위 소멸 · 평상 4케이스에서 "연속 여부 ↔ 범위표기" 일치 · 먼 도착일 날짜 병기 · 휴무 안내문 대체(#73) · 8시 컷오프(#171) · 토요일 발송 불가(#69) 무회귀.
- 🚨 **검증 자책**: 1차 테스트에서 기대 문자열(`수~목`)을 손으로 적었다가 요일을 잘못 짚어 **거짓 실패 3건**이 났다. 기대값을 적지 말고 **실제 날짜로 조건을 판정**하도록 교정.
- ℹ️ 대표가 기대한 「수~목」은 **8/13(목)이 휴무로 등록돼 있어 나올 수 없는 값**이다(그 등록은 실제와 맞다 — 대표 확인). 대표 확정: **8/17부터 발송 가능·8/13~16만 휴무** = 현재 DB 그대로. 「수요일 하나만 표기」도 대표 승인.

## v5.9.244 (2026-08-10)
- 📨 **#334(대표): 야간 알림톡 실패·보류·차단도 아침 브리핑에 표시** — 종전엔 야간 누적이 **성공 건수만**(`alertNightAcc('kakaosend', sent)`) 이라 밤사이 발송 실패가 아침에 아예 안 보였다(21:30~07:30 = 하루 10시간이 사각지대였음).
- 브리핑 줄 신설: `📨 알림톡 ❌ 실패 3건 · ⏸ 보류 2건 · 🚫 번호없음 1건 — [알림 발송 이력]에서 확인·재발송` + **실패 사유 상세 최대 5건**(수신번호 마스킹·에러 80자).
- 구현: `collectOrderNew`의 실패/보류/차단 값을 try 블록 지역변수 → **함수 스코프로 승격**, 야간이면 `kakaosend_fail`/`_hold`/`_block` 카운터 누적 + 실패 사유는 `alert_night_kakao_fail`에 보관. 브리핑 발송 시·브리핑 OFF 시 **양쪽 리셋 지점 모두**에서 함께 비움(무한 누적 방지).
- 🔵 **카운터 누적 방식을 고른 이유**: 야간 시작 시각을 JS Date로 만들어 SQL에 넘기면 #333에서 데인 시간대 함정(timestamp WITHOUT tz)을 다시 밟는다. 카운터는 시간대 변환이 개입하지 않는다.
- 무회귀: 성공 건수만 있을 때 문구 종전과 동일 · 전부 0이면 줄 생략 · `kakaosend` 토글 OFF면 미표시.
- 검증 16/16 — **실 server.js에서 브리핑 블록(903자)을 중괄호 매칭으로 추출해 그대로 실행**(하네스 재현 아님): 성공만/실패포함/보류·차단/성공0+실패만/전부0/토글OFF 6케이스 + 누적·리셋 지점 7곳 실삽입 정규식 확인.
- ⏱️ 동반: 신규주문 알림 문구 「최근 1시간 주문」 → **「직전 알림 이후」**(주기 5시간 전환으로 사실과 어긋나 교정 — DB 템플릿·화면에서 편집 가능).

## v5.9.243 (2026-08-10)
- 🔴 **#333 후속 — 경과시간 계산을 JS에서 DB로 이관(잠재 결함 제거)**: v5.9.242의 `new Date(last_run_at)` 방식은 `last_run_at`이 **timestamp WITHOUT time zone**이라 **Node 프로세스 시간대로 해석**된다. Render는 UTC라 지금은 맞게 돌지만, 시간대가 다른 환경에서는 9시간 어긋나 집계 창이 클램프 상한(주기 2배)으로 튄다.
- → `ROUND(EXTRACT(EPOCH FROM (NOW() - last_run_at))/60)::int`로 **DB가 직접 계산**하도록 교체. 프로세스 시간대 의존 0.
- 실증(이 PC=KST): 같은 행에 대해 **옛 JS 계산 566분 vs DB 계산 26분 — 정확히 540분(9시간) 차이**. 검증 6/6.
- 🚨 **자책**: 이 함정을 내가 검증 스크립트에서 그대로 밟아 "타이머가 9시간 멈췄다"고 오진했다(실제로는 전 타이머 정상 — 알림톡도 8분 간격으로 계속 발송 중이었음). CLAUDE.md에 이미 "날짜 대조는 DB 쪽에서 통일할 것"이라 적어둔 함정의 재범. **DB 시각끼리 빼는 계산은 예외 없이 SQL 안에서 할 것.**

## v5.9.242 (2026-08-10)
- ⏱️ **#333(대표): 「주문(신규 알림)」 타이머 주기 60분 → 300분(5시간)** — 텔레그램 신규주문 알림과 알림톡 발송 결과 요약이 **5시간에 한 통**으로 온다. 주기 값은 DB(`naver_auto_collect.order.interval_min`)에 저장 — 화면에서 언제든 재조정 가능.
- 🔴 **동반 필수 교정 — 알림톡 요약 집계 창이 `interval '60 minutes'`로 하드코딩되어 있었음**: 주기만 늘리면 5시간 중 **마지막 1시간분만** 집계돼 숫자가 1/5로 보이고 그 앞 4시간의 실패 건은 통째로 사라진다(실측: 같은 시점 60분 창 4건 vs 300분 창 24건 — **20건 누락**). → 집계 기준을 **직전 실행 시각(`last_run_at`) 이후 전부**로 교체. 주기를 몇 분으로 바꾸든 누락·중복 0.
- 고정 창이 아니라 직전 실행 기준인 이유: 타이머 틱이 60초 단위라 실제 실행 간격이 주기보다 최대 1분 길다 → 창을 주기와 똑같이 고정하면 매 회차 1분치가 새어나간다.
- 타이머를 껐다 켜거나 장기 정지 후 재개한 경우 과거분이 통째로 딸려오지 않도록 **주기의 2배로 클램프**. 알림 문구도 실제 집계 구간을 따라 「알림톡(5시간)」처럼 자동 표기(60분 미만이면 분 단위).
- 실발송은 무관 — 손님에게 나가는 알림톡은 별도 타이머(`kakao_notify` 3분)가 그대로 담당한다. 이번 변경은 **대표 폰으로 오는 보고 주기**만 바꾼다.
- 검증 8/8(실DB): `make_interval(mins => $1)` placeholder 실행 판정 2종 + 실패목록 쿼리 · 라벨 계산(300→5시간·60→1시간·30→31분) · 장기정지 클램프 · last_run_at 없음 폴백 · 저장 후 재조회 300 확인.

## v5.9.241 (2026-08-09)
- 🚁 **#324: 히어로 1번 배너 = 드론 영상(신규)** — 원본 「제주아꼼이네 드론_스마트스토어.mp4」(52초·1280×720·오디오 있음)에서 **25~30초 5초 컷**을 뽑아 가공. 오디오 제거·`+faststart`·**mp4 1.59MB / webm 1.62MB**(둘 다 2MB 이하)·`public/media/akkome_banner_drone_v1.{mp4,webm}`.
- 구간 선택 근거: 0~20초는 전깃줄·주차장이 섞여 산만, **25~30초는 귤밭 위 드론 + 사다리 위 농부**라 문구(「직접 키우고, 직접 보냅니다」)와 정확히 맞음(구간 썸네일 격자로 대조).
- 문구 「제주에서 우리집까지 / 아꼼이네가 직접 키우고, 직접 보냅니다」 · 순서 **드론 → 귤나무 → 나침반** · **오션라운지 배너 제거**(마크업·이미지는 보존, `display:none` 한 줄로 복구 가능).
- ffmpeg는 시스템에 없어 `ffmpeg-static`(프로젝트 로컬)로 확보 — 시스템 변경 없음.

## v5.9.240 (2026-08-09)
- 🎬 **#323: 「LUCKY 나침반」 배너도 동영상으로** — `public/media/banner_compass.mp4`(6.12MB) 추가. 귤나무(#319)와 동일 구조·규격.
- 문구 「LUCKY 나침반 / 꽝 없이 100% 당첨! 매일매일 참여해보세요」를 영상 위에 얹음. poster는 넣지 않음(#322 지적 반영 — 옛 이미지 깜빡임 방지), 로딩 배경은 영상 톤(연분홍)으로.
- ⚠️ 영상 배너가 2장이 되어 히어로 순서 셀렉터를 `.hs-video` → **링크 기준**(`a[href^="farm"]` 등)으로 교정(안 그러면 둘 다 1번이 된다).

## v5.9.239 (2026-08-09)
- 🎬 **#319: 자사몰 히어로 「귤나무 키우기」 배너를 대표 제공 동영상으로 교체** — `public/media/banner_farm.mp4`(4.58MB·H.264) 정적 서빙 추가.
- 🔴 **카페24는 `.mp4` 업로드를 차단한다**(assets2에 1KB 테스트 파일도 `Permission denied` 실측 · `.txt`·이미지는 정상). → 영상만 **회사프로그램(Render)에서 서빙**하고 스킨은 그 URL을 참조한다.
- 모바일 자동재생 3종(muted·playsinline·autoplay) + loop. 재생 불가 환경에서는 기존 이미지가 poster로 그대로 보이도록 폴백.

## v5.9.238 (2026-08-09)
- 🚨 **#312-b 자책 교정 3건 — 대표 지적("상세엔 후기 보이는데?")으로 드러난 내 시공 결함.**
- ① **필드명 불일치**: 화면 정본 스키마는 `text`인데 새 수집 경로가 `content`로 저장 → **상품 상세 후기 본문이 undefined로 렌더될 상태**였다(대표가 본 정상 화면은 교체 직전의 구 데이터). 서버 정규화에서 `text` 보정 + 중계서버도 `text`로 반환하도록 교정.
- ② **홈 후기 섹션은 여전히 7/30 정적본**: 홈은 `D.reviews`(정적)를 읽고, 새 수집분은 `D.prodReviews`(상세 전용)로만 들어가고 있었다. 그런데 **기준일 표기만 「리뷰 8.9 기준」으로 바뀌어 거짓 표기**가 됨. → 최신 수집분이 유효할 때 홈 후기 소스도 함께 교체(본문이 비어 있으면 교체하지 않음).
- ③ **labels(BEST·재구매) 누락**: 롤링 15건 「엄선」이 BEST(0) > 재구매(1) > 일반(2) 우선순위로 뽑는데 labels를 안 가져와 **전부 일반 취급 = 엄선 무력화** + 뱃지 소멸. 중계서버가 labels를 함께 반환하도록 교정(반영엔 install.sh 재실행 1회 필요).
- 🔴 **교훈: 데이터 경로를 새로 만들 땐 "화면이 실제로 읽는 필드명·소스"를 먼저 확인할 것.** 수집 성공(400건)만 보고 완료로 보고했는데, 화면이 그걸 안 읽고 있었다.

## v5.9.237 (2026-08-09)
- 🟢 **#312: 리뷰 본문 갱신 재개** — #234·#247에서 6일 연속 429로 실패해 꺼둔 리뷰 수집을, **중계서버 브라우저 안에서 리뷰 API를 호출**하는 방식으로 되살림(같은 출처·쿠키·지문을 갖춘 상태에서는 통과). 실패 시 종전대로 직전분 유지. 구 직접 호출 경로는 주석과 함께 보존.
  - ⚠️ 함정: 구 경로의 catch가 **새로 받아온 리뷰를 직전분으로 덮어쓰는** 구조였음 → `if (!reviews)` 가드 추가.
- ⭐ **#312: 「스토어 평점」 = 판매 상품 전체의 리뷰 수 가중평균**(대표 확정). 종전 4.76은 사실 **감귤 한 상품의 평점**이었다(네이버가 스토어 전체 평점을 공개 화면에 내보내지 않음). 상품별 평점은 네이버와 동일한 실측치이므로 가중평균이 근거가 명확하다 — 현재 **4.77**. 상품 평점·리뷰수가 갱신되면 함께 움직인다.
- 중계서버 RELAY_VERSION 2026-08-09.3 (리뷰 수집 action 추가 — 반영에 install.sh 재실행 1회 필요).

## v5.9.236 (2026-08-09)
- **#310-c: 첫 실수집 검산에서 네이버 표기 한계 2가지 발견 → 신뢰 가능한 값만 채택하도록 교정.**
- ① **리뷰수 상한 표기**: 리뷰가 아주 많은 상품은 상품 페이지에 **「리뷰 99,999+」**로 뜬다(감귤 실측 — 실제 136,029인데 99999로 수집돼 **누적 리뷰 합계가 229,561 → 193,910으로 줄어 보였다**). → 상한값·역행값은 버리고 **직전 스냅샷 값 유지**.
- ② **평점은 정수 반올림**: 상품 페이지의 「평점 N」이 전 상품 4 또는 5로만 수집돼 가중평균이 **4.92**라는 엉뚱한 값이 나왔다. → **평점은 채택하지 않음**(정확한 소스 확보 전까지 화면은 기존 값 유지). 스토어 홈 텍스트의 "평점 4.78"도 실은 **상품 카드 값**이었음이 판명돼 정적값을 4.76으로 되돌림.
- 🔴 **교훈: 수집한 숫자는 반드시 직전값과 대조해 검산할 것.** "리뷰 합계가 줄었다"는 물리적으로 불가능한 신호가 결함을 잡아냈다. 관심고객수(190,330)는 정확히 수집됨.

## v5.9.235 (2026-08-09)
- **#310: 네이버 공개 지표(관심고객수·상품별 리뷰수/평점) 매일 자동 갱신** — 04:30 상품 스냅샷 수집기에 결합.
- 🔴 **6일간의 "네이버 원천 봉쇄" 진단이 반쪽이었음**: 같은 IP에서 **일반 요청 = 429 / 실제 브라우저 = 200** 실측 → IP 차단이 아니라 **봇 지문 차단**. 중계서버(NCP 고정 IP)에 헤드리스 브라우저를 두고 **공개 화면만** 열어 숫자를 읽는다(로그인·개인정보 무관). 중계서버 `POST /naver-public`(RELAY_VERSION 2026-08-09.2) + `naver-relay.callNaverPublic()`.
- ⚠️ **스토어 홈에는 '스토어 전체 평점'이 텍스트로 없다** — 문서 전체에서 정규식으로 찾으면 「현재 판매 순위」 **상품 카드 숫자**가 잡힌다(대표 실행에서 리뷰 12,657 = 청귤 1개 값으로 오인식 실측). → 관심고객수만 스토어 홈에서 읽고, **평점·누적 리뷰는 상품별 실값에서 산출**(합계 = Σ리뷰수, 평점 = 리뷰수 가중평균). 근거가 명확한 값만 화면에 올린다.
- 스냅샷 테이블에 `store_meta` 컬럼 추가, `/api/public/store-snapshot` 응답에 `store` 포함. 자사몰 스킨은 관심고객수·평점·상품별 리뷰수를 이 실값으로 교체(없으면 정적 원본 유지 = 무회귀).
- 지표 수집이 실패해도 **가격·재고 스냅샷은 정상 저장**되고 지표는 직전분을 유지한다(사유는 note 기록).

## v5.9.234 (2026-08-09)
- #298-b: 봇 미답변(SKIP) 알림을 **직원 답변이 필요한 건만** 선별 — 실측 최근 미답변이 "감사합니다 시원하고 행복한날되세요~" 같은 인사·덕담이라 그대로 두면 회사폰이 계속 울림. 질문·요청·불만 신호가 있는 건만 알림(알려야 6건·조용해야 7건 **13/13 통과**).
- 🚨 자책: 이 버전 배포 시 PowerShell `Set-Content`로 CHANGELOG.md·version.js를 쓰다 **한글이 전부 깨짐**(CP949 오독 + BOM). git에서 복구·재커밋. **CLAUDE.md에 이미 적어둔 함정을 다시 밟았다 — 파일 쓰기는 예외 없이 Write/Edit 도구 또는 git bash 리다이렉션으로.**

## v5.9.233 (2026-08-09)
- 대표 실물: **카카오 채널 문의에 알림이 안 와 직접 처리를 못 하던 문제** 교정. 카카오 「챗봇 채팅」은 **구조상 카카오 앱 상담 목록·푸시 알림 대상이 아니므로**, 텔레그램 알림이 유일한 실시간 통로.
- **직원 확인 필요 알림 2종으로 확대**: ①요청형(변경·취소·반품 등) ②**AI가 답하지 못한 문의(`[SKIP-무응답]`)** — 봇이 침묵한 건은 정의상 직원이 봐야 하는 건인데 그동안 아무 신호가 없었음. 알림에 **채널 표시**(톡톡/카카오/자사몰)와 `·봇 미답변` 표기 추가.
- **취소·반품 감지 정규식 보강**: "취소**가능할까요**·취소 되나요·취소 하려면", "반품/환불/교환 가능·되나요" 형태가 미감지였음(대표 실제 문장 "…취소가능할까요?!" 실측 미감지 확인). 감지 10건·오탐 금지 7건 **17/17 통과**.
- 알림 폴러 주기 **3분 → 1분**(카카오 실시간성).

## v5.9.232 (2026-08-08)
- 대표 실물 지적: 문의 표 **「일시」가 잘려 분이 안 보이던 문제** 교정. 원인 = `toLocaleString('ko-KR')` 표기가 "08. 08. 오후 01:23"(17자)로 길어 좁은 열을 넘침 + 기본 셀 패딩(좌우 16px)이 글자 자리를 잠식. → **공용 짧은 표기 `aoFmtDtShort` 신설("08-08 13:23" · 24시간 · tabular-nums)** 로 3개 문의 표(톡톡·상품문의·고객문의) 통일 + 일시/채널 열 전용 패딩(`.cell-dt`·`.cell-ch`)·열 폭 재배분(일시 92→100px).
- 실측(실 styles.css 렌더 대조): 폰(iPhone 13)·PC(1440) 모두 일시 글자폭 67.6px / 가용 82px, 채널 배지 68.9px / 가용 76px — **넘침 0**.

## v5.9.231 (2026-08-08)
- **룰렛 이용권 = 배송완료(N40) 1건당 1회**(대표 확정) — 종전 "주문 즉시 1회"에서 변경. 배송 전·취소·반품 건은 티켓이 생기지 않아 **반품 후 룰렛 사용 사고를 원천 차단**. `countPaidOrders(memberKey, statuses)` 상태 파라미터화 + `countDeliveredOrders` 신설(캐시 키도 상태별 분리). 후기 물방울(리뷰 작성 시 지급)은 현행 유지.
- **시즌 오픈 대기 신청 공개 API 신설** `POST /api/public/season-waitlist` — 자사몰 품절 상품에서 손님이 직접 신청 → 기존 [문의 관리 > 시즌 오픈 대기 신청] 명단에 그대로 적재(created_by='자사몰'). 분당 6회 상한·중복 차단·연락처 숫자 검증·감사 로그.
- 귤나무 수확 알림 문구 = "지금 제일 싱싱한 제철 귤 4.5kg"(시기별 품목 변동 대응 — 대표 지정).

## v5.9.230 (2026-08-08)
- 대표 지시: 문의 관리 표에 **채널 열 신설**(일시 다음) — 「네이버톡톡 / 카카오채널 / 자사몰」 배지. 디자인 가이드 `.pill` 컴포넌트 계승(자사몰=주색상 인디고·카카오=옐로·톡톡=그린). 열 폭 재배분·min-width 940px.
- 대표 지시: 자사몰 챗 연속 대화 안내 **6턴 → 3턴**, 문구 = "더 궁금하신 점은 카카오톡 채널 「제주아꼼이네」 또는 고객센터 010-6687-4031로 연락주세요 🍊".
- PC 화면 2차 교정: 카페24 PC CSS가 **뷰포트 폭 기준**이라 body를 480px로 줄여도 장바구니 2단(상품목록/주문정보)이 발동해 압착·세로 깨짐(실측 .cart-product 176px) → 데스크톱에서도 1단 유지 규칙 추가.

## v5.9.229 (2026-08-08)
- 문의 관리 > 톡톡 문의 표에 **채널 배지**(톡톡·카카오·자사몰) 추가 — 3채널이 한 표에 모이므로 출처를 한눈에 구분(user_id 프리픽스 판별·표시 계층만).

## v5.9.228 (2026-08-08)
- **자사몰 AI챗 실가동(대표 지시 ①)** — 3채널 통합 완성(톡톡·카카오·자사몰이 같은 시나리오·시기별 지식·qnaGenerate 원천).
  - `/api/public/shop-chat`에 **대화 로그 기록 추가**: message_logs에 `mall:` 프리픽스(톡톡=없음·카카오=`kakao:`) — 문의 관리 화면에서 3채널 함께 확인. 시나리오 번호(scenario_nos)·출처(ai)도 함께 기록.
  - **라임 설정 공유**: `agent_office_config.bot_timing`의 `mall_buffer_sec`/`mall_cooldown_min`로 조절(미설정 시 자사몰 기본 = 즉답·연속답변 — 웹챗은 손님이 화면 앞에 있고 직원 인계 화면이 없어 침묵 시 방치되므로). 톡톡과 완전 동일을 원하면 30/30 입력만으로 적용.
  - 연속 6턴 초과 시 카카오채널 안내 자동 부가(직원 응대 체계가 있는 채널로 유도). 손님 식별 = 로그인 회원 아이디(릴레이) 또는 브라우저 세션키.

## v5.9.227 (2026-08-07)
- 러너 raw 가드에 boards **GET 전용** 허용 — 재동의(community) 반영 후 후기 게시판(board 4) 읽기 실측용(#259). 쓰기는 계속 차단.

## v5.9.226 (2026-08-07)
- 카페24 SCOPE에 `mall.read_category`·`mall.write_category` 추가 — **mains(메인 진열)의 요구 스코프는 store가 아니라 category**(재동의 후에도 403 지속 → 공식 문서로 확정). 대표 재동의 1회 더 필요.

## v5.9.225 (2026-08-07)
- 카페24 재동의 반영 진단: 러너 `status` 액션 신설 — 토큰 상태·만료시각·**승인 스코프 목록**(메타데이터만·토큰 값 미노출) + 러너 결과에 `server_version` 동봉(Render 실배포 버전 판별 — 8/7 store 403이 "재동의 미완"인지 "구버전 배포"인지 가르기 위함).

## v5.9.215 (2026-08-07)
- 대표 지시: 타이머 라벨 정리 — 알림톡·문자 안내 "켜도 dry-run만"(발사 전 낡은 문구) → "🚀 실발송 가동 중", 상품 스냅샷 "권한 필요" 제거. cafe24_sync 한글 라벨은 v5.9.214 기반영(브라우저 캐시 갱신 필요였음).

## v5.9.224 (2026-08-07)
- 카페24 SCOPE에 `mall.write_community` 추가(대표 승인) — 향후 자사몰 게시판 자동답변용 선확보(현재 쓰기 호출 코드 없음 — 위험 0). 재동의 1회에 read/write 함께 담김.

## v5.9.223 (2026-08-07)
- 게임 설정 리셋 재발 차단: 게임 운영 카드 저장 시 화면 미전송 필드가 코드 기본값으로 리셋되는 구조 → **기본값을 정책 확정값으로 교정**(water_cost 10→1·level_thresholds → [10,50,100] — 물 100=수확). DB 설정도 재교정 완료(2회 되돌아간 원인).

## v5.9.222 (2026-08-07)
- 지시 #259(대표): 후기 물방울 = **실제 후기 작성 확인 후 지급** — 서버가 카페24 상품후기 게시판(board 4)에서 그 회원의 후기 수를 대조(사진 무관·주문 1건당 1회 유지). scope `mall.read_community` 추가(재동의 전엔 검증 생략 폴백 — 주문 상한만). 미작성 시 안내 "후기를 먼저 작성해 주세요 🍊".

## v5.9.221 (2026-08-07)
- 핫픽스: v5.9.220 기동 실패(TDZ — const cafe24가 mall 마운트보다 뒤에 선언) → 지연 참조로 교정. Render는 실패 시 구버전 유지라 무중단이었음.

## v5.9.220 (2026-08-07)
- 지시 #256 (대표 (a)안 승인): **자사몰 게임 공개 API 결선** — mall-api에 /pub/* 6종(wallet·join-bonus·attend·review-claim·tree-water·spin). 신뢰 = 카페24 실주문 대사(orders/count 최근 180일·배송 계열) — 티켓 = 주문 수 − 사용 수·후기 지급 상한 = 주문 수(대표: 사진·중복 무관·주문당 1회). 게이트 = DB 스위치 mall_pub(무토큰·CORS 화이트리스트·레이트리밋). 룰렛 1일1회 UNIQUE 제거(주문당 1회 정책 — 구 라우트는 사전 SELECT로 호환). 실물 당첨(쿠폰·박스·업그레이드)=pending 기록+텔레그램(수동 발급 — 쿠폰 스코프 없음). 수확(물 100) 시 텔레그램.

## v5.9.219 (2026-08-07)
- 카페24 OAuth SCOPE에 `mall.read_store,mall.write_store` 추가(메인 진열 mains API용) — 대표 절차: 개발자센터 앱 권한에 상점 읽기·쓰기 추가 → [데이터관리] 카페24 카드 재동의 1회. 재동의 전 기존 연결 무영향.

## v5.9.218 (2026-08-07)
- bulk-image 스키마 교정(422 실측 "image_upload_type required"): ①상품 PUT에 base64 직접(image_upload_type 'A') ②폴백 images POST(같은 필드)+경로 지정.

## v5.9.217 (2026-08-07)
- 대표 지시(8/7 — 오픈 전환 마감 3종): ⓐ**bulk-detail 스타일 개선** — 텍스트 블록을 v5 렌더러(.pd-txt) 규칙의 스코프 스타일(.akm-det — 줄간격 1.8·문단 여백·keep-all·중앙정렬)로 동봉(간격 좁음 해소) ⓑ**bulk-image 액션 신설** — 네이버 스냅샷 대표 이미지 → base64 업로드 → `image_upload_type:'A'` 대표 지정(목록 이미지 공백 해소·업로드 경로 2중 폴백) ⓒ raw 가드에 `mains`(메인 진열) 경로 허용(DELETE는 mains 한정).

## v5.9.216 (2026-08-07)
- 지시 #246 오픈 전환(대표 GO): 러너 raw 가드에 `categories/{no}/products` 경로 추가 허용(POST/PUT — 분류 배정은 products PUT으로 불가·공식 문서 확인. DELETE 금지 유지). 신규 세트 분류 미배정 → 스토어프론트 404 교정용.

## v5.9.214 (2026-08-07)
- 지시 #246 판정2 (설계 승인분): **카페24 신규 세트 동기화 파이프라인** — 수집기 `cafe24_sync`(기본 OFF·05:10 앵커, 04:30 네이버 스냅샷 직후). 기본가 = 네이버 할인가+min(옵션 증감액)(#248 등록식 — 실등록가 19/19 검산 통과)·품절 대조. 가드: ①기본 dry-run(`cafe24_sync_mode`='fix'로만 실보정 — 대표 GO) ②±50% 초과 변동 자동 보정 금지·알림만 ③차이·보정 시 텔레그램 보고 ④품절 동기는 진열중(display=T)만(오픈 전 세트 무조작). 매핑 = `cafe24_sync_map` 설정. 러너 `{action:'sync'}` 수동 1회 실행 지원.
- 대표 지시(8/7): 카페24 러너(`cafe24_product_request`) 폴러 주기 60초→10초 — 응답 대기 단축(플래그 없으면 SELECT 1회뿐).

## v5.9.213 (2026-08-07)
- 지시 #246(대표 — 신규 세트 = 네이버 100% 완성): **bulk-detail 러너** — 네이버 상세 스냅샷(이미지·텍스트 블록 원순서)을 신규 19종 description으로 이관(PUT). 이미지 = CDN 참조 + referrerpolicy=no-referrer(skin6 검증 방식).

## v5.9.212 (2026-08-07)
- 지시 #248-③: **bulk-create 러너** — 네이버 정본(옵션 텍스트·추가금 포함)을 카페24 신규 등록(진열·판매 안함). 스키마 = raw 실험 확정분(products/options/variants 3단·텍스트 매칭). 옵션 추가금은 기본가=최저 옵션가 재계산으로 전부 양수화(최종 결제가 동일).

## v5.9.211 (2026-08-07)
- 지시 #248-③: 카페24 상품 API — verify body 래퍼 교정(422 실측: request 바로 아래 필드) + raw 액션(스키마 실험용 — products 경로 한정·DELETE 금지). read 확증 완료(재승인 반영), write·옵션 스키마 확정 단계.

## v5.9.210 (2026-08-06)
- 지시 #247-① (대표 확정): **리뷰 일일 갱신 시도 OFF**(REVIEW_FETCH_OFF — 원천 봉쇄 확정·매일 37분 백오프 낭비 제거·기존 직전분 유지 폴백 재사용, 상품 스냅샷은 현행 04:30 유지) + **표기 정직화 재료** — `/api/public/store-snapshot`에 `reviews_fresh_at`(실패·중단 note 없는 마지막 리뷰 보유 행의 run_at — 실제 마지막 성공 수집일) 신설. 종전엔 화면이 스냅샷 시각을 리뷰 기준일로 오표기.

## v5.9.209 (2026-08-06)
- 지시 #248-③ (대표 scope 재동의 완료): **카페24 상품 API 기반 신설** — cafe24.js SCOPE에 read_product·write_product 추가, 쓰기 겸용 `apiReq`(401 갱신·백오프 동일 방어), 러너 `cafe24_product_request`(verify=읽기 1콜+테스트 상품 1건 생성(진열·판매 안함 — 실서비스 노출 0) / delete-test=테스트 상품 정리(이름 가드)). B안(스냅샷 20종 등록) 확증 단계.

## v5.9.208 (2026-08-06)
- 지시 #245-2 (대표 실물 판정): ⓐ**체크 액션 바 스크롤 고정** — 바를 overflow 래퍼(sticky 무효화 원인) 바깥에 렌더 + position:sticky(top 8px) ⓑ버튼 옆 **보류 전용 안내 문구** 상시 표시("일괄 발송은 ⏸ 보류(8~9시) 건에만 적용 — 나머지 자동 건너뜀").

## v5.9.207 (2026-08-06)
- 지시 #245 (대표 확정): **알림 발송 이력 체크바** — ⓐ행 체크박스+전체선택 → 보류(8~9시) 다건 **[오늘/내일 발송으로 안내] 일괄 실행**(보류 아닌 건 자동 건너뜀·700ms 간격·기존 멱등 가드 재사용) ⓑ**선택 삭제** — 취소된 주문 등 조치 불요 건 수기 정리(**soft-delete** `deleted_at` — 물리삭제 금지·audit 기록·재수집 재등장 없음). 조회·요약 집계에서 삭제 건 제외. 전 직원 사용 가능.
- 확인 답변 기록: 수집 이후 취소된 주문의 보류 행은 자동으로 사라지지 않음(취소 자동 마킹은 수집 시점 한정 — #141) → 체크 삭제로 수기 정리.

## v5.9.206 (2026-08-06)
- 지시 #245 (대표 확정): **보류 건 [오늘/내일 발송으로 안내] 직원 개방** — `manual-send` 라우트의 adminOnly 해제(로그인 직원 전원). 문의 관리 = 전 직원 공용 원칙. 발송 로직·멱등 가드 무변경.

## v5.9.205 (2026-08-05)
- 지시 #235 (대표 실물 판정 교정 2건): ① /guide 헤더 로고 = 🍊 이모지 → **아꼼이 캐릭터 실이미지**(`public/akkomi.png` — v5-preview 정본 에셋과 바이트 일치, 자체 서빙) ② **「다른 상품들」 섹션 비노출** — `GUIDE_SHOW_OTHERS=false` 플래그 OFF(숏클립 방식 — 마크업·로직 보존, 재활성 가능). 결과 화면 = 로고 헤더 + 주문하신 상품 + 안내문만. 파라미터 없는 직접 접근은 알림톡 버튼 안내 문구로 대체(전 품목 나열 제거). 데이터 연동·URL·알림톡 파이프라인 무변경.

## v5.9.204 (2026-08-05)
- 지시 #228-①·#234-⑤ (대표 확정 ⓐ안): **/guide 먹는법 페이지 v5 톤 리뉴얼** — 인디고 → 자사몰 v5 토큰(#FFD400·#8CC63E·#3A4A2B), 히어로 그라디언트 헤더+브랜드 배지. **URL·로직·데이터·알림톡 버튼 링크 전부 무변경**(스타일 계층만 — 템플릿 재심사 불요 설계 유지).

## v5.9.203 (2026-08-05)
- 지시 #231-①ⓑ: **네이버 리뷰 일일 수집 복구** — 5일 연속 「리뷰 HTTP 429」 실패(스냅샷 note 실측) 교정 3점: ⓐ429 지수 백오프 8초 1회 → **15/30/60초 3회** ⓑ**부분 성공 허용**(기존엔 상품 1개 실패 시 전체 폐기 — 실패 상품만 스킵하고 수집분 저장, note에 실패 수 기록) ⓒ호출 간격 1.5초→3초. 다음 04:30 수집분으로 복구 판정.

## v5.9.202 (2026-08-05)
- 지시 #228-④ (대표 확정): **공개 버전 엔드포인트 신설** — `GET /api/public/version` (버전 문자열만·인증 불요·다른 정보 0). 이후 배포 검증 절차에 이 엔드포인트 사용.

## v5.9.201 (2026-08-05)
- 지시 #221-1 (대표 확정): **「재발송 1회」→「소급 발송」 라벨** — dry-run→실발송 소급 전환 건을 진짜 재발송과 구분. `kakao_notify_log.backfill` 플래그 신설(표기 계층 전용·additive), 소급 러너가 마킹, 오늘 소급 14건 일괄 마킹(다른 행 오염 0 확인). 소급 후 추가 재발송 발생 시 「소급 발송 · 재발송 N회」 병기(소급 1회분 카운터 제외). 발송 로직 무접촉.
- 지시 #221-2 (대표 확정, 설계만): 자사몰 리뷰 = **통합 목록 1개 + 출처 뱃지**(N스토어/쿠팡(향후)/자사몰) 안 채택 — #213의 2섹션안 폐기. 평점 혼합 집계 금지(출처별 분리 표기)는 유지. 시공은 카페24 이식 Phase 3에서.

## v5.9.200 (2026-08-05)
- 지시 #219 (대표 확정): **유선번호 = 「번호 오류」 표기 확정** — 기존 「🎁 선물하기」 오표기 교정. 휴대폰(01x)만 알림톡 가능하므로 유선(02~09권)·인터넷(070)·대표번호(15xx~18xx)는 **발송 시도 없이 `bad-tel` 상태로 기록**하고 재안내하지 않는다.
  - 서버: `isBadTel()` 신설 — 주문안내·발송안내·소급 러너 3곳의 **기존 가드(#208 지점) 바로 뒤에 분기 1건 추가**(정상 경로 무변경). 화면 요약 집계에 `bad_tel` 추가 + 마스킹 형태(02-3\*\*-…)도 앞자리로 유선 판정해 선물하기에서 분리(#204 표시-집계 통일 교훈).
  - 프론트(app.js v=349): 수신 칸 「📵 번호 오류 (유선)」 중립 회색 pill(**선물하기 판정보다 먼저 검사**·[재조회] 미노출), 상태 칸 `bad-tel` 뱃지, 요약 줄 「📵 번호 오류 N」(0건 시 생략).
  - 단위 검증: #208 8케이스(정상 휴대폰·마스킹·빈값) 전부 무영향 + 유선 5케이스(02·064·070·1588·하이픈 유무)·마스킹 유선/휴대폰 분리 — **13케이스 전체 합격**(실행 검증).

## v5.9.199 (2026-08-05)
- 🚀 지시 #214 발사 지원 + #215·#216·#217 시공 (서버만 — app.js 무접촉, 발송 파이프라인 함수 무변경):
  - **지시 #215 — 텔레그램 알림톡 결과를 60분 신규주문 알림에 통합**: 기존 건별(3분 주기 묶음) `kakaosend` 즉시 알림 제거 → `collectOrderNew`(60분)가 직전 1시간 `kakao_notify_log`를 집계해 「📨 알림톡(1시간): 성공 N · 실패 N · 보류 N · 차단 N」 줄을 신규주문 알림에 병기. 실패 건은 마스킹 번호+사유 1줄씩(최대 5). 0건 시간대는 줄 생략. 신규주문 0건인데 알림톡 결과만 있으면(소급 시간대) 요약 단독 발송. 야간에는 `kakaosend` 누적을 60분 지점에서 수행(아침 브리핑 줄 유지, 이중 집계 방지). **예외(문제는 시끄럽게)**: 한 주기 실패 5건+ 이면서 실패>성공이면 즉시 🚨 오류 알림.
  - **지시 #216·#217 — 발사 당일 소급 발송 러너 신설**(`kakao_backfill_request` DB 플래그, 1회성): `{mode:'preview'}` = 오늘(KST) `switch-off` 건 전수 + 네이버 재조회로 **발주확인(placeOrderStatus) 판정만** 기록(발송 0). `{mode:'send', order_keys:[...]}` = 지정 건만 발송 — 이중 안전(행 상태 `switch-off` 재확인 멱등 + 네이버 재검증: PAYED·무클레임·**발주확인 미완(NOT_YET)만**). 발주확인 완료(OK) 건은 대표가 이미 수동 문자 안내한 손님이라 제외(#217), 판정 불가 값은 발송 안 하고 목록 보고. 문면·차단 가드(gift-masked/no-tel)·발송 성공 시 발주확인 자동(#92 철칙)은 수집기와 동일 함수 재사용. 발송 간격 700ms. 결과 = `kakao_backfill_result` + audit(`kakao_backfill`).

## v5.9.198 (2026-08-05)
- 🚨 지시 #208-1 (대표 확정 "빈 번호는 발송 안 하는 게 맞다"): **빈 번호 발송 차단**. 기존 가드는 **마스킹(`010-9***-7***`)만** 걸러내고 **빈값(`''`·`null`)은 그대로 통과**시켜, 발사 후 알리고에 빈 번호로 요청이 나가 **실패 응답만 매일 쌓일 자리**였다(#207에서 실DB 6건 전부 이 경로임을 확인).
  - **주문안내·발송안내 양쪽**의 같은 지점에서 차단 — 발송 시도 0, 상태만 기록.
  - 상태는 **원인이 달라 구분**한다: `gift-masked`(네이버가 가려서 줌) / **`no-tel`(애초에 오지 않음)**. 화면 표기도 이미 갈려 있다 — 마스킹은 「🎁 선물하기」, 빈값은 「⚠️ 수동 연락 필요」+[재조회]. 빈값 건의 `receiver_masked`를 `***`로 남겨 **[재조회] 대상이 되게** 했다(시간이 지나면 네이버가 채워주는 특성 활용).
  - **정상 번호 무영향 단위 검증**: `010-6687-4031`·`010-9114-2233`·`01066874031` 전부 통과, 마스킹·빈값·`***`만 차단.

- 🚨 지시 #204-1: **요약 줄에서 선물하기가 분리되지 않던 것 교정** — 행 표시는 「🎁 선물하기」로 갈렸는데 요약 줄은 「⚠️ 수동 연락 2」에 그대로 남아, **조치 불요 건이 경고로 잡혀 직원이 헛걸음**하던 문제.
  - 원인: **표시와 집계가 서로 다른 기준을 봤다.** 행 표시는 *재조회한 실번호에 `*`가 있으면 선물하기*로 판정(`receiver_full`)하는데, 요약 집계는 DB의 `status='gift-masked'`만 셌다. 그 상태는 **실DB에 0건**이라 `gift`는 항상 0이었다.
  - 교정: 요약도 **행 표시와 같은 소스**로 판정한다. rows용으로 이미 재조회해 둔 `_telCache`를 **재사용**하고(추가 API 호출 없음), 그 페이지에 없는 건만 개별 재조회. 재조회 실패 시에는 **안전하게 경고(수동 연락)로 유지** — 경고를 임의로 지우지 않는다.
- 🚨 지시 #204-2: **"모두 표시됨 (60 / 30건)" — 분자가 분모를 넘던 것 교정**. DB는 30/30으로 정상이었고 **프론트 경합**이 원인.
  - 원인: 조회가 두 번 겹치면(필터 전환 직후 재조회, [재조회] 성공 후 자동 갱신 등) **두 응답이 각각 `notifyLogShown`에 더해져** 표시분이 실제의 2배가 됐다.
  - 교정: **요청 순번 가드**(늦게 온 응답 폐기, #181과 같은 방식) + 표시분을 누적(`+=`)이 아니라 **append 여부로 확정**. 수정 전 코드로 **60/30 재현 → 수정 후 30/30** 확인.

## v5.9.195 (2026-08-04)
- 지시 #198: **보류(08~09시) 건 중 발송안내가 이미 나간 건은 주문안내 버튼 자동 소멸**. 손님이 "발송했습니다"를 받은 뒤 "오늘 발송 예정입니다"가 또 가는 상황을 막는다. 같은 행의 발송안내 상태로 **자동 판정**해 「⏸ 보류 → 발송안내 완료 (주문안내 불요)」로 종결 표기. 발송안내가 아직 없는 보류 건은 현행대로 버튼 2개 유지.
  - [내일 발송으로 안내] 문면은 `computeShipping`을 그대로 쓰므로 휴무일이 반영됨 — 실측: **금요일 보류 → "모레 일요일 오전 발송"**(토요일 발송 불가 회피), 토요일 → "내일 일요일", 평일 → "내일 수요일".
- 🚨 지시 #199: **선물하기 주문 식별·표기** — 네이버 선물하기는 **수령자 번호를 마스킹해서 준다**(`010-9***-7***`). 사양이며 우리 결함이 아니다.
  - 🔴 **발견한 위험**: 마스킹 번호를 그대로 쓰면 숫자만 남겨 **"01097" 같은 엉터리 번호로 발송을 시도**하게 된다. `isUsableTel()` 신설(마스킹 문자·자릿수 판정) → 주문안내·발송안내 **양쪽에서 발송 시도 자체를 차단**하고 `gift-masked` 상태로만 기록.
  - 화면: 「⚠️ 수동 연락 필요」(경고) → **「🎁 선물하기 (번호 비공개)」(중립)**. [재조회] 미노출, 요약 줄 분리(`🎁 선물하기 N`), **필터 "실패·보류만"에서 제외**(조치 불요 건이 섞이면 경고가 의미를 잃음).
  - 진짜 번호 미확보 건(#193/194)은 현행 「⚠️ 수동 연락 필요」 + [재조회] 유지 — 두 상태가 명확히 갈린다.
- 지시 #196: 자사몰 챗 실API 배선 **보류**(입력·전송 비활성 원복). CS 구조 조사 후 대표 확정 시 재개.

## v5.9.192 (2026-08-04)
- 지시 #195-3: **자사몰 챗 → CS 시나리오 DB 동일 응대 API 신설** `POST /api/public/shop-chat`. 🔴 원천 하나 원칙 — 톡톡봇·상품문의·고객문의가 쓰는 **`qnaGenerate()`를 그대로 호출**(자사몰용 별도 응답 세트 신설 금지). 상품 상세에서 열면 **상품명을 맥락으로 전달**. 재료 부족·SKIP이면 **가짜 답변을 만들지 않고**  + 사람 연결 안내(#189 원칙). 쓰기 없음·PII 미수집·IP당 분당 20건 상한.

## v5.9.191 (2026-08-04)
- 🚨 지시 #193/#194(발사 전 필수): **수신번호 누락 방지 — A(발송 직전 재조회) + C(수동 연락 필요 표시)**.
  - **A. 발송 직전 재조회**: `resolveReceiver()` 신설 — 번호가 **비어 있는 건에만** 네이버 주문 상세를 다시 조회해 취득(주문자 → 수취인 폴백, #177 순서 계승). 정상 건은 **추가 API 호출 0**. 적용 = 주문완료 알림톡·발송안내 알림톡 **양쪽** + 수기 발송 라우트.
    - 안전: 재조회 실패해도 **예외를 던지지 않아** 해당 건만 건너뛰고 발송 루프는 계속 돈다. 무한 재시도 없음(429 백오프만). 취득한 번호는 **DB 미저장**(마스킹만 기록 — 미저장 원칙 유지).
    - 기록부도 재조회 결과를 반영해 구제된 건이 `***`로 남지 않게 교정(3곳). ⚠️ 작업 중 `out` 스코프가 없는 보류 기록 지점에 잘못 들어간 참조를 실측으로 발견해 원복.
  - **C. 수동 연락 필요 표시**: 이력 수신 칸에 `***`만 뜨던 것을 **⚠️ 수동 연락 필요 뱃지 + [재조회] 버튼**으로 교체(그 자리에서 다시 조회 — 시간이 지나면 네이버가 채워주는 특성 활용). 성공 시 마스킹만 갱신하고 **발송은 하지 않음**(소급 발송 금지).
  - 필터 **"실패·보류만"에 번호 미확보 건 포함** · 당일 요약 줄에 **"⚠️ 수동 연락 N"** 추가(0건이면 미표시).

## v5.9.190 (2026-08-04)
- 지시 #192-1: `/api/public/store-snapshot`에 **CORS 헤더 추가**(`Access-Control-Allow-Origin: *`). 자사몰(다른 도메인)에서 fetch할 때 헤더가 없어 브라우저가 차단하고 있었음 — 실측으로 확인(배선 후 `static-fetchfail`). 다른 공개 API 2종에는 이미 있던 헤더.

## v5.9.189 (2026-08-04)
- 지시 #192-2: **자사몰 발송일 안내용 공개 API 신설** `GET /api/public/shipping-eta` — 회사프로그램의 `computeShipping`을 그대로 호출해 결과만 내려준다. 🔴 **계산 이중화 금지**가 핵심(자사몰이 자체 로직을 만들면 알림톡과 자사몰이 서로 다른 발송일을 안내하게 됨 — #185 교훈). 응답: 발송 안내 문구·발송일·도착 구간·휴무 안내 대체 여부·**다음 마감 시각(오전 8시 컷오프)**·보류 구간(08:00~08:59) 플래그. PII·비용 정보 없음.
  - 시각대별 실측: 새벽 03시 → "오늘 화요일 발송"(자정 기준이었다면 "내일"로 오안내) · 07:59 → 당일 · 08:30 → 보류 구간 · 09:00 → 익일 · 금요일 오후 → **"모레 일요일 발송"**(토요일 발송 불가 반영) · 토/일 → 익일.

## v5.9.188 (2026-08-04)
- 지시 #190(대표 확정): [알림 발송 이력] **수신 풀번호 표시를 admin 전용 → 로그인한 전 계정으로 확대**. 근거 = 문의관리 화면은 직원 공용이고 네이버 판매자센터에서도 직원이 같은 번호를 조회할 수 있어, 우리 화면만 마스킹하면 주문 대조 업무가 막힘. **저장은 여전히 마스킹만**(표시 시점 네이버 재조회·배치 100·5분 캐시 방식 그대로).
- 지시 #190-2: **풀번호 조회 접근 기록** 신설(`phone_view_log`) — 계정·날짜·조회 건수·마지막 시각만 기록. 🔴 **번호 자체는 저장하지 않음**(로그 PII 금지). 화면 열 때마다 행이 쌓이지 않도록 **하루 1행(계정별) 누적** 방식이라 성능 부담 없음. 기록 실패가 조회를 막지 않도록 try/catch 분리.
- 🚨 지시 #190-3 조사 결과(코드 변경 없음·보고만): "*** 번호 조회 실패" 건은 **API 조회 실패가 아니라 수집 시점의 데이터 부재**였음. 진단 러너로 역조회한 결과 해당 건들은 **현재는 주문자 연락처가 존재**(3/3 응답 성공). 즉 네이버가 결제 직후에는 연락처를 아직 채우지 않았다가 나중에 제공하는 패턴으로, 수집 순간에 빈 값이 저장된 것. 전수 6건(kakao 6·lms 6, 오늘 2건).

## v5.9.187 (2026-08-04)
- 지시 #184: 자사몰 게임 **경제값 대표 확정값 반영 + 전면 DB 관리화**.
  - **룰렛 확률(합계 100%·꽝 없음)**: 물 1개 50% · 물 3개 8% · 쿠폰 5% 28% · 쿠폰 10% 10% · 업그레이드 이용권 3.5% · 하우스귤 3kg 선물용 1박스 0.5%.
    ⚠️ 지시서 원안(물 1개 48%)은 합계가 **98%**여서 저장이 불가능했음 — 대표 확인 후 **물 1개 50%**로 확정(2% 배분). 최저 보상이라 원가 영향 최소.
  - **신규 경제값 전부 DB화**(코드 수정·배포 없이 화면에서 조정): 나무 목표 물 개수(100) · 구매 금액당 물 지급 단위(1만원=1개) · 출석 물(1개/일) · 출석 누적 상한(0=없음) · 쿠폰 유효기간(30일) · 주문 1건당 룰렛 참여권(1회) · 업그레이드 대상 품목 매핑(미니밤호박 3kg→5kg, 하우스귤 2.5kg→4.5kg).
  - **확률 입력 방식 전환**: 정수 가중치 → **백분율(percent, 소수 첫째 자리)**. 3.5%·0.5% 같은 값을 그대로 입력 가능. `weight`는 percent×10 파생값으로 자동 계산(추첨 알고리즘 무수정).
  - **합계 100% 검증**: 입력 즉시 화면에 합계 표시(초록/빨강), 100.0%가 아니면 **저장 차단**(프론트 경고 + 서버 400). 부동소수 오차 방지 위해 ×10 정수 비교.
  - [데이터관리] > 🎮 게임 설정 화면 개편 — 확률표(칸·확률·물 개수·쿠폰 할인) + 나무/물 획득 + 참여권/쿠폰 + 업그레이드 매핑 4개 블록. 저장은 기존대로 audit 기록(누가·언제·before/after).
- 지시 #184-4: **룰렛 UX — 참여권 0회 이탈 방지**(v5-preview): 보유 횟수 배지 상시 노출(0회면 회색) · 0회 문구를 "참여 불가"류 대신 **"주문하시면 룰렛 1회가 쌓여요!" + [상품 보러가기]** 동선 · 획득 경로 상시 안내("🎟 룰렛권 얻는 법 — 주문 1건 = 1회") · 버튼에 잔여 횟수 표기 · 나무 화면에 **남은 물 개수**("62 / 100개 · 38개만 더 모으면 수확!").
- v5-preview 문구를 확정값에 맞춤: 경품 목록·결과 모달·히어로("주문 1건당 1번 — 꽝 없이 모두 당첨")·쿠폰 유효기간 7일→30일·물방울 표기→물 개수.
- 지시 #184-5: `docs/자사몰_게임_2차확장_설계.md` 신설 — 미니게임·목숨 회복 3경로·친구 초대·공유 문구 설계를 **기록만**(구현 착수 없음).

## v5.9.186 (2026-08-04)
- 🚨 지시 #182(발사 게이트): **예약 상품 주문이 일반으로 새어나가 발송일을 오안내하던 결함** 교정. 예약 판정이 상품명·옵션의 `'예약'` **문자열 매칭만**이어서, 같은 청귤이라도 네이버 옵션 표기에 '예약'이 빠진 주문은 일반(UJ_9084)으로 분기돼 `computeShipping` 계산값("오늘/내일 오전 발송")이 나갔음. **실제 발송일은 8월 10일** — dry-run 이력에서 **오분류 13건 실측**(청귤 5kg 4건·10kg 9건).
  - 교정: `isReserveOrder(optText, matched)` 헬퍼 신설 — `'예약'` 문자열 **또는** 품목에 등록된 **예약 발송 시작일(`bot_products.reserve_ship_start`)이 아직 오지 않은 경우** 예약으로 판정. 발송 시작일이 지난 뒤에는 실제 순차 발송이 시작되므로 일반 계산으로 자동 복귀. 데이터 소스는 기존 체계(판매현황 탭 편집 필드) — 하드코딩 없음.
  - 적용 2곳: 주문 수집기(자동 발송 라인)·보류 건 수기 발송 라우트. 일반 상품 문면(UJ_9084) 무접촉.
  - 검증(실주문 데이터 → 렌더 → 실DB 3겹): 오분류 케이스 2종 → 예약/UJ_9085 + "8월 10일부터 순차 발송 예정"으로 전환 · 기존 정상(사전예약특가) 무변화 · 일반 상품 2종 무변화 · 8/15 시뮬에서 일반 복귀 확인. 기존 이력 대조: 예약형 문면 241건은 전부 정상("8월 10일부터"), 일반형 497건도 정상.
- 참고: 리허설(#180-B)에서 UJ_9085 문면에 "내일 수요일 오전 발송"이 보인 것은 **리허설 한정 현상**(테스트 러너가 4종 템플릿에 같은 변수 묶음을 공통 주입) — 실운영 로직과 무관.

## v5.9.185 (2026-08-04)
- 지시 #180-A1: 알림 발송 이력 **표시 상한(300건) 폐지 → 100건씩 [더보기] 페이징**. 서버는 offset/limit 방식(1회 최대 100 — 무제한 전량 로드 차단), 응답에 `total`(조회 조건 전체 건수) 추가. **요약 줄은 항상 total 기준**이라 [더보기]로 표시분이 늘어도 숫자가 흔들리지 않음. 버튼에 진행 표기("더보기 (200 / 779건)"), 전부 표시되면 "모두 표시됨"으로 대체. 조건(날짜·검색어·필터·빠른 기간) 변경 시 1페이지로 초기화. 실측: 최근 7일 = **779건**(기존 상한에서 479건이 잘리고 있었음).
  - 부수 교정 2건: ①정렬에 타이브레이커(`id DESC`) 추가 — 동일 시각 행이 페이지 경계에서 중복·누락되는 것을 방지(3페이지 300건 누적 중복 0 실측) ②필터(미도래·실패/보류)를 **페이지 내 후처리 → SQL 조건으로 승격** — 후처리로 두면 "100건 뽑아 거른 나머지"가 페이지마다 달라져 총건수·페이징이 어긋남. `filter=issue` 21건·`pending` 322건 실DB 일치.
  - #179에서 넣었던 "상한 300건 도달" 경고는 페이징 도입으로 불필요해져 제거.
- 지시 #180-A2: 휴무일 달력 **조작 안내 문구 강조** — 회색 작은 글씨 → danger 토큰(#F04438) 알약 배지·13.5px·700(디자인 가이드 토큰 내, 임의 색 신설 없음). 달력 기능 무접촉.
- 🚨 지시 #181-1: **수정 이력이 탭과 무관하게 섞여 보이던 문제** 교정. 원인은 **프론트 2가지**(서버는 원래도 `target_type`으로 정확히 분리 저장·조회 중이었음) — ①**시기별 상품 지식 전용 라우트가 아예 없어** 그 탭에서 [조회]를 누르면 시나리오 이력이 나왔음 ②**탭을 바꿔도 이력 카드를 비우지 않아** 이전 탭 결과가 그대로 남아 있었음. 교정: `season-knowledge-logs` 라우트 신설 + 탭별 대상 매핑표(시나리오/판매현황/시기별) + 탭 전환 시 초기화 + 카드 제목에 대상 명시("🕓 시기별 상품 지식 수정 이력") + 이력 대상이 없는 탭(톡톡·상품문의·고객문의)에서는 카드 자체를 숨김. 교차 오염 실측: 세 라우트 응답의 공통 id **0건**(시나리오 68·판매현황 110·시기별 5, 전부 실DB 일치).
- 지시 #181-2: 빠른 기간 버튼 재조회 — 코드 경로는 정상 동작(수동→[오늘]→[최근 7일]→[오늘] 재클릭·연속 클릭까지 실측 갱신 확인)이었고, **"갱신 안 된 것처럼 보인" 실제 이유는 오늘·최근 7일이 실제로 0건이라 화면이 계속 "이력이 없습니다"로 같았기 때문**. 교정: **조회 요약 줄 신설**("조회 결과 0건 · 2026-08-04 ~ 2026-08-04 · 판매현황·가격")로 조회가 실행된 사실이 항상 드러나게 + 0건 문구에 기간 명시 + **요청 순번 가드**(늦게 온 이전 응답이 최신 화면을 덮지 않게).

## v5.9.184 (2026-08-04)
- 🚨 지시 #179 결함 2건 수정 (조회 필터가 통째로 무시되던 문제 — 원인은 #178 시공 때 **이스케이프 소실**):
  - **① 날짜 필터 무시**: 날짜 형식 검증 정규식이 `/^\d{4}-\d{2}-\d{2}$/` 여야 하는데 백슬래시가 소실돼 `/^d{4}-d{2}-d{2}$/`(리터럴 `dddd-dd-dd` 매칭)로 들어가 있었음 → `useRange`가 **항상 false** → 날짜 조건이 SQL에서 통째로 빠지고 "최근 100/120건"이 그대로 반환. 수정 이력(판매현황·시나리오 2탭)·알림 발송 이력 **3개 라우트 전부 해당**. 수정 전 실측: 8/1~8/4 조회 → 100건(7/31·7/26·7/25가 섞여 나옴), 수정 후 → 5건(전부 8/1).
  - **② 검색어 조회 500 오류**: SQL 파라미터 참조 `$1`·`$2`의 `$`가 소실돼 정수 리터럴로 박혀 있었음(`BETWEEN 1::date`, `LIKE 1`) → PostgreSQL `operator does not exist: character varying ~~ integer`로 **요청 자체가 500** → 화면 무반응. 수정 전 실측 재현 완료.
- 지시 #179-3: 알림 발송 이력에 **빠른 기간 버튼 [오늘]·[최근 7일]·[최근 30일]** 신설 — 날짜 구간 자동 채움 + 즉시 조회(기존 수동 날짜·검색어·필터와 공존).
- 지시 #179-4: 검색창 규격 변경 — placeholder "연락처 뒷자리 또는 품목명 검색" → **"연락처로 검색"**. 동작은 **풀번호(하이픈 유무 무관) 입력으로 검색**되게 확장(DB는 마스킹만 보관하므로 앞 3자리+뒷 4자리로 대조 — 풀번호 오탐 축소). 뒷자리 4자리 부분검색은 기능 유지, 문자 입력 시 품목명 검색도 기존대로 생존.
- 지시 #179-5: 발송 휴무일 **달력 확대·가운데 정렬** — 폭 560→780px(카드 안 중앙), 날짜 셀 높이 44→62px·날짜 글자 12→14px·요일 11.5→13px로 클릭 편의 확보. 색은 디자인 가이드 토큰만 사용(인디고 `#4F46E5`·토요일 `#3B82F6`·danger `#F04438`) — 폴백에 남아 있던 구 연두 테마(`#8CC63E`) 잔재도 정리. 모바일(≤768px) 별도 규칙 추가. 달력 기능(지정/해제·기간 선택·월 이동·범례)은 CSS만 변경이라 무접촉.
- 부수 교정 2건: ①조회 결과가 **표시 상한 300건**에 도달하면 요약 줄에 경고 표기(전체 건수로 오인 방지) ②문의관리 서브탭이 `.settlement-tab` 클래스를 재사용하는 탓에 정산 탭 핸들러가 함께 실행돼 콘솔 `TypeError`가 찍히던 문제에 옵셔널 체이닝 가드(기능 영향은 없었음).
- 검증: 로컬 실서버 기동 후 **API 15케이스 전부 실DB 대조 일치** + **실제 브라우저 DOM 클릭**(빠른 기간 3종·검색 3케이스·수정 이력 2탭 조회·달력 월 이동)으로 이중 확인. 발송 계산 엔진·발송 파이프라인 무접촉.

## v5.9.183 (2026-08-04)
- 송장변환 보내는사람연락처 보정 확대(대표 지시): 구매자 연락처가 더미번호 `010-0000-0000`(하이픈 없는 `01000000000` 포함)인 건도 `010-6687-4031`로 치환. 기존 마스킹(`*` 포함) 규칙은 그대로 유지하고 조건만 추가(`senderTel()` 헬퍼로 3채널 스마트스토어·자사몰·쿠팡 공통 적용). 정상 번호·빈값 동작 무변경.

## v5.9.182 (2026-08-04)
- 🚨 #178 결함 수정: [조회] 버튼이 동작하지 않던 문제 — 신규 버튼 5종(수정이력 조회·오늘·최근7일, 발송이력 조회·초기화)의 이벤트 바인딩이 시공 스크립트의 삽입 조건 불일치로 통째 누락돼 있었음(마크업·조회 함수는 정상, 리스너만 없어 클릭 무반응). 실행이 검증된 setup 블록에 바인딩 복구·실측 합격.

## v5.9.181 (2026-08-04)
- 지시 #178: 문의관리 UI 정리 3건 — ①발송 휴무일 목록 → **월 달력**(날짜 클릭 지정/해제·시작→종료 클릭 기간·사유/안내문구 입력·월 이동·오늘·토요일 상시불가 표시·범례, 등록 현황은 접이식 요약표로 계승). 데이터·API·발송일 계산 엔진 무접촉 ②수정 이력 **조회식 전환**(기본 비표시 + 기간 선택·[조회]·오늘·최근 7일, 서버 날짜 구간 파라미터 추가) ③알림 발송 이력 **날짜 조회 + 연락처 뒷자리·품목명 검색**(요약 줄에 조회 조건 반영·[초기화]·Enter 검색).

## v5.9.180 (2026-08-04)
- 🚨 지시 #177 조사 결과 결함 2건 교정: ①감지 누락 — 네이버 변경분이 뒤늦게 노출되면 체크포인트가 이미 지나가 영구 누락(8/3 11:24 결제 1건 실측). 주문·발송 두 수집기 모두 체크포인트에서 15분 되감아 조회(중복은 order_key UNIQUE·seen 필터로 제거 — 재발송 위험 0). ②수신번호 "***" — 주문자 연락처가 응답에 없는 건에서 발송 대상·기록 모두 공백. 수취인 연락처(shippingAddress.tel1/tel2) 폴백 추가(발송 8곳·기록 포함) — 실발송 시 번호 공백으로 인한 미발송 방지.

## v5.9.179 (2026-08-04)
- 지시 #177: [알림 발송 이력] 수신 풀번호 표시 — DB는 마스킹만 저장(원칙 유지)하고 표시 시점에 네이버 상세조회로 재취득(대표(admin) 전용·배치 100건·5분 메모리 캐시·실패 시 마스킹 폴백). 주문자 연락처 없으면 수취인 연락처 폴백("***" 표기 케이스 교정). 주문 역조회 진단 러너(order_probe_request — 날짜·상태만, PII 미수집) 추가.

## v5.9.178 (2026-08-02)
- 지시 #176 기능 계승 보완: 구 [발송 안내 보내기]의 상시 접근성을 유지 — 문면 펼침 안에 [발송안내 수동 재발송] 버튼 상시 제공(실패 행은 칸에 즉시 노출 유지).

## v5.9.177 (2026-08-02)
- 지시 #176(최종 확정판·#174·175 흡수): 발송 이력 2화면 → **[알림 발송 이력] 통합·주문 단위 한 줄** — 컬럼 일시(주문)|품목|수신|주문안내|발송안내|발주확인|문면. 조회 계층 FULL OUTER JOIN(order_key — 원본 테이블 무변경·한쪽만 있는 건 단독 행). 칸 상태: ✅알림톡/🔁문자 대체/❌실패+[수동 재발송]/⏸보류+[오늘][내일]/— 미도래(예약은 "N월 N일 발송 예정")/dry-run. 필터(전체·미도래·실패보류)+당일 요약 1줄. 재발송 횟수 컬럼(resend_count) 추가. 수동 재발송의 구 "안내문 없으면 차단" 제거(#173 교정 반영).

## v5.9.176 (2026-08-02)
- 🚨 지시 #171(발사 게이트): 발송일 계산 8시 마감 컷오프 — 주문 ~07:59=당일 발송("오늘 X요일")·09:00~=익일, 토요일 불가 규칙 유지(후보일 포함 탐색). 08:00~08:59 주문은 자동 발송 보류(hold-0809)+이력 화면 [오늘/내일 발송으로 안내] 수기 버튼(멱등·재조회 방식·dry-run 규격). computeShipping forceDay 옵션.
- 지시 #173: 발송안내 알림톡 skip 구멍 교정 — 안내문·매칭 무관 전 건 발송(SMS 대체 문면만 안내문 의존, 미등록 품목은 '(안내문 미등록)' 표기). 이력 2화면 종류 라벨(주문완료 일반/예약·발송안내·보류·제외)+문면 펼침에 템플릿 코드.

## v5.9.175 (2026-08-01)
- 대표 지시(8/1): 일일 스냅샷 리뷰 수집을 판매중 5종×5건 → 전 상품 20종×20건으로 확장(배지도 전 상품) — v5 상세 "리뷰" 탭(상품별 실리뷰) 재료. 전량(수십만 건)은 API 페이지 제한·429 차단·용량으로 불가 — 상품별 최근 20건 일일 갱신 방식.

## v5.9.174 (2026-08-01)
- 지시 #168: 일일 상품 스냅샷(04:30)에 네이버 구매 배지(marketing-message — "오늘 N명 구매" 등 네이버 산출 실수치) 동반 수집 편입 — items[].buyBadge. 실패 시 무시(부가 정보·본 스냅샷 무영향). v5 상세는 배지 페이드·썸네일 숨김·네이버 가격 위계·하단 고정 구매 바(플랫폼 파일 — v5-preview).

## v5.9.173 (2026-08-01)
- 지시 #164·#165: /guide 안내문 내 URL 자동 링크화(클릭 가능·새 창). 청귤 4종 발송 안내문은 대표 확정 최종본(#165 — 설탕 비율 제거판)으로 DB 등록(별도 데이터 작업·audit 기록).

## v5.9.172 (2026-08-01)
- 지시 #161: 테스트 발송 러너 확장 — 화이트리스트에 대표 개인 번호 추가(2개 하드 제한 유지)·플래그 vars 오버라이드(전 종 실감 변수 테스트용). 실발송은 대표 GO 플래그로만.

## v5.9.171 (2026-08-01)
- 지시 #160: 알림톡 뱃지 단계형 세분화 — ✅ 설정완료(판매중+안내문) / 🔵 주문톡 OK·발송안내문 필요(예약 시즌 전 정상 중간 상태 — 청귤 오경보 해소) / ⚠️ 미설정(상태값 이상 등 실제 결함만) / 회색 발송 대상 아님(비판매 상태). 판정 기준 docs 기록.

## v5.9.170 (2026-08-01)
- 지시 #158: 예약발송 날짜칸을 공통 달력 픽커(akm-date — 인디고/라임 디자인 토큰)로 교체 — 클릭 시 달력 팝업·YYYY-MM-DD 자동 입력·change로 행 저장 활성·모바일 390px 수용 실측. 기존 저장 게이트·청귤 값 무손상.

## v5.9.169 (2026-08-01)
- 지시 #155: 상세 원본 재현도 보강 — ①sanitize 허용 스타일에 background-color·text-decoration·font-style 추가(흰 글자+색 배경 문장 실종·형광펜·밑줄 강조 소실 수정) ②font-size 클래스 정규식 수정(실물 se-fs-fsNN — 기존 전량 미매치) ③imageStrip(이미지 N열 병렬)을 ig 블록으로 보존 ④캡션(se-caption) 스타일 힌트. 타이벡 실측: 실종 문장 복구·형광펜 12블록·병렬 4그룹(8장)·회수율 100%.

## v5.9.168 (2026-08-01)
- 지시 #156: 판매현황 탭 알림톡 칸을 입력칸→템플릿 상태 뱃지로 교체 — 판정: 상태 판매중 + 발송 안내문(LMS) 존재 = ✅ 설정완료 / 미충족 시 ⚠️ 미설정(마우스오버로 미비 항목 표시). notify_message 데이터는 DB 보존·UI만 제거(현재 저장분 0건). 행 저장 payload에서 notify_message 제외.

## v5.9.167 (2026-08-01)
- 지시 #152: 판매현황·가격 탭 UI 개편 — ①예약발송 여부 토글(O일 때만 날짜칸·X 전환 시 날짜 자동 클리어→일반 문면 복귀, 기존 날짜 입력 품목은 자동 O) ②행 일괄 저장(변경 행만 [저장] 활성 — 상태·가격·안내문 2종·예약발송 한 번에 PUT) ③안내문 [펼치기/접기]. 서버·봇 반영 로직 무수정(UI 계층만).
- 지시 #153-4: 단건 테스트 실발송 러너(aligo_test_send_request — 대표 GO 후에만 투입, 수신번호 대표 화이트리스트 하드 제한·스위치 전체 ON 불필요). 실발송 0 유지 — 러너는 대기 상태.

## v5.9.166 (2026-08-01)
- 지시 #153: 알리고 자가진단 확장 — aligo_selftest_request {full:true} 시 템플릿 문안 원문(templtContent)·버튼 구성까지 수집(이모지 저장 상태 판정·버튼 실구성 확인용, 읽기 전용).

## v5.9.165 (2026-08-01)
- 지시 #151: 상세 본문의 네이버 지도 플레이스 위젯(oglink 컴포넌트) 통째 제거 — 이미지+텍스트+링크 묶음 단위(stripMapWidgets, div 균형 매칭). 내부에 map.naver.com/dthumb 있는 것만 제거(다른 링크 카드 보존 — 과잘림 금지). count·imgs·blocks 전부 제거 후 기준(3자 일치). 타이벡 실측: 텍스트 21→20(위젯 1블록만)·인접 본문 생존·평문 회수율 100%.

## v5.9.164 (2026-07-31)
- 지시 #150 보강: sanitize에 태그 밸런싱 추가 — 미쌍 닫는 태그가 innerHTML 삽입 시 부모 컨테이너를 닫아 상세 pane이 조기 종료되던 결함(Playwright 실측 발견) 수정. 짝 없는 닫는 태그 제거·미닫힘 태그 자동 닫기.

## v5.9.163 (2026-07-31)
- 지시 #150: 상세 수집을 블록 시퀀스로 확장 — 이미지+텍스트를 원본 등장 순서 그대로(parseDetailBlocks). 텍스트는 화이트리스트 sanitize(XSS 차단)·스마트에디터 클래스(se-fsNN·align-*)를 인라인 스타일로 변환해 크기·굵기·정렬·색 위계 보존. 기존 imgs 키는 하위 호환 유지. 타이벡 실원본 검증: 58블록(텍스트 21·이미지 37) 순서 일치.

## v5.9.162 (2026-07-31)
- 지시 #149-5: 일일 상품 스냅샷(04:30)에 상세이미지 전량 갱신 동반 실행 — 실패 시 본 스냅샷 유지·직전 상세분 유지(사유 note 기록). 첫 전량 수집 실측: 20종·종당 24~42장·필터 제외 0.

## v5.9.161 (2026-07-31)
- 지시 #149: 상품 상세이미지 전량 수집 — 원인=수집기 이중 상한(판매중 10종 한정 + 이미지 10장 절단)·호스트 필터(shop-phinf 한정) 확정. 수정: 전 상품(품절 포함)·이미지 무상한·pstatic.net 계열 전반 허용 + 원본 img 총수(count)·필터 제외 수(dropped) 기록. product_detail_request 플래그 러너 신설(읽기 전용). 저장은 URL 참조 방식 유지.

## v5.9.160 (2026-07-31)
- 지시 #148: 룰렛 기본 확률표에서 꽝(lose) 제거(대표 확정 — 꽝 0%) — 새 경품 6종(box·upgrade·coupon10·coupon5·w50·w100, weight 임시·운영 화면 편집이 최종). 실물성 경품(box·upgrade·쿠폰 등급) 전부 reward_grants pending 기록(#143 당첨자 로테이션 데이터 소스 정합). MALL_API=off 유지 — 실지급 0.

## v5.9.159 (2026-07-31)
- 지시 #146(A안): 알림톡 상품명 정제 — 옵션 원문의 시스템 접두부(…선택:)·번호·세그먼트 라벨 구조 규칙으로 제거, 부작용 시 원문 폴백. dry-run 21종 전수 시뮬 부작용 0.

## v5.9.158 (2026-07-31)
- 🚨 지시 #144(발사 게이트): 예약(B) 주문 알림톡의 발송일 라인이 일반 계산으로 들어가던 결함 수정 — 예약 건은 품목별 예약 발송 시작일(bot_products.reserve_ship_start·판매현황 탭 편집)로 "N월 N일부터 순차 발송 예정" 안내, 미설정 시 "시즌 시작 시 주문 순서대로" 폴백. 일반 건은 기존 계산 유지. 청귤 4종 시드 2026-08-10. 기존 축적분 예약 건은 오보 표기. 실발송 무영향(dry-run).

## v5.9.157 (2026-07-31)
- 지시 #143: 공개 조회 GET /api/public/roulette-winners — 당일 실물성 룰렛 당첨(물방울·꽝 제외) 상위 2건, 닉네임 서버 측 마스킹(김*수·s**fklaks형), 경품 라벨=게임 운영 설정 매핑, CORS 허용. MALL_API 게이트 무관 읽기 전용(PII 없음) — 홈 상단 바 타이머↔당첨자 로테이션 데이터 소스.

## v5.9.156 (2026-07-31)
- 지시 #141: 주문 알림톡 취소·클레임 제외 로직 보강 — 상세조회 시점 productOrderStatus·claimType/claimStatus 재확인, PAYED 외·클레임 건은 skip(canceled-excluded 이력). 주기 60→3분은 DB 설정(naver_auto_collect)으로 변경. 실발송 무영향(dry-run 유지).

## v5.9.155 (2026-07-31)
- 지시 #138: 템플릿 삭제 실행기(aligo_template_del_request 플래그 러너) — 화이트리스트(구본 UJ_9082·반려 이미지형 UJ_9135~9138)만 허용, 실전 승인 4종(9084~9087) 하드 차단. 구본 문안 원문은 docs/알림톡_템플릿_문안.md에 백업. 실발송 무관(삭제 API만).

## v5.9.154 (2026-07-31)
- 지시 #137: 알림톡 심사 결과 반영 — 승인 텍스트 TPL 기본값 투입(A=UJ_9084·B=UJ_9085·D=UJ_9086·E=UJ_9087, env 우선). 주문 안내 문면·버튼·코드를 승인 템플릿(JSON 단일 소스)과 세트 일치(#93 잔여 일원화) + 예약 상품(옵션에 '예약') 자동 분기 A/B. 이미지형(UJ_9135~9138)은 반려 확정 — 미투입. 실발송 무영향(KAKAO_NOTIFY=off 유지 — dry-run 문면만 kakao_notify_log 축적).

# CHANGELOG — 제주아꼼이네 회사프로그램

> 규칙: 기능 추가 = 0.1 증가(v5.1), 대형 개편 = 정수 증가(v6.0), 핫픽스 = v5.0.1 형태.
> 모든 배포는 `npm run deploy` 사용 (DB 백업 성공 없이는 배포 불가).
> 롤백 발생 시에도 이 파일에 기록 (예: "v5.1 → v5.0 롤백, 사유: ...").

---

## v5.9.153 (2026-07-30 — #122 보강: 리뷰 429 완화 — 브라우저형 헤더·1.5s 간격·백오프 재시도 1회)
- 실측: Render IP에서 공개 리뷰 첫 콜부터 HTTP 429 — 폴백은 정상 작동 확인(직전분 유지·note 기록). 헤더 보강 후 재실측, 계속 429면 사실대로 보고(대안: 로컬 수동 수집 유지)

## v5.9.152 (2026-07-30 — 지시 #122: 리뷰 일일 갱신 포함)
- [수집기] product_snapshot에 판매중 상품 공개 리뷰(상품별 5건·마스킹형) 추가 — brand.naver.com 공개 경로 직접 fetch(IP 제한 없음). **실패 시 직전 스냅샷 리뷰 유지(자동 폴백)·note에 실패 기록**. naver_product_snapshot.reviews 컬럼(additive)·/api/public/store-snapshot에 reviews 포함
- [프로토타입(리포 밖)] v5 숏클립 섹션에 영상 GIF 썸네일 카드 4종(실영상 배지·클릭 시 스토어 상품 페이지·videos 필드 자동 확장)

## v5.9.151 (2026-07-30 — 지시 #118: [🔄 상세 다시 불러오기] 버튼)
- [서버] collectProductDetails — 판매중 상품(search SALE 필터) 상세 이미지 URL 재수집(350ms 간격·429 재시도·중복 실행 락) → config 'product_detail_snapshot'. POST /refresh-details·GET /detail-snapshot-info (authMiddleware·audit)
- [화면] 데이터관리 타이머 카드에 버튼+마지막 갱신 시각 상시 표시·진행/실패 정직 표기(#113 규약)·"자사몰 반영은 실서비스 연동 후 자동 — 현재는 스냅샷 저장까지" 명기 (app.js v=329)

## v5.9.150 (2026-07-30 — 지시 #116: 시기 지식 보조 설명 제거)
- [화면] 추가 폼·목록·인라인 편집에서 보조 설명 제거 — 구간은 선택값(초기~공통)만 (app.js v=328)
- [데이터] 라벨 6건 "초기 (포슬 최적)"→"초기" 정리 — 유실 방지: 본문 반영 5건 확인·청귤 초기만 "첫 출하 시기예요 —" 본문 병합 (audit)

## v5.9.149 (2026-07-30 — 지시 #114·#115: 시기별 상품 지식 탭 UI 개선)
- [#114] 구간 선택형(초기/중기/후기/마감기/시즌 전체/공통)+보조 설명 — 시드 11구간 표준화 마이그레이션(원명은 보조 설명 보존·audit). **황금향 → 하우스황금향·노지황금향 독립 품목 분리**(하우스/노지 구분 품목 별도 원칙). **월-일 전용 커스텀 달력 픽커(AkmMdCal — akm-cal 토큰 재사용·연도 없음·직접 타이핑 병행)** 추가 폼+인라인 편집 전부. 표기 "05/01 ~ 07/31" 통일(DB는 MM-DD 유지 — 서버·주입 로직 무변경)
- [#115] 행 상태 체계: 기본 [수정][삭제](읽기 표시) → 수정 시 편집 모드 [저장][취소] (app.js v=327)

## v5.9.148 (2026-07-30 — 지시 #113: 요청형 인계 알림 미발송 결함 수정 — 신뢰성 사안)
- [원인] #111 테스트 알림은 **발송 자체가 0건** — 감지 정규식 간격 상한 14자에 실제 주소("배송지를 서귀포시 화순로 101로 변경")가 걸려 미매칭. 보고자(클코)가 "queue 비어있음"을 발송 증거로 오독 + 실물 미확인을 "실측 증거"로 표기한 보고 오류(정직 인정)
- [수정] 정규식 간격 14→40자([\s\S]) — 미탐 4·오탐 0 케이스 검증. notifyTelegram이 결과('sent'/'failed'/...)를 반환하도록 개선(기존 호출부 무영향), 폴러는 'sent'일 때만 큐 비움(실패 시 다음 틱 재시도) + last_alert{at,count,result} 상태 기록
- [규약] "대표 폰 수신"류 표현 금지 — API 성공 응답까지만 주장, 실물 확인은 대표 몫 (CLAUDE.md 반영)

## v5.9.147 (2026-07-30 — 지시 #112: 시기별 상품 지식 독립 탭 승격)
- [화면] 문의 관리 탭 순서: 판매현황·가격 → 시나리오 → **📅 시기별 상품 지식(신설)** → 톡톡 문의 → 상품문의 → 고객문의. 시즌 오픈 대기 신청 카드도 같은 맥락이라 함께 이동(권장안 채택). 기능·저장 로직 무변경 — 위치 이동만 (app.js v=326)

## v5.9.146 (2026-07-30 — 지시 #109·#110·#111: 청귤 정정·응대 원칙 승격·요청형 인계)
- [#109] 청귤 시드 정정 — "달달" 오류 삭제(산도가 본질)·설탕 비율 이유(후반 산도↓·크기↑) 명기·구간별 💡 장점 포인트(초반=산도 최강·청 최적/후반=크고 작업 편함·속 노랑) — DB UPDATE 4구간+공통 soft delete
- [#110] 공통 지침 "정직한 사실 + 현재의 장점" — qna·inquiry 프롬프트 + 톡톡봇 ai-handler(별도 리포) 3곳. 장점도 재료에 있는 것만(환각 방지 조항 포함)
- [#111] 요청형 문의 지침(복창 확인·완료 단정 금지·발송 후는 직원) 3곳 + **요청형 직원 인계 알림 폴러**(message_logs 3분 감지 → 텔레그램·야간 보류 큐 — 봇이 자동응답해도 직원 목록에서 안 묻힘)

## v5.9.145 (2026-07-30 — 지시 #108: 시기별 상품 지식 시스템 — "포슬한가요" 오답 해결)
- [DB] `product_season_knowledge` (additive·soft delete·audit) + 시드 12구간(미니밤호박 3·청귤 5·황금향 2·하우스감귤 2 — 대표 원문)
- [주입] `seasonScenariosToday()` — 오늘 날짜(KST) 구간을 "[오늘 시기] 품목—구간" 가상 시나리오 재료로 변환, **3채널 공용 주입**(qnaGenerate·inquiryGenerate·봇 /api/scenarios — 봇 무수정). 경계 3일 전 전환기 뉘앙스 자동 부가. 시기 지식 없으면 기존과 완전 동일(변화 0)
- [화면] 판매현황 탭 "📅 시기별 상품 지식" 섹션 — 구간 목록(오늘 구간 초록 표시)·추가/수정/삭제(직원 가능·soft) (app.js v=325)
- [검증] `qna_sim_request` 플래그 — 생성만 하는 재현 테스트(게시·발송 0) — STEP4 게이트용

## v5.9.144 (2026-07-30 — 지시 #107 STEP3: 일일 상품 스냅샷 수집기 — 자사몰 동기화 1단계·기본 OFF)
- [타이머] `product_snapshot` 신설 (naver_auto_collect 시드 — **기본 OFF**·하루 1회·새벽 04:30 앵커·화면 타이머 카드에서 관리). 기존 타이머 무접촉
- [수집기] collectProductSnapshot — 공식 상품 API(릴레이) 목록 조회(100건×최대 5p·350ms 간격·429 재시도)→ `naver_product_snapshot` 저장(최근 30회 보존). ⚠️ 앱 '상품' API 권한 필요(GW.AUTHN 시 실패 기록 — 권한 개통 후 ON)
- [라우트] GET `/api/public/store-snapshot` — 최신 스냅샷 공개 조회(상품 공개 정보만·PII 없음) — v5/실서비스가 읽는 지점
- [프로토타입(리포 밖)] index_v5 v2: 시즌 준비 중 구간→캘린더 이주·캘린더 요약 섹션·후기 3건+더보기·상세 시트 "상세정보" 탭(공개 상세 이미지 5종×10장). calendar.html 재설계: 시기표 20종(+상품번호 매핑)·품목 클릭 상품 카드·품절 카드+대기 CTA

## v5.9.143 (2026-07-30 — 지시 #107 STEP2: 네이버 상품번호 컬럼 + 조회 오류 상세 캡처)
- [DB] `bot_products.naver_product_no` BIGINT (additive·initDB 반영) — 23건 전부 투입(청귤→5731582511·하우스감귤→6400134206·미니밤호박→5011476022·황금향→11126666859). 매칭은 번호 우선·이름 폴백 기초
- [화면] 판매현황·가격 탭 품목명 아래 "🛰️ 네이버 {번호}" 읽기 표시 (app.js v=323)
- [실행기] naver_query_request 오류 시 error_data(네이버 응답 본문) 기록 — 403 원인 판별용

## v5.9.142 (2026-07-30 — 상품 조회 읽기 인프라, 지시 #103·#104 자사몰 실데이터용 — 쓰기 0·기존 무접촉)
- [릴레이] ALLOW 읽기 2종 추가 — POST /external/v1/products/search(목록)·GET /external/v2/products/*(단건: 대표이미지·가격·옵션·상세). RELAY_VERSION **2026-07-30.1** (🔴 대표 SSH 재실행 필요)
- [실행기] `naver_query_request` 플래그 — 위 상품 조회 2종만 허용(그 외 경로 거부)·1회 최대 20콜·콜 간 400ms·결과 캡 900KB. 스냅샷용 읽기 전용 — PII 없음
- [실측] 네이버 커머스API **리뷰 조회 API 부재 확정**(개발자포럼 #1909 기능요청 상태) — v5 리뷰 지표는 스토어 공개 페이지 실측/대표 제공으로 대체 예정

## v5.9.141 (2026-07-30 — 알림톡 이미지 실물 배치(게이트① 충족) + 릴레이 버전 확인 플래그, 지시 #100 STEP2 진입 — 실발송 0)
- [자산] `assets/alimtalk_image.jpg` 실물 배치(대표 제공 알림톡이미지_v2_spoqa) — 규격 실측 합격: **800×400·50KB(상한 500KB)·JPEG**
- [실행기] `relay_health_request` 플래그 신설 — relayHealth()(인증 불필요 읽기) 호출해 RELAY_VERSION을 `relay_health_result`에 기록. 이미지형 등록 전 릴레이 갱신(2026-07-29.2) 여부 자율 검증용 (등록·발송 호출 없음)

## v5.9.140 (2026-07-29 — 이미지형 알림톡 본판 준비, 지시 #100 STEP1 — 등록은 게이트 대기·실발송 0)
- [문안] templates_image 4장 신설 — 본문 확정본 그대로 + **전 장 [문의하기] 버튼**(WL→카카오 채널 채팅 pf.kakao.com/_lbJLs/chat) + tpl_emtype=IMAGE·공용 이미지 1장. E 이미지판 = 4버튼(규격 최대 5개 내). 텍스트형 4장(UJ_9084~9087 심사 중)은 폴백 백업
- [릴레이] /aligo 이미지 업로드 지원 — image_b64 수신 → Node 내장 FormData로 multipart 재조립(1MB 상한·JSON 한도 4mb). RELAY_VERSION **2026-07-29.2** (🔴 대표 SSH 갱신 필요)
- [실행기] registerTemplates set='image' — 이미지 파일 존재·500KB 실측 게이트 내장. 이미지 경로 확정 = `assets/alimtalk_image.jpg`(README 안내 — 대표 저장 대기·PUBLIC 리포 고지)
- [게이트] 등록 실행은 ①이미지 파일 배치·규격 실측 ②릴레이 SSH 갱신 후에만 (#100 순서 엄수). KAKAO_NOTIFY=off·실발송 0

## v5.9.139 (2026-07-29 — E 3버튼 체제·채널추가형 등록 정정·택배조회 중계, 지시 #99 + #98 실패 정정. 실발송 0)
- [정정(실측)] 1차 등록 실패 원인 2건: ①A·B·D "채널 추가 버튼 사용 불가"(510) = **AC 버튼은 채널추가형 전용 → tpl_type=AD+채널추가 고정 문구 선언 추가** ②E "link-Mobile 입력 필수" = **등록 API 버튼 필드명 linkM/linkP**(발송 linkMo/linkPc와 다름) → 자동 매핑. 매핑 수정 후 E 1버튼판 등록·검수 신청 성공(UJ_9082)
- [E 3버튼(#99 대표 확정)] [채널 추가(AC 최상단)]+[택배 배송조회(WL)]+[맛있게 드시는 법·보관법(WL)]. UJ_9082는 심사 중 수정 불가(알리고 규정) → 새 이름 `제주아꼼이네_발송안내2`로 대체 등록(구본 미사용 예정)
- [중계] 공개 라우트 **GET /track?n=송장번호** → CJ대한통운 조회 리다이렉트(실측 200·숫자 필터·PII 0) — 템플릿 URL 자체 고정 = 택배사 변경에도 재심사 없음. 발송 변수에 송장번호 추가(상세조회 배송 정보 방어적 추출)
- [안전] KAKAO_NOTIFY=off·실발송 0. 프론트 무변경

## v5.9.138 (2026-07-29 — 알림톡 템플릿 등록·검수 신청 실행기, 지시 #98 — 대표 최종 GO. 실발송 여전히 0)
- [실행기] kakao-notify `registerTemplates()` + 60초 플래그 폴러(`aligo_register_request` {go:'yes', audit}) — 알리고 키가 Render env에만 있어 서버 측 실행(키 비노출). 문안·버튼 = JSON 단일 소스 그대로(status='confirmed' 게이트·임의 수정 차단), 릴레이 경유, audit_logs 기록. 텍스트형 4장 — 이미지형은 별도 트랙(대표 이미지 필요)
- [추적] selftest 템플릿 목록에 템플릿별 심사 상태 필드 추가(코드·이름·status·inspStatus — 심사 결과 폴링용)
- [안전] KAKAO_NOTIFY=off 유지·실발송 0. 프론트 무변경

## v5.9.137 (2026-07-29 — 알림톡 4장 체제·발송 안내 E 전환·안내 페이지·알리고 릴레이 경유, 지시 #93·#94·#95 — 실발송·등록·신청 0건)
- [문안(#93·94 대표 확정)] 4장 체제(A·B·D·E) — A·B 수정문("주문에 보답"·"발송 후 안내 예고"·"즐거운 하루")·D 확정문·**E(발송 안내) 신설 = 뼈대+짧은 변수 3종+웹링크 버튼**(장문 변수 폐기). 버튼명 14자 제한 → "맛있게 드시는 법·보관법"(13자·이모지 제거). docs·JSON·등록 스크립트 4장 갱신
- [페이지(#94)] 공개 안내 페이지 **GET /guide?p=상품id** — 인증 불필요·PII 0·모바일 퍼스트·noindex. 콘텐츠=bot_products.shipping_guide(화면 수정 즉시 반영), 클릭 상품 최상단+전 판매중 상품 아코디언(크로스셀), 구매 링크 자리 주석. 인디고 토큰·구조 수준(#81)
- [발송 경로(#93)] collectLmsGuide: **알림톡 E 우선 → 실패 시 SMS 대체**(aligo failover=Y·대체 문면=기존 LMS 전문). E 미승인·코드 미투입 동안 SMS 직행 자동 판단. sendAlimtalk에 button_1·testMode(공식 스펙 확인) 추가. 스위치 OFF·dry-run 원칙 동일
- [경로(#95 실측 정정)] 알리고는 등록된 발송 서버 IP만 허용(#91 실측 -99/-101) — **NCP 릴레이(101.79.16.213) 경유로 확정 전환**: naver-relay.js `callAligo`(+relay `/aligo` 화이트리스트 중계 — 알리고 키는 Render만 보관·릴레이는 전달만), kakao-notify 전 호출·selftest·등록 스크립트 릴레이 경유. 🔴 **릴레이 갱신 필요(대표 SSH — RELAY_VERSION 2026-07-29.1: /aligo + 발주확인 confirm ALLOW 동시 반영)** + 알리고 관리자에 101.79.16.213 등록
- [안전] 전부 additive·프론트 무변경(app.js v=322 유지)·실발송 0건

## v5.9.136 (2026-07-29 — 발송 성공→발주확인 자동 실행, 지시 #92 — 대표 철칙 공식 변경·스위치 OFF·실상태변경 0건)
- [철칙 변경(대표 확정)] "발주확인 수기" → 알림톡 가동 시: 주문 수집 → 알림톡/SMS **발송 성공 시에만** 네이버 발주확인 자동(신규주문→배송준비중). 실패·미발송 건은 상태 유지 + 수기 목록 + 텔레그램. 자사몰은 예외(원래 N20 진입), 쿠팡은 주문 수집기 신설과 함께 2차(설계 주석만)
- [API] `naverConfirmOrders()` — 공식 스펙 실판독(POST /external/v1/pay-order/seller/product-orders/confirm·1회 30개·success/failProductOrderInfos). 105306(이미 확인됨)='already' 무해 처리·응답 무언 통과 금지·350ms 간격+기존 백오프. **KAKAO_NOTIFY=off면 실호출 0건(confirm_status='dry-run' 시뮬레이션만)**. 🔴 릴레이 ALLOW에 confirm POST 미허용(403) — 가동 전 릴레이 갱신 필요(대표 확인 후 별도)
- [DB] kakao_notify_log에 confirm_status·confirm_error·confirmed_at (additive·기존 행 NULL)
- [화면] [문의 관리] 판매현황 탭 "📱 주문 알림톡·발주확인 이력" — 발주확인 상태 열 + "수기 처리 필요만" 필터. GET /api/agent-office/kakao-notify-logs(?filter=manual)
- [알림] 신규 키 `confirmneed`(기본 ON·야간 모드 준수) — "알림톡 발송 실패 N건 — 수기 발주확인 필요" (실발송 모드에서만)
- [안전] 전부 additive·스위치 OFF 상태 실상태변경 0건. 주기 60→3분은 화면 조정만(하한 3분 기존 지원·코드 무변경). app.js v=322

## v5.9.135 (2026-07-29 — 알리고 연동 자가진단, 지시 #91 — 과금·실발송·등록·검수 신청 없음)
- [모듈] `kakao-notify.js`에 `selftest()` 추가 — 무해한 '읽기' 호출 3종만: 알림톡 토큰 발급(키·ID 인증 검증)·템플릿 목록 조회(발신프로필 senderkey 검증)·문자 잔여건수 조회(과금 없음). **등록(template/add)·검수(template/request)·발송(send) API 미호출.** 키 값은 마스킹(존재·길이·앞2자)만 기록, 발신번호는 형식 확인만
- [서버] 독립 60초 폴러 — DB 플래그 `aligo_selftest_request` 감지 시 1회 실행(선제거로 반복 방지) → 결과를 `aligo_selftest_result`(agent_office_config)에 기록. 텔레그램 게이트와 무관·기존 코드 무접촉(additive)
- [안전] 프론트 무변경(app.js v=321 유지). 실발송 0건

## v5.9.134 (2026-07-29 — 자사몰 게임 백엔드 선행 시공, 지시 #85 — MALL_API=off 전면 잠금)
- [테이블] 신규 5종(additive): mall_members·points_ledger(append-only 원장·idem_key UNIQUE)·roulette_spins(**1일 1회 = DB UNIQUE 원자 보장**)·tree_state·reward_grants(+인덱스 2종)
- [API] 신규 모듈 `mall-api.js` → `/api/mall/*` 13종 결선: 회원 resolve·룰렛 spin/status(서버 가중치 추첨·트랜잭션 원자성)·포인트 balance/ledger/earn/redeem(멱등키 필수·음수 잔액 불가)·나무 tree/water·products(bot_products 읽기)·season-waitlist(기존 재사용)·rewards 스텁(카페24 scope 재동의 후 개방). **전면 게이트: env MALL_API=on + MALL_API_TOKEN 없으면 전 라우트 503** — 현재 둘 다 미설정 = 완전 잠금. 동시성=회원 행 FOR UPDATE 직렬화, KST 자정 경계, IP 레이트리밋, 어뷰징 시나리오 7종 방어 문서화
- [화면] 데이터관리에 "🎮 게임 운영" 카드(대표 전용) — 룰렛 확률표·물주기 비용·실물 보상 월 한도를 화면에서 편집(코드 수정 없이·audit 기록·기본값 복원 버튼). ⚠️ 확률·한도 실서비스 값은 대표 확정 필요(현재 프로토타입 수치)
- [QA] 교차 검수 2회(모듈·메인 작업분) — 결함 8건 발견·전부 수정: 검증 규칙 불일치(무언 폴백 차단)·오염값 저장 차단·1일 횟수 고정 표시·바디 크기 상한(413)·실패 시 입력 보존·기본값 복원·GET 대표 전용·캐시버전 동반
- [안전] 전부 additive·기존 기능 무접촉·실지급 0건. app.js v=321

## v5.9.133 (2026-07-29 — 지시함 텔레그램 알림 정책 변경: 평시 침묵·긴급만 호출, 지시 #84)
- [변경1] 지시함 상태 알림(진행·완료·경과)을 **'ccbox' 스위치로 게이트 — 기본 OFF** (CS폰 공유 소음 방지, 대표 확정). 알림 설정 카드에 "지시함 알림" 종류 추가(화면에서 재조정 가능). OFF여도 기준선(notified_*)은 전진 — 재활성화 시 과거 건 알림 폭주 없음
- [변경2] 긴급 호출 **"🚨 똑똑확인요청"** 신설 — DB 플래그(agent_office_config 'urgent_call_request') 감지 시 1회 발송 후 제거, **야간 모드 무관 항상 발송**. 발동 조건: 복구 불가 오류·기존 기능 이상·24시간+ 차단 (클로드 코드가 긴급 시에만 기록)
- [QA] 교차 검수(QA 서브에이전트)에서 결함 2건 발견·수정: 오형 플래그 무음 잔존(→ 어떤 값이든 발송 후 제거)·객체 reason "[object Object]" 표시(→ JSON 내용 표시)
- [유지] 기존 운영 알림(주문·반품·문의·브리핑·오류)은 현행 그대로 — 이번 변경은 지시함 알림에만 적용. app.js v=320

## v5.9.132 (2026-07-28 — 발송 안내 트리거 ①자동 감지 + ③수동 버튼, 지시 #76 — 스위치 OFF)
- [자동] 신규 감지기 `lms_guide`(타이머 키, **기본 OFF**·30분): 네이버 **발송처리(DISPATCHED) 감지** → 품목 shipping_guide 매칭 → 요일 치환 → LMS 발송(스위치 OFF면 dry-run 문면만 기록). 안내문 없는 품목은 skip-no-guide 기록. 기존 수집기 무영향(별도 체크포인트·별도 이력)
- [이력] 신규 테이블 `lms_guide_log` — **order_key UNIQUE = 같은 주문 중복 발송 방지**. 판매현황 탭 하단 "📨 발송 안내 이력(LMS)" 섹션: 일시·품목·수신(마스킹)·상태·문면 펼쳐보기
- [수동] 이력 행별 [발송 안내 보내기] 버튼(대표 전용·audit) — 수신번호는 이력에 미저장이라 네이버 상세조회(허용된 읽기)로 재취득 후 발송(OFF면 dry-run 재기록)
- [스펙 확정] 알리고 문자 API 공식 스펙 재검증(#74 불확실 해소): 호스트 **apis.aligo.in**(apisms 아님 — 정정)·토큰 불필요·title 44byte 제한·result_code 양수=성공·testmode_yn 지원(env ALIGO_TEST=Y)
- [안전] 전부 additive·실발송 0건(이중 차단 유지). app.js v=319

## v5.9.131 (2026-07-28 — 발송 안내 시스템 STEP1 + 알림톡 템플릿 규격 정리, 지시 #74·#75)
- [발송 안내문] bot_products에 **shipping_guide**(장문 — 먹는법·보관법·후숙 팁) 컬럼 + 판매현황 탭 "📦 발송 안내문" 편집 열. 시드 3종 투입(미니밤호박 9·하우스감귤 6·황금향 4개 품목 — 대표 원문, shipping_guide 비어있는 품목만 멱등)
- [요일 치환] `{{내일요일}}`·`{{모레요일}}` 플레이스홀더 — 발송 당일 기준 배달 가능일 1·2번째 요일 자동 치환(`renderGuidePlaceholders`, 일요일·휴무일 건너뜀 실측 검증)
- [LMS 뼈대] kakao-notify.js에 `sendLms`(알리고 문자 — 자유 문안·심사 불필요) 추가 — **알림톡과 동일 이중 차단(KAKAO_NOTIFY=on + 키)**, 현재 dry-run만. 발송 트리거는 미구현(후보 조사만 — 지시 #74 응답 참조)
- [템플릿] 알림톡 템플릿 2종(일반/예약) 알리고 규격 정리 → `docs/알림톡_템플릿_문안.md` + 환경변수 키 이름 확정. ⚠️ **실등록·검수 신청은 보류(지시 #75 — 다음 주 대표 GO 사인 후)**
- [안전] 전부 additive·실발송 0건·기존 기능 무수정. app.js v=318

## v5.9.130 (2026-07-28 — 휴무일 관리 확장: 명절 발송 마감 기간 + 안내 문구, 지시 #73)
- [기간 등록] 휴무일을 시작일~종료일 한번에 등록(최대 31일 — 일자 분해 저장, 화면은 같은 사유·문구 연속 날짜를 한 줄로 묶어 표시). 종료일 비우면 하루
- [안내 문구] 휴무 기간에 문구를 넣으면 **그 기간 주문의 알림톡 #{발송안내}가 자동 계산 대신 문구로 대체** (예: "설 연휴로 2월 19일(목)부터 순차 발송"). 비우면 자동 계산 유지. shipping_holidays.notice 컬럼(additive)·계산 모듈 override 지원
- [운영] 매년 택배사 공지 → 직원이 화면에서 기간+문구 등록 → 코드 수정 0 (명절 마감은 계산이 아니라 화면 운영으로 흡수 — 대표 실무 확정)
- [안전] 기존 기능 무수정·실발송 스위치 OFF 유지. app.js v=317

## v5.9.129 (2026-07-28 — C3-보정: 발송일 규칙 대표 확정값 + 휴무일 화면 관리 승격, 지시 #69)
- [규칙] 발송 가능 요일 = 월~금·**일** (**토요일만 발송 불가** — 대표 확정). 발송일 = 주문 다음날, 토·휴무일이면 밀기: 금 주문→일 발송·토 주문→일 발송·일 주문→월 발송. "오전 발송" 표현 유지. 도착 규칙 기존 유지(발송 후 배달일 1~2번째 — 일·휴무일 배달 제외·토 도착 가능)
- [승격] 공휴일 하드코딩 → **shipping_holidays 테이블 + 화면 관리**: 판매현황·가격 탭 하단 "🚫 발송 휴무일 관리" 섹션(날짜 akm-date+사유, 추가/삭제 직원 가능·audit·soft-delete). 2026 공휴일 19일 시드 투입. 발송일 계산이 DB를 읽음(장애 시 내장 시드 폴백) — 명절 임시휴무·태풍 결항 직원 즉시 등록 가능
- [안전] 기존 기능 무영향(additive) — 실발송 스위치 OFF 유지. app.js v=316

## v5.9.128 (2026-07-28 — 알림톡 자동화 기반 시공 C2~C6, 지시 #68 — 전부 additive·스위치 OFF)
- [C2] bot_products에 **알림톡 품목별 안내문**(notify_message) 컬럼 + 판매현황·가격 탭 편집란(개별 저장 버튼·audit 기록). 비면 공통 템플릿 사용
- [C3] 신규 독립 모듈 `shipping-schedule.js` — 주문일시→"내일 수요일 오전 발송, 목~금 도착 예정" 계산 (다음 영업일 발송·월~토 배송·2026 공휴일 목록 내장 — ⚠️ 공휴일 목록·규칙은 대표 검수 대상, 경계 케이스 14종 표는 지시 #68 응답 참조)
- [C4] 신규 모듈 `kakao-notify.js`(알리고 알림톡 — #67 조사 스펙 반영) + 수집기 `kakao_notify`(타이머 키, **기본 OFF**) + 이력 `kakao_notify_log`(수신번호 마스킹만 저장). **실발송 이중 차단**: env `KAKAO_NOTIFY=on` + 알리고 키 4종 필요 — 둘 다 없는 현재는 켜도 dry-run 문면 기록만(검증 재료)
- [C5] **시즌 오픈 대기 신청** — season_waitlist 테이블(soft-delete)·등록/조회 API(연락처 응답은 마스킹)·판매현황 탭 하단 섹션(등록 폼+명단). 발송 연동은 향후
- [C6] 텔레그램 알림 종류 'kakaosend'(알림톡 발송 결과) 추가 — **기본 OFF**·문구 편집 가능·야간 누적/아침 브리핑 자연 편입. 실발송 발생 시에만 알림(dry-run 무알림)
- [안전] 기존 수집기·변환·문의 3채널 코드 경로 무수정(라벨·fn맵·브리핑 1줄 추가만). app.js v=315

## v5.9.127 (2026-07-28 — 🔴 톡톡 답변 열 압착 버그 + 고객문의 답변 본문 수집·표시)
- [🔴 톡톡] 봇답변 표의 **'답변' 열이 모바일에서 아예 안 보이던 버그** 수정 — 고정 열폭 합계(725px)가 min-width(680px)를 초과해 auto(답변) 열이 0px로 압착되던 것. 열폭 재배분 + min-width 880px (상품문의와 같은 라임: 답변 본문이 표에 표시·60자 접기)
- [고객문의] 판매자센터에서 직원이 단 답변도 **본문이 표에 나오게** — 조회 응답의 `answerContent`(개발자포럼 #3161로 필드 확정)를 수집 시 마스킹 저장(`raw.answer_content`) + 답변 열에 표시. 기존 수집분은 다음 30분 수집(최근 3일 창)에서 자동 보충 — 3일 이전 과거 건은 "(판매자센터에서 답변)" 유지
- [참고] 톡톡은 실시간 대화라 이미 발송된 답변의 사후 수정은 불가 — '재료' 시나리오 링크로 다음 답변을 수정하는 구조(기존). 고객문의 게시 답변 수정은 판매자센터(상품문의와 동일 정책)

## v5.9.126 (2026-07-28 — 모바일 회전 고정 + 문의 표 라임 통일 + 서브탭 개별 버튼)
- [모바일] PWA(홈 화면 설치본) **세로 고정** — manifest orientation any→portrait (대표: 기울일 때 가로/세로 레이아웃 왔다갔다 방지). 브라우저 탭으로 열면 웹 기술로 회전 잠금 불가 — 기기 회전 잠금 사용
- [UI] 문의 3채널 표 열 이름 라임 통일(상품문의 기준): 톡톡 봇답변 표 시각·품목·손님 질문·봇 답변→**일시·상품·질문·답변**(재료·직원 답변 유지), 미매칭 표 동일 + 고객문의 '등록'→'게시'
- [UI] 톡톡 서브탭(자동답변/미매칭) — v5.9.125 세그먼트에서 **개별 버튼 2개**로: 각각 흰 배경+테두리+그림자(btn-outline 토큰), 선택 시 연인디고 배경+인디고 테두리 (대표: 각각 구분선·테두리로 잘 보이게). app.js v=313·styles.css v=119

## v5.9.125 (2026-07-27 — 톡톡 문의 서브탭 세그먼트화: 클릭 가능 명시)
- [UI] 톡톡 문의 서브탭(🤖 자동답변 / 📭 미매칭·무응답)이 **비선택 시 회색 텍스트뿐이라 클릭되는 버튼인지 안 보이던 것**(대표 지적) → 일정 화면 세그먼트 토글(.sch-vtoggle) 톤 재사용: 회색 트랙 + 선택 탭 흰 알약·인디고·그림자, 호버 시 배경 변화. 신규 클래스 `.inq-subtab-bar` 스코프(전역 settlement-tab 무영향·JS 무수정), styles.css v=118

## v5.9.124 (2026-07-27 — 반품·교환 재알림 제거: 건당 최초 1회만)
- [🔴 알림] 반품·교환 알림이 **기존 클레임 건의 후속 상태 변경(수거·환불·재배송 등)에도 반복 발송**되던 것 수정(대표 실관찰 7/27 18:07 — "새 반품인 줄 오인") → `naver_claim_alerted`(주문번호+종류) 기록으로 **건당 최초 1회만 알림**. 감지 범위·수집·기록은 그대로(놓침 방지), 야간 브리핑 누적도 신규 건만. 180일 지난 dedup 기록은 물리 정리(내부 상태 — 업무 데이터 아님)
- [주의] 배포 직후 첫 수집에서 진행 중이던 기존 클레임이 상태 변경되면 1회는 알림될 수 있음(이력 테이블이 빈 상태에서 출발) — 이후부터 조용

## v5.9.123 (2026-07-27 — AGENT OFFICE 알림 스위치(기본 OFF) + 글샘 평문 폴백)
- [알림] **AGENT OFFICE 지시 처리 알림 'office' 스위치 신설 — 기본 OFF** (대표 지시): 요원 완료(✅ [요원])·마루 완료·되묻기(❓ 답변 필요)·미소 생성 완료 4종이 대상. 알림 설정 카드에서 다시 켤 수 있음(문구 편집 대상 아님). **오류·실패 알림은 이 스위치와 무관하게 항상 발송**(미소 실패·수집 실패 등). 정산관리 입력 확인(✅ 정산관리 입력)·한수 자동 훅은 유지
- [🔴 글샘] **평문 폴백 이식** — 3연타 재테스트 실측(run#226): sonnet-5 도구 JSON 태그 오염으로 versions 본문이 재시도 포함 2회 연속 전멸 → "카피 본문을 생성하지 못했습니다" 오류. 마루 v5.9.110과 같은 해법으로 **도구 실패 시 도구 없이 평문 재질의**(오염 원천 불가 경로)해 본문 확보, 보고서에 '평문 폴백' 표기. 채널·글자수 표기도 파편 감지 시 안전값('카피'/실측 글자수)으로 방어. 재테스트 종합: 마루 5건 연속 정상(정화·재시도 가동 확인) / 글샘 3연타 중 본문 사망 1·제목 오염 2 → 이번 수정으로 본문 사망 경로 차단
- [메모] 다음 후보에 '알림 회사폰 연동'(텔레그램 외 회사폰 수신) 추가 — 대표 언급

## v5.9.122 (2026-07-27 — 표 겹침·답변 열 좁음 마감 수정)
- [수정] 상품문의·고객문의 완료 표 **일시가 초 단위까지 표시돼 옆 열과 글자 겹침** → 톡톡 탭과 같은 짧은 포맷("07. 27. 오후 3:47")으로 통일 + 셀 넘침 차단
- [수정] 고객문의 답변 열이 너무 좁아 세로로 꺾이던 것 → 일시 96px·유형 60px·상품 130px로 줄여 확보한 폭을 답변 열에 배분 (상품문의도 동일 조정)

## v5.9.121 (2026-07-27 — 문의 관리 전 탭 표 규격 통일 + 질문도 접기)
- [UI] **질문도 답변처럼 "눌러서 전체 보기"** — 접기 기준 130→60자 (모바일 좁은 칸에서 90자 질문이 15줄로 늘어지던 것 해소)
- [UI] 상품문의·고객문의 완료 표에도 톡톡과 동일 규격 적용 — 고정 열폭(colgroup)·상품명 말줄임(전체는 툴팁)·**min-width로 모바일은 표만 가로 스크롤**(칸 찌그러짐 방지). 문의 관리 전 탭 동일 라임 완성

## v5.9.120 (2026-07-27 — 톡톡 문의 서브탭 2칸 분리 — 상품문의 라임)
- [UI] 톡톡 문의 탭 안을 **"🤖 톡톡문의 자동답변" / "📭 미매칭·무응답" 서브탭 2칸**으로 분리(상품문의 탭과 같은 라임·기존 settlement-tab 컴포넌트 재사용). 서브탭별 맞춤 칩(자동답변=7일 문의·응답률·봇 답변 / 미매칭=미매칭 건수·TOP)
- [UI] **[📥 미매칭 엑셀]** — 미매칭 서브탭에서만 노출, 미매칭·무응답 목록 다운로드. 기간·날짜·품목·검색 필터는 양쪽 공용

## v5.9.119 (2026-07-27 — 톡톡 문의 시각 열 최종 보정)
- [UI] 시각 열 112→132px — "오후 12:22" 끝자리 잘림 해소 (대표 실화면 확인 반영)

## v5.9.118 (2026-07-27 — 시나리오 번호 기록·개명 안전 링크)
- [기록] 톡톡봇이 message_logs에 **사용 시나리오 번호(scenario_nos '40+50')** 기록 — 응답 로직 무수정·기록만 (별도 리포 커밋 00aa21f, push 대표). 컬럼은 양쪽 멱등 보장
- [링크] 톡톡 문의 탭 재료 링크 — **번호 기록 우선 연결(개명해도 링크 유지)**, 과거 건은 이름 매칭 폴백·안 되면 "기록 없음" 그대로

## v5.9.117 (2026-07-27 — 톡톡 문의 탭 표 규격 정리)
- [UI] 톡톡 문의 2개 표를 **고정 열 규격(colgroup + table-layout:fixed)**으로 재정렬 — 긴 품목명이 한 글자씩 세로로 꺾이던 것 → **한 줄 말줄임(전체는 마우스오버 툴팁)**, 직원 답변 열이 화면 밖으로 밀리던 가로 늘어짐 해소 (PC 한 화면 안에 정렬)

## v5.9.116 (2026-07-27 — 문의 관리 개편: 톡톡 문의 탭 + 탭 순서)
- [개편] 무응답 현황 → **"💬 톡톡 문의"** 탭: 📭 무응답·미매칭 / ✅ 봇 답변 **2섹션 상시 표시**(보기 드롭다운 제거). 봇 답변 목록 = 시각·품목·손님질문·봇답변(펼치기)·**재료(시나리오 N 클릭→편집 이동, 가격즉답 표기)**·직원답변. 응답률·미매칭 TOP·기간/품목/검색 필터·엑셀(답변여부 열 추가) 유지 — message_logs 기록 활용, 톡톡봇 로직 무수정
- [개편] 탭 순서: **판매현황·가격 → 시나리오 → 톡톡 문의 → 상품문의 → 고객문의** (초기 활성 탭은 시나리오 유지, 시나리오 이모지 📝로 조정 — 💬는 톡톡 문의가 사용)

## v5.9.115 (2026-07-27 — 타이머 주기 하한 5→3분)
- [설정] 자동수집 타이머 주기 하한 5분 → **3분** (틱 계산·서버 검증·화면 입력 3곳) — 3분 주기 = 하루 480회 수준으로 rate limit 여유 확인(대표)

## v5.9.114 (2026-07-27 — 반품·교환 중심 알림 재편 + 답변→재료 시나리오 번호 링크)
- [작업A] 취소반품 알림을 **반품·교환만** 대상으로 재편(취소는 스토어 자동 처리 → 알림 제외, **수집·기록은 전체 유지** — 타이머 요약에 "반품·교환 n" 병기). 문구 "⚠️ 반품·교환 N건 — 판매자센터에서 확인해주세요"(대표 편집 오버라이드도 지시 문구로 갱신·audit), 화면 라벨 "반품·교환 알림"으로 통일, 아침 브리핑 집계도 동일 기준
- [작업B] 상품문의·고객문의 목록(대기·완료)과 톡톡 무응답 탭에 **재료 시나리오를 번호로 표시**("시나리오 34, 16" — 조합 복수), **번호 클릭 → 시나리오 편집 화면으로 바로 이동**(계절 문구 즉시 수정 동선). 기록 없는 과거 건은 "기록 없음", 개명·삭제된 이름은 이름 그대로 표시. 답변 생성 로직 무수정 — 표시·링크만

## v5.9.113 (2026-07-27 — 야간 모드 전체 알림 확장 + 아침 종합 브리핑)
- [알림] **야간 모드 적용 범위 확장**: ON인 모든 알림(신규주문·취소반품교환·정산·문의 계열)이 야간(설정 시각, 기본 21:30~07:30)엔 발송 중지 — 수집·자동답변·기록은 평소대로, 🔴 오류 알림은 항상 발송(불변). 주문·취소반품·정산 야간분은 DB 누적(`alert_night_acc`, 재시작 안전)
- [알림] **아침 브리핑 종합 상황판**: 🚚 신규 주문 N건 · ⚠️ 취소반품교환 N건 · 📮 자동 답변완료 N건(채널 구분) · ✍️ 직접 처리 필요 N건(최장 경과) — **0건 항목 줄 생략, 전부 없으면 "밤사이 특이사항 없음" 한 줄, OFF인 종류는 집계 제외**. 브리핑 발송 시 야간 누적 리셋(브리핑 OFF여도 일 1회 리셋 — 무한 누적 방지)
- [원칙] ON/OFF=받을지 여부(주간·브리핑 공통), 야간=ON인 것을 모아 아침에. 수집·처리 로직 무수정 — 발송 계층만

## v5.9.112 (2026-07-27 — 🔴 자동 로그아웃 실작동 수정 + 계정별 예외 시간)
- [🔴 버그수정] 자동 로그아웃이 사실상 만료 불가였던 문제 — 알림 배지 30초 폴링 등 **자동 요청도 '이용'으로 카운트**되어 화면만 켜두면 무한 연장. 수정: 프론트가 실제 사용자 행동(클릭·입력·터치·스크롤) 후 30초 내 요청에만 `X-User-Active` 헤더 → 서버는 **만료 검사는 모든 요청·연장은 그 헤더 요청만**
- [기능] **계정별 자동 로그아웃 예외**(`users.idle_hours`, NULL=전체 설정) — 사용자 관리 수정 모달에 "자동 로그아웃 예외(시간)" 입력(0.5~24h·비우면 전체), 목록에 🔒 Nh 배지 표시. 예: 발주컴퓨터 12시간 (신규 계정은 생성 후 수정에서 지정)
- 유지: 서버 재시작 시 전원 유예(끊김 방지 — 의도된 설계)

## v5.9.111 (2026-07-27 — 텔레그램 문의 알림 개선: 상황별 분리·야간 모드·리마인더·아침 브리핑)
- [알림] 문의 3채널 알림 상황별 재편 — 0건=무알림 · **자동 처리 건 "📮 {채널} 답변완료 N건 — 확인바람"** · **직원 필요 건 "✍️ 직접 처리해야 할 문의 N건"** (구 문의·상품문의·고객문의답변 키 은퇴)
- [알림] **미처리 리마인더**: 주간에 직원 답변 대기 건이 남아 있으면 설정 간격(기본 2시간)마다 "⏰ 미처리 문의 N건 — 가장 오래된 M시간 경과". 건수·경과는 발송 시점 DB 실계산 — 답변되면 자동 제외·전부 처리 시 자동 중단
- [알림] **야간 모드(기본 21:30~07:30)**: 문의 알림·리마인더 발송 중지 (수집·자동답변은 평소대로 — 오류 알림은 항상). **아침 브리핑(기본 07:30) 1회**: 밤사이 자동 답변완료·직접 처리 필요·신규 없음까지 한 건으로 정리 (첫 가동일은 오발송 방지로 기준일만 기록)
- [설정] 야간 시작·종료·브리핑 시각·리마인더 간격 전부 DB 저장 + 알림 설정 카드에서 편집(HH:MM 검증·라임 토스트). 신규 알림 4종도 기존처럼 종류별 ON/OFF·문구 편집·치환 변수 안내
- [원칙] 수집·게시·답변 로직 무수정 — 알림 계층만 교체

## v5.9.110 (2026-07-27 — 마루 answer 평문 폴백 — 도구 JSON 태그 오염 대응) *(CHANGELOG 누락 소급 기재)*

## v5.9.109 (2026-07-27 — 마루 max_tokens 4000 + stop_reason 실측 기록)
- [🔴 계속 조사] 규칙8 수정(v5.9.108)·sonnet-5 후에도 answer_text 공백 재현(#309·#310, audit 2053~2054) — 스키마 차단설은 기각(같은 선택 필드인 clarify_question·subtasks는 정상 출력). 유력 원인: **sonnet-5 내부 사고(adaptive thinking)가 max_tokens(1200)를 잠식 → 마지막 긴 필드 answer_text가 예산 부족으로 실종** (글샘이 2048~4000 쓰는 사유와 동일 — 코드 주석에 이미 박제돼 있던 특성)
- [수정] 마루 max_tokens 1200→**4000** + **stop_reason을 audit에 기록**(다음 실행에서 가설 실측 확인용)

## v5.9.108 (2026-07-27 — 🔴 마루 answer_text 공백의 진짜 원인: 규칙 8 목록에서 answer 누락)
- [🔴 원인 최종 확정] sonnet-5로 바꿔도 answer_text 공백 재현(#305·audit 2042) → 모델 문제가 아니라 **마루 프롬프트 규칙 8("해당 action에 필요한 필드만 출력") 목록에 route·clarify·schedule 등은 있는데 answer: answer_text만 빠져 있었음** — 규칙을 충실히 따를수록 answer_text를 생략하는 구조. 목록에 answer 추가(실사고 박제 주석 포함)
- [정리] `.claude/worktrees` 옛 세션 워크트리 3개(prunable — 구버전 server.js 사본에 sonnet-4-6 잔존) 제거. 실코드 4-6 잔존 0건 확인(마루 격상 매핑 1줄만 의도적 존치)

## v5.9.107 (2026-07-27 — 전 요원 기본 모델 sonnet-5 통일)
- [모델] 기안·미래·미소·예리·지율·한결 코드 기본값 claude-sonnet-4-6 → **claude-sonnet-5** 일괄 상향 (구형의 필드 오염·본문 누락 고질 원천 차단 — 글샘·마루는 v5.9.105~106에서 완료. 세미·한수는 AI 미사용이라 무관. env `*_MODEL`로 개별 교체 여전히 가능)

## v5.9.106 (2026-07-27 — 🔴 마루 즉답 실패 진범 수정: 구형 모델(env)·answer_text 정화·토큰 상향)
- [🔴 원인 확정] audit 실측(#2023~2035): 마루 실모델이 **Render env `MARU_MODEL=claude-sonnet-4-6`**(글샘 "test" 사고와 같은 구형) — 4회 전부 응답 오염 감지 + answer_text 누락, v5.9.105 재시도로도 회복 불가
- [수정] ①구형(sonnet-4-6) 지정 시 **claude-sonnet-5로 임시 격상 매핑**(정석은 Render env 정리 — 대표) ②answer_text를 정화기에 포함(태그만 제거, 통째 무효화 안 함) ③마루 max_tokens 800→1200(스냅샷 즉답 잘림 방지)

## v5.9.105 (2026-07-27 — 🔴 문의 연결 첫 실테스트 3건 실패 수정)
- [🔴 마루] "요즘 뭐 물어봐?"·"시나리오 몇 개야?" — action='answer' 판단은 정확했으나 **answer_text 필드를 비워 출력** → "답변을 생성하지 못했습니다" 폴백(#300·#301). 수정: 스냅샷 규칙에 answer_text 필수 명시 + **필수 필드 누락 시 오염 여부와 무관하게 1회 재시도**(기존엔 오염 감지 시에만 재시도)
- [🔴 글샘] 문의답변 본문에 **"test" 4자만 제출**(run#219 — char_counts는 "약 490자"라고 주장). 수정: ①본문 실물 검증(전 버전 40자 미만이면 경고 재시도, 재실패 시 정직 오류 — 허위 보고 차단) ②프롬프트에 자리 텍스트 금지 규칙 ③기본 모델 claude-sonnet-4-6 → **claude-sonnet-5** 상향(구형의 제목 오염 파편·필드 누락 고질 — 톡톡봇과 통일. env GEULSAEM_MODEL로 교체 가능)

## v5.9.104 (2026-07-27 — AGENT OFFICE 글샘·마루에 문의 DB 연결 — 전부 읽기 전용)
- [글샘] 지시가 문의 관련(문의·답변·시나리오·Q&A 등)이면 **문의 DB 재료 자동 주입**: 시나리오 전건(톤 교과서)·bot_products(사실 유일 출처)·최근 30일 수집 문의 유형·SKIP 질문 예·직원 실답변 사례(주입 전 마스킹 재적용). 신규 채널 `문의답변`(우리 라임 그대로 — 🍊 인사·존댓말·이모지·📞 꼬리, 알리고 규칙 미적용)·`문의요약`(동향 보고). **재료에 없는 사실은 "⚠️ 대표님 확인 필요" 표시+missing_fields — 지어내기 금지**
- [마루] 용어집에 '문의 관리' 추가 + **문의 현황 스냅샷**(시나리오 채널별 건수·7일 수집 유형·답변 대기·자동답변 스위치 상태) 실시간 주입 — "요즘 문의 뭐 많아?"류 즉답(answer), 문구 작성은 글샘 route(기존 라우팅의 '문구' 키워드)
- [안전선] 전부 읽기 전용 — 요원의 시나리오 등록·수정 경로 없음(초안→보고서함→사람이 [문의 관리]에 등록), 고객문의는 마스킹본만, 톡톡봇·상품문의 로직 무수정

## v5.9.103 (2026-07-27 — 고객문의(문의하기) 자동답변 + 답변 확인 알림)
- [기능] **고객문의(pay-user) 자동답변** — 상품문의 v2 구조 재사용: 30분 수집 시 신규 미답변 → 시나리오 재료 생성(1:1 비공개 톤 전용 프롬프트) → 자동 답변 등록(`POST pay-merchant/inquiries/{no}/answer` body `{answerComment}` — 공식 문서 확정). **반품·교환·환불 카테고리는 생성 호출 없이 무조건 직원(클레임)**. 스위치 `inquiry_auto_post` **기본 OFF**(승인 모드 — 초안만 쌓임) → SSH·첫 검증 후 대표가 ON
- [화면] [문의 관리 > 📮 고객문의] 탭 — 대기(AI 초안·클레임·SKIP·실패)/답변 완료 목록·자동 답변 토글(대표 전용)·수동 [답변 등록]·전체 보기 접기 재사용
- [알림] **답변 확인 알림(대표 지시)**: 신규 종류 '고객문의 답변'("📮 자동 답변 M건 완료 — 확인해주세요") + 상품문의 기본 문구에 "게시된 답변 확인" 추가. 기존 알림 설정 화면에서 ON/OFF·문구 편집
- [중계서버] ALLOW에 고객문의 답변 등록 POST 1줄(쓰기 2번째, inquiryNo 숫자 정확일치), RELAY_VERSION `2026-07-27.3` — **install.sh 재실행 필요(대표 SSH)**
- [안전장치 동일] 쓰기 함수 1개(PII 차단·미답변만·audit — 네이버도 기답변 건 ERR-NC-101010 재차단), 수정 API 미사용(정정은 판매자센터)

## v5.9.102 (2026-07-27 — 시나리오 목록 행 클릭 = 수정 화면)
- [UI] 문의 관리 시나리오 목록 — **행 아무 데나 누르면 그 시나리오의 수정 화면이 열림**(기존 [수정] 버튼도 유지). 사용 ON/OFF 토글 칸은 행 클릭과 분리(토글 눌러도 수정 화면 안 열림)

## v5.9.101 (2026-07-27 — 문의 관리 라임 완료 토스트 + 상품문의 답변 전체 보기)
- [UI] **저장·변경 성공 시 라임 토스트**(대표 지시): 문의 관리 시나리오(저장·삭제·사용 토글·전체 자동응답), 판매현황·가격(상태 변경·가격 저장·품목 추가·삭제), 상품문의(게시·자동 게시 토글) — 전부 "✅ ~ 완료" 라임 그라데이션 토스트. `showToast(msg, 'lime')` 타입 추가(기존 호출 무영향)
- [UI] 상품문의 **게시·답변 완료 목록의 긴 질문·답변 — 누르면 전체 펼치기** ("▼ 눌러서 전체 보기/▲ 접기"). 기존엔 120·150자에서 잘려 끝까지 볼 수 없었음

## v5.9.100 (2026-07-27 — 🔴 게시 후 DB 기록 SQL 오류 수정 + 게시 완료 토스트)
- [🔴 버그수정] 첫 수동 게시(#683035819)에서 **네이버 게시는 성공(204)했는데 DB 기록 UPDATE가 pg 오류**("inconsistent types deduced for parameter \$2" — \$2를 varchar 대입·텍스트 비교에 겹쳐 쓴 것)로 실패, 화면에 원시 오류 팝업 노출 + 게시 실패로 표시되던 문제 — 비교 파라미터를 boolean으로 분리해 수정. 해당 건은 게시 기록 수동 복구(게시자 전승범·audit 포함)
- [UI] 게시 결과를 디자인 토스트로 통일 — 성공 "✅ 게시 등록 완료 — 판매자센터에 반영되었습니다" · 실패 "❌ 게시 실패 — 사유" (원시 브라우저 alert 제거, 상세 사유는 목록 실패 표시)

## v5.9.99 (2026-07-27 — 🔴 상품문의 답변 게시 400 수정: 중계서버 PUT 바디 미전달)
- [🔴 버그수정·중계서버] `relay/server.js`가 **POST일 때만 요청 바디를 네이버로 전달** → 답변 등록 PUT의 `commentContent`가 탈락해 첫 실게시(#683035819 밤호박)가 400 거절. `GET 외 전부 바디 전달`로 수정, RELAY_VERSION `2026-07-27.2` — **install.sh 재실행 필요(대표 SSH)**
- [개선] 게시 실패 시 네이버 응답 본문(invalidInputs 등)을 `post_error`·화면 오류 메시지에 포함 — 거절 사유가 탭에서 바로 보임
- 참고: "게시 성공 4건"으로 보였던 것은 자동 게시가 아니라 **판매자센터에서 이미 답변된 과거 문의 수집분**(answered=true). 실패한 밤호박 건은 초안 대기 중 — relay 반영 후 [게시] 수동 게시 = 첫 실게시 검증

## v5.9.98 (2026-07-27 — 설계 변경: 배정(복사)→시나리오 기반 생성 — ⚠️ 대표 프롬프트·시뮬레이션 검수 후 배포)
- [변경] 상품문의(Q&A) 답변 방식 — 시나리오 확정 문구 복사 대신 **AI가 시나리오를 재료로 질문 맥락에 맞게 생성**(1개면 각색·복수면 조합). **철칙: 재료에 없는 사실 생성 금지, 부족하면 SKIP.** 고객센터 안내 꼬리 문구 서버 강제(누락 시 자동 부착). 공개 게시판 규칙(개인정보 금지·사진/개별확인은 톡톡·고객센터 유도) 프롬프트 내장. 모델 claude-sonnet-5(톡톡봇과 동일, env `QNA_MODEL`)
- [변경] `naver_qnas.ai_scenarios`(JSONB) — **사용된 시나리오명(복수) 기록**, 탭·로그에 표시. {{가격표}}/{{판매현황}} 치환 시 "(M/D 기준)" 시점 표기(공개 글 영구 잔존 대비). (예시) 배송일정 줄은 Q&A에선 원문 유지(시점성 표현 부적합)
- [유지] v5.9.97 안전장치 전부 — 공용 쓰기 함수·PII 차단·미답변만 PUT·자동 게시 스위치(기본 ON)·audit
- [톡톡봇(별도 리포)] ai-handler.js 프롬프트·파싱만 생성 방식으로 교체 — 딜레이·쿨다운·이미지·가격 즉답·치환 무수정. scenario_name에 사용 재료명 복수 기록

## v5.9.97 (2026-07-27 — STEP E: 상품문의(Q&A) 자동답변 — 첫 쓰기 기능)
- [기능] 네이버 **상품문의(Q&A) 수집+자동답변** (설계 v2·대표 승인): 30분 수집(`contents/qnas`, 타이머 `qna` 키·기본 OFF) → 시나리오 배정(채널 상품문의·공통, 키워드 1차 + 애매하면 LLM이 번호/SKIP만 — **AI 문장 생성 금지, 확정 문구 그대로**) → 확신 건 **자동 게시**(PUT `contents/qnas/{id}`) → SKIP·실패 건은 [문의 관리 > 🛰️ 상품문의] 탭에서 직원 작성·게시
- [안전장치] 쓰기는 `naverPostQnaAnswer()` 공용 함수 1개 경유(호출 지점: 수집기 자동·수동 게시 라우트 2곳뿐) — **개인정보 패턴 검사 내장**(전화·이메일·14자리+ 숫자·주소 — 고객센터 번호/공개 메일만 예외, 걸리면 게시 차단+사유), 미답변 건만 게시(이미 답변된 건 재호출 금지 — 수정은 판매자센터에서), audit 기록(자동='auto'·수동=계정)
- [안전장치] **자동 게시 ON/OFF 스위치**(`qna_auto_post`, 기본 ON, 대표 전용) — OFF면 승인 모드(초안 대기·수동 게시). 시나리오 0건이면 전부 SKIP → 시나리오 등록이 곧 유형별 자동화 ON(단계적 가동)
- [알림] 텔레그램 `qna` 종류 추가 — "상품문의 신규 N건 — 자동 게시 M건 · 직원 답변 필요 K건" (문구 편집·ON/OFF 기존 알림 설정과 동일)
- [중계서버] ALLOW에 상품문의 **읽기 GET 1줄 + 쓰기 PUT 1줄**(questionId 숫자 정확일치) 추가, RELAY_VERSION `2026-07-27.1` — **install.sh 재실행 필요(대표 SSH)**. 그 외 쓰기 경로 전면 차단 유지
- 스펙: `docs/superpowers/specs/2026-07-26-naver-qna-autoreply-design.md` (v2). 기존 고객문의 수집기·톡톡봇·시나리오 로직 무수정(추가만)

## v5.9.96 (2026-07-26 — 자동 불러오기 % 진행바)
- [UI] 송장변환 자동 불러오기 3채널·중간발주에 **% 진행바** (미소 이미지 생성 진행바와 같은 스타일 — 주황 게이지·크리핑). 예상 소요시간 기반 추정치로 95%까지 차오르고 완료 시 "N건 완료되었습니다!"로 교체. 중간발주는 게이지 아래 채널별 상태 유지. 표시만 — 처리 로직 무수정

## v5.9.95 (2026-07-26 — 확인함 보고서 바로 열람)
- [🔴 버그수정] 보고서함에서 **확인 처리(보관)된 건의 [보고서] 버튼이 "실행 기록을 찾을 수 없습니다"** 로 막히던 것 수정 — [다시 보기] 없이 바로 열람됩니다. 직원(다시 보기 버튼 없음)도 과거 확인된 보고서를 찾아볼 수 있음. 열람은 읽기 전용 — 확인 상태·보관함 동작은 그대로

## v5.9.94 (2026-07-26 — 보고서 질문 표시 개선)
- [UI] 보고서 모달 "대표님 질문" — **멀티 배정 건도 원지시(첫 질문) 표시** (배정 요약 "톡톡 이미지(미소)" 대신 원문). 일반 지시는 기존 그대로
- [UI] 보고서함 전체 이력에 **질문 열 추가** (상태와 결과 사이) — 30자 넘으면 "…"로 잘라 표시, **클릭하면 전체 펼침**. 서버는 run↔지시 연결로 질문 자동 조회

## v5.9.93 (2026-07-26 — 지시바 상시 표시 + 새로고침 디자인 + 수정 이력 관리자 전용)
- [UI] AGENT OFFICE **지시 입력칸 상시 표시** — 호버 전엔 안 보이던 입력칸에 테두리·흰 배경 상시 적용 (직원 지시 진입점 가시화)
- [UI] 무응답 현황 **[🔄 새로고침] 버튼 디자인 통일** — 엑셀 버튼과 같은 아웃라인 스타일
- [권한] 문의 관리 **🕓 수정 이력 카드 관리자 전용** — 직원에겐 카드 숨김 + 서버 조회도 차단(시나리오·판매현황 이력 공통). 직원 편집 기록 자체는 계속 남음(조회만 제한)

## v5.9.92 (2026-07-26 — 저장·완료 피드백 문구 3종)
- [UI] 데이터관리 타이머 카드 — 주기/시각 [저장]·수집 ON/OFF·알림 설정 변경 시 **토스트 문구 표시**("✅ 저장되었습니다" 등) — 눌렀는지 바로 확인 가능
- [UI] 중간발주 — 3채널 집계 완료 시 **"✅ 준비 완료되었습니다!"** 를 크게 표시(채널별 건수 유지)
- [UI] 송장변환 자동 불러오기 — 완료 시 업로드 안내 문구 자리에 **"✅ N건 완료되었습니다!"** 크게 표시(3채널 공통, 초기화하면 원래 안내로 복원)

## v5.9.91 (2026-07-26 — AGENT OFFICE 직원 개방 2차 + 리마인더 닫기)
- [권한] **보고서 [✔ 확인] 직원 개방** — 누가 확인했는지 계정 기록·표시("확인함·이름"). [↩️ 다시 보기]는 대표 전용 유지
- [권한] **[실패 표시] 직원 개방** — 피드백 API에서 fail 유형만 직원 허용(👍✏️👎💬 평가·교훈은 대표 전용 유지). 실패수집함에 "표시: 이름" 기록·표시 — 직원 실사용 실패가 요원 개선 재료로 쌓임
- [권한] **미소 이미지 [생성] 전원 허용** (대표 결정) — 기본급 생성 하나로 통일, **[고급 생성] 버튼 제거(비활성)** — 기본급과 품질 차이 체감 안 됨. 코드·경로는 보존(고급 모델 개선 시 재활성 가능). 계정·비용 승인 기록은 audit 유지. 참고: 다운로드는 v5.9.90에서 이미 전원 개방
- LIVE 로그 발송·이벤트 리마인더 배너는 현행 유지(대표 결정 — 날짜 지나면 자동 소멸)

## v5.9.90 (2026-07-26 — AGENT OFFICE 직원 공유 + 자동 로그아웃 + 퇴사 처리)
- [기능] **AGENT OFFICE 직원 개방** — 화면·마루 지시·LIVE 로그·보고서함·자료실 열람을 직원도 사용(조회+지시만, 13개 라우트 개방). **관리(⚙️)·피드백·교훈·역량테스트·정산저장·요원구성은 대표 전용 유지**(서버 차단 + 직원 화면에서 버튼 숨김). 조직도 상단 인물 = **로그인 사용자 이름·직책** 표시(요원 구성은 공용)
- [기록] 지시에 **보낸 사람 계정 기록**(pending_orders.created_by) — LIVE 로그에 "이름 직책 → 마루"로 표시, 계정별 호출량 집계 가능(요원 실행은 run_id로 연결). audit에는 이미 계정별 기록 중
- [보안] **유휴 자동 로그아웃** — 마지막 요청 기준 3시간(기본) 무이용 시 자동 로그아웃, 이용 중이면 자동 연장. 시간은 DB 설정([데이터관리] 타이머 카드에서 0.5~24시간 조절). 서버 재시작 시엔 전원 유예(끊김 방향 아님)
- [🔴 버그수정] **직원 삭제 FK 오류 → 퇴사 처리(soft-delete)** — 기안서류 결재 기록이 있는 직원도 퇴사 처리 가능(기록 보존 원칙). 퇴사 = 로그인 불가+목록·조직도·선택 목록에서 숨김, 과거 기안·audit 이름 보존, [재입사]로 복구. 사용자 관리에 퇴사자 회색 표시

## v5.9.89 (2026-07-26 — 🎨 중간발주 디자인: 수량 1,000+ 잘림 수정 + 화면·캡처 이미지 리디자인)
- [UI 수정] **중간발주 수량 입력칸 1,000개 이상 잘림** — 고정 58px 폭이라 4자리+스피너가 잘리던 것 → 84px로 확대 + tabular-nums (CSS만, 집계 로직 무수정)
- [UI] **중간발주 화면 목록·거래처 발송 캡처 이미지 리디자인** (theme.css 한 블록, 원복=블록 삭제):
  - 품목별 색 구분 유지(qtyCategory: 노랑=기본·주황=호박·파랑=블러드오렌지·초록=자몽·흰=미매칭) — 원색 배경(#FFFF00 등) → **파스텔 배경 + 진한 색 왼쪽 띠 7px**로 구분이 더 잘 보이게
  - 캡처 글씨 맑은 고딕 → **Pretendard**(대표 지시), 수량 칸 흰 배경·굵게·tabular-nums
  - 합계 행 노랑 → **인디고 강조 + '합계' 라벨**(CSS content), 바깥 테두리 2px 먹색
  - 화면 목록 행: 둥근 8px 카드형 + 색 띠, 체크박스 인디고
- 캐시: theme.css v=11 (app.js·styles.css 무수정)

## v5.9.85 (2026-07-26 — 🎨 디자인 통일: 문의 관리 탭바 + 정산현황 인디고 리스킨)
- [UI] **문의 관리 탭 3개(시나리오·판매현황가격·무응답 현황)** — 일반 버튼 → 정산관리와 동일한 탭바 컴포넌트(`.settlement-tab-bar`) 재사용. 클릭한 탭에 인디고 밑줄+글자색 활성(불 들어옴). 탭 전환 로직 무수정(클래스 문자열만 교체)
- [UI] **정산관리 > 정산현황** — 구 남색(#1B3A6B)/골드(#F5C800) 디자인을 인디고 테마 토큰으로 통일. theme.css 맨 아래 **한 블록** 오버라이드(원복 = 블록 삭제): 헤더·날짜배너·총합계 카드 = 인디고 그라데이션(정산 히어로 카드와 동일 톤), 달력 선택일·데이터 점 = 인디고, 카드 헤더 = 옅은 그라데이션+먹색 글자(전역 표 헤더와 통일), 합계행·저장 버튼 = 인디고, 양수/음수 = 토큰 초록/빨강, 그림자 = 전역 규격. 계산·저장·달력 로직 무수정
- 캐시: theme.css v=10, app.js v=284

## v5.9.83 (2026-07-26 — 🔙 노랑 스킨 원복 → 인디고 확정)
- [롤백] v5.9.81~82 노랑 스킨 전체 제거 (대표 결정: "보라가 낫다") — theme.css 스킨 블록 삭제, 앱 제목줄(#4F46E5)·manifest(#F5A623) 원복. **인디고/화이트 테마 확정**

## v5.9.82 (2026-07-26 — 🍋 스킨 2탄: AGENT OFFICE·앱 제목줄·라인까지 노랑)
- [UI] **AGENT OFFICE 노랑 버전** — 사무실 탭·⚙️ 관리 칩·대장존·수신함·책상·마루 예고 카드·보고서(거래처 헤더·총계 바는 먹색)·조직도 흐름선(짙은 노랑 #DBA800, 가시성 확보)·실행중 이름표
- [UI] **앱 제목줄/브라우저 상단색** 인디고 → 회사 노랑(#F7F008) (theme-color·manifest)
- 사이드바 활성 메뉴 왼쪽 바도 노랑으로 마감

## v5.9.81 (2026-07-26 — 🍋 브랜드 컬러 스킨 시험판: 아꼼이 옐로 85 + 딥블루 15)
- [UI] 포인트 컬러 인디고 → **회사 노랑(#F7F008) 중심 스킨** (대표 지시·확인 대기). 원칙: 노랑은 바탕+먹색 글씨(가독성), 파랑(#1D4ED8) 15%는 링크·정보·당직 포인트
- 적용: 사이드바 활성 메뉴·주 버튼·토글 선택·달력 선택일/오늘·일정 브리핑·주간 오늘 카드·정산 히어로 카드(노랑 그라데이션)·기안 스텝퍼·표 행 호버(옅은 노랑)·당직 배지(파랑)·순위 개선 화살표(파랑)
- theme.css 맨 아래 한 블록으로만 구현 — **맘에 안 들면 블록 삭제로 즉시 인디고 원복**. 상태색(초록/빨강/앰버)·행사 보라·상품 초록은 유지

## v5.9.88 (2026-07-26 — 카페24 토큰 자동갱신 버그 수정 + 텔레그램 알림 문구 편집)
- [🔴 버그수정] **카페24 토큰 자동 갱신 실패** — 원인: 카페24 `expires_at`이 타임존 표기 없는 KST 문자열(공식 문서 실증)이라 UTC 서버가 **만료를 9시간 늦게 감지** → 갱신 시도 자체가 없었음. 수정: ①KST(+09:00)로 해석(parseKstTs — 만료 60초 전 선제 갱신 정상화) ②**401 수신 시 강제 갱신 후 1회 재시도**(이중 방어). 연결테스트의 만료시각 표시·재승인 판정도 같은 파싱 사용
- [기능] **텔레그램 알림 문구 편집** — 타이머 카드 알림 설정에 종류별(신규주문/취소·반품·교환/문의/정산) ✏️ 문구 편집 추가. `{{건수}}` `{{유형}}` `{{시작일}}` 등 변수 치환(모르는 변수는 원문 유지 — 알림 누락 없음), DB 저장, 빈칸 저장 = 기본 문구 복원. 기본값은 기존 문구 그대로. 오류 알림은 편집 대상 아님(항상 ON·고정 문구)

## v5.9.87 (2026-07-26 — 송장변환 직원 권한 개방 + 진행 표시 개선)
- [권한] **송장변환 조회 4종을 직원도 사용 가능** — 네이버·쿠팡·자사몰 자동 불러오기(중간발주 포함) + 쿠팡 취소 재확인. 전부 읽기 전용 조회(발주·발송은 판매자센터 수기)라 위험 기능 아님. 사용 기록은 audit에 계정별로 남음. **연동 승인·연결테스트·타이머·알림 설정 등 관리자 전용은 그대로 유지**
- [UI] **진행 표시 4곳** — 자동 불러오기 3채널·중간발주에 단계(1/2 조회 → 2/2 변환)·경과초 표시. 중간발주는 채널별 실시간 상태(⏳ 조회 중 → ✅ N건 / ⚠️ 실패). 처리 로직 무수정 — 표시만. (서버가 페이지 순회를 한 요청으로 처리해 페이지 단위 실시간 표시는 불가 → 경과초+단계로 대체)

## v5.9.86 (2026-07-26 — 중간발주 3채널 확장 + 무응답 탭 디자인 정리)
- [기능] **중간발주 3채널** — [중간발주 시작하기]가 네이버+쿠팡+자사몰 배송준비를 한 번에 불러와 합산(각 채널 자동 불러오기와 같은 API·실행 시점 조회·읽기 전용). 기존 네이버 로직·합산(recomputeQtyAggregate)·품목 매칭(matchProduct) 무수정 — 쿠팡·자사몰 rows를 옵션정보·수량 공통 키로 변환해 합치기만
- [안전] 조회 기간은 채널별 API 상한 자동 준수(쿠팡 31일·자사몰 90일 — 임의 초과 호출 없음). 한 채널 실패 시 나머지로 진행 + ⚠️ 실패 채널 표시(전 채널 실패 시에만 중단). 채널별 건수 표기(네이버 N · 쿠팡 N · 자사몰 N)
- [UI] 무응답 현황 탭 디자인 가이드 적용 — 통계는 브리핑 스트립+칩(sch-brief), 필터는 form-input, 빈 화면은 empty-row (대표 지시)

## v5.9.84 (2026-07-26 — 텔레그램 알림 설정 화면 관리)
- [기능] 데이터관리 자동수집 타이머 카드에 **🔔 텔레그램 알림 설정** — 신규주문/취소·반품·교환/문의/정산 종류별 ON/OFF(수집과 별개로 알림만 제어). **오류 알림은 끌 수 없음(항상 ON — 안전장치)**
- [DB] 설정은 `agent_office_config` 'telegram_alert_settings' 저장(코드 하드코딩 없음, 주기 설정과 같은 원리). 기본 전부 ON — 명시적으로 끈 것만 OFF, 설정 조회 실패 시에도 발송(안전 방향)
- 문의 관리: 새 시나리오 4종 등록(선물포장·청귤 수확기간·황금향 시즌·밤호박 조리후숙 — 대표 확정 문구, 바이트 일치 검증) — DB 등록이라 배포 무관, 봇 5분 내 반영

## v5.9.80 (2026-07-26 — 송장변환 조회기간 통일 + 무응답 현황 탭 이관)
- [기능] **[문의 관리] 3번째 탭 📭 무응답 현황** — 톡톡봇 /unmatched 화면 이관: 미매칭 SKIP 목록(기본)·전체/미답변/답변 필터·최근 7일 자동응답률·미매칭 TOP·기간/날짜/품목/검색 필터·엑셀 다운로드. 시나리오명·응답출처(AI 선택/가격즉답) 열 추가(신규 기록분부터 표시)
- [개선] **송장변환 조회기간 통일** — 스마트스토어·자사몰 기본 **50일**, 쿠팡은 API 상한에 맞춰 **31일**(v5 발주서 조회 최대 31일 — 문서 확인, 임의 초과 호출 없음). 중간발주도 50일
- [DB] message_logs에 scenario_name·response_source 컬럼(봇/회사프로그램 양쪽 멱등 생성 — 배포 순서 무관)
- 톡톡봇 측(별도 리포): 사진요청 문구 제거를 "📸 시작 줄" 규칙으로(문의 관리에서 문구 수정해도 자동 추종) + 시나리오명 기록 + /unmatched 은퇴 스위치(UNMATCHED_PAGE, 검증 후 off)

## v5.9.79 (2026-07-26 — 모바일 정리 + 네이버 정산 빠른 조회)
- [기능] 네이버 일별 정산 조회 — **[당일][일주일][한달] 빠른 조회 버튼**(누르면 기간 채우고 바로 조회) + "⚠️ 조회 기간은 최대 1개월 이내" 안내 문구 추가
- [UI/모바일] **월간 상단 2줄 정리** — 1줄: 제목+새로고침, 2줄: [주간|월간|연간]·[오늘]·[< >] 균등 배치. 잔여연차·당직일수 배지 한 줄로, 유형 필터 알약 컴팩트 (제목 두 줄로 감기던 문제 해결)
- [UI/모바일] **주간 뷰 깨짐 수정** — 긴 일정 제목이 카드 폭을 밀어내던 원인(min-width) 차단 + 폰에서는 글씨 축소·긴 제목 줄바꿈으로 칸에 딱 맞게 (대표 지시: 글씨 작아도 칸 우선)
- [UI/모바일] 표 안쪽 스크롤 높이 72%→**60%** (위 필터·요약이 더 보이게). 연간 뷰·정산 달력은 현행 유지(대표 확인: 좋음)

## v5.9.78 (2026-07-26 — 정산관리 편의 2건)
- [개선] 정산 달력 날짜 모달 — **완료/결제완료 체크해도 창이 닫히지 않음**(갱신된 상태로 그 자리에 유지). 대성·효돈·CJ 연속 체크 후 [닫기] 한 번이면 끝 (기존: 체크마다 창이 꺼져 재진입 필요)
- [개선] 정산 데이터 목록 상세 — 합계 줄에 **합계 수량** 표시(수량 칸 아래), 수정모드 실시간 계산에도 반영

## v5.9.77 (2026-07-26 — 표 스크롤 방식 통일: 필터·집계 라인은 남고 표만 스크롤)
- [UI] **표 14곳을 스크롤 래퍼로 통일** — 박스재고 전체 입출고처럼 필터(시작일·종료일 등)와 집계 라인(업체 입고/시온 이동/…)이 화면에 계속 남고, **표만 그 아래(화면 72% 높이)에서 스크롤**되며 제목줄 고정. 대상: 박스재고 입출고, 업무일지, 기안서류 목록·승인완료, 연차 조회·수기부여, 정산 목록, 선지급, 품목별 금액, 문의관리 시나리오·판매현황, 자동수집 타이머, 네이버 정산 등
- 데이터 로드 전 빈 래퍼는 테두리 숨김. 기능 무수정(HTML 래핑+CSS)

## v5.9.76 (2026-07-26 — 대표 피드백: 일정 글씨 확대 + 전체 표 헤더 고정)
- [UI] 일정 **주간 뷰 확대**(날짜 26px·일정 칩 14.5px·여백 업) · **연간 뷰 확대**(월 이름·날짜·점) · **월간 칩 소폭 확대**(11→12px). 모바일은 기존 축소 비율 유지
- [UI] **모든 data-table 표 헤더 고정(sticky)** — 순위관리 일자별 기록·박스재고·품목별 금액·문의관리·지출조회/이력·카드내역·연차현황 등 스크롤해도 제목줄이 위에 붙어 있음. 긴 표는 화면 72% 높이 안에서 스크롤(엑셀 틀고정 느낌), 짧은 표는 변화 없음

## v5.9.75 (2026-07-26 — 디자인 3차: 메뉴별 마감)
- [UI] **정산관리(C-1)** — 기존 요약 카드 재스킨: 총 결제예정금액은 인디고 히어로 카드, 거래처 카드는 둥근 테두리+그림자, 금액 자릿수 정렬
- [UI] **기안서류(C-3)** — 상세 모달에 결재 스텝퍼(작성→대표 승인→완료 / 반려 시 빨강 표시)
- [UI] **박스재고(C-4)** — 박스 카드에 보관처 분포 바(업체 빨강·대성 초록·효돈 주황 + 범례). 실제 수량 비율 그대로, 임의 기준선 없음
- [UI] **CS처리방(C-5)** — 템플릿 내용을 고객이 받는 말풍선 모양으로
- [UI] **문의관리·데이터관리(C-6)** — 시나리오/자동수집 타이머 ON/OFF 체크박스를 토글 스위치로(기능 동일)
- 송장변환(C-2)은 대표 지시대로 틀 유지 — 공통 시스템(버튼·표·서체·달력)으로 이미 정리 완료 확인. 기능 로직 무수정, 시안 v1 3차 완료

## v5.9.74 (2026-07-26 — 디자인 2차: 공통 시스템)
- [UI] **상태 배지 통일(B-1)** — 기안서류 승인/대기/반려를 파스텔+색점 배지로, 문의관리 사용중/타이머 작동중·꺼짐/판매현황 준비중 인라인 스타일을 공통 `.pill` 클래스로 교체 (시안 승인분)
- [UI] **빈 화면(B-3)** — "기록이 없습니다"류 빈 표·알림에 아이콘+여백 안내 스타일
- [UI] **로딩 스켈레톤(B-4)** — 공통 `.skel-line` 유틸 추가, 일정 연간 뷰 로딩에 우선 적용(다른 화면은 3차에서 화면별 적용)
- 버튼 위계(B-2)는 v5.9.73의 btn-outline/btn-secondary 정의로 완료. 기능 로직 무수정

## v5.9.73 (2026-07-26 — 카페24(자사몰) 송장변환 연동: 3채널 완성) ※ 아래 '글씨체·버튼' 작업과 같은 버전으로 동시 배포됨(세션 2개 병행)
- [기능] 송장변환 자사몰 카드에 **[🏠 자사몰 자동 불러오기(배송준비중)]** — 카페24 N20 주문을 OAuth API로 불러와 기존 통합변환에 주입(변환·품목 매칭 무수정). 조회 1~90일(기본 3). **스마트스토어·쿠팡·자사몰 3채널 자동화 완성**
- [🛡️ 취소 방어] **품목 단위 상태 필터(N20만)** — 취소신청 품목은 상태가 바뀌어 자연 제외(결제 즉시 N20 자동 진입 몰 특성 대응) + **수량 = 주문수량 − 취소요청수량**(0 이하 제외)
- [인증] 카페24 OAuth — [연동 승인] 1회(대표 [동의]) → 토큰 자동 갱신(access 2시간/refresh 2주, 동시 갱신 락). 토큰은 **AES-256-GCM 암호화 저장·값 미노출**(상태·만료시각만 표시). 갱신 실패 시 텔레그램 + "재승인 필요" 표시. Secret은 Render 환경변수 `CAFE24_CLIENT_SECRET`(대표 직접 입력)
- [화면] 데이터관리 🏠 카페24 연동 카드([연동 승인]·[연결 테스트] — Secret/토큰/왕복 3줄). IP 화이트리스트 없음 확인 → 중계서버 불필요(Render 직접 호출)
- 주문 조회 외 API 호출 코드 없음 · 상태 처리/발송 자동화 없음 · 반품 로직 없음

## v5.9.73 (2026-07-26 — 글씨체 전체 통일 + 기본 모양 버튼 60개 수정)
- [🔴 수정] **`.btn-outline`(54곳)·`.btn-secondary`(6곳) CSS 정의 누락** — 엑셀 다운로드·적용·닫기 등 버튼들이 브라우저 기본 회색 모양으로 나오던 원인. 공통 아웃라인 버튼(흰 배경·둥근 모서리·호버 시 인디고)으로 전 메뉴 일괄 통일 (대표 스크린샷 제보)
- [UI] 화면 콘텐츠 글씨체를 **Pretendard로 완전 통일** — 스마트스토어 정산 구역 5곳의 'Noto Sans KR' 강제 지정(미로드 → 시스템 기본체로 튀던 원인) 제거, body 기본 폰트 스택도 Pretendard 우선으로
- 예외(의도적 유지): ①AGENT OFFICE 로그·프롬프트 콘솔(고정폭 Consolas — 코드/로그 가독성) ②거래처 발송용 중간발주 캡처 이미지·기안서류 인쇄물(맑은 고딕 — 받는 쪽 문서 모양 변경 방지, 원하면 통일 가능)

## v5.9.72 (2026-07-26 — 일정 메뉴 업그레이드: 주간/월간/연간 뷰 + 오늘 브리핑, 디자인 1차)
- [기능+UI] **주간|월간|연간 뷰 토글** (대표 아이디어) — 주간: 7일 세로 카드(오늘 인디고 강조·체크박스 그대로·카드 클릭=기존 일정 모달), 연간: 12개월 미니 달력+유형색 점(월 클릭→해당 월간). 월간이 기본, 기존 화면 그대로. < > 이동은 뷰에 따라 주/월/년 단위, [오늘] 버튼으로 즉시 복귀
- [UI] **오늘 브리핑 스트립** — "📌 오늘 일정 N건 · 🎉 이번 주 행사 N건 · 🌙 오늘 당직 이름" (1분 캐시)
- [UI] 월간 스킨 — 오늘 셀 인디고 링+배지, 유형 파스텔 칩(기존 색상 언어 유지: 행사 보라·상품 초록·당직 인디고·휴가 티일), 일정 4건 초과 시 "+N건 더보기"(셀 클릭=기존 모달에서 전체), 달력 보더·헤더 소프트 톤
- 일정 등록·수정·체크·필터 로직 무수정(디자인 시안 v1 A-1~A-4 승인분, 1차 배포). 데이터는 기존 월별 API 재사용

## v5.9.71 (2026-07-26 — 커스텀 달력·표 디자인 전 화면 일괄 적용)
- [UI] **커스텀 달력(akm-cal) 전 화면 확대** — 기안서류(기간·수정모달)·연차 조회/수기부여·지출결의(사용일·조회기간)·카드내역 기간·CJ 이월 기간·정산 입력일·선지급일·스마트스토어 날짜·품목별 금액 적용기간·D-day·박스재고 입출고 기간·네이버 정산 조회·AGENT 리포트 기간 + 동적 모달(일정 등록·기안 수정·박스 이동·정산 날짜) = **총 33곳** 브라우저 기본 달력 → 회사 테마 달력으로 교체. 달력은 class 위임 방식이라 모달 안에서도 자동 작동
- [UI] **표 공통 디자인 일괄 적용** — 전 화면 `.data-table`(정산관리·기안서류·연차현황·지출결의·카드내역·박스재고·품목별 금액·문의 관리·타이머 카드 등)에 순위관리 일자별 기록과 같은 디자인: 둥근 테두리+은은한 그림자·그라데이션 헤더·행 호버·부드러운 구분선. 품목별 금액 격자·색상 행은 유지(이중선만 정리)
- 날짜 값 형식(YYYY-MM-DD)·저장/조회 로직 무수정. 모바일 정산 표 full-bleed 해제(테두리 박스 유지)

## v5.9.70 (2026-07-26 — 순위관리 커스텀 달력)
- [UI] 순위관리 날짜 입력 3곳(기록 날짜·추이 시작/종료)의 **브라우저 기본 달력 → 회사 테마 커스텀 달력(akm-cal)** 교체 — 흰 카드·둥근 모서리·떠 있는 그림자, « ‹ 연/월 이동 › », 요일(일 빨강·토 파랑), 오늘=인디고 테두리, 선택일=인디고 채움, 하단 [오늘] 버튼, 입력칸에 달력 아이콘
- 값 형식은 기존과 동일한 YYYY-MM-DD → 저장·조회·카톡 날짜 자동추출 로직 무수정. 다른 화면 날짜 입력은 그대로(순위관리만 적용)

## v5.9.69 (2026-07-26 — 순위관리 일자별 기록 표 정렬·디자인 개선)
- [수정] 쇼핑·광고·파워링크 칸 밀림 해결 — 헤더(왼쪽)와 본문(가운데) 정렬이 어긋나고, 증감(▲▼) 유무에 따라 숫자 위치가 행마다 달라지던 문제. **열 폭 고정(table-layout: fixed) + 순위 숫자·증감 표시 폭 고정**으로 세로 정렬 일치
- [UI] 표 디자인 리뉴얼(회사 테마 인디고/화이트) — 둥근 테두리+은은한 그림자(입체감)·헤더 옅은 그라데이션·행 호버 강조·쇼핑 순위 굵게·▲ 인디고/▼ 빨강·빈 값 옅은 '-'
- 순위관리 일자별 기록 표만 변경 — 순위 저장/삭제·차트·다른 화면 무수정
- [보정] 쿠팡 키 재발급 일정(2027-01-07)을 **대표 계정으로 시드** — user_id 없음으로 메인 달력에 안 보이던 것 수정(기존 시드도 자동 보정, 리뷰 지적 반영)

## v5.9.68 (2026-07-26 — 쿠팡 송장변환 연동: 상품준비중 자동 불러오기)
- [기능] 송장변환 쿠팡 카드에 **[🛒 쿠팡 자동 불러오기(상품준비중)]** — Wing 발주확인 후 상품준비중 주문을 API로 불러와 기존 통합변환에 주입(변환·품목 매칭 로직 무수정). 조회 일수 1~31일(기본 3)
- [🔴 수량] **발주 가능 수량 = 주문수량 −(취소예정+취소확정)** — 쿠팡 공식 문서 명시 공식 적용(부분취소 반영), 0이면 행 제외. "부분취소 수량 반영 N건" 표시
- [🛡️ 안전장치] [통합 변환] 클릭 시 **쿠팡 취소요청(출고중지요청) 재확인 → 자동 제외** (쿠팡은 상품준비중에도 취소요청 존재 — 네이버와 다른 정책). 실패 시 변환 진행+경고
- [인프라] 중계서버 /coupang 경로 신설(HMAC-SHA256 서명·vendorId 서버 강제·**조회 2종만 허용**, 상품·가격 API 호출 코드 없음) — install.sh 재실행 필요. 키는 NCP .env에만(절차서 docs/쿠팡_키입력_절차.md)
- [화면] 데이터관리 🛒 쿠팡 연결 카드(3줄 초록불). [일정] 2027-01-07 쿠팡 API 키 재발급 알림 자동 등록(1/21 만료)
- 발주확인·발송처리는 대표 수동 유지. 네이버 연동 코드 무수정

## v5.9.67 (2026-07-26 — 판매현황 삭제 보호 확대: 미래 단가표 포함)
- [수정] 판매현황 품목의 '연동(삭제 불가)' 판정을 **오늘 유효 단가표 → 오늘+미래 유효 단가표**로 확대 — 다음 주 단가표에 올린 품목이 당일에 '예외'로 보여 삭제 버튼이 뜨던 빈틈 보완(테스트 품목 사례, 대표 확인). 지난 주에만 있던 품목은 계속 삭제 가능
- 프론트 무변경(캐시 v=271 유지) — deletable 플래그·삭제 API 둘 다 같은 판정 사용

## v5.9.66 (2026-07-26 — 정산 조회 표 재설계)
- [UI] 숨김: 정산 예정일·가맹점 ID·계좌번호·가맹점명(대표 지시). 남은 영어 번역(예금주 등)
- [UI] **정산 기준 시작=종료일이면 '정산 기준일' 한 칸으로 병합**(일별 조회 시 칸 절약)
- [UI] 표 디자인: 금액 우측 정렬·**마이너스 빨강**·정산 금액 굵게·줄무늬 행·둥근 테두리·헤더 고정 + **맨 아래 합계 행**(기간 총 정산액 한눈에)

## v5.9.65 (2026-07-26 — 문의 관리 UI 다듬기)
- [UI] 시나리오 목록 '사용' 체크에 **🟢 사용중 / ⏸ 꺼짐 배지**(데이터관리 타이머와 동일 스타일)
- [UI] **[+ 새 시나리오] 편집 카드를 목록 위(상단)로 이동** — 46번 아래로 열리던 것을 맨 위에서 바로 작성

## v5.9.64 (2026-07-26 — 정산 조회 한글 완성 + 0원 칸 자동 숨김)
- [UI] 일별 정산 조회 뒤쪽 영어 필드까지 **공식 문서 title로 전부 한글화**(공제 환급·지급 보류·마이너스 충전금 상계·차액 정산·반품안심케어·일반/빠른정산·우대 수수료 환급·한도 보류/해제·정산 방법·은행). 정산 방법(계좌 이체/충전금)·은행 코드도 한글 표기
- [UI] **값이 전부 0원/빈값인 칸 자동 숨김**(칸 낭비 정리 — 값이 생기면 자동으로 다시 표시, 숨긴 개수 하단 안내)

## v5.9.63 (2026-07-26 — 문의 알림 개선: 미답변 신규만 + 유형 분해)
- [개선] 문의 수집 텔레그램 알림 — **미답변 신규만** 알림(이미 답변된 옛 문의는 저장만·알림 제외 — "새 문의"로 오인하던 문제 해결). 형식: "💬 고객문의 미답변 신규 N건 (상품 1·배송 1)" + 반품·교환·환불 유형이면 "클레임 관련 문의 글" 꼬리표(취소반품 타이머 알림과 구분)
- 저장 범위는 전 유형·답변 포함 그대로(자동답변 재료). 프론트 무변경(캐시 v=268 유지)

## v5.9.62 (2026-07-26 — 타이머 상태 표시 + 정산 조회 한글 헤더)
- [UI] 자동수집 타이머: ON이면 **🟢 작동중**, OFF면 ⏸ 꺼짐 배지 표시 (켜졌다는 느낌이 확실하게)
- [UI] 네이버 일별 정산 조회: 영어 필드명 → 한글 헤더(정산 기준 시작/종료일·정산 예정/완료일·정산 금액·수수료(차감)·혜택·할인 부담(차감) 등, 모르는 필드는 원문 유지)

## v5.9.61 (2026-07-26 — 고객문의 자동수집 구현: 공식 문서 확정 기반)
- [기능] 문의 타이머 '지원 예정' 스텁 → **고객문의 수집기 구현** — `GET /v1/pay-user/inquiries`(문서 확정: startSearchDate/endSearchDate 필수·size 10~200·최대 365일). 최근 3일 창, 30분 주기(기본 OFF 유지)
- [개인정보 A안] 저장 = 유형·제목·내용(**연락처/이메일 자동 마스킹**)·상품번호/상품명/옵션·등록일·답변여부만. **구매자 이름·ID 저장 안 함**. 텔레그램엔 건수만. **90일 경과분 자동 정리**
- [확정] 고객문의(pay-user)와 **상품문의 Q&A(contents/qnas)는 별개 API** — 상품문의 수집·자동답변은 중계서버 허용목록 추가(SSH) 후 3번 프로젝트에서
- 프론트 무변경(캐시 v=267 유지)

## v5.9.60 (2026-07-25 — 네이버 자동수집 타이머 [B] + 송장변환 직전 취소 재확인)
- [기능] **자동수집 타이머 4종** — 정산(하루 1회, 기본 09:30)/주문(60분)/취소·반품(90분)/문의(30분). **전부 기본 OFF**, 설정은 DB(naver_auto_collect)만 — 재시작해도 유지, 주기·시각 화면에서 변경. 60초 틱에 **가장 밀린 수집기 1개만** 실행(rate limit 몰림 방지), 429 백오프 재사용, 실패 시 텔레그램(정상→실패 전환 시 1회)
- [기능] [데이터관리]에 **⏱️ 자동수집 타이머 카드** — [전체 ON/OFF] + 개별 ON/OFF·주기(분)/실행시각·마지막 수집·✅/❌. [연결 테스트]는 그대로(진단용)
- [동작] 정산=최근 3일 스냅샷 저장+텔레그램 요약 / 주문=신규 결제 건수 알림만(발주확인 수기) / 취소·반품=알림만(자동처리 X) / **문의=API 파라미터 문서 확정 전까지 '지원 예정'**(켜면 안내 후 실패 표시 — 꺼 두세요)
- [🛡️ 안전장치·확정] **변환 직전 취소 재확인은 넣지 않음**(대표 확정 — 취소·반품은 배송준비와 무관, 취소는 PAYED에서 자동 이탈). 안전장치 = **[자동 불러오기]가 항상 실행 시점에 새로 조회**(타이머 수집분은 변환에 재사용 안 함 — 현황·통계용). 취소·반품 판정(claimType/claimStatus)·변경분 페이지네이션 교정은 타이머 알림에 반영
- 전부 읽기 전용 — 발송처리·답변등록 등 쓰기 없음. 중계서버 재실행 불필요

## v5.9.59 (2026-07-25 — 판매현황 삭제 규칙 정교화: 예외 품목만 삭제 가능)
- [변경] 삭제 규칙(대표 지시): **품목별 금액과 매칭되는 연동 품목 = 삭제 버튼 없음**(상태로만 관리) / **예외·수동 추가 품목(사전예약특가 등 단가표에 없는 것) = 대표만 삭제 가능**. 매칭 여부는 오늘 유효 단가표 기준 실시간 판정(단가표에 이름이 추가되면 자동 보호)
- [기능] [+ 품목 추가 (예외)] 버튼 부활 — 단가표에 없는 예외 상품 등록용. 서버 삭제 API도 동일 규칙으로 재개(연동 품목 삭제 시도 시 400 안내)
- ★사전예약특가★ 청귤 2종 복구·가격 세팅(18,800/29,800) + 대표 가격표 기준 판매가 23종 일괄 세팅 완료(중대과 49,800 포함)

## v5.9.58 (2026-07-25 — 판매현황 품목 삭제 금지 + 미매칭 일괄 정리)
- [변경] **판매현황·가격 탭 삭제 버튼 제거 + 삭제 API 차단**(대표 지시 — 실판매 상품이므로 삭제 불가). 판매 중지는 상태(품절/시즌종료)로만 관리
- [정리] 품목별 금액과 **미매칭 품목 34개 일괄 soft-delete**(복구 가능·audit 기록) — 유지 21개 = 오늘 유효 단가표 품목명과 일치분(최상품 청귤 5/10kg는 가격 유지). ⚠️ ★사전예약특가★ 청귤 2종도 미매칭이라 삭제됨 — 계속 판매 시 복구 요청
- 톡톡봇 구 /products 페이지 삭제 버튼도 제거(미push 커밋). 삭제된 품목이 봇 답변에서 빠지는 건 봇 push 이후

## v5.9.57 (2026-07-25 — 품목별 금액 → 판매현황 자동 연동)
- [기능] 품목별 금액(결제가) 저장 시 **새 품목명이 판매현황(판매가) 탭에 '준비중'으로 자동 등록** — 판매가는 빈칸(수기 세팅). ⚠️ 결제가·판매가는 이름만 공유, 금액은 절대 복사 안 함
- [기능] **'준비중' 상태 신설 = 봇 답변 미노출**(봇은 판매중/품절/시즌종료만 표시 — 봇 코드 무수정). 판매가 입력 + [판매중] 클릭 시 손님에게 노출. 준비중 행은 목록 최상단 + "가격 미세팅·봇 미노출" 배지
- ON CONFLICT DO NOTHING — 기존·삭제된 품목은 주간 단가 재등록으로도 부활하지 않음. 자동 등록은 audit(pricing-sync) 기록
- 오늘 유효한 품목별 금액 품목명 백필 1회 실행(19개 준비중 등록)
- [변경] 탭2 수동 [+ 품목 추가] 버튼 제거(대표 지시) — 품목 유입은 품목별 금액 연동으로 일원화

## v5.9.56 (2026-07-25 — 판매현황·가격 관리를 [문의 관리] 탭으로 이전)
- [기능] [문의 관리]를 탭 구조로 개편 — 탭1 **시나리오**(기존 47건 그대로) / 탭2 **판매현황·가격**(🏷️ 스마트스토어 판매가 — 품목별 금액(거래처 결제가)과 별개 데이터임을 화면에 명시)
- [기능] 탭2: 톡톡봇 가격표(bot_products, 같은 DB 공유 36건) 목록·상태 토글(판매중/품절/시즌종료)·가격 입력·품목 추가. **저장하면 톡톡봇 답변({{가격표}}·{{판매현황}})에 1분 내 자동 반영**(봇 무수정). 직원 편집 가능·삭제는 대표 전용(soft-delete)·audit_logs 이력
- [API] `/api/agent-office/bot-products` CRUD + 이력. soft-delete 동명 재추가 시 복구+덮어쓰기
- 다음: 톡톡봇 정리 배포(시즌 시드 하드코딩 제거 — "삭제해도 부활" 함정 박멸 + deleted_at 필터 + /products 페이지 PRODUCTS_PAGE 스위치). 그 전까지 기존 /products 페이지도 정상 동작(이중 상태)

## v5.9.55 (2026-07-25 — 문의시나리오 DB 통합 1단계: [문의 관리] 화면 + 시나리오 DB·API)
- [기능] **[문의 관리] 메뉴 신설**(사이드바 CS처리방↔데이터관리 사이) — 톡톡봇 시나리오를 화면에서 직접 편집. 목록(번호·이름·채널·동작·사용토글)·새 시나리오·편집폼·"고객에게 이렇게 보입니다" 미리보기·수정 이력. **직원 편집 가능**, 삭제·전체 자동응답 ON/OFF는 대표 전용
- [DB] `inquiry_scenarios` 테이블 신설 + 톡톡봇 시나리오 **47건 이관 완료**(46개 + 영업시간외, 바이트 단위 대조 확인). soft-delete·audit_logs 기록(source: inquiry-scenario)
- [API] 톡톡봇 조회용 `GET /api/scenarios`(신규 Bearer `SCENARIO_API_TOKEN`, 상수시간 비교) + 관리 CRUD 5종 + 이력 조회. ⚠️ Render 환경변수 `SCENARIO_API_TOKEN` 등록 필요(톡톡봇에도 동일값)
- 톡톡봇은 아직 기존 하드코딩 그대로(무중단) — 비교 검증 후 `SCENARIO_SOURCE=db` 스위치 예정. 기존 기능(품목·정산·송장변환·네이버 연동) 무영향

## v5.9.54 (2026-07-25 — 부분취소 수량 반영: quantity → remainQuantity)
- [🔴 수정] 4박스 주문 후 2박스 부분취소 시 배송준비엔 2박스인데 불러오기는 **4박스로 나오던 오류**(김혜경 고객 사례) — 네이버 `quantity`는 **최초 주문 수량**이라 취소 반영 안 됨. 공식 구조체의 **`remainQuantity`(남은 수량)** 우선 사용, 없으면 quantity(기존 주문은 동일 동작). 남은 수량 0이면 그 주문 제외
- [표시] 송장변환·중간발주 결과 메시지에 **"부분취소 수량 반영 N건"** 표시(검증용)
- 중간발주도 같은 API rows 사용이라 **자동 연동 수정**. 변환 로직 무수정 · 중계 재실행 불필요

## v5.9.53 (2026-07-25 — 중간발주도 네이버 API 직결: 파일 업로드 → [중간발주 시작하기])
- [기능] 송장변환 > 중간발주 탭 — **비번(4031) 엑셀 업로드 없이** [📦 중간발주 시작하기] 버튼으로 네이버 배송준비 전체를 바로 불러와 품목별 합계 집계. 조회 기간(일) 입력(기본 40·최대 180). 안내 문구 "현재 스마트스토어 배송준비로 중간발주를 진행합니다"로 교체
- API rows가 옵션정보·수량 키 그대로라 기존 합산(recomputeQtyAggregate)·거래처 필터·품목추가·이미지 저장 **무수정** 동작. 업로드 영역·파일 input·복호화 호출 제거(서버 /api/invoice/decrypt는 존치)
- 참고: 불러오기 API는 관리자 전용(기존 정책 동일) — 직원 계정에선 버튼 사용 불가

## v5.9.52 (2026-07-25 — 배송준비 불러오기 하루 300건 잘림 수정: 페이지 순회 방식 교체)
- [🔴 수정] 배송준비 1298건 중 **999건만** 오던 문제(기간 40→180일 늘려도 동일) — 원인 추정: 응답의 totalPages **필드명이 예상과 달라 1페이지로 오판** → 주문 몰린 날(예약 오픈일)은 하루 300건에서 잘림. → totalPages를 믿지 않고 **"300건 꽉 채워 오면 다음 페이지 있음"** 방식으로 교체(덜 채워 오면 그 날 끝, 상한 50p=하루 15,000건)
- [진단] 🔧 진단에 **페이지 정보**(응답 상위 필드, PII 없음) 표시 — 실제 필드명 확인용
- 변환 로직 무수정 · 중계 재실행 불필요

## v5.9.51 (2026-07-25 — 네이버 429(호출 제한) 대응: 호출 간격 + 자동 재시도)
- [🔴 수정] 배송준비 전체 불러오기(40일=40회+ 연속 호출)에서 **네이버 초당 호출 제한(429)** 발생 → ①호출 사이 **350ms 간격** ②429면 **1s→2s→4s→8s→16s 점증 대기 후 재시도(최대 5회)** ③재시도 끝에도 실패 시에만 텔레그램 알림 1번(재시도 중 스팸 방지) ④화면 오류 문구: "네이버 호출 제한(429) — 1~2분 뒤 다시 눌러주세요"
- 프론트 무변경(캐시 v=258 유지). 중계 재실행 불필요

## v5.9.50 (2026-07-25 — 배송준비 전체 불러오기: 중복방지 제거 + 조회창 확대)
- [🔴 수정] 배송준비 1299건인데 **183건만** 오던 문제 — 원인: 결제일(PAYED_DATETIME) 기준 **최근 3일만** 조회. 네이버 API는 '현재 배송준비 상태 전체'를 주는 기능이 없고(상태 목록조회 불가), **결제일 기준·1회 최대 24시간·최대 180일**만 가능(공식 문서/개발자포럼 확인). → 24시간씩 여러 날 훑도록 **기본 40일**로 확대(청귤 예약주문 성수기·결제일 한 달 이내분 커버). 화면에서 조회 일수 조절 가능(상한 180)
- [변경] **중복방지("이미 올린 것 제외") 완전 제거** — 중간발주 연동상 매번 '배송준비 전체'를 그대로 넘겨야 함(대표 지시). `naver_invoice_uploaded` 기록/제외 로직 삭제. 신규주문 발주확인은 수기 유지
- [UI] 버튼 2개(자동/전체다시) → **[🛰️ 네이버 배송준비 전체 불러오기]** 하나로 통합 + 조회 기간(일) 입력칸 추가
- 변환 로직(convertDataSmart)·다운로드·수동 엑셀 업로드 무수정. 중계 재실행 불필요

## v5.9.49 (2026-07-25 — 송장 자동불러오기 미매칭 수정: 옵션정보 매핑 + 응답 중첩 방어)
- [🔴 수정] 자동 불러오기 결과가 전부 **[미매칭]·빈값**이던 것 — 원인 2가지: ①`옵션정보`를 상품명+옵션으로 넣었는데 수동 엑셀은 **옵션(productOption)만** 사용(대표 제공 실엑셀 확인: 8열 옵션정보 = "아꼼이네 상품선택: … / 상품 및 과수: 특품 5kg" = 옵션 문자열). → `옵션정보=productOption`(상품명 제외, 수동과 동일)으로 수정 ②네이버 응답 항목이 `content` 래퍼에 중첩돼 필드 추출이 빈값 → content 래퍼 방어 추가
- [진단] 응답 첫 건을 **개인정보 가림 처리**해 구조/옵션 예시를 화면에 노출(🔧 진단) — 실데이터로 매핑 확정
- [추가] **🔄 전체 다시 불러오기(all=1)** 버튼 — 이미 올림 기록 무시하고 재조회(테스트·재발송용). 변환 로직(convertDataSmart)·다운로드 무수정
- 검증: 매핑/에러/허용목록 로컬 검증 유지. 중계 재실행 불필요(회사프로그램만 배포)

## v5.9.48 (2026-07-24 — 네이버 중계 견고화: 403 원인 구분 + 읽기 전체 허용 + 버전 노출)
- [수정] 자동 불러오기 `naver_relay_error_403` — 원인은 중계 허용목록에 조건형 주문조회 경로가 아직 없었음(install.sh 미재실행). **중계 허용목록을 '읽기(GET) 전체'로 확대**(pay-settle·pay-order/seller·pay-user·seller) → 앞으로 새 조회 기능마다 재실행 불필요. 쓰기(발송처리 등)는 여전히 차단(상세조회 query POST만 예외)
- [개선] **403 원인 구분** — 중계 허용목록(재실행 필요) vs 네이버 권한/IP vs Bearer 불일치 vs 연결불가를 각각 명확한 한국어 메시지로(naverFriendlyError). 정산·주문 조회 공통 적용
- [개선] **중계서버 버전(/health)** 노출 + 연결 테스트 화면에 표시 → install.sh 재실행 반영 여부 확인 가능(현재 2026-07-24.2)
- [검증] 로컬: 조건형 주문조회 허용·쓰기 차단·버전 노출·에러매핑 4/4 통과
- ⏳ 대표: install.sh 한 번 더 재실행 → 연결 테스트에서 '중계버전 2026-07-24.2' 확인 → 자동 불러오기 재시도

## v5.9.47 (2026-07-24 — 네이버 4단계[A]: 송장변환 자동 불러오기)
- [신규] **송장변환 스마트스토어 카드에 [🛰️ 네이버 자동 불러오기]** — 배송준비 주문(productOrderStatus=PAYED + placeOrderStatus=OK, 문서 확인값)을 조건형 조회(`GET /external/v1/pay-order/seller/product-orders`, rangeType=PAYED_DATETIME, 24h 순회·페이지네이션)로 가져와 **스마트스토어 엑셀과 동일 컬럼**으로 매핑 → `invoiceDataSmart` 주입 → 기존 [통합 변환] 그대로 동작
- [원칙 준수] **변환 로직(convertDataSmart) 무수정**, **수동 엑셀 업로드 유지**. 옵션정보=productName+productOption(퍼지 매처가 파싱), 배송지=baseAddress+detailedAddress 등 구조체 필드 매핑(문서 확정)
- [중복방지] 이미 자동업로드한 productOrderId는 `naver_invoice_uploaded` 테이블로 제외+기록. 발송처리 시 PAYED에서 빠지는 것과 이중 안전
- [안전] 발주확인·발송처리는 자동화 안 함(대표 수동). 매핑 로직 로컬 7/7 검증. 중계 허용목록에 조건형 조회 경로 추가 → **install.sh 재실행 필요**
- ⏳ 다음: [B] 타이머 자동수집 (실제 주문 응답 구조 확인 후 취소·반품 재확인까지 정확히)

## v5.9.46 (2026-07-24 — 네이버 중계 HTTPS 적용 (주문 PII 대비))
- [보안] **Render ↔ 중계서버 HTTPS 암호화** — 주문 상세엔 고객 이름·전화·주소(PII)가 있어 HTTP 평문 금지(대표 원칙). 중계서버가 자체서명 인증서로 HTTPS 서빙, 회사프로그램은 그 인증서를 **핀(고정, NAVER_RELAY_CA)**해 검증 → 도청·중간자 차단
- [구현] 새 의존성 없이 내장 http/https 모듈로 구현(naver-relay.js). install.sh가 자체서명 인증서(SAN=고정IP) 자동 생성·출력, relay는 인증서 있으면 HTTPS·없으면 HTTP(하위호환). 로컬 검증 4/4(올바른CA 연결·틀린CA 거부·CA없이 암호화)
- ⏳ 대표 조치: 중계 install.sh 재실행(→HTTPS 전환·인증서 출력) → Render 환경변수 NAVER_RELAY_CA(인증서 붙여넣기)+NAVER_RELAY_URL을 https로 → 연결 테스트. 이후 4단계(주문 수집→송장변환)
- 참고: 네이버 호출 한도는 '하루 총량'이 아니라 **초당 rate limit(토큰버킷·계층별)** — 자동수집 시 간격만 두면 됨(자동수집·캘린더는 대표 요청으로 보류)

## v5.9.45 (2026-07-24 — 네이버 커머스API 3단계: 일별 정산 조회)
- [신규] **네이버 일별 정산 조회**(읽기 전용·PII 없음) — 공식문서 확인한 정확한 스펙: `GET /external/v1/pay-settle/settle/daily`, 필수 파라미터 startDate·endDate(yyyy-MM-dd)·pageNumber·pageSize. 이전 404는 경로가 `/settle/day`가 아니라 `/settle/daily`였고 필수 파라미터 누락이 원인
- [신규] 백엔드 `GET /api/agent-office/naver/settlements?from&to`(adminOnly, 페이지 순회 취합, audit 출처 naver-api) + 데이터관리 화면 **📅 네이버 일별 정산 조회**(기간 선택→표, 응답 필드 동적 표시, 금액 원화 포맷)
- [설계] **세미(거래처 결제가 정산)와 별개 '네이버 정산' 영역** — 네이버 실입금 기준, 대조·병합 안 함(대표 확정 원칙)
- [중계] 중계서버 허용목록에 `pay-settle/settle/*`(읽기) 추가 → **install.sh 재실행 필요**. 일별 정산은 PII 없어 HTTP 안전(건별·주문상세는 PII라 HTTPS 후)

## v5.9.44 (2026-07-24 — 네이버 연결 테스트 판정 보강, 2단계 확인)
- [수정] 연결 테스트의 왕복 확인을 **확정된 '변경 주문 조회' 엔드포인트**로 교체(정산 경로는 3단계에서 확정). 판정도 **"네이버가 응답을 돌려주면 도달·인증 성공"**으로 보강 — 네이버 4xx(경로/파라미터 차이)와 실제 연결/인증 실패(중계 연결불가·Bearer 불일치)를 구분
- 근거: 실사용 테스트에서 중계 도달✅·네이버 토큰✅·Bearer 통과(네이버 404 응답)까지 확인됨 → **연결 체인은 정상**, 정산 상세 경로만 3단계에서 확정 필요

## v5.9.43 (2026-07-24 — 네이버 커머스API 2단계: 회사프로그램 ↔ 중계서버 연결)
- [신규] **네이버 중계서버 호출 클라이언트**(`naver-relay.js`) — 회사프로그램 → NCP 중계서버(101.79.16.213:4000) → 네이버 왕복. Bearer 인증, 오류 시 텔레그램 알림(회사프로그램 측)
- [설계] **알림 구조 변경(대표 제안)** — 중계서버가 텔레그램을 직접 부르지 않고 오류를 응답으로만 반환 → **회사프로그램이 알림 발송**. 텔레그램 시크릿이 회사프로그램 한 곳에만(중계서버에서 텔레그램 제거)
- [신규] 연결 테스트 `GET /api/agent-office/naver/test`(adminOnly) + 데이터관리 화면 **🛰️ 네이버 연동 연결 테스트 버튼**(관리자만) — 중계 도달·네이버 토큰·정산 왕복(Bearer) 3가지 확인
- 필요 환경변수(Render): `NAVER_RELAY_URL`, `NAVER_RELAY_TOKEN`. 로컬 검증: 중계 클라이언트 7/7·중계서버 부팅 정상. app.js v252
- ⏳ 다음: 대표가 Render 환경변수 설정 → 연결 테스트 → 3단계(정산 조회)

## v5.9.42 (2026-07-22 — 모바일 햄버거 메뉴 가시화 + 아꼼이 캐릭터 가림 해결, 대표 지시)
- [🔴 수정] **모바일 좌측 상단 메뉴 버튼이 '흰 동그라미'로만 보이던 것** — `☰` 글자가 흰색인데 배경도 흰색(`--sidebar-bg:#FFFFFF`)이라 아이콘이 안 보였음. **진한 3줄 삼단바 SVG 아이콘**으로 교체 + 색 `#2b2b40`·테두리·그림자로 "메뉴 버튼"임이 확실히 보이게
- [수정] **메뉴를 열면 아꼼이 캐릭터·회사명이 삼단바 버튼에 가려지던 것** — 모바일 사이드바 헤더에 `padding-left:66px`(+transition) 부여, 아꼼이 로고와 "제주아꼼이네 농업회사법인(주)"를 **버튼 폭만큼 오른쪽으로 이동**해 가림 해소
- [기타] styles.css v107 / theme.css v6 캐시 버전 상향(폰 즉시 반영)

## v5.9.42 (2026-07-22 — 조직도 흐름선 SVG 오버레이 재구현, 대표 지적)
- [개선] **흐름선이 담당팀 지점을 넘어가고(overshoot) 이음새마다 잘려 보이던 문제** — 가짜요소(::after) 조각을 이어붙이던 방식의 근본 한계. **마루→담당 요원까지 하나의 연속 실선을 좌표로 계산해 SVG로 그리는 방식**으로 교체. 넘침·이음새 잘림 없이 정확히 담당 요원까지만 이어짐
- [개선] 진한 보라 **실선(끊김 없음)** + 밝은 펄스 한 조각이 마루→요원 방향으로 흐름(움직이는 느낌). 선은 픽셀 스프라이트 **뒤로 지나가** 깔끔(팀장 아래 요원도 자연스럽게 연결). 창 크기 변경 시 좌표 자동 재계산
- [검증] 브라우저로 재무팀(세미, 한수 아래) 타깃 확인 — 마루→재무 열→한수 뒤→세미 연속 실선, 나머지 팀 회색. 작업 끝나면 오버레이 제거로 모션 소멸

## v5.9.41 (2026-07-22 — 흐름선 연속·진하게 + 멀티 일정 자동등록 수정, 대표 지적)
- [🔴 수정] **멀티 지시의 일정이 등록 안 되던 버그**(실사용 #245~248) — 대표가 "네"로 전체 승인한 멀티 안의 일정 서브태스크가 각각 "이대로 등록할까요?"로 **또 확인을 요구** → 순차 실행 중 서로 '대체됨'으로 밀려 3건 다 미등록. **멀티 서브태스크(multiSeq)면 재확인 없이 바로 등록**하도록 수정(단일 일정 등록은 확인 1회 유지 — 회귀 없음). 등록 로직은 공용 헬퍼로 추출
- [수정] **조직도 흐름선이 담당 팀까지 안 이어지고 중간이 짤리던 것** — ①중앙(마루 세로선 도착점)에서 담당 팀까지 '사이 브랜치 가로선(::after)'도 흐르게 해 **선이 쭉 연속**(마케팅팀이면 재무팀 구간 지나서까지) ②점선(짤려 보임) → **진한 보라 실선 + 밝은 하이라이트가 흐르는 애니메이션**, 굵기 5px·glow로 "움직이는 느낌" 강조
- [검증] 4팀 조직도로 마케팅/개발 타깃 모두 중앙→담당팀 선 연속 확인, 나머지 팀 회색 유지. 브라우저 렌더 확인

## v5.9.40 (2026-07-22 — LIVE 로그 헤더 가로 고정 + 조직도 배정 흐름 모션, 대표 지시)
- [수정] **LIVE 로그 헤더 글씨가 세로로 줄바꿈**되던 것(PC) — 한 줄 가로 고정(flex-wrap:nowrap·white-space:nowrap), 📡 이모지 제거(빨간 점과 중복), 로그비우기 버튼 컴팩트화. "LIVE 로그 · 🧹 로그 비우기 · 전체 보기" 한 줄
- [신규] **조직도 배정 흐름 모션** — 지시하면 대표→마루→해당 팀→담당 요원까지 **연결선이 진한 보라색 점선으로 흐르는 애니메이션**(작업 중에만). 담당 팀 경로만 흐르고 나머지 팀은 회색이라 "누가 맡았는지" 한눈에. 담당 요원 이름 보라색 강조. **작업이 끝나면(running 해제) 모션 자동 소멸**. aoRefreshOrgFlow가 running 요원 경로에 .ao-flow 부여/제거(상태 변경·재렌더 시 갱신)
- [검증] 브라우저로 확인: 재무팀(세미) 지시 시 재무팀 경로만 보라색 흐름·마케팅팀 회색, 로그 헤더 한 줄. CSS/기능 회귀 없음

## v5.9.39 (2026-07-22 — AGENT OFFICE 헤더/로그/지시바 리뉴얼 + 글샘 오류마크 수정, 대표 지시)
- [디자인] **헤더** — 🎮 이모지 → 다크 봇 아이콘 배지, "AGENT OFFICE" + 회색 버전칩. **세그먼트 토글**(전체/법인/오션라운지 = 흰 pill 활성 / 사무실·보고서함 = 인디고 활성)을 참고 디자인대로. 네이비/골드 게임 톤 제거
- [디자인] **LIVE 로그** — 네이비 헤더 → 흰 카드 + 빨간 점 "LIVE 로그", 상태 배지(완료 초록·질문/진행 주황·오류 빨강) 정리. **지시 입력바** — 흰 라운드 + 인디고 라벨·전송. 세미 보고서 잔여 네이비/골드도 인디고 톤으로
- [🔴 수정] **글샘 '오류 마크'가 조직도에 영영 남던 버그** — 완료는 잠시 후 대기(idle) 복귀하는데 **오류는 복귀 로직이 없어 error 상태로 고정**되던 것. ①부팅 시 running뿐 아니라 done/error도 idle 초기화(글샘 즉시 해소) ②오류도 완료처럼 6초 후 idle 복귀(서버·클라이언트). 오류 기록 자체는 실행 이력·LIVE 로그에 그대로 남음
- [안전] 디자인은 theme.css 오버라이드만(즉시 원복 가능). 오류상태 수정은 server 2곳·app.js 1곳(표시 상태만, 실행/기록 무변경). 계산된 CSS 값으로 적용 검증 완료

## v5.9.38 (2026-07-22 — 사이드바 SVG 아이콘 + 아꼼이 로고, 다크모드 제거, 대표 지시)
- [디자인] **사이드바 이모지 → 참고 디자인의 깔끔한 SVG 라인 아이콘** 14종 전부 교체(🎮📅📊… → grid·calendar·chart 등). 참고 완성본에서 직접 추출한 아이콘, currentColor라 활성 시 인디고
- [디자인] **좌측 상단에 아꼼이 캐릭터 로고** — 기존 큰 중앙 로고 → 참고 디자인처럼 42px 둥근 사각 아꼼이 + "제주아꼼이네 / 농업회사법인 (주)" 가로 배치. "메뉴" 섹션 라벨 추가. 로그아웃/앱설치도 SVG 아이콘
- [삭제] **다크모드 제거**(대표: 너무 안 보임) — 토글 버튼·초기 스크립트·theme.css 다크 블록 전부 제거, 라이트 단일. 잔재 0 확인
- [유지] AGENT OFFICE 픽셀 게임 조직도는 그대로(요청). 기능/ID/data-page/onclick 무변경 — theme.css 링크 제거로 즉시 원복 가능
- [검증] 브라우저로 새 사이드바(아꼼이 로고·SVG 아이콘·활성 상태) 렌더 확인 완료

## v5.9.37 (2026-07-22 — 연속 되묻기 맥락 소실 수정, 대표 지적)
- [수정] **되묻기→"네"→또 되묻기** 상황에서 마루가 **"이전 맥락 불명확, 재확인 필요"**로 이상하게 되묻던 버그(실사용 order 239). 원인: 결합 시 직전 지시의 content를 원지시로 썼는데, 직전이 "네"(이전 답변)면 **원래 요청이 소실**돼 마루가 맥락을 잃음. 되묻기 result에 **뿌리 원지시(root_content)**를 실어 체인 내내 승계하도록 수정 — 몇 번을 이어 물어도 원래 요청 유지. 단일 되묻기는 동작 동일(회귀 없음, 로컬 시뮬레이션 검증)
- 영향 범위: 마루 되묻기 결합 로직(server.js 2곳)만. 라우팅·세미·이어가기 다른 경로 무변경

## v5.9.36 (2026-07-22 — 디자인 리뉴얼: 인디고/화이트 + 다크모드, 대표 지시)
- [디자인] **전면 리뉴얼** (참고: `디자인/제주아꼼이네 오피스 (완성본).html`) — 주황+다크네이비 → **인디고(#4F46E5) + 화이트 + Pretendard 폰트**. 사이드바 화이트화(둥근 네비 + 좌측 인디고 액센트 바), 카드 라운드+연한 그림자, 로그인 인디고 그라디언트, 조직도(AGENT OFFICE) 새 톤(화이트/인디고 그리드·프레임, 픽셀 캐릭터·팀색은 유지)
- [신규] **다크모드 토글**(우하단 ☀️/🌙) — localStorage 저장, 렌더 전 적용해 깜빡임 없음. 셸(사이드바·카드·조직도·헤더) 전부 다크 대응
- [안전] **기능/HTML/JS 로직 무변경** — 원본 `styles.css`는 그대로 두고 뒤에 로드되는 `theme.css`로만 덮어씀(문제 시 링크 한 줄 제거로 즉시 원복). app.js는 인라인 강조색(주황→인디고) 7곳만 조정(효돈 이동 등 데이터 의미색은 유지). Pretendard는 시스템 폰트 폴백 포함(CDN 끊겨도 안 깨짐)
- [검증] 배포 전 브라우저로 라이트/다크/로그인/조직도 실제 렌더 확인 — 흰 배경 흰 글자 등 대비 문제 없음

## v5.9.35 (2026-07-22 — 품목별 결제가(단가) 이력 조회 신규, 대표 지적)
- [신규] **세미: 품목별 결제가(단가) 이력 조회** — "4월 품목별 결제가 주차별로/거래처별로" 물으면 그 시기의 **단가**를 거래처별·주(날짜범위)별 그리드로 보여줌. 결제가는 주마다 변동돼서 **내년에 작년 동기 단가와 비교**하려는 용도. 데이터 출처 = `pricing`(품목별 금액 메뉴) 주간 단가 이력이라 화면과 완전 동일. 매출(settlements)과 명확히 구분
- [수정] 이전엔 "품목별 금액 주마다" 물어도 세미가 **4월 전체 매출 품목 리스트만** 반복 반환(같은 답)했음 — 대표가 원한 건 매출액이 아니라 그 시기 **결제가(단가)**였음. 서버 감지 추가: `결제가/단가` 명시 또는 (품목+주마다/주차별)이고 매출어(매출·판매액·팔린)가 아니면 → 결제가 이력으로 라우팅(매출 조회는 그대로 유지). 8/8 감지 검증 + 세미 로직 9/9 로컬 검증
- [보강] 마루 용어집: "과거 시기 결제가/단가 조회는 세미에게 배정"(단가 이력 vs 매출 집계 구분 명시). 매출·순위·비교 라우팅 무변경
- 영향 범위: 세미 신규 리포트(semi_price_history)·서버 조건 감지·app.js 렌더 추가. 기존 매출/순위/비교/거래처필터 경로 무변경(감지가 매출어 있으면 발동 안 함) — 회귀 방지 검증 완료

## v5.9.34 (2026-07-22 — 이미지+질문 오류 수정 + 로그 비우기, 대표 지적)
- [🔴 버그수정] **이미지 첨부 + 질문 지시가 오류**나던 근본 원인 — 프론트가 보낸 `image_data`는 `data:image/png;base64,…` 전체 data URL인데, 마루 직접판단 경로(processOrderWithMaru→maruDecide)는 접두사를 안 벗기고 그대로 Anthropic 비전 API에 넘겨 **invalid base64**로 실패. 정산 OCR 경로처럼 접두사를 벗겨 순수 base64만 전달하도록 수정(4가지 입력 형태 로컬 검증 통과). 정산 등록 이미지 경로는 원래 정상이라 무변경
- [개선] **LIVE 로그 '🧹 로그 비우기' + 오류도 확인 가능** — 전엔 완료(done) 실행만 ✔확인 버튼이 있어 **오류(error) 실행 로그가 영영 안 지워지고** 쌓였음. 이제 ①오류 실행에도 ✔확인 버튼 ②로그 헤더 [🧹 로그 비우기]로 완료·오류 기록을 한 번에 확인 처리. 모두 soft-delete(숨김)라 "전체 보기"에서 복구 가능
- 영향 범위: 이미지 수정=마루 판단 입력 전처리만(정산·요원 로직 무변경). 로그 비우기=프론트 표시+기존 archive API 재사용(신규 서버 로직 없음)

## v5.9.33 (2026-07-22 — LIVE 로그 한눈에 정리, 대표 지시)
- [개선] **LIVE 로그 도배 제거** — 진행 중 실행은 **최신 스텝 1줄만**(작업중 표시), 완료/오류는 **미리보기 1줄만**(📄 클릭→보고서). 기존엔 실행 스텝 전부를 쏟아내 확인(✔)을 다 눌러도 글이 가득 차던 것 해소
- [개선] **대표 지시를 원문 그대로 표시** — 로그·보고서에서 `[상세 조건은 원지시 참조: …]`·`[멀티 x/y …]` 서브태스크 래퍼 문구를 숨기고(`aoCleanContent`) 대표가 실제 입력한 지시만 깔끔하게 노출
- [개선] **처리 흐름 한 줄로** — 라우팅 완료 시 `🔧 마루 → {팀} → {요원} 배정`으로 짧게. 배정된 요원의 실제 작업은 별도 미리보기 줄로 이어짐(마루→팀→요원→작업 흐름이 한눈에)
- [개선] **완료 문서 클릭 시 '대표님 질문' 원본**을 답변 위에 함께 표시(run_id↔지시 매핑). 마루 즉답은 로그에 140자로 요약 노출
- 영향 범위: 프론트 LIVE 로그·보고서 모달 표시 로직만(app.js). 서버·요원 로직 무변경 — 회귀 위험 없음

## v5.9.32 (2026-07-22 — 예리 인스타 전담 AI + 글샘 원복, 대표 지시)
- [개편] **예리를 인스타그램 전담 AI로 재구성**(Sonnet) — 계정 아이디 추천·첫 영상 방향·릴스/영상 대본·게시물 문구·해시태그·성과 분석(데이터 제공 시). 유연한 body 필드(대본·목록·분석 무엇이든)+max_tokens 8000으로 글샘이 겪던 대본 스키마 충돌을 회피. 프론트 yeri_insta 렌더(복사 버튼), AO_TRAIT·조직도 description 갱신
- [원복] **글샘은 원래(문자·톡톡 카피)대로 복구** (대표 지시: 글샘 잘 해왔으니 건들지 말 것) — 대본 관련 추가분 전부 제거: channel enum(인스타/릴스 삭제), 인스타 대본 규칙(1-2), genOnce 재시도, max_tokens 5000→4000. 문자·톡톡 카피는 검증된 원래 로직 그대로
- [수정] 마루 라우팅: 인스타 콘텐츠·대본·문구·분석 → **예리** (실제 이미지/영상 생성만 미소, 글샘은 문자·톡톡). 인스타/릴스 배정에만 국한(타 라우팅 무변경)
- ⚠️ 예리가 API 소진 직원(Sonnet)으로 전환됨(기존 0원 코드 → AI). 인스타 지시마다 Claude 호출

## v5.9.31 (2026-07-22 — 글샘 인스타 릴스 대본 실패 근본 수정, 대표 지적)
- [수정] 글샘 **인스타 릴스 대본이 재시도에도 계속 빈 결과**(run 160·163 error)로 실패하던 근본 원인 — 글샘 출력 스키마(COPY_TOOL)의 channel이 **LMS/SMS/톡톡만 허용**해 인스타/릴스 대본이 strict 스키마와 충돌→AI가 유효 출력을 못 냄(아이디 추천은 톡톡으로 우겨져 됐으나 대본은 인스타라 실패). **channel enum에 '인스타'·'릴스' 추가** + 프롬프트에 대본 출력형식 명시(channel=릴스, 대본 전체를 versions[].text에, 빈 versions 금지). 글샘 한정(문자·톡톡 카피 무변경)

## v5.9.30 (2026-07-22 — 인스타 콘텐츠 라우팅 + 글샘 빈결과 재시도, 대표 지적)
- [수정] 오션라운지 인스타 요청(아이디·영상 방향·대본)이 '인스타' 키워드 때문에 **예리(성과 분석)로 잘못 배정**돼 "데이터 없음 분석불가"로 빠지던 것 — 마루 프롬프트에 인스타 배정 구분 추가: **콘텐츠 제작(계정명·영상방향·대본·문구)=글샘/미소, 성과 분석(성과·경쟁사)=예리**. 인스타/릴스 배정에만 국한(다른 라우팅 무변경, 대표 승인 후 진행)
- [수정] 글샘이 "영상 대본"에서 **빈 결과(0건) 반환해 오류**나던 것 — 빈 결과 시 1회 재시도 + max_tokens 4000→5000(긴 대본 잘림 방지). 재시도도 실패면 정직 안내. 영향 범위 글샘 한정

## v5.9.29 (2026-07-22 — 명시 월 조회확인 생략 + 거래처별 순위, 대표 지적)
- [수정] "4월 효돈 매출"을 물어도 **"『4월』 조회로 진행할까요?" 확인이 계속 떠서(#201·#203)** 데이터를 못 받던 것 — 3개월+ 과거 조회 복창 확인이 명시한 월에도 걸림. **대표가 월/날짜를 명확히 말하면(parseExplicitMonth/hasExplicitDay) 확인 없이 바로 조회**. 기간 미명시·기본값의 오래된 조회는 확인 유지(안전장치)
- [수정] 거래처별 **순위** 조회("효돈 4월 품목 순위")도 거래처 필터 적용 — rankReport에 partner 파라미터 추가(v5.9.28 filteredReport와 동일 패턴). 거래처 0건 시 정직 안내. 이제 "효돈 4월 매출+순위"가 효돈 것만 나옴

## v5.9.28 (2026-07-22 — 거래처별 월/기간 매출 필터, 대표 지적)
- [수정] **"효돈 4월 매출" 같은 거래처+월 조회가 거래처 필터 없이 회사 전체 매출(306,076,600원)로 나오던 것** — 세미 매출 조회에 거래처 필터가 '주차'에만 있고 '월/기간'엔 없었음(대표가 "효돈만 필요해"를 여러 번 재입력). filteredReport에 partner 필터 추가(효돈농협·대성(시온)·기타거래처), 서버 조건 파싱이 주차 없이 거래처만 있어도 conditions.partner 설정. 거래처 0건 시 "그 거래처만 없음 + 있는 거래처 목록" 정직 안내
- ⚠️ 부작용 방지: CJ/택배비는 거래처 매출 필터 제외("택배비 얼마야"가 CJ 0건으로 빠지지 않게). 거래처는 새 질문 기준(이어가기 시 "5월은?"엔 거래처 안 이어짐 → "효돈 5월"로 명시). 전체·품목 조회는 무변경

## v5.9.27 (2026-07-22 — 마루 되묻기 선택지 버튼, 대표 지시)
- [추가] 마루가 **선택지 2개 이상인 되묻기**를 하면 [1. …][2. …][둘 다] 버튼으로 표시(네/아니오 대신) — 대표가 눌러서 정확히 선택. MARU_ROUTE_TOOL clarify_choices 필드 신설, "선택지 — 눌러서 고르거나 말로 답해도 됩니다" 라벨. 라이브 로그·말풍선 모달 모두 적용. 자유 텍스트 답변도 계속 작동(aoAnswerChoice가 선택지 텍스트를 답으로 전송→기존 clarify 결합 경로). 단순 예/아니오 질문은 기존 네/아니오 유지

## v5.9.26 (2026-07-22 — 마루 직접 답변 + 이미지 읽기 A+B, 대표 지시)
- [추가] **(A) 마루 '직접 답변'(answer)** — 개념·용어 뜻("계열합계가 뭐야")·이미지 설명·사용법 등 배정 아닌 질문에 마루가 바로 답변(모달+로그). 단 데이터 조회(매출·정산·수치)는 여전히 세미로 route(수치 지어내기 금지 프롬프트). MARU_ROUTE_TOOL에 action=answer+answer_text, maruDecisionUnusable/Normalize 반영
- [추가] **(B) 마루 이미지 직접 읽기** — 이미지 첨부 시 '정산/등록/올려/발송목록/입력' 정산 등록 의도가 있으면 기존대로 정산 OCR(**핵심 경로 보존**), 그 외 이미지("이 이미지 뜻이 뭐야")는 마루가 비전으로 직접 보고 answer/route. maruDecide(content, image)로 이미지 전달
- ⚠️ 정산 이미지는 "정산관리 올려줘/등록해"를 붙여야 정산 등록으로 감(기본 문구는 이미 포함). 마루가 수치 질문에 직접 답할 위험은 세미 route 지시로 방어(모델 판단이라 확인 권장). 선택지 되묻기 네/아니오 UI 개선은 별도 안건

## v5.9.25 (2026-07-22 — 🔴 정산관리 데이터 부팅 시 전량 삭제 버그 수정, 대표 지적)
- [🔴 긴급수정] **서버 재시작·배포 때마다 정산관리(from_pricing=true) 데이터가 전부 삭제되던 치명적 버그** — initDB()(매 부팅 실행) 안에 옛 일회성 정리 코드 `DELETE FROM settlements WHERE from_pricing = true`(server.js:263)가 무조건 실행되고 있었음. 오늘 다수 배포로 21일 정산관리 데이터가 반복 소실(증거: settlements id 282~293 통째 삭제·gap). 해당 삭제 줄 제거 → 이제 배포·재시작해도 정산관리 데이터 보존. 절대 되살리지 말 것

## v5.9.24 (2026-07-21 — 세미 연속 질문 반복 응답 수정, 대표 지적)
- [수정] 마루에 연속 질문 시 **세미가 다른 질문(레몬만·5월 비교)에도 같은 6월 순위 TOP10 표를 반복**하던 것 — 맥락 이어가기(v5.9.13)가 옛 질문을 합쳐 처리하는데, 서버의 **조회 종류 판단(순위·비교 감지)이 합쳐진 텍스트에서 옛 질문의 "기여/순위" 단어를 보고 순위 조회를 강제**한 게 원인(run 138·139 동일 확인). 조회 종류(비교·순위)는 **이번 새 질문(order.content)에서만** 판단하도록 수정(typeSrc). 기간·품목 필터는 문맥 유지, 종류만 새 질문 기준. 단일 질문은 회귀 없음(effContent===order.content). ※기간 비교는 두 기간을 한 문장에 쓰기 권장("5월이랑 6월 비교")

## v5.9.23 (2026-07-21 — 영상 자동 재시도 제거, 대표 지시)
- [수정] v5.9.22의 영상 자동 재시도 제거 — 재시도도 과금이 걸릴 수 있어 **대표가 직접 [생성] 재클릭으로 결정**(건별 승인제). 영상 생성은 1회 시도, 실패 시 정직 안내("잠시 후 [생성] 다시 눌러 재시도 · 과금은 대표 결정"). durationSeconds 숫자 8 유지

## v5.9.22 (2026-07-21 — 미소 영상 구글 내부오류 자동 재시도, 대표 지적)
- [수정] durationSeconds 수정 후에도 영상이 "Video generation failed due to an internal server issue"(구글 Veo 서버 일시 오류)로 실패 — 구글이 재시도 안내하는 일시적 오류. **자동 1회 재시도(15초 대기) + 2차 시도는 resolution 설정 제외 폴백** 추가. 재시도도 실패하면 "구글 Veo 서버 일시 오류 — 몇 분 뒤 다시 시도" 정직 안내. 과금 없음(생성 실패분 미과금). 반복 실패 시 원인 후보=Veo 일시 장애 또는 프롬프트 과다(한국어 자막·이모지 오버레이) → 프롬프트 단순화 검토

## v5.9.21 (2026-07-21 — 미소 영상 생성 실패 수정, 대표 지적)
- [수정] 미소 영상(Veo) 생성이 400 INVALID_ARGUMENT로 실패하던 것 — `durationSeconds`를 문자열 '8'로 보냈으나 Veo API가 **숫자 8**을 요구. 숫자로 수정. **과금 없음**(요청이 생성 전 파라미터 검증에서 거부돼 영상 생성 자체가 시작 안 됨). 재시도 시 정상 생성 예정. ※영상은 캐릭터 참조 이미지 미첨부(텍스트 프롬프트로 캐릭터 묘사) — 이미지 생성만 참조 적용

## v5.9.20 (2026-07-21 — 직원별 학습노트 업그레이드 + 미소 캐릭터 고정 + 마루 메뉴 이해, 대표 지시)
- [추가] **미소 브랜드 캐릭터 '아꼼이' 고정** — 캐릭터 이미지 2종 레포 등록(assets/brand/), 미소가 use_character=true로 표시한 이미지는 서버가 **실제 아꼼이 이미지를 Gemini에 참조로 넣어 생성**(생김새를 새로 상상하지 않고 배경·포즈만 변경). 미소 스키마(use_character)·프롬프트에 캐릭터 규칙 추가
- [추가] **마루 메뉴 용어집** — 정산관리/정산현황/품목별금액/박스재고/송장변환/지출결의/일정을 정확히 구분해 배정하도록 마루 두뇌에 주입(송장변환=플랫폼 양식을 판매상품명으로 통일). **마루도 활성 학습 노트를 읽도록 연결**(기존엔 워커만 반영)
- [추가] **글샘 인스타 대본** — 릴스·게시물 대본 작성 능력(후킹→장면별 자막→캡션→해시태그→CTA), 농업회사법인/오션라운지 카페 구분
- [추가] **직원별 시작 학습 노트 등록**(대표 승인분, scripts/seed-lessons.js 1회) — 세미(택배비·0건 정직), 글샘(대표지시 우선·마감일), 미소(판매배너·캐릭터), 지율(5인 기준·지침밖 안내), 예리(데이터 정리·추정치 없음)
- [참고] 한수 비교는 세미가 이미 동기대비·기간 비교(표)를 담당해 별도 미추가. 예리 단골고객 관리(2회↑ 주문·상품별 정리·클레임 번호 삭제)는 추후 과제로 기록

## v5.9.19 (2026-07-21 — AI 요원 상세 패널 정리, 대표 지시)
- [개편] 요원(에이전트) 클릭 시 상세 패널을 **직원 소개 카드**로 재구성 — ①**역량 점검·지금 실행 버튼 제거**(시험 폐지·대표 지시로 자동 작동), 팀장(한수·지율·미래) "🔒 다음 업데이트 예정" 문구·기안 미팅 입력칸 제거 ②**받은 피드백 섹션 제거**(피드백 미사용) ③각 요원 **특징을 같은 형식으로 상단 표시**(AO_TRAIT 큐레이션 1줄) + 지식 문서 ④**도구는 실제 연결된 것만 표기**(미소=🎨Gemini 이미지·🎬Veo 영상), 없으면 도구 섹션 미표기(기존 stale "(예정)" 제거) ⑤**최근 실행 이력에 "확인(보관)"한 실행도 포함**("확인함" 배지) — 서버 runs 조회의 is_deleted 제외 필터 해제(is_test만 제외, LIMIT 8)

## v5.9.18 (2026-07-21 — 실패 수집함 개편 + 통합본 v8, 대표 지시)
- [개편] **실패 수집함 전면 개편** — ①**⚙️ 관리 메뉴 안으로 이동**(바깥 칩은 관리 1개, 실패 있으면 관리 칩에 건수 배지) ②**피드백 이력(칭찬·수정·코멘트) 제거** — 실패(fail)만 수집 ③항목에 **내 질문 + 마루/요원 답변** 표시(길면 … 접힘, 클릭 시 전체보기) ④항목별 **🗑 삭제**(잘못 눌렸거나 필요 없어진 건 제거) ⑤**라이브 로그 각 지시에 [❌ 실패] 버튼** 신설(마루 답변을 질문+답변째 담기) + 보고서 [❌ 실패 표시]도 사유 프롬프트 제거·자동 캡처. 서버: order_id로 질문/답변 캡처, GET에 original_output(답변) 추가, 삭제 엔드포인트, growth에 fails 카운트. **학습 방식 = 쌓이면 대표가 "실패수집함 정리하자" → 함께 원인 분석·보강** (개별 자동 재검증 아님)
- [문서] **통합본 v8 작성·아카이브 추가** (docs/archive/통합본_v8_20260721.md) — v5.5~v5.9.18 전체를 담은 **인수인계 종합본**. 조직도·재무/정산·마케팅·실패수집함·절대원칙·버전 히스토리. README 갱신

## v5.9.17 (2026-07-21 — 보고서 모달 중복 스택 수정, 대표 지적)
- [수정] 미소 이미지 생성 후 보고서 모달이 이중/삼중으로 겹쳐 떠서 확인을 여러 번 눌러야 다 닫히던 것 — aoOpenReport가 새 모달을 만들 때 기존 것을 안 지우고 document.body에 계속 쌓던 것(열기→생성 재렌더→완료 재렌더로 최대 3겹). 새 모달 생성 전 기존 보고서 모달(.ao-report-overlay 전용 클래스) 제거 → 항상 1개만 유지. 이제 확인 한 번에 닫힘. 다른 모달(마루 질문·요원 상세·정산 확인표)은 무영향

## v5.9.16 (2026-07-21 — 미소 이미지 생성 진행률+완료 자동 표시, 대표 지적)
- [수정] 미소 보고서에서 이미지/영상 생성을 시작하면 완료돼도 화면이 안 바뀌어(텔레그램만 오고, 나갔다 다시 미소 클릭해야 생성물이 보임) — 프론트에 완료 폴링이 없던 것. 생성 누르면 **즉시 진행바(0→85% 크리핑) 표시 + 2초마다 서버 폴링**으로 media_generating 해제 감지 → **완료 시 모달 자동 재렌더해 이미지/영상을 그 자리에 바로 표시**(aoPollMediaGen). 진행바는 생성 버튼 자리에 인라인, 모달 재열기·창 유지 모두 커버(폴러 중복 방지), 영상 최대 6분 대응, 실패 시 사유 표시. 텔레그램 알림은 그대로

## v5.9.15 (2026-07-21 — 재작업 감지 키워드 보강, 대표 질문 중 발견)
- [수정] 창작 요원 재작업 감지(isRework)에 "만들어줘/그려줘/제작/생성/작성"이 빠져 있어 "이미지 밝게 만들어줘"류가 피드백으로 분류될 경우 재생성 누락 가능 → 만들·그려·제작·생성·작성 추가. (대부분은 마루가 route로 미소 재배정해 정상이나, 피드백 분류 시에도 확실히 새 안 생성되도록 안전망)

## v5.9.14 (2026-07-21 — 멀티 열린 스레드 맥락 매칭 + 창작 요원 재작업, 대표 지적)
- [수정] **멀티(글샘 문구·미소 이미지 등 동시 배정) 후** 확인 안 하고 후속 질문하면 맥락이 항상 정확히 매칭되지 않던 것(직전 완료 1건만 참조) — 열린(확인 안 된) 요원 답변을 **전부 수집**해 마루에게 목록 제시(buildMultiFollowUpText), 마루가 후속 내용으로 **맞는 요원 선택** → 그 요원 '자기 스레드'(자기 이전 답변)로 맥락 좁혀 재작업(buildFollowUpText). 열린 스레드 1개면 그 요원에게 바로 이어감. 확인(run archive)된 스레드는 제외
- [개선] **창작 요원(글샘·미소) 재작업 활성화** (대표 7/21): "다시 써줘/다시 만들어줘/수정해서/다른 안/보완" 같은 명시적 재작업 요청이면 피드백 반영해 **새 안 1라운드 재생성**(문구·이미지 안 3개 → 한 번 더). 단순 칭찬은 재작업 안 함 — 무한 자동 재작성이 아니라 대표 요청 시마다 1회. 자문 요원(지율·세미 등)은 기존대로 지적·코멘트에도 재답변

## v5.9.13 (2026-07-21 — 요원 답변 후 맥락 이어가기 + 질문 UI 즉시 정리, 대표 지적)
- [수정] 지율(등 요원)이 답한 뒤 대표가 이어서 물으면(예: "23일 근무했어") **마루가 맥락을 잃고 처음부터 되묻던 것** — 맥락 이어가기가 '마루 clarify 질문'에만 작동하고 완료된 요원 답변엔 없었음. 직전 요원 답변이 아직 '확인'(보고 보관 = run archive) 안 된 **열린 상태면 [원지시+요원답변+후속]을 결합해 그 요원에게 재배정**(확인 누른 상태면 마루가 새로 판단 — 대표 정의: "확인 누른 상태=처음부터 / 안 누른 상태=이어가기"). buildFollowUpText + processOrderWithMaru else 분기 (서브태스크 실행엔 미적용). 마루 escape hatch("명백히 새 지시면 후속만 기준")로 새 주제는 정상 라우팅
- [수정] 마루 질문에 네/아니오 답변 후에도 **라이브로그 버튼이 다음 폴링에 다시 그려져 "안 사라짐"·중복 클릭으로 '네'가 2번 전송**되던 것 — ①답변한 질문(aoAnsweredQ)은 네/아니오·✔확인 버튼 재표시 억제(10978·10993행이 aoAnsweredQ 미참조였음) ②aoQuickAnswer 중복 클릭 방어(pending 질문 없으면 즉시 무시)

## v5.9.12 (2026-07-21 — 지율 자문 금액 오류 + 피드백 재자문 경로, 대표 지적)
- [수정] 지율 노무 자문(소희 주휴수당)에서 **계산표는 정확(주5일 54,000원·주4일 43,200원)한데 결론 헤드라인만 "60,000원"으로 틀리던 것** — AI가 시급×1일근로시간(5h×12,000) 단순환산 값을 결론에 써버린 자기모순(자기 계산표의 "단순환산 오류 주의" 경고를 스스로 위반). 지율 프롬프트에 ①결론 수치는 calculation 도출값과 글자 그대로 일치 ②주휴수당은 반드시 (소정근로÷40)×8×시급 공식만 ③근무일수 등 미확정 시 경우별 금액 제시(단일 금액 단정 금지) 규칙 강화. 노무지침 공식은 원래 정확(무변경)
- [수정] 대표가 요원 자문·결과물에 수정·지적 피드백을 줘도 **"피드백 감사"만 나가고 재답변이 없던 것**(문서화된 '수정 시 1회 재작성' 경로가 코드엔 미구현) — 피드백 처리에 live 요원 1회 재자문/재작성 추가. 자문·조회 요원(지율·세미·한수·미래)은 지적·코멘트에도 피드백 반영해 재답변, 창작 요원(글샘·미소)은 명시적 '수정'일 때만 재작성(자동 재작성 금지 원칙 유지). 재답변엔 직전 결론+대표 지적을 컨텍스트로 주입

## v5.9.11 (2026-07-21 — 법무팀(지율) 노무 자문 미작동 수정, 대표 지적)
- [수정] 대표 노무 질문(라이브 #158 "오션라운지 소희 주휴수당")에 지율(법무팀)이 실행 안 되고 "아직 실전 연결 전입니다" 안내로만 빠지던 것 — 근본 원인: dispatchLiveAgent 실행 게이트가 `agent.role === 'worker'`를 요구했는데 지율은 role='manager'(법무팀장)라 live:true인데도 차단됨(세미만 live였던 초기 잔재). role 조건 제거 → live:true+is_active면 직급 무관 실행. **덤으로 같은 버그에 막혀있던 한수(마진 계산)·미래(백로그·버전 안내)도 함께 정상화**(셋 다 매니저 직급 live 요원). 부작용 없음: executeAgentTestRun이 세 요원 result() 정상 처리, 날짜 대조·한수 검산 부가로직은 이들 report에 미적용, 워커 요원(글샘·미소·세미 등) 무변경. 보안 이슈 아님(권한 누수 아니라 정상 요원 실행 차단 결함)

## v5.9.10 (2026-07-21 — 지시 입력바 이미지 붙여넣기, 대표 요청)
- [추가] 마루에게 지시하기 입력란에 이미지 복사→붙여넣기(Ctrl+V) 첨부 지원 — 파일 선택(📷)과 동일하게 aoOrderImages에 적재, 여러 장 가능. 정산관리 이미지 등을 캡처 후 바로 붙여넣어 전송

## v5.9.9 (2026-07-21 — 직원 권한: 송장변환 새 기능 미적용, 대표 지적)
- [수정] 직원 아이디로 송장변환 매칭·거래처 색상·필터가 안 되던 것 — 원인: aoLoadInvoicePricing이 GET /api/pricing(adminOnly=대표 전용) 사용→직원 403→하드코딩 폴백. 품목명·거래처만 주는 경량 API(GET /api/invoice/catalog, authMiddleware, 단가 제외) 신설하고 프론트 전환. 이제 직원도 송장변환 pricing 매칭·색상·필터 정상. 단가 노출 없음(정산관리·품목별금액 화면 권한은 무변경)

## v5.9.8 (2026-07-21 — 중간발주 이미지 저장 필터 미적용, 대표 지적)
- [수정] 중간발주 거래처 선택 후 선택분 이미지 저장 시 미매칭·타 거래처 품목까지 포함되던 것 → saveQtyImage에 거래처 필터 적용(aoQtyPartnerFilter). 화면에 보이는 것만 저장(거래처 선택 시 그 거래처만, 전체보기는 전부)

## v5.9.7 (2026-07-21 — 정산관리 단가 뒤섞임 버그, 대표 지적)
- [수정] 정산관리 금액이 품목별 금액과 달라진 문제(어제 효돈 490,500이 419,000으로) — 근본 원인: extractFeatures 무게 파싱 정규식 (d+)이 정수만 읽어 "2.5kg"·"4.5kg"을 둘 다 "5kg"으로 뭉갬 → 특징 매칭이 2.5kg 소과(15,500)를 4.5kg 소과(28,000)에, 4.5kg 로얄과(33,000)를 2.5kg 로얄과(19,000)에 뒤섞음. 소수점 허용((d+(?:.d+)?))으로 수정. 발현 계기: 어제 대표가 pricing 품목명 변경(하우스감귤 제거)→정확일치 실패→특징매칭 fallback→무게버그 노출. 정산 표시·집계 재매칭에 정확 substring(matchSettlementItemExact) 우선 추가. 검증 490,500 ✅. 저장값 무관(조회 시 재계산)이라 기존 정산도 자동 정상화

## v5.9.6 (2026-07-21 — 중간발주 거래처 필터 + 엑셀 거래처 색상, 대표 요청)
- [추가] 중간발주 품목추가 옆 거래처 필터(전체보기/효돈농협/대성(시온)/기타거래처) — 클릭 시 해당 거래처 품목만 표시. 거래처 판정 = 오늘 품목별 금액(pricing) 거래처별 품목명 정확 대조(aoItemPartner). 미매칭은 전체보기에만
- [추가] 송장변환 통합 엑셀 다운로드 시 품목명(E열) 거래처별 색상: 효돈=아주 연한 파랑(E8F1FB)·시온=아주 연한 보라(F0EAF8)·기타거래처=아주 연한 갈색(F2EBE3). 미매칭 빨강·제주·날짜 기존 색 유지. 거래처 분류 편의

## v5.9.5 (2026-07-21 — 정산 확인표 수동 매칭, 대표 요청)
- [추가] 정산관리 이미지 확인표에서 미매칭 품목(가격없음) 소계 클릭 → 해당 거래처 품목별 금액 상품 목록에서 선택해 수동 매칭 (OCR 글씨 불완전으로 자동 매칭 실패 시 대표가 직접 맞춤). 매칭된 품목도 소계 클릭으로 변경 가능. 선택 즉시 단가·소계·합계 갱신, 저장 시 수정분 반영(settlement-ocr-save rows). 서버는 확인표 candidates에 거래처별 품목 카탈로그 첨부

## v5.9.4 (2026-07-20 — 날짜 파서 구두점 오류, 대표 지적)
- [수정] 미소 이미지 날짜 오류(7.29~7.31 요일 틀림) — 지시 원문 "담주 화요일!부터 목요일"의 느낌표 때문에 parseWeekdayRange가 파싱 실패→확정 날짜 미주입→미소 자체계산 오류. 요일 뒤 구두점(!.,·) 허용으로 수정. 검증: "화요일!부터 목요일"→7/28(화)~7/30(목) 정확. 날짜 미주입 시 요원 자체계산 방지(단일 소스 복원)

## v5.9.3 (2026-07-20 — 마루 질문 말풍선 타이밍, 대표 지적)
- [수정] 네/아니오 답변 시 마루 머리말·LIVE 버튼이 늦게(폴링 8초) 사라지던 것 → 답변 즉시 마루·대표 머리말+LIVE 버튼 동시 제거 + 답변한 질문 폴링 재표시 억제(aoAnsweredQ)
- [추가] 대표 머리말: 오더 시 대표 머리 위 "📢 내 오더" 표시 → 마루가 질문하면 유지(대표 답변까지), 작업 시작/완료 시 제거. CEO(대표) 캐릭터 말풍선 지원 추가

## v5.9.2 (2026-07-20 — 마루 질문 가시성 4종, 대표 요청)
- [수정] LIVE 로그에서 대표 질문·마루 질문이 같은 색이라 마루 질문이 안 보이던 것 → 대표 지시 카드 흰 배경, 마루 질문은 🟠 튀는 테두리(주황 박스)로 구분
- [수정] 전체보기에서 마루 질문 미표시 → 응답됨·대체됨·종결 상태에서도 마루 질문 표시 (실수·되묻기 이력 확인 가능)
- [추가] 마루 질문에 [✅ 네][✕ 아니오] 버튼 — 로그에서 바로 클릭해 진행
- [추가] 미응답 질문 있으면 조직도 마루 머리 위 "❓ 확인해주세요!" 말풍선 + 마루 클릭 시 질문 답변 모달(네/아니오)

## v5.9.1 (2026-07-20 — 멀티 서브태스크 재분해 유실, 대표 지적)
- [수정] 멀티 순차 실행 중 톡톡문구 subtask가 되묻기로 빠져 유실 — subtask content의 [상세 조건: 원지시 전문]을 마루가 다시 보고 재멀티 판단→subtasks 비우면 clarify로 빠짐. 서브태스크(noMulti)는 이미 요원 배정됐으므로 multi로 나와도 assignee route 강제 (재분해·재질문 차단). 톡톡문구 유실 해결

## v5.9 (2026-07-20 — 송장변환 품목 pricing 연동, 백로그 #6)
- [개편] 송장변환·중간발주 품목 매칭을 하드코딩 PRODUCT_CATALOG → **오늘 품목별 금액(pricing) 기반**으로 전환 (대표 7/20): matchProductRaw(정교한 옵션 파서)로 표준품목명 생성 → 오늘 유효 pricing 품목과 **특징 매칭**(과일·용도·중량·등급) → pricing에 있으면 그 품목명, 없으면 [미매칭]. 효돈처럼 형식 미세하게 달라도 특징으로 매칭(검증 ✅). pricing 미로드 시 표준명 폴백(안전). 중간발주도 matchProduct 공유로 자동 적용
- [추가] 송장변환 화면 "통합 변환" 밑에 📋 현재 판매 품목 목록 표시 (오늘 pricing 기준, 읽기 전용) — 신규품목은 품목별 금액에 추가하면 해당 기간 매칭 자동 반영

## v5.8.1 (2026-07-20 — 품목별 금액 수정 버튼, 대표 요청)
- [추가] 품목별 금액(pricing) 목록에 삭제 옆 ✏️ 수정 버튼 — 누르면 모달에서 품목명·단가·박스종류 편집 + 품목 추가/삭제, PUT /api/pricing(제자리 수정, 중복표 방지). 효돈 박스 세팅도 여기서 (박스 select에서 만감/귤 박스 선택 → 정산 시 박스재고 차감)

## v5.8 (2026-07-20 — 상단 화면 정리, 백로그 #3)
- [정리] 에이전트 오피스 상단 칩 6개→2개: 🧰 실패 수집함(자주 씀·바깥 유지) + ⚙️ 관리(가끔 씀·묶음: 지식 노트·마루 오배정·통합본 아카이브·텔레그램 알림 테스트). 🧪 성적표는 시험 채점 폐지로 제거. 피드백 칩은 실패 수집함으로 라벨 통합. 모바일 화면 간결화

## v5.7.3 (2026-07-20 — 글샘 대표 지시 우선 + 어그로 완화, 대표 지적)
- [수정] 글샘이 대표가 말 안 한 셀링포인트(지식문서의 "초반 호박이 1년 중 가장 포슬" 등)를 임의로 강조 → 프롬프트에 "대표 지시 내용이 최우선 근거, 지식문서는 참고용 예시, 대표가 언급 안 한 특성 임의 강조 금지" 명시
- [수정] 금지어·어그로 제한 완화 (대표 방침: 전환되고 사실이면 강한 어필 OK, 최종 판단은 대표) — 글샘 프롬프트 과장 금지어 강제 삭제, 어그로 허용(명백한 거짓·근거없는 수치만 금지). 시험 채점 과장어 검사는 백로그 #7 (다음 게이트 전 완화)

## v5.7.2 (2026-07-20 — 멀티 지시 실사용 결함 3건, 대표 지적)
- [수정] 멀티 분산 시 같은 요원(글샘 톡톡문구+문자문구) 동시 배정 → 하나가 "이미 실행 중"으로 유실 → **순차 실행**(각 서브태스크 요원 실행 완료까지 대기 후 다음, dispatchLiveAgent awaitExec/multiSeq). 각자 순서대로 보고, 유실 없음
- [수정] 멀티 과잉 분해 (원문에 없는 "일정 등록"을 추론 추가해 3건→4건) → 프롬프트에 "대표가 명시 요청한 작업만 분해, 추론 추가 금지" 명시
- [수정] 미소 톡톡 이미지가 순수 상품사진(쿠폰·타이틀·이벤트 없음) → 금지어 discount가 쿠폰 요소 차단하던 것 완화 + "톡톡/발송용=판매 배너"(큰 타이틀·쿠폰 스티커 금액·이벤트 뱃지 텍스트 요소 명시) 규칙 추가 (톡톡_실전사례 예시 반영)

## v5.7.1 (2026-07-20 — 효돈 이동 등록 추가, 대표 지적)
- [수정] 박스 입고/이동 모달에 효돈이 빠져있던 것(카드엔 효돈 표시했으나 이동 등록 구분은 업체입고·시온이동만) → "🚚 효돈 이동"(업체재고-/효돈재고+) 라디오 추가 + movement_type transfer_hyodon 처리(computeBoxStocks·POST 검증·이력 표시·요약). 업체→효돈 박스 전달 경로 완성

## v5.7 (2026-07-20 — 효돈 박스재고 신설, 대표 지적/백로그 #5)
- [추가] 효돈 박스재고 — 기존 박스 차감이 대성(시온) 하드코딩이라 효돈 정산은 박스 미차감(대표 확인=실수)이던 것을 거래처별로 일반화: box_inventory.hyodon_stock 컬럼, getBoxTypeMapFor(partner) 일반화, computeBoxStocks가 대성·효돈 각각 pricing 박스세팅 품목 차감, API hyodonStock, 박스재고 카드에 효돈 재고 표시·수정. 효돈 pricing 박스세팅은 아직 0개(대표가 추후 세팅 시 자동 작동). 대성 로직 무변경(하위호환)

## v5.6.2 (2026-07-20 — 정산 품목 모달 박스 수량 총합계, 대표 요청)
- [추가] 정산관리 정산 품목 편집 모달 총 합계 행에 박스 수량 총합계 표시 (기존엔 금액만) — 수량 입력 시 금액과 함께 자동 갱신

## v5.6.1 (2026-07-20 — 다중 이미지 확인표 폴링 버그, 대표 지적)
- [수정] 이미지 2장 올리면 첫 장(효돈) 확인표만 뜨고 2번째(대성)는 안 뜨던 문제 — 백엔드는 2장 다 정확히 처리(효돈→효돈농협·대성→대성(시온))했으나, 프론트 aoPollOrder가 타이머 하나를 공유해 2번째 폴링이 1번째를 clearInterval로 꺼버림 → order마다 독립 타이머로 교체. 이제 여러 이미지 확인표가 순차(큐)로 모두 표시

## v5.6 (2026-07-20 — 정산관리 이미지: 다중 + 거래처 자동인식 + 확인표 수정, 대표 설계)
- [추가] **거래처 품목 자동 인식**: 이미지 품목 구성을 3개 거래처(효돈농협·대성(시온)·기타거래처) 가격표와 대조해 최다 매칭 거래처 자동 선택 (대표: 거래처끼리 품목 안 겹침 → 오인식 없음). 지시문/이미지에 거래처가 있으면 그것 우선, 없으면 품목 자동 인식. 효돈 이미지 검증: 효돈 6/6·타 거래처 0/6 ✅
- [추가] **확인표 거래처·날짜 수정** (대표 안전장치 제안): 확인표 모달 상단에 거래처 드롭다운(자동 인식값 기본, 3개 중 수정 — 각 옵션에 품목 일치 수 표시)과 날짜 입력(잘못 인식 시 교정). 거래처 바꾸면 표·합계 즉시 전환(3개 미리 계산). 전용 저장 API(settlement-ocr-save)로 선택 거래처·날짜 반영, 중복은 덮어쓰기
- [추가] **다중 이미지**: 파일 여러 장 첨부(📷 multiple) → 각 장을 독립 order로 판독 → 각자 거래처 자동 인식 → 확인표를 순차(큐)로 표시. 효돈·대성 2장 한 번에 올려도 각각 구별 처리. 용량 문제 없음(장별 독립, 각 10MB 이내)
- 로컬 검증: 거래처 자동 인식·정확 매칭·날짜 파싱 전부 ✅. 저장은 정산관리 화면과 동일(from_pricing) + 확인표 승인 필수

## v5.5.3 (2026-07-20 — 지시문 거래처 우선, 대표 지적)
- [수정] 대표가 지시문에 거래처를 써도("21일 효돈 정산관리 등록해") 마루가 이미지에서만 거래처를 찾아 다시 되묻던 문제 → 지시문 거래처 우선 파싱(normalizePartnerName(order.content)), 없을 때만 이미지. 되묻기 1회 제거

## v5.5.2 (2026-07-20 — 정산관리 중복 방지 + 날짜 지정, 대표 지적)
- [수정] **중복 저장 방지** (대표 질문으로 발견 — settlements에 유니크 제약 없어 같은 날짜·거래처 2건 저장 시 매출 2배 집계 위험): 확인표에 기존 정산 존재 여부 표시(🔁 금액), 저장 시 같은 날짜·거래처 기존 행 삭제 후 저장 = **덮어쓰기** (중복 추가 아님). 버튼도 [🔁 덮어쓰기 저장]으로 전환, audit overwrote 기록
- [추가] **정산 날짜 지정** (대표 "21일로 올려줄까"): parseSettlementDate — "21일"(이번달)·"7/21"·"7월 21일"·"내일/어제"·YYYY-MM-DD 인식, 없으면 오늘. 정산은 미래여도 올해 기준(조회용 작년 규칙 미적용). 9케이스 검증 ✅. 확인표·저장·텔레그램에 날짜 명시

## v5.5.1 (2026-07-20 — 정산관리 이미지 실사용 버그 3건)
- [수정] 실사용 결함 (대표 직보고): ①거래처명이 이미지에 없으면(품목목록만 있는 이미지) 비전이 partner를 못 읽고 **읽은 품목까지 버려서** "효돈이야" 답변을 이어갈 수 없었음 → 거래처 미확정 시 read_items 보관 + 거래처만 재질문(settlement_ocr_need_partner) ②대표의 거래처 답변("효돈이야!")을 받아 보관 품목으로 확인표를 잇는 경로 신설(maruTrySettlementPartnerReply — AI 재호출 없음) ③partner 필드에 도구 파편 노출 → 질문 문구에서 raw partner 제거
- [수정] 확인표 UI 정리 (대표 지적 — LIVE 로그 안 좁은 테이블이 깨져 보임): 확인표를 **전용 모달**로 분리 (모바일·PC 반응형, 가로 스크롤, 품목·수량·단가·소계·합계 표 + [✅ 저장하기]/[취소] 버튼 — 버튼이 "응"/"아니오" 자동 전송). LIVE 로그엔 요약 1줄 + [확인표 열기] 버튼(캐시 참조)

## v5.5 (2026-07-20 — 정산관리 이미지 자동 입력, 대표 지시)
- [추가] 발송목록 이미지 → 정산관리 자동 입력 (매일 수기 입력 대체). 대표가 지시 입력바 📷로 거래처 발송목록 이미지 첨부 + "오늘 정산관리에 올려줘" → 마루 비전(소넷)이 거래처·품목·박스수량 판독 → 서버가 가격표 정확 대조로 금액 계산 → **확인표 제시 → 대표 "응" → settlements 저장** (돈 원본이라 승인 없이 저장 절대 안 함)
  · DB: pending_orders.image_data/image_mime (base64), 프론트 이미지 첨부 UI(미리보기·제거), POST /orders 이미지 수신(10MB 제한)
  · 거래처 별칭 정규화: 시온감귤랜드→대성(시온), 효돈농협유통센터→효돈농협, 기타→기타거래처 (미매칭 시 대표 확인 요청)
  · **정확 이름 대조 매칭**(matchSettlementItemExact): 기존 유사매칭(matchItemToPricing)이 2.5kg↔4.5kg을 못 구분해 오금액 발생 → 가격표 정식명 substring 정확 대조로 교체. 실측 검증: 효돈 이미지 6품목 19박스 = 490,500원 정확 재현 ✅ (기존 유사매칭은 381,500원 오산)
  · 안전장치: 저장 전 확인표(품목·수량·단가·소계·합계) 필수, 가격 매칭 실패 품목 ⚠️ 경고, "아니오"로 취소, audit(via image_ocr) 기록. 저장은 정산관리 화면과 동일 경로(from_pricing=true)
  · MVP 범위: 이미지 1장=1거래처. 여러 거래처는 장별로 반복. 과금=이미지 판독 1회(약 100원), 매칭·계산·저장은 0원 코드

## v5.4.6 (2026-07-20 — 순위 조회 자연어 확장, 대표 리허설 발견)
- [수정] 대표 리허설 중 발견: "가장 많이 판매된/팔린게 뭐야" 자연어를 품목 순위(semi_rank) 트리거가 인식 못 함 → 마루 AI 재량에 맡겨져 총액만 답할 위험. 트리거에 "많이 팔린·판매·나간·가장 많이·베스트·N위" + 목적어 추가. 8케이스 검증 ✅ (대표 원문 2건 순위 감지, 매출/일정/문구는 오탐 없이 통과)

## v5.4.5 (2026-07-20 — 미소·글샘 실전사례 학습)
- [추가] 대표 제공 1년치 톡톡 발송 실물 15건(이미지+문구 캡처) 분석 → docs/knowledge/marketing/톡톡_실전사례_v1.md 신설·주입 (글샘 [H]·미소 신규 블록): 배너 글씨 크기 위계(타이틀 압도적→쿠폰 금액→조건 순)·쿠폰 스티커·시즌 컬러(감귤 주황/호박 초록/명절 남금)·아꼼이 캐릭터 연출 / 문구 공통 구조(후킹 제목→인사 고정→맛·시기·명분→혜택 리스트→긴박 마감→신뢰 장치). 대표 지침 명문화: **고정 금지 — 참고+발전**, 과거 사례의 "미친~" 표현은 현행 금지어 정책 우선, "바로가기"는 링크 칸(디자인 대상 아님)

## v5.4.4 (2026-07-20 — 정산관리/정산현황 혼동 가드)
- [수정] 실사고 (7/20 아침, 대표 직보고): "정산관리에 수량 입력" 지시를 마루가 settlement_input(정산현황 자금 집계)으로 오인 — 수량 19를 "효돈 19원"으로 저장 (판단 근거에 수량임을 인지하고도 진행). 오입력 행은 대표 화면 입력이 대체해 현 DB 오염 없음 확인 → ①프롬프트: 정산현황 ≠ 정산관리 박제 (품목·규격·수량 나열이면 금지, 정직 안내) ②서버 가드 (0원): '정산관리' 단어 또는 금액 단위 없는 규격·수량 나열이면 settlement_input 차단 + 안내 전환 — 5케이스 검증 ✅ (정상 금액 입력은 통과)

## v5.4.3 (2026-07-19 밤 — 멀티 분산 실측 후속)
- [수정] 실측 order #73: "응" 긍정 인식 ✅·multi 선언 ✅이나 모델이 subtasks를 비워 냄 (기안 deliverables와 동일 습성, 오염 아님) → ①**subtasks 서버 복원** (0원 코드): 직전 확인 질문의 ①②③ 항목을 파싱해 복원 (각 항목에 원지시 전문을 조건으로 첨부) — order #66·#72 실측 질문으로 3건 복원 검증 ✅, "— 세 건" 꼬리 제거·괄호 안 "3건" 보존 확인 ②프롬프트에 "multi 선언+빈 subtasks = 실패" 직설 명시 + 출력 예 추가
- [추가] LIVE 오류 잔존 정리 (대표 지적): 오류 항목에 [✔ 확인] 버튼 — 오류확인 상태로 soft-close (기본 뷰 제외·전체 보기 조회 가능·audit 기록). 잔존 order #67(parseWeekdayRange 건) 즉시 정리

## v5.4.2 (2026-07-19 밤 — 대표 실사용 지적 2건)
- [수정] 마루 멀티 지시 분산 실행 (실사용 결함: "문구랑 디자인도" 3건 확인·승인 후 문구 1건만 배정, 디자인·일정 몰래 소실 — 실패 수집함 #5): 도구에 action multi + subtasks(완결된 독립 지시 문장 2~5건) 신설 — 대표 긍정 답변 시 각 건을 개별 지시로 등록해 기존 파이프라인에 **병렬** 배정 (요원별 독립 run — 끝나는 순서대로 각자 보고, 대기 없음). 정화·Unusable(분해 없는 multi=재시도)·재귀 방지(서브태스크는 단일 강제)·LIVE 로그 🔀 표시 포함
- [수정] 마루 되묻기 답변 안내 (실사용 지적): clarify 질문 끝에 "(네/아니오로 답해주세요 — 네라고 하시면 전부 진행)" 자동 부착 + 프롬프트에 긍정 표현 전폭 인식 명시 (네·맞아·응·그래·좋아·진행해·ㅇㅇ·ok 등 — 표현 불문 승인 의사면 긍정)
- 정적 검증 11항목 ✅. 역량 회귀는 다음 태그 게이트 합산

## v5.4.1 (2026-07-19 밤 — 핫픽스)
- [수정] 실사용 첫 결함 (대표 직보고 — 글샘 "담주 화~목" 지시 멈춤): ①server.js require에 parseWeekdayRange 누락 → ReferenceError로 실행 중단 (스모크는 날짜 자체 주입 경로라 미발현 — 실전 라이브 경로 첫 통과에서 발견) ②parseWeekdayRange가 "화~목" 축약형 미지원('요일' 표기 필수였음) → 범위 기호(~·-·부터) 동반 시 축약 허용, 단독형은 '요일' 필수 유지("다음주 수확" 오탐 방지). 파서 8케이스 검증 ✅. 고착 run #70은 error 전환+사유 기록 (재지시 안내)

## v5.4 (2026-07-19 확정 — git tag v5.4)

> 게이트: 역량 점검 run #69 = **73/73 전 문항 만점** (마루 33 · 고난도 8 · 세미 15 · 글샘 4 · 미소 3 · 지율 3 · 한수 2 · 미래 2 · 예리 2 · 기안 1 — 지시 #62-3 확정 문항 수와 실측 일치). 똑똑이 MCP 게이트 호출 (audit 580, 대표 ㄱ 승인 전제).
> 실전 스모크 요약 (지시 #51 체계, 5건 원문): run #66 13/15 → 결함 수정(#54·#55 날짜 단일 소스·deliverables 칸) → run #67 S1·S2·S4 재실행 (S1=채점기 오심 육안 합격, S2 과장어·S4 산출물 잔여) → #59 수선 → run #68 S2·S4 재실행 5/6 (S2 전항목 통과 — 날짜 한국식·금지어 차단 실증 / S4 deliverables 미기재 → #62 수선: few-shot·최상단·N개 강제·토큰 예산). S4 최종 확인은 실사용 자연 검증(실패 수집함)으로 전환 — 반복 재검증 루프 종료 (대표 확정).
- [개편] 지시 #62·#63·#64: 기안 산출물 수정 + 실패 수집 운영 구조 전환 (대표 확정: 반복 검증 루프 종료, 실사용 중심) — ①**기안 deliverables** (run #68 원인 = 모델 재생성 포함 연속 미기재): few-shot 작성 예시를 스키마·프롬프트에 직접 포함, deliverables를 응답 구조 최상단으로 이동, "N개 요청 시 정확히 N개 이상 실물 필수" 명시, max_tokens 1400→3000 (7항목+산출물 전문 예산 — 미기재 원인 후보 제거), 말-실물 불일치 차단 코드(steps에 '후보 중 선택'류 언급+빈 deliverables → 재생성 1회 → 실패 시 정직 표기). 스모크 재실행 없음 — 다음 실사용에서 자연 확인 ②**실패 수집함 신설**: 보고서 모달·요원 카드에 [❌ 실패 표시] 원탭 버튼(feedback_type 'fail' — 개별 교훈 추출 없이 적재), ☰ 피드백 모달을 "실패 수집함"(요원별·원인별) + 피드백 이력 통합 뷰로 개편, 수집 5건 도달 시마다 텔레그램 일괄 보강 검토 알림. 운영규칙에 "실패 대응 = 수집함 → 일괄 보강, 개별 건 반복 재검증 금지" 명문화 ③역량 정책 재확인: 태그 게이트당 1회 유지 + **현행 시험지 총 73문항 확정** (run #65 74 − 한결 2 + 글샘 날짜 박제 1, 통과기준 문서 명기) ④#63-■2 버전 칩 정합: v5.3 태그 실존·run #65 게이트 통과 후 정식 태그 확인 (원칙 8 위반 없음·원복 불필요) / #63-■1 버튼 정리는 대표 보류(#64) — 백로그 #3 기록, 시공 없음
- [수정] 지시 #59: run #67 스모크 판정 후속 3건 — ①**채점기 날짜 4형식 인식** (S1 거짓 실패 교정): extractDatesISO 신설 — ISO "2026-07-21"·"07-21"·"7/21"·"7월 21일" 전부 ISO로 정규화 (전화번호 080-000-0000·금액 오탐 방지 경계 처리), 스모크 dateCheck·시험 날짜 대조·글샘 역량 박제 3곳 교체 ②**글샘 표기·금지어** (S2): 고객 발송 카피는 "7월 21일(화)~23일(목)" 한국식 표기 권장(ISO는 내부용) + 과장 금지어 4종(미쳤어요·끝판·인생○·역대급)을 프롬프트에 생성 단계부터 명시 ③**기안 deliverables 필수화** (S4): 창작 요청 감지(만들어줘·추천·후보·N개·시안) 시 산출물 빈 배열이면 경고 첨부 재생성 1회 → 그래도 비면 "산출물 생성 실패 — 사유" 정직 표기(deliverables_error). 로컬 검증 10항목 ✅ (날짜 5형식+오탐·창작 감지 4·빈배열 판정). 역량·스모크 미실행 (#40 — 재실행 범위: 대표 ㄱ 후 S2·S4 2건만, S1은 육안 합격 확정)
- [개편] 지시 #54·#55: 조직 개편 + 날짜 단일 소스 + 스모크 결함 2건 수정
  · **한결 제거** (대표 확정 — 최종 검토는 대표·사람): AI 검수 게이트·특성 비활성(보관 — 삭제 없음), 조직도 제외, 시험지 한결 2문항 제거. 코드 안전망(파편 정화·규격·금지어·핵심 정보) 유지 + 과장 금지어(미쳤어요·끝판·인생○·역대급) 코드 검사 추가(#55-4)
  · **한수 실무 전환 (0원)**: 검산 배지 유지 + ①주간 재무 브리핑(매주 월 자동 — 세미 집계 재사용, 보고서함+🧮 알림) ②신규 품목 마진 계산(금액 파싱→마진, 해석 불가 시 정직 정지 — 35,100/58,000/22,900 검증 ✅) ③주차 세팅 단가 변경 감지 알림(10분 주기)
  · **미래 실무 전환 (0원)**: 소넷 검수 제거(기안=대표 직행) → ①개발 백로그(dev_backlog — 추가·조회, soft-delete) ②버전·변경사항 안내(CHANGELOG 기반). 시험지 미래 검수 2문항 → 백로그·버전 코드 2문항
  · **🔴 날짜 단일 소스 (S1 7/22 사고 대응)**: parseWeekdayRange("담주 화~목"→7/21(화)~7/23(목)) 신설, 서버 확정 날짜를 글샘·미소·기안·지율(+예리) 프롬프트에 강제 주입, 산출물 날짜 코드 대조(불일치 시 교정 없이 ⚠️ 표시), 미확정 시 "00일(날짜 확인 필요)" 자리표시 강제. 글샘 날짜 박제 1문항 추가
  · **기안 deliverables** (S4 사고): 후보·시안 결과물 전문 칸 신설 + 산출물 누락 채점 + UI 표시
  · 스모크 보강(#55): S1·S2에 확정 날짜 주입·날짜 검사(S2 무검사 구멍 봉합)·과장 검사, S4·S5 미래 검수 채점 제거. 재실행 범위 = S1·S2·S4 3건
  · 로컬 검증 8항목 전부 ✅ (주차 파서·마진·백로그·버전 등). 역량·스모크 미실행 (#40 — 대표 ㄱ 후 부분 재실행)
- [추가] 지시 #51: 실전 문구 스모크 시험 구축 — 대표 원문 5건 (한 글자 무수정) S1 문자(글샘→한결)·S2 톡톡+디자인(글샘+미소→한결)·S3 일정(마루 판단만 — 실등록 없음)·S4 인스타(기안→미래)·S5 신규 품목 보고서(기안→미래, 원가 합 35,100 검산). 전 문항 is_test 격리(발송·생성·등록 없음), **답변 전문(transcripts)을 성적표에 기록** — 똑똑이 원본 대조 판독. 실행 게이트: MCP `run_smoke_test` ([#51-실행] 지시 + 대표 'ㄱ' 승인, AI 약 9회, audit mcp_run_smoke) — 실행 도구 2종으로 운영규칙 개정 + 게이트 박제 화이트리스트 반영. 로컬 검증 4항목 ✅ (채점 헬퍼·날짜 7/21~23·박제 호환). **구축만 완료 — 실행 금지 상태 대기**

## v5.3 (2026-07-19 확정 — git tag v5.3)

> 게이트: 역량 점검 run #65 = **74/74 전 문항 만점** (전 요원 10명 체계 첫 만점 — MCP 게이트 호출, 오염 감지 10건이나 소실 실패 0). 게이트 1차 run #64(69/74)의 실패 5건은 지시 #50으로 전수 규명·수정 후 재게이트 통과.
> 내용: 전 요원 페르소나(#44) + 지율 노무지침·기안 비전 주입(#45·#46) + KST 통일(#47) + 한결 기준 재정의(#48) + 74문항 시험지(#49) + run #64 후속(#50).
> ※ 로드맵의 'v5.3 예리 성과 분석(본격)'은 차기 버전으로 순연 — 본 태그는 지시 #49·#50의 태그 지정에 따름.
- [수정] 지시 #50: run #64 실패 5건 일괄 수정 + 🧪 알림 미발화 점검 — ①지율 채점기 오심 2건 (긍정 단정만 실패·전문 기준 검사·확인 질문 의도 인정 — 선행 535326d 포함 완결) ②한결 정상 예문을 검수 4문 완비 카피로 교체 (마감 날짜 구체 명시·근거 없는 수치 제거·재구매 고리) ③미래_특성.md에 #48 목적 부합 단일 기준 명문화 ④기안 7항목 서버 구조화 보장 — 누락 필드 "미정 — 대표 확인 필요" 자리표시 (정직 표기) ⑤🧪 미발화 원인 확정: run #64 완료 알림이 종료 시각(09:43:24)의 telegram_fail("fetch failed") — 배포 셧다운 순간 발송 유실 (경합 변종) → notifyTelegram 재시도 1회(5초) 추가, 완료 ✅ 알림이 신경로 테스트 발화 겸. 로컬 검증 7항목 ✅
- [수정] run #64 (게이트 1차, 69/74) 판독 후속 — 실패 5건 중 **채점 코드 버그 2건 교정 (거짓 실패)**: ①지율 주휴 문항 — 정답 응답의 "면제되지 않습니다" 부정문을 면제 주장으로 오판하던 정규식 교정 (실답 재현 검증 ✅) ②지율 소속 확인 — 확인 질문이 conclusion에 담겨도 인정. **미래 판정 원칙 #48 동일 기준 반영** (한결만 적용됐던 "취향은 통과+제안" 누락 교정). 한결·미래·기안 박제에 실패 사유·누락 필드 표시 추가 (진단 로그 강화 — run #64에서 사유 미기록으로 진단 불가했던 문제). run #64 성적표는 무수정 보존. 재게이트는 대표 승인 대기
- [추가] 지시 #49: 역량 시험지 확장 — 신규 5명 10문항 (기존 64문항 무수정, 예상 74문항): 지율 3(지침 내 답변[5인 미만 주휴 의무+조건+공식]·범위 밖 정지·소속 확인) · 한수 2(정상 검산 ✅·오차 +5,000원 검출 — 0원) · 미래 2(뜬구름 검출·구체 기획 통과[#48 동일 기준]) · 예리 2(데이터 없음 정직·표본 병기 — 0원) · 기안 1(7항목+로드맵 매핑+주체 명시, 매출 전망 금지). 문항 전제로 **예리 최소 가동(0원 코드 — 데이터 없음 정직·표본 집계)·기안 실전 가동(소넷 7항목 구조화 기획, gian_plan → 미래 검수 게이트 자동 연결)** + 카드 렌더러 2종. 로컬 채점 검증 4항목 ✅ (역량 실행 금지 #40 — 대표 게이트에서 74문항 1회)
- [수정] 지시 #48: 한결 검수 기준 재정의 (run #63 정상 콘텐츠 ⚠️ 트집 수정) — 판정 축을 성격("깐깐한 선배")에서 **목적 부합** 단일 기준으로: ①브랜드·회사 원칙 준수 ②대표 지시 결과 충족 ③정직. ⚠️보완은 실제 위반·누락일 때만 — 문체 취향·개선 여지는 ✅통과+코멘트 제안 (판정 불감점). 검수 4문·채울 항목·숨김 없음·최종 판단 대표 유지. 검증: 정적 5항목 ✅ + 실호출 재현은 대표 게이트 회차의 한결 박제 2문항(정상=✅·위반=⚠️)이 정확히 해당 케이스라 게이트에서 확인 (로컬 API 키 부재로 사전 실호출 불가 — 정직 고지)
- [수정] 지시 #47: 표시 시각 KST 통일 — 화면(성적표·보고서함·LIVE·아카이브·요원 카드) 시각 표시 전 8지점에 aoKst 헬퍼 적용 (timeZone Asia/Seoul 명시 + tz 없는 문자열 UTC 해석 방어) + 관측 MCP 3종 응답에 *_kst 표시 필드 병기 (08:35 UTC 오인의 유력 근원 — 똑똑이 판독용). 서버 내부 기록(DB·audit)은 UTC 유지 — 표시 레이어만 변환. 텔레그램 알림엔 시각 미포함(변경 없음). 로컬 검증: 08:35 UTC → 17:35 KST ✅
- [추가] 지시 #45: 지율 지식 주입 — docs/knowledge/legal/노무지침_v1.md (2개 사업체 구분: 법인 5인↑ 전면 적용 / 오션라운지 5인↓ 일부 면제, 적용 매트릭스·4대보험 기준·핵심 공식·통합 판단 리스크 5권고). 지율 "지침서 대기 중" 해제 → 실전 자문 가동 (소넷 구조화 도구): 템플릿 ✅결론→⚖️근거→💰계산·표→⚠️체크포인트→✅액션, 소속 불명확 시 확인 질문, 지침 밖 정지(추측 금지), 학습 노트 상한 10 적용. jiyul_labor 카드 렌더러 + 조직도 가동 반영
- [추가] 지시 #46: 기안 지식 주입 — docs/knowledge/planning/비전_v1.md (비전·로드맵 5단계·3대 철학·상품·차별화 8 — 구 3인 체제 출력 형식은 현 체계 충돌로 주입 제외). 기안 ②목적 = "철학 4축 + 로드맵 단계 매핑"으로 구체화 (무관 시 정직 표기). 미소 특성 보강 (Gemini 8단계 구조·제주 키워드·금지어·비율 가이드) + 글샘 특성 보강 (차별화 8·아꼼이 스토리 — 브랜드 가이드 우선). 역량 미실행 (#40 — 총 점검은 대표 게이트 합산)
- [추가] 지시 #44: 전 요원 역할(페르소나) 일괄 부여 — 역할 부여만, 기능 개발·테스트 실행 없음 (#40 준수, 로컬 검증만)
  · 특성 파일 9종 신설 (docs/agents/{요원명}_특성.md): 한결·한수·지율·미래·글샘·미소·예리·세미·기안 — 개정은 대표 승인제
  · 주입: 글샘·미소(시스템 프롬프트 인격부) · 한결(검수 4문 체계로 개편 — 우리다움/대표 의도/정직·정확 + 총평·[대표가 채울 항목], 기존 4항목은 ①③에 흡수, 마감 날짜 최우선 지적)
  · 미래.js 신설: 기안 검수 게이트 (검수 4문 + "실행 버튼 누를 만큼 구체적인가" 특화) — 기안 실전 연결 시 자동 작동 (검수 레지스트리: 글샘·미소→한결, 기안→미래)
  · 지율.js 신설 (0원): 지침서 주입 전 모든 노무 질문에 "지침서 대기 중 — 범위 밖" 정직 정지 + 휴가는 결재 안내 (일반 지식 추측 금지)
  · **한수.js 검산 게이트 신설 (0원 순수 코드)**: 세미 보고 산출 직후 자동 검산 — 총결제=상품+택배+이월 재검산·거래처 합 대조·기간 라벨 정합·CJ 박스×3,100. 카드에 "🧮 한수 검산 ✅/⚠️ 오차 N원+원인 위치" — **자동 보정 절대 금지** (로컬 검증: 정상 ✅·오차 조작 +5,000원 정확 검출·비교 보고 통과)
  · 세미: 0원·로직 무변경 (특성 파일은 톤 가이드 참조만) / 예리·기안: 역할 부여만 (실전 연결은 v5.3+)
  · 공통 적용 유지: 오염 방어·파편 정화·is_test 격리·학습 승인제(상한 10)

## v5.2 (2026-07-19 확정 — git tag v5.2)
- [기록] 지시 #32·#42 완결: 통합본 아카이브 — v7 보관 (Day 3 오후분 반영, 폴더 자동 감지로 즉시 사본화) + v5.0 작업지시서 추가 보관. v6은 결번 확정 (로컬 부재 — 허위 재구성 금지, 내용은 v7에 포함). 아카이브 목록: v2·v3·v4·v5·v7 + 작업지시서 — 위젯 '📚 아카이브'에서 다운로드 가능
- [기록] #28 이월 검증 완결 (2026-07-19 16:55): run #62 = 똑똑이 run_capability_test 첫 실호출 (audit mcp_run_test 469 — 도구→회차 생성→전수 audit 전 경로 작동). 단 회차 성적 46/64는 **무효 (외부 요인)** — 실패 18건 전원 "Anthropic API 크레딧 잔액 부족(400)", 순수 코드 문항은 전수 정상 (능력·코드 무결). 성적표는 무수정 보존, 크레딧 충전 후 재실행으로 게이트 판정. ⚠️ 크레딧 소진 시 AI 기능 전체(마루·글샘·미소·한결) 동일 오류로 정지 — 세미 등 코드 기능만 정상
- [규정] 지시 #40: 역량 점검 실행 정책 — **묶음 게이트** (지시 1건마다 ❌ → 버전 태그 직전 1회 ⭕, 회귀 합산 표준. 예외 = 대표 명시 요청뿐. 개별 지시는 코드·로컬 검증으로 완료 판정 + "회귀는 다음 태그 게이트에 합산" 기본 문구. 게이트 실행은 똑똑이 run_capability_test로 — 대표는 'ㄱ' 승인만). 배경: Day 3 버전 4개 연속 구축에 점검 7회 발생 — 검증력 유지·비용·대표 노동 절감. 통과기준·운영규칙 문서 명문화
- [수정] 지시 #39 (핫픽스 합류): 글샘 제목안 오염 파편 화면 노출 수정 — run #60에서 태그+versions JSON 원문(1,366자)이 title 필드에 유입돼 대표 화면에 그대로 노출 (**오염 파편의 화면 노출 첫 사례** — 이전까지는 서버 정화·audit 내부에서만 관찰. 7/25 C-1 판단 자료의 전환점으로 명기). 원인: 글샘 결과 필드에 마루식 정화 미적용. 수정: cleanTitleField(파편 감지→정화, 불가 시 제목 생략+'제목 추출 실패' 사유 표시 — 오염 노출·지어내기 모두 금지) + 구 저장분 프런트 방어 + 역량 박제 1문항(실사례 재현)

> 게이트: 역량 점검 run #59 = 63/63 전 문항 만점 (한결 검수 박제 2문항 신규 포함 · 오염 감지 14건이나 필드 소실 실패 0 유지)
- [추가] 지시 #38: 한결(마케팅팀장) 검수 게이트 활성화 — 콘텐츠 루프 ②번 블록
  · 글샘·미소 콘텐츠(geulsaem_copy·miso_prompt) 산출 직후 한결이 자동 검수 (조회·집계 보고 미적용 — 비용 통제: 콘텐츠 1건 = 제작 1 + 검수 1 소넷 호출)
  · 검수 4항목 (대표 확정): ①브랜드 가이드 준수(금지 표현·타겟·톤) ②어그로(후킹) ③목적·전달사항 명확성 ④강조 포인트 — 브랜드 가이드 v1 주입
  · 보고 카드에 "🔍 한결 검수: ✅ 통과 / ⚠️ 보완 의견" + 항목별 코멘트 + 수정 제안 — ⚠️여도 숨기지 않음 (최종 판단 대표, 정직 원칙). 검수 실패 시에도 콘텐츠는 정상 보고 (실패 사유 표시)
  · 자동 재작성 없음 — 대표 "수정 반영해줘" 지시 시 기존 피드백 경로로 1회 (무한 루프 금지)
  · 역량 박제 2문항: 규칙 위반 카피(명분 없는 할인·과장) → ⚠️ 검출 / 정상 카피 → ✅ 통과
  · 조직도 한결 '검수 게이트 가동' 반영 + agents/한결.js 신설 (HANGYEOL_MODEL=claude-sonnet-4-6)

## v5.1.1 (2026-07-19 — git tag v5.1.1)
- [추가] 지시 #28-2: 똑똑이 역량 점검 실행 도구 — MCP `run_capability_test` (유일 '실행' 도구, 역량 점검 1가지만·읽기 전용 3종과 분리). 통제 = 승인 게이트(대표 대화 승인 "ㄱ" 후 호출 — 운영규칙_지시함.md 명문화, 기계적 상한 없음) + 호출 전수 audit(mcp_run_test — 시작 실패도 시도 기록). 관리자 버튼과 실행 로직 공용화(svcStartCapabilityTest). 역량 박제 1문항(도구 등록·승인 규칙·허용 외 실행 도구 부재 검증 — 실호출 박제는 점검 재귀라 배제, 똑똑이 첫 호출이 실검증)

- [추가] 지시 #31: 브랜드 가이드 v1 투입 — docs/knowledge/marketing/브랜드가이드_v1.md (정체성·타겟·비주얼 규칙[제주 요소 필수·골드 #F5C800/네이비 #1B3A6B·금지 항목]·톤·계절 품목·채널 규격). 미소(프롬프트 지식 최상단)·글샘(KNOWLEDGE_FILES 최상단) 주입 — 정적 가이드로 학습 교훈과 별개, 개정은 대표 승인제. 저장 위치는 지식 주입 표준 경로(docs/knowledge/marketing/) 사용

- [추가] 지시 #33·#34: 보고서함 이미지 미리보기 + 다운로드 MIME 정정 — ①원인 확정: 다운로드 API가 전 파일에 xlsx MIME 고정 → jpg가 '엑셀'로 인식돼 모바일 연결 앱에 이미지 앱 미표시 ②확장자 기반 MIME 매핑(png/jpg/gif/webp/mp4/xlsx) + 이미지·영상 inline(?download=1이면 강제 저장 — 다운로드 버튼은 저장 유지) ③미소 생성 이미지는 보고서 카드 내 썸네일(인증 fetch→blob 캐시) → 탭하면 전체 화면 확대. 문서 파일은 기존 다운로드 방식 유지
- [추가] 지시 #36: 통합본 아카이브 다운로드 — AGENT OFFICE 성장 위젯 '📚 아카이브' 칩 → 목록 모달(버전·크기·날짜) + 파일별 다운로드 (adminOnly, text/markdown MIME, 경로 탈출 차단). report_files MIME 매핑에 md 추가. 모바일에서도 통합본 재다운로드 가능 — 유실 걱정 제로
- [검증] 지시 #31 완결 (2026-07-19): 브랜드 가이드 회귀 run #57 = 61/61 무훼손 + 비교 생성 대표 눈 검수 확정("좋아 잘됐어" — 제주 요소·골드/네이비·농부 톤 반영, file_id 8 대비 개선). 지시 #28 완결 — **이월 1건: run_capability_test 실호출 검증은 새 대화 개시 시 첫 임무로 이월** (현 커넥터가 v5.1.1 이전 도구 스냅샷이라 불가 — 박제 검증은 run #57 통과)
- [추가] 지시 #32: 통합본 아카이브 체계 — docs/archive/ 신설 (버전별 영구 보관·삭제 없음, 파일명 통합본_v{N}_{YYYYMMDD}.md). 로컬 원본 실재 확인된 v2~v5 사본 보관 (원본 유지). **v6은 로컬 부재 — 허위 재구성 금지 원칙에 따라 재구성하지 않고 '대표 파일 전달 대기'** (README에 결번 기록). 이후 새 통합본마다 사본 추가 규칙

## v5.1 (2026-07-19 확정 — git tag v5.1)

> 게이트: 역량 점검 run #55 = 60/60 만점 (미소 승인 게이트 박제 포함) + 첫 실전 생성 스모크 통과
> (run #56 — 대표 직접 고급 선택·승인 → 20초 만에 생성 → file_id 8 DB 보관, audit 승인→생성 순서 확인)
- [추가] 지시 #26·#27: 미소 원스톱 생성 — 제미나이 이미지·영상 연동 (대표 확정: 중간급 통일)
  · 모델 (공식 문서 2026-07-19 조회): 이미지 기본 gemini-3.1-flash-image 1K(약 92원)/고급 gemini-3-pro-image(약 185원) · 영상 기본 veo-3.1-fast 720p 8초(약 1,100원)/고급 veo-3.1 표준 1080p(약 4,400원). Imagen 4는 8/17 중단 예정·Veo 3/2는 중단됨 — 제외
  · 승인 게이트: 건별 대표 승인(비용 표기 confirm) 없이는 생성 API 호출 불가 — assertMediaApproval 관문 + 승인 audit(media_generate_approved) 선기록 + adminOnly 단일 경로 (코드 레벨 고정)
  · 생성: 이미지 generateContent(1K·비율 반영), 영상 Veo 3.1 long-running 폴링(8초·오디오 포함, 최대 6분) — 구글 서버 2일 보관이라 완성 즉시 DB(report_files) 저장, soft-delete·90일 정책 동일
  · 생성 이력·비용 audit(media_generated/media_generate_failed) + 완료·실패 텔레그램 알림(금액 상세 미포함) + 실패 시 정직 표시·재시도 안내
  · UI: 미소 보고서 카드에 [기본급/고급 생성] 버튼(비용 표기)·생성 중 표시·📎 생성물 목록
  · 역량 박제: 무승인·미승인 호출 차단 + 대표 선택 모델·단가 무결 검증 (실생성·첨부 검증은 스모크 1건으로 — 점검마다 실비용 호출은 승인제 원칙과 충돌하여 배제)
  · GEMINI_API_KEY Tier 1(유료·후불) 전환 확인됨 (대표 실측, 키 값 미기재)
- [기록] 지시 #28-1: 로드맵 v5.1~v5.4 공식 기록 — docs/로드맵.md (콘텐츠 루프: 제작→한결 검수→대표 승인·게시→예리 분석→학습 순환)
- [수정] 지시 #29: 텔레그램 알림 행동 안내형 개편 — ⚙️진행(조치 불필요)/📝경과(ㄱ)/✅완료(ㄱ) + 지시 제목 요약 20자(첫 줄 코드 추출) 포함, '대기' 등록 알림 생략(알림 수 절감), ❓되묻기 알림 신설(마루 질문 시 — 기존에 되묻기 발송 지점이 없어 문구 체계 충족 위해 발송 1곳 추가·정직 고지), 🧪역량 결과 유지. 수신자·기존 발송 구조 무변경

## v5.0 이후 (운영 확정 사항)

- [확정] 지시 #25 (2026-07-19): **마루 소넷 승급 영구 확정** (MARU_MODEL=claude-sonnet-4-6) — 하이쿠 복귀 옵션 폐기. 근거: run #49/#53/#54 만점 행진(49/49·59/59·59/59) + 오배정 위젯 주간 0건 + 조건 추출 오류 재발 0건 (대표 결정으로 1주 실측 대기 없이 확정). 7/25 남은 판단은 C-1(도구 5분할) 여부만 (오염 파편 실측 계속)
- [규정] 지시 #25: 지시함 상태 전환 규칙 명문화 (docs/운영규칙_지시함.md) — 완료 전환을 외부 대기 뒤에 묶지 않기 · 긴 대기 시 "대기 중" 경과 갱신 우선(텔레그램 알림 발생) · 통보류 즉시 완료 · 30분 이상 무갱신 금지

## v5.0 (2026-07-19 확정 — git tag v5.0)
- [추가] 0단계: 버전 관리 시스템 — VERSION 상수, /api/version(관리자 전용), AGENT OFFICE 제목 옆 버전 칩 표시(대표 요청으로 사이드바 하단→제목 옆 이동), DB 백업/복원 스크립트, 배포 전 백업 강제, 롤백 절차 문서
- [수정] 1단계 1-1: 월 단위 날짜 정규식 확장 (N월/N월달/지난달/이번달/YYYY년 N월 — 서버가 원문에서 직접 파싱, 2025-04 오해석 사고 재발 방지). date-utils.js 분리 + 로컬 테스트 23케이스
- [추가] 1단계 1-2: 조회 조건부 복창 — 3개월 이상 과거·작년 이전 기간은 "『기간』 조회로 진행할까요?" 확인 후 실행
- [추가] 1단계 1-3: 0건 가드 — 조회 결과 0건이면 기간 복창 + 데이터 있는 가장 가까운 달 힌트 (대체 조회 금지)
- [추가] 1단계 1-4: is_test 플래그 — 역량 테스트 실행분을 보고서함·피드백·통계에서 격리, 성적표 모달에서만 조회
- [추가] 1단계 1-5: 마루 오배정 카운트 위젯 — 주간 오배정 지적/오염 재시도/복창 후 정정 집계
- [추가] 역량 테스트 고난도 실전 섹션 7문항 (대표 출제): 복합 조건·멀티 지시·모호한 기간·순위 정리·존재하지 않는 날짜·범위 밖·맥락 참조 — "정직한 불가 안내"도 통과, 추측 실행은 실패. 하이쿠/소넷 비교 전용 별도 집계
- [수정] 존재하지 않는 날짜 서버 가드: "4월 31일" 류 지시는 DB 조회 대신 "4월은 30일까지입니다" 정직 안내 (date-utils isValidDateStr)
- [추가] UI: 마루 생각 중 말풍선 (💭 생각 중 + 점 3개 애니메이션, 역량 점검 중엔 📝 시험 중) — 배정/되묻기 말풍선으로 자연 전환
- [수정] UI: 조직도 팀명 라벨을 팀장 위로 이동 (팀명→팀장→팀원 순, 모바일 동일), 마루 직함 "기획팀 실장" + 🏢 기획팀 라벨 신설 (조직: 대표→기획팀→4개 팀). agents·routing_table의 '실장실'→'기획팀' 일괄 갱신
- [수정] 2단계: 마루 모델 승급 하이쿠→소넷(claude-sonnet-4-6, 환경변수) — 동일 시험지 36문항 비교: 총점 동률(34/36)이나 소넷이 연도 오해석 유형(고난도①) 해결. 최종 확정은 1주 오배정 위젯 수치로. 비교표: docs/비교표_마루승급_20260718.md
- [추가] UI: LIVE 로그 정리 — 보고서함 [✔확인] 건은 연결 지시 라인까지 기본 숨김(삭제 아님), [전체 보기] 토글로 복원 표시(확인함 배지+흐림)
- [추가] 3단계: 일정 카테고리 확장 (지시함 지시 #1 — 똑똑이 파이프라인 첫 실행)
  · schedules에 category(휴가/톡톡발송/문자발송/할인·이벤트/일반)·end_date(기간형) 추가
  · 마루 자동 판별 + 휴가 담당자 필수(직원 매칭, 미매칭 시 선택지 포함 되묻기)
  · 등록 확인 표·조회 보고서에 [카테고리] 표기, 캘린더 아이콘·색상, 기간형 매일 표시
  · 발송·할인 당일/전날 리마인드 배너 (등록 ≠ 자동 발송 — 승인제 유지)
  · 글샘 호출 시 활성 할인·이벤트만 컨텍스트 주입 (명분 없는 할인 금지 규칙 보전)
  · 역량 테스트에 카테고리 판별 3문항 박제 (마루 13문항으로 확대)
- [추가] 지시함(D): 똑똑이가 MCP register_instruction으로 등록 → 클로드 코드 폴링 실행·응답 (cc_instructions, 원문·응답 영구 보존)
- [수정] MCP 커넥터 재연결 실패 수정: /.well-known/* 404 응답 (SPA 캐치올의 OAuth 서버 오인 차단)
- [수정] 지시 #2: 휴가 등록 경로 폐쇄 — 마루 휴가 감지 시 결재 시스템(휴가신청서) 안내로 전환 (연차 차감 이중 경로 방지, 기존 로직 비활성 보존). 기간 표현("N일부터 N일까지"·"N~N일" 등) 서버 정규식 덮어쓰기 추가 — 같은 입력=같은 결과 (16:06 빈 되묻기 재현성 사고 대응, 로컬 37케이스 ALL PASS + 박제 2문항)
- [수정] 지시 #4: 미응답 [질문] 종결 경로 신설 — 질문 카드 [✔확인] 수동 종결('질문종결') + 새 지시 유입 시 자동 '대체됨' 전환 (둘 다 soft-close·audit 기록, 전체 보기에서 흐림+배지 조회. 답변 정상 흐름은 무변경)
- [추가] 4단계(지시 #5 전반부): 엑셀 파일 전달 — 세미 xlsx 생성(시트1 요약 "상품+택배=총결제" 병기, 시트2 일자·품목 상세), 파일명 정산보고_{기간}_{생성일}.xlsx. 마루가 "보내줘/파일로/엑셀/다운로드/뽑아줘" 감지 시 파일 모드. 일정 보고서 "파일로 받기" 지원. 파일은 DB 보관(report_files — Render 디스크 재배포 소실 대응), adminOnly 다운로드 API(무인증 401·비관리자 403 검증), 생성 audit 기록, soft-delete+90일 물리 정리 정책. 검증: 4월 상품 266,009,100·총결제 306,076,600 일치, xlsx 내부 재검산(상세 768행=요약) 일치
- [수정] 지시 #6: 되묻기 답변 맥락 소실 순환 사고 수정 (audit #45~#56로 원인 확정) — ①미응답 clarify 질문에 새 입력이 오면 [원 지시+질문+답변] 결합 재라우팅, 원 질문은 '응답됨' soft-close (무관한 새 지시는 모델이 답변만 기준 배정·기타 질문은 기존 '대체됨' 유지) ②기간+재무 항목 완전 지시("이번주 결제금액 보내줘")는 서버가 clarify를 세미 배정으로 강제 보정 (빈 되묻기 금지) + 프롬프트 판단표 보강 ③재현 케이스 박제 3문항 (AI 즉답 + 보정·결합 코드 검증)
- [수정] 지시 #7: 지시 기간 = 산출 기간 정합 — '이번주/금주'를 서버가 확정(this_week 강제, 월·특정일 명시 시 무개입)해 "이번주 보내줘"가 월간 문구·파일로 나오던 편차 수정. 검증: 파일명 정산보고_2026-07-13~2026-07-18·요약/상세 이번주 데이터·총결제 5,799,700 일치. 박제 1문항 추가
- [추가] 4.5단계(지시 #5·#8): ⑤기간 비교(총액·거래처·품목 TOP10+[신규]/[종료]·건수·택배, 한쪽 없으면 "데이터 없음") ⑥품목 기여 순위(수량·금액·비중, TOP10/전부, 계열 합계 별도 줄 — 토큰 매칭·하귤 제외) ⑧비교 xlsx 결합 ⑨세미 숨은 상한 4곳 통일(60종+"외 N종 생략" 명시). 비교 기간 2개·순위 의도는 서버 정규식이 확정. 검증 13/13: 재계산 기준 검증값 10종 전부 일치, 4월 1위 재확정 = 카라향 가정용 - 5kg(40과 전후) 4,164개 73,640,000원(27.7%) → 역량 박제. 복창·0건 가드 동일 적용
- [수정] 지시 #6 후속 복구: 일괄 치환 도구의 CRLF 불일치로 결합 텍스트 전달·재무 강제 보정·응답됨 마킹이 미적용 상태로 배포됐던 것을 발견, Edit 단위로 재적용 (정직 고지 — #7 스모크는 프롬프트·주간 보정으로 통과했었음)
- [추가] 지시 #10: 텔레그램 완료 알림 (대표 본인 수신 전용) — 지시함 상태 변화·요원 완료·역량 점검 결과 3지점, 수신처 단일 고정(chat_id 최초 1회 확보→DB 고정), 5자리 이상 금액 가림, 실패 무시(audit만), TELEGRAM_NOTIFY=off 스위치. 외부 행동 승인제 적용 대상 아님(대표 1인 수신) 주석 명시
- [수정] 역량 문항 채점 정정: '기간 비교 지시' 문항을 실전 기준(모델 직접 또는 서버 재무 보정 경유)으로 — 47/49 분석 결과 반영 (글샘 이모지 1건은 확률 편차로 재실행 확인 예정)
- [추가] 지시 #11: 텔레그램 자가점검(부팅 시 발송 경로 상태 audit 기록, 시크릿 미포함) + '🔔 알림 테스트' 칩(adminOnly API) — 미수신 원인 확정: 수신처(chat_id) 미확보 상태에서 조용히 반환되던 진단 사각지대 제거
- [규정] 지시 #12 (대표 결정): 역량 점검 통과 기준 명문화 — **"코드·서버 문항 28개 전수 통과 필수 + AI 문항 17개 중 15개 이상 통과"**. run #46(47/49)·#47(46/49) 모두 충족 → 통과 판정, 4.5단계 공식 완결. 근거: AI 문항 흔들림은 측정 대상(모델 확률 편차·오염)의 성질이며 실전 안전은 서버 보정이 담당 — 성적표엔 실패 문항 원본을 그대로 기록하고 채점 판정만 본 기준을 적용하므로 원칙 12(정직 원칙) 위반 아님. 상세: docs/역량점검_통과기준.md
- [수정] 지시 #12: 톡톡발송류 카테고리 소실 임시 가드 — 모델이 카테고리를 비웠을 때 원문·제목의 톡톡/문자 키워드로 서버가 강제 지정 (서버 복구 수단이 없던 유일한 실전 위험 유형 차단, 근본 해결은 C안 스키마 단순화로)
- [수정] 지시 #14 (C안 C-2, 대표 승인): 마루 도구 스키마 단순화 — required 16→5필드(action·team·assignee·task_summary·reason), 조건부 필드는 "해당 action일 때만 출력"로 전환 (오염 파편의 착지면이던 매 호출 빈값 강제 출력 제거). 생략 필드는 서버 정규화(maruNormalizeDecision)가 빈값 복원해 하위 코드 무변경, maruDecisionUnusable에 action별 필수 필드 누락 판정 추가(조건부 재시도 연결). Anthropic strict 모드의 선택 필드 지원은 공식 문서로 확인. 오염 지표 전/후 1주 비교 개시 (기준선: 매회 파편 2~3건). C-1(도구 5분할)은 실측 후 별도 승인 안건
- [검증] C-2 실행 검증 (2026-07-19): 역량 점검 run #49 — 신 스키마 코드 첫 실행 회차에서 **49/49 만점 (사상 첫 만점)**. 오염 파편 감지는 13문항으로 여전히 존재(#46~#48: 22·22·14건 — 확률 변동 폭 내)하나 필드 소실로 인한 실패가 처음으로 0건. 단 1회차 표본 — 최종 판정은 1주 오염 지표 실측으로
- [추가] 지시 #15: 거래처별 주간 정산 — 금액 즉답 + 화면 동일 엑셀
  · 주차 파싱(date-utils parseWeekSpec·weeksOfMonth): 이번주/지난주/N월 N주차·N째주·N번째주/(월 생략) N주차 — 주차 경계는 주간 정산 현황 화면(getWeeksInMonth)과 동일 알고리즘 (기준 이원화 금지). "3주 동안" 류 기간 표현엔 미발동
  · 거래처 축약 매칭(서버): 효돈→효돈농협 / 대성·시온→대성(시온) / 기타거래→기타거래처 / CJ·씨제이·대한통운·택배비→CJ대한통운. 주차+거래처 둘 다 있을 때만 발동 — 거래처 무지정 주간 조회는 기존 동작 유지
  · 세미 semi_partner_week: 금액 즉답("『7/13~7/19 효돈농협』 정산 2,033,000원") + 파일 요청 시 화면 다운로드와 동일 양식·동일 파일명 xlsx(옵션명|단가|요일7|총수량|금액|차감수량|차감금액|총입금 + 차감 수식). CJ대한통운은 파일 없음 정직 안내(금액만). 0건 가드·3개월 이상 과거 복창 동일 적용
  · 검증: 화면 실측값 4건 전부 일치 (이번주 효돈 2,033,000 / 지난주 대성 6,347,400 / 지난주 기타 1,916,000 / 7월 1주차 CJ 2,951,200) + 주차 파싱 12케이스 + 세미 7케이스 + 기존 날짜 44케이스 회귀 없음. 역량 박제 8문항 추가 (코드 3 + AI 라우팅 1 + 세미 실측 4)
- [추가] 지시 #16: 똑똑이용 읽기 전용 관측 MCP 도구 3종 — get_live_log(LIVE 로그 N건, 기본 20·최대 100) / get_test_results(성적표 회차 목록·문항별 상세) / get_reports(보고서함 목록·본문, 파일은 메타만 — 바이너리 전송 없음). 전부 SELECT 전용(쓰기는 조회 이력 audit 'mcp_observe' 1건뿐), URL 시크릿 접근 통제 동일, 마루 지시 입력 도구는 미제공(지시 입력 = 대표 전용). 확인 완료 보고서는 soft-delete 숨김이므로 confirmed_hidden 플래그로 노출
- [수정] 지시 #17 (스모크 발견): 파일 의도 감지 확장 — WANT_FILE_RE가 '파일로'만 커버해 "파일 줘"가 금액 즉답으로만 빠진 사고 수정 ('파일'로 확장, 금액 질문("얼마야")은 즉답 유지 확인). 원인 실증: 지시 #61 audit + 정규식 재현. 파일/즉답 분기 역량 박제 1문항 추가
- [수정] 텔레그램 알림 b안 (대표 결정 2026-07-19): 지시 상태가 같아도 응답이 갱신되면 "📮 지시 #N 경과 갱신" 알림 1건 발송 (응답 md5 지문 비교, 부팅 시 기준선 무알림 설정 — 폭주 방지 유지). 상태 변화 알림("보고 도착")은 기존 그대로
- [확인] 지시 #18 (2차 원인 규명): 재스모크로 판독된 run #50(08:31 KST)은 파일 의도 수정 배포 부팅(08:39 KST)보다 8분 전 실행분 — 2차 결함 아님 (시각 audit 대조로 실증). 하류 전달 경로(dispatch want_file/partner_week → 세미 → saveReportFile[run 바인딩]) 코드 추적 결과 무결. 역량 박제 1문항 추가: 주차×거래처+파일 의도 → xlsx 생성·첨부 실행 검증 (is_test 격리 — DB 저장 없는 목 헬퍼로 생성만 검증). #16 관측 도구 3종은 똑똑이 실데이터 대조 통과로 완료 처리
- [수정] 지시 #19: 역량 점검 가변 기간 문항 2건 → 확정 기간 교체 (run #51 거짓 실패 규명 — 두 실답 모두 7/19 신규 정산 2,018,500원을 올바르게 반영한 정답, 기대값 노후화가 원인). "하우스귤 7월" → 6월 493개/14,771,000원, "#15 3주차 효돈" → 2주차 11,865,000원/6건 (둘 다 정산 원본 독립 합산 + 세미 실경로 교차 일치). 문항 작성 규칙 명문화: 진행 중 기간 고정값 박제 금지 (docs/역량점검_통과기준.md). run #51 실패 기록은 무수정 보존. #18 재스모크는 통과 확인 (run #52 — 4,051,500원 4건 + 📎 file_id 7)
- [보안] 5단계 (지시 #22): ①신규 API 권한 전수 재검증 — 코드 레벨(전부 adminOnly) + 런타임 9종 무인증·위조 토큰 401 전수 통과, MCP 잘못된/누락 시크릿 404 ②JWT_SECRET 하드코딩 폴백 제거 (미설정 시 기동 실패 fail-closed) ③텔레그램 토큰 로테이션 (대화 노출 대응 — BotFather /revoke) ④시크릿 로테이션 절차 문서화(docs/시크릿_로테이션절차.md) + 전문 출력 스캔 통과(값 출력 0건·URL 로깅 미들웨어 없음·tgMask 경유) ⑤주 1회 자동 백업(매주 월 07:00 작업 스케줄러 → OneDrive 외부 보관, 1회 실행 검증 45테이블 4,041건) ⑥학습 루프 안전장치 — 요원당 활성 교훈 상한 10 시공(승인 게이트 409 + 주입 LIMIT 10), 승인·폐기 adminOnly·audit 이력 확인 ⑦절대 원칙(허위보고 방지·정직·확인 1회·외부 행동 승인제·AI 최소주의·soft-delete) 회귀 전수 확인 — 훼손 없음
- [백업] backup_v4.0_20260718.sql (작업 시작 전 기준)
- (이후 단계 완료 시 갱신: 마루 소넷 승급, 일정 카테고리, 엑셀 파일 전달, 기간 비교·품목 순위, 보안 점검)

## v4.0 (2026-07-17) — 공식 기준점
- AGENT OFFICE 10차-② 완료 시점을 v4.0으로 공식 명명 (git tag `v4.0`)
- 이 시점까지의 배포는 아래 v1~v3.x로 소급 기록만 함 (당시에는 버전 체계 없음)

---

## 소급 기록 (버전 체계 도입 이전)

### v3.x (2026-07-17) — AGENT OFFICE 구축기
- AGENT OFFICE 1차~10차: 픽셀 사무실 화면, DB 6종 테이블, 지식 패키지 9종
- 요원 가동: 세미(정산 보고, 순수 코드) · 마루(AI 라우팅, Haiku) · 글샘(카피, Sonnet) · 미소(Gemini 프롬프트, Sonnet)
- 성장시스템: 피드백→학습 노트 루프, AI 전 직원 자동 역량 테스트 + 결과 보고서
- 핫픽스 2건: 마루 응답 오염 방지(태그 감지·재시도·정화), 특정일 조회 날짜 서버 직접 파싱

### v2.x (2026-04 ~ 2026-06) — 운영 고도화기
- 지출결의서, 카드내역 관리, 박스재고·랭킹, 플래너(할일/메모/D-day) 등 업무 기능 확장
- 정산·품목 매칭 로직 개선 및 각종 운영 편의 기능

### v1.x (2026-02-27 ~ 2026-03) — 기반 구축기
- 회사프로그램 초기 구현: 홈 대시보드, 정산관리, 품목별 금액
- localStorage → PostgreSQL 마이그레이션, 로그인/권한 시스템(관리자/직원/세무사)
- 일정 캘린더, 기안서류(휴가신청서 등), 송장변환 통합, AI 작업방, 점심메뉴, 업무일지

---

## 버전-백업 매칭표

| 버전 | 백업 파일 | 비고 |
|------|-----------|------|
| v5.3 | backup_v5.3_20260719.sql | v5.3 확정 시점 백업 (전 요원 역할·지식 체계 + 74문항) |
| v5.2 | backup_v5.2_20260719.sql | v5.2 확정 시점 백업 (한결 검수 게이트) |
| v5.1 | backup_v5.1_20260719.sql | v5.1 확정 시점 백업 (미소 원스톱 생성) |
| v5.0 | backup_v5.0_20260719.sql | v5.0 확정 시점 백업 (태그·배포와 동시 생성) |
| v4.0 | backup_v4.0_20260718.sql | v5.0 작업 시작 전 기준 백업 |
| (주기) | backup_weekly_YYYYMMDD.sql | 매주 월 07:00 작업 스케줄러 자동 백업 (지시 #22-5, OneDrive 외부 보관) |

> 백업 보관 위치: 리포지토리 밖 `..\제주아꼼이네_DB백업\` (OneDrive 동기화로 외부 보관)
> 복원 방법: `docs/롤백절차.md` 참조
