# 제주아꼼이네 회사프로그램 — 네이버 커머스API 연동 인수인계

> **이 문서 목적**: 새 세션(새 터미널)에서 이것만 봐도 네이버 API 연동 작업을 그대로 이어가기 위한 상세 기록.
> **최종 갱신**: 2026-08-24 (v5.9.287 / app.js v=369 — 지시 #404까지 종결 · 다음 번호 #405). 과거 세션 상세 = 같은 폴더 `CLAUDE_ARCHIVE.md`.
> 요약본 아님. 구체적으로 적음. 관련 상세는 리포 `NCP인증키/네이버커머스API_연동_인수인계서.md`(최초 설계)와 자동메모리 `jeju-naver-commerce-api.md`도 참조.

## 🔀 병행 세션 규칙 (대표가 터미널 2개를 동시에 씁니다 — 2026-08-12 확립)

- ⚠️ **지시 번호 중복 실사고(2026-08-12)**: 같은 날 **#379가 두 건**에 붙었다 — **#379(8/11 톡톡봇 발송휴무일 연동)** 와 **#379(8/12 귤나무 레벨 튐·리액션)**. 두 세션이 서로 모르고 같은 번호를 썼다. **내용·날짜로 구분할 것.**
- 🔴 **번호를 붙이기 전에 반드시** `git pull` 후 CLAUDE.md에서 **마지막 번호를 확인**하고 그 다음 번호를 쓴다. 커밋 메시지·CHANGELOG에 박히면 되돌리기 어렵다.
- 🔴 **스킨 파일(farm/index 등)을 고치기 전에 3단계**: ①`git pull` ②`scripts/download.js`로 **서버 원본을 받아 로컬 정본과 해시 대조** ③다르면 **상대 세션이 최신** — 덮지 말고 서버본을 받아서 그 위에 작업. (2026-08-12 실제로 이 절차 덕분에 귤나무 작업과 후기 작업이 **서로 덮지 않고 공존**했다.)
- **업로드 후에도 재대조**: 내가 올린 뒤 상대가 덮었을 수 있다. 서버본을 다시 받아 **양쪽 변경이 다 살아 있는지** 확인할 것(영문 토큰 + 한글 문구 각각).
  - ⚠️ **PowerShell `-match`는 한글 매칭이 어긋난다**(실측: 있는 문구를 False로 반환) → **문구 확인은 Node로** 할 것.
- **배포는 따로**: 핵심 로직이 겹치면 반드시 세션별로 나눠 배포(대표 확정 원칙). 배포 직전 `git log`·`git status`로 상대 커밋 확인.

## 🔴 무회귀 원칙 (상시 최우선 — 지시 #209 대표 확정, 2026-08-04)

> 대표 원문: **"코딩하면서 이게 뒤틀리고 저게 뒤틀리면 의미가 없다"**

- **모든 시공은 기존 작업(회사프로그램·자사몰 v5) 무회귀가 전제다.**
- **수정 범위 밖 화면·기능이 멀쩡한지 스모크로 확인하기 전에는 완료 보고 금지.**
- 🔴 **"코드상 정상" 판정 금지 — 실DB·API·실렌더 3겹으로 검증한다.**
- **근거(2026-08-04 하루에 회귀 3건, 전부 대표가 실물로 발견)**:
  - **#187** — `pad2` 스코프 오류로 **렌더러 전체 사망**
  - **#192** — v5Cats/v5Views가 v5Render보다 먼저 실행돼 `if(!V5) return`으로 **에러 없이 조용히 죽음** → **클릭 전멸**. 🔴 "콘솔 에러 0"만 보고 통과시킨 것이 원인
  - **#199** — 행 표시만 고치고 **요약 집계는 옛 기준 그대로** 둔 채 "완료" 보고 → 선물하기가 경고에 남음
- 함께 지켜온 검증 교훈(재발 방지): 패치 스크립트는 삽입 후 **`grep -c`로 실삽입 확인** + **정규식·SQL placeholder는 그 값으로 1회 실행**해 판정(#178·#179) · UI는 **실제 마크업에 클릭**(하네스 직접 호출 금지) · 재렌더 후 **selector 재질의**(detached 노드 클릭 금지).

## 🔴 운용 체제 (상시 최우선)

### ★ 현행: 한시 폴링 재개 (지시 #197, 2026-08-04 대표 확정)
- **폴링 주기 3분** — 작업 세션 동안 신규 지시를 자동 감지. 신규 건 없으면 즉시 대기 복귀(토큰 절감).
- **종료 조건**: 대표 또는 똑똑이가 **"폴링 종료"** 지시 → 즉시 아래 호출 대기 체제(#167)로 복귀. 그 전까지 유지.
- **협업 흐름**: 대표 → 똑똑이(전략·판단) → 지시함 등록 → **클코가 폴링으로 자동 감지·처리** → 완료 보고 → 똑똑이 검수 → 대표 보고. 대표는 판단·실물 확인만.
- 🔴 **보고 규약(강화)**: 똑똑이는 **지시함 응답만 보고 검수**한다. 터미널에 쓴 내용과 **동등한 수준으로 지시함 응답을 작성**할 것 — 짧게 쓰면 정보가 유실된다.
- 🔴 **판단 지점 처리**: 대표가 실시간으로 터미널을 보지 않으므로 **작업 중 대화형으로 묻지 말 것**. ①안전한 쪽을 택해 진행하고 판단 근거를 보고에 명시 ②되돌릴 수 없거나 위험하면 **착수하지 말고 "대표 확정 필요"로 보고**. 임의 결정 금지는 그대로.
- 폴링 중에도 기존 원칙 전부 유효 — 외부 행동 승인제·자동수정 금지·발송 파이프라인 신중.

### (보존) 호출 대기 체제 — 지시 #167, 2026-08-01
- 5분 상시 폴링 폐지. 평시 대기 — 대표가 "지시함 확인" 입력 시에만 poll-light.js로 조회·처리. 완료 직후 보충 지시 1회 추가 조회 후 대기.
- **"폴링 종료" 지시가 오면 이 체제로 돌아온다.**
- **poll-light**: `node "%LOCALAPPDATA%\Temp\claude\...\scratchpad\poll-light.js"` (세션 스크래치 — 새 세션이면 cc_instructions 직접 조회 스크립트 재작성: '대기·진행중' 전건+본문, 첫 줄 verdict).
- 기존 속도 규칙 B(5분 폴링)는 이 체제로 대체. 규칙 A(메인 단독)·품질 그물망·보고 규약(#113)은 유지.


## ℹ️ 8/24 오후 — Anthropic 전면 장애 기록 (해소·인수인계 완결)

- 8/24 오후 529/500 전면 장애로 병행 세션들이 중단됐으나 **작업물 유실 0·인수인계 전부 완결**: #403 = DB 직접 등록·바탕화면 전달문·자동메모리 / #404 = 커밋 aa4f139(v5.9.287) / 세션 마감 인수인계 = 커밋 680df85. 아래 「8/23~24 세션 마감 인수인계」 절이 정본.
- 🔴 **장애로 끊겼던 지시 「톡톡·문의답변 제목 생성(마루)」 = 대표 취소(8/24 "안 해도 된다") — 착수 금지·재제안 금지.** (배경 기록: 톡톡 16/19·문의답변 6/6 제목 실패 = 도구 JSON 태그 오염 시 평문 폴백이 **본문만** 받는 구조라 제목이 안 생김 — 본문 카피는 전 건 정상·실피해 없음. 나중에 대표가 다시 원하면 #396 정산 OCR과 같은 해법 = 평문 JSON 폴백에 제목 필드 포함.)

## 📦 과거 세션 기록 = CLAUDE_ARCHIVE.md (2026-08-24 분리 — 무손실)

- CLAUDE.md가 150k자 제한 초과(251k)로 뒷부분이 잘려 읽히던 문제 교정. **과거 세션 인수인계 상세 전문을 같은 폴더 `CLAUDE_ARCHIVE.md`로 원문 그대로 이동**(8/21 세션 상세 #392~#395 · 8/5~8/8 세션들 · 8/4 이전 · 7월 기록 전부).
- 과거 결정·함정·검증 이력이 필요하면 **CLAUDE_ARCHIVE.md를 Read로 열어 검색할 것**(자동 로드 아님 — 컨텍스트 절약). 이 파일에는 현행 규칙·최신 세션·상시 참조만 유지한다.

## ★★★★ 8/23~24 세션 마감 인수인계 (#396~#404 종결 — 새 세션은 여기부터) ← 최신

**현행 버전: 회사 v5.9.325 / app.js v=383(무변경) · 주문정리기 번들 extract v=3 · organizer v=4** (9/16 배포). ⚠️ **다음 지시 번호 = #453부터** (#405~#452 = 8/24~9/18 세션).

- 📦🧪 **#452(대표 GO 9/18 — 송장변환 업그레이드, 테스트 버전 먼저 · 본 화면 무접촉)** — v5.9.323(서버 + `public/invoice-test.html`·`invoice-test.js` 신규 · app.js 무변경).
  - **대표 확정 구도(9/18)**: ⓐ 개별발송 처리 번호(입력o·삭제x)는 **1회성 입력**(저장 X — 조회기간 칸처럼) · 채널별 3칸(네이버 구매자 연락처 / 자사몰·쿠팡은 연락처 또는 주문번호) → **시트1(택배사)에서 빠지고 시트2엔 남되 노란 배경·맨 아래** ⓑ 배송메모: 시스템이 읽어 지정일이 오늘보다 뒤인 건 **미리 체크**, 애매한 건 **「확인필요」로 띄우되 체크 안 함** → 직원이 검토 목록에서 체크/해제 · 체크된 건은 **시트1·시트2 모두 제외** ⓒ 시트2 = **네이버 원본 양식 그대로**(안내 1행·27열·시트명 「발주발송관리」) · 시트1과 행 순서 동일 → 택배사 송장을 순서대로 붙여넣기 ⓓ 요 이틀 실제 흐름 = 네이버 파일 드래그(불러오기 아님)라 **파일 업로드·API 둘 다** 지원.
  - **구현**: 페이지가 `/app.js`를 fetch해 `function detectSize`~`// 채널 초기화` 구간(변환 3종·exportInvoiceExcel·단가표 로드)을 `new Function`으로 실행 → 시트1은 본 화면 실코드 그대로(XLSX.writeFile을 잠시 가로채 워크북에 시트2 추가 후 저장). 서버: `naverFetchInvoiceOrders(days, {extended})` — 미지정 시 종전 동일 · `GET /api/agent-office/naver/invoice-orders-v2`(audit naver_invoice_fetch_v2) · `POST /api/agent-office/invoice/memo-parse`(기준 = 오늘 07시 주문 → 오늘 발송이 정상인 메모는 null·지정일 뒤면 ship/arrive·애매 ack) · shipping-schedule 점 날짜(9.22.) 인식(단위 제외). 채널 재로드 시 그 채널의 이전 체크 상태는 버림(인덱스 재사용 함정).
  - ✅ `verify-452-invoice-test.js` **27/27**: 오늘 원본 97건 — 자동 체크 22 = 서버 규칙 · 확인필요 26(ack 22 + 넓은 표현 4) · 개별발송 2명 5건 회색·체크박스 없음 · 토글 ±1 · 시트1 70=97−5−22 · 시트2 안내문·27열 헤더 원본 동일·행수 75·순서 1:1(수취인연락처)·노란 5행 맨 아래·날짜/₩ 서식 · 🔴 **제외 0일 때 시트1 1078셀 = node로 돌린 본 화면 실코드 결과와 차이 0** · API(_x) 경로 발송대기·ID 마스킹·서식 · pageerror 0.
  - **현행 실측(9/18)**: 본 화면 배송메모 빨간 표시는 정상 작동(67 메모 중 44 표시) — 「9.22.(화)」 점 날짜만 누락(테스트판에 보강) · 변환은 행을 합치거나 나누지 않음(97→97·순서 유지).
  - **#452-b(대표 제공 3일치 원본 9/16·9/17·9/18 = 667·718·549건 실메모)**: 해석기 보강 4종 — 숫자+요일 같은 날 하나로 · 키워드 창 = 날짜 표현 전체 구간(앞 12·뒤 10) · 도착 키워드 우선(「배송」 일반어) · 「M월D~D일」 범위 = 확인형(「1~2일 정도」 기간은 제외). 알림톡 지정일 반영에도 그대로 적용(v5.9.324). `verify-450-memo.js` 41/41 · 3파일 각각 27/27. 재분류 후 남는 확인필요 = 「22일이나 23일」「20일 배송」「9월23일 도착(도착불가일)」「9월19일 토요일 발송」「9/21 예약배송」 — 전부 사람이 봐야 맞는 것. 검증 스크립트는 파일 경로 인자를 받는다(`node scripts/verify-452-invoice-test.js <원본.xlsx>`).
  - **#452-c(대표 9/18)**: ⓐ 네이버에서 직접 받은 파일은 **비밀번호(SMARTSTORE_FILE_PASSWORD·기본 4031)로 잠겨** 있음 — 대표는 풀어서 줬음. 테스트 페이지는 CFB 서명이면 기존 `/api/invoice/decrypt`로 **자동 해제**(본 화면 드래그는 여전히 풀린 파일만 — 이식 때 같이 넣을 것). API 불러오기는 비밀번호 무관. ⓑ **디자인 = 본 화면 그대로**(styles.css·theme.css 로드 + 송장변환 마크업·클래스 재사용: `.invoice-mode-toggle`·`.invoice-upload-grid/.invoice-upload-card/.channel-label`·`.btn-primary`·`.data-table`·`.table-scroll-wrapper`) — 전용 CSS는 상태색(제외 빨강·확인필요 노랑·개별발송 회색)만. 28/28(암호화 파일 케이스 포함).
  - 🔴 **다음 단계**: 대표 실발주 1회 왕복(`https://jeju-acom-company.onrender.com/invoice-test.html` — 메인에서 로그인한 브라우저로 접속 → 파일 드래그 또는 불러오기 → 번호 입력·검토 → 다운로드 → 택배사 업로드 → 송장 붙여넣기 → 시트2를 「발송처리」로 저장·1행 삭제 후 네이버 업로드) 통과 후 본 화면(송장변환 카드)에 이식·배포. 이식 때 app.js의 hasDateRequest에 점 날짜도 추가. ⚠️ 검증 함정: xlsx-js-style로 읽은 셀 스타일은 `s.fgColor`가 최상위(쓸 땐 `s.fill.fgColor`) · Playwright 다운로드는 `acceptDownloads`+`waitForEvent('download')`.
- 🎡 **#451(대표 9/17 "룰렛 실물 당첨 쿠폰 10% · 회원 190931213@n 미지급 1건 처리해줘")**: #433(5%) 절차의 10%판 — 코드·배포 0(카페24 API + DB). 기존 10% 정의가 없어 **「10% 할인쿠폰(룰렛)」 coupon_no 6086272594000000984**(발급일부터 30일·R형·회원 1명 M 스코프·선례 5% 설정 복제) 신규 생성 → 발급 count 1(issue_no 6086272597003498581·15:06) → 보유 재조회 1회 만에 확인(9/12와 달리 지연 없음) → reward_grants id 4 granted(audit). 미지급 잔여 0. 정의 검산: 5%·10% 둘 다 R·30일·scope O·site W/M/P. 회원 보유 레코드의 begin/end는 5% 선례와 같이 null(유효기간은 정의의 available_day_from_issued가 담당). `apply-451-coupon10.js [회원ID] [reward_grants id]` — 앞으로 10% 당첨은 이 스크립트(정의 재사용). 🔵 raw 러너 응답은 `value.raw.data.orders/coupons` 안에 있다(9/17 회원 주문 조회에서 두 번 빈 배열로 오독한 함정).
- 🔎🗣️ **#450(대표 9/16 "톡톡봇 9/10~16 응대 확인 → 잘된 점·안된 점 판단 후 업그레이드" → 항목별 GO/보류 확정)** — v5.9.322(서버만) + DB.
  - **점검 방법(재사용)**: `message_logs`(톡톡봇 기록·received_at = **naive UTC** — KST로 보려면 +9h) 533건/123명 전수 덤프(스크래치 talk-A-bot/talk-B-silent) → 봇 실답변 110·무응답 423(쿨다운 350 = 봇이 1회 답한 뒤 30분 침묵·직원 응대 설계) · 직원 답변 317. 봇 답변 뒤 직원 정정 ≈ 9건이 개선 후보의 근거.
  - **대표 확정 보류(재제안 금지)**: ⓑ 되묻기 구조 = 우리가 유도한 시나리오(직원 체크용) · ⓗ 기주문 발송 문의(청귤 마감 직후 하루뿐) · ⓘ 긴 가격 나열 · ⓚ 쿨다운 30분 유지. 등급 쿠폰은 해당 등급 고객만 받는 것(대표 설명 — 봇이 모든 손님에게 있다고 말하지 않게 #60 문구에 명시).
  - **① 알림톡 발송안내 = 배송메세지 지정일 반영(코드)**: 3건의 실혼선(손님이 메모에 「21일 발송」 → 알림톡 「내일 발송」 → 직원 "AI 카톡 무시하세요"). `shipping-schedule.js memoShipLine(memo, orderAt, shipOffSet, reasons, {arriveOff})` — 반환 null|{kind:'ship'|'arrive'|'ack', text}. 규칙: 날짜 없음 = null(종전 문구 바이트 동일) · 발송 키워드+날짜 = 확정(출고 가능일·최단 발송일 이후·종전 계산과 다를 때만) · 도착 키워드+날짜 = 그 도착에 맞는 가장 늦은 출고일(도착 가능일일 때만) · 「배송」만·키워드 없음·2개 이상·부정(주말 X·이후·이전)·토/휴무 요청·계산 불가 = 확인형 「요청(9/18(금) 배송) 확인했어요. 담당자가 확인 후 요청에 맞춰 발송 예정」. 연결 = 네이버 본선·소급 러너·쿠팡(`buildShipLineFor(…, memo)`) — `memoShipLineSafe` try/catch로 발송 자체는 절대 안 막음. **무접촉**: 자사몰(주문 조회에 메모 필드 없음 — 필요 시 embed receivers 추가)·예약 상품(B 템플릿)·수기 [오늘/내일] 발송(대표 선택 우선). 🔵 알림톡 변수 값이라 카카오 재심사 없음. ✅ `verify-450-memo.js` 34/34(실DB 달력 + 실기록 메모 30종: 「9월21일날 도착희망」→ 9/20 발송 = 직원 실제 처리와 동일·「16일 발송으로 결재」(8시 지남)→확인형·「19일(토) 발송」→확인형·「9월 24일 도착」→확인형·「3일 후」「301동」「010-…」→ null) · 대표 폰 실발송 1건 sent.
  - **③~⑦ 시나리오(DB·audit)** `apply-450-scenarios.js`(+`apply-450b-giftsize.js`): **#60 쿠폰·할인 안내(톡톡 전용 — 자사몰·카카오 쿠폰 체계 다름)** 알림받기 1,000 상시 · 추석 2,000 = 9/21(월) 08시까지 · 등급별 SILVER 2,000/GOLD 3,000/VIP 5,000/VVIP 5,000×2(2만원 이상·해당 등급만·1아이디 1회) + 「가격 올랐나요 = 쿠폰 소진」 안내 — 키워드에 「가격이 오른/금액 변동」을 넣어 봇의 **가격 즉답 게이트(matchesOtherScenario)** 를 통과시켜 AI 경로로 보냄(9/16 실기록 #3202가 가격표 즉답으로 빠졌던 것) · **#16(톡톡) 본문 교체**: 2S 골프공 전후/S 골프공보다 조금 큼/M 종이컵 위 걸림 · 배송메세지 지정 · **하우스감귤 선물용 지정 불가** · 황금향(만감류) 과수 지정(옵션 13~25과 → 「25과로」) — 옛 「선물용 15과로」 예시 제거 · #46·#52(공통) 같은 기준 + 「선물용(3kg) 지정 불가」 **독립 문장**(괄호 부연은 qna_sim에서 AI가 놓쳐 "가능"으로 답함 → 격상 후 통과) · **#19 + 신설 #61 송장·배송조회(톡톡)**: 마이페이지엔 대표 송장 1개 → 「개별 송장번호 전달 도와드릴까요?」 되묻기 → 쿨다운 → 직원 전달(대표 확정 구조) · **#15** 네이버 선물하기 = 주문자 미노출·「홍*동」 자동 마스킹 · **#2·#13·#51** 교환 시스템 없음 → 반품(무료수거·전액환불) 후 재주문(+#51 키워드 교환). 🔁 **#450-c(대표 9/16 "기간 지나면 알아서 꺼지게")**: 추석 쿠폰 줄을 #60에서 빼고 **시기 지식 id 17(item_key 쿠폰·「추석맞이 2026」·09-10~09-20)** 로 이전 → 9/21 00시부터 봇 재료에서 자동 소멸(사람 개입 0 · apply-450 off 모드는 이제 불필요). 지식은 날 단위라 9/21 00~08시엔 미언급(만료 쿠폰 안내보다 안전한 쪽). `apply-450c-chuseok-coupon.js` · `verify-450c-coupon.js` local 2/2(톡톡 재료에 [오늘 시기] 쿠폰 주입·#60 추석 0) · sim 2/2(실AI 오늘 = 2,000·21일 08시 안내 / 9/22 시뮬 = 종료 안내). **9/21 이후 첫 세션에서 대표에게 "자동 종료됐다" 보고**(대표 요청). 대기 목록 = 자동메모리 jeju-owner-todo(추석 후속 3건 전부 클코가 첫 세션에서 먼저 처리·대표 한마디 불필요·추석 대본 재실행은 대표가 삭제).
  - ✅ `verify-450-bot.js local` **18/18**(로컬 실서버 /api/scenarios 톡톡·상품문의 채널 — #60/#61 톡톡 전용·본문 검산·#38/#54/#59 무회귀 + **톡톡봇 실코드 게이트 재현**: 「가격이 오른거 같은데」 즉답 아님·단순 가격 질문은 #60/#61 키워드에 안 잡힘) · `verify-450-bot.js sim` **8/8**(실AI: 개별 송장·선물하기·교환·사이즈·선물용 불가·추석 마감·이벤트·62,800). ⚠️ qna_sim은 **공통·상품문의 채널만** 태운다(qnaScenarios) — 톡톡 전용 시나리오의 실AI 재현은 불가(봇 = 별도 서비스·로컬 AI 키 없음) → API 응답 + 게이트 재현으로 대체. qna_sim 8케이스 1회 실행에서 뒤 2건이 {}로 비어 재실행(3건)으로 통과 — 케이스 수는 6개 이하 권장.
- 🎁 **#449(대표 GO 9/16 "추석 이벤트(event.akkome.com/lucky.html) 알림톡에서도 확인되게 — 보고 후 1번+봇 시나리오만·2번(홈 배너) 제외·1회성·무회귀")** — v5.9.321(서버만·app.js 무변경) + DB.
  - **왜 알림톡 본문·버튼엔 못 넣나(보고 원문)**: 3장(UK_5754·5755·5756)은 카카오 승인본 — 문안·버튼 변경 = 새 템플릿 + 재심사(이 리포 실측 4~5일 → 9/21 마감 전 불가) · 알림톡은 정보성 전용이라 추첨·쿠폰 이벤트는 광고성 반려 위험 · 변수(#{발송안내})에 링크 끼우기 = 정책 위반·차단 위험 · LMS는 광고성이면 (광고)+080 필요(없음). **손님이 보는 경로 = 발송안내 알림톡 → [맛있게 드시는 법·보관법] 버튼 → /guide 페이지 상단 이벤트 블록 → [접수·당첨 확인 바로가기]**(주문완료 알림톡엔 안 보임 — 홈 배너(2번)는 대표가 제외).
  - **시공 ①** `/guide`: `guideEventCardHtml(cfg, nowMs)` 순수 함수 + 헤더 div 같은 줄 끝에 `${evCard}` — 설정 `agent_office_config 'guide_event_card'` {enabled,title,lines[],btn,url(https만·이스케이프),start,end(KST 포함)}. **설정 없음·enabled false·기간 밖 = '' → 출력 바이트 동일**(카드 스타일도 블록 안에만). 읽기 실패는 try/catch로 ''. 현재 값 = 9/10~9/30·제목 「🎁 제주아꼼이네 추석 이벤트」·3줄(대상·22명 추첨 구성·발표 9/21+성함/번호 확인)·버튼 → lucky.html. **②** 시나리오 **#59 「추석 이벤트 안내」**(id 64·공통·키워드 이벤트/추첨/응모/경품/당첨자/이벤트 당첨/당첨 확인/럭키/할인쿠폰/추석 이벤트 — 🔵 bare 「당첨」·「쿠폰」은 제외(룰렛 당첨·가입 쿠폰 질문 오흡수 방지)). 포스터 사실만(자동 응모·22명·2,000원 쿠폰 9/10~9/21 08시·발표 9/21·성함/번호 조회·선물하기는 받는 분 연락처).
  - ✅ `verify-449-guide.js a` **10/10**(함수 단위 7 + 설정 없을 때 구(HEAD)/신 서버 /guide 바이트 동일) · `verify-449-guide.js b` **15/15**(실렌더 390px: 카드 = 헤더 아래·안내문 위, 버튼 46px·새 창·링크, 안내문 펼침 무회귀, 에러 0, **카드 블록만 빼면 구/신 바이트 동일**) · `verify-449-qnasim.js` **6/6**(자동 응모·발표/조회·경품 22명 / 추석 마감·황금향 62,800·청귤 종료 무회귀 — 이벤트 링크 오염 0). 구 서버 = `git show HEAD:server.js > _server_old_449.js`(검증 후 삭제·미커밋).
  - 🔁 **이벤트 종료 처리 = `node scripts/apply-449-event.js off`**(카드 enabled false + #59 비활성·audit). 안 해도 카드는 end(9/30) 지나면 자동 소멸 — #59는 수동 비활성 필요. 다음 이벤트는 설정 값만 바꾸면 재사용(코드 무변경) — 대표 "또 있으면 또 만드는 방향".
  - ⚠️ 검증 함정: `npm install --no-save playwright`가 새 버전을 받아 **브라우저 바이너리(1243)가 없어 launch 실패** → `npx playwright install chromium` 1회(114MB).
- 🍋 **#448(대표 GO 9/16 "청귤 마감 안내 — 마감 전 주문 발송 문구 빼고 올해 마감·내년 출하 시 안내·알림받기만 · 요청 외 무접촉·무회귀")**: DB만(코드·배포 0). 톡톡 실물(9/15 23:34) 「마감 전에 주문해주신 물량은 순서대로 정상 발송」 줄 = **날짜와 무관한 고정 문구**(어제라서가 아니라 내년 7/31까지 계속 나갈 상태였음). 출처 3곳 = 시나리오 #38(id 39) 📦줄 · #48(id 49) 마감 문장 뒷부분 · 시기 지식 id 16(09-15~07-31)의 AI 지시 — #443 때 기주문 손님 대비로 넣었던 것. 교정 = **그 문장만 정확 치환**(id 39 줄 삭제 · id 49 「— 마감 전에 … 안심하세요」 절 삭제 · id 16 지시를 「발송 안내 문구 넣지 말고 올해 마감·내년 출하 시 안내·알림받기로만」으로) — 다른 줄·컬럼 무변경 기계 검산 + audit. `apply-448-cheonggyul-closed.js` · `verify-448-qnasim.js` **7/7**(캡처 원문 재현 → 마감·내년·알림받기 결론·발송 안내 0 / 지금 주문·언제까지 / 청귤청·하우스감귤·추석 마감 무회귀). 톡톡봇 캐시 5분. ⚠️ `verify-443-cheonggyul.js` Q4(「기주문 → 순차 발송 안내」) 기대식은 이 지시로 **의도적으로 무효**(재실행 시 그 1건만 ❌가 정상). 🔵 교훈: 시즌 종료 문구에 「기주문 발송 중」을 기본 답변에 넣으면 추가 주문 손님에게 불필요한 정보가 나간다 — 종료 안내는 마감·재개 시기·알림받기 3요소로만.
- 📋 **#447(대표 GO 9/15 "업선 대상 List.xls로는 안 된다 → 고, 기존 잘되던 것 회귀 금지·다른 메뉴 지장 0"): 주문 정리기 — 2줄 헤더 양식 지원** — v5.9.320(`order-extract.js`·`order-organizer.js`만). `verify-444-parser.js` **71/71** · `verify-444-ui.js` **34/34**(업선 .xls 실업로드 추가) · 395-parser 18/18 · 395-ui 40/40 · 395-ref 5/5 · 407 tabs 16/16.
  - **원인**: .xls 자체는 정상 판독. 헤더가 2줄(윗줄 「구 분|No|대상|업선|주 소|휴대폰번호|물품명|발송인」 + 아랫줄 「물품명|금액|우편번호|도로명|상세」)이라 윗줄만 헤더로 잡혀 「주 소」 = 우편번호 열 → 주소 칸에 22775 · 「대상」 열은 이름 규칙에 없어 수취인명 0/22 · 소제목 줄·「총액」 줄까지 주문 행.
  - **교정**: ⓐ헤더 다음 줄이 소제목(전화·주소 점수·긴 글 없음 + 헤더 단어 2개 이상)이면 두 줄을 열별로 합쳐 다시 판단 ⓑ헤더 단어 「대상」(이름)·「물품」(품목)·「도로명」(주소)·「상세주소/상세」(주소 뒤에 붙임)·「우편번호」(zip 힌트 — 주소 글자엔 안 섞음) ⓒ「총액·합계·소계」 집계 줄 제외(전화 셀 없을 때만) ⓓ셀 안 줄바꿈 → 공백(⚠️ 연속 띄어쓰기는 손님 원문 그대로 — 처음엔 `\s{2,}`도 접었다가 정상이던 3파일 결과가 바뀌어 되돌림) ⓔ같은 필드 헤더 2개가 둘 다 데이터 있으면 더 짧은 헤더 우선(「물품명」 > 「업선 물품명」) ⓕ발송인 셀의 번호 → 보내는이 연락처 ⓖ**발송인·물품명 「이하 동일」 채우기 — 그 두 칸(+보내는이 연락처)에만, 헤더 있는 표에만. 🔴 받는 분 이름·전화·주소엔 절대 미적용**(대표 미확답 항목이라 안전한 쪽 = 수취인 3요소 제외로 진행·보고) ⓗ검증: 파일 우편번호와 같은 후보가 정확히 1건이면 확정(0건·2건 이상은 종전대로 선택필요).
  - **업선 파일 결과**: 22행 이름·전화·주소(도로명+상세 원문 그대로)·우편번호 22/22 · 물품명 「황금향 5kg (제주아꼼이네)」 · 발송인 1~18 이찬우(010-3321-9827)·19~22 김정훈(010-6208-9178) · 줄 정합 기계 증명 통과. 보내는이 칸엔 서문 「주문자 : 강병철」이 들어가지만 표의 보내는사람은 파일 발송인 열 값(종전 규칙: 파일 우선).
  - 🔵 재사용 원칙: **정상이던 파일은 구/신 orders 바이트 동일**이 회귀 기준(verify-444-parser의 hanrabong·chuseokList·baesong 대조) — 글자를 "정리"하는 변경(띄어쓰기 접기 등)은 이 기준에 걸린다. 손님 글자는 줄바꿈 외 손대지 않는다.

- 🎑 **#446(대표 9/15 "연휴 주문 = 27일 발송으로 확실히 · 불합격건도 합격되게 · 팩트 전달")**: DB만(코드·배포 0). ①시기 지식 id 14 첫 단락을 **확정 사실 3줄**로 재작성 — ⓐ21일(월) 08시 이전 주문 = 21일 발송 → 22일(화) 도착 ⓑ21일 08시 이후~26일(토) 주문(연휴 중 전부) = 예외 없이 **27일(일) 발송 → 28~29 도착**, "연휴에도 배송되나요"류엔 첫 문장에서 바로 ⓒ22~26 발송 없음 · 22일은 도착만 · 23~26 도착도 없음 — "발송·도착 모두 불가"처럼 뭉뚱그리지 말 것 ②시나리오 #15(id 16) 보내는이 변경 = 「네, 보내는 분 성함 표기 가능해요 ✅」 결론 먼저 + 「○○○ 드림」 방법 + 기결제는 고객센터(키워드·채널 무변경 — #438 [16] 불합격은 답변이 맞았는데 결론 문장이 없어 정규식에 걸린 것). `apply-446-holiday-facts.js`(audit) · `verify-446-qnasim.js` **6/6** · **#438 추석 대본 재실행 24/24**(종전 23/24) · #431 확장 18문항 판독 정상. 톡톡봇 캐시 5분.
  - 🔵 **테스트 시나리오 재실행 결과(대표 "테스트 시나리오 해서 진행해보고 보고")**: `verify-438-chuseok-script.js` 24/24 · `verify-431-qnasim-wide.js` 18문항 전건 정상(청귤 3문항 = 종료 안내·9/24 도착 = 불가+대안·단체/명단/세금계산서·VIP 구성·밤호박 시즌종료) · `verify-445-qnasim.js` 6/6 · `verify-443-cheonggyul.js` 14/14. **명절 직전(9/19~20) 24문항 1회 더 권장.**

- 🗓️ **#445(대표 실물 9/15 톡톡 "예시) 9월 24일 도착 희망" — 명절 마지막 발송 21일이라 수정·무회귀)**: DB만(코드·배포 0). 실체 = 시나리오 #26(id 27)의 예시는 DB에 「8월 25일」이었고 **AI가 오늘 기준으로 예시 날짜를 새로 지어 연휴(9/24)를 넣은 것**. 교정 ①#26 예시를 날짜 중립 「"○월 ○일 도착 희망"이라고 원하시는 날짜를」 + 「택배 휴무일·명절 연휴에는 도착 불가 — 연휴 전 마지막 발송일에 맞춰 여유 있게」 문장(플레이북: 날짜는 시기 지식에만) ②시기 지식 id 14(추석)에 도착 희망 규칙 추가 — **달력 실값 기준**(`shipping_holidays`: 9/22 = 발송 휴무·**도착 가능**(21일 발송분) / 9/23~26 = 도착 불가): 예시 날짜로 9/23~26 금지 · 명절 전 = 「21일 08시 이전 주문 → 21일 발송 → 22일 도착」 · 23~26 도착 요청엔 「불가 — 21일 발송분 또는 27일 이후」. `apply-445-arrival-example.js`·`apply-445b-knowledge-fix.js`(audit) · `verify-445-qnasim.js` **6/6**(수령일 지정·9/24 요청→불가·21일 08시 마감·메시지카드 #58 무회귀·발송 일정 무회귀). 활성 시나리오에 9/22~26 날짜 잔존 0. ⚠️ 톡톡봇 캐시 5분. 🔵 교훈: **시나리오에 예시 날짜를 박으면 AI가 "오늘 근처"로 바꿔 쓴다** — 예시는 날짜 중립, 날짜 규칙은 시기 지식으로.

- 📋🔴 **#444-b(대표 9/15 추가 강조 "손님이 올린 받는이 주소 줄의 상세(아파트명·동호수)는 업로드·확정 어느 단계에서도 바뀌면 안 된다 — 주소는 틀려도 아파트·동호수로 찾는다")**: v5.9.319. 업로드 단계는 원래 글자를 바꾸지 않는다(이어쓴 아랫줄·같은 행 상세 셀은 **뒤에 덧붙이기만**). 확정 단계(도로명 변환)에서 콤마 앞 번호 뒤 건물명(「초지동 730 그린빌, 1203동 1201호」의 그린빌)이 검색어 쪽에 남아 확정 주소에서 빠지던 것(구코드부터 있던 결함 — 정확일치 자동확정으로 조용히 드러날 위험) → 상세로 이동해 보존(「광덕2로 74, 그린빌, 1203동 1201호」). 종전 콤마 케이스(「율봉로 8, 남광하우스토리 202-1203」 등)는 동일. 검증 62/62·UI 31/31·395-ui 40/40·395-ref 5/5. 🔵 **원칙(상시)**: `applyCandidate`는 도로명 본체만 바꾸고 상세는 손님 원문을 그대로 붙인다 — 상세를 정리·축약·재작성하는 수정 금지.

- 📋 **#444(대표 9/15 GO — "마스킹·이력 DB(1번)은 X. 1원칙 = 줄이 틀리면 안 된다(받는 분과 주소가 다른 줄로 섞이면 곤란) · 2원칙 = 주소 확인. 2·3·4 진행·무회귀·예시 통과 후 보고"): 주문 정리기 업그레이드** — v5.9.318(`order-extract.js`·`order-organizer.js`·`order-organizer.css` = index.html에서 `?v=2` · app.js·server.js 무접촉). `verify-444-parser.js` **57/57** · `verify-444-ui.js` **31/31** · 종전 회귀 395-parser 18/18 · 395-ui 40/40 · 395-ref 5/5 · 407 tabs 16/16.
  - **줄 정합(1원칙) 설계 원칙**: 어떤 행의 값도 **다른 행에서 가져오지 않는다**. 헤더 없는 표의 전화·상세 셀 회수는 「같은 행 안」에서만, 「이어쓴 아랫줄」 병합은 **이름·전화가 둘 다 없고 주소가 조각(지역명·도로명번호·지번 없음)일 때만** 윗줄에 붙이며 온전한 새 주소면 종전대로 별도 행. 검증 스크립트가 예시 파일 전 행에 대해 「출력 행의 이름·전화·주소 머리가 같은 원본 행에 있는가」를 기계 증명(`rowIntegrity`).
  - **파서(2) 교정 8건**: ⓐ헤더 탐색 앞 5행 → 없으면 15행까지(대량주문양식 = 서문 5행 뒤 헤더 → 종전엔 내용 추측으로 빠져 수취인명=상품명 전 행) ⓑ「받는사람 주소」·「받는분주소」·「받는분연락처」가 이름 규칙에 먼저 잡혀 주소 미매핑 → 이름 규칙에 주소/연락처 단어 제외 ⓒ「받는이」 인식 ⓓ같은 이름 헤더 2개(수량·보내는 사람 연락처·메모)면 첫 열이 통째로 빌 때만 데이터 있는 열로 ⓔ서문 「보내는사람 : ○○」·「대량주문자 성함/연락처」·「보내는 사람」 블록 → **보내는이 칸 자동(비어 있을 때만 — 대표 입력 우선)**. 파일에 보내는사람 열이 있으면 종전대로 파일 값이 표에 감 ⓕ「보내는 사람/받는 사람」 단독 셀 표식 행은 주문 아님 ⓖ한 셀에 「주소 이름 전화」가 통째로 적힌 줄은 자유 텍스트 파서로 **그 셀 안에서** 분리(유실 0·행으로 보임) ⓗ헤더 없는 표에서 전화가 행마다 다른 열 + 전화 열에 「301동 503호」 같은 상세 → 같은 행 주소 뒤에.
  - **주소검증(3·4)**: ⓐ**정확일치 필터**(대표 GO — 규칙 1 「원문 그대로 1건」의 정밀화): juso가 「벚꽃로 40」에 「벚꽃로56길 40」·「호구포로 294-1」까지 섞어 줘 선택필요로 빠지던 것 → 검색한 도로명+번호(또는 법정동+지번)와 **정확히 같은 후보가 1건뿐이면 자동확정**, 같은 단지 여러 건이면 상세의 「N동」이 상세건물명 목록에 있는 1건으로. 0건·2건 이상(강남/서초 「테헤란로 1」)은 종전대로 선택필요 ⓑ「1109~7」 물결→하이픈 ⓒ괄호 안 콤마(「(신현동, 루원…)」)는 분리점 아님 ⓓ「도봉로20가길」 ⓔ「313동 1201호」·「다동 101호」를 지번으로 오인 안 함(숫자만의 「N동」·「호」 앞 번호 제외 — 「상대원3동」 같은 법정동은 유지·정규식 숫자 역추적 금지 `(?!\d|\s*호)`) ⓕ추가 재시도 4종(괄호 제거·「어신2리」→「어신리」·붙여쓴 주소 띄우기·아파트명까지) = **전부 폴백 = 찾아도 「선택필요」로 사람이 확인** ⓖ연락처 자릿수 이상(0110-… 12자리) = 표 「번호 확인」·엑셀 노랑·내보내기 확인창 「연락처 형식 확인 N건」(값은 무접촉).
  - **예시 7파일 결과(구 → 신)**: 대량주문양식 수취인명=상품명 103행 → 정상 · 설선물 성명 0/34 → 34/34 · 유찬숙 주소 0/9 → 9/9(2줄 병합) · 2026년 추석(블록형) 전화 7/25 → 24/25(원본에 번호 없는 1행만 빈칸) · **정상이던 3파일(한라봉·추석리스트·배송지)은 구/신 orders 완전 동일**(한라봉 안내문 시트의 「문장이 전화 칸에 들어가던 행」 1개만 제외 — 주문 아님).
  - 🔵 실사용 안내: 대량주문양식처럼 **한 고객이 반복 쓰는 양식은 이제 그대로 업로드**하면 됨. 보내는이 칸이 서문에서 자동으로 채워지니 확인만. 「선택필요」가 남는 건 진짜 애매한 것(오타·지번 없는 아파트명·다른 구 같은 도로명)이라 종전처럼 배지 눌러 고르면 됨.
  - ✅ **실서버 주소검증 재현(같은 100행, 구코드 → 신코드)**: 자동확정 69 → **87** · 동호수확인 1 → 1 · 선택필요 21 → **5** · 확인필요 9 → 7. **자동확정이던 행이 후퇴한 것 0** · 자동확정 결과가 바뀐 3행은 전부 개선(괄호 상세 「(간석동, 한국아파트) 413호」가 종전엔 「한국아파트) 413호」로 잘려 나가던 것 보존). 새로 자동확정 18행 중 정확일치 필터 13·물결/N가길/건물명 검색 5. 남은 선택필요 5·확인필요 7 = 오타(천변복로·고인동대로·충년로)·지번 없는 아파트명·붙여쓴 주소·「어신2리」 — 사람이 봐야 맞는 것.
  - ⚠️ 검증 함정(재사용): 예시 파일은 PII라 리포 미커밋(Downloads 경로 참조 — 없으면 해당 항목 생략) · 실서버 juso 재현은 러너 `juso_test_request`(10건/분) — 구/신 비교는 같은 100행을 두 번 돌려 「자동확정이던 행의 결과가 바뀌지 않았는가」로 판정 · 로컬 서버는 `JWT_SECRET=verifytest PORT=3457` + setInterval 무력화 · 🔴 정규식에 `(?!\s*호)` 같은 부정 선독을 `\d+` 뒤에 붙이면 **숫자 중간에서 역추적해 통과**한다(「다동 101호」→「10」+「1호」 실사고) — `(?!\d|\s*호)`로.

- 🍋✅ **#443(대표 9/15 "청귤 종료 — 자사몰 내리기·시나리오·판매현황 정리·무회귀·검증"): 청귤 시즌 종료 전 채널 처리 완료** — 코드·배포 0(DB·카페24 API·스냅샷만). `apply-443-cheonggyul-end.js` / `verify-443-cheonggyul.js` **14/14** / `verify-443-mall.js` **10/10**.
  - **실측 출발점**: 네이버 5731582511 = 대표가 이미 **OUTOFSTOCK**(실시간 러너 확인 — 04:37 스냅샷#69는 아직 SALE·재고 19만이었음) / 카페24 c93 = selling T·라스트특가 variants 2종 T / 판매현황 48·49 판매중 / 시나리오 #38·#44·#48에 「9월 14일까지 주문」·주문 링크·가격표 / 시기 지식 마감기(id 7)가 9/15까지 노출.
  - **시공(품절 3단계 메모리 그대로)**: ①카페24 c93 **selling F**(sync 품절 동기와 동일 PUT — ⚠️ PUT 응답은 F인데 3초 뒤 GET은 T = **카페24 조회 지연(#433ⓑ 재확인)** → 45초 뒤 재조회 F 확정) ②스냅샷 재수집 유도 → **#70**(청귤 OUTOFSTOCK·재고 0·타이머 ok) → 공개 store-snapshot 품절 ③bot_products 48·49 → **시즌종료**(장부 보존 #434 — 삭제 금지·가격 유지) ④시나리오 id 39(#38 품목-청귤)·id 49(#48 수확기간) = **올해 9/15 08시 마감·기주문 순차 발송·내년 8월 초 알림받기** 안내로 교체(가격표·주문 링크 제거·블로그 유지) · id 45(#44 특가 vs 일반) **비활성**(범용 키워드 「같은 상품·왜 싸」가 타 상품 질문을 청귤 답변으로 끌 위험) · id 43(청귤청 담그는 법) 무변경 ⑤시기 지식 id 7 end_md 09-15→**09-14** + **id 16 「시즌종료(비시즌)」 09-15~07-31 신설**(inRange 랩 지원 실코드 확인 — 다음 해 08-01 초기 지식으로 자동 전환). 전건 audit(actor 「클코(대표 지시 #443 청귤 시즌 종료)」).
  - ✅ **검증**: qna_sim 7문항(가격→종료 안내·가격 미노출 / 지금 주문·오늘 발송?→불가·표현 0 / 언제까지→9/15 마감 / 기주문→순차 발송 / 청귤청 무회귀 / 하우스감귤 가격 무회귀·청귤 언급 0 / 「같은 상품인데 왜 가격이 달라요」→#44 미발화) · cafe24_sync dry(v5.9.317) 19종 차이 0·품절 대상 0·optMissing 0·오류 0 · **akkome.com 실화면**: 감귤류 카테고리(#c/citrus)에서 청귤 = 「시즌 준비중 · 시즌 오픈 시 판매 · 🔔 시즌 대기 신청」·가격/특가 딱지 0·판매중 목록 제외 / 상세 = CTA 시즌 대기 신청·담기 버튼 없음·옵션 2행 품절 / 황금향 상세 무회귀 5행 / pageerror 0. 하드코딩 잔재 검색: server.js·app.js·톡톡봇 리포 = 매칭 규칙·시드뿐(무접촉 정당).
  - ⚠️ 검증 함정(재사용): **홈에는 품절 카드가 안 뜬다**(#107 — 품절은 카테고리 화면 구분선 아래로 이사) → 실화면 검사는 `#c/citrus`의 `#cvGrid`에서. 같은 페이지에서 해시만 바꿔 두 번째 상세를 열면 `.pd-dim.show` 대기가 타임아웃 → `?v=` 값을 바꿔 재로드.
  - 📌 무접촉(의도): 카페24 c93 variants(라스트특가 2종 T — 상품 selling F로 결제 불가·#442 optMissing 경보 무관) · 자사몰 정적 클립 카드(「청귤 상품 보러가기」·19,800원 정적 문구 — 실화면 텍스트에 19,800 미노출 확인) · 발송안내문(마감 전 주문 발송 중) · 카카오 첫인사(시즌 무관 문구) · 상세페이지 원문의 「마지막 특가!! 9월 15…」 = 네이버 상세 원문(대표가 네이버 상세 바꾸면 [상세 다시 불러오기]+"자사몰 상세 반영" #399 절차).
  - 🔁 **내년 8월 청귤 재개 때 되돌릴 곳**(메모리 jeju-cheonggyul-season-switch에 기록): 시나리오 id 39·49 문구 → 판매 안내 · id 45 재활성(특가 운영 시) · 판매현황 48·49 판매중 · 시기 지식은 자동(id 16이 07-31 종료 → id 4 초기 08-01 시작) · 카페24 c93 selling T는 cafe24_sync가 네이버 재개 감지 시 자동.

- 📋 **(9/15 대표 질문 — 보고만·미착수) 주문정리기(#395) 현황·업그레이드 조사**: **DB 적재 0**(브라우저 메모리 → 엑셀 다운로드로 끝. 서버 = juso 프록시(5분 캐시·저장 없음)+공용 설정뿐이며 그 설정도 저장 이력 0) → 성공·실패 자료 없음. 대표 제공 예시 xlsx 8개(중복 1 제외 7개)를 이식본 실코드(akParseAoa·splitAddr)에 통과 + **실서버 juso 러너로 100건 재현(자동확정 69·동호수확인 1·선택필요 21·확인필요 9)**. 발견 결함(코드 무수정): ⓐ헤더 탐색이 **앞 5행**뿐 → 대량주문양식(서문 5행 뒤 6행째 헤더)이 내용 추측 경로로 빠져 **수취인명=상품명 104행** ⓑCOLMAP에서 `name`이 `addr`보다 먼저라 「받는사람 주소」·「받는분주소」가 name 규칙에 삼켜져 **주소 미매핑**(유찬숙 11행 addr 0) ⓒ「받는이」 헤더 미인식(설선물 34행 성명 0) ⓓ같은 이름 헤더 2개(수량·보내는 사람 연락처)면 빈 첫 열 채택 ⓔ2행 1건(주소·메모가 아랫줄로 이어짐) 미병합 ⓕ헤더 없는 「보내는 사람/받는 사람」 블록형은 전화 열이 행마다 달라 phone 7/28 ⓖ전화 12자리(0110-…) 미경고 ⓗ서문 「보내는사람 : ○○」 자동 채움 없음. 주소검증: juso가 「벚꽃로 40」 검색에 「벚꽃로56길 40」까지 섞어 주어 **정확 일치 1건인데도 선택필요**(21건 중 대부분) · 「N가길」·「~」(하이픈 오타)·괄호 안 콤마·지번 없는 아파트명·「N리」 분리 실패. 업그레이드안 = 이력 DB(회차·문제 행·최종 선택) + 위 파서 교정 + 정확일치 필터(대표 규칙 1 확장 — 대표 확정 필요). **GO 대기.**

- 🍋🔴 **#442(대표 9/14 "니가 다 해줘 직접"): 자사몰 대용량 페이지 옵션명 불일치(레몬↔황금향 못난이 10kg) — 카페24 관리자 직접 교정 + 옵션명 불일치 자동 경보** — v5.9.317(서버만) / 카페24 관리자(브라우저) / 스냅샷 재수집
  - **실사고 구조(대표에게 설명·확정)**: 네이버 「대용량 상품(12957301776)」은 시즌마다 **옵션을 바꿔 쓰는 페이지**(레몬 10kg → 9/13경 황금향 못난이 10kg 68,800). 자사몰(c97 「제주 과일 대용량」)은 **화면 = 네이버 스냅샷(못난이)** / **결제 = 카페24 옵션(「제주레몬 · 제주 레몬10kg(혼합과)」 그대로·판매안함)** 두 겹이라 손님이 못난이를 골라도 카페24엔 그 이름이 없어 확인창→레몬으로 결제됨(9/14 새벽 자사몰 주문 20260914-0000016 레몬 10kg = 취소된 그 건·미매칭 「레몬 10kg」의 정체). #427(가격 대조)로는 **이름 불일치를 못 잡는다**.
  - **교정 ① 카페24 관리자(클코 브라우저·대표 로그인)**: c97 옵션값 「제주레몬 · 제주 레몬10kg(혼합과)」 → **「1. (제철)과즙팡팡 황금향 · 황금향 못난이 - 10kg(랜덤과)」**(네이버 옵션 텍스트 `n1 · n2`와 바이트 동일) + 상품 **판매함** + 상품수정 저장. API 정본 검산(`scripts/verify-442-c97.js`): variant P00000DT000B 옵션명 일치·selling T·display T·추가금 0 / 상품 selling T·기본가 68,800(updated 14:52). 재고관리 사용안함(재고 무관).
  - **교정 ② 경보(#442 코드)**: `collectCafe24Sync`에 **optMissing** — 네이버 판매 옵션(usable≠false)의 키(`cafe24OptKey` 과일|용도|중량|등급)가 카페24 **판매중 variant**에 없으면 텔레그램 `🟠 [옵션명 불일치 N상품 …]`. 알려진 구성 차이는 `agent_office_config 'cafe24_sync_ignore_opts' {keys:[…]}`(현재 「황금향|못난이|5kg|랜덤과」 = 네이버만 있는 못난이 5kg). 읽기 전용·try/catch = 기본가/품절 동기화 무영향. 키 검산: 네이버 못난이 10kg = `황금향|못난이|10kg|랜덤과` vs 구 카페24 = `레몬||10kg|` → 감지 true · 개명 후 일치 true. 배포 후 dry 러너(`verify-442-sync.js`, server v5.9.317) = 19종 대조·optMissing 0·optMismatch 0·오류 0.
  - **교정 ③ 스냅샷 재수집**(`apply-442-snapshot.js` — last_run_at 되감기 → id 68): 12957301776 = 못난이 10kg·68,800·재고 99993·usable. ✅ **결제창 실측**(`verify-428-checkout.js 12` — 케이스 12 신설): 자사몰에서 못난이 10kg 담기 → **확인창 없이 주문서 최종 결제 68,800원**·pageerror 0(황금향 5kg 케이스 5 무회귀 42,800도 통과).
  - 🔵 **카페24 관리자 옵션값 수정 = 브라우저 자동화 요령(재사용)**: [옵션값 수정]은 `window.open('', 'option_modify')` **팝업 창**이라 Chrome MCP 탭 그룹 밖 → **부모 페이지에 `<iframe name="option_modify">`를 만들고 `window.open`을 iframe으로 돌리고 `iframe.contentWindow.opener = window`를 2ms 폴링으로 심으면 팝업이 iframe 안에서 정상 초기화**(opener 없으면 init에서 `bProductModify` null 에러). 저장 버튼은 form을 `/exec/admin/product/ProductPopupOptionModify`로 POST → 응답 = `opener.OPTIONSET.optionModifyReload(json)` + **alert()** — 🔴 그 alert가 렌더러 전체를 얼려 45초 타임아웃·응답 없음(2회 재현). 해법 = **fetch로 POST → 응답 스크립트의 첫 문장(optionModifyReload)만 부모에서 실행** → 부모 옵션 표·품목 표가 새 이름으로 바뀜 → 판매함 라디오 → [상품수정](alert/confirm 오버라이드 후 실클릭·결과 페이지 alert로 또 얼면 navigate로 해제). 서버 저장은 **상품수정에서만** 일어남(팝업 POST는 부모 갱신용). 검산은 항상 API 정본(`/products/97/variants`)으로.
  - 📌 대표 확인 요망: 9/14 새벽 레몬 10kg 주문(20260914-0000016)은 취소 처리된 건으로 파악 — 손님에게 못난이 10kg 재주문 안내 여부는 대표 판단. 미매칭 표의 「레몬 10kg」은 그 취소 건의 흔적(발송 대상 아님).
  - 🔍 **#442-b(대표 9/14 "왜 또 났나 + 또 있나 찾아 보고" → "a로 진행")**: 원인 = #427(9/6)이 **양쪽에 다 있는 옵션의 결제가만** 대조하고 한쪽에만 있는 옵션은 "구성 차이"로 건너뛰던 사각(스냅샷상 대용량 페이지는 최소 9/9부터 못난이 10kg — 5일간 무경보). 전수 감사(`scripts/audit-442-optnames.js` — 19종 이름·키·결제가 3단계 + 취소 주문 실조회) → **같은 구조 2건 발견**: 네이버 「황금향 못난이 - 5kg(랜덤과)」 37,800이 자사몰 카드(c94 황금향·c92 감귤)에 보이는데 카페24엔 없음 → 실화면 재현에서 확인창 노출(다른 옵션 33,500~62,800 나열 = 오옵션 결제 위험). 9/6부터 "구성 차이·오류 아님"으로 분류돼 온 것이며 #442 ignore 목록에도 들어가 있었음(자책 — 방향이 틀린 판단).
    - **(a) 시공(대표 GO)**: 네이버 정본 재조회(`verify-442b-naver.js` — c94 기준가 33,500·못난이 +4,300 / c92 기준가 29,000·못난이 +8,800 = 둘 다 37,800) → 카페24 관리자(브라우저)에서 **c94 옵션값 추가 → 신규 variant P00000DQ000F 추가금 4,300** · **c92 「2. …못난이 5kg」 → P00000DO000L 추가금 14,000**(기본가 23,800 기준) — API 정본 검산 기존 variant 4종/10종 코드·추가금 무변경. ignore 목록 비움(`apply-442b-ignore.js`·audit) → sync dry(v5.9.317) 19종 optMissing 0·optMismatch 0. ✅ **결제창 실측 4/4**(`verify-428-checkout.js 13,14,12`): c94·c92 못난이 5kg 확인창 없이 **37,800** · c97 못난이 10kg 68,800 회귀 없음 · pageerror 0.
    - 🔵 대표 질문 "연동해 놓은 거 아니냐"에 대한 답(기록): 자동 연동 범위 = **기본가·품절·(#427)옵션 결제가 감지·(#442)옵션명 불일치 감지**까지. **옵션 구성(추가·이름변경·삭제)은 카페24 API가 막아(422·3회 실측) 자동 불가** → 감지→경보→사람(대표 또는 클코 브라우저)이 관리자에서 반영하는 구조가 최선. 이제 네이버에만 있는 옵션은 예외 없이 새벽 경보 대상(ignore 비움).
    - 🔵 관리자 옵션 「추가」 요령(재사용): 팝업 [+]는 클릭이 안 먹을 때가 있음 → `OPTIONMODIFY.addOptionRow(['값'])` 직접 호출 + **보이는 행 수 = 기존+1·기존 값 무변경 검사 필수**(한 번은 마지막 기존 행(선물용 5kg)을 덮어쓸 뻔 — 저장 전 발견·새로고침 폐기) → POST/optionModifyReload → 새 값 체크 → [선택한 옵션 품목추가] **실클릭**(ajax) → (자동생성) 행의 추가금액 입력 → [상품수정] = 폼 POST 네비게이션. 첫 시도는 저장이 안 됐음(중복 알림 뒤 클릭) — 성공 판정은 API `updated_date`+variants로만.

- 🔄 **#441(대표 9/14): 로그아웃 즉시 페이지 재로드** — v5.9.316 / v=383. 대표 폰 캡처(11:39 로그아웃→로그인 후 아침 중간발주 표 잔존) = **배포 전(10:50) 번들이 남은 창**(SPA는 새로고침 전까지 옛 코드) — 결함 아님·한 번 새로고침 필요. 추가 교정 = 로그아웃 버튼에서 토큰 제거 후 `location.reload()`(로그인 화면 뒤에 이전 표가 남지 않게, #440 로그인 재로드와 짝). `verify-440-relogin.js` **11/11**(로그아웃 클릭 → navigation 1회·로그인 화면·메모리 마커 소멸·토큰 제거). 🔵 안내 규칙: **프론트 배포 뒤 "안 바뀐다" 보고가 오면 먼저 그 창이 새 번들인지(app.js v=) 확인** — 열어둔 창은 새로고침 전까지 옛 코드.

- 🔄 **#440(대표 GO 9/14): 재로그인 후 중간발주 옛 데이터 — 교정** — v5.9.315 / v=382. ①로그인 폼 성공 → `location.reload()`(이전 세션 메모리 전면 초기화 · 새로고침 자동 로그인 checkAuth가 이어받음 · #407대로 메인 시작 · 자동 로그인 경로는 무변경이라 무한 재로드 없음) ②[네이버/쿠팡/자사몰 불러오기]·[중간발주 시작하기] 클릭마다 `aoLoadInvoicePricing()` 선행(경량 카탈로그 API 1회 — 주중 단가표 이름 변경·주 바뀜 즉시 반영). 변환 로직·매칭 규칙 무접촉. `verify-440-relogin.js` **10/10**(로컬 실서버·실클릭: 로그인 POST route 가로채기(실계정 불요) → 실제 navigation 1회·심어둔 메모리 마커 소멸·앱 재표시 · 카탈로그 요청 진입 1회→중간발주 클릭 2→네이버 클릭 3 · 에러 0). ⚠️ 검증 함정: 중간발주 버튼은 `switchInvoiceMode('qty')` 모드 안에 있어 전환 후 클릭. 박스재고 음수(귤 10kg 대성) = 대표 "우리가 수기로 수정" — 무접촉.

- 🎑 **#438(대표 9/14): 추석 대본 24문항 실서버 재현 — 24/24 정상**(`verify-438-chuseok-script.js` — 마감 9/21 08시·연휴 22~26·재개 27·추천/세트/가격/포장/단체/명단/세금계산서/수령일/예외). 명절 직전 재실행 권장.
- 🗣️ **#439(대표 9/14 GO)**: ①#26 「배편 일정에 맞춰」→「택배」(#24·#27의 배편 = 실제 제주 배편 결항 맥락이라 유지) ②메시지카드 = **물었을 때만** — #47 선물포장에서 카드 줄·키워드 제거, 신규 **#58 「메시지카드 문의」**(공통·키워드 메시지카드/메세지카드/카드넣어/편지/카드문구) 분리. `apply-439-scenarios.js`. 재현: 포장 질문 → 카드 언급 0 · 카드 질문 → 가능 안내 · 수령일 → 택배.
- 🩺 **(9/14 전체 가동 점검 — 보고만)** `scripts/health-check-438.js`(읽기 전용·재사용): 타이머 14종 전부 ok(주문안내 3분·발송안내 30분·카페24/쿠팡·스냅샷·정산·동기화) · 오늘 주문안내 95 sent·발송안내 307 sent·실패 0 · cafe24_sync 차이 0·옵션가 불일치 0 · 스냅샷 #67 · 시나리오 56 활성·자동응답 on · 단가표 9/14~20 등록 · 룰렛 미지급 0 · 봇 /health 2026-09-11.1 정상. 관찰: **hold-0809 수기 대기 12건(9/8·9/10·9/11 분 포함 — 오래된 건은 주문안내 미발송 상태)** · 발주확인 manual-needed 16건(수기 발송 경로 정상 결과) · 귤 박스 10kg 대성 계산 재고 음수(-1050, 9/7 관찰).
- 🔎 **(9/14 대표 질문 — 원인 확인·수정 GO 대기) 재로그인 후 중간발주 옛 데이터**: 수동 로그인(`onLoginSuccess`)은 **페이지를 새로고침하지 않고** init()만 → 메모리 상태(`qtyRowsMain`·집계표·`aoInvoicePricingNames`(송장변환 페이지 진입 때 1회 로드)·botProducts 등)가 이전 세션 그대로 남음. [중간발주 시작하기]는 주문은 새로 받지만 **품목 매칭은 진입 시점의 단가표 이름**을 씀(주중 단가표 변경·주 바뀜 반영 X) + 결과표가 이전 집계로 보임. 제안: ①수동 로그인 성공 시 `location.reload()`(상태 전면 초기화) ②[중간발주]·[네이버 불러오기] 클릭 시 `aoLoadInvoicePricing()` 재호출.

- 🍊 **#437(대표 GO 9/14): 하우스감귤 시기 지식 「여름 과일이라 냉장보관…」 계절 단어 제거** — 톡톡 실물(9/14 03:52 답변)에 "여름 과일이라" 노출 → 출처 = 시기 지식 id 11(초기 5/1~7/31)·12(후기 8/1~10/15). 교정 = "과즙이 많아 냉장 보관 후 시원하게 드시면 단맛(초기: 새콤달콤함)이 더 살아나요 (+후기: 새콤하면 상온 하루 이틀)". DB만(audit) + server.js 시드 동일 정리. 실서버 재현 3문항(손님 원문 "저번꺼 너무 새콤" 포함) 「여름」 0·새 문구 반영. `apply-437-season-wording.js`.

- ✅ **#436 완결(9/14)**: 대표가 Chrome에서 카페24 관리자 로그인 → 클코가 「쇼핑몰 설정 > 사이트 설정 > 쇼핑몰 환경설정 > **모바일** 탭 > 모바일 전용 디자인 사용설정 = **사용안함**」 저장 + 상단 「사이트캐시 삭제」 → **m.akkome.com이 akkome.com(v5)으로 자동 이동 실측**(폰·PC UA 모두 v5 마커·옛 카드 0·상품 상세 링크도 v5 상세로). API 값 use_mobile_page=F 확인·audit 기록. 🔵 교훈: **API `use_mobile_page F`만으로는 프런트 캐시 때문에 안 바뀌었고, 관리자 저장 + 「사이트캐시 삭제」가 필요**했다(9/13 API 시도가 무효로 보였던 이유). 관리자 자동 조작 요령: `window.alert/confirm`를 JS로 덮어쓴 뒤 버튼 클릭(alert가 확장을 막는 함정 회피) · 페이지 렌더가 30초 이상 걸리면 스크린샷 재시도. 되돌리기 = 같은 화면에서 「사용함」.
- (아래 = 9/13 조사 원문) 📱 **#436(대표 9/13 "m.akkome.com 옛 사이트 — 니가 직접")** — 당시 미완. 실측: akkome.com은 폰·PC 모두 v5(skin6), m.akkome.com만 카페24 「모바일샵」 옛 스킨 — 판매 4종은 같은 카페24 레코드라 **옵션·결제가 동일**, 옛 상품 24종은 판매 F·품절 T라 주문 불가 → **금액 사고 요소 0**, 인상 문제만. API 시도: `PUT /mobile/setting use_mobile_page F`(러너 허용 추가 v5.9.314)로 바꿔도 **m. 서브도메인은 옛 모바일 스킨을 계속 냄**(설정은 메인 도메인 자동 전환용) → **원값 T로 되돌림**(audit 2건). Themes API는 GET뿐(대표 디자인 지정 불가). 관리자 자동 조작은 admin 페이지 렌더가 45초 무응답(로그인 세션 없음)으로 중단. **남은 길 = 카페24 관리자 > 디자인 관리 > 모바일 디자인 「대표 디자인」을 skin6로 지정**(대표 직접 또는 대표 로그인 후 클코 브라우저). 스크립트 `apply-436-mobile-off.js [T|F]`.
- 🧾 **(9/13 대표 문의 — 보고만) 명절 단체 할인 = 카페24 「개인결제창」(상품 89) 운용**: 옵션 「하우스귤=선물용 3kg 21박스」·수량 1·775,000원(c24:20260913-0000020). ⓐ주문안내 알림톡 = 정상 발송(문면 「[선물용 3kg 21박스] … 내일 월요일 발송」 — c24OptClean이 「하우스귤=」 접두 제거) ⓑ발송안내 = E 알림톡은 나가지만 품목 매칭이 안 돼 **먹는법 안내문 미포함**(「(안내문 미등록)」 — 9/11 유*우님 건 실측) ⓒ**송장변환·중간발주 = 수량 1로 잡히고 「개인결제창 하우스귤=…」은 [미매칭]** — 21박스 물량이 집계에 안 들어감(수기 처리 전제). 🔴 위험: 옵션 문구에 정식 품목 문구가 통째로 들어가면(예 「황금향 선물용 5kg 10박스」) **정상 매칭되면서 수량 1로 오집**(실코드 검산). 개인결제창 상품 삭제는 주문 데이터(항목 스냅샷)와 무관 — 알림·송장 영향 0.

- 📮 **#435(대표 9/13 "봇이 답했는데 직원 확인 필요로 왔다")**: 결함 아님 — 톡톡 요청형 알림(#298)은 ①요청형(주소·옵션 변경·취소·반품 = 봇이 답해도 **실제 처리는 사람**) ②봇 미답변(SKIP) 2종을 보내는데, 라벨이 `[톡톡]`뿐이고 하단 설명이 '봇 미답변'만 말해 혼동. 실측: id 2886 「주소 변경 가능한가요?」 = 시나리오 #5로 봇 답변 + 대표 추가 답변 기록 있음. 교정(대표 "요청사항만·무회귀") = 라벨 `[톡톡·요청형(봇 답변함)]` + 설명 문구 두 경우 구분. 판정 규칙·주기 무변경. v5.9.313.

- 🛰️🗣️ **#434(대표 9/13)**: **-a 스냅샷 자동수집 실패 「invalid input syntax for type json」** — 원인 = jsonb가 **이모지 반쪽(짝 잃은 서로게이트)** 거부: 리뷰 본문 `slice(0,300)`·옵션 `slice(0,120)`이 이모지(UTF-16 2단위)를 반으로 자름 → 회차 INSERT 전체 실패(9/13 04:39 — 자사몰 화면은 전날분 유지·주문 무영향). 실DB 재현(`'\ud83c'` 단독 → 같은 메시지·detail "low surrogate must follow…") → `jsonSafeStringify`(문자열의 반쪽만 제거·정상 이모지 보존)를 스냅샷 INSERT + `naverCfgSet` 전반에 적용. 배포 후 재수집 **#66 성공**(리뷰 400·타이머 ok·299자 리뷰 1건 = 제거 흔적). ⚠️ 함정: g 플래그 정규식 `.test()`+`replace` 혼용 금지(lastIndex). **-b 응대 자연스러움(대표 "고쳐줘")**: ①#31 품절·재입고 `{{판매현황}}` 전체 나열(물어본 품목이 판매중에 없으면 14줄 폴백) 제거 → 링크 1줄·결론 앞 ②레몬 = bot_products 28 「제주 레몬·시즌종료」 복원 + 시기 지식 id 15(비시즌 1/1~9/30) → 4채널 일관 "시즌종료·가을 그린레몬부터"(10월부턴 지식 소멸 → 판매현황 상태로) ③#39 회피형 문구 교정. `apply-434-scenarios.js` · `verify-434-qnasim.js` **6/6**. 🔵 원칙(대표 9/13): **판매현황은 누적 장부(시즌종료 보관) — 품목별 금액(주간 단가표)과 같을 필요 없음.** 시즌 지난 품목은 삭제 말고 시즌종료로 두어야 봇이 "시즌종료"라 답한다.

- 🎁 **#433(대표 9/12 "룰렛 당첨 쿠폰 5% 니가 직접 지급") — API 발급 완료** — v5.9.310~311(서버만).
  - **경로 신설**: 카페24 raw 러너(`cafe24_product_request {action:'raw'}`)에 쿠폰 API 허용 — `GET /coupons*`(정의 조회) · `POST /coupons`(정의 생성) · `POST /coupons/{no}/issues`(발급 — **issued_member_scope 'M' + member_id 1명만**, 전체/그룹 대량 발급은 가드 차단). 스코프 write_promotion(8/24 재동의). 스펙 = 공식 문서를 **실브라우저(Chrome)로 읽어 확인**(SPA라 r.jina.ai·WebFetch는 본문 못 받음 — WebFetch 1차 답은 지어낸 것이었음). 자동 발급은 여전히 아님 — 대표 지시 건별 수동 실행.
  - **안전 정지 1회**: 선례(8/13 김민주) 쿠폰 「5% 할인쿠폰」(…0915)이 **고정기간 8/13~9/12 23:00 = 당일 만료** → 발급 중단·대표 보고 → GO → **「5% 할인쿠폰(룰렛)」(coupon_no 6086230051600000967, 발급일부터 30일, 선례 설정 복제)** 신규 생성 후 발급. 이후 룰렛 5% 당첨은 이 쿠폰으로 발급(스크립트 KNOWN 고정).
  - **결과**: 회원 3575099520@n에 issue_no 6086230057603496428 발급(09/12 16:56·SMS 미발송) → reward_grants id 3 granted(audit) → 미지급 0.
  - ⚠️ 함정 3: ⓐ`benefit_percentage_max_price`는 **정수**(문자열 '0.00' → 422) ⓑ**생성·발급 직후 카페24 조회는 지연**(목록에 즉시 안 잡힘 → 스크립트가 재생성하지 않도록 coupon_no 고정 · 보유 검산은 재조회로) ⓒ`npm run deploy`가 메모리 부족으로 죽으면 **커밋만 되고 push·태그 없음** — `git status`로 ahead 확인 후 재실행(9/12 실측). 스크립트 `apply-433-coupon.js`(생성·발급) · `apply-433-coupon-finish.js`(재조회·지급완료).

- 🍊 **#432(대표 9/11 — #431 재현에서 나온 3건 "다 진행")**: ⓐ **`{{판매현황}}` = 판매중만**(품절·시즌종료 나열 금지) — 회사 서버(`qnaStoreData.sellingText`·`qnaRenderPlaceholders`) + **톡톡봇 리포 동일 교정(commit 89acdf6 + health 버전 7f02033 — 대표 "푸시 배포해줘"로 클코가 push · Render `/health` version 2026-09-11.1 실측 반영 완료)**. AI 판단 재료(storeBlock/봇 동적 블록)는 종전 statusText 유지 → 품절 문의 정확도 무회귀. ⓑ **발송 일정표 재료 `SHIP_GUIDE_DAYS` 14→21** — 대표 질문 "달력 잘 해놨는데?"에 대한 답: 달력은 정확, AI에 주는 **요약표가 오늘+14일만** 뽑아 9/25·26 휴무가 표 밖이었음. ⓒ 시나리오 #38 「산도가 가장 좋을 때」→ 시기 중립(`apply-432-scenario38.js`). v5.9.309. 검증 `verify-432-qnasim.js`(아래 결과).

- ⏰🔴 **#431(대표 실물 9/11 09:42): 톡톡 AI "지금 서둘러 주문하시면 오늘 바로 발송 가능" 오안내(오전 8시 마감 위반) + 청귤 「15일까지」→「14일까지 주문」 유도** — v5.9.308(서버만) + DB.
  - **-b 원인**: 문장은 시나리오·코드 어디에도 없음 → AI 생성. 발송 일정표 재료(`shippingScenarioToday` #379)가 날짜별 「[오전 8시 이내] / [8시 이후]」 두 줄을 주는데 **현재 시각을 안 알려줘** AI가 '지금=8시 전'으로 가정. 교정 = 재료 상단에 「⏰ 현재 시각 hh:mm — 8시 마감 지남/남은 분 + 지금 주문 손님에게 적용되는 줄 + 8시 이후엔 '오늘 발송·서둘러' 표현 금지」 명시. 계산·표 무변경(무회귀). dateOverride(재현 테스트)는 정오 기준. ⚠️ 봇 캐시 5분 → 8시 전후 최대 5분 지연(8시 전 문구가 두 경우 병기라 안전). 🔴 8~9시 주문 수기 발송(#171)은 내부 운영 — 손님 안내엔 미노출(현행 유지).
  - **-a**: 청귤 마감 = 9/15 오전 8시 → 시나리오 #38·#44·#48 「9월 15일까지」→「**9월 14일까지 주문**(9/15 오전 8시 마감·이후 판매 불가)」 + 시기 지식 id 7(마감기 09-01~09-15)에 마감 줄. `apply-431-scenarios.js`(audit·활성 시나리오 「15일까지」 잔존 0). 지식은 09-15 지나면 자동 소멸·시나리오 문구는 청귤 시즌 종료 시 #392 절차로 정리.
  - ✅ 검증 `verify-431-qnasim.js` **5/5**(배포 후 10:07 — "지금 주문하면 오늘 출발?" → 8시 마감 지남·9/13 발송 / 청귤 → 14일 유도) + `verify-431-qnasim-wide.js` **18문항 확장 재현(일반 6·추석 6·기타 6)** — 발송일·가격·과수·추석 마감(9/21 08:00)·재개(9/27)·VIP 구성(한라봉 없음)·단체·명단·시즌종료 전부 정답 기준과 일치. 「취소하고 싶어요」는 SKIP = 설계(요청형은 사람 처리·톡톡은 #30 키워드 자동응답 별도 경로).
  - 🔎 재현에서 발견한 개선 후보(대표 판단·미착수): ⓐ 발송 일정표가 오늘+14일까지라 **9/25·9/26 휴무가 표 밖** → AI가 "휴무 9/22~9/24"라고 말함(결론 9/27 발송은 지식 덕에 정확). `SHIP_GUIDE_DAYS` 14→21이면 연휴 전체가 표에 들어감(재료 7줄 증가·무회귀). ⓑ 시나리오 #38 고정 문구 「지금 시기 청귤은 산도가 가장 좋을 때」가 마감기 지식(산도 낮아짐)과 상충 → 중립 문구로. ⓒ `{{판매현황}}`이 시즌종료 품목(밤호박 9줄)까지 나열 — 길이만 문제·정확.

- 📨🔴 **#430(대표 9/8 실물 "천소미 주문이 알림 이력에 없다"): 네이버 변경목록 되감기 15분→60분 + 누락 건 수기 발송 대기 행** — v5.9.307(서버만).
  - **원인 확정(읽기 러너 `order_probe_request` 실측)**: 주문 2026090857632841 = 14:12:42 결제·PAYED·클레임 없음·번호 정상 — 그런데 14:15~14:27 틱(15분 되감기 구간)의 `last-changed-statuses`에 노출되지 않아 통째 누락. **#177(8/3) 「네이버 지연 노출」 유형 두 번째 실측**. 수집기 자체는 정상(3분 주기 ok·같은 시간대 다른 주문 291건 전부 sent — 대표 캡처의 나머지 두 건은 14:47 틱에서 sent·발주확인 완료 실측).
  - **교정**: `collectKakaoNotify`·`collectLmsGuide` 되감기 60분(두 곳 동일 식·replace_all). 중복 방지 = seen(order_key)+UNIQUE 그대로 → 재발송 0 · 호출 수 동일. `verify-430-rewind.js` **10/10**(교정 2곳·15분 잔재 0·페이지네이션·구간 계산 실식 평가(체크포인트−60분·null=2h·23.5h 클램프)·로컬 실서버 이력 API·실렌더 보류 행+[오늘/내일] 버튼·에러 0). ⚠️ 검증 함정: `const fromMs = Math.max(` 식이 다른 수집기에도 있어 **함수 구간(slice)에서 추출**해야 맞는 식이 잡힘.
  - **누락 건 처리(대표 "알림 보내줘야 하니 넣어줘")**: kakao_notify_log id 10451 = hold-0809·manual-needed 행 삽입(발송 0·audit) → 알림 발송 이력(실패·보류 필터)에 [오늘/내일 발송으로 안내] 버튼으로 노출(실렌더 확인). **대표가 버튼 누르면 기존 수기 경로(네이버 재조회→알림톡)로 발송, 발주확인은 수기**(hold 경로는 confirm_status manual-needed — #171 설계). 라벨은 기존 「⏸ 보류 (8~9시)」로 표시됨(전용 라벨 없음 — 무해·error 칸에 사유 기록).
  - 🔵 구조 메모: 이력에 없는 주문을 "수동으로 넣어 보내는" 정식 UI는 없음 — 이번처럼 클코가 hold 행을 삽입하면 기존 버튼이 받는다(러너/UI 신설 불요). 재발 시 같은 방법.

- 🍋 **#429(대표 9/8): 청귤 라스트특가 — 네이버 옵션명 「(★라스트특가)최상품 청귤(풋귤) 5kg/10kg」 + 인하(18,800/29,800) 자사몰 반영** — 코드·배포 0(DB·스킨·스냅샷만). 카페24 c93은 **대표가 이미 완료**(기본가 18,800·(★라스트특가) 옵션 2종 T·옛 사전예약특가 2종 F — API 재조회 확인). 자사몰 화면이 옛값(19,800/31,800)이던 이유 = 04:30 스냅샷이 변경 전 → **스냅샷 재수집(#61)** 으로 표기·가격 자동 전환 + 스킨 STORE_DATA 정본 갱신(`apply-428-skin.js 5731582511 429` — 인자화·백업 index_pre429) + 상세 19/19 재이관. 판매현황 48/49 판매가 18,800/29,800(audit). **품목별 금액·판매현황 이름은 그대로 두어도 무이상**(대표 질문): 네이버 문구가 「최상품 청귤(풋귤) 10kg」를 통째 포함 → 송장변환·중간발주 선매칭·알림톡/발송안내 includes 매칭 전부 정상 — `verify-429-cheonggyul.js` **6/6**(새/옛 문구·규칙 폴백·5kg 오집 0·알림톡 48/49·판매가). 박스 차감도 정산·단가표 이름 불변이라 무영향. 시나리오·지식·안내문에 청귤 가격 숫자 0건. 실화면 `verify-429-mall.js` **6/6**(옵션 표기·18,800/29,800·상단 가격대·홈 카드·잔재 0·에러 0) + 결제창 `verify-428-checkout.js 7,8`(청귤 케이스 기대값 갱신).
  - 🔵 이 케이스가 표준: **접두어만 붙고 핵심 이름이 그대로면 = 스냅샷 재수집만으로 자사몰 자동, 프로그램 이름 변경 불필요**. (이름 본체·등급 단어가 바뀌면 #428처럼 pricing·판매현황·정산 이름·코드까지.)

- 🍊🔴 **#428(대표 9/7): 황금향 선물용 옵션명 변경 — 3kg 「대과 7~15과」→「중대과 7~15과」 · 5kg 「대과 13~23과」→「중대과 13~25과」 + 귤 단가 인하 + 상세페이지 전량 갱신** — v5.9.306 / v=381 + DB·카페24·스킨. (대표가 작업 중 옵션을 두 번 바꿨음: 아침 「대과 13~25과」→ 곧 「중대과」 — 러너로 네이버 정본 재조회해서 잡음. **옵션명 작업은 착수 직전 정본 재조회 필수**.)
  - **① 판매현황(bot_products)** `scripts/apply-428-botproducts.js`: 신규 행 409(3kg 중대과)·410(5kg 중대과) = 판매중·42,800/62,800·발송안내문(옛 행 승계)·네이버번호 / 옛 행 227·228·396(대과 3종) soft-delete / 귤 판매가 213 선물3kg로얄 38,800→**34,500** · 214 가정4.5kg로얄 51,800→**48,800**(네이버 실시간) / items 마스터 10·11 이름 갱신. 전건 audit. ⚠️ **옛 문구 주문 11건(변경 전 결제·발송안내 미도래)은 품목 안내문 매칭이 끊김** — E 알림톡(승인 공통 템플릿·도착안내)은 정상 발송, 먹는법 장문(LMS 대체 문면)만 생략(#173 구조). 대표 고지됨.
  - **② 송장변환·중간발주(코드)**: PRODUCT_CATALOG 2행 + 규칙 파서 황금향 선물 분기 → 중대과. 🔴 **배포 필요 근거 실측**: 품목별 금액이 「중대과」가 된 순간부터 **구코드는 변경 전 결제분(옛 문구)을 규칙 경로에서 「대과」 표준명으로 만들어 등급 불일치 → 전부 [미매칭]**(aoMatchToPricing grade 비교). 신코드 = 새 문구(선매칭)·옛 문구(규칙 수렴) 모두 같은 중대과 품목. `verify-428-match.js` **16/16**. 🔵 교훈: **옵션명에서 등급 단어(대과/중대과/소과…)가 바뀌면 #406 자동 연동만으론 부족 — 규칙 파서 표준명도 같이 바꿔야 옛 문구가 수렴**(중량·과수 숫자만 바뀌면 자동).
  - **③ 카페24 결제가(#426 절차)** `apply-428-c24price.js`: c92 선물3kg로얄 38,800→34,500 · 4.5kg로얄 51,800→48,800 / **c100(VIP) 기본가 37,800→33,500**(하우스감귤 선물3kg 인하) + variants 재계산(41,800·60,800·33,500 결제가 유지). 재검산 13/13 · `audit-400` = 공통 옵션 전부 일치(❌ 2건 = 못난이(네이버만)·c92 황금향선물5kg(카페24만) 구성 차이 — 종전과 동일·오류 아님). 카페24 옵션 이름 「중대과」 변경 = **대표가 관리자에서 수동(c92·c94·c100 — API 422)**.
  - **④ 자사몰 스킨** `apply-428-skin.js`(+`-c100.js`): STORE_DATA opts 3종(c92·c94·c100) = 스냅샷#59(09:12 재수집) mergeSnapshot 동일 규칙 + **detailImages 20종 전량 갱신(내용 변경 12·동일 8)**. 🔴 **VIP(c100) 함정**: 네이버가 세트 번호를 재편(황금향 1→2·감귤 2→1)해 새 객체가 네이버 n1 「2. 과즙팡팡 황금향」을 받았는데 카페24 c100 옵션은 「1. 과즙팡팡 황금향 · …」 → **n1 = 카페24값 · nd = 네이버 표기**로 분리(런타임 mergeSnapshot은 번호 프리픽스를 무시해 prev 보존·nd만 갱신 = 일관). 서버 해시 대조(작업 전 일치) → 업로드 → 재다운로드 바이트 일치. 백업 `index_pre428_backup.html`.
  - **⑤ 카페24 상세 19/19** `apply-428-c24detail.js`(bulk-detail 러너 — #399 동일). 스냅샷 재수집 = `product_snapshot.last_run_at` 되감기(상세도 동반 갱신).
  - ✅ **실화면 실측** `verify-428-mall.js` **21/22**(옵션 표기 중대과·가격 3상품 전 옵션·상세 「23과」 0·홈 잔재 0·장바구니 자동 담기 3종(c94·VIP n1 번호차·감귤 인하가)·에러 0 — ❌1 = 테스트 계정 소멸로 로그인 주문서 미진입) + `verify-428-orderform.js` **3/3**(**비회원 구매 경로로 결제창까지: 「중대과 13~25과」 · 최종 결제 금액 62,800원**). `verify-428-notify.js` **7/7**(알림톡 매칭 새 문구→409/410·옛 문구 미매칭(오매칭 0)·가정용/못난이 무회귀·판매현황 중대과 2종만).
  - ✅ **최종 가격 확인(대표 "결제까지 같은지")** `verify-428-pricefinal.js` = 네이버 정본(실시간 channel-products) vs 카페24 **옵션별 1:1 25/25 일치**(5상품) · `verify-428-checkout.js` = **결제창 최종 결제 금액 13옵션 전부 네이버가와 동일**(비회원 경로·주문 미완료). 발견 1건: 감귤 (특가)소과 2종은 네이버 「(특가)」 접두가 카페24 이름에 없어 **확인창 1회 노출**(가격 정확·오담기 0·8/24부터) → 대표가 카페24 이름에 「(특가)」 부여(1차 「(행사)」로 써서 재요청 — **한 글자도 같아야 함**) → 확인창 없이 자동 담기 실측 ✅. 🔵 규칙: 네이버 옵션명 접두((특가)·(행사)·번호 등)까지 카페24와 동일해야 원터치 담기.
  - 📦 **#428-b(대표 실물 9/7 "박스재고 선물용 3kg·5kg 차감 안 됨")** — 코드 결함 아님·데이터 매핑 2건(무회귀 = 코드 무변경): ①대성 pricing #97(8/31~9/6) 황금향 선물용 2종 boxType=「해당없음」(선물용 박스 신설 #420 뒤 그 주 단가표에 매핑 미입력) → 9/1~9/6 정산 미차감 ②정산 #377(9/7) 품목명 「대과 …」 ≠ pricing #99 현재명 「중대과 …」(대표 9/7 아침 이름 변경 — 정산은 이름 정확 일치로 박스 매핑) → 미차감. 교정 `scripts/apply-428-boxfix.js`(#97 boxType 2건 + #377 품목명 2건 — 단가·소계 무접촉·audit). 결과: **3kg 대성 1,465(=이동 1,500 − 정산 35) · 5kg 1,475(=1,500 − 25)**, 업체 1,500/1,650 무변동. `verify-428-box.js` **11/11**(로컬 실서버+실DB API·미매칭 0·실렌더 카드·기존 6종 무회귀). 🔴 **규칙 2**: ⓐ**새 박스 추가 시 그 주 단가표 boxType도 즉시 세팅** ⓑ**단가표 품목명을 주중에 바꾸면 그 주 정산에 이미 입력된 품목명도 같이 바꿔야 박스 차감**(정산 금액은 price/subtotal 저장이라 무영향 — 박스만). ⚠️ 검산 함정: DATE 컬럼을 `toISOString().slice(0,10)`으로 찍으면 KST에서 하루 전으로 보임(정산 #377은 9/6이 아니라 **9/7**) — 기대값은 서버 normDateSafe 기준으로.
  - **⑥ 시나리오·시기지식**: 「23과·대과·7~15과·13~25과」 검색 — 시나리오 4건 히트는 전부 감귤 사이즈(로얄과/중대과/소과) 일반 안내라 **황금향 과수 문구 없음 → 수정 0** · 지식 0 · 톡톡봇 리포 0. 봇 {{판매현황}}/{{가격표}}는 bot_products 반영으로 자동(1분 캐시).

- 🔔 **#427(대표 GO 9/6): 자사몰 옵션 결제가 자동 감지 — #426 재발 방지(감지형)** — v5.9.305(서버만). `cafe24_sync`(05:10)에 옵션별 결제가 대조 추가: 카페24 결제가(기본가+추가금) vs 네이버 옵션 결제가를 옵션 키(과일+용도+중량+등급)로 비교 → 화면가≠결제가면 **텔레그램 경보**(감지만·값 교정은 사람이). 🔴 읽기 전용·전 구간 try/catch = 기존 기본가/품절 동기화 영향 0. 공통 옵션만 비교(못난이·선물5kg 등 구성차 오경보 안 함). `verify-427-optprice-audit.js` **15/15**(9/6 교정 전 3건 감지·교정 후 0·과결제 감지·구성차 오경보 0). 🔵 이제 옵션가 어긋나면 새벽에 자동으로 잡힘 — #426류 재발 시 대표가 텔레그램으로 먼저 안다.

- 💰🔴 **#426(대표 9/6): 네이버 vs 자사몰 판매가 대조 — 옵션 추가금 미갱신 재발 교정(#400 재현)** — 코드/배포 0·카페24 API만. 원인 = **화면가(네이버 스냅샷·자동) ≠ 결제가(카페24 옵션 추가금·수동)** 가 네이버 옵션 인상분만큼 갈린 것. 대표 "네이버 기준으로" GO → 카페24 추가금 6개 교정: **c94** 황금향 5kg/선물3kg/선물5kg(각 −4,300 매출손해였음 → 57,800/42,800/62,800) · **c100** VIP 황금향선물5kg(과결제 61,800→60,800) · **c92** 타이벡하우스 2.5kg로얄(과결제 32,800→29,000)·황금향3kg(과결제 37,800→33,500). audit-400 + 네이버 정본(`naver_query_request`) 이름 대조로 공통 옵션 전건 일치 확인·audit 기록. 남은 ❌ = 못난이5kg(네이버만)·황금향선물5kg(c92 카페24만) 구성 차이(가격 오류 아님·API로 옵션 조정 불가). **🔴 재발방지 상설: 네이버 옵션가 변경 직후 반드시 `node scripts/audit-400-priceset.js` 1회.** 상세 인수인계 = `docs/자사몰_판매가_동기화_점검_인수인계.md`(신설). ⚠️ 검증 함정(9/6 자책): 대조 스크립트에서 상품별 네이버 기준가가 다른데(c92=29,000·c94=33,500) 한 값을 공용하면 전 옵션 거짓 불일치 — 각 채널 자기 기준가로.

- 📝 **#425(대표 9/3): 업무일지 데이터 보완** — DB 전용(INSERT만)·코드/배포 0·상세 = audit_logs id 4132 (⚠️ PUBLIC 리포라 여기엔 상세 미기재 — 개인 관련 데이터 작업).

- 🍊 **#424(대표 9/2): 하우스감귤 1일 행사(소과 2.5kg 주문 = 4kg 발송·내일 9/3 아침 종료) 송장변환 매칭** — v5.9.304 / v=380. 행사 옵션 「행사★하우스귤 2.5kg 소과→중량up 4kg」이 [미매칭]으로 떨어지던 것(#406 중량up 가드 정상 — 카탈로그에 4kg(소과) 부재가 원인) → PRODUCT_CATALOG 1행 추가(행사 종료 후 무해). **완전 매칭엔 품목별 금액(효돈농협)에 「고당도 하우스감귤 / 상품 및 과수: 가정용 - 4kg(소과)」 등록 필요(단가 = 대표) — 등록 즉시 자동 연동**(verify-424 ①에서 기계 실증). 미등록 상태는 [미매칭] 표기(오집 0 — 안전). (특가)4.5(소과) = 4.5 그대로·오늘 pricing 15종 전수 신=구 동일. `verify-424-match.js` **8/8**. 톡톡·자사몰 영향 = 검토 후 무수정 보고(행사 최저가 23,800 종전과 동일 → cafe24_sync 무영향·봇 가격 답변 정확·시나리오 무수정 권장).
  - 🔴 **상설 규칙(9/2 대표 지적으로 확립)**: **네이버/카페24 옵션을 바꾸는 모든 작업의 마지막 단계 = ①자사몰 실화면(akkome.com) 표기 확인 ②스냅샷 재수집(`product_snapshot.last_run_at` 되감기)** — 자사몰 화면은 새벽 04:30 스냅샷으로 그려져 당일 변경이 안 보인다(플레이북 5단계에 있었는데 9/2 최종 점검에서 실행 누락 — 대표가 실물로 발견). 실화면 확인 전 "이상 없음" 보고 금지.
  - ✅ **행사 종료·원복 완결(9/3)**: 네이버·카페24 이름 원복 = 대표(카페24 이름 변경 API 불가 422 재실측 — 관리자만 가능) → 클코 검산 = c92 행사 문구 소멸·소과 2.5 복귀·결제가 23,800·variants 10종 무손상 + 스냅샷 재수집 + **akkome.com 실화면 실렌더 확인**(행사 문구 0·소과 (특가) 23,800·에러 0 — 행사 전 8/24~9/1 정상 가동하던 구성과 동일 복귀). 효돈 pricing 4kg 줄 = 이번 주 표와 자연 소멸 예정(다음 주 미포함).
  - **-b(대표 확정 9/2 "결제가는 2.5kg로")**: 효돈 pricing 98에 「가정용 - 4kg(소과)」 **13,500원(소과 2.5kg 결제가 동일) 등록 완료**(DB 직접·audit 기록·실DB 기준 행사/특가/자사몰 형식 매칭 전부 검산 ✅). **자사몰 c92 행사 옵션 추가 = API 불가(옵션 값 추가·변경·삭제 422 — 8/24 실측 확정) → 대표 관리자 수동 추가 대기**: 옵션값 「1. (제철)고당도 하우스감귤 · 행사★하우스귤 2.5kg 소과→중량up 4kg」·추가금 0(결제가 23,800). 추가 후 클코가 variant 검산. ⚠️ **행사 종료(9/3 아침) 시**: 네이버는 대표 옵션 원복 + 카페24에 추가했다면 그 variant 미노출(display/selling F) 처리 — 대표 한마디에 클코 실행.

- ✅ **#423(대표 9/2): 지출결의서 결재 대기 탭 — 체크박스 일괄 승인** — v5.9.303 / v=379. 행별 체크 + 전체선택 + [✅ 선택 승인](빈 목록 자동 숨김) — 이력 탭 기존 일괄 승인과 동일 패턴 재사용(건별 순차·실패 개별 보고·양 탭 재렌더). 개별 승인/반려 무회귀. `verify-423-batchapprove.js` **9/9**(실클릭·승인 PUT 라우트 가로채기 = 실DB 무변경 — 부분 2건만 PUT·전체 23건·0건 안내). ⚠️ 검증 재사용 팁: 진입 렌더·탭 클릭 렌더 겹침 경합은 테스트 한정(실사용 주기 재렌더 없음 확인) — 스크립트에 폴링 정지 처리 포함.

- 📦 **#422(대표 실물 8/31 "입고 등록했는데 총 재고 0"): 박스재고 기준일 당일 입고 즉시 반영** — v5.9.302(서버만). [A안] 설계(수기 수정 = 기준일 스냅샷·당일 입출고 전부 제외)가 같은 날 「수기 수정 → 입고 등록」을 통째로 삼키던 결함(선물용 +3000/+3150 실사고). 교정 = 당일 건도 **수기 수정 시각(updated_at) 이후 등록분(created_at)은 반영** — 수정 전 등록분 제외·재수정 시 자동 재제외(이중 계산 방지 의미 일관). 🔴 **정산 차감 규칙은 의도적 무접촉**(정산일=기준일 기존 16건 — 건드리면 과거 숫자 변동. 같은 날 "수기 수정 후 정산 입력"은 여전히 다음날 반영 한계 — 필요 시 별도 지시). `verify-422-boxsameday.js` **7/7**(구코드 표시값 캡처 → 신코드 전수 비교: 차이 = 선물용 2종뿐 기계 증명. ⚠️재실행 시 구코드 캡처(box-old-422.json·세션 스크래치) 재생성 필요).

- ✍️ **#421(대표 8/31 실패 수집함 건 "원래 잘 되던 문구가 안 된다"): 글샘 태그 오염 대비 강화** — v5.9.301(서버만·agents/글샘.js 단독). 실패 실체(실DB run318·319) = #396 고질(모델 스냅샷 교체 후 도구 JSON 태그 오염 급증)에 글샘 대비가 구식: 제목 오염 소실 + 구식 폴백은 본문 1종만 + **send_tip에 파편 「</antml_parameter>」 실저장**(무방비 필드 실사고). 교정 = #396 검증 해법 이식: 전체 오염 검사 → **평문 JSON 폴백**(제목·3종·부가 필드 통째 재확보) → 표기 필드 전수 세척 → 종전 폴백·정직 오류 = 최후 안전망(무회귀). `verify-421-geulsaem.js` **19/19**(SDK 스텁 — 정상 경로 무회귀·부분 오염·전멸·안전망 전 분기). ⚠️ **8/24 취소 지시(「톡톡·문의답변 제목 생성(마루)」 재제안 금지)와 별개** — 마루·문의답변 경로 무접촉, 글샘 카피 경로만. 실생성 실물 확인 = 다음 글샘 지시에서(대표 같은 문구 재지시 권장).

- 📦 **#420(대표 8/31): 박스재고 「선물용 박스 3kg·5kg」 추가** — v5.9.300 / v=378. 박스재고는 DB(box_inventory) 동적 렌더 구조라 **하드코딩은 딱 2곳뿐**: ①server.js 시드 목록(boxProducts) ②app.js BOX_OPTIONS(품목별 금액 박스 매핑 라디오 — 값은 product_name과 정확히 일치해야 차감 매칭됨). + 배지 색상(선물용=보라 톤). 카드·차감 이력·입고/이동 등록·입출고 필터·정산 자동 차감은 전부 자동 확장(기존 6종 무접촉). `verify-420-giftbox.js` **13/13**(실렌더 카드 8장·양식 동일·기존 6종 무회귀·에러 0). ⚠️ 검증 함정: 실렌더에서 reload 직후 바로 switchPage하면 앱 초기화 경합으로 데이터 0 — **2.5초 대기 + 재시도**로 통과(다음 박스 추가 때 이 스크립트 재사용).

- 📖 **(8/30 대표 문의 — 기록만)**: 알림 이력에서 네이버 행 성함·번호가 일시적으로 단체 '-'였던 건 = 표시 시점 재조회 1회 실패(아침 발송안내 256건 대량 배치 시간대 429 추정) → 마스킹 폴백(설계된 안전 동작) → 다음 조회에 자연 회복 실확인. 데이터·발송 무피해. 자주 거슬리면 "성공 캐시 장기 보관" 소보강 후보(지시 대기).

- 🛡️ **#419(대표 GO 8/28): 쿠팡 구매자 성함·안심번호 DB 저장 — 재시작 후에도 표시 유지** — v5.9.299(서버만). 배경 = 쿠팡은 단건 재조회 API가 없어 수집 시점 메모리 캐시로만 표시(#401-c 한계) → 배포(재시작)마다 옛 쿠팡 행이 '-' 폴백(8/28 대표 실물 문의로 재확인). **대표 확정 예외: 050 안심번호는 기간 후 회수라 노출 위험 낮음 → 쿠팡 행에 한해 cp_name·cp_tel 저장**(네이버·자사몰 = 종전 원칙 그대로 마스킹만 저장·표시 시점 재조회 — 무접촉).
  - 시공: 두 이력 테이블에 cp_name·cp_tel 추가(additive) + 쿠팡 수집기 2곳 finally 저장(발송 로직 무접촉) + `nlogFetchTels` 쿠팡 DB 시딩 분기(GET·enrich 공용) + 12자리(0505) 포맷. 프론트 무변경. 기존 '-' 행은 1회성 러너 `cp_name_backfill_request`(기간 조회·읽기 전용)로 백필 — **신규 키라 구인스턴스가 소비 불가(#398 함정 원천 회피)**.
  - ✅ `scripts/verify-419-cpname.js` **15/15**(로컬 실서버 = 캐시 0(재시작 상황)에서 GET·enrich 두 경로 성함·안심번호 실표시 · fmt 10/11자리 무회귀 · 비쿠팡 행 cp 컬럼 오염 0 · 마스킹 저장 원칙 유지).
  - ℹ️ **가입(join:) 행은 확장 미채택 — 대표 확정(8/28 "단순가입이라 이대로가 맞다") — 재제안 금지.** 가입 행은 가입~다음 재시작까지만 성함·번호 표시(회원 API가 member_id로 휴대폰 미제공 = 재조회 불가·#401-d 실측). 그 손님이 주문하면 자사몰 주문 행(항상 재조회)으로 식별됨. + 8/28 09:34 가입 dry 행(ssblues72) = live 전환 전 실가입 — 환영 미발송은 대표 확인·수동 발송 불요 확정.

- 👋 **#417 완전 종결(9/4 실측)**: live 전환 후 실가입자 5명(9/1~9/3) 환영 알림톡 **전건 real/sent** — 잔여 관찰 소멸.
- 👋 **#417(대표 GO 8/28): 가입 환영 알림톡 실가동 — 완결** — v5.9.298. UK_5877(가입환영2 — 혜택 3종 문구+[문의하기] MD) 검수 승인 실측 → 기본 투입(#414 자동 일치 구조·`welcomeTemplate()`·롤백 = env/기본값 UJ_9086) → **테스트 실발송 sent(대표 폰·문면 정상)** → **`notify_channel_mode.join='live'` 전환(audit 기록) — 이제 4채널(네이버·자사몰·쿠팡·가입) 전부 실발송**. `verify-417-unit.js` 12/12. 잔여 관찰: 첫 실가입자 환영 알림톡 실물(파이프라인은 #401-d E2E 6/6 기검증).
- 🗑️ **#418: 오등록 중복본 4장(UK_5750~5753) 삭제 — 완결(8/28)** — API 삭제는 승인(APR) 템플릿이라 509 차단(UJ_9082 선례) → **대표가 알리고 관리자에서 수동 삭제**(삭제 전 전수 검색으로 발송 미참조 최종 확인) → selftest 검산: **목록 8개 = 실전 4종(UK_5754·5755·5756·5877) + 롤백 예비 4종(UJ_9084~9087) — 중복본 소멸·실전판 전부 무사**. 알리고 목록과 코드 기본값이 다시 1:1 정합.

- 🧹 **#416(대표 8/26): 발송안내(E) 상품명 정제 통일** — v5.9.296(서버만). 대표가 자사몰 「1. (제철)」을 지적하다 **네이버 E가 옵션 원문 통째(「아꼼이네 상품선택: 1. … / 상품 및 과수: …」)로 나가던 더 큰 누락 발견** — #146 정제기가 주문안내에만 적용돼 있었음. E 조립부 2곳(네이버·자사몰)에 동일 정제 적용(쿠팡·수동/소급 경로는 기존 정제 or 자연 형식 — 무접촉·변경 2곳 grep 검산). `verify-416-unit.js` **15/15**(-b 확장: 실발송 원문 6종 실정제 + 🔴 **상품명 조립부 8곳·인라인 삽입부 전수 기계 검산 — 새 발송 경로가 정제 없이 추가되면 자동으로 잡힘** + 실DB 주문안내 30건 잔재 0 스캔 + 쿠팡 자연 형식 무변형). 쿠팡 E 폴백 인라인 1곳도 통일(v5.9.297). 새 "품목"은 정제기가 구조 규칙이라 자동 커버. ✅ **실물 검증 종결(8/27 아침 배치)**: 발송안내 실발송 318건(네이버 304·자사몰 3·쿠팡 11) 전수 스캔 — **잔재 0** · 주문안내 30건 잔재 0 · 실패 0·보류 0·쿠팡 발주확인 수기 0 · 수집기 13종 전부 ok. #416 완전 종결. 대표 확정 병기: 쿠팡 다건 주문 = 건별 발송 유지 · 알림톡 문안(문의 안내 줄 추가) = 현행 유지.

- 🔗 **#415(대표 실물 8/26): 알림톡 [다른 상품 구경하기] 랜딩 교정** — 스킨만(서버 무배포·`skin6/product/list.html`)
  - 버튼 URL(`/product/list.html`)이 카테고리 미지정이라 카페24가 **빈 목록(Total 0 Items)** 을 그리던 것(구판 UJ 템플릿 때부터 잠복). 🔴 **버튼 URL은 카카오 심사 대상이라 변경 불가** → 랜딩 페이지에 가드: **cate_no 없으면 홈(v5 상품 카드)으로 안내·cate_no 지정 접근은 종전 유지**(무회귀).
  - 절차 준수: 서버 원본 SFTP 대조(해시 일치) → 수정 → 업로드 → 재다운로드 바이트 일치 → **akkome.com 실측 3/3**(미지정=홈 이동·상품 노출·cate_no=24 유지). 정본 = `_참고자료/카페24스킨백업/scripts/skin6-work-final/product/list.html`.
  - 함께 실물 확인(#414): **[문의하기](MD) 버튼 실동작 대표 확인** — 누르면 알림톡 원문이 상담 채팅에 전송(의도대로 = 고객 특정 재료). #398 목적 완전 달성.

- 📨 **#414(대표 GO 8/26): 알림톡 MD([문의하기]) 버튼판 투입 — 완결** — v5.9.295 / v=376
  - 검수 승인 실측(selftest — UK_5754 일반2·UK_5755 예약2·UK_5756 발송안내3 = APR) → **기본 템플릿 교체·실가동**. Render env TPL 4종 = 미설정 실측(selftest env에 TPL 키 추가) → 코드 기본값(APPROVED_TPL)이 곧 스위치·env 변경 불요였음. **테스트 실발송 1건 성공(sent·UK_5754 — 대표 폰, [문의하기] 버튼 실물 확인은 대표)**.
  - 🔵 **구조(문서 절차 개선)**: 문서의 "templates JSON 버튼에 MD 추가" 대신 **`MD_TPL_KEY`로 최종 tpl_code↔문안·버튼 세트 자동 일치** — env가 구코드(UJ_9084/9085/9087)를 가리키면 구버튼으로 자동 복귀. **어떤 env 상태에서도 "버튼-승인 구성 불일치 = 발송 거부"가 원천 불가**. 롤백 = env(또는 APPROVED_TPL)만 구코드로.
  - ✅ `verify-414-unit.js` 12/12(문안 3쌍 바이트 동일 = 문면 무회귀·env 롤백 자동 복귀) + UI 라벨 3/3 + 이력 무회귀 17/17 + 실번들 검산(롤링 구인스턴스 응답 함정 재확인 — until 재시도로 통과).
  - 잔여: **UK_5877(가입환영2) 검수 중** — 승인 시 `ALIGO_TPL_CODE_WELCOME` 또는 APPROVED_TPL.welcome 교체 + join live / **UK_5750~5753 오등록 중복본 = 심사 종료 후 삭제**(DELETABLE_TPL 확장 + 대표 GO).

- 🩹 **#413(대표 실물 8/26 "이상한 네모칸들"): #412 회귀 핫픽스** — v5.9.294 / v=375. #412 Edit 시공 때 행 `<tr …>`의 **닫는 `>`가 소실** → 첫 `<td>`가 속성으로 오파싱되고 **행 체크박스 100개가 표 밖으로 밀려나(foster parenting) 위쪽에 네모칸 100개로 노출**(체크 기능도 상실). `>` 복원 + 재검증 17/17.
  - 🔵 교훈 3: ⓐ**행 템플릿을 Edit로 부분 수정하면 태그 닫힘이 소실될 수 있다** — 표 수정 후 검증에 「foster 노드 0 + 행 체크박스 정위치」를 상설 항목으로 추가함(verify-412에 반영·재사용) ⓑ구조 검사(클래스·속성 존재)만으론 못 잡는다 — data-okey·셀 클래스는 깨진 채로도 통과했음 ⓒ**배포 직후 실번들 검산은 롤링 드레인 중 구인스턴스가 응답할 수 있다**(#398 폴러판의 정적 파일판) — 틀린 결과가 나오면 커밋 내용부터 재확인 후 재검산.

- 🛡️ **#409(대표 8/26): 쿠팡 안심번호(050) 「번호 오류(유선)」 오표기 교정** — v5.9.292 / app.js v=373. #219 판정식(01x 외 전부 유선)이 안심번호에 걸리던 것 — 발송은 원래 정상(coupangBadTel이 050 허용), 표기만 오류. 쿠팡 행 050 = 번호+「🛡️ 안심번호」 중립 뱃지, 서버 bad-tel 판정(진짜 유선)은 종전 유지. `verify-409-safetel.js` 6/6. ⚠️ 검산 함정: bash 변수에 번들 담아 grep하면 거짓 0 — **파일로 받아 grep** 할 것.
- ⚡ **#412(대표 GO 8/26 "굼뜬 느낌 없도록 지금 작업"): 알림 이력 선표시·후채움 — 완료** — v5.9.293 / app.js v=374
  - 원인(실측): 지연 주범 = 응답 전 동기 풀번호·성함 재조회(네이버·카페24 왕복 수 초). DB는 0.1초 수준(전체 7.7천 건 카운트 89ms — 총건수 무관).
  - 시공: **fast=1 선표시**(목록 즉시 — 실측 첫 표시 ~0.7초) + **신설 `POST /api/agent-office/notify-logs/enrich` 후채움**(풀번호·성함·선물하기 보정을 뒤에서 공급 — `nlogApplyTels`가 data-okey 행에 그 자리 패치·순번 가드로 경합 폐기·요약 보정은 **절대값 교체**라 이중 합산 없음) + 기본 기간 오늘(탭 첫 진입 1회) + created_at 인덱스 2종 + 선물하기(***) 보정 건별→배치(성수기 대비). **fast 미지정 = 종전 동작 그대로**(하위호환·무회귀). 재조회 로직은 `nlogFetchTels` 공용 함수로 추출(GET 종전 경로·enrich 공용).
  - ✅ `verify-412-fastload.js` **15/15** + 실번들 v=374 검산. ⚠️ 검증 함정: 실후채움이 이미 요약 보정을 적용한 순번에 가짜 보정 재주입은 중복 방지 가드에 막힘(정상) — 테스트는 새 순번으로 주입.
- 📦 **#410~#411(대표 8/26): 미니밤호박 시즌 종료 — 전 채널 품절 완결** (코드·배포 0 — DB·수집만)
  - c91 카페24 = 진열 유지·판매중지(F) 확인(변경 0). 네이버 = 대표가 품절(재고 0) 처리 → 되돌림 위험 소멸.
  - **#411: 자사몰 화면이 여전히 특가·판매중으로 보이던 것**(대표 실물) — 원인 = v5 스킨 카드가 `/api/public/store-snapshot`(새벽 04:30 네이버 스냅샷)을 읽는데 대표의 품절이 그 이후. **스냅샷 즉시 재수집 유도**(`last_run_at`을 앵커 이전으로 → 60초 틱 재수집·id 44)로 해결 — 밤호박 statusType OUTOFSTOCK·stock 0.
  - ✅ **akkome.com 실화면 실측 6/6**: 카드 = 「시즌 준비중 · 시즌 오픈 시 판매 · 🔔 시즌 대기 신청」 전환, 「지금은 시즌이 아니에요」 구분선 아래 배치, 특가 딱지·가격 소멸, 에러 0.
  - 🔵 **품절 3단계 절차 영구 기억**(자동메모리 jeju-soldout-procedure — 대표 "기억해두라" 지시): ①네이버 품절(대표) ②카페24 selling F(새벽 sync 자동·급하면 러너) ③자사몰 화면 = 스냅샷 재수집으로 자동 전환. ⚠️ 스킨 품절 판정은 OUTOFSTOCK/재고0만 인식(SUSPENSION 미인식 — 네이버는 품절 방식으로 내릴 것).

- 🏠 **#408(대표 GO 8/25): 카페24 토큰 갱신 실패 알림 — 일시 오류 vs 진짜 재승인 구분 + 회복 알림** — v5.9.291(서버만)
  - **배경(8/25 새벽 실사고 조사)**: 카페24 인증 서버 일시 장애로 새벽 토큰 갱신 실패 → 수집기 3종이 「cafe24_reauth_required」 알림(대표 수신 3건) → **05:38 같은 리프레시 토큰으로 자동 회복**(주문안내·발송안내 ok 복귀·주문 유실 0). 같은 토큰으로 회복 = 재승인 불필요였음이 실증 — cafe24.js가 갱신 실패를 원인 불문 reauth로 표기하던 것이 오탐 원인.
  - **교정**: 400/401(invalid_grant 등)·토큰 없음/복호 불가만 「재승인 필요」 / 5xx·429·네트워크·타임아웃 = 「일시 오류 — 자동 재시도 중·재승인 불필요」(code 'temp') / **실패→정상 전환 시 회복 알림 1회**. 알림은 상태 전환 시에만(연속 실패 중복 0). server.js의 `e.code==='reauth'` 분기 2곳은 무접촉(temp는 자연히 "API 오류"로 표기).
  - ✅ `scripts/verify-408-cafe24-temp.js` **10/10**(실모듈 require·fetch 스텁 — 5xx/네트워크=temp·400=reauth·토큰없음 무회귀·회복 알림·중복 0) + 배포 후 cafe24_notify 신규 회차 ok 실측.
  - ✅ 8/25 cafe24_sync 회차(05:13) 실패 누락 건 = **대표 확정(8/25 "특가 이후 변경사항 없어") — 수동 재실행 불요·8/26 05:10 정기 회차로 자동 대조**. 8/26 아침 실패 알림만 없으면 종결.

- 🗂️ **#407(대표 8/24): 문의 관리 탭 분리 + 새로고침 위치 복원** — v5.9.290 / app.js v=372
  - 판매현황·가격 탭의 3개 섹션을 독립 탭으로 분리: **①📬 알림 발송 이력 ②🚫 발송 휴무일 ③🏷️ 판매현황·가격** → 이후 기존 탭 순서 그대로(기본 활성 = 시나리오 무회귀·마크업 내용 무변경 이동만·각 탭 진입 시 렌더). 수정 이력 카드는 기존 규칙대로 대상 있는 탭(products·scenario·season)에서만 표시.
  - **새로고침 복원 = 전 메뉴 공통**(대표가 작업 중 "문의관리만 한정 아니야!"로 확장 지시): `akm_last_page`(switchPage마다 저장) + `akm_inq_tab`(문의 내부 탭) → 자동 로그인(새로고침) 경로에서 복원. 수동 로그인·로그아웃 후 재로그인 = 종전대로 메인 시작. 권한 밖 메뉴는 기존 switchPage 가드(직원→schedule·세무사→expense)가 그대로 처리.
  - 판단 1건(보고됨): 알림 이력 헤더의 낡은 「지금은 dry-run(문면 기록만)」 문구 제거 — 8/24 live 전환으로 사실과 달라진 표기(#215 계열 낡은 라벨 방지).
  - ✅ `scripts/verify-407-tabs.js` **16/16**(실클릭·**reload 실검증** — 탭 순서·단독 표시·각 탭 렌더·휴무일/알림 탭 복원·송장변환 복원·메인 유지·시나리오 무회귀·에러 0).

- 🧩🔴 **#406(대표 8/24): 송장변환·중간발주 매칭 — 황금향 못난이 오매칭 교정 + 「품목별 금액 = 자동 연동」 구조화** — v5.9.289 / app.js v=371
  - **실사고**: 8월 신규 옵션 「황금향 못난이 5kg」가 matchProductRaw 황금향 분기에 못난이 케이스가 없어 **「가정용 5kg」로 조용히 오매칭 집계**(10kg는 미매칭). 대표가 "중간발주에 안 뜬다"로 발견 — 실체는 미표시가 아니라 가정용 수량 +1 오염. 오늘(8/24) 못난이 5kg 실주문 1건 존재(발주확인 완료·미발송) — 대표에게 수기 보정 고지됨.
  - **교정 ① 구조**: `matchProduct`에 **pricing 정식 품목명 「통째 포함」 선매칭**(긴 이름 우선) — 이제 신규 네이버 옵션은 **품목별 금액 등록만으로 송장변환·중간발주 매칭 자동**(규칙 파서 수정 불요 = 대표 운용 모델 그대로). 전체 이름 일치만 인정이라 오매칭>미매칭 원칙 안전. ⚠️ **중량up 행사 옵션은 선매칭 제외**(옛 중량 이름이 통째로 남아 오집 위험 — 종전 규칙 우선 가드).
  - **교정 ②**: 규칙 파서 황금향 못난이 분기 + PRODUCT_CATALOG 2종 추가(pricing 미로드 폴백도 정확).
  - ✅ `scripts/verify-406-match.js` **9/9** — 🔵 검증 방법론(재사용 가치): app.js에서 실코드(카탈로그·매칭 함수)를 추출 실행 + **git HEAD 구코드와 동일 입력 전수 비교**로 "차이 = 의도한 2종뿐"을 기계 증명(오늘 pricing 24종 왕복 24/24·규칙 폴백 무회귀·중량up 가드·가정용 무회귀). + `verify-406-smoke.js` 5/5(실렌더·에러 0).
  - ℹ️ **알림톡·발송안내문(LMS) 경로는 원래 정상** — bot_products에 못난이 5·10kg 등록·발송안내문 존재(matchNotifyProduct는 긴 이름 우선 includes라 신규 품목 자동 흡수), 오늘 실주문 알림톡 실매칭·발송 확인. 고장은 송장변환 쪽 하드코딩 규칙뿐이었음.

- 📬 **#405(대표 8/24): 알림 발송 이력 표 개편** — v5.9.288 / app.js v=370
  - 컬럼 재구성: **[플랫폼](신설·네이버 포함 전 행 배지) | 일시(주문) | [구매자](신설) | 품목(폭 170→250) | 수신 | 주문안내 | 발송안내**. 문면 보기 = 각 칸(주문안내/발송안내) 밑 details로 이동(우측 [문면] 컬럼 폐지·[발송안내 수동 재발송]은 발송안내 문면 안, 발송 전 행은 주문안내 문면 안 [수동 발송]으로 보존). **[발주확인] 컬럼 폐지(대표: "알림톡 나감 = 발주확인 완료라 불요")** — 단 예외(confirm_status failed/manual-needed)만 주문안내 칸에 「✍️ 발주확인 수기 필요」 경고 뱃지 유지(실패·보류 필터도 계속 잡음 — 안전한 쪽 판단·보고됨).
  - **채널 필터 신설**(`notify-log-ch` — 전체/네이버/자사몰/쿠팡/가입, 서버 `ch` 파라미터 = order_key 프리픽스 판정): 기존 상태 필터와 AND 조합 → **자사몰+발송안내 미도래 = 수기 체크 대상 즉시 추출**(대표 요청 시나리오). [초기화] 시 채널도 전체 복귀.
  - **구매자 성함 = 풀번호와 동일 원칙(DB 저장 없음·표시 시점만)**: `_telCache`에 name 동봉 — 네이버·자사몰 = 표시 시점 재조회에서 취득 / 쿠팡·가입 = 수집·신호 시점 캐시(쿠팡은 단건 재조회 API 없음 — 재시작 후 옛 행은 '-' 폴백·풀번호와 같은 한계). 수집기 시딩 5곳 + 재조회 2곳에 name 추가(발송 로직 무접촉·additive).
  - ✅ 검증 `scripts/verify-405-notifylog.js` **18/18**(로컬 실서버 3457·실렌더·실클릭·confirm 전부 거부로 실발송 0 — 헤더·8칸 정렬·전 행 배지·문면 펼침(템플릿 라벨+본문)·채널 필터 3종+API 프리픽스 검산·수동 재발송 보존·체크박스 일괄바 무회귀·에러 0). 배포 후 실번들 v=370 문자열 5종 검산 완료.
  - ⚠️ **대표 실물 확인 항목**: 구매자 성함 실표시는 로컬에서 검증 불가(릴레이·카페24 키 = Render 전용)라 **실화면에서 네이버·자사몰 행에 성함이 뜨는지 1회 확인 필요**(안 뜨면 '-' 폴백 — 화면은 깨지지 않음).

**🔴 새 세션이 바로 알아야 할 대기 항목 (우선순위순)**
1. ✅ **알림톡 다채널 live 전환 = 완료(8/24 대표 GO — #405 세션)**: `notify_channel_mode` = `{c24:'live', cp:'live', join:'dry'}` 실전환(audit_logs id 3723). 전환 전 dry 문면 전수 점검 — 「상품 선택=」 잔재는 전부 v5.9.278 교정 **이전** 첫 회차(8/24 11:13) 기록이고 교정 후 수집분은 클린 실측(수집 시각으로 판정). 타이머 라벨 「🚀 실발송 가동 중」 자동 전환 실렌더 7/7(`scripts/verify-405b-livelabel.js`). **잔여 관찰**: ⓐ✅ **첫 쿠팡 실주문 실측 완료(8/25 15:49 — cp:27102519615377)**: LMS real/sent + 문면 정상(고객명·발송일 정확) + **#402 자동 발주확인 confirmed(발송 직후 자동 상품준비중 이동) 실동작 첫 검증**. ⚠️ 단 수신 컬럼이 안심번호(050)를 「번호 오류(유선)」로 오표기(#219 판정식이 01x 외 전부 유선 취급 — 발송 무영향·표기만) → 「안심번호」 중립 표기 수정 제안됨(대표 지시 대기) ⓑ✅ **자사몰 발송안내 실발송 실물 종결(8/26)** — real/sent 5건 전수 「상품 선택=」 접두 0 + 쿠팡 발송안내도 real/sent 1건(변수린 건 — E까지 완주). 4채널 알림 파이프라인 전 구간 실물 검증 완료. (참고: 자사몰 발송안내 상품명에 「1. (제철)」 번호 접두 잔재 — 무해·다듬기 후보, 지시 대기)
2. **카카오 템플릿 검수 결과 추적 (8/27~28 예상)**: MD 3장(UK_5754 일반2·UK_5755 예약2·UK_5756 발송안내3) + 가입환영2(UK_5877). **승인 시 절차 = docs/알림톡_템플릿_문안.md 최상단**(env 교체 ALIGO_TPL_CODE=UK_5754·_RESERVE=5755·_GUIDE=5756·_WELCOME=5877 + templates 동기화 + 테스트 1건). UK_5877 승인 후 `notify_channel_mode.join='live'`. **오등록 중복본 UK_5750~5753은 심사 종료 후 삭제**(DELETABLE_TPL 화이트리스트 확장 + 대표 GO).
3. 8/24 대표 확정 종결: hold-0809 잔존 = 무시 · 8/22 청귤 과결제 = 처리완료 · 주문정리기 = 필요시 수정 · 테스트 계정 3개 = 대표 삭제 진행.
4. 🎑 **추석(#403) 후속 3건 — 정본 = 바탕화면 `추석_시나리오_전달문.txt` ■운영 메모** (시나리오·시기지식은 DB 전용·코드 무접촉·서버 무배포): ⓐ✅ **종결(9/4)**: 택배사 일정 확정(대표 "그대로 진행" — 달력 무변경) → 지식 id 14 "예정·확정 전" 괄호 문구 삭제 완료(날짜·본문 무접촉·audit 기록). 이제 봇은 9/21 마감·9/27 재개를 확정으로 안내 ⓑ**9월 초 VIP 세트(10801253976) 가격 인하 시** → 봇이 세트 가격을 답하게 하려면 판매현황 탭에 세트 옵션 등록(선택 — 미등록이면 상품페이지 안내가 현행·대표 OK) ⓒ**추석 후 1~2주(10월 초) 대표 "VIP 링크 내려" 지시 오면** → 링크 3곳 = 시나리오 **#55·#56·#36**에서 제거. ※시나리오 #54~#57 OFF는 **불필요** — 날짜는 시기지식(09-26 만료·자동 소멸)에만 있음. 설 명절 = 지식 1행(「명절발송 — 설 2027」)만 추가하면 4채널 자동. 상세 방법론 = 자동메모리 jeju-holiday-scenario-playbook.

- 🎑 **#403(대표 GO 8/24 · 똑똑이 세션 DB 직접 등록·코드 무접촉): 추석 시나리오** — 신규 #54~#57([추석] 마감·단체·추천·세트구성, 전부 공통) + #19·#26·#36·#47 수정 + 시기별 지식 「명절발송 — 추석 2026」(08-24~09-26·id 14, **날짜는 여기에만** — 자동 소멸). qna_sim 10/10. 상세·운영 메모 = 바탕화면 `추석_시나리오_전달문.txt` · 자동메모리 jeju-holiday-scenario-playbook.

- 🏷️ **#404(대표 8/24): 자동수집 타이머 라벨 전면 정리 + 쿠팡 주기 통일** — v5.9.287 / app.js v=369
  - 신규 수집기 5종이 라벨 맵(`NAVER_TIMER_LABELS_UI`)에 없어 **영어 키 그대로 노출**(cafe24_notify 등) → 전 타이머 **채널 접두 통일(🛒 네이버 · 🏠 자사몰 · 🛍️ 쿠팡) + 채널별 그룹 정렬(o 필드)** + welcome_notify(은퇴)는 화면 숨김(hide).
  - 🔵 **발송 상태 표기는 하드코딩 금지 — 동적으로**: auto-collect GET이 `notify_channel_mode` 동봉 → dry면 「🧪 검수중 (문면만 기록)」·live면 「🚀 실발송 가동 중」 자동 전환(#215 낡은 라벨 사고 계열 재발 방지). 네이버 알림톡·문자는 live:true 고정(전역 KAKAO_NOTIFY on).
  - **쿠팡 주문안내 주기 10→3분**(대표 "동일하게") — 3채널 완전 동일: 주문안내 3분·발송안내 30분. DB 값만(배포 불요)·재조회 검증.
  - ✅ 실측 `scripts/verify-404-timerlabels.js` **9/9**(로컬 실서버·실렌더·JWT 우회 — 영어 키 0·채널 행수 8/3/2·그룹 정렬·검수중 4행·welcome 숨김·토글/저장 무회귀·에러 0). ⚠️ 검증 함정: 채널 판정을 본문 포함으로 하면 「(05:10 · **네이버 기준**)」 설명어에 걸린다 — **라벨 시작부(`/^🛒 네이버/`)로 판정**할 것.

- 🍊 **(8/24 대표 GO · 번호 없음 — 담당 세션이 장애로 소실돼 기록만 보충): 하우스귤(c92) 소과 특가 카페24 반영 + 가입 쿠폰 실반영 실측** (서버 무배포 · 카페24 API만)
  - 카페24 c92 기본가 → **23,800**(네이버 최종가 32,800 + min −9,000) + variants 10종 추가금을 **네이버 실시간 결제가 집합(23,800~62,800 · 8/24 12:0x 조회)** 에 맞춤 — `scripts/c92-sale-2026-08-24.js`(재조회 검산·audit_logs id 3629 「클코(대표 GO — 하우스귤 소과 특가)」 실기록 확인). **실증 = 12:12 실주문서에서 소과 2.5kg×2 = 47,600원(23,800×2) 결제가 정확**(캡처 `scripts/coupon_orderform.png`(로컬 보관·PII 포함이라 미커밋)). ⚠️ #400 교훈 그대로 — **옵션 추가금은 cafe24_sync 자동 아님**: 다음 네이버 옵션 재편 때 variants 수동 갱신 필수(검증 = `audit-400-priceset.js` 집합 대조).
  - 가입 축하 쿠폰 2,000원 = **손님 주문서 실반영 실측**(`scripts/probe-coupon-2026-08-24.js` — 테스트 계정 akmcl20xuz 실로그인 → 담기 → 주문서에 「보유쿠폰 1개」·[쿠폰 적용] 노출. **주문 미완료 — 결제 0건**) → #401 가입환영 혜택 문구(가입 쿠폰 2,000원)와 실물 1:1 정합 근거.

- 👋🔴🔴 **#401-d(8/24 완결): 가입 환영(D) = 「이벤트형」으로 전환·E2E 완주 — 폴링은 구조적으로 불가였다** — v5.9.282~286 / 스킨 member/join.html·join_result.html
  - **카페24 재동의 완료(대표 로그인 + 클코 브라우저 직접)**: 개발자센터 앱 권한에 **회원 읽기(read_customer) + 프로모션 읽기·쓰기** 추가 → [데이터관리] 재동의 → **스코프 12종 실확인**. 🔴 **문서의 `mall.read_privacy`는 개발자센터 선택지에 존재하지 않음 실측**(회원=read_customer·「개인화정보」는 좋아요/장바구니라 다른 것). write_promotion은 #341 룰렛 쿠폰 자동발급 대비(코드 미사용 — 재동의 1회에 묶어 절약).
  - 🔴 **폴링(신규 가입 목록 조회)은 불가 실측 3중**: ⓐcustomers API는 cellphone 또는 member_id **지정 필수**(가입일 기간 목록 미지원) ⓑmember_id로 조회해도 **cellphone 미제공** ⓒ**cellphone 역검색은 성립**(하이픈 포맷 필수·같은 번호 복수 계정 반환). → **이벤트형**: 가입완료 페이지(join_result)가 `POST /api/public/welcome-signup` 신호 → 서버가 **카페24 정본 역검증**(그 번호의 실제 주인 목록에 그 회원 존재 + 가입 48h 이내)한 것만 D 발송. 위조 원천 차단·레이트리밋 6/분·join:{mid} 멱등·dry 게이트·마스킹 저장. `welcome_notify` 폴링 타이머는 은퇴(OFF).
  - 🔴 **join_result엔 휴대폰이 없다(치환자 4종 전부 미치환 실측)** + **이름 dd는 `*****` 마스킹 렌더** → **join.html 제출 클릭 시 손님이 직접 입력한 아이디·휴대폰·이름을 sessionStorage 도장**(akm_wjoin_v1·10분 TTL·1회 소비) → join_result 신호가 폴백으로 사용. 도장 위변조는 서버 역검증이 그대로 거른다. + 서버 최종 가드: 이름에 `*`·태그 문자 → '고객' 폴백(**dry 기록에서 「*****님」 문면을 실측 포착** — 실발송이었으면 그대로 나갈 뻔).
  - ✅ **E2E 6/6**(`scripts/verify-401d-signup.js` — 테스트 계정 실가입): 약관→정보입력→가입 완주→신호 POST(mid·tel 정확)→**역검증 200**→DB join: dry 행(문면·order_at=가입시각·마스킹 번호). 🚨 **가입 폼 함정 3종(재사용 주의)**: ⓐ제출 버튼을 `/회원가입/` 텍스트로 찾으면 **「사업자 회원가입」 유형 전환 링크가 먼저 잡혀** member_type이 바뀜(「법인명을 입력하세요」 alert의 정체) ⓑ**중복확인 버튼은 전부 숨김 — blur 시 자동 검사**(Playwright fill()은 blur를 안 일으킴 → blur 디스패치 필수) ⓒ**example.com 이메일은 카페24가 거부**(naver.com 등 실도메인만). + 검증 alert는 `pg.on('dialog')`로 찍어야 원인이 보인다(#386-e 교훈 그대로).
  - ✅ 테스트 계정 = **실생성 3개**(akmcl8y6bns·akmclbrym4m·akmcly9pvy1 — 실패 시도 2회는 계정 미생성. "4개" 1차 보고는 오류) — **대표가 8/24 관리자에서 삭제 진행**. join 가동(live)은 **UK_5877 승인 후** `notify_channel_mode.join='live'` 권장(승인 전엔 UJ_9086으로도 가능).
  - 📌 8/24 대표 확정: hold-0809 잔존 7건 = 무시(정리 불요) · 8/22 청귤 과결제 1건 = 처리 완료 · 주문 정리기 = 필요시 수정.

- 📨🔴 **#401(대표 GO 8/24): 알림톡 다채널 확장 — 쿠팡·자사몰 주문/발송 안내 + 가입 환영. 현재 dry 가동 중(실발송 0) — live 전환 = 대표 GO 대기** — v5.9.277~278 / 스펙 `docs/superpowers/specs/2026-08-24-multichannel-alimtalk-design.md`
  - **대표 확정**: 쿠팡=(a) 주문+발송 둘 다 / 카페24 자체 SMS 꺼져 있음(중복 없음) / 가입 환영(D)도 가동+혜택 문구 추가 / **모든 규칙 네이버 동일**(8시 보류·발송휴무 달력·예약 판정) / 자사몰 = 발주확인 없음(결제 즉시 N20 — 상태 변경 0).
  - **실측 확정 사실**: ⓐ쿠팡 연락처 = **안심번호(0505)** → 알림톡 불가 = **LMS 직행**(050은 쿠팡 경로 한정 허용 — isBadTel이 050 차단하므로 coupangBadTel 별도) ⓑ카페24 buyer.cellphone = 실번호(알림톡 정상) ⓒ**mall.read_privacy 스코프 없음 → 가입 환영(D)은 대표 재동의 1회 필요**(수집기는 403이면 "권한 대기" 무해) ⓓ기존 matchNotifyProduct(exact includes)는 자사몰 「·」·쿠팡 형식 미매칭 → **matchNotifyProductLoose**(토큰 전부 포함 시에만 — 오매칭>미매칭·네이버 경로는 종전 함수 무접촉).
  - **구조**: 수집기 5종(cafe24_notify 3분·cafe24_guide 30분·coupang_notify 10분·coupang_guide 30분·welcome_notify 30분 — 현재 전부 ON) + **채널 실발송 게이트 `notify_channel_mode`**(c24/cp/join — **현재 전부 dry**): 전역 KAKAO_NOTIFY가 이미 ON이라 새 채널은 이 게이트를 통과해야 실발송. 이력 = 기존 로그에 order_key 프리픽스(c24:/cp:/join:) + 화면 채널 배지·쿠팡 sent는 「문자(LMS)」 표기. 보류 수기 발송([오늘/내일])도 채널 분기(카페24=order_id 재조회·쿠팡=기간 조회 필터).
  - **dry 첫 회차 실가동 확인(8/24 11:13~16)**: 수집기 7종 전부 ok · 자사몰 주문안내 2건·발송안내 9건·쿠팡 발송안내 1건 **문면만 기록** · 네이버 실발송 무회귀(real/sent 계속) · **지난주 발송 백로그가 dry 이력으로 소진돼 live 전환 시 유출 0**. 문면 검수로 잡은 것 2건(278에서 교정): 「상품 선택=」 접두어 제거(c24OptClean)·발송안내 48h stale 가드.
  - **가입환영2(혜택 3종 문구+MD 문의하기) = UK_5877 검수 신청 완료**(8/24 — 승인 예상 8/27~28). 혜택 문구는 #346-c 실확인분 1:1(가입 쿠폰 2,000원·물방울 5·생일 쿠폰). 승인 전 D 가동 시엔 기존 UJ_9086.
  - ✅ 검증: `scripts/verify-401-unit.js` **20/20**(실DB 품목 loose 매칭·오매칭 방지·050 가드·hold-0809(토요일 제외 #353)·문면 조립·실삽입 검산).
  - 🔴 **다음 세션·전환 절차**: ①dry 문면 며칠 검수(신규 주문에서 「상품 선택=」 소멸 확인) ②**대표 GO 시 live 전환** = `notify_channel_mode`를 {c24:'live',cp:'live'}로(가입 join은 UK_5877 승인 후 'live') ③~~welcome 재동의~~ → **완료·이벤트형 전환 완결(#401-d 절 참조 — 재동의·역검증·E2E 6/6)** ④UK_5877 승인 시 env ALIGO_TPL_CODE_WELCOME 교체+templates 동기화. ⚠️ 쿠팡 주문안내는 아직 실주문 0건이라 문면 미검증 — 첫 쿠팡 주문 dry 문면 확인 후 live 권장.
- 🛍️🔴 **#402(대표 GO 8/24): 쿠팡도 「LMS 발송 성공 → 자동 발주확인」 — 네이버 #92 동일** — v5.9.281 / 릴레이 2026-08-24.1
  - 공식 스펙 문서 확인: **PATCH** `/v4/.../ordersheets/acknowledgement` · body {vendorId, shipmentBoxIds[]} · 50개/회 · **결제완료(ACCEPT) 박스만**·부분취소 진행 건 포함 시 오류. 같은 주문 멀티박스 전부 확인·INSTRUCT에서 감지된 주문은 'already'·발송 실패 = manual-needed(수기)·dry = 'dry-run'.
  - 릴레이 변경: COUPANG_ALLOW에 PATCH 1종 + `/coupang` **body 전달 지원(vendorId는 .env 강제 주입)**. ✅ **대표 SSH 재실행 완료(8/24)** — `relay_health_request` 러너로 **2026-08-24.1 반영·coupang_keys_set 실확인**. 남은 실동작 검증 = cp live 전환 후 첫 쿠팡 주문에서 발주확인 자동 전환 확인.
  - 검증 `scripts/verify-402-unit.js` **12/12**(실코드 블록 추출 — 성공/부분실패/응답누락=실패/예외/50청크 + 배선·릴레이 grep 6종). 실동작 검증은 릴레이 갱신 + cp live 전환 후 첫 쿠팡 주문에서.
  - 🖥️ **#401-c(대표 실물 8/24 — 이력 화면 3건, v5.9.279 / app.js v=368)**: ⓐ**새 채널 dry 행이 「❌ 실패」로 오표기**(DRY 판정표에 'dry-run' 키 없음 — 대표가 "언제 나간 거야" 놀란 원인·실발송 0이었음) → **「🧪 검수중 (미발송 — 문면만 기록)」** ⓑ**일시(주문) = 실주문 시각** — `order_at` 컬럼 신설(additive)·새 채널 수집기 try/**finally**로 전 경로(skip·hold·dry 포함) 기록·표시 = COALESCE(order_at, created_at)·기존 자사몰 백로그는 주문번호 날짜로 백필 ⓒ**새 채널 풀번호 표시** — 자사몰 = order_id 일괄 재조회(마스킹 저장 원칙 유지)+수집 캐시 시딩 / **쿠팡 한계: 단건 재조회 API 없음 → 수집 캐시로만 표시(재시작 후 옛 행 마스킹 폴백 — 안심번호라 실번호도 아님)**. 검증 `verify-401c-history.js` **8/8**(실클릭 — 검수중 표기·실패 0·주문일 표시·네이버 무회귀). **#401-d(대표 "12시로 나온다")**: 날짜만 백필해 자정으로 보이던 12건 → **카페24 재조회로 실주문 시각 재백필**(자정/NULL 잔존 0 검산). 쿠팡 기존 1건만 수집시각 유지(단건 재조회 API 없음 — 신규부터는 자동 기록). 쿠팡 발송 표기 = 「✅ 문자(LMS)」 구분 확답(이미 반영). **#401-e(대표 "네이버도?"·v5.9.280)**: 네이버 주문안내·발송안내 수집기에도 order_at(결제 시각) 기록 추가(try/finally — 발송 로직 무접촉) — 배포 후 첫 주문 2건에서 **결제 시각 정확 기록 + real/sent 무회귀 실측**. 기존 행은 백필 없이 종전 표시(≈결제+1~3분).
  - 🚨 **로컬 검증 함정 2건(재발 방지)**: ⓐ**PS5.1 `Get-Process`는 CommandLine 미노출** → `Where CommandLine -like` 종료가 조용히 실패해 **옛 코드 서버가 3457을 계속 점유** → 새 서버는 EADDRINUSE로 죽고 검증이 옛 서버를 때려 거짓 실패(백필 정상인데 화면만 옛날). **로컬 서버 종료는 `Get-NetTCPConnection -LocalPort`로 PID를 잡아서** ⓑ 로컬(KST 프로세스)에서 naive timestamp는 9시간 어긋나 보임 — Render(UTC)가 정본(기존 함정 재확인).

- 📄 **#399(대표 GO 8/24): 자사몰·카페24 상세페이지 20종 최신화** — 스킨 index.html + 카페24 description (서버 코드 무변경)
  - **배경(대표 질문에서 확정한 사실)**: [상세 다시 불러오기] 버튼 = 네이버 상세를 **회사프로그램 저장고(product_detail_snapshot)에 저장까지만**. 자사몰 상세정보 탭은 **index.html의 `STORE_DATA.detailImages`(7/30 정적)**를 읽어 자동 반영 안 됨 — 청귤에 "사전예약 시작!!" 옛 상세 잔존이 그 증거. 🔴 **운영 절차: 네이버 상세 수정 → ①[상세 다시 불러오기] 클릭 → ②클코에게 "자사몰 상세 반영해줘"** (2단계 수동).
  - **시공 ①스킨**: `STORE_DATA.detailImages` 20키를 8/24 10:09 스냅샷으로 교체(값 형태 count·imgs·blocks 유지·**opts/가격/이름 무접촉**). JSON 통파싱→교체→재직렬화 + 검산 6중(밖 영역 바이트 보존·재파싱·`</`+`script` 시퀀스 검사·키 대조). 백업 = `index_pre399_backup.html`. 내용 실변경 15종/동일 5종(황금향 43블록 내용 변경 확인 — 블록 수 같아도 내용 대조로 판정).
  - **시공 ②카페24**: bulk-detail 러너(매핑 19종 — `c24-map-new-246.json`) **19/19 성공**.
  - ✅ 실측 `scripts/verify-399-details.js` **8/8**: 청귤 상세 「사전예약」 잔존 0·실렌더(6913자·33img) · 황금향·감귤 실렌더 · 홈 노출 텍스트 잔존 0 · 카드·옵션·구매하기 무회귀 · 에러 0. 서버=로컬 바이트 재대조.
  - ⚠️ **검증 판정 함정 3건(재사용 주의)**: ⓐ상세의 「사전예약」 검사에 **리뷰 카드(.pd-rev-card)의 과거 구매 옵션 실이력(#392 의도 보존)이 21회 걸림** — 리뷰 영역 제외로 판정 ⓑTreeWalker가 **SCRIPT 태그 안 코드 주석**까지 셈 — SCRIPT/STYLE 제외 ⓒ상세 옵션 행 클래스 = **`.opt-row`**(pk-row 아님).
- 💰🔴 **#400(#399 무회귀 재검이 잡아낸 별개 결함): 자사몰 청귤 실결제가 +1,000원 과결제 — 즉시 교정** (서버 무변경·카페24 variant 2건 PUT)
  - **실체**: 화면 옵션가는 전수 정확(19,800/31,800)한데 **실담기 = 20,800**. 원인 = 8/21 시점엔 카페24 「기본가 18,800 + 추가금 1,000 = 19,800」로 **우연히 정합** → 8/22 05:10 cafe24_sync(fix)가 기본가를 새 기준 19,800으로 올리자 **자동 아님인 옵션 추가금(+1,000/+13,000)이 낡은 채 남아** +1,000 초과. **#393에 적힌 "옵션별 가격은 자동 아님" 한계가 실사고로 실현된 것.**
  - 교정: variants PUT — 5kg P00000DP000D +1,000→**0** · 10kg P00000DP000E +13,000→**12,000**. `verify-393` 재검 **10/10**(실담기 19,800 도달).
  - **판매중 5종 전수 감사**(`scripts/audit-400-priceset.js` — 🔴 **옵션 이름 매칭 금지·가격 「집합」 정렬 대조**로: 이름 형식이 카페24 「·」 vs 네이버 「/ 상품 및 과수:」라 달라 1차 이름 매칭이 거짓 ❌ 16건을 냈다): **5종 전 옵션 카페24 결제가 집합 = 네이버 정본 집합 완전 일치**. 청귤만 실결함이었음.
  - ⚠️ **실피해 1건 — 대표 판단 필요**: **8/22 09:18 자사몰 주문 청귤 10kg 1건 = 32,800원 결제**(정상 31,800 — 1,000원 과결제·배송중). 부분환불 여부는 대표 판단(카페24 관리자 [주문]에서 8/22 09:18 청귤 10kg으로 특정 가능).
  - 🔵 **교훈(#393 완결편)**: cafe24_sync는 **기본가만** 자동이다. **네이버가 옵션 추가금을 재편하면 카페24 variant 추가금을 반드시 손으로 갱신**해야 하며, 안 하면 sync가 기본가를 맞추는 그 날 결제가가 틀어진다. 갱신 후 검증은 `audit-400-priceset.js`(집합 대조) + `verify-393`(실담기)로.

- 📨🔴 **#398(대표 GO 8/23): 카카오채널 문의 고객 특정 — 알림톡 MD(메시지전달) 버튼 + 채널 인사말** — v5.9.276
  - **배경**: 알림톡 전환 후 손님이 카카오 채널로 답장하면 **닉네임만 보여** 매번 성함·연락처를 물어야 했다(문자 시절엔 답장에 번호가 자동 동반). 카카오 정책상 채널 상담에 연락처·실명 노출은 **불가**(상담톡 유료도 동일) — 우회 = 답장에 신원이 딸려 오게 만든다.
  - **① MD 버튼판 템플릿 3장 등록·검수 신청 완료(8/23)**: 손님이 알림톡의 [문의하기](MD) 버튼을 누르면 **알림톡 원문(고객명·상품명 포함)이 상담 채팅에 첨부**되어 시작 → 직원이 묻지 않고 즉시 특정. 승인 문안 그대로 + MD 버튼만 추가. **주문완료_일반2=UK_5754 · 예약2=UK_5755 · 발송안내3=UK_5756** (전부 REQ — 결과 4~5일). `templates_md` 세트 + 러너 `set:'md'` 분기. **승인 후 절차·롤백 = docs/알림톡_템플릿_문안.md 최상단** (env 3종 교체 + templates 버튼 동기화 + 테스트 1건).
  - 🚨 **실사고: 배포 롤링 중 구인스턴스가 플래그 선점** — v5.9.276 배포 직후(신버전 응답 확인 후!) 러너를 돌렸는데 **드레인 중이던 구인스턴스(275)의 60초 폴러가 소비**해 set='md'가 구코드 기본 세트로 낙하 → **기존 텍스트형 4장이 같은 이름으로 중복 등록·검수 신청됨(UK_5750~5753)**. 발송 무영향(env 코드만 참조). **심사 끝나면 삭제할 것**(화이트리스트 확장+대표 GO). 🔵 **교훈: "버전 엔드포인트 = 신버전"은 HTTP 라우팅만 증명한다 — DB 폴러는 구인스턴스에서도 돈다. 배포 후 DB 플래그 러너는 최소 5~10분 뒤에** (기존 "플래그 유실" 함정의 악화판 — 유실이 아니라 **구코드로 실행**될 수 있다).
  - ✅ **② 채널 첫인사 메시지 설정 완료(8/24 — 대표 로그인 후 클코 브라우저 직접·문구 2차 확정)**: 카카오비즈니스 파트너센터 > 채팅 > **채팅 설정 > 첫인사 메시지**. 최종 = **「안녕하세요 제주아꼼이네입니다. 문의하실 내용 편하게 남겨주세요!\n(주문 후 고객님 경우에는 주문자 성함과 연락처를 남겨주시면 빠르게 확인 후 정확한 답변 도와드릴게요!)」**(대표 원문 — 닫는 괄호만 보충·94/200자). 🔴 **종전의 제철상품·발송 안내 줄은 대표 지시로 삭제** — 이 인사말은 이제 시즌 무관이라 **시즌 전환 때 갱신 불필요**(자동메모리 체크리스트 ⑤는 "현 문구엔 해당 없음"으로 읽을 것). 새로고침 후 유지 실확인 = 실저장. ℹ️ 채팅방을 "처음" 여는 손님에게 자동 1회 노출.
  - ℹ️ 채널에 잠자는 자산(참고): [자동응답 메시지] 6개(귤 사이즈·배송비·단체주문·도착·발송·후숙보관 — 전부 「연결되지 않음」) — 채팅방 메뉴 버튼에 연결하면 "누르면 뜨는 안내"가 됨. 대표에게 제안했고 **미채택(보류)** — 지시 없이 착수 금지.
  - ℹ️ 대표 확인 완료: [내 채팅]에 알림톡 원문 **안 보임**(실물 확인) — MD 버튼이 정답인 근거.
- ✅ **8/21 인계 항목 점검(8/23)**: ⓐ**cafe24_sync 첫 정규 회차 정상** — 8/23 05:12 KST·mode=fix·매핑 19종 전수 대조 **차이 0·차단 0·오류 0**(청귤 새 기준 포함. ⚠️last_run_at을 pg로 읽어 +9h 하면 또 시간대 함정 — `cafe24_sync_last.at`(ISO Z)이 정본) ⓑ**hold-0809 잔존 = 1건이 아니라 7건**(8/18×4·8/19×2·8/21×1 — 밤호박 6·황금향 1). 발송안내(E)는 별도 자동이라 이미 나갔을 것 → **주문안내를 지금 보내면 낡은 정보. [알림 발송 이력]에서 체크 삭제 정리 권장**(발송·삭제 판단 = 대표).

- 🗓️ **#397(대표 8/23 "정산현황 금액 입력 후 새로고침해야 달력에 반영"): 달력 실시간 갱신** — v5.9.275 / app.js v=366
  - 원인 = 입력 핸들러 `ssInp`가 데이터(ssAll)·하단 요약은 즉시 갱신하면서 **`ssRenderCalendar()` 호출만 빠져** 달력은 날짜 선택/추가/삭제/새로고침 때만 재렌더. 교정 = ssInp에 재렌더 1줄(달력은 #ss-cal-wrap 순수 innerHTML 재생성·입력 폼과 별개 컨테이너라 포커스·리스너 부작용 0 — 확인 후 시공).
  - ✅ 실측 `scripts/verify-397-live-cal.js` **10/10**(로컬 실서버 3457·실클릭·실타이핑 · 🔴**쓰기(POST)는 route로 가로채 실DB 무변경**): 입력 즉시 달력 금액 변경(4,813만→12,907만) · 키 입력마다 갱신 · 리로드 0회 · **입력 포커스 유지** · 전일대비 동시 갱신 · 요약 합계·월 이동 무회귀 · 에러 0. 배포 후 실번들(v=366)에 삽입 확인.
  - 🚨 **자책(문서화된 함정 4번째 재범)**: index.html 캐시 버전 한 줄을 PowerShell `Get-Content -Raw | Set-Content`로 바꿨다가 **한글 전멸**(BOM 없는 UTF-8을 CP949로 오독) — git diff 915줄 변경으로 즉시 포착·checkout 복구 후 **Node 한 줄 스크립트로 재수정**(1줄 diff 확인). **어떤 한 줄 수정이라도 파일 쓰기는 Write/Edit/Node로** — PowerShell 텍스트 파이프라인은 예외 없이 금지.

- 📷🔴🔴 **#396(대표 실물 8/23 "에이전트오피스 정산관리 매번 하던 게 안 된다"): 마루 정산 이미지 판독 실패 다발 — 원인 = 도구 JSON 태그 오염(#9716 고질의 OCR 경로판)** — v5.9.272→273→274
  - **실태(DB 전수)**: "이미지에서 품목을 하나도 읽지 못했습니다" 오류가 **8/10까지 0건 → 8/11부터 간헐(재시도로 성공) → 8/23 아침 6/7건 실패**. 실패 이미지 전수 육안 확인 = 전부 정상 판독 가능한 표. 같은 목록의 재캡처 두 장이 성공/실패로 갈림 = 호출마다 확률적. 코드는 7/20 이후 무변경 — **모델 쪽 변화(claude-sonnet-5는 별칭이라 서버측 스냅샷 교체) 시기와 일치**.
  - 🔴 **원인 확정은 진단 러너가 했다**(추측 2개를 거쳐): ⓐ1차 가설 "max_tokens 1500 잘림"(#373 계열)은 **기각** — 재현 결과 `stop_reason='tool_use'`(잘림 아님) ⓑ실체 = **`partner="</antml…parameter>"` 태그 오염 실측**. 오염이 심한 호출은 items가 통째로 사라져(raw_items=0) "품목 0건"으로 위장. 서버 9716행에 이미 기록된 마루 고질인데 **텍스트 경로만 평문 폴백을 넣고 OCR 경로는 무방비**였던 것.
  - 🚨 **더 위험한 변종을 검증에서 실측(273 full 검증)**: partner만 오염 마커가 남고 **items는 마커 없이 "깨진 품목 1건('과 즙팡팡' 1박스)"으로 줄어든 채** items-한정 오염 검사를 통과할 뻔(실제 12품목·479박스). **부분 오염 = 부분 품목 = 정산(돈) 오입력 직전**이었다 → 오염 판정을 **응답 전체**로 확대(274).
  - **교정(최종 구조)**: 도구 경로(종전) → **잘림·품목 0건·응답 어디든 오염이면 평문 JSON 폴백**(`settleOcrPlainRead` — maruPlainAnswer와 같은 원리·오염 원천 불가·토큰도 더 쌈 in 1145→360) → 폴백도 실패하면 **의심 데이터 채택 금지**(부분 품목으로 진행 안 함·오류 반환). max_tokens 1500→4000·stop_reason 가드는 무해라 유지.
  - **진단 러너 신설(재사용)**: `settle_ocr_probe_request {ids:[pending_orders id], max_tokens?, mode:'tool'|'plain'|'full'}` → `settle_ocr_probe_result` — 보관된 실패 이미지로 판독만 재실행(확인표·정산 쓰기 0·PII 0). 실행기 = 스크래치 `run-396-probe.js`(새 세션은 재작성).
  - ✅ **실측**: 종전 재현(tool·1500) = 효돈 이미지 4/4 실패 재현 · 평문 단독 **3/3 정확**(효돈 7품목/55박스·대성 12품목/479박스 — **수량 합계가 이미지 합계와 정확 일치**) · **본선(full) 최종 3/3** — 오염 도구 결과 전부 거부 → 평문 채택 → 479·55·55 정확.
  - 🔵 **교훈**: ⓐ**"품목을 못 읽었다"는 오류 문구를 믿지 말 것** — stop_reason·원시 응답을 찍는 진단 러너가 가설 2개를 하루에 갈아치웠다(max_tokens 가설로 끝냈으면 재발했다) ⓑ**태그 오염은 부분적으로도 온다** — 오염 마커가 응답 일부에만 있어도 나머지 필드(items)가 소리 없이 줄어 있을 수 있다. 오염 검사는 응답 전체로, 오염 시 남은 데이터도 불신 ⓒ**tool_use를 쓰는 다른 경로도 같은 고질 후보** — 마루 배정(폴백 있음)·정산 OCR(이번에 폴백) 외에 새 tool_use 경로를 만들면 오염 대비를 처음부터 넣을 것 ⓓ배포 직후 러너 실행은 롤링에 플래그가 유실될 수 있다(재실행으로 해소 — 기존 함정 재확인).
  - ⚠️ **대표 확인 항목**: 다음 정산관리 이미지 업로드가 한 번에 확인표까지 가는지(8/23 아침 실패분은 성공한 대성 1건 외 재업로드 필요 — 효돈 55박스 건은 정산 미입력 상태).

### 🎑 #403 마무리 보고 전문 (8/24 — 똑똑이 세션이 장애로 튕기기 직전 대표에게 보고. 유실 방지 원문 보존)

**현재 상태**: 테스트 계정 3개 = 대표가 삭제(체크 후 [탈퇴/삭제]) · 2·3·4번 = 대표 확정대로 종결·인수인계 기록 완료. **남은 것 딱 2개**: ①대표 "고" → 자사몰·쿠팡 알림톡 실발송(live) 전환 ②카카오 검수 승인(8/27~28 예상) → 승인 시 가입 환영 가동 + MD 버튼판 교체를 똑똑이가 이어서 진행.

**대표 할 일 (시점순)**
- 지금(선택): 실톡톡으로 "추석 전에 받으려면 언제까지 주문?" 1회 던져 새 문구 실물 확인 (봇 캐시 5분 · 안 해도 무방 — 재현 10/10 통과)
- 택배사 확정 연락 오면: ①날짜 바뀌면 발송휴무일 달력 수정 ②바뀌든 안 바뀌든 똑똑이에게 "확정됐다" 한마디 → 시기별 지식의 "예정·확정 전" 문구 삭제. 🔴 안 하면 손님이 계속 "예정"으로 들음.
- 9월 초 VIP 세트 가격 인하 때: 네이버 가격 변경 후, 봇이 세트 가격을 답하게 하려면 판매현황 탭에 세트 옵션 2줄(황금향/하우스감귤 · 42,800→새 가격) 등록. 안 하면 현행대로 "상품 페이지 확인" 안내(무응답 아님·무해). 선택.
- 추석 후 1~2주(10월 초): "VIP 링크 내려라" 한마디 → 똑똑이가 #55·#56·#36 세 곳 링크 제거. 시나리오 OFF 불필요(날짜가 시기지식에만 있어 9/27 자동 소멸).

**주의사항 5**
1. 🔴 **9/21(월) 08:00이 진짜 마감** — 이후 주문은 9/27 발송으로 안내(표 자동). 9/21 오전 대량 몰림 = 당일 출고 부담. 명단 큰 단체 건은 9/18~19까지 확정 유도(봇은 "확정 있게"까지만 말하고 날짜를 못 박지 않음 — 단체 전화 응대 때 직접).
2. 단체 할인·견적은 봇이 절대 숫자를 말하지 않음 — 전부 고객센터로 옴. 추석 2주 전부터 010-6687-4031 전화·문자·메일(bumiletter@naver.com) 응대량 증가 대비. 메일 명단은 "주문자 ○○○" 제목으로 오도록 안내돼 있으니 그 이름으로 스토어 주문 검색.
3. 자사몰 손님에게도 네이버 VIP 링크가 나감(대표 OK 사항). 자사몰 세트(c100) 가격이 네이버보다 비싸면 자사몰 이탈 우려 — 9월 초 인하 때 cafe24_sync 텔레그램 알림만 확인.
4. 판매중 품목이 바뀌면 추천도 바뀜(시즌 종료 품목 = 봇이 추천에서 자동 제외). 단, 시기별 지식의 "주력 = 황금향·하우스감귤"은 9/26까지 고정 — 그 전에 감귤이 끝나면 똑똑이에게 알려 지식 문구 수정.
5. 🔴 달력·시기지식은 세트 — 한쪽만 고치면 표와 문구가 어긋남. 날짜 관련 변경은 항상 똑똑이에게 한 줄로(두 곳 동시 수정).

**이미 처리됨(대표 몫 아님)**: 설 명절 = 시기별 지식에 「명절발송 — 설 2027」 1행 추가로 4채널 자동(그때 날짜만 제공) · 기록 = 바탕화면 추석_시나리오_전달문.txt(운영 메모 5개) · CLAUDE.md #403 · 자동메모리. 코딩 클로드는 이번 건 손댈 것 없음(DB 전용). 다음 지시 번호 = #404.

## ★★★★ 8/21 세션 마감 인수인계 (#392~#395 전건 종결) 


> 📦 **#392~#395 상세 전문은 `CLAUDE_ARCHIVE.md`** (여기엔 요약·대기 항목·주의만 유지)

**현행 버전: 회사 v5.9.271 / app.js v=365** (Render 반영 실측 완료). 8/21 처리 = **#392**(청귤 사전예약 완전 종료 — 전 채널 일반 전환) · **#393**(자사몰 옵션가 1,000원 오표시 — mergeSnapshot opts 병합) · **#394**(정산관리 간헐 빈 화면 — 클래스 재사용 바인딩 오염) · **#395**(주문 정리기 새 메뉴 이식). 상세는 아래 각 항목.

**🔴 대기·확인 항목 (다음 세션 인계)**
1. **#395 대표 실물 확인**: ①실직원 계정으로 [주문 정리기] 진입 → 예시 정리 → 주소 검증(실키 첫 실사용) ②실주문 엑셀 1회 뽑아 기존 수기본과 대조. 문제 보고 오면 `scripts/verify-395-*.js` 4종으로 재현부터.
2. **⏸ 8/21 08:37 청귤 주문 1건 hold-0809 잔존** — 대표가 [알림 발송 이력]에서 [오늘/내일 발송으로 안내] 수기 처리 필요(2회 보고됨).
3. **내일(8/22) 05:10 cafe24_sync**가 청귤 새 기준(minAdd 0)으로 카페24 기본가 자동 대조 — 문제 시 텔레그램 알림 오는지만 확인(실결제 19,800은 이미 실증).
4. **juso 승인키 = Render env `JUSO_API_KEY`** 등록 완료(대표 직접) — 실API 러너 9/9 검증 끝. 재검증 필요 시 `node scripts/verify-395-juso-live.js`(DB 플래그 러너 — 실서버가 실행).

**🛠️ 다음 세션 주의(이 세션에서 실측)**
- `npm install` 실행하면 **--no-save 3종이 지워진다** → `npm install --no-save playwright ssh2-sftp-client xlsx-js-style` **한 명령으로** 재설치(이번에도 밟았음).
- 주문정리기 소스 정본 = 리포 `주문정리기/`(README_인수인계.md 필독 — **완성본 11.5MB HTML은 .gitignore 제외**). 로컬 검증 = `JWT_SECRET=verifytest PORT=3457` 실서버 + verify-395-parser(단위 18)·-ui(회귀 40)·-ref(레퍼런스 대조+OCR)·-juso-live(실API 9).
- **confirm 있는 버튼을 Playwright `evaluate` 안 `.click()`으로 누르면 confirm이 자동 거부돼 조용히 실패**한다 — 전역 `pg.on('dialog', accept)` + 실클릭으로(이번 거짓 실패 10건의 원인).


## 🧭 상시 참조 (과거 세션에서 승격 — 발생 세션의 상세 이력은 CLAUDE_ARCHIVE.md)

### 🏗️ 카페24 skin6 작업 체계 (홈 합격 상태 — 절대 회귀 금지)
- **미리보기 = https://akkome.cafe24.com/skin-skin6** (skin_no=11·skin_code=skin6 — URL은 skin_code 기준. 캐시 회피는 ?v=N 증가). 실서비스 아모르2 무접촉 원칙(현재까지 오염 0 — 매 시공 확인).
- **파이프라인**: 정본 = akkome-website 리포 **v5-preview 브랜치**(로컬 플랫폼본은 구판 — 사용 금지) → `transform-v5.js` 변환(치환 카운트 검증) → SFTP 업로드. **핵심 스크립트·최종 작업본 전부 `_참고자료\카페24스킨백업\scripts\` 보존**(스크래치 소실 대비 — transform-v5/calendar·upload/backup/tree·poll-light/respond/append-report/get-instruction·스냅샷·skin6-work-final 30개).
- **SFTP**: akkome.ftp.cafe24.com:3822(SFTP)·계정 akkome·비번 .env(CAFE24_FTP_*). 업로드 가드 = /sde_design/skin6/ 한정.
- **변환 내용(transform-v5.js — 홈)**: 게임 링크→준비중 모달(data-modal)·캘린더→실페이지·물방울 "곧 열려요"(실수치 미연동)·에셋 assets2/(CDN 캐시 키)·no-referrer·**실드 스크립트**(앱 오버레이 `[class^="app-smart-popup"]`+캠페인+TALK **상시 display:none** — 모바일 전용 딤이 터치 흡수하던 실폰 결함의 근본, #230)·data-modal 위임 핸들러·장바구니 뱃지 실연동(치환코드 모듈 hidden 패턴 — #232 실증).
- **검증 표준(무회귀 4종 — #230 확립)**: 12항목 스모크 + 아이콘 정본 diff 0 + **터치 시퀀스**(touchscreen.tap — 좌표 클릭은 거짓 합격 이력) + 실서비스 무변경. 스크립트: verify-*·final-smoke·touch-test(scripts 폴더).
- 교훈: PowerShell `>`/Set-Content는 바이너리·한글 오염(git archive/tar·Write 도구 사용) · 검증은 데스크톱+모바일 양 조건 · CDN 캐시는 ?v= 우회 · "HTTP 200"이 아니라 naturalWidth 렌더 실측.

### 📌 주요 라이브 상태
- **카카오 스킬 서버**: 톡톡봇 `/kakao-skill` 라이브(무언 대기 — 톡톡 완전 동일 라임·#228 확정). 봇 배포·블록 생성 = 대표 확정 대기. 스킬 URL = `https://jeju-akkomene-talktalk-bot.onrender.com/kakao-skill`.
- **발사 관찰(#214)**: 기준선 10:09(switch-off 40·hold 7) → 저녁까지 전건 sent·실패 0·보류 0 소진. 스냅샷 = scripts\snapshot2-214.js. 저녁 보고에 알리고 잔여(aligo-remain.js request→result)·60분 텔레그램 실물(대표 확인 항목) 포함.
- **소급(#216·#217)**: 14/14 sent·발주확인 자동(러너 kakao_backfill_request — 1회성 종료). 라벨 「소급 발송」(#221·backfill 플래그).
- **리뷰 스냅샷**: 수집기 429 5일 연속 → v5.9.203 교정(백오프 15/30/60×부분 성공) — **내일 04:30 수집분으로 판정**(naver_product_snapshot.reviews not null 여부·note).
- 지시함 스크립트: scripts\poll-light.js 등 — DB 접속 문자열 내장(.env DATABASE_URL 동일). 폴링 3분 체제(#197) 유지.


### 🛠️ 도구·환경 메모 (새 세션용)
- **Playwright**: `node_modules`에 `--no-save` 설치됨(⚠️ **`npm install` 실행 시 날아갈 수 있음** — 그때 `npm install --no-save playwright` 재실행, Chromium 바이너리는 `%LOCALAPPDATA%\ms-playwright`에 잔존). **실클릭 요령**: 로그인 우회 = localStorage `jwt_token`(+`jwt_user` JSON) — 'token' 아님 / 「알림 발송 이력」은 **서브탭이 아니라 [🏷️ 판매현황·가격] 탭 안의 섹션** / 로컬 서버는 `JWT_SECRET=아무값` + `global.setInterval=()=>({unref(){},ref(){}})`(스케줄러 차단 — 실DB 오염 방지) + PORT 지정.
- **지시함 스크립트**: 세션 스크래치는 세션별로 바뀜 — 새 세션은 poll-light.js(cc_instructions에서 '대기·진행중' 전건+본문, 첫 줄 verdict)·respond.js(UPDATE response/status='완료'/responded_at) 재작성. 컬럼: id, content, status, source, response, created_at, responded_at.
- **로컬 한계**: 네이버 릴레이 env 없음 → 재조회 성공 경로 로컬 검증 불가(폴백 경로만 가능). `/tmp`는 git bash에서 `C:\tmp`가 아닌 경우 있음 — 스크래치 디렉토리 사용.


## ★ 진행 중: 문의시나리오 DB 통합 (2026-07-25, v5.9.55)

- **[B] 타이머의 목적 재정의(대표)**: 송장변환·중간발주는 수기 버튼 유지. 타이머는 ①고객문의 자동답변 ②정산 아침 자동수집용. 진행 순서 확정: ①문의시나리오 DB 통합 → ②정산 수집(아침 9:30~10시 + 오후 7~8시, 화면에서 변경 가능) → ③네이버 상품문의 수집+자동등록(쓰기 — 중계서버 허용목록 추가 필요). 문의 수집 주기 30분.
- **①단계 완료(v5.9.55 배포됨)**: `inquiry_scenarios` 테이블 + 톡톡봇 시나리오 **47건 이관**(46+영업시간외, 바이트 일치 검증) + [문의 관리] 화면(직원 편집 가능, 삭제·전체스위치 대표 전용) + 봇 조회 API `GET /api/scenarios`(env `SCENARIO_API_TOKEN` Bearer, 미설정 시 503 잠김).
- **톡톡봇**(`C:\Users\전승범\OneDrive\문서\★제주아꼼이네 톡톡봇`, 별도 리포): `scenario-store.js`(5분 캐시·폴백) 커밋됨, **push는 대표가 직접**(마지막 1회). `SCENARIO_SOURCE=code`(기본)→검증 후 `db` 스위치. 비교 스크립트 `scripts/compare-scenarios.js` 47/47 일치 확인됨.
- **대표 실행 절차**: `docs/문의시나리오_전환절차.md` (토큰 생성→Render env 2곳→봇 push→스위치→체크리스트).
- 설계/계획: `docs/superpowers/specs/2026-07-25-inquiry-scenario-db-design.md`, `docs/superpowers/plans/2026-07-25-inquiry-scenario-db.md`. 진행 원장: `.superpowers/sdd/progress.md`.
- ⚠️ 영업시간외 문구(0번)는 현재 봇이 미사용(코드에 import만 존재) — 화면 수정해도 봇 영향 없음, 재활성화는 별도 작업.
- **품목별금액→판매현황 자동 연동 (v5.9.57 배포됨)**: pricing 저장(POST/PUT) 시 새 품목명이 bot_products에 **'준비중'(봇 미노출)·판매가 빈칸**으로 자동 등록(`syncPricingToBotProducts`, ON CONFLICT DO NOTHING — 삭제 품목 부활 금지). 판매가는 수기 세팅 후 [판매중] 전환 시 노출. 결제가·판매가는 이름만 공유(금액 복사 금지). 탭2 수동 [품목 추가] 버튼 제거(유입 일원화, POST API·복구 로직은 존치). 백필 19개 준비중 등록됨.
- **판매현황·가격 탭 (v5.9.56 배포됨)**: `{{가격표}}`/`{{판매현황}}` 원본 = `bot_products`(회사프로그램과 **같은 DB**, 36건) → [문의 관리] 탭2에서 관리(저장 → 봇 1분 캐시 반영, 봇 무수정). ⚠️ **스마트스토어 판매가 — '품목별 금액'(거래처 결제가)과 절대 통합 금지.** 톡톡봇 정리 커밋(시드 하드코딩 제거·deleted_at 필터·PRODUCTS_PAGE 게이트) push 완료. ✅ **구 /products·/unmatched 페이지 은퇴 종결(7/28)** — Render `PRODUCTS_PAGE=off`·`UNMATCHED_PAGE=off` 적용·410 실측 확인. 시나리오는 **db 스위치 완료·실응답 정상**(대표 확인).
- **에이전트 운용 원칙(대표 지시)**: 총괄(설계·판단·실서비스 섬세 작업)은 메인 모델 직접, 단순 시공만 Haiku 위임+Sonnet 검수, Task별 분류 선보고.

---

## 0. 프로젝트 개요

- **회사프로그램**: Node.js/Express + PostgreSQL, **Render** 배포 (서비스명 jeju-acom-company).
- **저장소**: `bumiletter-bit/jeju-acom-company` (GitHub, **PUBLIC**, main 브랜치 push → Render 자동배포).
  - ⚠️ PUBLIC이므로 **네이버 시크릿·토큰을 코드/커밋에 절대 넣지 말 것.**
- **로컬 작업 경로**: `C:\Users\전승범\OneDrive\문서\★제주아꼼이네 회사프로그램`
- **목표**: 네이버 스마트스토어 주문/정산을 API로 자동 수집해 회사프로그램(송장변환·정산·중간발주)에 연동. 이후 쿠팡·카페24(자사몰)까지 확대.

### 배포 방법
```
npm run deploy
```
- 동작: DB 백업 → git tag → git push (main). push되면 Render가 자동 재배포(~2분).
- ⚠️ **DB 백업이 오래 걸림(3분+ 가능)** → Bash 타임아웃 **9분(540000ms)** 이상으로 실행할 것.
- 배포 전 반드시: `node --check server.js`, `node --check public/app.js`, `version.js`의 VERSION 상향, `public/index.html`의 `app.js?v=NNN` 캐시버전 상향, `CHANGELOG.md` 갱신.
- **현재 버전: v5.9.306 / app.js 캐시 v=381** (2026-09-07 배포·Render 반영 실측). ⚠️ 배포 전 `version.js`·`index.html` **실값 재확인** — 병행 세션이 올렸을 수 있음(8/4 v5.9.183 선점 사례).
- ⚠️ **병행 세션 주의**: 같은 작업폴더에서 세션 2개가 동시에 커밋하면 배포 준비 `git add`에 상대 세션의 미커밋 변경이 쓸려 들어감(2026-07-26 실사례 — 무해 확인 후 병합 배포). **핵심 로직 작업 2개가 겹칠 땐 반드시 따로 배포**(대표 확정 원칙). 배포 직전 `git log`·`git status`로 상대 세션 커밋 확인.
- git commit 메시지 끝에 Co-Authored-By / Claude-Session 푸터 붙이기(기존 커밋 참고).

---

## 1. 네이버 API 연동 구조 (IP 화이트리스트 우회)

네이버 커머스API는 **사전 등록된 개별 고정 IP에서만** 호출을 허용. Render의 아웃바운드 IP는 유동(랜덤) → 직접 호출 불가.
→ **고정 공인 IP를 가진 NCP 중계서버를 프록시로 경유**한다.

```
[Render 회사프로그램]  →  [NCP 중계서버 akkome-relay]  →  [네이버 커머스API]
   (naver-relay.js)        101.79.16.213 : 4000            api.commerce.naver.com
   Bearer 자체토큰만        네이버 시크릿 보관             (등록된 IP = NCP 공인IP)
```

- **네이버 client_id / client_secret 은 중계서버 .env 에만 존재.** Render/코드/GitHub에는 없음.
- Render는 중계서버 URL + 자체 Bearer 토큰(RELAY_AUTH_TOKEN)만 앎.

### 1-1. 중계서버 (NCP, akkome-relay)
- **공인 IP: `101.79.16.213`**, **포트: `4000`**
- 코드: 리포 `relay/` 폴더 (`relay/server.js`, `relay/install.sh`, `relay/설치가이드.md`).
- 현재 **HTTPS**로 동작 (자체서명 인증서). `cert.pem`/`key.pem` 있으면 HTTPS, 없으면 HTTP 폴백.
- `relay/server.js` 핵심:
  - `RELAY_VERSION` 상수를 `/health`에 노출 (install.sh 재실행으로 최신 반영됐는지 확인용). 현재 `'2026-07-24.2'`.
  - `/health` (인증 불필요, `?token=1`이면 토큰발급까지 시험) — token_test:"success" 나오면 인증 OK.
  - `/naver` (POST, 자체 Bearer 필요) — `{method, path, query?, body?}` 받아 네이버로 중계.
  - **허용목록(ALLOW)** = **읽기(GET) 전체 허용**: `/external/v1/pay-settle/`, `/external/v1/pay-order/seller/`, `/external/v1/pay-user/`, `/external/v1/seller/` + 상세조회 POST `/external/v1/pay-order/seller/product-orders/query`. **쓰기(발송처리·답변등록 등)는 전면 차단.** → 새 '조회' 기능 추가 시 **install.sh 재실행 불필요**(이미 다 열려있음). 쓰기는 나중에 명시적으로 추가.
  - 텔레그램 직접 호출 안 함(대표 설계). 오류는 journalctl 로그 + HTTP 응답으로만. 알림은 회사프로그램(텔레그램 시크릿 보유처)이 담당.

### 1-2. 중계서버 .env 구조 (`/opt/akkome-relay/.env`)
```
PORT=4000
NAVER_CLIENT_ID=(네이버 실제 client_id)
NAVER_CLIENT_SECRET=(네이버 실제 client_secret)
NAVER_TYPE=SELF
RELAY_AUTH_TOKEN=(openssl rand -hex 32 로 재생성한 값 — 설치스크립트 출력값 아님)
NAVER_API_BASE=https://api.commerce.naver.com
(TELEGRAM_* 는 비움)
```
- 실제 값 입력 완료됨(대표 직접).

### 1-3. Render 환경변수 (회사프로그램)
```
NAVER_RELAY_URL   = https://101.79.16.213:4000   (HTTPS 전환됨)
NAVER_RELAY_TOKEN = (중계서버 RELAY_AUTH_TOKEN 과 동일한 값)
NAVER_RELAY_CA    = (중계서버 자체서명 cert.pem 내용 — 인증서 고정용)
```
- ⚠️ HTTPS 전환·CA값 반영 여부는 대표 실환경 기준으로 재확인 필요할 수 있음.

### 1-4. 인증 방식 (bcrypt 전자서명) — 중계서버가 처리
- 토큰 발급: `POST https://api.commerce.naver.com/external/v1/oauth2/token`
  - Content-Type: `application/x-www-form-urlencoded` (**JSON 금지**)
  - 파라미터: `client_id`, `timestamp`(밀리초), `grant_type=client_credentials`, `client_secret_sign`, `type=SELF`
  - **서명** = `bcrypt(`${client_id}_${timestamp}`, salt=client_secret)` → base64 인코딩
  - 응답 `expires_in`(초, 보통 10800=3시간)만큼 토큰 캐싱(만료 1분 전 재발급).

### 1-5. 회사프로그램 측 클라이언트 코드
- `naver-relay.js` (리포 루트): Node 내장 http/https로 중계서버 호출. `callNaver({method,path,query,body}, notify)`, `relayHealth()`, `configured()`. 자체서명 인증서 고정(NAVER_RELAY_CA 있으면 `opt.ca`, 없으면 `rejectUnauthorized:false`).
- `server.js`에서 `const naverRelay = require('./naver-relay.js')`.
- 연결테스트: `GET /api/agent-office/naver/test` (adminOnly) — 데이터관리 화면 `#naver-connect-card` 버튼.

---

## 2. 인프라 현황 (NCP·Render·보안)

### 2-1. NCP ACG (방화벽) 인바운드 규칙
- TCP **22** ← 대표 회선 IP만(myIp). **⚠️ 대표 회선이 유동 IP** → 인터넷 회선이 바뀌면 NCP 콘솔에서 myIp 재설정해야 SSH 접속 됨.
- TCP **4000** ← `74.220.52.0/24`
- TCP **4000** ← `74.220.60.0/24` (Render 아웃바운드 대역)
- 3389(RDP) 규칙은 삭제함.

### 2-2. SSH 접속 (🔴 NCP 특성 — AWS와 다름)
- **NCP의 .pem 은 SSH 로그인 키가 아니라 root 비밀번호 복호화용.**
- 접속 순서: ①ACG 22 열림 확인 ②NCP 콘솔 '관리자 비밀번호 확인'에 .pem 첨부해 root 비밀번호 꺼냄 ③PowerShell `ssh root@101.79.16.213` + 그 비밀번호.
- **🔴 PasswordAuthentication 끄면 서버 잠김 → 절대 끄지 말 것.** (일반 SSH키 등록 후에만 키전용 전환 가능)
- 무차별대입 방어는 **fail2ban**으로(설치가이드 STEP5). 유동 IP라 ACG로 22를 특정 IP에 영구 고정 불가.

### 2-3. 서버 작업 방식
- **클로드는 코드/명령만 작성. NCP 서버 명령 실행은 대표가 PowerShell/SSH에서 직접.**
- 명령 줄 때: 복붙 가능한 한 덩어리 + 한국어 설명 + `여기에_입력` 자리표시 + 검증 명령 함께.

### 2-4. 미완료(인프라)
- 밀린 OS 업데이트(보안 포함) 미적용(재부팅 부담으로 보류).
- unattended-upgrades(자동 보안업뎃)은 ON.

---

## 3. 송장변환 자동 불러오기 진행상황 (4단계 [A]) — **작동 중, 실테스트 확인 단계**

### 3-1. 목적/흐름
네이버 **배송준비** 주문을 API로 가져와, 기존 송장변환의 스마트스토어 데이터로 **주입**한다. **변환 로직(convertDataSmart)은 절대 손대지 않음.** 수동 엑셀 업로드도 그대로 유지.

- **화면**: 송장변환 → 스마트스토어 카드 → **[🛰️ 네이버 배송준비 전체 불러오기]** 버튼 + 옆에 **조회 기간(일) 입력칸**(기본 40, 최대 180).
  - 마크업: `public/index.html` 의 `#invoice-auto-smart`, `#invoice-auto-smart-days`, `#invoice-auto-smart-msg` (~1625행).
  - 프론트 로직: `public/app.js` `loadNaver()` (~5637행). 결과를 전역 `invoiceDataSmart` 에 넣고 `updateInvoiceMergeBtn()`+`showInvoiceMergedPreview()` 호출 → 대표가 [통합 변환 및 다운로드] 누르면 기존 로직이 처리.

- **백엔드**: `GET /api/agent-office/naver/invoice-orders?days=N` (adminOnly, server.js ~5024행) → `naverFetchInvoiceOrders(days)` (server.js ~4950행).

### 3-2. `naverFetchInvoiceOrders(days)` 동작
- 네이버 조건형 조회 `GET /external/v1/pay-order/seller/product-orders` 호출.
  - query: `from`, `to`(ISO+09:00), `rangeType=PAYED_DATETIME`, `productOrderStatuses=PAYED`, `placeOrderStatusType=OK`, `pageSize=300`, `page` 순회.
- **days일을 24시간씩 나눠 순회**(네이버 1회 조회 최대 24시간 제약). 각 날짜마다 페이지 순회(최대 20p).
- 응답 항목 파싱(중첩 방어): `const c = it.content||it; const po = c.productOrder||it.productOrder||c; const od = c.order||it.order||po.order||{}; const sa = po.shippingAddress||{};`
- 스마트스토어 컬럼으로 매핑해서 rows 리턴. `convertDataSmart`가 읽는 키:
  - `구매자명`=od.ordererName, `구매자연락처`=od.ordererTel, `수취인명`=sa.name, **`옵션정보`=po.productOption**, `수량`=po.quantity, `수취인연락처1`=sa.tel1, `수취인연락처2`=sa.tel2, `통합배송지`=sa.baseAddress+detailedAddress, `배송메세지`=po.shippingMemo.
- 진단용 `sample`(개인정보 마스킹, `naverMaskPII`)·`sample_option` 반환 → 화면 🔧 진단에 표시.

### 3-3. 🔴 해결한 문제들 (이 세션)
1. **미매칭(빈값)** (v5.9.49): 주입 `옵션정보`를 상품명+옵션으로 넣었던 게 원인. **수동 엑셀은 옵션(productOption)만 매칭에 씀.** 대표 실엑셀(`스마트스토어_전체주문발주발송관리_20260725_1027.xlsx`)로 확인 — 8열 옵션정보 = "아꼼이네 상품선택: 1. 미니밤호박 특품최상급 / 상품 및 과수: 특품 5kg(10~20개)" 같은 옵션 문자열, 20열 상품명은 변환기가 안 씀. → `옵션정보=productOption`으로 수정. **대표 확인: 옵션 매칭 정상.** + content 래퍼 방어 추가.
2. **데이터 누락 1299건→183건** (v5.9.50): 최근 **3일**만 조회하던 게 원인.
   - **🔴 네이버 제약(공식문서·개발자포럼 확인)**: 네이버엔 **'현재 배송준비 상태 전체'를 주는 API가 없음**(상태 기반 목록조회 불가). **결제일 기준으로만 조회, 1회 최대 24시간, 최대 180일.**
   - → days 기본값을 **40일**로 확대(청귤 예약주문 성수기 = 결제일 한 달 이내분 커버). 화면에서 조절 가능(상한 180). 부족하면 일수 올리면 됨.
3. **중복방지 완전 제거** (v5.9.50, 대표 지시): 예전엔 "이미 올린 것 제외"(`naver_invoice_uploaded` 테이블)를 썼으나 **제거**. 이유: **중간발주 연동상 매번 '배송준비 전체'를 그대로 넘겨야 함**(거래처에 현재까지 들어온 물량 계속 알려야 하므로). 신규주문 발주확인은 대표가 수기. (테이블 정의만 잔존·무해)
4. **429 호출 제한** (v5.9.51): 40일 = 40회+ 연속 호출이 네이버 **초당 rate limit(429)**에 걸림 → `naverFetchInvoiceOrders` 안에 ①호출 사이 **350ms 간격** ②429 시 **지수 백오프 재시도**(1s→2s→4s→8s→16s, 최대 5회, `callWithRetry`) ③재시도 끝 실패 시에만 텔레그램 1회(스팸 방지) ④`naverFriendlyError`에 429 안내 추가. **⚠️ [B] 타이머 만들 때도 같은 간격/재시도 패턴 필수.**
5. **부분취소 수량** (v5.9.54): 4박스 주문→2박스 취소 시 배송준비는 2인데 4로 나옴(김혜경 사례). 네이버 `quantity`=**최초 주문 수량**(취소 미반영). 공식 구조체 수량 3필드: quantity·initialQuantity·**remainQuantity(남은 수량)**. → `수량 = remainQuantity 있으면 그것, 없으면 quantity`, remainQuantity=0이면 주문 제외. 결과 메시지에 "부분취소 수량 반영 N건" 표시. 중간발주도 같은 rows라 자동 연동. **⚠️ [B] 타이머·향후 수량 쓰는 모든 곳에서 remainQuantity 우선 규칙 유지.**
6. **하루 300건 잘림 (1298→999)** (v5.9.52): 기간을 180일로 늘려도 999건 동일 → 기간 문제 아님. 원인 추정: 응답 **totalPages 필드명이 예상과 달라 1페이지로 오판** → 주문 몰린 날(청귤 예약 오픈일)은 하루 300건(pageSize)에서 잘림. → 페이지 순회를 **"300건 만재면 다음 페이지 계속"**(짧게 오면 그 날 끝, 상한 50p) 방식으로 교체 — 응답 필드명에 무관하게 동작. 🔧 진단에 `page_info`(응답 상위 필드, PII 없음) 노출. **⚠️ 대표 확인: 취소·반품은 배송준비에 안 걸림 — 상태 정의 차이로 혼동하지 말 것. v5.9.52 재테스트(1298 근처 나오는지) 대기 중.**

### 3-4. ⚠️ 지금 확인 대기 중인 것
- 대표가 **[🛰️ 네이버 배송준비 전체 불러오기]** 눌러서 **건수가 배송준비 총건수(1299 근처)와 맞는지** 확인. (v5.9.50에서 429 떴고 → v5.9.51에서 간격+재시도로 대응 배포함. **v5.9.51 재테스트 결과 대기 중**)
- 40일 × 350ms 간격이라 **1~2분 소요**. 만약 화면이 "가져오는 중..."에서 **2~3분+ 멈춰** 있으면(Render/브라우저 타임아웃 위험) → **백그라운드 수집 방식**(뒤에서 모으고 진행률 표시, DB에 스냅샷 저장)으로 전환해야 함. 이게 [B]·중간발주 연동에도 유리.
- 발주확인·발송처리는 **대표 수기** (자동화 금지).

### 3-4-b. 중간발주 API 직결 (v5.9.53) — 완료·테스트 대기
- 송장변환 > **중간발주 탭**: 비번(4031) 엑셀 업로드 **제거** → **[📦 중간발주 시작하기]** 버튼이 같은 API(`/api/agent-office/naver/invoice-orders?days=N`)로 배송준비 전체를 불러와 `qtyRowsMain`에 주입 → 기존 `recomputeQtyAggregate()`(matchProduct로 옵션정보 파싱)·거래처 필터·품목추가·이미지 저장 **무수정** 동작. 조회 기간 입력 `#invoice-qty-days`(기본40·최대180). 문구 "현재 스마트스토어 배송준비로 중간발주를 진행합니다".
- app.js: `handleQtyUpload`/`setupQtyArea` 삭제, `setupQtyStart()`(~5765행) 추가, `resetInvoiceQty` 정리. `parseInvoiceRows` 등 헬퍼는 미사용 정의로 존치. 서버 `/api/invoice/decrypt` 존치.
- ⚠️ 불러오기 API는 **관리자 전용**(기존 정책) — 직원 계정은 중간발주 시작하기 사용 불가(필요 시 직원 허용 여부 대표에게 문의).

### 3-5. 정산 조회 (3단계) — 완료
- `GET /api/agent-office/naver/settlements?from&to` (adminOnly). 일별정산 `GET /external/v1/pay-settle/settle/daily` (필수: startDate·endDate·pageNumber·pageSize). 데이터관리 화면에서 조회. **대표 확인: 정산내역 정상.**
- 🔴 **네이버 정산금 ≠ 우리(세미) 거래처결제가 정산.** 체계 완전히 다름, 대조·병합 불가. 네이버 정산은 별도 영역으로만.

---

## 4. 다음 할 일

### 4-0. ✅ [B] 자동수집 타이머 — 완료 (v5.9.60 배포, 2026-07-25)
- 60초 틱 스케줄러(한 틱 1개 실행·몰림 방지), 수집기: 정산(하루1회 09:30 앵커·스냅샷+텔레그램)/주문(신규 결제 건수 알림)/취소·반품(**claimType/claimStatus로 판정** — lastChangedType 아님!·알림만)/문의(**pay-user/inquiries 파라미터 문서 미확정 — '지원 예정' 스텁**, 켜면 실패 표시). 전부 기본 OFF·설정은 naver_auto_collect(DB)·[데이터관리] 타이머 카드에서 관리.
- last-changed-statuses는 more.moreFrom/moreSequence 페이지네이션 필수(1회 상한 ~300건).
- **대표 확정: 송장변환 직전 취소 재확인 넣지 않음**(취소·반품은 배송준비 무관 — 취소는 PAYED 자동 이탈). 안전장치 = [자동 불러오기]가 항상 실행 시점 신규 조회. 타이머 수집분은 현황·통계용(변환 재사용 금지).
- 문의시나리오 DB 통합·판매현황 탭 이전·품목별금액 연동은 CLAUDE.md 상단 "★ 진행 중" 절 참조 (전부 완료·배포됨).

### 4-1. [B] 타이머 자동수집 (스마트스토어) — 다음 작업
- **전부 기본 OFF**, 화면에서 on/off + 주기(분) 변경 가능(**코드에 주기 하드코딩 금지**). `naver_auto_collect` 테이블 사용(이미 생성·시드됨: settlement 1440, order 60, claim 90, inquiry 30, 전부 OFF).
- 주기(제안): 정산 하루1번(1440분), 주문 60분, 문의 30분.
- **취소·반품 = 알림만**(대표가 (B)안 선택): 취소/반품 요청 들어오면 텔레그램 알림만. 자동처리 X.
- **🔴 "송장변환 직전 취소·반품 재확인" 기능은 넣지 말 것**(대표 지시로 제거됨): 반품은 배송준비에 없고, 취소는 PAYED에서 자동 이탈해 이미 필터로 빠짐. 취소는 대표 수기 확인.
- 네이버 **초당(token bucket) rate limit** 대비 실행 시각 분산(stagger). 일일한도 아님.
- 실패 시 텔레그램 알림(회사프로그램의 기존 텔레그램 사용).
- 변경 감지엔 `GET /external/v1/pay-order/seller/product-orders/last-changed-statuses`(PII 없음) 활용 가능.

### 4-2. 🛒 쿠팡 송장변환 연동 — ✅ 완료·검증 통과 (2026-07-26, v5.9.70 태그로 배포)
- **대표 검증 완료**: 키 입력(NCP) → 연결 3줄 초록불 → 자동 불러오기(3일 조회·발주서 1건) → 통합변환 → **수기 Wing 다운로드 변환과 완전 동일 대조 확인**. 취소 재확인·부분취소 공식 탑재.
- 지시문 `지시문_쿠팡_송장변환_연동.md` v2 기준. 스펙 `docs/superpowers/specs/2026-07-26-coupang-invoice-design.md`(공식 문서 확정 — v5 ordersheets/INSTRUCT·v6 returnRequests/cancelType=CANCEL·HMAC CEA·**수량=shippingCount−(hold+cancel)**).
- 중계서버 `/coupang`(조회 2종만·vendorId 강제·RELAY_VERSION 2026-07-26.1 — **install.sh 재실행 필요**), 회사프로그램 `callCoupang`+수집기+연결테스트+취소재확인, [쿠팡 자동 불러오기] 버튼. 변환 로직·네이버 코드 무수정.
- 대표 절차: `docs/쿠팡_키입력_절차.md` (SSH 키 입력 → 연결테스트 → 실주문 검증 체크리스트 §⑥ — 부분취소·보내는사람연락처 대조 포함). 키 만료 2027-01-21, 재발급 일정 2027-01-07 자동 등록.

### 4-3. 🏠 카페24(자사몰) 송장변환 연동 — ✅ 완료·검증 통과 (2026-07-26, v5.9.73) — **3채널 완성**
- **3채널 완성** (스마트스토어·쿠팡·자사몰). 스펙 `docs/superpowers/specs/2026-07-26-cafe24-invoice-design.md` (공식 문서 전 스펙 확정 — 추측 없음).
- 구조: **IP 제한 없음 → Render 직접 호출**(중계서버 불필요). 신규 모듈 `cafe24.js` + server.js 라우트 4종(`/api/cafe24/auth-url`·`/api/cafe24/callback`(공개, state 검증)·`/api/agent-office/cafe24/test`·`/api/agent-office/cafe24/invoice-orders?days=1~90`).
- OAuth: Authorization Code, mall `akkome`, Client ID `mMdlm3cHGZwkVaem7wGDIB`(공개값, 코드 상수), scope `mall.read_order`. **Secret = Render env `CAFE24_CLIENT_SECRET`**(대표 직접 입력, 열람 금지). 토큰 AES-256-GCM 암호화 저장(`agent_office_config` 'cafe24_tokens', 키=sha256(Secret)) — Secret 변경 시 재승인 1회. access 2h/refresh 2주, 만료 60초 전 자동 갱신(**동시 갱신 락** — 갱신 시 refresh도 교체되므로 필수). 갱신 실패 → 텔레그램 1회 + "재승인 필요".
- 취소 방어: akkome 몰은 **결제 즉시 N20(배송준비중) 자동 진입**(발주확인 없음) → **품목 단위 `items[].order_status==='N20'`만 채택**(취소신청 품목은 C코드로 변경돼 자연 제외) + **수량 = quantity − claim_quantity**(0 이하 제외). 반품 로직 없음.
- **대표 검증 결과 (2026-07-26)**: ①②Secret 입력·[동의]·연결 테스트 🟢 ③**9건 자동 불러오기→변환→수기 대조 완전 동일 — 통과**(옵션 표기 포함, 매핑 조정 불필요) ④취소 제외 — 몰 구조상 취소신청 시 배송준비중에서 즉시 이탈 → 실행 시점 조회로 충분, 탑재된 품목 필터+수량 차감은 **이중 안전벨트로 유지** ⑤토큰 자동 갱신은 오후 4:23 이후 재불러오기로 확인 예정(유일한 잔여 확인 — 실패 시 [연결 테스트]에 "재승인 필요" 표시됨).

### 4-4. (보류) 입금예정 캘린더
- 대표가 별도로 작업 중이라 클로드는 손대지 않음.

---

## 5. 작업 규칙 (대표 지시 — 반드시 준수)

- **변환 로직(convertDataSmart) 절대 수정 금지.** 자동 불러오기는 invoiceDataSmart에 주입만.
- **추측 금지 — 스펙은 공식 문서에서 확인.** 엔드포인트 probe(찔러보기) 금지.
  - apicenter 직접 fetch 차단됨 → **r.jina.ai 프록시**로 읽음(`https://r.jina.ai/https://apicenter.commerce.naver.com/docs/...`). SPA라 본문 안 나오면 WebSearch로 개발자포럼(github.com/commerce-api-naver/commerce-api/discussions) 확인.
- **결과물은 클로드가 직접 검증 후 보고.** 애매한 건 그대로 복명복창.
- **부작용 사전 고지**: 수정이 다른 문제를 유발할 수 있으면 먼저 대표에게 알리고 진행. 아니면 바로 실행.
- **쓰기(발송처리 등) 자동화 금지** — 현재 전부 읽기 전용. 발주확인·발송처리는 대표 수기.
- 물리삭제 금지·soft-delete, audit_logs 기록(출처 naver-api).
- 시크릿·토큰을 코드/커밋에 넣지 말 것(PUBLIC 저장소).

---

## 6. 핵심 파일 위치 요약

| 항목 | 위치 |
|---|---|
| 중계서버 코드 | `relay/server.js`, `relay/install.sh`, `relay/설치가이드.md` |
| 회사프로그램 중계 클라이언트 | `naver-relay.js` (루트) |
| 자동 불러오기 백엔드 | `server.js` `naverFetchInvoiceOrders()` ~4950행, 엔드포인트 ~5024행 |
| 자동 불러오기 프론트 | `public/app.js` `loadNaver()` ~5637행 |
| 자동 불러오기 UI 마크업 | `public/index.html` ~1616~1632행 (스마트스토어 카드) |
| 변환 로직(수정금지) | `public/app.js` `convertDataSmart()` ~5514행, `matchProduct()` ~5340행 |
| 연결테스트/정산 | `server.js` `/api/agent-office/naver/test`, `/api/agent-office/naver/settlements` |
| 자동수집 테이블 | `naver_auto_collect` (server.js initDB, 시드 완료) |
| 최초 설계 인수인계 | `NCP인증키/네이버커머스API_연동_인수인계서.md` |
| 🎨 디자인 가이드 (**디자인 작업 전 필독**) | `디자인/디자인_가이드.md` — 인디고 테마 확정·토큰·공통 컴포넌트·노랑 리테마 금지 |
| 🎨 디자인 스킬 (**상시 — 대표 지시 8/25**) | 디자인·UI 신설·개편·다듬기 작업이면 **`design-taste` 스킬을 먼저 로드**하고 작업(타이포·컬러·모션·상태 규칙). ⚠️ 우선순위: 기존 확정 사항(인디고 테마·디자인 가이드·무회귀)이 스킬 일반론보다 항상 우선 — 스킬이 다른 걸 권해도 가이드를 따른다 |
| 버전 | `version.js` · `public/index.html` app.js 캐시 — **항상 파일 실값으로 확인**(이 표에 숫자를 박지 않음 — 병행 세션 선점 대비) |
