# 제주아꼼이네 회사프로그램 — 네이버 커머스API 연동 인수인계

> **이 문서 목적**: 새 세션(새 터미널)에서 이것만 봐도 네이버 API 연동 작업을 그대로 이어가기 위한 상세 기록.
> **최종 갱신**: 2026-08-04 (v5.9.182 / app.js v=340 배포 시점 — 지시 #178까지 종결)
> 요약본 아님. 구체적으로 적음. 관련 상세는 리포 `NCP인증키/네이버커머스API_연동_인수인계서.md`(최초 설계)와 자동메모리 `jeju-naver-commerce-api.md`도 참조.

## 🔴 무회귀 원칙 (상시 최우선 — 지시 #209 대표 확정, 2026-08-04)

> 대표 원문: **"코딩하면서 이게 뒤틀리고 저게 뒤틀리면 의미가 없다"**

- **모든 시공은 기존 작업(회사프로그램·자사몰 v5) 무회귀가 전제다.**
- **수정 범위 밖 화면·기능이 멀쩡한지 스모크로 확인하기 전에는 완료 보고 금지.**
- 🔴 **"코드상 정상" 판정 금지 — 실DB·API·실렌더 3겹으로 검증한다.**
- **근거(2026-08-04 하루에 회귀 3건, 전부 대표가 실물로 발견)**:
  - **#187** — `pad2` 스코프 오류로 **렌더러 전체 사망**
  - **#192** — v5Cats/v5Views가 v5Render보다 먼저 실행돼 `if(!V5) return`으로 **에러 없이 조용히 죽음** → **클릭 전멸**. 🔴 "콘솔 에러 0"만 보고 통과시킨 것이 원인
  - **#199** — 행 표시만 고치고 **요약 집계는 옛 기준 그대로** 둔 채 "완료" 보고 → 선물하기가 경고에 남음
- 함께 지켜온 검증 교훈(재발 방지): 패치 스크립트는 삽입 후 **`grep -c`로 실삽입 확인** + **정규식·SQL placeholder는 그 값으로 1회 실행**해 판정(#178·#179) · UI는 **실제 마크업에 클릭**(하네스 직접 호출 금지) · 재렌더 후 **selector 재질의**(detached 노드 클릭 금지).

## 🔴 운용 체제 (상시 최우선)

### ★ 현행: 한시 폴링 재개 (지시 #197, 2026-08-04 대표 확정)
- **폴링 주기 3분** — 작업 세션 동안 신규 지시를 자동 감지. 신규 건 없으면 즉시 대기 복귀(토큰 절감).
- **종료 조건**: 대표 또는 똑똑이가 **"폴링 종료"** 지시 → 즉시 아래 호출 대기 체제(#167)로 복귀. 그 전까지 유지.
- **협업 흐름**: 대표 → 똑똑이(전략·판단) → 지시함 등록 → **클코가 폴링으로 자동 감지·처리** → 완료 보고 → 똑똑이 검수 → 대표 보고. 대표는 판단·실물 확인만.
- 🔴 **보고 규약(강화)**: 똑똑이는 **지시함 응답만 보고 검수**한다. 터미널에 쓴 내용과 **동등한 수준으로 지시함 응답을 작성**할 것 — 짧게 쓰면 정보가 유실된다.
- 🔴 **판단 지점 처리**: 대표가 실시간으로 터미널을 보지 않으므로 **작업 중 대화형으로 묻지 말 것**. ①안전한 쪽을 택해 진행하고 판단 근거를 보고에 명시 ②되돌릴 수 없거나 위험하면 **착수하지 말고 "대표 확정 필요"로 보고**. 임의 결정 금지는 그대로.
- 폴링 중에도 기존 원칙 전부 유효 — 외부 행동 승인제·자동수정 금지·발송 파이프라인 신중.

### (보존) 호출 대기 체제 — 지시 #167, 2026-08-01
- 5분 상시 폴링 폐지. 평시 대기 — 대표가 "지시함 확인" 입력 시에만 poll-light.js로 조회·처리. 완료 직후 보충 지시 1회 추가 조회 후 대기.
- **"폴링 종료" 지시가 오면 이 체제로 돌아온다.**
- **poll-light**: `node "%LOCALAPPDATA%\Temp\claude\...\scratchpad\poll-light.js"` (세션 스크래치 — 새 세션이면 cc_instructions 직접 조회 스크립트 재작성: '대기·진행중' 전건+본문, 첫 줄 verdict).
- 기존 속도 규칙 B(5분 폴링)는 이 체제로 대체. 규칙 A(메인 단독)·품질 그물망·보고 규약(#113)은 유지.

## ★★★★ 8/5 종일 세션 인수인계 (지시 #211~#234 — 야간 자율 큐 진행 중) ← 최신, 여기부터 읽기

**버전: 회사 v5.9.205**(#235 — /guide 아꼼이 로고 실이미지(`public/akkomi.png`)+다른상품 섹션 `GUIDE_SHOW_OTHERS=false` OFF) / **톡톡봇 320148b** / **🚀 알림톡 발사 첫날 무사고 마감**(sent 128=자동114+소급14·실패0·보류 전량 소진·포인트 832원≈128×6.5 정합 — #214 저녁 보고 완료. ⚠️보류 7건 발주확인 manual-needed 잔존·"주문기록밖" 8/5 신규 1건=선물하기 skip) / **홈 대표 합격 유지**(skin6 — 야간 시공 후 스모크 전 회 통과).

## ✅ 8/8 낮 세션 (Opus 5) — ①자사몰 AI챗 가동 ③PC 화면 대응 완료 / ②정식 전환만 대표 GO 대기 ← 최신, 여기부터

**버전: 회사 v5.9.229 / app.js v=355.** 대표 지시 순서 ①→③→② 중 ①③ 완료.

### ① 자사몰 AI챗 실가동 (3채널 통합 완성 — v5.9.228)
- **스킨**: `CHAT_OPEN=true`(홈 검색바 "궁금한 건 언제나 물어보십셔!!" → 챗 시트). '준비 중' 배지 제거·입력창 개방·전송 버튼 활성색. 손님 식별값 전달 = 로그인 회원(`akm_mid_v1` 릴레이) 또는 브라우저 세션키(`akm_chat_sid_v1`, PII 아님).
- **서버**: 기존 `/api/public/shop-chat`(qnaGenerate 원천 하나)에 **로그 기록 신설** — message_logs에 `mall:` 프리픽스(톡톡=프리픽스 없음·카카오=`kakao:`)·시나리오명·**scenario_nos**·response_source='ai'. 문의 관리 > 💬 톡톡 문의 화면에 3채널이 함께 뜨고, **채널 배지**(톡톡/카카오/자사몰)로 구분(v5.9.229).
- **라임 정책(판단 근거 명시)**: 톡톡·카카오는 30초 버퍼·30분 쿨다운. **자사몰 웹챗은 기본 즉답·연속답변**으로 뒀다 — 웹챗은 손님이 화면 앞에서 기다리고, **자사몰엔 직원 응대 화면이 없어 봇이 침묵하면 아무도 답하지 못하고 방치**되기 때문(구조적 차이). 대신 ⓐ모든 대화가 문의 관리에 기록돼 직원이 확인 가능 ⓑ연속 6턴 초과 시 "자세한 상담은 카카오톡 채널로" 자동 안내(직원 응대 체계가 있는 채널로 유도). **🔴 대표가 "톡톡과 완전 동일"을 원하면 코드 수정 없이** `agent_office_config.bot_timing`에 `mall_buffer_sec:30, mall_cooldown_min:30` 넣으면 즉시 적용(범위 가드 0~120초/0~180분).
- **실측 합격**: 칩 클릭 → 배송 시나리오 337자 실답변 / 직접 입력 "청귤 언제부터 배송돼요?" → 청귤 시나리오 406자 실답변(8/18 전후 출하) / API 200 2회 / **DB 로그 2건 실기록**(scenario_nos 1, 38·48) / pageerror 0.

### ③ PC(데스크톱) 화면 대응 (v5.9.229 동반 — 스킨측)
- **실측 원인**: 홈·귤나무·룰렛·캘린더는 이미 480px 중앙 정렬 정상. **layout.html을 쓰는 서브 페이지 전부**(로그인·장바구니·약관·주문내역·주소록·가입완료)가 1440px 전개 — 제목은 왼쪽 끝, 헤더는 가운데, 푸터는 좌우 양끝으로 흩어져 "깨짐"으로 보였음.
- **교정**: layout.html + orderform.html에 `@media (min-width:768px)`로 **body 480px 중앙 고정 + 바깥 배경/그림자**. position:fixed 요소(#orderFixArea·.gFixed)도 동일 규격 중앙 정렬(탭바 .akm-tabbar는 자체 규칙으로 이미 480px 중앙). **모바일(767px 이하)엔 규칙 자체가 적용 안 됨 = 완전 무회귀.**
- **검증**: PC 1440 — 서브 3페이지 body 480px·좌측 480(정중앙)·가로 넘침 0 / 모바일 390 — 7페이지 전부 390px·넘침 0 / pageerror 0.

### ①-b 대표 추가 지시 반영 (v5.9.230)
- **문의 관리 표에 「채널」 열 신설**(일시 다음) — 네이버톡톡·카카오채널·자사몰 배지. 디자인 가이드 `.pill` 계승(자사몰=인디고 주색상 #4F46E5·카카오=옐로·톡톡=그린). 열 폭 재배분(min-width 940px)·colspan 보정. **배포본 실검증**(app.js에 chanCell·`<th>채널</th>`, styles.css에 pill-ch-* 존재 확인).
- **자사몰 챗 연속 안내 6턴 → 3턴**, 문구 = "더 궁금하신 점은 카카오톡 채널 「제주아꼼이네」 또는 고객센터 010-6687-4031로 연락주세요 🍊".

### ③-b PC 화면 2차 교정 (대표 실물 재보고 — 장바구니 세로 깨짐)
- 🚨**핵심 함정**: 카페24 PC CSS는 **뷰포트 폭 기준**이라 body를 480px로 줄여도 PC 레이아웃이 그대로 발동. 실측 = `.cart-container` display:flex → 상품목록 176px로 압착돼 상품명이 세로 한 글자씩 깨짐. + `.gFixed`(하단 주문 바)를 PC에서 **display:none 처리**(카페24는 PC에서 페이지 안 버튼 사용).
- **교정**(layout.html PC 미디어쿼리): `.cart-container{display:block}`·`.cart-product/.cart-total{width:auto}` 1단 강제 + `#orderFixArea.gFixed:not(.displaynone){display:block}`로 하단 주문 바 되살림. **`:not(.displaynone)` 필수** — 빈 장바구니는 카페24가 displaynone 클래스로 숨기므로 무조건 켜면 빈 바구니에도 주문 바가 뜬다(실측 확인 후 조건 확정).
- 검증: PC 담긴 상태 = 주문 바 480px 중앙·"선택수량 1개 25,800원·주문하기" 표시 / 빈 바구니 = 숨김 유지 / 모바일 완전 동일 / 에러 0.

### 🛒 8/8 밤 — #280·#281 상세 구매 개편·대기 신청 확장 (대표 실물 지시) ← 최신, 여기부터

**#281 상세 하단 바 전면 개편**(대표 원문: "수량은 없어지고 상품 체크 시 옆에 상품수량이랑… x표시… 2품목 3품목도 선택 주문가능하도록")
- **바 = 두 줄 구조**: [선택 상품 목록 `#pdPicks`] + [💬 · 구매하기 `.pb-row`]. 바 수량 스테퍼(pbQty) **제거** — 수량은 **선택한 상품마다** 조절.
- 옵션 행 클릭 = **다중 선택 토글**(재탭 해제 · ✕로 해제 시 옵션 행 `.sel`도 동시 해제). 목록 줄 = 옵션명 + [− n +] + 금액 + ✕, 하단에 `선택 N종 · M개 / 합계`.
- **상태 단일 소스 = `window.__akmPd`**(상세 열 때마다 초기화) · 위임 핸들러는 **화면당 1회만 바인딩**(`_akmPickBound`/`_akmPickBarBound`). ⚠️기존 구조는 재렌더마다 addEventListener가 **누적**되고 있었음 — 다중선택에선 한 번 탭에 2번 push되는 사고가 나므로 가드 필수.
- **여러 옵션 한 번에 담기(`doAddMulti`) — 🔴 실측 확정 스키마**: 카페24 옵션 select에 change를 **연속으로 주면 옵션 행이 그 수만큼 생성**되고 각 행에 `input[name="quantity_opt[]"]`가 **선택 순서대로** 붙는다 → 수량을 채우고 **[장바구니] 1회 클릭으로 전부 담김**(실측: 2옵션 → 2줄·수량 2/3 정확). 수량 오배정 방지로 **인덱스가 아니라 "행 텍스트에 그 옵션명이 들어있는지"로 짝지음**(인덱스는 폴백).
- `akmC24Flow(c24, {items:[{text,qty}]})` 신설 — 1종이면 종전 단건 흐름으로 자동 축약(무회귀). 하나라도 카페24 옵션과 불일치하면 단건 확인 레이어로 폴백.
- 🚨 **겹침 결함 재발·교정(#252와 같은 계열)**: 옵션을 고르면 바가 69→176px로 커지며 **방금 고른 행을 덮어** 다음 탭이 `.pk-row`에 먹혔음(elementFromPoint 실측). → 선택 직후 **커진 만큼 `pdBody.scrollTop`을 밀어 올려** 고른 행이 항상 바 위에 남게 교정(실측: 행 530→421px·재탭 해제 정상). `akmPdBarFit()`이 바 높이에 맞춰 스크롤 영역 높이도 재계산.
- **실측 합격**: 기능 11/11 · 무회귀 10/10 · **1품목 5.0초 / 2품목 5.4초 / 3품목 6.2초 — 담긴 줄 수·수량 전부 정확**(1·2·3종) · 품절 상세 대기 버튼·홈·장바구니·게임 무회귀 · 에러 0.

**#280 대표 실물 지시 3건**
- **캘린더 시즌 대기 = 실접수 전환**(홈 상세만 되어 있고 캘린더는 "준비중" 모달이었음): `calendar.html` 모달을 연락처 입력 폼으로 개편 → 홈과 **같은 공개 API**(`/api/public/season-waitlist`, memo='자사몰 캘린더')로 대기 명단 등록. 대기 버튼 3곳(월별 행·품목 카드·시즌 준비중 그리드)에 **`data-wait-nm`으로 품목명을 렌더 시점에 심음**. 실측: "노지 감귤"로 실접수 성공.
- **경품 문구 통일**: 룰렛 "오늘의 경품" 정적 마크업·당첨 모달·귤나무 수확 보상에 **잔존하던 "하우스귤 3kg 선물용 1박스" 전멸** → **"제철 귤 4.5kg (그때 가장 싱싱한 품목)"**. ※"업그레이드 이용권"의 `하우스귤 2.5kg→4.5kg`는 중량 상향 설명이라 의도적 유지.
- **로그인 여백 압축**: 실측으로 원인 4곳 특정(카카오 하단 8 + 카드 패딩 18 + 배너 마진 16 + 모듈 하단 40px) → **카카오→가입배너 43px→17px · 배너 아래 40px→10px**.

**#282(대표 실물 재보고 — "청귤은 수량이 안 된다") — 근본 원인 2겹 교정**
- 🔴 **실체 = 옵션 행이 하단 바 뒤에 깔림**(elementFromPoint 실측: 선택 직후 행 중심에 `.pk-row`가 잡힘). 원인 = `#pdBody` 높이를 **`calc(100% - 바높이)`로 잡은 것** — `100%`가 뷰포트 기준이라 **pdBody 상단 오프셋(약 107px)만큼 바 뒤로 더 내려가** 있었음. 청귤처럼 옵션이 짧은 상품에서 특히 치명적(4행 중 3행이 바 뒤). → **실좌표 계산으로 교체**(`바.top − body.top − 8`, 이상값이면 종전 calc 폴백). #252·#278의 겹침 계열을 구조적으로 종결.
- **폰 전용 원인 동반 교정**: 수량 [+]/[−]가 30×32px로 작고 **연타 시 iOS 더블탭 확대 제스처가 탭을 삼킴** → **40×40px + `touch-action:manipulation`**(✕는 34px, 옵션 행·구매하기에도 동일 속성). 실측: 빠른 연타 5회 = 수량 6 정확.
- **장바구니 반영은 원래 정상이었음**(청귤 1품목 수량3·2품목 2·4, 감귤 5, 밤호박 2 — 4/4 정확). 화면 터치 문제였음.
- **경품 문구**(대표): 업그레이드 이용권 설명에서 품목 특정 삭제 → **"주문 상품에 중량을 업그레이드 하여 보내드립니다"**(목록·당첨 모달 2곳).
- ⚠️**검증 지표 자책**: "바에 가려진 행"을 세면서 **스크롤로 아직 안 보이는 아래 행까지 세어** 교정 후에도 실패로 표시됐음(옵션 많은 상품이면 항상 실패로 보이는 지표). 겹침 판정은 **스크롤 영역 안에 보이는 행만** 대상으로, 그리고 **각 행을 노출시켜 elementFromPoint로 실제 클릭 가능 여부**까지 봐야 한다.

**#284(대표 실물 — "수량이 또 안 된다 · 청귤만의 문제가 아니다") — 실기기 전용 실패 경로 2개 차단**
- 🔴 재현 실패 기록: iPhone13·**갤럭시(360×800·Android UA)** 에뮬레이션, 대표 캡처 상품(혼합 선물세트)까지 전부 **정상 동작**(1→2→3). 그래서 **원인 특정 대신 실기기에서만 깨질 수 있는 경로를 전부 막는 방식**으로 전환.
- ⓐ **누른 버튼이 사라지는 구조였음**: 수량 탭마다 `akmPdPicks()`가 `#pdPicks`를 innerHTML로 통째 재렌더 → 손가락 아래 버튼이 파괴·재생성. → **`akmPdQtySync(i)` 신설 — 숫자·행 금액·합계만 제자리 갱신**(재렌더 없음. 추가/삭제 때만 전체 렌더).
- ⓑ **폰에서 손가락이 조금만 밀리면 브라우저가 스크롤로 보고 click을 취소**한다(`.pk-list`가 overflow-y:auto라 더 쉬움). → **touchstart/touchend 직접 처리**(이동 12px 미만만 인정) + `preventDefault()`로 뒤따르는 합성 click 제거, click 경로는 `_tAt` 700ms 가드로 이중 반영 차단(PC 마우스는 click 경로 그대로).
- 실측: **안드로이드·아이폰 × 판매중 5종 전부** +3/−1 정확·표기 일치 · 한 번 탭 = 정확히 1 증가(중복 0) · 좌표 직접 터치 반영 · **화면 수량 3 = 장바구니 3** · 에러 0. (`scripts/verify-299-qty.js`)
- ⓒ **🔴 구조 결함(대표 3번째 보고 후 발견 — 가장 유력한 실체)**: `.pd-bar`가 `position:absolute`인데 **스크롤 컨테이너 `#pdBody` 안에** 들어 있었다(실측 `바의부모: div#pdBody`). 기준 박스는 `.pd-sheet`(transform 보유)라 화면엔 제대로 그려지지만, **스크롤 영역 안의 escaped absolute 요소는 폰 브라우저에서 보이는 위치와 터치 판정이 어긋날 수 있다** — 바가 69→176px로 커진 뒤 특히 취약(구매하기는 바 맨 아래라 살아남고 위쪽 수량 버튼만 죽는 양상과 일치). → 렌더 직후 **`.pd-sheet`로 바를 옮김**(한 줄·화면 완전 동일). 실측: `바의부모: div.pd-sheet` · `바가스크롤영역안: false` · 스크롤 영역이 바를 **−8px로 침범 0**.
- ⓓ **#285 최종 방식 전환(대표 4번째 보고 — "크롬·삼성 둘 다 안 됨")**: click/touchend·위임(delegation)에 의존하던 것을 버리고 **버튼마다 `pointerdown`을 직접 배선**. 근거 = click은 브라우저가 스크롤·제스처로 판단하면 **아예 발생하지 않고**, 위임은 중간에서 이벤트가 끊기면 도달하지 못한다. `pointerdown`은 그 판단 이전에 발생 = 실기기에서 가장 확실. 중복 방지 = 버튼에 `_akmPdAt` 도장 → 뒤따르는 click 800ms 무시(폴백 click 경로는 PointerEvent 미지원용으로 유지). `akmPdQtySync`가 재렌더를 안 하므로 배선은 유지되고, 전체 렌더 때만 `akmPdBind()`로 재배선.
  - 실측(`scripts/verify-300-qty.js`): **click 없이 pointerdown만 발생시켜도 수량 증가**(실기기 실패 상황 재현) · 실제 탭 = 정확히 1 증가(중복 0) · 연타 6회 = +6 · 하한 1 · 화면=장바구니 일치 · **PC 마우스 클릭·✕ 무회귀** · 9/9.
- ⓔ **🔴🔴 #286 — 에뮬레이터로는 절대 재현 불가였던 진짜 조건: `dvh`(동적 뷰포트)**. 대표 5번째 보고의 캡처가 결정적 단서였다 — **첫 줄 수량이 6까지 올라가 있고 합계도 정확**(198,600원) = 버튼은 살아 있고 **특정 순간에만 죽는다**. `.pd-sheet{height:100dvh}`·`.pk-list{max-height:38dvh}`는 **폰 주소창이 나타났다 사라질 때마다 값이 바뀌어 시트·하단 바가 재배치**되고, 그 찰나의 터치가 유실된다. **에뮬레이터엔 주소창 애니메이션이 자체가 없어 100% 통과했던 것**(내가 5번 "정상"이라 보고한 이유).
  - 교정: `.pd-sheet` → `height:100%`(부모 `.pd-dim`이 `position:fixed; inset:0`이라 흔들리지 않는 고정 박스) · `.pk-list` → `38vh` · `#pdBody`·`.pk-list`에 **`overscroll-behavior:contain`**(목록 스크롤이 문서로 전파돼 주소창을 여닫는 것 자체를 차단).
  - 실측(`scripts/verify-301-dvh.js`): 시트 높이 = 고정 부모와 동일 · **화면 높이를 740/800/712로 바꾼 직후마다 수량 +1 정상** · 스크롤 직후 +1 · 2줄일 때 첫 줄·둘째 줄 모두 정상 · 화면 수량 = 장바구니 수량 · 10/10.
  - 🔴 **교훈: "폰에서만 안 된다"는 보고에 에뮬레이터 통과를 근거로 반박하지 말 것.** 주소창 show/hide·safe-area·실제 관성 스크롤은 에뮬레이터에 없다. `dvh`는 고정 오버레이 안에서 쓰지 말 것.
- ⓕ **🔴 #287 — 진짜 원인 확정: "첫 누름만 안 먹는다"(대표 6번째 보고의 결정적 표현)**. 옵션을 고르면 내 코드가 `pdBody.scrollTop`을 밀어 올리고 있었는데(#281에서 겹침 막으려고 넣은 것), **폰에서는 스크롤이 막 움직인 직후의 첫 터치가 "스크롤 멈춤"에 소모돼 버튼에 도달하지 않는다**. 그래서 첫 누름만 씹히고 두 번째부터 먹혔다. → **스크롤 보정 코드 제거**(겹침은 `akmPdBarFit()`의 높이 계산이 이미 담당 — 제거 후에도 가림 0·전 행 클릭 가능 실측). 추가로 `padding-bottom`을 **바 높이 + 24**로 같이 키워 **바가 커져도 스크롤 가능 범위가 줄지 않게**(브라우저가 scrollTop을 강제로 당기는 튐도 차단).
  - 🚨 **동반 결함 즉시 발견·교정(#287-b)**: 중복 방지 도장을 **버튼 자신에게** 찍고 있었는데 ✕는 목록을 다시 그려 그 버튼이 사라진다 → 뒤따르는 click이 **새로 그려진 다른 ✕에 떨어져 한 번 눌렀는데 2개가 삭제**(검증에서 실측 포착). 도장을 **전역(`window.__akmPdAt`)** 으로 변경.
  - 실측(`scripts/verify-302-firsttap.js`): **판매중 5종 × 전 옵션에서 "선택 직후 첫 누름" 100% +1** · 선택해도 화면 이동 0(하네스 스크롤과 구분해 클릭 캡처 시점 기준으로 측정) · 스크롤 상태에서도 첫 누름 정상 · 옵션 행 가림 0. + 기능 11/11 · pointerdown 9/9 · dvh 10/10 · 무회귀 10/10.
- ⓖ **#288 — 터치 처리 자체를 근본 교체 + 실기기 자가 진단 도입(대표 7번째 보고 "직접 원인 찾아라")**
  - 🔴 남은 구조: **`touch-action:manipulation`은 패닝(스크롤)을 여전히 허용**한다 → 브라우저는 손가락이 탭인지 스크롤인지 판정될 때까지 `pointerdown`을 **미루거나 스크롤로 판정되면 `pointercancel`로 취소**한다. 버튼이 스크롤 영역 안 바에 있어 판정이 스크롤 쪽으로 기울기 쉬웠음. → **CSS `touch-action:none`**(이 버튼에서 스크롤 시작 자체를 금지) + **가장 먼저 뜨는 `touchstart`에서 직접 처리**(preventDefault로 이후 click 정리), `pointerdown`은 마우스·펜 전용으로 축소해 중복 원천 차단.
  - **갤러리 자동 슬라이드(1.5초마다 smooth 스크롤)** 도 정지 조건 추가 — 갤러리가 화면 밖이거나 **고른 상품이 있으면(구매 중) 정지**. 진행 중인 스크롤 애니메이션이 터치를 먹는 경합 제거.
  - 🩺 **실기기 자가 진단**: 주소에 **`?akmdiag=1`** 을 붙이면 바 위에 검은 상자로 `touchstart/touchend/pointerdown(타입)/pointercancel/click`과 반영 여부·수량이 찍힌다. **대표 폰 캡처 1장이면 원인 확정** — 재현 실패로 추측을 반복하지 않기 위한 장치(평소엔 완전 비활성).
  - 실측(`scripts/verify-303-touch.js`): touch-action=none · **touchstart만으로 즉시 +1** · 실제 탭 정확히 +1 · 연타 5회 +5 · 선택 중 갤러리 정지 · ✕ 하나만 삭제 · 화면=장바구니 · 진단 표시 · 9/9. + 첫 누름 5/5 · 무회귀 10/10.
- ⓗ **#289 — 대표 진단 캡처로 좁혀진 사실**: `?akmdiag=1` 캡처 결과 **옵션 2개 선택 상태에서는 `touchstart 1 → 반영 q=[2,1]`이 정상 기록**되는데, **옵션 1개만 선택했을 때는 진단창에 아무 줄도 안 뜬다** = 버튼에 touchstart·pointerdown·click이 **하나도 도달하지 않는다**(코드 문제가 아니라 접점 문제). 에뮬레이터에서는 1개 선택 상태에서도 버튼 9지점 히트테스트 9/9 성공(옵션 0~3 전부) — 여전히 환경 차이.
  - 조치 ①**진단 2단계**: 문서 전체 touchstart를 캡처해 **`닿음(x,y)→실제요소 | +버튼 y=..~.. x=..~..`** 를 기록 → 손가락이 실제로 어디에 닿는지와 버튼 좌표를 나란히 보여 **접점 어긋남을 한 캡처로 확정**. ②**터치 영역만 확대**(`::after`로 상하 14px·좌우 10px — 보이는 크기 불변).
  - 🔴 다음 세션: 대표 캡처의 `닿음(...)` 줄이 원인 확정 자료. 버튼 좌표 밖이면 좌표 어긋남(브라우저 UI·확대), 버튼 좌표 안인데 다른 요소면 그 요소가 덮개.
- ⓘ **🔴🔴🔴 #290·#291 — 최종 해결: 요소(hit-test) 기반 처리를 버리고 「좌표 판정」으로 전환**
  - **대표 진단 캡처가 정답을 줌**: `닿음(249,645)→div | +버튼 y=625~665 x=230~270` = 손가락이 **[+] 정중앙에 정확히 닿았는데 그 지점의 요소가 버튼이 아닌 투명 div**. 즉 무엇인가가 버튼을 덮고 있고, **요소 기반으로는 무슨 이벤트를 쓰든 그 덮개를 통과할 수 없다**(내가 7번 실패한 이유 — 에뮬레이터엔 그 덮개가 없다).
  - 교정 ①**문서 최상위 touchstart에서 좌표로 직접 판정** — 터치 좌표가 어느 수량/✕ 버튼 영역인지 계산해 실행. 덮개가 무엇이든 무력화. 안전 범위 = **하단 바 안쪽 터치만**(스크롤 영역 탭을 삼키지 않게). 터치는 이 경로 하나만(중복 0), pointerdown은 마우스·펜 전용, click은 폴백.
  - 교정 ②**옵션 행 선택도 click 의존 제거** — 실측: **화면 700px에서 스크롤 직후 탭하면 touchstart는 오는데 click이 0**(브라우저가 클릭을 아예 만들지 않음) → 선택 실패. `touchend`(항상 발생·이동 12px 미만)로 토글하고 뒤따르는 click은 도장으로 무시. 이 결함은 대표 보고 전에 검증에서 잡았다.
  - 교정 ③**토스트에 `pointer-events:none`** — `#akmC24Toast`(bottom:84px·z-index:400)가 사라질 때 opacity만 0으로 두어 그 띠가 영구 터치 차단 구역이었다. 안내용 요소는 예외 없이 pointer-events:none(farm/roulette 토스트도 점검 — 탭바만 남아 정상).
  - 실측(`scripts/verify-306-geo.js`): **버튼을 투명 div로 완전히 덮은 상태에서도** +1·연타 4회 +4·− 정상 · 덮개 제거 후 중복 0 · 2번째 옵션 추가 · ✕ 하나만 삭제 · 화면=장바구니 · PC 마우스 무회귀 · **11/11**. + 기능 11/11 · 첫 누름 5/5 · 전 상품 7/7 · 무회귀 10/10.
  - 🔴 **교훈(최상위)**: "폰에서만 안 된다"가 반복되면 **이벤트 종류를 바꿔가며 추측하지 말고, 접점(무엇이 눌리는지)을 실기기에서 찍어라.** `?akmdiag=1` 진단이 8번째 시도에서 5분 만에 원인을 확정했다 — 처음부터 넣었어야 했다.
- ⓙ **#292 — 「클릭 구제」로 상세 화면 전 버튼 커버 (대표: "쇼핑 돌다가 구매하기가 안 먹힌다")**
  - 진단 캡처가 또 확정: `닿음(481,640)→button#pdOrder.pb-cta` **×4** — 손가락은 구매하기에 정확히 닿는데 아무 일도 안 일어남 = **브라우저가 click을 만들지 않음**. 수량 버튼만 터치 경로로 바꿔놨고 **구매하기·옵션 탭·닫기·문의는 여전히 click 의존**이라 그대로 죽어 있었다. (대표 화면은 **폴드/태블릿 폭 960px** — 캡처의 x=481이 화면 중앙과 일치.)
  - 교정 = **클릭 구제(click rescue)**: `.pd-dim.show` 안의 버튼/링크에서 손가락을 뗀 뒤 **350ms 안에 click이 오지 않으면 직접 `el.click()`** 을 발생시킨다. 정상적으로 오면 아무것도 하지 않아 **중복 0**(그 버튼에서 난 click만 "정상"으로 인정). 수량·옵션은 전용 터치 경로가 있으므로 제외.
  - 실측(`scripts/verify-307-rescue.js`): **합성 click 생성을 원천 차단한 상태**(touchend preventDefault)에서 **넓은화면 960 · 폰 360 양쪽 모두** 옵션 탭 열림·옵션 선택·수량 +1·구매하기 진행·**장바구니 도달 수량 2** 전부 성공 · 정상 환경에서 탭 클릭 **1회만**(구제 중복 없음) — **13/13**.
  - 대표 질문 답변(겹침·구버전): `scripts/verify-308-browse.js` — 상품 3개를 오가며 스크롤·닫기 반복 후 **창 누적 0**(pd-dim·pd-bar·#pdOrder·#pdPicks 전부 ≤1, 옵션 확인 레이어 none, iframe 누적 0). **겹겹이 창·구버전 잔존 아님** — 원인은 위의 click 미생성. 구 스킨 삭제 불필요.
- ⓚ **🔴 #293 — 마지막 원인: 「손가락 흔들림」 허용치가 너무 빡빡했다**
  - 대표 3번째 캡처에서 `닿음→button#pdOrder` 4회는 찍히는데 **`클릭구제` 줄이 안 찍힘** = 구제 자체가 실행되지 않았다는 뜻. 원인 = 내가 스크롤 제스처를 걸러내려고 둔 **이동 12px 기준**. 실제 손가락은 탭 한 번에 **15~25px 흔들리는 게 정상**(대표 화면은 폴드 960px 폭)이라 **정상 탭이 전부 스크롤로 분류**돼 버려졌다. 자동 탭은 이동 0px이라 100% 통과 — 또 에뮬레이터에서만 멀쩡했던 항목.
  - 교정: **거리 기준 폐기 → "손가락을 뗀 지점이 그 버튼 영역(+24px) 안인가"** 로 판정(옵션 행은 행 영역 +16/+10px). 의미도 더 정확하고 흔들림에 강함.
  - 진단 강화: 뗀 좌표·버튼 안/밖·이동량을 `뗌(x,y) 버튼안 | 이동 20,14` 로 기록 — 이 줄이 있었으면 한 번에 잡았다.
  - 실측(`scripts/verify-309-drift.js`): **CDP로 touchStart→touchMove(15~20px)→touchEnd** 를 직접 만들고 **합성 click도 원천 차단**한 상태에서 **960px·360px 양쪽** 구매하기(옵션 탭 열림)·옵션 선택·수량 +1·**장바구니 도달** 전부 성공 · 큰 스크롤(120px)은 무시 — **12/12**. + 클릭구제 13/13 · 쇼핑 시나리오 6/6 · 기능 11/11 · 전 상품 7/7 · 무회귀 10/10.
  - 🔴 **교훈: 자동 탭은 이동 0px이다.** 터치 판정에 거리 임계값을 쓰면 실기기에서만 죽는다 — **영역 포함 여부로 판정할 것.**
- 🔴🔴🔴 **#294 — 진짜 원인 확정·해결: 하단 바가 상품을 열 때마다 「중복 생성」되고 있었다 (내가 만든 결함)**
  - 대표 캡처 2장이 결정타: `클릭구제 → pdOrder`가 **찍혔는데도** 아무 일이 없었다 = **구제가 버튼을 실제로 눌렀는데 그 버튼에 기능이 없다**.
  - 실측(`scripts/probe-dupbar2.js` — 카드로 상품을 옮겨 다니며 계수): **바 1→2→3→4→5개**, `#pdOrder`도 5개. 원인 = #284-c에서 바를 `.pd-sheet`로 옮겨두면 다음 렌더의 `pdBody.innerHTML` 초기화로 **지워지지 않아 계속 쌓인다**. 화면에 보이는 건 **마지막**(맨 위에 그려짐) 바인데 `getElementById('pdOrder')`는 **첫 번째(옛것)** 를 돌려주므로 **기능은 안 보이는 버튼에 붙고, 보이는 버튼은 껍데기**가 된다. → "첫 상품은 되고 쇼핑 돌다가 누르면 안 된다"와 정확히 일치.
  - 교정: 새 바를 시트에 붙이기 전에 **시트에 남아 있는 옛 `.pd-bar`를 전부 제거**. 실측: 상품 5개 순회 후에도 바 1개·`보이는 구매하기 클릭 반응 ✅`.
  - 🔴 **내 검증이 이 결함을 놓친 이유(가장 중요한 교훈)**: 모든 테스트가 **상품마다 `page.goto()`로 새로 로드**했다 — 그러면 DOM이 초기화되어 중복이 절대 생기지 않는다. **대표는 새로고침 없이 카드를 눌러 돌아다닌다.** → `scripts/verify-310-spa.js` 신설: **한 번만 로드하고 카드 탭으로 4개 상품 순회 → 구매 완주**(폴드960·폰360 **14/14**). 앞으로 SPA 화면 검증은 **반드시 무새로고침 순회**로 할 것.
- ✅ **#295(대표): 진단 모드 완전 OFF** — `window.akmDiagOn = false` 하드 오프(코드는 보존). `?akmdiag=1`을 붙여도 안 뜸을 실측(`scripts/verify-311-diagoff.js` 6/6 — 일반 접속·플래그 접속 양쪽 미노출 + 구매 동선 정상). **다시 켤 땐 그 한 줄을 `/[?&]akmdiag=1/.test(location.search)`로 되돌리면 됨.**
- 🟢 **#304(대표 확정 8/9): `cafe24_sync` 타이머 ON + `cafe24_sync_mode='fix'` — 네이버↔자사몰 자동 동기화 실가동 개시**
  - 그동안 **꺼져 있었음**(enabled=false·자동 실행 이력 0건 — 8/7 오픈 전환 때 수동 1회만). 대표 질문("매일 가격 매칭하고 있지?")에 DB로 확인해 사실 정정 후 즉시 가동.
  - 동작: **04:30 네이버 스냅샷 수집 → 05:10 카페24 반영**(매일). 대상 = 매핑 19종(c91~c109). 가드 = ±50% 초과 차단·진열(T) 상품만.
  - ⭕ **자동 반영**: 기본가 · 품절/판매재개 ❌ **자동 아님**: **옵션 구성·옵션별 가격** · 상품 추가/삭제 · 상품명 · 상세페이지
  - 🔴 **대표 오해 정정 기록**: "네이버 오픈하면 **옵션이랑 판매가격** 자동으로 맞춰지지?" → **가격·품절만 맞춰지고 옵션은 안 맞춰진다.** 시즌 오픈으로 옵션 구성이 바뀌면(중량 추가/삭제·옵션가 변경) **자사몰은 옛 옵션 그대로 남아 없는 옵션이 주문될 위험** → 시즌 오픈 때마다 **"○○ 옵션 갱신해줘" 요청 필요**.
  - 실측 확인(8/9 04:31 스냅샷): 매핑 19종 중 **판매중 5종**(c91 밤호박·c92 감귤·c93 청귤·c94 황금향·c100 혼합세트) / **품절 14종**. **한라봉 = c107**(제주 한라봉 노지 하우스, n4840212264) 매핑 정상·현재 네이버 품절.
- 🕹️ **#332(대표): 하단 탭바 — 전부 흑백, 선택된 탭만 컬러로 "불 들어오게" + 나침반·귤나무 아이콘 네모칸 제거·확대**
  - 🔴 **왜 5칸이 전부 컬러였나**: 원 CSS(889·891행)는 원래 `grayscale(1)` + 활성만 컬러였는데, **뒤쪽 오버라이드 블록이 「아이콘 항상 컬러」로 덮고 있었다**(1372행대). 같은 파일 안에 `.tab.active .t-ic` 선언이 **네 곳**(889·1022·1379·1424) 있어 마지막 것이 이긴다 — 고칠 때 **전수 grep 후 마지막 선언까지 확인할 것**(1424행의 연노랑 pill을 놓쳐 1차 검증에서 홈만 실패).
  - **네모칸의 정체 = CSS 테두리가 아니라 그림 자체**. 탭이 쓰던 `cat_roulette.png`·`cat_tree.png`는 **크림색 사각 타일 배경을 포함한 카테고리용 아이콘**이었다 → 배경을 제거한 **탭 전용 `assets2/tab_compass.png`·`tab_tree.png` 신설**(34·28KB). 카테고리 화면은 타일이 디자인 요소이므로 **원본 무접촉**.
  - 🔴 **컷아웃 임계는 실측으로 확정**(`scripts/tab-probe.js` → `tab-icons.js`): 배경이 코너에서 중앙으로 갈수록 밝아져 **색거리 15→64**까지 벌어지고 모형은 **280 이상**. 1차에 SPREAD=40으로 잡았다가 **모형 둘레에 크림색 후광이 남았다**(자홍 배경 QA로 눈 확인) → **100**으로 교정. 기법은 #326 계승(가장자리 flood fill — 모형 안쪽 흰 칸 보존) + **bbox 크롭**(이미지 경계 = 모형 경계라야 탭에서 크기를 정확히 통제할 수 있다).
  - **크기 균형**: 아이콘 칸 높이 **28px 통일** · 이모지 23px · 이미지 28px(타일이 없어 이모지와 광학적으로 맞는다) · 활성만 `scale(1.06)`. **활성 pill 배경 제거** — 둥근 모형을 사각 pill에 넣으면 대표가 없애라 한 "네모칸"이 되살아난다.
  - 적용 = **index·farm·roulette·calendar 4파일 동일 규칙**(`scripts/patch-tabicons.js` — 삽입 후 파일별 실삽입 개수 검산 내장).
  - 실측(`scripts/verify-343-tabbar.js` **55/55**): 4화면 × 5칸 — 🔴**CSS 문자열이 아니라 canvas로 다시 그려 실픽셀 채도 측정**(비활성 0.0 = 완전 무채색 / 활성 78.8·114.6 = 컬러) · 비활성 4칸 높이 균일 28 · 활성 30 · `cat_*` 잔존 0 · 아이콘 404 0 · 탭 실이동 · 홈 무회귀 · 에러 0. 캡처 `scripts/tabbar_shots/`.
  - ⚠️ **검증 기준 자책 2건**: ⓐ"5칸 높이 균일"로 재서 **활성 확대(의도한 강조)를 실패로 표시** → 비활성끼리 비교로 교정 ⓑ**한 탭으로 4페이지를 순회**하다 카페24 자체 스크립트 오류(`EC$`·`EC_PlusAppBridge`)가 누적돼 "내 변경 탓"으로 오판할 뻔했다 → **페이지마다 새 탭**으로 에러 분리 측정(페이지별 실측 0).
- 🚚🔴 **#336(대표 8/10 — 발송 안내 체계의 전제가 뒤집힘): 「발송 휴무」와 「도착 불가」는 별개다** — 회사 v5.9.246 / app.js v=358.
  - 🔴 **가장 중요한 사실**: 발송 휴무일 달력의 체크는 **"우리가 출고하지 않는 날"** 이지 "택배가 안 가는 날"이 아니다. 대표 원문 — *"13일 발송이 없는거! 13일 배송은 되!! 14일이 택배없는날 휴무로 13일 발송 시 다음날 도착 14일을 못해서 그러는거야"*. 종전 코드는 휴무일 하나를 `isShipDay`·`isDeliveryDay` **양쪽에서 똑같이 제외**해, 8/12 발송분이 8/13에 정상 도착하는데도 **8/17 도착으로 안내**하고 있었다. (v5.9.245 기록의 「"수~목"은 나올 수 없는 값」은 **오판** — 「수~목」이 정답)
  - **스키마**: `shipping_holidays.no_ship`(발송 불가)·`no_arrive`(도착 불가). 한 날짜가 둘 다일 수도, 하나만일 수도 있다 — **광복절 대체휴무 = 우리는 발송 O · 택배 배달 X**. `loadShippingHolidayInfo()`가 `{set(발송휴무), arriveOff(도착불가), reasons}` 3갈래 반환.
  - 🚨 **DB 함정(실제로 밟음)**: `ALTER TABLE ADD COLUMN ... DEFAULT FALSE`는 **기존 행을 NULL이 아니라 그 값으로 즉시 채운다** → 뒤따르는 `WHERE no_arrive IS NULL` 마이그레이션이 **한 건도 안 걸리고**, 시드 공휴일 17건이 전부 「도착 가능」으로 남아 **설날·추석 배달 계산이 뒤집힐 뻔했다**(적용 후 요약 `arr_off: 4`가 이상해 포착). → **DEFAULT 없이 ADD COLUMN** 후 값을 명시 지정할 것.
  - 🔴 **발송안내(E)의 "내일/모레"는 문자열로 박혀 있었다**: `내일 {{내일요일}}요일~모레 {{모레요일}}요일`에서 **요일만 갈아끼우는 구조**라, 실제로는 닷새 뒤 도착인 건에도 "내일"이 붙어 나갔다. → `computeArrival()` 신설 — 실제 간격을 재서 맞을 때만 "내일". **전 기간 365일 대조 결과 종전 표기가 틀렸던 날이 125일**이었다.
  - **휴무 사유 = 고객 문구로 통일**(대표 "휴무사유 하나만 나가면 되니깐"): #73의 「안내 문구로 통째 대체(override)」 **폐지** — 발송일이 사라져 8/13~16 주문 고객은 `▶ 택배사 택배 없는날 및 공휴일(광복절) 휴무`만 받고 **언제 받는지 한 글자도 못 듣는 상태**였다. → 자동 계산 **뒤에 사유 병기**: `▶ 월요일(8/17) 오전 발송, 화~수 도착 예정 (택배사 …휴무)`. 휴무 등록 입력창도 사유 1개(안내문구 칸 삭제).
  - **달력 3종**: 발송휴무(danger)·도착불가(앰버)·둘 다(반반). **지정된 칸을 누를 때마다 발송휴무 → +도착불가 → 도착불가만 → 해제** 순환(`PATCH /api/agent-office/shipping-holidays/:id`). 폭 780px 중앙 고정 → **카드 틀 전체**(1368px). ⚠️사유를 6→18자로 늘리며 grid 칸이 밀려 넘침 → `minmax(0,1fr)`+`min-width:0`+2줄 말줄임.
  - **8월 세팅**(대표 확정): 8/13 발송휴무·**도착가능** / 8/14~16 발송휴무·도착불가 / 8/18 정상 / 시드 공휴일 17건 도착불가.
    - ⛔ **8/17은 등록하지 않는다**(대표 지적 "17일 표시가 안 되는 게 맞는 것 같은데 헷갈린다"). 내가 「발송 O·도착불가」로 넣었다가 **영향 0임을 실측하고 제거** — 8/16이 발송휴무라 그날 도착할 물건 자체가 없고, 8/12 발송분 도착은 이미 8/13 하나로 확정돼 8/17 상태와 무관하다. **문면 11/11 전부 동일**. 화면만 복잡해졌다. 🔵 교훈: **예상값을 넣을 땐 "이 값이 어떤 문구를 바꾸는가"를 먼저 계산할 것.** 바꾸는 게 없으면 넣지 않는 편이 직원에게 낫다.
    - 🔴 **반대로 8/14~16 「도착불가」는 필수**: 빼면(=발송휴무만 체크) **8/14 금요일이 도착일로 안내**된다(실측 3케이스 — 8/11 주문 `목요일`→`목~금`, 8/12 발송분 `내일 목요일`→`내일 목요일~모레 금요일`). 택배사가 안 움직이는 날이라 실제로는 안 간다.
    - **직원 판단 기준(한 줄)**: **우리만 쉬는 날 = 발송휴무만** / **택배사도 쉬는 날 = 도착불가까지**. 새로 지정하면 항상 발송휴무만 붙고, 택배사도 쉬면 그 칸을 한 번 더 누른다.
  - 검증: **주문안내 무회귀 = 종전 코드(HEAD)와 2026 전 기간 × 3시각 1,095건 전수 대조 → 설명 안 되는 차이 0건**(변경 6건 전부 8/13~17 = 의도된 변경). 실렌더 13/13(실제 탭 클릭·칸 상태·순환·폭·에러 0). **실서버 반영 실측**: 구 `수요일 도착 예정` → 신 **`수~목 도착 예정`**.
  - 🔵 **검증 기법(재사용)**: `git show HEAD:파일 > 임시본` 후 신·구 모듈을 **같은 입력으로 전 기간 전수 대조**하면 무회귀를 숫자로 증명할 수 있다. "설명 안 되는 차이 0건"이 목표치.
  - ⚠️ **대표 확인 대기**: ①8/17 「도착불가」가 실제와 맞는지(대체공휴일 택배 배달 여부 — 틀리면 달력에서 그 칸 클릭) ②8/13~16 사유 문구가 그대로 손님에게 나가므로 문구를 다듬을지.
  - ℹ️ 판매현황 탭 가로 넘침 258px은 **달력을 숨겨도 동일** = 이번 변경과 무관한 기존 문제(범위 밖·미조치).
- 📨 **#334(대표 8/10): 야간 알림톡 실패·보류·차단도 아침 브리핑에 표시** — 회사 v5.9.244.
  - **사각지대였음**: 야간 누적이 **성공 건수만**(`alertNightAcc('kakaosend', sent)`) 이라 21:30~07:30 **하루 10시간 동안의 발송 실패가 아침에 아예 안 보였다.** 대표가 #333 보고 중 이 한계를 듣고 즉시 지시.
  - 브리핑 줄 신설: `📨 알림톡 ❌ 실패 3건 · ⏸ 보류 2건 · 🚫 번호없음 1건 — [알림 발송 이력]에서 확인·재발송` + **실패 사유 상세 최대 5건**(마스킹 번호 + 에러 80자).
  - 구현 = `collectOrderNew`의 fail/hold/block을 **try 지역변수 → 함수 스코프 승격**, 야간이면 `kakaosend_fail`/`_hold`/`_block` 카운터 누적 + 사유는 `alert_night_kakao_fail`에 보관. **리셋 지점 2곳(브리핑 발송 시·브리핑 OFF 시) 모두**에서 함께 비움(무한 누적 방지 — 한쪽만 고치면 OFF 기간에 영구 누적).
  - 🔵 **카운터 누적을 고른 이유**: 야간 시작 시각을 JS Date로 만들어 SQL에 넘기면 **#333에서 데인 시간대 함정(timestamp WITHOUT tz)을 그대로 재범**한다. 카운터는 시간대 변환이 개입하지 않는다. (기존 `autodone` 집계는 `posted_at >= $1, [w]` 방식이라 같은 잠재 위험을 안고 있다 — Render UTC라 현재는 정상. 손대지 않음)
  - 실측 **16/16**(`scratchpad/verify-334-briefing.js`): **실 server.js에서 브리핑 블록 903자를 중괄호 매칭으로 추출해 그대로 실행**(하네스 재현 금지 원칙 준수) — 성공만(무회귀)·실패포함·보류/차단·**성공0+실패만**·전부0(줄 생략)·토글OFF 6케이스 + 누적/리셋 지점 7곳 실삽입 정규식 확인. 배포 후 서버 200·타이머 실행 재개 실측.
  - ⏱️ 동반 교정: 신규주문 알림 문구가 **「최근 1시간 주문」으로 박혀** 있어 5시간 주기와 어긋남 → **「직전 알림 이후」**(주기를 또 바꿔도 안 어긋남). DB 템플릿이라 화면에서 편집 가능.
  - 🔴 **대표 확인 대기**: 내일 아침 07:30 브리핑 실물(보고 규약 #113 — 실물 수신은 대표 확인 항목).
- ⏱️ **#333(대표 8/10): 「주문(신규 알림)」 타이머 60분 → 300분(5시간)** — 회사 v5.9.242·243.
  - 대표 질문("180으로 저장하면 되지? 알림톡도 같이 3시간마다 오나?")에 **답만 하고 확인**한 결과 → 신규주문 알림과 알림톡 결과 요약은 **한 통으로 묶여 있고**(`collectOrderNew` 안에 병기 — #215) 발송 시점은 이 타이머 주기를 그대로 따른다. 이후 대표가 5시간으로 확정.
  - 🔴 **주기만 바꿨으면 숫자가 1/5로 보일 뻔했다**: 알림톡 요약 집계 창이 `interval '60 minutes'` **하드코딩**이라 5시간 중 마지막 1시간분만 잡힌다(실측: 같은 시점 **60분 창 4건 vs 300분 창 24건 = 20건 누락**, 그 사이 실패 건은 ❌ 줄도 안 뜸). → 집계 기준을 **직전 실행 시각(`last_run_at`) 이후 전부**로 교체. 주기를 몇 분으로 바꾸든 누락·중복 0.
  - 고정 창을 안 쓴 이유: 틱이 60초 단위라 실제 실행 간격이 주기보다 최대 1분 길다 → 창을 주기와 똑같이 고정하면 **매 회차 1분치가 샌다**. 장기 정지 후 재개 대비 **주기 2배 클램프**, 문구도 실집계 구간 따라 「알림톡(5시간)」 자동 표기.
  - **실발송은 무접촉** — 손님에게 가는 알림톡은 별도 타이머(`kakao_notify` 3분). 이번 변경은 **대표 폰 보고 주기**만 바꾼다. 주기 값은 DB(`naver_auto_collect.order.interval_min`)라 화면에서 언제든 재조정.
  - 🚨🚨 **자책(중대 — 같은 함정 재범): `last_run_at`은 `timestamp WITHOUT time zone`이다.** `NOW()`(timestamptz, pg가 정확 파싱)와 섞어 JS에서 빼면 **KST 환경에서 정확히 9시간 어긋난다**(실측 566분 vs 26분). 이걸 검증 스크립트에서 밟아 **"전 타이머가 9시간 멈췄다"고 오진**했다 — 실제로는 전부 정상 가동 중(알림톡도 8분 간격 발송 중이었음). CLAUDE.md에 이미 적어둔 함정("날짜 대조는 DB 쪽에서 통일할 것")의 재범.
    - 교정 = v5.9.243에서 **`ROUND(EXTRACT(EPOCH FROM (NOW() - last_run_at))/60)::int`로 DB가 직접 계산**(프로세스 시간대 의존 0). Render는 UTC라 242도 실동작은 정상이었으나 잠재 결함이라 제거.
    - 🔴 **원칙: DB 시각끼리 빼는 계산은 예외 없이 SQL 안에서 할 것.** 특히 `timestamp`(tz 없음)와 `timestamptz`를 한 쿼리에서 같이 꺼내 JS로 넘기지 말 것.
  - 실측(`scratchpad/verify-332-window.js` **8/8** · `verify-243-since.js` **6/6**): `make_interval(mins => $1)` placeholder 실행 판정 3종(CLAUDE.md 함정 규약 준수) · 라벨 계산(300→5시간·60→1시간·30→31분) · 클램프 · `last_run_at` 없음 폴백 · 저장 후 재조회 300 확인 · 배포 후 타이머 실행 재개 실측(18초 내 qna 실행).
- 🎡 **#341(대표 8/10): 룰렛 귤 경품 이름 교정 — 「하우스귤 3kg 선물용 1박스」→「제철 귤 4.5kg (그때 가장 싱싱한 품목)」**
  - 🔴 **핵심 사실(다음 세션 주의): 룰렛 경품 이름은 두 곳에 따로 정의돼 있다.** ⓐ스킨 `roulette.html`의 `SEGS` 배열 = **손님 화면 전용**(원판·당첨 모달 전부 이 값) ⓑ DB `agent_office_config.mall_game_config` = **텔레그램 당첨 알림 전용**. **#280에서 화면(ⓐ)만 고쳐 알림(ⓑ)엔 옛 이름이 그대로 남아 있었다** — 손님 화면과 대표 폰이 서로 다른 물건을 가리키는 상태. **확률 0.5%라 아직 한 번도 안 나와(실측 0건) 드러나지 않았을 뿐.** 앞으로 경품·혜택 문구를 고칠 땐 **화면과 DB 설정 양쪽을 함께** 볼 것.
  - **지급 방식 확인(대표 질문 — "물은 자동 맞지?")**: 경계선은 **`points > 0`** 하나다. 물방울(w1 50%·w3 8%)은 당첨 즉시 `points_ledger`에 **자동 적립**(`mall-api.js` 420행) / 쿠폰5%·10%·업그레이드권·귤박스는 `reward_grants`에 **`pending` 기록 + 텔레그램 알림만** — **실발급 코드 자체가 없다**(카페24 `mall.write_promotion` 스코프 미보유). 실측 = 참여 3회 중 w1 2회 → 원장 2 물방울 자동 적립 / coupon10 1회 → pending 1건(#340 대표 직접 지급분).
  - **시공 = 코드 무접촉·DB 값 1개만(배포 불필요).** 안전장치 = 트랜잭션 `FOR UPDATE` + **변경 전/후 전체 JSON 경로 대조로 "바뀐 곳이 `probabilities.5.label` 하나뿐"임을 증명**(1개가 아니면 자동 롤백) + `audit_logs` 기록. 재조회로 확률·`water_cost`·합계 1000 무변동 확인. 🔵 이 「경로 대조 후 1건이 아니면 롤백」 방식은 **설정 JSON을 손댈 때 재사용할 것** — 화면 저장 경로와 달리 다른 필드가 딸려 바뀌는 사고(#259 추보의 리셋 계열)를 원천 차단한다.
  - 🔵 **쿠폰 자동 발급 가능성(대표 질문 — 미착수·조사만)**: 쿠폰 **38%** 는 **권한 `mall.write_promotion` 추가 + 재동의 1회**면 열릴 가능성이 높다(`reward_grants`에 대상 회원·경품 종류가 이미 정확히 쌓이고 있어 **발급 호출 한 줄 자리만 비어 있음**). 업그레이드권(3.5%)·귤박스(0.5%)는 포장·배송이라 **자동화 불가** → **쿠폰만 자동화해도 수동 부담 약 90% 소멸**(배송 100건 기준 38건 중 34건 자동). ⚠️착수 전 확인 2건 = ①**카페24가 「특정 회원 지정 발급」을 실제로 허용하는지 실측 필요**(#338에서 옵션 API가 문서와 달리 전부 거부된 전례 — 문서만 보고 단정 금지) ②**쿠폰은 손님에게 나가면 회수 불가** → 발급 상한·중복 방지 가드 선행. 급하지 않으면 **다음 카페24 재동의 때 묶어서** 권한 추가(재동의 횟수 절약).
- 💬 **#348·#349(대표 8/10): AI상담 문구 + 귤나무 이름 「아꼼이」 고정 보강**
  - **#348**: 홈 AI상담 입구 `「궁금한 건 언제나 물어보십셔!!」 → 「궁금한 건 언제나 물어보세요!」`(대표 지시). ℹ️ 챗 창 안 인사말의 「뭐든 물어보십셔!」는 지시에 없어 **그대로 뒀다** — 톤을 맞추려면 그 한 줄도 바꾸면 된다.
    - 🚨 **검증 자책**: 「탭해도 챗이 안 열린다」로 판정했는데 **실제로는 정상**이었다. `.chat-dim` 클래스를 **`#chatDim`·`#benefitDim`·`#waitDim` 셋이 공유**해서 `querySelector('.chat-dim')`가 문서 첫 요소(benefitDim)를 잡은 것. 🔵 **같은 클래스를 여러 오버레이가 공유하는 화면에서는 반드시 id로 질의할 것.**
  - **#349(대표 "로그인하면 구매자 이름인데 지금 다 아꼼이로 뜬다")**: 비로그인 화면은 정상(그땐 '아꼼이'가 정답)이라 **로그인 상태에서 이름을 못 읽는 것**. 실로그인 없이 원인 확정은 불가 → **이름을 받는 길이 하나뿐이던 구조**를 보강했다.
    - 🔴 취약점 2개: ⓐ`MEM.login`이 **파싱 시점 1회 평가**라 치환이 늦으면 false로 굳어 이름 로직이 통째로 건너뛰어진다(#311과 같은 뿌리) ⓑ재시도가 5초뿐이라 그 뒤 채워지면 영영 못 읽는다.
    - 교정 = ①**로그인 판정에 기대지 않고 "이름이 읽히면 적용"**(비로그인은 `{$name}` 자체가 렌더되지 않아 빈 값 → '아꼼이' 유지 = 무회귀) ②**레이아웃 이름 릴레이 신설**(`layout.html`이 `{$name}`을 `akm_mnm_v1`에 심고 귤나무가 폴백으로 읽음 — #267-d의 ID 릴레이와 같은 방식) ③**MutationObserver로 채워지는 순간 즉시 반영** + 재시도 12초.
    - 실측 **10/10**(`verify-349-name.js`): 비로그인 무회귀 · 릴레이 폴백으로 h1 교체 · **로그인 블록을 나중에 주입해도 즉시 반영** · 마크업 오염값 무시 · 나무·탭바·물주기·맨위로 무회귀·에러 0.
    - 🔴 **대표 확인 필요**: 로그인 상태에서 이름이 뜨는지. 그래도 「아꼼이」면 **`{$name}`이 렌더 자체가 안 되는 것**이므로 다른 화면(마이·주문내역)에도 이름이 안 뜰 것 — 그 두 곳을 함께 봐주시면 원인을 좁힐 수 있다.
- 🔴🔴 **#347(대표 실물 8/10 — "진동은 오는데 안 들어가 · 뭘 수정하면 또 안 되고"): 오버레이가 탭바를 덮고 있었다**
  - **접점 실측이 즉시 답을 줬다**(#290 원칙): 탭바 자리를 눌렀는데 잡히는 요소가 **`DIV#vwBody.cv-body`** — 마이·주문내역·카테고리 오버레이(`.cv-dim`)가 `inset:0`이라 **탭바를 통째로 덮고** 있었다. 햅틱은 문서 전체에서 잡으니 **진동만 오고 이동은 안 되던 것**. 클릭 로그도 `BUTTON [prevented]`로 오버레이가 먹고 있었다.
  - 🔵 **내가 이번에 만든 게 아니라 원래 구조였다**(#166 계열). 오버레이를 먼저 닫지 않으면 다른 탭으로 갈 수 없어, 손님은 "탭이 죽었다"고 느낀다.
  - 교정 = **z-index 싸움 대신 자리 비우기**(#318에서 검증된 방식): `.cv-dim`을 `inset:0` → `bottom:calc(탭바 + safe-area)`. 겹치지 않으니 가림·터치 흡수가 원천적으로 없다. 시트도 `100dvh` → **`100%`**(#286 — 고정 오버레이 안에서 dvh 금지).
    - ⚠️ **상품 상세(`.pd-dim`)는 무접촉** — #318에서 대표가 "상세는 전체 화면"으로 원복을 지시한 영역이다.
  - 🎞 **동반: 로그인 배너 영상 「빈 화면에 영상 표시가 먼저 뜬다」(대표)** — **용량 문제가 아니었다**(Render 첫 바이트 0.16초·총 0.33초 실측·73.7KB). 카카오톡 인앱 브라우저처럼 **자동재생이 막히는 환경**에서 정지한 빈 틀이 보이던 것. → 영상 0.6초 지점을 뽑아 **poster**(`assets2/akkome_login_poster.png` · 114KB · 팔레트화)로 깔았다. png라 카페24에 올라간다.
    - 🔴 **함께 잡은 결함**: `onerror`가 영상 실패 시 `.akm-emblem`을 **통째로 숨기고** 있었다 → 회선이 나쁘거나 인앱이 mp4를 막으면 히어로가 빈칸. poster 도입 후에는 숨기면 손해라 **배너를 유지**하도록 교정(영상 요청을 차단해 재현·검증).
  - 실측 **43/43**(`verify-347.js`) — 오버레이 열린 채 **탭 5칸 전부 눌리고 실제 이동**(주문내역·마이·귤나무) · 카테고리 화면 동일 · poster 배선/실로드 · **영상 차단 상태에서도 배너 유지** · **무회귀 스모크 7화면**(홈·귤나무·나침반·캘린더·장바구니·로그인·약관 — 탭바 5칸·눌리는 위치·넘침 0·에러 0).
  - ✅ **이전 작업 전수 재검(대표 지적 "수정하면 또 안 되고" 반영)**: #344 후기·맨위로 **56/56** · #344c PWA **7/7** · #344d 후기펼침 **8/8** · #345 네이버 **33/33** · #346 로그인 **126/126** · #346c 통일 **14/14** · #346d 마이 **22/22** — **전부 통과**.
  - 🔵 **교훈: "진동은 오는데 안 눌린다"는 접점 문제다.** 이벤트 종류를 바꿔가며 추측하지 말고 `elementFromPoint`로 그 자리에 무엇이 있는지부터 찍을 것 — 이번엔 1분 만에 확정됐다.
- 🎨 **#346(대표 8/10): 로그인 화면 리디자인 시안 이식** — 스킨 `member/login.html` + 회사 `public/media/akkome_login_banner.mp4`(커밋 `b32816b`).
  - 시안 = `디자인\아꼼이네_로그인_시안 (1).html`(105KB — 영상이 base64로 내장). **구조 = 영상 배너 히어로 → 소셜 최상단 → 아이디/비번은 아코디언 → 하단 혜택 6종**.
  - 🔴 **영상은 파일로 분리**: base64는 캐시가 안 돼 매 접속마다 74KB를 다시 받는다. 카페24는 `.mp4` 업로드를 막으므로 **회사프로그램에서 서빙**(`/media/akkome_login_banner.mp4` · 73.7KB · **faststart 확인** · 5.0초 loop). #319와 같은 경로.
  - **이식 원칙(#238 핸드오프 방식)**: 마크업을 새로 만들지 않고 **있는 요소를 옮기고 감쌌다** — `.login__sns`를 fieldset 맨 앞으로 이동, 아이디·비번·로그인·찾기링크를 `.akm-accbox`로 감싸 접음. **입력 필드는 fieldset 안에 그대로**라 전송 경로·검증 JS 무접촉(실측 확인). 로그인 실패로 되돌아오거나 아이디가 남아 있으면 **자동으로 펼친다**(접힌 채면 뭘 고칠지 못 본다).
  - **#346-b 시안 대조 지시 반영**: 상단은 「‹ 로그인」 한 줄만(레이아웃 헤더·부제 제거) · 「아이디 저장」→**「로그인 유지」** · **「보안접속」 삭제** · 체크박스 = 시안 초록 라운드.
    - 🔴 **부제 「제주에서 우리집까지…」는 카페24 `#header`가 아니라 우리가 #240-③에서 만든 `.akm-hdr`에 있었다**(실측). 그 66px가 상단 여백의 정체. 셀렉터를 추측하지 말 것(#316 재확인).
    - 뒤로가기는 새로 만들지 않고 **레이아웃 표준 앵커(`{$go_back}`)를 옮겨** 썼다.
  - 🔴🔴 **곁가지에서 잡은 기존 결함 2건(둘 다 이번 변경 이전부터)**
    - ⓐ **「비회원 구매」 버튼이 눌러도 아무 일도 안 일어났다** — 카페24가 `displaynone`으로 숨겨둔 껍데기(href·onclick 없음)를 **#240의 `.btnNormal{display:block}`이 이겨서 되살려 놓은 것**(#267 주소록과 같은 계열). → `.displaynone`이 붙었을 때만 숨기게 교정(#340-b 교훈).
    - ⓑ **비회원 주문조회가 완전히 막혀 있었다** — [회원/비회원] 탭이 `display:none`이라 손님이 전환할 방법이 없었다. 탭 전환 로직 자체는 멀쩡(숨은 링크를 click하면 주문번호 칸이 나온다 — 실측) → 하단에 「비회원 주문 조회」 링크 신설 + `?noMemberOrder` 감지 시 **그 표준 링크를 눌러준다**(폼 재조립 금지).
      - ⚠️ **`?noMemberOrder` 단독으로는 폼이 아예 렌더되지 않는다** — `/myshop/order/list.html`로 보내야 카페24가 `&returnUrl=…`를 붙여 주문번호 칸까지 그린다(실측). 링크는 그 주소로.
  - 실측 **127/127**(`verify-346-login.js` — 아이폰390·안드로이드360·PC1440 + 비회원 조회): 상단 바·부제 소멸·영상 4속성/실로드/프레임 이동·순서·아코디언 펼침접힘·**입력 필드 fieldset 보존**·실타이핑·혜택 6종·죽은 버튼 숨김·비회원 주문번호 칸 노출·가로 넘침 0·에러 0. 캡처 `scripts/login_shots/`.
  - 🚨 **검증 자책 2건**: ⓐ영상이 **loop이라 currentTime이 되감기는데** "t가 커졌는가"로 재서 **정상 재생을 3번 실패로 표시**했다 → `paused` + 프레임 이동으로 판정. ⓑ`?noMemberOrder`만 붙이면 되는 줄 알고 검증 경로를 잘못 잡아 2회 헛돌았다.
  - ⚠️ **남은 확인 1건**: 「**로그인 유지**」 — 실제 기능은 **아이디 저장**(`check_save_id`)이라 다음 방문에 비밀번호는 다시 넣어야 한다(대표 지시로 글자만 교체·되돌리기 한 줄).
  - ✅ 카카오톡 채널 「회원혜택」 링크는 **대표가 직접 수정**(8/10).
  - 🟢 **#346-c(대표 캡처로 사실 정정): 「회원가입 축하 쿠폰 2,000원」이 발급중이었다**
    - 🔴 **내 기록이 낡아 있었다** — #303-c의 "신규 가입 쿠폰 없음"을 그대로 믿고 헤드라인이 거짓이라 보고했는데, 대표 [발급중인 쿠폰현황] 캡처에 **회원가입 축하 쿠폰 2000원(발급일로부터 14일)** 이 있었다. 리뷰 500원(포토·텍스트)·생일 쿠폰도 함께 발급중. → **헤드라인 「지금 가입하면 바로 받는 혜택 / 물방울 5개 + 2,000원 쿠폰」은 사실과 일치**. 교훈: **혜택 사실은 기록이 아니라 그 시점 관리자 화면으로 확인할 것**(설정은 대표가 언제든 바꾼다).
    - **혜택 목록 = 7종으로 통일**(대표 "들어가는 곳 모두 통일"): 회원가입 쿠폰 2,000원 → 물방울 5개 → 생일 쿠폰 2,000원 → 귤나무 → 나침반 → 후기 → 시즌 알림. 🔴 **적용 3곳 = `member/login.html`(혜택 카드) · `index.html` 홈 회원혜택 시트(`#benefitDim .bn-row`) · `member/join_result.html`(가입완료 콜아웃)**. **하나를 고치면 셋 다 고칠 것**(코드 주석에 명시).
  - 🔴🔴 **#346-d(대표 실물 — 내 회귀 2건 + 기존 결함 1건)**
    - ⓐ **「마이」를 누르면 비회원 조회 화면만 떠서 회원 로그인을 할 수 없었다** — 마이·주문내역 탭은 `/myshop/order/list.html`로 가는데 비로그인이면 카페24가 **`?noMemberOrder&returnUrl=…`을 붙여** 로그인 화면으로 보낸다. 그 파라미터는 "비회원 조회도 가능한 화면"이라는 뜻일 뿐인데 내가 "비회원으로 왔다"로 해석해 강제 전환했다. → **URL로 판단하지 않는다**: 손님이 「비회원 주문 조회」를 직접 눌렀을 때만 `sessionStorage('akm_guest_intent')`로 의도를 넘기고 1회 쓰고 지운다. 🔵 **교훈: 카페24가 붙이는 파라미터는 "가능성"이지 "의도"가 아니다.**
    - ⓑ **「로그인 유지」 체크가 안 됐다** — 두 겹이었다. ①**터치 영역이 20×20px**라 손가락이 못 맞춤(기능은 멀쩡 — 탭하면 checked=true였다) → 체크박스+글자를 한 덩어리로 묶어 **56px** 확보(보이는 크기 불변, #289 방식) ②**`:checked` 규칙이 매칭은 되는데 적용이 안 됨**(실측 `matches()=true`인데 배경 흰색 — 카페24 쪽 더 강한 배경 규칙) → `!important`. **눌러도 색이 안 변하니 "안 눌린다"로 보였다.**
    - ⓒ **PC 폭에서 푸터 두 줄이 겹쳐 글씨가 포개졌다**(기존 결함·전 서브 페이지 공통) — 1280px에서 재현(util y695~725 / copyright y698~717). 카페24 PC 푸터가 두 줄을 좌·우로 놓는데 우리는 480px 중앙 고정이라 자리가 없다. → `layout.html` PC 미디어쿼리에서 세로로 풀었다(모바일 무영향).
    - 실측 **195/195**(`verify-346-login` 126 · `verify-346d-my` 22 · `verify-345-login` 33 · `verify-346c-unify` 14): 마이 탭 → 회원 화면·비회원 링크만 비회원 모드·1회성 소멸 · 체크박스 4지점 탭 전부 토글·초록 표시 · PC 로그인/장바구니/약관 푸터 겹침 0·480px 중앙 유지·모바일 무회귀.
    - 🚨 **검증 자책 2건**: ⓐ`.bn-row b`로 세다가 **본문 강조용 중첩 `<b>`까지 잡아** 7종을 8종으로 오판 ⓑ영상 loop 되감김을 "정지"로 오판(#346 기록 참조).
- 🟢 **#345(대표 8/10): 네이버 로그인·가입 되살리기 + 「3초만에 시작하기」 문구** — 스킨 전용. 파일 = `member/login.html`·`member/agreement.html`.
  - ✅ **선행 실측이 전제였다**: #250에서 네이버가 "서비스 설정 오류"라 **일부러 숨겨둔** 버튼이다. 되살리기 전에 실제로 되는지부터 쟀고 — **지금은 정상**(팝업이 `nid.naver.com/oauth2.0/authorize`로 진입, 인가 화면에 앱명 **제주아꼼이네** 노출). 그 사이 설정이 완료된 것.
    - 연동값 실측: `client_id=k9V7xvQeSeXMFB1hiD22` · `redirect_uri=https://akkome.com/Api/Member/Oauth2ClientCallback/naver/` · `scope=login userinfo`.
    - **Apple은 숨김 유지** — 미확인 채널(#250에서 invalid redirect). 카페24는 미사용 SNS에 `displaynone`을 스스로 붙이는데 **네이버·애플엔 안 붙어 있다**(= 관리자에선 둘 다 켜짐) → 클래스만 보고 "된다"고 판단하면 안 된다. **실제로 눌러 인가 화면까지 가봐야 안다.**
  - 🔴 **되살리자마자 드러난 배치 결함**: `.login__sns`가 **가로(flex row)** 였다 — 카카오 하나뿐일 땐 혼자 전폭을 먹어 안 드러났는데, 네이버가 켜지자 **둘이 좌우로 반씩 나눠 가졌다**(실측 각 162px·같은 줄). → `flex-direction:column` 강제. **버튼을 하나 더 켤 땐 그 컨테이너의 방향을 먼저 볼 것.**
  - 문구: 카카오·네이버 모두 **「○○로 3초만에 시작하기」**(대표 지정 — 걸리는 시간을 박아 진입 부담을 낮춤). 네이버 아이콘은 **인라인 SVG N 심볼**(에셋 업로드 불필요). `#280`의 `margin-bottom:0`을 덮어야 해서 **#345 블록이 뒤에 와야** 한다(같은 특이도 = 마지막 선언 승 — #332 교훈).
  - 🔴 **가입 화면(agreement/join)엔 SNS 진입점이 아예 없었다** — 카페24는 SNS 가입을 **로그인 화면 버튼 하나로만** 태운다. 그 화면에선 `MemberAction.snsLogin`이 **undefined**(실측)라 함수를 흉내 낼 수 없다(페이로드 재조립 금지 원칙). → 가입 화면에 버튼을 두되 **`/member/login.html?akmsns=naver`로 보내고, 도착하면 그 버튼으로 자동 스크롤·강조**(`#345-b`).
    - ⚠️ **자동 클릭은 하지 않았다**: 네이버 로그인은 **팝업 방식**이라 사용자 제스처 없이 열면 브라우저가 차단해 **"눌러도 아무 일이 없는" 상태**가 된다. 짚어주고 손님이 누르게 하는 편이 확실하다.
  - 실측 **50/50**: `verify-345-login.js` **33/33**(폰390·PC1440 — 문구·N 심볼·높이 58px·같은 폭·간격 8px·겹침 0·덮개 0·onclick 무접촉·Apple 숨김·폼 무회귀·**실탭 → 네이버 인가 화면 진입**) + `verify-345b-join.js` **17/17**(가입 화면 버튼·링크·약관 무회귀 · 실탭 → 로그인 화면 도착 → 자동 스크롤 → **네이버 인가 화면 완주**).
  - 🔴 **대표 확인 필요(내가 못 하는 마지막 한 칸)**: **네이버 계정으로 실제 가입이 끝까지 되는지**는 실계정이 있어야 확인된다. 막히면 볼 곳 3군데 — ①**네이버 개발자센터 > 내 애플리케이션 > 검수 상태**(「개발 중」이면 **등록된 테스트 계정만** 로그인되고 일반 손님은 실패 → "서비스 적용" 신청 필요) ②**카페24 [회원 > SNS 로그인 설정]의 네이버 회원가입 허용** ③네이버 앱의 **제공 정보(이메일·이름 등)** 설정.
  - 🔵 되돌리려면: `login.html`의 `#345` 블록 삭제 + `#250` 숨김 목록에 `.btnNaver` 재추가 / `agreement.html`의 `#345` 블록 삭제. ℹ️ 버튼 룩은 **임시** — 대표가 별도 디자인 진행 중.
- ⭐⬆️ **#344(대표 8/10): ①후기 최신순 정렬 ②「맨 위로」 버튼 신설** — 스킨 전용(회사프로그램 버전 무변동). 적용 5파일 = index·farm·roulette·calendar·layout/basic/layout.html.
  - **① 후기 최신순**(대표 원문: "과거도 너무 많이 있어서 최신순으로 · 최신날짜여야 손님 신뢰가 올라간다") — 지적이 실측으로 사실 확인됨: 종전 롤링 15건 날짜 = `2026-03-26 · 2025-12-14 · 2026-05-06 …`.
    - 🔴 원인 = #131 `rollPick`(상품별 라운드로빈). ⓐ우선순위로 쓰던 **BEST/재구매 라벨은 네이버 응답에 아예 없어**(400건 중 보유 **0건**) 한 번도 작동한 적이 없고 ⓑ"상품 골고루"를 맞추려 **오래된 후기를 앞으로 끌어왔다**. → **폐지**하고 롤링 = 최신 15건.
    - 적용 범위 = 홈 롤링 · 전체 400건 목록 · **상품 상세 리뷰**(배열 자체를 날짜 내림차순 정렬 — `D.reviews`/`D.prodReviews` 둘 다). 날짜는 `YYYY-MM-DD` 고정이라 문자열 비교로 정확(400건 전부 형식 일치·이상값 0).
    - ℹ️ 최신 15건 = 밤호박 10 · 감귤 3 · 황금향 2 = **현재 판매중 상품**이라 마케팅상으로도 유리. "여러 상품 골고루"를 다시 원하면 그때 대표 확정 필요(최신순과 상충).
  - **② 「맨 위로」 버튼** — 하단 바 **바로 위 오른쪽**에 뜨는 46px 원형(흰 배경·네이비 ^). 260px 이상 내려가야 나타나고, 오버레이(상세·챗·카테고리)가 열리면 숨는다.
    - 위치는 **숫자를 박지 않고 하단 바 높이를 실측**해 그 위에 놓는다(화면마다 탭바·주문 바·안내 카드가 달라 고정값은 반드시 어디선가 겹친다).
    - 터치는 **click만 믿지 않는다**(#292·#293) — `touchend`로도 처리하고 판정은 "뗀 지점이 버튼 영역(+24px) 안인가". 거리 임계값은 쓰지 않음.
  - 🔴 **시공 중 실측으로 잡은 결함 — 안내 카드가 버튼을 덮고 있었다**: 「홈 화면에 추가」 카드(`.akm-pwa`, z-index **140**)는 탭바보다 10px 위에 떠 있어 **화면 바닥에 닿지 않는다** → "바닥에 붙은 것"만 세던 내 배치 로직에 안 걸려 버튼이 카드 뒤로 들어갔다. 접점 실측 = **버튼 한가운데를 눌렀는데 카드의 ✕ 버튼이 잡힘**(#290 교훈 그대로). → 하단 **220px 구역**에 걸친 fixed 요소는 전부 하단 바로 본다(전면 딤은 높이 200px 초과로 제외).
  - 🩹 **동반 교정(범위 밖이지만 같은 뿌리)**: **「후기 숨기기」 FAB(#313)도 같은 카드에 덮여 눌리지 않고 있었다** — 이번 변경 이전부터의 결함. 첫 방문 손님이 후기 400건을 펼치면 접을 수가 없었다. 같은 계산으로 함께 올림(카드가 없을 땐 종전 위치와 동일 = 무회귀).
  - 실측 **71/71**: `verify-344-review-top.js` **56/56**(홈 롤링·전체·상세 날짜 내림차순·데이터 최댓값 일치 · 버튼 노출/숨김·탭바 겹침 0·실제 눌리는 위치·실탭 맨 위 도달 · farm·roulette·calendar·약관(layout)·장바구니·PC1440 · pageerror 0) + `verify-344c-pwa.js` **7/7**(카드 뜬 상태 겹침 0·이벤트 도달·카드 닫으면 복귀) + `verify-344d-revfab.js` **8/8**(후기 펼침 상태 두 버튼 겹침 0·둘 다 눌림).
  - 🚨 **자책 2건** ⓐ**패치 스크립트가 index.html 63KB를 삭제**했다 — 앵커 마커 `⬆️ #344`를 **리뷰 정렬 주석에도 같은 문자열로 써서** 엉뚱한 블록이 걸렸다. 업로드는 안 됐고(검사·업로드 분리 덕분), **SFTP로 서버 원본을 받아 복구**(신설 `scripts/download.js` — skin6 로컬 정본은 git 밖이라 이게 유일한 복구 수단). **앵커는 그 블록에만 있는 문자열로, 교체 전후 바이트 수를 찍어 확인할 것.** 1~2곳 수정은 스크립트보다 Edit 도구가 안전. ⓑ**1차 검증이 전부 PC로 돌았다** — Playwright 컨텍스트는 `viewport:{...}`인데 `width/height`를 최상위에 둬 조용히 무시되고 1280×720으로 돌았다(그 바람에 위 겹침 결함을 못 잡을 뻔). + 검증 중 **좌표 재측정 함정 재범**(3초 뒤 안내 카드가 뜨며 버튼이 72px 위로 이동 → 옛 좌표 헛탭).
  - 🔵 되돌리려면: 각 파일 맨 아래 `__akmTopBound` 블록 삭제(버튼) / `revNewestFirst`·`revFlat` 블록을 상품번호 순회로 되돌리고 `revCards.slice(0,15)`를 라운드로빈으로 교체(후기 정렬).
- 🎁 **#340~#343(대표 실사고 8/10): 룰렛 실물 당첨을 줄 수 없었다 → 지급 관리 탭 신설 + 티켓 기준일 8월 시작 (v5.9.248~251)**
  - 🔴 **사고**: 당첨 알림이 `회원 ●●●@k`로 와서 **누구에게 쿠폰을 줄지 알 수 없었다.** 원인 2겹 — ⓐ`tgMask`의 「5자리 이상 숫자=금액」 규칙이 **회원ID를 삼킴** ⓑ 알림이 안내하는 「데이터관리 게임 운영」에 **당첨자 목록이 아예 없었다**(`reward_grants` UI 0). 확인 통로가 하나도 없었다.
  - **문의 관리 > 「🎁 당첨 지급」 탭 신설**(고객문의 옆): ⏳미지급/📋전체 서브탭 · 기간·날짜·검색 필터 · `.data-table` — **[톡톡 문의] 탭 규격 그대로**(대표 지시). 미지급 건수 탭 배지 · [지급완료]/[되돌리기] · 감사 로그 · 직원 조회 가능.
  - **마스킹 교정**: 숫자가 글자·@·.·_·- **또는 다른 숫자**에 붙어 있으면 식별자로 보고 안 가림. 금액·전화·주문번호는 유지. 실행 판정 8/8.
  - 🎟 **티켓 기준일 = `mall_ticket_since`(2026-08-01)** — 대표 확정 "자사몰 리뉴얼 후부터". 종전 180일 소급이라 **리뉴얼 전 주문까지 티켓이 나갔다**(실측: 회원 `4790625123@k` 4~7월 배송완료 7건 → 티켓 7장·3회 사용). 적용 후 그 회원 티켓 7→0. **기지급 경품은 회수 안 함**(대표 방침).
  - **#341**: raw 러너에 `GET /api/v2/admin/orders` 허용(티켓 근거 추적). 가드 판정 7/7 · 호출 시 `fields`로 PII 제외 · 결과 행 즉시 삭제 · 카페24 주문 조회는 **구간 3개월 제한**.
  - 🚨 **자책 3건(전부 대표가 발견) — 같은 기능 하나에서**: ⓐ`api()`는 **위치 인자**(`api(url,'PUT',body)`)인데 옵션 객체로 넘겨 버튼이 죽음 ⓑ존재하지 않는 `aoToast`·`pill-warn` 사용(정적 검증에서 자력 발견) ⓒ**PostgreSQL `$2`를 값·비교에 함께 써 `inconsistent types deduced`** → `$2::text` 캐스팅.
  - 🔴 **가장 큰 교훈**: ⓒ를 1차에서 놓친 이유 = **라우트의 실제 SQL이 아니라 내가 새로 쓴 SQL로 왕복 테스트**했기 때문. CLAUDE.md 규약(실코드를 그대로 떼어 실행)을 지키지 않았다. 재검증은 `server.js`에서 SQL 문자열을 추출해 실DB 실행 → 3/3. 자동메모리 `jeju-company-app-conventions`에 등재.
  - ⚠️ 인라인 `node -e`로 CHANGELOG를 쓰다 **백틱·`$`가 소실**돼 문장이 비었다(문서화된 함정 재범) → Write 도구로 복구.
- 💬 **#339(대표 8/10): 톡톡 사전예약 대기 고객 안내 추가** — 대표 실물(7/12 청귤 예약 손님 "배송 날짜 대략이라도?")
  - 추가 문구 = 「📮 이미 사전예약 주문하시고 기다리고 계신 고객님은 **주문자 성함과 연락처**를 톡톡으로 남겨주시거나 📞010-6687-4031로 문자 주시면, 주문 순서를 확인해 대략적인 발송 예정일을 알려드릴게요!」 → 시나리오 **id=29(예약상품 순차발송)·id=39(품목-청귤)** 에 반영(둘 다 `channel='공통'`).
  - 🔴 **대표 지시 문구를 그대로 안 넣고 바꿔 넣은 이유(보고함)**: 지시는 "여기로 남겨주세요"였으나 `공통` 시나리오는 **공개 게시판인 상품문의에도 나간다** → 손님이 공개된 곳에 개인정보를 적게 된다. **"톡톡으로 남겨주시거나 문자"** 로 두면 톡톡에선 "여기"와 같은 뜻이고 공개 게시판에선 비공개 경로로만 유도된다.
  - 실측(`scratchpad/sim-preorder.js` → `recheck-sim.js` **5/5**): 캡처와 같은 질문에 새 안내 실제 출력 · 「예약했는데 언제 오나요」에도 출력(AI가 자기 말로 재작성) · **주문 전 손님 질문엔 성함 요구 없음**(불필요한 개인정보 요구 회피) · 공개 유도 문구 0.
  - ⚠️ **검증 자책**: 1차 판정이 문장을 **글자 그대로** 찾아 거짓 실패 1건. AI는 같은 내용을 자기 말로 바꿔 쓴다 — **의미 요소로 판정**할 것.
  - 📋 **전환 대비 전수 조사 완료**(대표 요청): 청귤 사전예약→일반 발송 전환 시 손볼 곳 = 시나리오 **id 29·39·45** · `bot_products` **46~49**(`reserve_ship_start=2026-08-18` — 경과 시 자동 일반 복귀라 무손) · 스킨 「사전예약특가」 7곳(상품 카드 **빨간 딱지** `.p-flag red` 1 + 상품명·캘린더 버튼) · ⚠️`product_season_knowledge`에 **청귤 구간 0건**(등록해두면 시기 전환이 자동). 자동메모리 `jeju-cheonggyul-season-switch` 에 동일 기록.
- 🧮 **#338(대표 GO 8/10): 일일 스냅샷에 네이버 옵션 수집 → 카페24 기준값 자동 갱신 (v5.9.247)**
  - **배경 = #337에서 터질 뻔한 사고**: 카페24 기본가 공식 「네이버 최종가 + min(옵션 증감액)」의 min을 **7/30 시점으로 고정 저장**해 쓰고 있었다. 밤호박 특품 종료로 min이 −11,000 → −5,000이 됐는데 방치했으면 **다음날 05:10 fix가 14,800원을 8,800원으로 내렸다**(±50% 가드 미작동 −40.5%).
  - 시공 = **새 타이머 없음**. 기존 04:30 수집 / 05:10 반영에 **재료만 추가**: 목록 API엔 옵션이 없어(실측) **상품별 단건 조회** 20콜·400ms(+8초). `usable` 포함 저장·옵션 없는 상품은 `optsNone`.
  - 동기화는 저장값 대신 **그날 옵션에서 기준값 계산**(종료 옵션 제외) → 바뀌면 매핑표 갱신 + 텔레그램 「기준값 자동 갱신 c91 −11000→−5000」.
  - 🔴 **안전장치: 옵션을 못 받은 상품은 가격 보정을 통째로 건너뛴다**(`optSkip`). 종전엔 재료가 낡아도 그냥 계산했다.
  - **보관 정리 신설** — `naver_product_snapshot`에 자동 삭제가 없어 무한 증가하던 것 → 90일 경과분 삭제.
  - 실측: 로직 **15/15**(실 server.js 블록 1,025b 추출 실행) + 실가동 **9/9**(재수집 → 옵션 20종 확보 · 밤호박 6종 · 기준값 −5000 자동 산출 · 기본가 14,800 일치 · dry 동기화 19종 대조 차이 0 · **공개 API에 옵션 노출** = 자사몰이 바로 읽을 수 있음).
  - 🔴 **카페24 API 한계(테스트 상품으로 실측·삭제 완료)**: 옵션 **이름변경·추가·전면교체 전부 거부**(`Existing option value is missing` / `사용 중인 옵션은 재등록 불가`). → **옵션 구성 변경은 자동화 불가**. 자동 = 가격·품절·추가금·종료분 미노출까지. **신규 옵션은 알림 후 관리자에서 사람이 추가**해야 한다.
  - ⚠️ 검증 중 실측: 리뷰·공개지표 수집이 `naver_public_error_429`로 실패(직전분 유지 — 화면 무영향). 중계서버 **동시 실행 금지**(#310) 때문으로 보이며, 짧은 간격 재수집 시 발생. 정규 04:30 단독 실행에선 정상.
- 🥔 **#337(대표 8/10): 미니밤호박 옵션 동기화 — 네이버 특품 3종 종료 반영**
  - 카페24 9종 → **6종**(특품 5·3·10kg 미노출). 🔴 **카페24는 옵션 삭제·이름변경·추가를 API로 전부 거부**(테스트 상품 실측) → `variant`를 `display:'F'·selling:'F'` 로 내려 처리(되살리기 = 'T').
  - 손님 결제가 **6종 전부 불변**(14,800/19,800/29,800/30,800) — 바뀐 건 정가 표시뿐(48,800→37,800). 기준 옵션이 특품5kg → 꼬마5kg로 내려온 결과.
  - 🔴 **가격 오하향 직전 차단**: `cafe24_sync_map`의 minAdd가 −11,000 고정이라 **다음날 05:10에 14,800 → 8,800원**이 될 뻔(±50% 가드 미작동) → −5,000으로 교정(이후 #338로 자동화).
  - **표기 = 「보이는 글자 / 찾는 글자」 분리**(대표 확정 방식): 카페24 이름을 못 바꾸므로 `STORE_DATA.opts[].n1`=카페24 저장값(매칭 전용)·`nd`=네이버 현재 표기(화면). 장바구니·주문서·주문내역은 `layout.html`·`orderform.html`의 **표시 치환 스크립트**(`__akmOptDispBound`)가 텍스트 노드만 바꾼다 — **폼 전송값 무접촉이라 주문 데이터·발주·송장변환 원본 유지**.
  - 실측: 자사몰 **16/16**(옵션 6종·가격 6개 일치·실구매 완주 장바구니 19,800원) · 카페24 상품 페이지 **4/4**(특품 소멸) · 표기 분리 **12/12** · 장바구니 표시 **8/8**.
  - ⚠️ 관리자 화면·발주서는 원본(「3. …」) 유지 — 대표 확정 "굳이 변경 불필요".
- 🏢 **#333·#334(대표 "다른 곳은 다 있는데 우린 없다"): 하단 사업자정보 신설 → 접이식(온브릭스 스타일)으로 개편**
  - **실태 확인**: 장바구니·로그인 등 layout 계열엔 카페24 기본 푸터로 **이미 있었고**, 우리가 직접 만든 **홈·귤나무·나침반·캘린더 4화면에만 없었다**(손님이 가장 많이 보는 곳). → 4화면에 신설.
  - 🔴 **값은 하드코딩 0** — 관리자 [상점관리 > 기본정보 관리 > 내 쇼핑몰 정보] 등록값을 `Layout_footer` 치환코드로 받는다(관리자에서 고치면 화면도 따라감). **치환 실패 시 「{$company_name}」 날코드가 손님에게 보이므로 `/\{\s*\$/` 감지 시 통째로 숨기는 가드** 내장. 미등록 항목(빈 값)은 라벨만 남으므로 JS로 그 줄 제거.
  - **#334 개편(대표 지시)**: 「🍊 제주아꼼이네 농업회사법인(주) 사업자 정보 ▼」 한 줄 → 누르면 대표부터 펼쳐짐. 상호 「(주)」는 이름을 적지 않고 **등록값 끝의 "주식회사"를 정규식으로 변환**(상호가 바뀌어도 따라감). 삭제 = 개인정보보호책임자·회사소개·사업자정보확인.
  - ⚖️ **법적 판단(대표 요청분 중 2건 거부·근거 보고)**: **호스팅제공자**(전자상거래법 시행령 제10조)와 **전자우편주소**(법 제10조①3호)는 **법정 표시사항이라 삭제 불가** → 지우는 대신 **접힘 안쪽으로** 넣어 화면은 깔끔하게. 개인정보보호책임자 삭제는 **개인정보처리방침에 성명·소속·전화·이메일이 정식 기재된 것을 실측 확인**하고 진행(개인정보보호법 제31조 충족). 운영시간은 법정 항목 아님 — 미등록이라 화면에서 자동 제외 중.
  - 실측(`verify-343-biz.js` 48/48 · `verify-344-bizfold.js` **55/55**): 4화면 치환 성공·날코드 0 · 기본 접힘 → 실탭 펼침/재접힘 · 등록값 일치(422-81-04127·2026-제주삼양-0002) · 법정 2종 존치 · 링크 3종 200 · 가로넘침 0 · 홈 무회귀 · 에러 0.
  - 🚨 **검증 자책 3건(전부 거짓 통과가 날 뻔함 — 재발 방지)**: ⓐ**`scrollIntoView({block:'end'})`는 요소를 뷰포트 맨 아래에 붙여 무조건 탭바 아래로 넣는다** → 교정 후에도 4화면 전부 실패로 나옴. 실조건은 `window.scrollTo(0, scrollHeight)` ⓑ**`.bz-row:last-child`가 다른 div로 감싸인 중간 줄(대표전화)을 먼저 잡아** "안 가림" 거짓 통과 → 진짜 마지막은 `[...qsa()].pop()` ⓒ**자동 스크롤로 버튼이 이동했는데 옛 좌표로 두 번째 탭**을 날려 "접힘 실패" → 좌표 재측정(문서화된 함정 재범).
  - 동반 교정: 저작권 줄이 탭바에 가려 있어 **푸터가 탭바 높이만큼 아래 여백을 갖게** 하고 부모의 탭바용 여백은 `:has()`로 해제(미지원 브라우저는 여백만 넓어질 뿐 무해). 페이지 맨 아래에서 펼치면 내용이 화면 밖으로 나가 **펼친 직후 자동으로 끝까지 스크롤**.
  - ⚠️ **대표 확인 대기**: 관리자 등록값이 사업자등록증·통신판매업 신고증과 일치하는지는 대표만 대조 가능.
- 📳 **#332(대표 "탭 누를 때 폰 진동처럼 알림 오게 할 수 있어? 앱 받아야 하나?"): 햅틱 피드백 신설**
  - **답 = 앱 불필요. 단 안드로이드만.** 웹 표준 `navigator.vibrate()`는 안드로이드 크롬/삼성인터넷 지원, **아이폰 사파리는 미지원**(애플이 웹에 안 열어줌 — PWA로 설치해도 WebKit이라 동일). iOS 17.4+ 한정 `<input type="checkbox" switch>` 클릭 트릭을 폴백으로 넣었고, 안 되면 조용히 무시.
  - 대상 = **탭바 5개 · 카테고리 아이콘 10개 · 물주기 · 돌리기 · 상세 구매하기**. 손가락이 닿는 순간(`pointerdown`) 12ms, 뒤따르는 click은 700ms 도장으로 중복 차단, **마우스는 제외**. 적용 6파일(index·farm·roulette·calendar·layout·akm_frame).
  - 🔵 **전체 OFF 스위치 내장**: `localStorage.setItem('akm_haptic_off','1')`. 세기 조정은 12ms 숫자 한 줄.
  - 실측(`verify-342-haptic.js` **19/19** + `verify-342b-pd.js` 3/3 — `navigator.vibrate` 후킹해 호출 발생으로 판정): 탭 5개 각 정확히 1회(중복 0)·카테고리·물주기·돌리기 각 1회 · 빈 영역 0 · OFF 스위치 동작 · PC 마우스 0회 · 무회귀·에러 0.
  - ⚠️ 검증 함정: 탭하면 페이지가 이동해 측정 컨텍스트가 끊긴다 → **링크 이동만 막고(capture preventDefault)** 진동은 세도록. 앞선 탭이 오버레이(주문내역·마이)를 열어 탭바를 덮으므로 **탭마다 새로 로드**.
- 🧭 **#331(대표): 나침반 돌리기 화면 리디자인 — 통짜 원판 → 레이어 조립**
  - 에셋 6종(`디자인\나침반\1~6.png`) → `akkome_wheel_frame`(720×772·181KB)·`_pointer`·`_hub`·`akkome_prize_water`·`_coupon`·`_box`. 전부 200KB 이하 PNG + WebP.
  - 구조: **회전 대상 = `#wheelRot`**(프레임 이미지 + 칸 SVG가 함께 회전) · 칸은 **반투명**이라 프레임의 크림 면이 비침 · 허브 정중앙 고정(z3) · 바늘 상단 고정(z4). 종전 노랑 테두리 div·중앙 실사 아꼼이 제거.
  - 배경 = 네이비 단색 → **귤나무 화면과 같은 크림 그라데이션**(#FFFDF2→#FFF8DC→#FDF3C9), 흰 글자 전제였던 뒤로가기·물방울 칩·안내문·티켓 배지를 전부 네이비로.
  - 연출: 회전 중 금빛 전구 점멸 → ease-out 감속 → 정지 시 **바늘 bounce + 골드 파티클 14개**.
  - 🔴 **대표 지시는 8칸이었으나 6칸으로 진행** — 확정 경품이 6종이고 확률 합이 100%라, 8칸은 **없는 경품 2개를 지어내야** 한다(#303-c 원칙 위반). 8칸을 원하면 **추가 경품 2종·확률 재배분 확정 필요**.
  - 실측(`verify-341-wheel.js` **27/27**): 각도 공식 역산 6/6 + **실스핀 3회 전부 바늘 아래 칸 = 결과 문구 일치**(151°→5%쿠폰, 198°·214°→10%쿠폰) · 라벨 뒤집힘 교정(아래 반원 제자리 180°) · 에셋 404 0 · 에러 0.
  - ⚠️ **카페24가 webp 3장(frame·hub·pointer)을 지웠다** → **소량(3개)·1초 간격 재업로드로 전량 복구**(#326의 14개 동시 업로드 소실과 대조 — 개수/속도가 원인으로 보임). PNG `onerror` 폴백은 항상 배선할 것.
  - 동반: 리뷰 섹션 「(가격·재고 매일 갱신 · 리뷰 8.9 기준)」 삭제(앞에 날짜·시각 중복) · 하단 탭바 나침반/귤나무 아이콘을 `cat_roulette.png`·`cat_tree.png`로 통일(5파일).
- 🧭 **#330(대표): 「행운 룰렛」 → 「행운 나침반」 문구 통일 + 메인 카테고리 나침반 아이콘 이미지 적용**
  - 아이콘 = **`assets2/cat_roulette.png`**(#327의 「나침반 돌리기」 카테고리와 **같은 파일**) — 새 에셋을 만들지 않아 메인·배너·퀘스트가 한 그림으로 통일된다. 이모지 자리에 img를 끼우고 크기만 CSS로 고정(배너 44px·제목 앞 19px·혜택 시트 20px·퀘스트 30px).
  - 교체 4곳: ⓐ홈 혜택 배너 b2(제목+우측 큰 아이콘) ⓑ회원혜택 시트 `.bn-row` ⓒ귤나무 퀘스트(버튼도 「나침반 보기」) ⓓ**roulette.html 본문**(보유 뱃지·「나침반권 얻는 법」·하단 안내·JS 뱃지 문자열) — 대표가 지목한 건 ⓐⓒ지만 같은 게임을 다른 이름으로 부르면 다시 지적될 문구라 손님 노출분을 전부 통일(주석·변수명은 무접촉).
  - ⚠️ **동반 교정(범위 밖이지만 실제와 어긋나 있던 것)**: 퀘스트 설명이 아직 「구매 1회마다 룰렛 1번」 — 실정책은 **배송완료 1건당 1회**(#231 대표 확정)라 「배송완료 1건마다 1번」으로 바로잡음(#307 원칙: 고객 노출 문구는 실지급 로직과 1:1).
  - 🚨 **병행 세션 충돌 확인 기록**: 작업 중 다른 세션이 **같은 roulette.html을 #331(휠 프레임 개편)로 수정**했다. 로컬 파일이 내 수정본 **위에** 얹혀 있었고 서버본에도 양쪽이 다 들어 있음을 실측(`wheel-frame` 2건 + 내 문구 3건) → **roulette.html은 재업로드하지 않음**(로컬이 서버보다 최신 = 병행 세션 영역). 같은 폴더 병행 시 **업로드 전 로컬 수정시각·서버 실물 양쪽을 확인할 것.**
  - 실측(`scripts/verify-342-compass.js` **26/26**): 배너·시트·퀘스트 아이콘 **naturalWidth 208 실렌더**(HTTP 200이 아니라 실렌더로 판정) · 홈·귤나무·나침반 3화면 "룰렛" 잔존 0 · 퀘스트 버튼/배너 클릭 → 나침반 페이지 실도달 · 홈 무회귀(상품11·카테고리10·영상3)·에러 0. 캡처 `scripts/compass_shots/`.
  - ℹ️ 배너 제목 앞 아이콘은 19px이라 나침반 형태가 또렷하진 않다(종전 🎰 이모지와 같은 자리·크기). 답답해 보이면 그 img 한 줄만 빼면 된다.
- 🏷️ **#329(대표): 귤나무 6단계 이름 교체 — 청귤→「풋귤(청귤)」 · 반숙→「수확직전」 · 완숙→「감귤수확」**
  - 수정 지점은 `farm.html` **두 곳뿐**: 하단 단계 칩 마크업 + `TREE_LV` 배열. 뱃지(`#lvlBadge`)·다음 단계 안내(`#lvNext`)·승급 토스트는 전부 `TREE_LV[].name` 참조라 **자동 반영**(하드코딩 0 — 전 스킨 파일 grep으로 farm.html 외 사용처 없음 확인). 그림·물 기준치(0/10/25/45/70/100)·레벨 로직 **무접촉**.
  - ⚠️ 「풋귤(청귤)」은 6칸 칩 중 가장 긴 이름(`white-space:nowrap`) → **글자 실폭을 Range로 측정해 잘림 검사**: 42 / 가용 52.3px(다른 칸 17~35px). 여유는 10px뿐이라 이보다 긴 이름을 넣으면 잘린다.
  - 대표 폰 캐시 대비로 index.html 진입 링크 꼬리표 **?v=305 → 306**(12곳) 동반 갱신(#271 전례 — 폰이 구버전 farm.html을 물고 있던 사고).
  - 실측(`scripts/verify-341-lvname.js` **37/37** · `verify-341-smoke.js` **10/10**): 6레벨 각각 실제 재로드해 칩·활성위치·뱃지·다음 단계 안내 전수 일치 · 옛 이름 3종 화면 잔존 0 · 칩 잘림 0 · Lv.6 나무/아꼼이 그림 무회귀 · 홈(상품11·카테고리10·영상3·탭5)·탭 진입 실도달 · 에러 0. 캡처 `scripts/lvname_shots/`.
- 🧒 **#328(대표): 귤나무 게임 아꼼이 캐릭터 6단계 — 나무 왼쪽 배치**
  - 에셋: `디자인\귤키우기1~6.png`(1024²) → `assets2/akkome_char_lv1~6.{png,webp}` · **30~43KB**(요청 150KB 이하) · bbox 크롭(여백 제거 → 크기 제어 정확).
  - 🔴 **나무(#326)와 다른 난이도 — 옷이 배경과 같은 크림색**: "밝고 무채색" 판정을 쓰면 베이지 바지·연노랑 원피스가 함께 날아간다. → **네 모서리에서 실제 배경색을 샘플링**하고 그 색과의 **거리**로 판정(옷은 거리가 벌어짐). 실측 배경색 rgb(248,245,227) 등.
  - 🔴 **바닥 그림자가 채도 40대였다**(실측 rgb(211,200,182)) — "무채색" 2차 패스로는 안 잡혀 **흰 타원이 계속 남았다**. 결정적 구분은 **「배경색을 균일하게 어둡게 한 값인가」** — 채널별 비율 편차가 그림자는 0.02, 옷은 0.1+. 이 조건으로 2차 패스를 돌려 흰 타원·lv6 흰 띠를 전멸시켰다. **감으로 임계를 잡지 말고 픽셀값을 측정할 것**(`scripts/char-probe.js`).
  - **배치**: 기존 오른쪽 하단 2D 아꼼이 제거 → **나무 왼쪽**(캐릭터가 오른쪽을 보는 그림이라 나무를 응시하는 구도). 레이어 **배경 → 아꼼이(z1) → 나무(z2)**. 레벨 승급 시 아꼼이도 함께 bounce.
  - 🔵 **WebP는 이번엔 전부 살아남았다**(12/12) — #326에서 지워졌던 건 **14개를 한꺼번에 올려 일부가 유실**된 것으로 보인다. 캐릭터는 webp 우선 + png 자동 폴백(`onerror`)으로 배선. 나무도 나중에 재업로드하면 살릴 수 있다.
  - ⚠️ **검증 기준 교정(중요)**: 1차 검증이 "발끝 11px 어긋남·겹침 35px"로 실패를 냈는데 **실화면은 정상**이었다 — 나무 PNG에 **좌우·아래 여백**(아래 4.8~7.3%, 좌 9~18%)이 있어 **img 경계 ≠ 실제 나무**이기 때문. → 알파 bbox(`scripts/tree-bbox.js` → `tree-bbox.json`)로 **진짜 윤곽**과 비교하도록 교정. 이미지 위에 무언가를 정렬할 땐 **여백을 반드시 실측**할 것.
  - 실측(`scripts/verify-340-char.js` **10/10** · 격자 `scripts/char-levels.png`): 6단계 교체 · **발끝-잔디선 차 -1/-4/3/-6/0/0px** · 발끝 높이 6단계 동일 · **나무와 간격 30/6/10/29/16/5px(겹침 0)** · 크기 39%로 6단계 동일 · 레이어 순서 · 로드 실패 0 · 에러 0.
- 🎨 **#327(대표): 메인 카테고리 아이콘 8종 교체** — `디자인\메뉴텝 *.png`(각 1.25~1.5MB) → **208×208·56~73KB**로 리사이즈해 `assets2/cat_*.png`.
  - 대상: 감귤류·채소·호박·선물세트·특가할인·리뷰보기·시즌캘린더·나침반 돌리기·귤나무 키우기 (+#325 감귤체험) = **9종이 신규 아이콘**. **오션라운지만 종전 유지**(대표 미제공).
  - 교체 방식: 각 버튼을 **라벨 텍스트로 특정**해 그 안의 첫 `img`만 치환(`scripts/patch-cat-icons.js`) — 다른 버튼 오염 방지. **뒤에서부터 치환**해 인덱스 밀림 방지.
  - 🔵 **부수 효과**: base64 인라인 아이콘 5개가 파일 참조로 바뀌며 **index.html 1.95MB → 1.58MB**(#325 포함 누계 **약 370KB 감소**). 남은 인라인은 오션라운지 1개(49KB)뿐.
  - 실측(`scripts/verify-339-cats.js` **8/8**): 9종 전부 `cat_*.png`·208×208 실렌더 · 카테고리 10개 유지 · 로드 실패 0 · 감귤류 클릭 → `#c/citrus` · 귤나무 클릭 → farm.html · 홈 무회귀(상품 20·영상 3·넘침 0)·에러 0. 캡처 `scripts/cats.png`.
- 🌳 **#326(대표): 귤나무 키우기 게임 — 신규 에셋 6단계 적용 (배경+나무 레이어)**
  - **에셋 가공**: `디자인\귤1~6.png`(1024²·1.2~1.8MB) 배경 제거 → `assets2/akkome_tree_lv1~6.png`, `귤배경.png` → `akkome_tree_bg.png`. 전부 **200KB 이하**(나무 36~88KB·배경 147KB).
    - 🔴 **단순 colorkey 금지**: 배경에 비네팅 그라데이션이 있어 임계를 키우면 잎의 밝은 반사까지 날아간다. **가장자리에서 flood fill**로 "바깥과 연결된 영역"만 지운다(나무 내부의 밝은 픽셀 보존).
    - 🔴 **바닥 그림자는 그냥 남기면 「흰 접시」가 된다**(크림 배경 위 그림자라 오려내면 밝은 회색 타원). → 2단 판정으로 **순수 배경=투명 / 그림자=반투명 검정**으로 변환 → 잔디 배경 위에서 자연스러운 그늘. 1차 시도에서 **lv6 좌하단 배경이 통째로 남은 것**도 이 완화된 임계로 해결(자홍 배경 QA 이미지로 눈 확인).
    - PNG 200KB는 **팔레트화(520px·256색·sierra2_4a 디더·alpha_threshold)** 로 달성(트루컬러는 450~714KB).
  - 🔴 **WebP는 포기**: 요청대로 동시 생성해 14개를 올렸으나 **카페24가 서버에서 webp를 제거**했다(업로드 성공 → 서버엔 8개만·webp는 lv1만 잔존, PNG도 자체 재압축). **PNG 단일 사용**으로 확정(36~88KB라 실익 손실 없음). ⚠️ 앞으로 스킨에 webp를 쓰지 말 것.
  - **화면 구조**: 합성 없이 **배경 고정 + 나무 절대위치**. 나무는 `bottom` 기준 중앙 정렬이라 **레벨이 바뀌어도 밑동이 안 움직인다**(실측 편차 **0px**). 레벨 변경 = `img.src`만 교체.
  - **레벨 기준(대표 확정)**: Lv.1 묘목 0 · Lv.2 새싹 10 · Lv.3 꽃봉오리 25 · Lv.4 청귤 45 · Lv.5 반숙 70 · Lv.6 완숙 100(수확). 단계 표시도 4칸 → **6칸**으로 교체.
  - 🔴 **종전 논리 오류 해소**: 뱃지는 "Lv.1 씨앗"인데 그림은 다 자란 나무 🌳였다 — 이제 단계마다 실제 그림이 바뀐다.
  - **연출**: 물 줄 때마다 물방울 낙하 → 흙에 스며듦 → 나무 흔들림(±1°) → 초록 반짝임 7개 → 진행바 차오름 + **레벨 안 미세 성장**(`--grow` 최대 +16%). 승급 시 bounce 교체 + 빛 터짐 + 토스트("Lv.3 꽃봉오리가 되었어요!").
  - 실측: **6단계 전부 실렌더**(`verify-337-tree.js` **10/10** · 격자 `scripts/tree-levels.png`) — 밑동 편차 0px·중심 오차 0px·그림 교체·뱃지 일치·단계 추종·에셋 404 0. **연출**(`verify-338-waterfx.js` **9/9`) — 낙하·반짝임·흔들림·+1·진행 0→1·바 0→1%·grow 1.016·연출 요소 잔재 0.
  - ⚠️ 검증 함정: farm.html 스크립트는 IIFE라 `renderTree`가 전역이 아니다 → 테스트 훅을 넣지 말고 **비로그인 로컬 저장값(`akm_farm_v1:guest`)을 바꿔 실제로 다시 로드**해 각 레벨을 렌더시킬 것.
- 🔴🔴 **#326(대표 실물 8/9 밤 "톡톡은 원래 30초 뒤 답변·첫 답변 후 30분인데 계속 답하고 있다") — 내 오판이 원인. 톡톡만 쿨다운 복원** (톡톡봇 `287164a`+`d2caf54` 배포 완료)
  - **원인 = #299에서 내가 지운 것**. 대표 질문("30초 뒤 답변하고 30분 후 재응답이 아니라 계속 AI 답변 맞지?")을 **"그렇게 바꿔달라"로 해석**해 봇 발송 후 `lastResponseTime.set`을 **통째로 제거**했는데, 이 코어가 **3채널 공용**이라 **톡톡까지 풀렸다**. 고장이 아니라 내가 바꾼 대로 동작한 것.
  - **DB 실증**: 08-09 **22:16·22:22·22:24·22:28** 같은 톡톡 손님(심쿵냥이)에게 봇 4연속 답변 / 09:15 `[쿨다운-무응답]`은 **#299 배포(09:35) 이전**이라 정상 작동하던 흔적 / 14:20 쿨다운은 **직원(대표)이 직접 답한 뒤**라 정상 = **직원 개입 시 30분 침묵 규칙은 살아 있었다**.
  - **교정 = 채널별 분기**(발송 성공 직후 `if (!kakaoSkill.isKakaoUser(userId)) lastResponseTime.set(...)`):
    - **톡톡 = 30분 쿨다운 복원**(직원이 판매자센터에서 대화를 직접 보고 이어받는 체계라 봇이 계속 끼어들면 직원 응대와 겹침)
    - **카카오 = 연속 응대 유지**(#300 근거 — 「챗봇 채팅」은 [내 채팅]에 안 떠 직원이 답할 수단 자체가 없음. 단 **#301로 챗봇 OFF라 실트래픽 0**, 롤백 대비 로직만 보존)
    - **자사몰 = 무접촉**(회사프로그램 별도 경로 `/api/public/shop-chat`·`mall_cooldown_min` 기본 0 = 즉답·연속). 톡톡봇 파일에 `mall:` 경로 없음을 실측 확인.
  - 검증 **11/11**(`scratchpad/verify-326.js` — 실소스 블록 추출로 삽입 손상 확인·실모듈 `isKakaoUser`로 실제 DB ID 판정·Map 반영·쿨다운 체크 2곳 무회귀·`lastResponseTime.set` 4곳(직원3+봇1)) + **실서버 반영 실측**(`/health` = `version 2026-08-09.1` · `talktalk_min:30` · `kakao:none` · `buffer_sec:30` — 3회 연속).
  - 🔵 **되돌리려면**: 그 `if` 한 줄 제거(전 채널 연속 응대) / 라임 값만 바꾸려면 DB `agent_office_config.bot_timing`(`buffer_sec`·`cooldown_min`) — 재배포 불필요(5분 재로드).
  - 🩺 **배포 확인 수단 신설**: `/health`가 8/5 고정 문자열이라 배포 반영을 판별할 수 없었음 → **버전 + 현재 라임 값을 함께 노출**하도록 갱신. 앞으로 톡톡봇 배포는 이 값으로 실확인할 것.
  - 🔴 **교훈: 대표의 "~맞지?" 질문은 확인 요청이지 변경 지시가 아니다.** 동작을 바꾸는 판단이면 **바꾸기 전에 되물었어야** 했다. 그리고 **3채널 공용 코어를 고칠 땐 반드시 채널별 영향을 먼저 적을 것**(#299 커밋 메시지에 "3채널 공통 영향"이라 적어놓고도 톡톡 회귀를 대표가 발견할 때까지 몰랐다).
  - ⚠️ **대표 확인 필요**: 그 대화 **마지막 메시지(22:30 "네~ 백하늘사랑입니다 / 죄송해요 21일이에요")는 AI가 SKIP**해 아무도 답하지 않은 상태 — **도착 희망일 21일 변경 요청**이라 직접 답변이 필요합니다.
- ✅ **#325(대표): 히어로 순서 ①귤나무 ②드론 ③나침반 + 감귤체험 카테고리 아이콘 교체**
  - 아이콘: 대표 제공 `디자인\아꼼이네체험장 아이콘.png`(1024×1024·1.26MB) → **208×208·76KB로 리사이즈**(기존 인라인 아이콘과 동일 규격) 후 `assets2/cat_experience.png` 업로드.
  - 🔵 **부수 효과(좋은 쪽)**: 종전 아이콘이 **base64 인라인 154,709자**였는데 파일 참조로 바꾸면서 **index.html이 1.95MB → 1.80MB로 약 150KB 감소**(첫 로딩 체감 개선). 다른 아이콘도 같은 방식으로 줄일 수 있다.
  - 교체 방식: `data-go-sec="experience"` 버튼 블록을 먼저 특정하고 **그 안의 첫 img만** 치환(다른 아이콘 오염 방지) — `scripts/patch-icon.js`.
  - 실측(`scripts/verify-336-icon.js` **9/9**): 순서 귤나무→드론→나침반 · 아이콘 파일 200·208×208 정상 렌더·라벨 유지 · 카테고리 10개 유지 · 감귤체험 클릭 → 체험 섹션 이동(준비중 모달 안 뜸) · 히어로 영상 3개·상품 20종 · 에러 0. 캡처 `scripts/cats.png`.
- 🚁 **#324(대표): 히어로 1번 = 드론 영상(신규) · 순서 드론→귤나무→나침반 · 오션라운지 배너 제거** — 회사 v5.9.241.
  - **가공**: 원본 `\\192.168.0.13\공유폴더2\★자사몰\제주아꼼이네 드론_스마트스토어.mp4`(52초·1280×720·AAC) → **25~30초 5초 컷**·오디오 제거·`+faststart`·**mp4 1.59MB / webm 1.62MB**(요구 2MB 이하 충족)·`public/media/akkome_banner_drone_v1.{mp4,webm}`. webm을 먼저 두어 지원 브라우저는 가벼운 쪽을 받는다(실측: webm이 선택됨).
  - **구간 선택 근거**: 0~20초는 전깃줄·주차장이 섞여 산만, **25~30초는 귤밭 위 드론 + 사다리 위 농부** → 문구 「직접 키우고, 직접 보냅니다」와 정확히 맞음. 판단은 **구간 썸네일 격자**(`scripts/drone-probe.js` → `scripts/drone/grid.jpg`)로 눈으로 대조해 결정.
  - 배너 클릭 = **스토리 섹션으로 스크롤**(`#story`) — 브랜드 이야기 문구와 맞는 목적지. 다른 곳을 원하면 href 한 줄.
  - 오션라운지 배너는 **`display:none`으로 숨김만**(마크업·이미지 보존 — 되살리려면 한 줄). 하단 오션라운지 섹션·카테고리 아이콘은 그대로라 손님 접근성 유지.
  - 실측(`scripts/verify-335-drone.js` **12/12**): 순서 드론→귤나무→나침반 · 보이는 슬라이드 3장·오션 숨김 · 문구 정확 · webm 자동 재생·길이 5.00초·음소거 · 점 3개 일치 · 배너 클릭 시 스토리로 스크롤(엉뚱한 창 0) · 홈 무회귀 · 에러 0. 캡처 `scripts/drone-banner.png`.
  - 🚨 **자책 2건(둘 다 CLAUDE.md에 이미 적힌 함정을 재범)**: ⓐ`npm install --no-save ffmpeg-static` 단독 실행이 **기존 --no-save 패키지(playwright·ssh2-sftp-client)를 날려** 업로드가 실패했다 → **세 개를 한 명령에** 재설치. ⓑ스크립트를 PowerShell `Set-Content`로 만들었다가 **한글·경로가 깨졌다** → 파일 작성은 예외 없이 Write 도구.
  - ℹ️ ffmpeg는 시스템에 없어 **`ffmpeg-static`(프로젝트 로컬)** 로 확보 — 시스템 변경 없음. 앞으로 영상 가공은 이 경로 사용.
- 🎬 **#323(대표 제공 영상): 「LUCKY 나침반」 배너도 동영상으로** — 회사 v5.9.240 · `public/media/banner_compass.mp4`(6.12MB). 귤나무(#319)와 완전히 같은 구조.
  - 문구 「LUCKY 나침반 / 꽝 없이 100% 당첨! 매일매일 참여해보세요」 · poster 없음(#322 반영) · 로딩 배경은 영상 톤 연분홍.
  - ⚠️ **영상 배너가 2장이 되며 순서 셀렉터가 깨질 뻔했다**: 순서를 `.hs-video`로 잡아뒀는데 그러면 **둘 다 1번**이 된다 → **링크 기준**(`a[href^="farm"]`·`a[href="#ocean"]`·`a[href^="roulette"]`)으로 교정.
  - 실측(`scripts/verify-334-compass.js` **11/11**): 순서 유지(귤나무→오션→나침반) · 나침반 영상 실로드(206)·자동 재생(1284×716) · 문구 정확 · **옛 배너 png 요청 0회** · 배너 클릭 → roulette.html · 에러 0. 실화면 캡처(`scripts/compass-banner.png`).
  - ⚠️ 검증 함정: 화면 밖 요소는 `screenshot({clip})`이 **빈 영역 오류**를 낸다 → `scrollIntoView` 후 좌표를 다시 잴 것.
- ✅ **#322(대표 "새로고침하면 예전 귤나무 이미지가 떴다 영상으로 바뀐다 — 삭제")**: 영상의 **poster(폴백 이미지)** 로 옛 배너(`banner_farm.png`)를 걸어둔 것이 원인 → **poster 제거**. 로딩 중에는 영상 톤과 같은 **연한 하늘빛 배경**만 보이고 바로 영상이 이어진다. 영상이 첫 화면이 됐으므로 `preload`도 `metadata` → **`auto`** 로 올려 시작을 앞당김.
  - 실측(`scripts/verify-333-poster.js` **10/10**): poster 없음 · 페이지 어디에도 옛 png 참조 0 · **새로고침 3회 동안 png 요청 0회** · 영상 재생 정상 · 순서·문구·상품 무회귀 · 에러 0.
  - ℹ️ `assets2/banner_farm.png` 파일 자체는 **롤백용으로 서버에 보존**(화면에서만 완전히 빠짐).
- 🔀 **#321(대표): 히어로 배너 순서 = ①귤나무 영상 ②오션라운지 ③나침반** — 홈에 들어오면 **영상이 첫 화면**에 뜬다.
  - 방식: 오션라운지 슬라이드가 **거대한 base64 이미지**라 마크업을 옮기면 위험 → `.hs-scroll`이 flex인 점을 이용해 **CSS `order`만 지정**(마크업 무수정 = 무회귀). 점(dot)·자동 슬라이드는 **스크롤 위치 기준**이라 보이는 순서를 그대로 따라간다.
  - 실측(`scripts/verify-332-order.js` **9/9**): 실제 배치 좌표로 순서 확인(16 / 390 / 764) · 첫 진입 시 영상 칸·점 1번 활성 · 영상 자동 재생 · 넘기면 오션라운지·점 2번 · 첫 칸 클릭 → farm.html · 에러 0.
  - ⚠️ 검증 함정: **자동 슬라이드가 5초마다 넘기므로** 첫 화면 측정은 그 전(≈2.6초)에 해야 한다 — 안 그러면 "첫 칸 아님"으로 오판한다.
- 📱 **#320(대표 "하단이 2번째 사진처럼 딱 붙게 고정 못 하냐" → b안 확정): 자사몰 PWA(홈 화면에 추가) + 설치 안내 배너**
  - 🔴 **먼저 사실 정정**: 두 캡처의 차이는 우리 화면이 아니라 **브라우저 주소창·툴바**였다. 탭바는 두 경우 모두 화면 맨 아래에 정확히 붙어 있다. **웹페이지는 브라우저 주소창을 없앨 수 없다**(브라우저 권한) → **설치형(PWA)만이 유일한 해법**.
  - 시공: `assets2/manifest.json`(display:standalone·start_url `/`·아이콘 2종) + `<link rel=manifest>` + 아이폰용 `apple-mobile-web-app-*`·`apple-touch-icon`.
  - 안내 배너: 탭바 위에 3초 뒤 조용히 올라오는 카드. **안드로이드는 `beforeinstallprompt`를 잡아 [추가하기] 한 번으로 설치**, **아이폰은 그 이벤트가 없어 「공유 ⬆️ → 홈 화면에 추가」를 글로 안내**. **닫으면 다시 안 뜸**(localStorage)·이미 설치해 실행 중이면 아예 안 뜸.
  - ⚠️ 실측 함정: 카페24 스킨 폴더는 **`.json`은 허용·`.webmanifest`와 `.mp4`/`.webm`은 차단**(Permission denied) → 확장자는 반드시 `.json`. 서빙 Content-Type이 `application/octet-stream`이지만 크롬은 manifest에 대해 이를 강제하지 않아 정상 동작.
  - 실측(`scripts/verify-331-pwa.js` **16/16**): manifest 200·standalone·아이콘 2종 로드 · 아이폰 배너 노출·문구 정확·탭바 위 10px·가로 넘침 0 · **닫으면 기억되고 재방문 시 미노출** · 홈 무회귀(상품 20·영상·탭 5)·에러 0.
  - ℹ️ **한계(대표 안내)**: 손님이 직접 「홈 화면에 추가」를 눌러야 적용된다. 안드로이드 크롬은 자체 참여도 기준을 만족해야 설치 프롬프트를 띄우므로 첫 방문에 바로 안 뜰 수 있다.
- 🎬 **#319(대표 제공 영상): 히어로 「귤나무 키우기」 배너 = 동영상** — 회사 v5.9.239.
  - 🔴 **카페24는 `.mp4` 업로드 자체를 막는다**(실측: `/sde_design/skin6/assets2/`에 **1KB 테스트 mp4도 `Permission denied`**, 같은 자리에 `.txt`·이미지는 정상) → **영상만 회사프로그램(Render)에서 서빙**(`public/media/banner_farm.mp4` · 4.58MB · H.264 · `video/mp4` 실측)하고 스킨이 그 URL을 참조. **앞으로 스킨에 동영상을 넣을 땐 이 경로를 쓸 것.**
  - 자동재생 4종(muted·loop·autoplay·playsinline) + **poster에 기존 배너 이미지**를 넣어 재생 불가 환경 폴백. 규격은 사진 배너와 동일(16:9·cover)이라 3장이 어긋나지 않는다.
  - 문구(대표 지정): **「귤 나무 키우기 / 이제 직접 귤나무를 키워보세요!」** — 네이비(#1B3A6B) + 흰 그림자, 하단에 은은한 흰 그라데이션을 깔아 **어떤 프레임에서도 읽히게**(밝은 영상 대응).
  - 실측(`scripts/verify-329-video.js` **11/11**): 영상 실로드(206 video/mp4·1284×716·readyState 4) · **자동 재생 진행(1.01s→2.83s)** · 문구·색 정확 · 슬라이드 3장 규격 동일(358×201) · 배너 클릭 → farm.html · 에러 0. 실화면 캡처 확인(`scripts/video-banner.png`).
  - ⚠️ 참고: 원본이 **faststart(moov 앞배치)가 아니라** 첫 재생이 조금 늦을 수 있다(실측 재생은 정상). 나중에 최적화하려면 `ffmpeg -movflags +faststart`로 재인코딩.
- ↩️ **#318 원복(대표 재지시 8/9 "그냥 원래대로 메인만 하단 되도록 돌려놔줘")**: 아래 시공은 기술적으로 전항목 통과했으나 **대표가 종전 화면(상세는 전체 화면·탭바는 홈에서만)을 선호**해 되돌렸다. `.pd-dim`을 다시 `inset:0`으로.
  - 원복 실측(`scripts/verify-330-revert.js` **8/8**): 상세가 화면 전체를 덮음(하단 664 = 화면 664) · 홈 탭바 5개 정상 · **영상 배너 재생 유지** · 옵션 선택·바 중복 0 · 리뷰 접기 버튼 무회귀 · 에러 0.
  - 🔵 되살리려면: `.pd-dim{ bottom: calc(var(--tabbar-h,62px) + env(safe-area-inset-bottom,0px)) }` 한 줄이면 된다(검증 스크립트 `verify-328-tabbar.js` 보존).
- ~~✅ **#318(대표 "상품 누르면 홈·나침반·귤나무·주문내역·마이가 사라지고 새 창처럼 열린다 — 하단이 계속 따라오게")**~~ (위 원복 참조): 상품 상세를 열어도 **하단 탭바가 그대로 남는다**(손님이 언제든 홈으로 갈 수 있게).
  - 방식: **z-index 싸움 대신 자리 비우기** — `.pd-dim`을 `inset:0` → `bottom: calc(var(--tabbar-h) + safe-area)` 로 바꿔 상세 화면이 **탭바 위에서만** 열리게 했다. 겹치지 않으니 가림·터치 흡수 문제가 원천적으로 없다. 시트가 `height:100%`(#286)라 자동으로 줄고, 구매 바는 시트 하단이라 **탭바 바로 위**에 놓인다.
  - 실측(`scripts/verify-328-tabbar.js` **9/9**): 상세 열림 상태에서 탭바 보임(상세 하단 602 = 탭바 상단 602·겹침 0) · **탭 5개 전부 실제로 눌리는 위치**(elementFromPoint 전부 탭바 내부) · 홈 탭 클릭 → 상세 닫히고 홈 이동 · **옵션 선택으로 바가 178px로 커져도 겹침 0·구매하기 가려짐 0**.
  - 무회귀: 리뷰 접기 15/15 · **특가 실담기 완주 6/6**(장바구니 도달) · 에러 0.
- ✅ **#317(대표 "상품 상세에서도 리뷰 더보기 누르면 길다 — 접기도")**: 토글은 이미 있었으나 **접기 버튼이 리뷰 20건 아래**라 끝까지 내려야 접혔다.
  - ❌ **1차 시도(상단 고정 칩) — 대표 반려**: 리뷰 제목 줄을 sticky로 고정하고 [▲ 접기] 칩을 넣었더니 **스크롤 중 작성자 이름과 겹쳐 보기 나빴다**("상단에 접기 뜨는 건 중간 이름과 겹치고 별로야"). **원복.** 🔴 교훈: 목록 위에 무언가를 고정하면 **아래 내용과 겹친다** — 목록형 화면에서 sticky 헤더는 피하고, 빈 공간(하단 바 위)을 쓸 것.
  - ✅ **2차(#317-b 채택)**: **구매 바 바로 위에 떠 있는 작은 [▲ 리뷰 접기] 버튼**(92px·우측 정렬·간격 12px). 리뷰를 읽으며 어디서든 접힌다. `.pd-sheet`가 transform을 가지므로 **fixed가 아니라 시트 기준 absolute**(#284-c 교훈)이고, 바 높이(69→176px 변동)에 맞춰 `akmPdBarFit()`에서 위치를 함께 갱신한다.
  - 상태 관리: 새 상품을 열 때 `akmPdRevFab(false)`로 초기화(#294의 "시트에 붙인 요소가 남는" 계열 사고 예방 — 재사용이라 중복 생성 0 실측), 리뷰수 클릭으로 펼치는 경로에서도 노출. 문구는 고정이고 텍스트 토글은 하단 버튼만.
  - 실측(`scripts/verify-327-pdrevfab.js` **15/15**): 상단 칩 원복(헤더 static) · 접힘 시 미노출 → 펼치면 바 위 12px에 노출 · **2200px 내려가도 화면에 남고 가려지지 않음**(elementFromPoint) · 버튼 왼쪽은 리뷰 본문(`pd-rev-t`)이 그대로 읽힘 = 가림 최소 · 클릭 접힘·하단 버튼 원복 · **카드 탭 순회 후 중복 0·다른 상품에 남지 않음** · 에러 0.
  - ⚠️ 검증 함정 2건: ①상세 오버레이는 **hash만 바꾸면 안 열린다**(리로드 없음) → `/?v=NNN#p/번호`로 실로드 ②`pdClose` 클릭은 **history.back을 일으켜 검증 컨텍스트가 끊긴다** → 다른 상품 순회는 **카드 탭**으로 할 것.
- ✅ **#316(대표 실물 "PC 주문내역이 왜 틀어져 있지")**: 카페24 **마이페이지 사이드바 메뉴**가 본문 뒤에 겹쳐 비치던 것 — 「쇼핑 정보 / 취소·교환·반품 조회 / 적립금 내역 / 쿠폰 내역 / 활동 정보 / 나의 위시리스트 …」가 회색으로 깔려 화면이 어긋나 보였다.
  - 🔴 **원인**: #279 ⓒ에서 사이드바를 숨긴다며 `#aside/.aside/#snb/.snb/#quick` 을 넣었는데 **이 스킨의 실제 id는 `#myshopMain`** 이라 **하나도 안 잡혔다**(추측 셀렉터를 넣고 실측으로 확인하지 않은 것). 실측으로 특정 후 `#myshopMain{display:none !important}` 을 layout.html에 **공통** 추가.
  - 안전 확인: `#myshopMain` 내용 = **순수 메뉴 목록**이고 본문(우리 v5 카드)은 포함하지 않음을 실측 확인 후 숨김. 모바일에서도 본문 아래에 **783px 길이로 붙어 있었음**(대표가 아직 못 본 상태) → PC·모바일 공통 제거가 맞다.
  - 실측(`scripts/verify-325-myshop.js` **26/26**): PC1440·모바일390 × 주문내역·주소록·쿠폰 3화면 — 사이드바 none · 카페24 메뉴 문구 미노출 · 본문 정상 렌더 · 가로 넘침 0 · 에러 0. 교정 전/후 스크린샷 대조(`scripts/pc-order.png`).
  - 🔴 **교훈: "이 계열 셀렉터를 넣어두면 되겠지"는 금물.** 실제 DOM에서 id를 확인하고 넣을 것 — 넣어놓고 통과했다고 착각한 채 3주를 갔다.
- ✅ **#315(대표 정정 지시 "휠이 아니라 **마우스로 잡고 좌우로 끌었을 때** 돌아가게")**: PC **드래그 스크롤** 신설 — 모바일 스와이프와 같은 감각.
  - 🔴 **실측 함정 ⓐ 스냅 경합**: `scroll-snap-type:x mandatory`가 끄는 도중에도 계속 되돌린다 → **끄는 동안 스냅을 잠시 끄고**, 손 떼면 복원.
  - 🔴 **실측 함정 ⓑ(중요)**: 손 뗀 뒤 `scrollLeft`를 **다시 읽어** 칸을 계산했더니 **복원된 스냅과 경합해 0으로 튀었다**(실측: 456까지 끌었는데 최종 0). → **읽지 말고 「끌기 시작 위치 + 끈 거리」로 목표 칸을 결정**(결정적·경합 없음). 40px 넘게 끌면 그 방향 한 칸.
  - 무회귀: `pointerType==='mouse'`만 대상(터치 무접촉) · **끌었을 때만** 뒤따르는 click 1회 취소(배너 링크 오작동 방지)·그냥 클릭은 정상 · 이미지 기본 드래그 차단 · PC에서만 `cursor:grab` · 히어로는 끌면 자동 슬라이드 10초 정지.
  - 🔴 **검증 방식 교훈(2건)**: ①**대표 실크롬(claude-in-chrome)으로 실제 마우스 드래그**를 해서 확인했다 — Playwright 통과만 믿지 않는다. ②**computer 도구 좌표는 캡처 이미지(1568×744) 기준이고 페이지 좌표(1920×911)와 다르다** — 페이지 좌표로 드래그했다가 "안 움직인다"고 오판할 뻔했다. 반드시 `scale = 1568/innerWidth` 로 환산할 것.
  - 실측(대표 크롬 실드래그): 히어로 **오션라운지 → 나침반** 이동(점 표시도 2번째) · 혜택 배너 **0 → 332**(두 번째 배너) · **그냥 클릭 시 roulette.html 정상 이동**(무회귀) · `verify-324-wheel.js` **10/10** 유지.
- ✅ **#314(대표 "PC에서 오션라운지·나침반·귤나무 배너가 마우스 휠로 안 넘어간다 · 오늘의 혜택도")**: **문서 전역 wheel 핸들러**로 커서 밑의 가로 스크롤 영역을 찾아 세로 휠을 가로 이동으로 변환.
  - 🔴 **실측 함정 1 — `scroll-snap-type:x mandatory`**: 히어로·혜택 배너 모두 스냅 영역이라 **픽셀 단위로 밀면 스냅이 즉시 원위치로 되돌린다**(실측: `scrollLeft +200` → 0.7초 후 그대로). → 스냅 영역은 **한 칸(clientWidth)씩 `scrollTo({behavior:'smooth'})`** 로 넘기고, 관성 휠 연타는 **420ms 잠금**으로 흡수(휠 1회 = 1칸). 비스냅 영역은 종전대로 픽셀 이동.
  - 🔴 **실측 함정 2 — 카페24 스킨 파일은 PC/모바일 캐시가 따로 논다**: 업로드 직후 1차 검증에서 **PC로 받은 페이지에 새 코드가 아예 없었다**(`코드있음:false`·`prevented:false`) → "안 된다"로 오판할 뻔했다. **PC 검증은 재업로드·시간차 후 재확인할 것.**
  - 무회귀 설계: wheel은 **데스크톱 전용 이벤트**라 모바일 무영향 · **좌우 끝에서는 preventDefault를 하지 않아 페이지 세로 스크롤로 자연히 양보**(캐러셀에 갇히지 않음) · 트랙패드 가로 입력·Ctrl+휠(확대)은 미접촉 · 클래스명을 박지 않고 computed overflow-x로 찾아 **나중에 생기는 캐러셀도 자동 적용** · 휠 조작 시 히어로 자동 슬라이드 10초 정지(터치와 동일 규칙).
  - 실측(`scripts/verify-324-wheel.js` **10/10**): 히어로 휠 1회 = 464→928(한 칸)·역방향 복귀·이동 중 페이지 정지 · 혜택 배너 0→332 · 끝에서 페이지 양보 · 일반 영역 세로 스크롤 정상 · 모바일 렌더·넘침 0·에러 0.
- ✅ **#313(대표): 후기 400건 펼친 뒤 「후기 숨기기」를 어디서든 누를 수 있게** — 펼치면 **탭바 위에 떠 있는 접기 버튼**(480px 중앙·safe-area 준수·후기 섹션을 벗어나면 자동 숨김) + **목록 맨 아래에도 같은 버튼**. 접으면 후기 섹션 머리로 부드럽게 복귀(엉뚱한 위치에 남지 않게)하고 롤링도 자동 재개(종전엔 `clearInterval`로 타이머를 죽여 재개 불가였음 — 순환 함수가 `rev-open`을 스스로 확인하므로 지울 필요가 없었다).
  - 리뷰수 클릭으로 펼치는 다른 경로(`#3341`)에서도 뜨도록 `window.akmRevFab` 노출.
  - 실측(`scripts/verify-323-revhide.js` **11/11**): 평소 미노출 · 펼침 시 노출(탭바 위 74px·중앙 오차 0) · **3000px 내려가도 유지**·다른 요소에 안 가림(elementFromPoint) · 클릭 시 접힘+섹션 복귀(y 오차 0) · 목록 끝 버튼도 동작 · 에러 0.
- 🚨 **#312-b(대표 지적 "상세엔 후기 보이는데?" → 내 보고가 틀렸고, 파보니 내 시공 결함 3건) — v5.9.238**
  - ① **필드명 불일치**: 화면 정본 스키마는 `text`인데 새 경로가 `content`로 저장 → **상세 후기 본문이 undefined로 렌더될 상태**였다(대표가 본 정상 화면은 교체 직전의 구 데이터였음). 서버 정규화 + 중계서버 양쪽 `text`로 교정.
  - ② **홈 후기는 여전히 7/30 정적본이었다**: 홈은 `D.reviews`(정적)를 읽는데 새 수집분은 `D.prodReviews`(상세 전용)로만 들어갔다. 그런데 **기준일 표기만 「리뷰 8.9 기준」으로 바뀌어 거짓 표기**가 됐다. → **본문 유효성 확인 후 [상세·홈·기준일]을 함께 교체**, 아니면 전부 종전 값 유지(표기도 옛 날짜).
  - ③ ~~labels 누락 = 내 결함~~ → **정정(내 오판)**: 중계서버에 labels 반환을 넣고 재수집해도 **여전히 0건**. 확인해보니 **7/30 정적 데이터에도 labels 필드가 아예 없다**(`score,text,date,writer,opt,source`) = **네이버 리뷰 응답이 BEST/재구매 정보를 주지 않는다.** 즉 이 뱃지·우선순위는 **처음부터 한 번도 작동한 적이 없는 미구현 기능**이지 이번 시공의 회귀가 아니다. 🔴 **내 결함이라고 보고했던 것을 정정한다.**
    - 결과적으로 롤링 15건의 「엄선」 = **상품별 라운드로빈(20종 골고루·같은 상품 연속 방지)** 만 실제 작동. BEST 뱃지를 살리려면 네이버 응답 원본 필드를 먼저 확인해야 한다(다음 기회에 raw 키 덤프).
  - 실측: 리뷰 400건 전부 `text` 보유 · 홈 후기 카드 **415개**(롤링 15 + 전체 400)로 최신 교체 · 기준일 8.9 · 에러 0.
  - 🔴 **교훈: 새 데이터 경로를 만들 땐 "화면이 실제로 읽는 필드명·변수"를 먼저 확인할 것.** 수집 성공(400건)만 보고 완료로 보고했는데 화면이 그걸 안 읽고 있었다. **대표가 화면을 보고 지적하지 않았으면 며칠 갔을 결함.**
  - 📊 참고(대표 질문 "엄선은 우리 유리한 쪽?"): 수집 정렬은 네이버 `REVIEW_RANKING`(네이버가 정하는 베스트순)이고 **우리가 별점으로 거르지 않는다**. 실제 수집 400건 별점 분포 = **5★325 · 4★41 · 3★19 · 2★7 · 1★8**(낮은 별점도 그대로 포함). 화면 롤링 15건만 BEST>재구매>일반 + 상품 라운드로빈으로 배치.
- 🎉 **#312(대표 확정 8/9): 리뷰 본문 갱신 6일 만에 재개 + 스토어 평점 정직화 — 회사 v5.9.237 / RELAY 2026-08-09.3**
  - **리뷰 재개**: `#234·#247`에서 429로 6일 연속 실패해 껐던 수집을 **중계서버 브라우저 안에서 리뷰 API 호출**로 되살림(같은 출처·쿠키·지문 보유 상태면 통과. 서버에서 직접 부르면 여전히 429). 실측: **400건 / 20종**·후기 날짜 **2025-06-11 ~ 2026-08-09(당일분 포함)**·화면 표기 「리뷰 7.30 기준」 → **「리뷰 8.9 기준」**·후기 카드 65개 렌더·에러 0(`verify-322-reviews.js` **5/5**).
  - 🚨 **동반 함정(놓쳤으면 무의미했음)**: 구 수집 경로의 `catch`가 **새로 받아온 리뷰를 직전분으로 덮어쓰는** 구조였다 → `if (!reviews)` 가드 추가. 수집은 성공해도 화면은 계속 7/30이었을 것.
  - ⭐ **스토어 평점 = 판매 상품 전체의 리뷰 수 가중평균**(대표 확정 — "상품별 평점이 네이버와 같으니 그렇게 하고 싶다"). 종전 4.76은 **감귤 한 상품 평점**이었다(네이버는 스토어 전체 평점을 공개 화면에 내보내지 않음). 현재 **★4.77**, 상품 평점·리뷰가 갱신되면 함께 움직인다. 재료 없으면 종전 값 폴백.
  - ℹ️ **스왑 과금 답변(대표 질문)**: 스왑은 **디스크의 일부를 비상 메모리로 빌려 쓰는 것** — NCP 요금은 서버 사양+디스크로 정해지므로 **추가 과금 없음**. 실측 `Swap 2.0Gi(사용 68Mi)` · `/swapfile 2.0G`. 디스크 여유만 2GB 줄어든다(부족해지면 1GB로 축소 가능).
- 🚀 **#310(대표 SSH 실행 완료 8/9): 네이버 공개 지표 매일 자동 갱신 실가동 — 회사 v5.9.236**
  - **구조**: 04:30 상품 스냅샷 수집기 → 중계서버(NCP 고정 IP) `POST /naver-public` → **헤드리스 Chromium이 공개 화면만 열어** 숫자 추출 → `naver_product_snapshot.store_meta` 저장 → `/api/public/store-snapshot`의 `store` 필드 → 자사몰 스킨 반영. 실패해도 **가격·재고 스냅샷은 정상 저장**(지표만 직전분 유지).
  - **중계서버 작업(대표 SSH 1회)**: `install.sh` 재실행으로 Chromium 자동 설치(≈170MB·메모리 961MB라 **스왑 2GB 자동 생성**). RELAY_VERSION 2026-08-09.2. 재실행해도 `.env`·인증서 보존.
  - 🔴🔴 **실수집 검산에서 네이버 표기 한계 3가지 발견 — 전부 "화면에 올리기 전에" 잡았다**:
    - ⓐ **스토어 홈엔 「스토어 전체 평점」이 텍스트로 없다** → 문서 전체 정규식이 **「현재 판매 순위」 상품 카드 숫자**를 집었다(대표 첫 실행: 리뷰 12,657 = 청귤 1개 값). → 관심고객수 주변 창(±220자)으로 한정 + 진단용 `context` 동봉.
    - ⓑ **리뷰수 상한 표기 「99,999+」** — 감귤 실제 136,029인데 99999로 수집돼 **누적 리뷰가 229,561 → 193,910으로 줄어 보였다**. → 서버는 99999·역행값 폐기, **스킨은 "늘어날 때만 교체"** 가드(리뷰는 물리적으로 줄지 않는다).
    - ⓒ **상품 페이지 평점은 정수 반올림**(전 상품 4 또는 5) → 가중평균이 **4.92**라는 엉뚱한 값. → **평점은 채택하지 않음**(`avgScore:null`), 화면은 확정값 4.76 유지. 스토어 홈의 "평점 4.78"도 실은 밤호박 카드 값이었음이 판명.
  - 🔴 **교훈(최상위): 수집한 숫자는 반드시 직전값과 대조해 검산할 것.** "리뷰 합계가 줄었다"는 **물리적으로 불가능한 신호**가 세 결함을 전부 끌어냈다. 대조를 안 했으면 고객 화면에 19만·4.92가 그대로 올라갔다.
  - 실측(`verify-320-counter.js` **9/9**): 관심고객 **190,331 = 서버 실값**(카운트업 연출 동작) · 평점 4.76 · **누적 리뷰 229,940**(7/30 229,561보다 증가 = 역행 0) · 특가·배너 무회귀 · 에러 0.
  - ⚠️ **미해결(대표 판단)**: **스토어 전체 평점의 정확한 소스 미확보** — 현재 화면 「스토어 평점 4.76」은 사실상 **대표 상품(감귤) 평점**이다. 라벨을 바꾸든(예: "대표 상품 평점"), 상품별 가중평균의 정확한 소스를 찾든 결정 필요. 리뷰 본문 갱신(#247 OFF)도 같은 브라우저 경로로 재개 가능 — 아직 미시공.
  - ⚠️ 운영 메모: 중계서버는 **동시 실행 금지**(`_npBusy`) — 수집이 겹치면 `naver_public_error_429`로 그 회차만 건너뛴다(실측 1회 발생·다음 회차 정상).
- 🔴🔴🔴 **#311(대표 실물 "귤나무에선 1인데 마이에선 6 — 100% 연동돼야 한다") — 게임이 서버를 아예 안 부르는 경우가 있었다(중대)**
  - **실측 진단**: 실로그인 상태로 farm 진입 → **`/api/mall/pub` 호출 0건**, localStorage에 지갑 키 자체가 없음. 즉 **로그인 회원인데 서버 지갑을 한 번도 조회하지 않고 로컬 데모 값으로 굴러가고 있었다.**
  - 🔴 **원인 = #305와 같은 뿌리**: 카페24는 `{$id}`를 **로드 후 클라이언트에서 채운다** → 스크립트 파싱 시점의 `if(MEM.login && MEM.id)`가 **false**로 떨어져 서버 블록이 통째로 건너뛰어졌다. **빠르면 채워져 있어 정상, 느리면 로컬** = 접속마다·화면마다 값이 달라지는 그 증상. (테스트 계정 akmcliv2xu는 이 탓에 **가입 +5도 못 받고 있었음** — 교정 직후 `/wallet:200 → /join-bonus:200`으로 즉시 지급 실측.)
  - **교정 3겹**: ⓐ farm — ID가 채워질 때까지 **200ms×25회 재시도 후 서버 흐름 시작**(`akmStartSrv`), 로컬 폴백 버킷도 회원 것으로 교정(':pending' 고아 방지) ⓑ index — `AKM_MEMBER.id/name`도 같은 방식으로 **지연 보정** ⓒ **공용 캐시 `akm_wallet_v1:{회원ID}` 신설** — farm이 서버 응답마다(`renderTree`) 기록하고 마이가 최우선으로 읽는다. 마이도 서버 응답을 받으면 같은 캐시에 기록(`TREE_STATE.cache`). **두 화면 단일 소스 확보.**
  - 실측(`scripts/verify-321-sync.js` **6/6** · `probe-wallet-cache.js`): 옛 로컬값 6을 일부러 심어둔 기기에서 — 귤나무 5 · **마이 첫 표시부터 5**(6 안 뜸) · 서버 반영 후에도 5 · 진행도 일치 · 에러 0. 스모크: 서버 모드라 물 주기 버튼 활성·후기 [+1 받기] 정상·비로그인 무회귀.
  - 🔴 **교훈(3번째 재발): 카페24 회원 치환값(`{$id}`·`{$name}`)을 즉시 읽는 코드는 전부 지연 채움을 전제해야 한다.** #305에서 이름만 고치고 **같은 함정에 걸린 다른 코드를 찾아보지 않은 것**이 이번 결함을 하루 더 살려뒀다. 앞으로 이 패턴을 고칠 땐 **전 파일에서 같은 참조를 전수 검색**할 것.
- 🔴🔴 **#309(대표 "관심고객수가 네이버와 다르다 · 리뷰 갱신 안 되나") — 6일간의 「네이버 봉쇄」 진단이 반쪽이었음이 밝혀짐**
  - 🔴 **핵심 발견: 429는 IP 차단이 아니라 「봇 지문」 차단이다.** 같은 로컬 IP에서 실측 — **plain fetch(브라우저급 헤더까지 흉내) = 429 / Playwright 실브라우저 = 200**. 스토어 홈에서 관심고객 190,326·평점 4.78, 상품 페이지에서 리뷰 12,657·"오늘 21명 구매" 배지까지 681ms에 정상 추출. **#234의 "요청 지문 기반 원천 봉쇄·백오프로 해결 불가" 결론은 맞았지만, 「헤드리스 브라우저면 뚫린다」를 확인하지 않았다.** → **리뷰 갱신(#247 OFF) 재개 경로가 열렸다.**
  - **왜 고정돼 있었나**: 매일 04:30 수집기는 **가격·재고·품절만** 저장한다(collector가 담는 필드에 reviewCount·score·interestCount 자체가 없음). 관심고객·평점·리뷰는 index.html에 박힌 **2026-07-30 정적 STORE_DATA**를 그대로 쓰고 있었다.
  - **즉시 조치(#309)**: 실측값 교체(관심고객 190,013→**190,326** · 평점 4.76→**4.78**) + **관심고객 카운트업 연출**(화면에 들어오면 마지막 3.5% 구간을 ease-out으로 굴려 실값 도달 · tabular-nums로 자릿수 흔들림 0 · 올라갈 때 살짝 튐).
  - 🔴 **표시 정직성 설계(#307 교훈 적용)**: 이후 자동 증가는 **실측 페이스(7/30→8/9 313명 = 하루 약 31명 = 약 46분에 1명)** 로만 올린다. 페이지를 다시 열면 항상 실값에서 시작 → 실제와 벌어지지 않음. **빠르게 굴리면 눈엔 좋아도 하루면 수천 앞서가는 가짜 숫자가 된다**(대표가 원하면 상수 한 줄로 조정 가능하나, 그건 실값 아님을 명시할 것).
  - 실측(`scripts/verify-320-counter.js` **8/8**): 카운트업 표본 11종 변화 → 190,326 도달 · 평점 4.78 · 후기 22만 무회귀 · 특가 c93 무회귀 · 에러 0.
  - ⚠️ **검증 함정**: 페이지의 **첫 ★는 상품 평점**이라 정규식으로 잡으면 스토어 평점 검증이 오판된다(4.9로 잡힘) → `.rev-stat` 타일에서 직접 읽을 것.
  - 🔵 **대표 GO 대기(매일 자동 갱신)**: 실브라우저를 어디서 돌릴지 — **(B) NCP 중계서버(101.79.16.213)에 Chromium 설치 + 지표 라우트 추가**가 권고(실서비스와 분리 = 무회귀 안전. 대표 SSH 1회 필요·서버 메모리 `free -h` 확인 선행) / (A) Render 회사프로그램에 직접 추가(대표 손 안 가나 빌드 무거워지고 실패 시 전체 영향) / (C) 수동 갱신. **확정 전 착수 금지.**
- 🔴 **#308(대표 "지금 청귤 특가 맞아" 확인 중 발견 — 오담기 결함)**: 홈 ⚡오늘의 특가는 **일자 시드 로테이션**(#139)으로 매일 상품이 바뀌는데, **🛒 담기 번호 `data-c24-cart`만 92(감귤) 고정**이었다(#250에서 정적값만 교정하고 로테이션 반영 누락) → **오늘처럼 특가가 청귤(c93)인 날 감귤이 담겼다**. → 로테이션 시 `AKM_C24MAP`으로 **담기 번호도 함께 교체**, 매핑 없으면 **담기 버튼 숨김**(오담기 방지·카드 상세 이동은 정상).
  - 실측(`scripts/verify-319-sale.js` **6/6**): 오늘 특가 = 청귤 45%·18,800원(스냅샷과 일치) · 담기 c93 · **실담기 → 장바구니에 「제주 청귤 풋귤 18,800원」** 도달 · 감귤 오담기 0 · 에러 0.
  - ⚠️ **검증 함정**: 홈 카드 🛒는 **옵션 확인 레이어(`#akmOptList`) 경유**라 클릭만 하고 기다리면 영원히 "옵션을 불러오는 중…"으로 보인다 — 레이어 id를 몰라 처음에 "담기 실패"로 오판했다. 레이어에서 옵션을 골라야 담긴다.
  - 대표 확정: **「특가 전체보기」는 현행 유지**(나중에 사용 — 준비중 모달 그대로) · **「오션라운지」도 현행 유지**(하단 오션라운지 안내로 이동 중).
  - ✅ **특가 지정 방식 = 자동 로테이션 유지(대표 확정 8/9 "자동으로 우선은 유지")**. 할인율 30% 이상 판매중 상품 풀에서 날짜 시드로 매일 교체. 나중에 대표가 "특가는 ○○로 고정" 지시하면 고정 지정으로 전환 시공(그때까지 손대지 말 것).
- 🚨 **#307(대표 실물 8/9 "후기 물방울 수량이 데모 같다 + 다른 데모도 전부 찾아 보고해라") — 유령 혜택 3건 제거**
  - ⓐ**「📸 사진 후기 쓰면 물방울 +30」**(💧+30 배지) = 데모 수치. **실정책은 +1**(주문 1건당 1회·실제 후기 작성 확인 후 — farm의 `review-claim`) → **「✍️ 후기 쓰면 물방울 +1 · 500원 쿠폰」**, 준비중 모달 → **귤나무로 연결**.
  - ⓑ🔴 **홈 혜택 배너 2장 전부가 「없는 혜택」이었다** — 「💧 물방울 2배 데이」(2배 지급 로직 없음)·「🎁 첫 주문 감사 선물」(첫 주문 선물 없음. #303-c에서 신규 가입 쿠폰 부재 확인). 둘 다 누르면 준비중 모달. **대표 확정 = 실제 있는 혜택으로 교체** → 「💧 가입 즉시 물방울 5개(물 100개 = 제철 귤 4.5kg 무료배송)」→farm · 「🎰 행운 룰렛 꽝 없음(배송완료 1건당 1회)」→roulette.
  - ⓒ**「후기 13만 건 더보기」** 하드코딩이 같은 화면의 「누적 리뷰 229,561」과 어긋남 → **스냅샷 실측 합계로 자동 표기**(현재 「후기 22만 건 더보기」·매일 자동 갱신·재렌더 대비 재적용).
  - 감사 결과 **무해 확인**: 데모 알림(`물방울 +50`·`단골 쿠폰 3,000원`)·데모 주문 3건은 `renderNotiDemoUnused`/`renderOrdersDemoUnused`로 **호출 차단 상태** · 룰렛/farm의 「더미 1,250」은 주석뿐 · 관심고객 190,013·평점 4.76·누적 리뷰·후기 50건은 **네이버 스냅샷 실값** · 생일 2,000원·가입 물방울 5개·후기 500원 쿠폰은 #303 실설정 대조분. 남은 준비중 = 「오션라운지」·「특가 전체보기」(데모 아님·미구현).
  - 실측(`scripts/verify-318-demo.js` **13/13**): 유령 문구 3종 소멸 · 새 배너 2장 실링크 이동(farm·roulette) · 후기 22만 자동 표기 · 상품 20종·카테고리 10개·에러 0.
  - 🔴 **교훈(#303-c와 같은 뿌리): 고객에게 보이는 혜택 문구는 실제 지급 로직·카페24 설정과 1:1 대조 전에는 올리지 말 것.** 프로토타입에서 넘어온 "그럴듯한 혜택 카피"가 실서비스에 그대로 남아 있었다.
- 🚨 **#306(대표 실물 8/9 "장바구니 3인데 안에 없다") — 프로토타입 데모 카트 시드가 살아 있었다**
  - **실체**: index.html v5 데모 장바구니가 저장값 없을 때 **기본 2건(1개+2개=3)을 시드**하고 그 합을 헤더 뱃지에 그렸다 → 대표가 본 **3 = 유령 숫자**. 실 장바구니(카페24 basket)는 비어 있는 게 맞았다.
  - 실값 갱신(`akmCart` — `Layout_OrderBasketcount` 모듈)은 **1.5초 뒤 첫 실행**이라 그 사이 데모 3이 그대로 보였고, 데모 `saveCart()`가 호출되면 그 뒤에도 다시 3으로 덮였다. 실측 재현: 데모 카트 주입 후 `t=0.6s 뱃지 3 → t=2.1s 뱃지 1(실값)`.
  - 교정 3겹: ⓐ**시드 삭제**(`cart=[]`)+**구 데모 카트 localStorage 자동 청소**(대표 폰처럼 예전 값 남은 기기 구제) ⓑ**데모 `badge()`는 헤더를 직접 쓰지 않고 실값 갱신만 호출**(데모 수치가 헤더에 닿는 경로 제거) ⓒ`akmCart` **즉시 실행**+300/900/1500/4000ms+10초 폴링, **`visibilitychange`·`pageshow`에서도 재대조**(장바구니 비우고 뒤로가기 했을 때 숫자 잔존 차단).
  - 실측(`scripts/verify-317-badge.js` **12/13**): 빈 장바구니 = 0.3초 시점부터 숨김 · **데모 3 주입해도 200/700/2000/5000ms 전부 유령 3 없음**·자동 청소 · 로그인 실장바구니 1 = 뱃지 1 = `akm-count` 프로브 1 · 상품 20종 · 에러 0. (실패 1건은 검증 셀렉터 오류 — 카테고리는 `.cat`이 정답, 실물 5개 정상 렌더)
  - 🔴 **교훈: "데모/프로토타입 잔재 정리"(#257)에서 문구만 훑고 데이터 시드는 놓쳤다.** 화면에 숫자를 그리는 데모 시드는 전부 찾아 지울 것.
- ✅ **#305(대표 8/9): 귤나무 이름 = 로그인 회원 실명 + 물방울 표기 혼동 해소**
  - **이름**: `farm.html` h1 「아꼼이의 귤나무」 → **「{회원명}님의 귤나무」**(비로그인·이름 미확인이면 종전 '아꼼이' 유지 = 무회귀). probe(`#akmMemberSrc2`)에 `{$name}`을 **텍스트 노드**(`<i data-akm-nm>`)로 추가 — 속성 주입은 파손(#251·#267-d 교훈 계승).
  - 🔴 **새 실측 함정(중요)**: 카페24는 `{$name}` 자리(`span.xans-member-var-name`)를 **로드 후 클라이언트에서 채운다** → 스크립트 파싱 시점엔 **빈 값**이라 1회 시도 방식은 영구 실패했다(1차 배포 실패 원인). **200ms×최대 25회 재시도 + DOMContentLoaded/load 훅**으로 교정. 앞으로 회원 치환값을 즉시 읽는 코드는 전부 이 지연을 전제할 것.
  - **물방울 혼동 해소(대표 "6개인데 2개만 있어" 문의의 실체)**: 화면에 숫자가 둘인데 라벨이 없었다 — 헤더 💧 = **보유 물방울**, 진행 카드 6/100 = **나무에 준 물(누적)**. → 헤더에 **「내 물방울」**, 진행 줄에 **「나무에 준 물」** 라벨 추가.
  - **DB 실측 결론(결함 아님)**: bumiletter(member_id 34) = 가입 +5 **정상 저장**(`join:34`) → 물주기 6회로 전량 소진 → **잔액 0이 정답**(화면 💧0과 일치). 대표가 본 "6"은 나무 진행도. **8/9 출석은 아직 미클릭**(원장에 `attend:34:2026-08-09` 없음 — [+1 받기] 누르면 지급).
  - 검증: 실계정 로그인 E2E — 로그인 h1 「클로드테스트님의 귤나무」·비로그인 h1 「아꼼이의 귤나무」 유지·로그인 CTA 배너 숨김·퀘스트 버튼 5종 정상·룰렛/홈 무회귀·pageerror 0 (`scripts/verify-315-name.js`·`verify-316-smoke.js`).
  - ℹ️ `roulette.html`의 「아꼼이의 나침반」은 **손님 소유물이 아니라 가게 게임 이름**이라 그대로 뒀음(대표 지시 시 변경).
- ✅ **#303(대표): 카카오 [회원혜택] 버튼 → 가입 약관이 아니라 「혜택 내용」이 먼저 나오게** — 스킨에 **회원 혜택 안내 시트 신설**(`#benefitDim` · chat-sheet 디자인 계승 · `.bn-row` 5항목: 가입 즉시 물방울 5개 / 귤나무 물 100개 = 제철 귤 4.5kg 무료배송 / 룰렛 배송완료 1건당 1회 꽝없음 / 후기 물방울 +1 / 시즌 오픈 알림) + 하단 [로그인]·[가입하고 혜택 받기]. **`?benefit=1` 자동 열기**(카카오 버튼 전용) · 카카오 메뉴 URL `https://akkome.com/?benefit=1` 등록 완료. 실측 **8/8**(자동 열림·항목 5·가로넘침 0·링크 정상·닫기·홈 무회귀·평소 접속 무영향·에러 0).
  - ℹ️ 혜택 문구는 **현재 실제 가동 중인 혜택**만 기재(코드·DB 확인분).
  - 🚨 **#303-b·c(쿠폰) — 잘못 넣을 뻔한 것을 대표 확인으로 잡음**: 대표 기억("첫 가입 2천원")에 따라 「신규 가입 쿠폰 2,000원」을 넣었으나, **대표가 카페24 [프로모션] 캡처로 확인한 실제 발급 쿠폰은 달랐다** — ⓐ**2,000원 = 「생일 축하」 쿠폰**(신규 가입 아님) ⓑ**500원 = 「리뷰 작성」 쿠폰(포토/텍스트 각각)** ⓒ**신규 가입 쿠폰은 없음**(진행중인 고객혜택도 0건). 전부 발급일로부터 14일.
    - 즉시 교정: 「신규 가입 쿠폰」 삭제 → **🎂 생일 축하 쿠폰 2,000원**(생일 자동 지급·14일) 신설 · 후기 항목을 **✍️ 후기 쓰면 500원 쿠폰 + 물방울 1개**로 통합(사진·글 후기 모두).
    - 🔴 **교훈: 고객에게 보이는 혜택 문구는 실제 설정과 1:1로 대조하기 전에 올리지 말 것.** 스킨엔 근거가 없고 쿠폰 API 스코프(`mall.read_promotion`)도 없어 내가 검증할 수단이 없었다 — 이럴 땐 **먼저 대표 확인을 받고 올리는 것이 맞았다**(없는 혜택 광고 = 클레임 직결). 코드에 경고 주석 삽입.
- ✅ **#302(대표): 카카오 [AI상담] 버튼 → 홈이 아니라 「챗」이 바로 열리게** — 스킨 index.html에 **`?chat=1`(또는 `#chat`) 자동 열기** 추가(로드 600ms 후 `openChat()`). 카카오 메뉴 URL을 **`https://akkome.com/?chat=1`** 로 변경·상시노출 등록 완료. 실측: `?chat=1` → 챗 자동 열림·AI 실답변(배송 시나리오) / 평소 접속 → 안 열림(무영향) · 에러 0 · **5/5**.
- 🎯🎯 **#301(대표 확정 Ⓐ) — 카카오 챗봇 OFF · 「상담 채팅 전용」 전환 완료(클코 브라우저 직접, 8/9)**
  - **대표 요구 = "네이버 톡톡과 동일한 조건"**: 문의 오면 회사폰 알림·안읽음 표시·직원이 직접 답장. **카카오는 「챗봇 채팅」과 「상담 채팅」이 배타적**이라 챗봇이 켜져 있으면 그 셋이 전부 불가(무료 채널엔 톡톡식 병행 구조가 없음). → **챗봇을 끄는 것이 유일한 해법**(상담톡 유료 도입 전까지). 대표 승인.
  - 실행 순서(⚠️함정): ①**커스텀 메뉴 3번째 버튼이 「챗봇」 연결이라 해제가 거부됨**("챗봇과 연결된 메뉴가 존재하여 채널 연결을 끊을 수 없습니다") → 발화 삭제 후 **연결 유형을 URL(`https://akkome.com/`)로 변경·메뉴명 「AI상담」**(자사몰 AI챗으로 유도 — AI를 완전히 잃지 않는 보완) → 임시저장 → 상시노출 메뉴 등록 ②챗봇 설정 > 운영 채널 **[연결 끊기] → 해제** 완료(운영 채널 빈 상태 확인).
  - 결과: 카카오 문의는 이제 **[내 채팅]에 뜨고 · 회사폰 카카오 알림 · 안읽음 표시 · 직원 답장** 가능. AI는 **톡톡·자사몰에서 계속 가동**.
  - ⚠️ **과거 챗봇 대화는 소급 불가**(「전체 상담」 필터로도 미노출 실측) — 대표 캡처의 09:13~09:17 미답변 손님들은 [내 채팅]에 없다. 그 손님들에게 연락하려면 **주문 기록의 연락처로 직접 연락**하는 수밖에 없다.
  - 🔁 **롤백(상담톡 도입 시 등)**: 챗봇 설정 > 운영 채널 선택하기 > 제주아꼼이네 재연결 + 메뉴 3번 버튼을 챗봇(발화 "문의드려요")으로 복구. 봇·시나리오·스킬은 그대로 보존됨(봇 ID `6a727d924ea9d954e49c663e`).
  - ℹ️ **대표 질문 "답변이 있는데 왜 [발송실패]?"**: AI가 답을 만들었지만 **카카오 콜백 발송에 실패**한 것 — 카카오 콜백은 유효시간이 짧은데(문서 1분/5분 상충) **30초 버퍼 + AI 생성 9초**라 아슬아슬했다. 챗봇 OFF로 이 문제 자체가 소멸.
- 🔴🔴 **#300(대표 실물 "카카오 미답변을 어디서 답해주나?") — 답할 수단이 아예 없었다**(톡톡봇 `2509b7a`)
  - 실태: ⓐ카카오 **「챗봇 채팅」 대화는 채널 관리자 [내 채팅]에 뜨지 않음**(구조) ⓑ회사프로그램 [문의 관리]도 **카카오는 조회 전용**(답장 API 미연결) → **봇이 침묵하면 손님은 방치되고 직원은 손도 못 댐**. 대표 캡처 2장으로 확인(문의 관리엔 카카오 미답변 4건 보이는데 직원 답변 열은 전부 '-', 카카오 [내 채팅]엔 그 손님들이 없음).
  - 교정: **카카오 손님에 한해 AI가 SKIP하면 침묵 대신 「상담원 연결」 안내 발송** — "채팅방 아래 [상담 연결] 버튼을 눌러주세요" + 고객센터 번호. 손님이 누르면 **상담 채팅으로 전환 → [내 채팅]에 뜨고 회사폰 카카오 알림도 오며 직원이 직접 답장 가능**. 로그는 `[상담원 연결 안내]`.
  - 🔴 **이것이 대표가 원하던 ⓑ의 장점(알림·직원 답장)을 AI를 잃지 않고 얻는 구조** — AI가 답할 수 있는 건 AI가, 못 하는 건 상담원으로.
  - 톡톡은 직원이 판매자센터에서 직접 보고 답하므로 **종전대로 침묵**(무회귀).
  - ⚠️ **이미 쌓인 과거 미답변(대표 캡처의 4건 등)은 소급 답변 불가** — 그 대화들은 챗봇 방이라 접근 경로가 없다. 앞으로 들어오는 건부터 상담원 연결로 잡힌다.
- 🔴 **#299(대표 확인 요청으로 발견 — 중대): 봇이 한 번 답하면 그 손님에게 30분 침묵하고 있었다**(톡톡봇 `4f6aecb`)
  - 대표 질문("30초 뒤 답변하고 30분 후 재응답이 아니라 계속 AI 답변 맞지?")에 코드 확인 → **아니었음**. `sendMessage` 직후 `lastResponseTime.set()`으로 **쿨다운이 걸려** 이후 문의가 `[쿨다운-무응답]`. 실측 = 08-08 11:51 봇 답변 → 11:52 재문의 → 쿨다운 무응답.
  - 톡톡 시절 전제("봇이 한 번 답하면 이후는 직원이 이어받는다")였으나 **카카오·자사몰은 직원 응대 화면이 사실상 안 보여 손님이 그대로 방치**된다. → **봇 답변 후 쿨다운 제거**(연속 AI 응대). 쿨다운은 **직원이 답했을 때만** 유지(설정 지점 3곳 전부 직원 응답 경로임을 확인) = 직원 개입 시 30분 물러나는 원래 의도는 그대로.
  - 3채널 공통 영향(톡톡 포함) — 되돌리려면 그 한 줄 복원.
- ✅ **#298(대표 실물 "알림이 안 와 직접 처리를 못 한다") — 텔레그램 알림 확대**(회사 v5.9.233·234)
  - 기존 알림 폴러(`notifyTelegram`)는 있었으나 ⓐ**대표 실제 문장 "…취소가능할까요?!"를 정규식이 못 잡았고**(취소해주세요/부탁류만 감지) ⓑ**AI가 답 못 한 문의 자체에 알림이 없었다**.
  - 교정: 취소·반품 어미 확장(가능/되나/될까/하려…) + **`[SKIP-무응답]` 건을 알림 대상에 추가** + **채널 표시**(톡톡/카카오/자사몰·`·봇 미답변`) + 폴러 **3분→1분**.
  - ⚠️**과잉 알림 방지(#298-b)**: 미답변이라고 전부 알리면 "감사합니다 행복한날되세요~" 같은 인사·덕담까지 폰이 울림(실측) → **질문·요청·불만 신호가 있는 건만** 알림(`NEEDS_STAFF_RE`). 검증 = 감지 10·오탐금지 7 **17/17**, 선별 알려야 6·조용해야 7 **13/13**.
  - 🔴 **대표 오해 정정 기록**: ⓑ(상담원 연결)로 바꾸면 **그 대화에서 AI가 빠진다**("AI 하면서 알림도" 불가). AI 유지 + 알림은 **텔레그램 경로(ⓐ)** 가 유일한 답 — 카카오 「챗봇 채팅」은 구조상 카카오 앱 상담 목록·푸시 대상이 아님.
  - 🚨**자책**: v5.9.234 배포 때 **PowerShell `Set-Content`로 CHANGELOG.md·version.js를 써서 한글 전멸**(CP949 오독+BOM). git 복구·재커밋(`d4ae755`). **CLAUDE.md에 이미 적어둔 함정을 다시 밟았다 — 파일 쓰기는 예외 없이 Write/Edit 도구 또는 git bash 리다이렉션.**
- ✅ **#297(대표 실물): [상담채팅연결] 눌러도 봇이 무반응 — 원인 규명·해결(톡톡봇 `f384dbf` 배포)**
  - 🔴 **원인**: 버튼은 **"문의드려요" 발화만** 보내는데 **질문 내용이 없어** AI가 "재료 없으면 침묵" 원칙대로 **SKIP**. DB 실측 = `message_logs` 해당 건 전부 `[SKIP-무응답]`, 반면 **질문이 붙은 건은 정상 답변**(예: "문의드려요 + 지금 청귤 주문 가능한지?" → 품목-청귤 답변). 대표 관찰("문의드려요에만 대답 안 함")과 100% 일치.
  - ❌ **시나리오 추가 방식은 실패**: `inquiry_scenarios`에 "문의드려요" 인사 시나리오(#53)를 넣어도 **AI가 여전히 SKIP**(재현 도구 `qna_sim_request`로 확인 — 재료=null). 시나리오 캐시 아님(`qnaScenarios()`는 매번 DB 조회). → #53 **비활성화**(다른 "…문의드려요" 질문을 가로챌 여지 제거).
  - ✅ **해결(대표 제안 채택)**: `kakao-skill.js`에서 **메뉴 버튼 발화는 AI를 태우지 않고 즉시 안내로 동기 응답**. 30초 버퍼도 없고 AI SKIP과 무관하게 **항상** 뜬다. 대상 발화 = 문의드려요/문의 드려요/문의하기/상담채팅연결/상담연결 등. 손님의 **다음 메시지부터는 기존 파이프라인 그대로**(무회귀). `logMessage`로 기록(문의 관리에 "상담 시작 안내(메뉴 버튼)").
  - 실측(실서버 POST): `문의드려요`·`상담채팅연결` → **즉시 안내 200** / `배송 언제 오나요?` → 종전 대기문구 후 AI(무회귀).
  - ⚠️ **미해결(대표 확정 필요) — 회사폰 알림**: **카카오 구조상 「챗봇 채팅」 대화는 채널 앱 「내 채팅」에 안 뜨고 푸시 알림도 안 온다**(상담원 채팅만 대상). 선택지 ⓐ**텔레그램 알림 신설**(권고 — AI 응대 유지하면서 회사폰에 울림. 대화는 이미 회사프로그램 [문의 관리 > 톡톡 문의]에 카카오채널 배지로 전부 보임) ⓑ버튼을 상담원 연결로 전환(알림·앱 노출은 되지만 **그 대화는 AI가 답하지 않음**).
- ✅ **#296(대표): 카카오 채팅방 메뉴 3번째 버튼 「문의하기」 → 「상담채팅연결」 변경 완료**(클코 브라우저 직접 시공, 8/9)
  - 🔴 **메뉴명은 최대 6자** — 대표 지시안 "바로상담채팅연결"은 8자라 불가 → **"상담채팅연결"(6/6자)** 로 등록(‘바로’만 생략). 바꾸려면 같은 자리에서 6자 이내로 재입력.
  - **동작은 무접촉**: 메뉴 연결 = **챗봇 / 발화 "문의드려요"** 그대로 → 폴백 블록 → `/kakao-skill` → **톡톡·자사몰과 같은 시나리오·시기별 지식으로 AI 응대**(대표 질문 답변 = 예, AI 봇이 그대로 운영·직원 답변 시 30분 쿨다운 동일).
  - 경로: 카카오비즈니스 파트너센터 > 채팅 > **채팅방 메뉴** > 커스텀 메뉴 [수정하기] > 미리보기에서 3번째 버튼 클릭 > 메뉴명 수정 > **임시저장 → [상시노출 메뉴 등록] → 확인**. 「현재 등록된 상시노출 메뉴 확인하기」로 **전체상품보기·회원혜택·상담채팅연결** 실등록 확인.
  - ⚠️함정 재확인: ①메뉴명 입력 후 필드를 클릭하면 **메뉴 연결 드롭다운이 열림** → ESC로 닫아야 발화 설정 보존 ②아이콘을 다시 고르면 메뉴명이 추천명으로 덮어써짐 ③임시저장만으로는 실채팅방 미반영.
- ⚠️ ~~옵션 행 선택은 일부러 손대지 않음~~ → #291-b에서 touchend 경로 추가(위 참조)
- (구) 옵션 행 선택 관련 메모 — 대표가 정상 동작 확인한 부분이고, 여기에 같은 터치 처리를 넣으면 touchend+click 이중 발화로 **선택→해제가 연달아 일어나 "아무 일도 안 일어나는" 사고**가 날 수 있음.

**#283(대표 실물 — 문의 표 일시 잘림) · v5.9.232 / app.js v=357 · styles.css v=121**
- 원인 2겹: ⓐ`toLocaleString('ko-KR', {month,day,hour,minute})`가 **"08. 08. 오후 01:23"(17자)** 로 길다 ⓑ`.data-table td` 기본 패딩 **좌우 16px**이 좁은 열의 글자 자리를 잠식 → 92px 열에서 분이 잘림.
- 교정: **공용 `aoFmtDtShort()` 신설**("08-08 13:23" · 24시간 · tabular-nums) → **톡톡 문의·상품문의·고객문의 3표 통일** + `.cell-dt`(패딩 12/6·13px)·`.cell-ch`(패딩 8/8) 전용 클래스 + 일시 열 92→100px 재배분.
- 검증 방식(재사용 — `scripts/verify-298-dt.js`): **실 `styles.css`를 로드하고 app.js에서 포맷터 함수를 그대로 뽑아** 표를 렌더 → Range로 **실글자폭 vs 셀 가용폭** 대조. 결과: 폰·PC 모두 일시 67.6/82px·채널 배지 68.9/76px — **넘침 0**(로그인 벽 없이 폰트 메트릭까지 실측 가능한 방법).

**🔴 함정 기록(다음 세션 필독)**
- **검증 스크립트에서 상품 데이터는 `window.STORE_DATA`가 정본 — `window.V5.D`는 존재하지 않는다**(undefined). 이걸 몰라 "상품 0건"으로 보여 검증 항목이 조용히 건너뛰어졌음(사이트는 정상). 폴백을 넣어둔 스크립트만 우연히 통과했던 것.
- 인라인 `node -e`·heredoc은 한글·역슬래시를 먹는다 → **패치·프로브는 파일 도구로 작성**.
- **문법 검사(`scripts/check-html.js` 신설 — HTML 내 script 블록 전수 검사)와 업로드는 반드시 분리 실행**(8/7 사고 재발 방지).

**⚠️ 대표 정리 필요**: 검증으로 생긴 시즌 대기 명단 테스트 **3건**(`0109980560`·`0109251036`·`0108352805`) 삭제.

### ✅ 8/8 전환 후 대표 지시 처리 (v5.9.231)
- **룰렛권 = 배송완료(N40) 1건당 1회**(대표 확정 — 종전 주문 즉시). 서버 `countPaidOrders(key, statuses)` 파라미터화 + `countDeliveredOrders` 신설(캐시 키 상태별 분리)·wallet tickets·spin 검증 모두 배송완료 기준. **반품·취소 후 룰렛 사용 사고 원천 차단**. 후기 물방울은 현행(리뷰 작성 시) 유지 — 대표 확정.
- **룰렛/귤나무 문구**(대표 지정): "✨하루 1번 무료 참여" 배지 **삭제** · "주문 1건당" → "**배송완료 1건당**" · 경품 "하우스귤 3kg 1박스" → "**제철 귤 4.5kg (그때 가장 싱싱한 품목)**"(시기별 품목 변동 대응) · 귤나무 수확 문구·텔레그램 알림도 동일 문구.
- **🔔 시즌 오픈 대기 신청 실가동**(그동안 준비중 모달만 떴음): 공개 API `POST /api/public/season-waitlist`(분당 6회·중복 차단·연락처 숫자 검증·감사) + 스킨 대기 신청 시트(연락처 1칸·번호 기억). **접수분은 [문의 관리 > 시즌 오픈 대기 신청] 명단에 created_by='자사몰'로 적재** — 실측 DB 등록 확인.
  - 🚨함정: 상품명을 DOM에서 추출하니 실패("상품번호 133…"로 저장) → **버튼 렌더 시점에 `data-wait-nm`로 상품명을 직접 심어** 해결(실측 재검 = 정확한 상품명 저장).
- **PC 잔여 깨짐 3건 교정**: ⓐ로그인 등 서브 화면 상단 여백 **PC만 124px** → 24px(#container/#contents 상단 여백 해제·.path 숨김) ⓑ주문서 헤더 898px 삐져나옴 → 480px 정렬 ⓒ주문내역 `h3.title`(Search)·사이드바 잔재 1440px → 숨김.
- 🚨**자책(무회귀 위반 — 재발 방지)**: 패치 스크립트에서 **따옴표 1개 초과**(`+''">`)로 index.html 스크립트 블록 2개가 문법 오류 → **깨진 파일을 실서비스에 업로드**했다가 즉시 복구. 원인 = 치환 문자열 끝을 `+'`로 두고 앵커 뒤 `'">`와 이어붙인 것. **교훈: 업로드 전에 반드시 `check-scripts.js`(HTML 내 script 블록 문법 검사)를 통과시킬 것 — 이번엔 업로드와 검사를 같은 명령에 넣어 검사 실패에도 업로드가 실행됐다. 검사→업로드는 반드시 분리하거나 `if ($?)`로 가드할 것.**
- 검증: 실서비스 전수 재점검 통과(구매 사슬 4.3초·장바구니·챗·게임·로그인 게이트·에러 0) + 대기 신청 실등록 + PC 여백 24px.
- ⚠️**대표 할 일**: [문의 관리 > 시즌 오픈 대기 신청]에 **검증용 2건**(0109980560·0109251036) 등록됨 — 삭제해 주세요.

### 🎉 ② 스킨 정식 전환 **완료 (8/8 — akkome.com 실서비스 = skin6 리뉴얼)**
- **스킨 정체 확정**: 디자인 보관함에 "skin6"이라는 이름이 없어 혼선 → **PC 탭의 「무료 스킨 - 기업형」(등록 2026-08-05)이 우리 skin6**. 편집창 URL `?skin_no=11&skin_code=skin6&shop_no=1`로 확증(CLAUDE.md 기록과 일치). 앞으로 관리자에서 이 이름으로 찾을 것.
- **전환 실행**: 대표가 PC 탭에서 [대표 디자인 설정]. 모바일 탭에는 skin6이 없지만 **모바일 전용 디자인이 '사용 안 함'이라 PC 대표디자인이 모바일에도 그대로 노출** — 모바일 탭 추가 작업 불요(모바일 복사는 파일 2벌 관리가 되어 금지).
- ⚠️**캐시 전파 지연 실측**: 전환 직후 모바일 UA는 즉시 신스킨, **PC UA는 몇 분간 구 스킨**이 나왔다가 자동 전환됨. 전환 직후 "PC가 안 바뀐다"는 정상 — 몇 분 기다릴 것(대표 실물도 동일 시점에 정상 확인).
- **전환 후 실서비스 전수 점검 전항목 통과**: 홈 렌더·상품 상세·구매 사슬(4.6초)·장바구니 수량/주문 바·로그인·귤나무 게이트·룰렛·캘린더·**주문내역 로그인 게이트(returnUrl에 프리픽스 없음 = 전환 후 정상)**·자사몰 AI챗(337자)·모바일 에러 0 / **PC 4화면 정렬·에러 0** / **카카오 채팅방 메뉴 링크 2종 실도달**(akkome.com·가입 약관).
  - ℹ️ 홈 탭바는 `nav.tabbar`(서브는 `.akm-tabbar`) — 점검 스크립트 셀렉터만 달랐고 실물은 정상.
- **잔여(대표 판단)**: m.akkome.com은 구 모바일 스킨(나인코드)이 그대로 살아 있음 — akkome.com으로 들어오는 손님엔 무영향. 정리 여부는 2~4주 안정 후 결정. 구 스킨(아모르2)·기존 상품 23종도 보존(롤백 안전핀).

### ②-old 사전 점검 기록 (전환 전 · 18/18 통과)
- **점검 결과**(scripts\pretransition-check.js — 재사용 가능): 홈 렌더·상품 상세·옵션 마지막행 선택·구매 사슬(수량2, 3.7초)·장바구니 수량 반영·선택 주문 바·**수량 무리로드(리로드 0)**·로그인·가입약관·캘린더·룰렛·귤나무 게이트·주문내역/주소록 로그인 게이트·**자사몰 AI챗 실답변(337자)**·모바일 에러 0·**PC 전 화면 정렬·가로넘침 0**·PC 에러 0.
  - ⚠️"홈 상품카드 11장"은 결함 아님 — 현재 **판매중 5종(91·92·93·94·100)+특가·추천·체험 큐레이션**이 전부 노출된 정상 상태(품절 14종은 캘린더로 분리 = 설계대로). 점검 스크립트의 기준값(20+)이 과했던 것.
- **하드코딩 재확인**: 스킨 전체에 `/skin-skin6` 리터럴 0건(주석 1건뿐)·동적 매칭 9곳은 전환 후 자동으로 빈 값이 되어 정상.
- **전환 절차(대표)**: 카페24 관리자 → [디자인] → 디자인 보관함/스킨 목록 → **skin6을 「대표 디자인」으로 지정**(1클릭). 롤백 = 구 스킨(아모르2) 재지정 1클릭(구 스킨·기존 상품 23종 모두 보존 중이라 즉시 복귀 가능).
- **전환 직후 클코 재검 예정**: akkome.com 루트에서 동일 점검 재실행 + 카카오 채팅방 메뉴 링크(akkome.com)·가입완료 '메인으로'(href='/')·주문서 마이 버튼 실동작.

## 🎯 착수 지시 (8/8 오전 인계 — 대표 확정: ①자사몰 AI챗 ②스킨 정식 전환)

**대표 확정 순서: 자사몰 AI챗 연결 → 스킨 정식 전환(GO) → PC 화면 대응.** 아래 계획대로 진행하되 무회귀 원칙(실DB·API·실렌더 3겹 검증) 유지.

### ① 자사몰 AI챗 ("궁금한 건 언제나 물어보십셔" — 3채널 통합 완성)
- **기성 자산**: 스킨 챗 UI = index.html `CHAT_OPEN=false` 단일 스위치(true 한 줄 = 입력창 활성+실API — #236-3단계 시공분·OFF 시 호출 0 실증). 시나리오·시기별 지식 = 톡톡·카카오와 같은 DB(inquiry_scenarios + seasonScenariosToday).
- **시공 순서**: ⓐ회사프로그램에 자사몰 챗 수신 API 신설(공개 라우트 — mall-api /pub 패턴 참고: CORS 화이트리스트·레이트리밋. 회원 식별 = akm_mid 릴레이 or 비회원 익명 키) ⓑ**30초 버퍼·직원 우선·30분 쿨다운 = 톡톡봇 라임 이식**(톡톡봇 server.js 버퍼링 타이머 224행 부근·bot_timing DB 공유 구조 참고 — 대표 확정: 3채널 동일 라임) ⓒ응답 생성 = 기존 `qnaGenerate` 공용(재료 없으면 SKIP·꼬리 문구 서버 강제) ⓓ스킨 CHAT_OPEN=true + 폴링/콜백 방식으로 답변 표시(웹챗은 콜백 불가 — 클라이언트 폴링 권장) ⓔmessage_logs에 `mall:` 프리픽스로 로그 분리(카카오 `kakao:` 패턴 동일).
- ⚠️직원 답변 채널: 자사몰 챗 직원 응대 화면이 없음 — 1차는 AI 전용+"자세한 상담은 카카오채널/톡톡" 안내로 가고, 직원 콘솔은 후속(대표와 협의).

### ② 스킨 정식 전환 (대표 "전환 GO" 시)
- **사전 전수 점검**(보존 스크립트 재실행): verify-261(구매 사슬·수량)·verify-264(장바구니 수량/체크)·verify-270(주소록·farm)·verify-271(릴레이)·verify-274(layout 무회귀)·verify-277(farm 링크)·e2e-full.js(실가입 E2E — 테스트 계정 1개 추가 생성됨 주의). + 홈 렌더·상품 20종·캘린더·룰렛.
- **전환 실행**: 대표가 관리자 [디자인 관리] → skin6 대표 디자인 지정(1클릭). 클코는 직전에 스킨 파일 전체 백업 확인.
- **직후 재검**: akkome.com 루트에서 위 스모크 재실행(이제 미리보기 아님 — 프리픽스 재작성 소멸 환경. `/skin-skin6` 경로 의존 코드는 pre 매칭이 빈 값이 되므로 자동 정상 — #269~#271에서 리터럴 방식으로 통일해둠). **카카오 채팅방 메뉴·가입완료 '메인으로'(href='/') 실동작도 재확인**.
- **동반 작업**: cafe24_sync 타이머 ON(dry → 이상 없으면 fix — 대표 확정), 구 스킨·기존 상품 정리는 2~4주 안정 후.
- ⚠️전환 후 함정 예상: 미리보기 서버가 해주던 라우트 재작성이 사라짐 — 코드에 `/skin-skin6` 하드코딩이 남아 있으면 결함(전수 grep 필요: 스킨 파일에서 `skin-skin6` 검색 — pre 동적 매칭은 무해, 리터럴은 교정).
- 테스트 실회원 2명(akmcliv2xu·akmclj3w49 — "클로드테스트") 존재 — 대표가 관리자에서 삭제 가능.

### ③ PC 화면 대응 (대표: "PC로 보면 모바일 기준이라 깨짐")
- 방향: index 등 스킨이 모바일 우선(390px 상정) — **데스크톱은 중앙 고정 폭 컨테이너**(예: max-width 480~520px 중앙 정렬 + 좌우 배경) 방식이 최소 침습·무회귀(토스/배민 웹 방식). 미디어쿼리 `@media (min-width: 768px)`로 body 래퍼만 제약 — 내부 레이아웃 무수정.
- 대상: index.html + layout.html(서브 공통) + farm/roulette/calendar. 하단 고정 바(pd-bar·orderFixArea·탭바)도 같은 폭으로 중앙 고정 필요(léft:50%; transform 또는 max-width+margin:auto).

### 🔧 이 세션(8/8 오전)의 실측 함정 모음 — 새 세션 필독
- 카페24 치환: `{$id}`가 회원ID 정답(member_id 아님) — 단 **마크업(span 래핑)으로 렌더되므로 속성 주입 금지·텍스트 노드(<i data-akm-tid>)로만**. layout 파일에선 스크립트 안 `{`+`$` 연속 리터럴을 치환 엔진이 먹어 SyntaxError(String.fromCharCode 조립로 우회).
- 미리보기 서버: JS 라우트 리터럴 재작성 — **직접 프리픽스 붙이면 이중**(returnUrl도 독립 리터럴로 두면 재작성됨). myshop/index 리다이렉트 pre+리터럴 이중 프리픽스가 "마이 404"의 실체였음(교정 완료).
- 카카오 챗봇: 기존 상담 이력 방은 봇 미개입(손님이 방 나가고 새 채팅부터) · 봇 답변 후 30분 쿨다운은 정상 스펙 · 채팅방 메뉴는 임시저장+[상시노출 메뉴 등록]까지 눌러야 실반영 · 아이콘 선택 시 메뉴명 덮어써짐.
- 검증: 로그인 벽은 테스트 계정 실가입(e2e-full.js)으로 돌파 — 가입 = 약관 전체동의→다음→ID 중복확인→가입(재시도 루프 필요).

### 🌆 8/7 저녁~밤 세션 (#250~#261 — 실서비스 준비 총력전)
**버전: 회사 v5.9.226 / 오픈 전환 완료(신규 19종 실판매) / 게임 서버 결선 라이브 / basket·상세 구매 사슬 v6 마감.**
- **#260(수량 주문)**: 상세 pd-bar에 수량 스테퍼(pbQty) — 카페24 수량 input은 `#quantity`(숨김·무효)가 아니라 **옵션 선택 후 생성되는 `input[name="quantity_opt[]"]`**에 세팅해야 실반영(실측 함정). basket 카드 = **grid 재배치**(sumPrice가 원 CSS에서 absolute → 겹침 원흉 — static화·3행 grid로 원천 차단).
- **#260 후속(대표 실물 "장바구니 실패")**: 같은 상품·같은 옵션 재담기 시 ①iframe `confirm`을 무조건 false로 응답해 "이미 담긴 상품 추가?" 질문에서 담기 취소 ②수량만 합쳐지면 줄 수(basket_count) 불변 → 실패 오판. 교정 = **confirm 분기(/이동/ 질문만 false)** + **프로브(order/akm-count.html)에 품목별 수량 렌더(`AKMQ[{$quantity}]`+AKMQOK 마커) → 줄 수 또는 총수량 증가 = 성공**. 재검: 빈바구니 수량2 3.9s·재담기 수량3(합침 5) 3.8s 전부 합격.
- **#261(선택 주문 바)**: basket 하단 고정 바 = **체크된 상품만 합산**(선택수량 N개·금액 재계산) + 주문 클릭 = **선택상품주문 액션 프록시**(`#orderFixItem`의 `{$action_order_select}` 앵커 클릭 — checked_product 파라미터로 선택분만 주문서행 실측) + 두 칸 합친 폭 대형 라운드 버튼("선택수량 N개"/"{금액}원 · 주문하기" 동일 폰트). ⚠️ `#orderFixArea a.btnSubmit`(#250 블록)이 특이도로 이겨 radius 눌림 → 동급 셀렉터 재선언으로 해결.
- **카페24 스코프 대장정(✅ 완결 21:28)**: mains(메인 진열)는 store가 아니라 **`mall.read_category`/`write_category` 요구**(공식 문서 확정 — 재동의 후에도 403이라 판명). 대표가 개발자센터에 상품분류(Category) 읽기+쓰기 추가 → 재동의 → **토큰 스코프 9종 전원 실측 확인**(order·product2·store2·community2·category2 — 러너 `status` 액션 v5.9.225 신설·server_version 동봉). **mains GET 실호출 성공**: 그룹 2 신상품·3 전체상품(신규 5종+기존 24종)·4 베스트·5 무명. **메인 품절 구상품 제거는 실행 안 함** — 대표 오후 확정("판매중이 위로 정렬돼 제거 불요·현상 유지") 유지, 능력만 확보. 후기 게시판 읽기 실측용 raw 가드 boards GET 허용 = v5.9.227. **boards·boards/4/articles 실조회 성공**(board 4 = 고객후기·게시물에 member_id·order_id 실림 — #259 후기 검증 파이프라인 실가동 조건 완비. 회원 실작성 시나리오 실측은 대표 실주문 계정 후기 1건으로 확인 권장).
- **#267-c(회원ID 후보 실측 체계)**: E 미채움(마이 경유 `{$member_id}`도 미치환) → **후보 변수({$id}·{$log_member_id}) 일괄 심기 + 치환된 첫 깨끗한 값 자동 채택**: 게임 3면 probe(data-akm-id2/id3)·마이 릴레이·**layout.html 공용 릴레이**(로그인 후 layout 페이지 지나기만 하면 심김)·진단 v3(/akm-diag.html — 1~4 격자). 🚨함정 실측: **layout 파일에선 스크립트 안 `'{'+'$'` 두 글자를 나란히 쓰면 치환 엔진이 코드를 먹어 SyntaxError**(페이지 파일은 무사 — 룰렛 연결식의 이유) → layout 릴레이는 String.fromCharCode 조립로 재작성·에러 0 재검. **✅ 대표 캡처로 확정(8/8): 정답 = `{$id}`**(Layout_stateLogon — member_id 아님). 후보 자동 채택 체계라 추가 배포 없이 즉시 가동. 최종 실증 = 대표 farm 진입 시 mall_members 실행(row) 생성+join +5 지급 DB 확인(대기).
- **🚀#272(카카오톡 채널 챗봇 라이브 — 8/8 09:46 대표 GO·클코 브라우저 직접 시공)**: 오픈빌더에서 ①스킬 아꼼이네-AI응대(URL=/kakao-skill) — 대표가 등록 ②폴백 블록 = 파라미터 스킬 선택+스킬데이터 응답 단독 ③Callback ON(대기문구 비움 = 무언 대기) ④전체 배포 v1.0 — 전부 클로드가 claude-in-chrome으로 실클릭(대표는 카카오 로그인만). **실증**: 봇테스트 발화 → 서버 도달 → 30초 버퍼 → AI 시나리오 6(가격 문의) 실답변 생성 → message_logs 기록(user_id `kakao:` 프리픽스·response_source ai). 롤백 = 폴백 블록 스킬 해제 1클릭. **✅ 실채널 완전 개통(8/8 10:2x — 대표 폰 답변 실수신 확인)**. **#273 채팅방 메뉴(8/8 — 클코 브라우저 직접)**: 커스텀 메뉴 = **1단 3버튼·노랑**(벤치마크 온브릭스 스타일) — 전체상품보기(URL akkome.com)·회원혜택(URL 가입 agreement)·문의하기(**챗봇 연결·발화 "문의드려요"** → 폴백 AI 응대 시작). 함정: 아이콘 선택 시 메뉴명이 추천명으로 덮어써짐(재입력 필요)·임시저장 후 [상시노출 메뉴 등록]까지 눌러야 실반영. "챗봇 채팅중" 상단 표기 = 카카오 고정 UI(변경 불가 — 대표 문의 답변). 🚨함정 실측 2건: ⓐ**기존 상담 이력 방은 상담원 모드 고정 — 봇은 새 채팅(방 나가기 후 재시작·신규 손님)부터 개입**(대표 첫 발화 무응답의 실체. 기존 손님은 새 채팅 시작 시 자연 전환) ⓑ배포 직후 1~2분 전파 지연. 운영: 상담함은 기존 그대로 병행(봇 미개입 방 유입 — 직원 마무리)·직원 답변 시 30분 쿨다운은 서버 라임 공유. **자사몰 AI챗("궁금한 건 물어보십셔") 연결 = 다음 시공**(CHAT_OPEN 스위치+30초/쿨다운 라임 서버 이식 — 3채널 공통 시나리오).
- **🏆#267-d(E2E 실로그인 검증 — 종결)**: 대표 3연속 재보고에 **테스트 계정 실가입(headless — 가입 절차: 약관 전체 동의→다음→ID 중복확인 클릭→가입, 실패 시 재시도 루프)으로 끝까지 실측**. 원인 3중 확정·교정: ①**주문서 마이 404의 실체 = myshop/index 리다이렉트 `pre+'/index.html'` 이중 프리픽스**(서버가 리터럴 재작성 — /skin-skin6/skin-skin6/ 실측) → 리터럴 단독 ②**`{$id}`는 `<span class="xans-member-var-id">ID</span>` 래핑 렌더** — 속성 주입 시 속성 파손(실측 `"<span class="`) → **텍스트 노드(<i data-akm-tid>) 추출**(#251 교훈 3번째 재현) ③join_result 아이디는 **마스킹(*****) 렌더** — 릴레이 오염 → 전 지점 엄격 검증 `/^[A-Za-z0-9@._-]{3,60}$/`+오염 자동 삭제. **최종 실측: farm 진입 = "+5 토스트"·/wallet·/join-bonus 200·mall_members 실등록(id19)+join:19 원장 — 실회원 파이프라인 최초 실가동** + 주문서 마이 → v5 마이 정상 도착. skin6 문제 아니었음. ⚠️테스트 실회원 2계정 생성됨(akmcliv2xu·akmclj3w49 — 이름 클로드테스트·대표가 관리자에서 삭제 가능·무해). 검증 교훈: **로그인 벽 앞에서 하네스로 대신하지 말고 테스트 계정을 만들어 끝까지 실측할 것**(e2e-full.js·e2e-farm-diag2.js 보존).
- **#271(재보고 후속 — 8/8)**: DB 실측 = 대표 출석 +1이 서버 미도달(mall_members 무변) → **폰 브라우저가 구버전 farm.html 캐시** 유력 → farm/roulette 진입 링크 전부 `?v=267` 꼬리표(index+akm_frame — 강제 신선화. 이후 farm/roulette 갱신 시 꼬리표 숫자 올릴 것). 주문서 마이 = href 배선 위에 **클릭 capture 가로채기 추가**(#270-b — 카페24 JS 바인딩이 어디로 보내든 로그인/마이 확정 이동. 하네스: 목적지 /myshop/index.html 실측 ✅).
- **#274(가입 +5를 마이에서도 첫 지급 — 8/8)**: DB 실측 = tmdqja1263 완전 실사용(+5·출석·물주기 6회) / **bumiletter는 회원 생성만(마이 지갑 조회발)·+5 없음 — farm에 한 번도 실도달 못 함(404 반복)이 원인**. 교정 = 마이 wallet 응답 `join_bonus:false`면 join-bonus 자동 POST(+토스트) — 로그인만 하면 +5(farm 미방문 회원 커버·멱등이라 이중 지급 불가). 무회귀: 비로그인 마이 /pub 호출 0·에러 0. ⚠️404 재발(로그인/로그아웃 시) = 미리보기 세션 소멸 → 루트가 실서비스(skin5)로 낙하 — **코드 아님·정식 전환 시 구조적 소멸**(검증은 /skin-skin6/ 직주소).
- **#269·#270(대표 실물 2건 — 8/8 아침)**: ⓐ**귤나무 로그인 후 404** = akmGoFarm의 returnUrl `/farm.html`이 프리픽스 없이 실려 로그인 후 루트(실서비스 스킨 — farm 없음)로 떨어짐 → `encodeURIComponent('/farm.html')` **독립 리터럴**로 두면 미리보기 서버가 재작성해 프리픽스 동봉(실측 ✅). 🚨직접 `_pre+`를 붙였다가 **이중 프리픽스**(#243 교훈 재실측 — 라우트 리터럴에 프리픽스 붙이지 말 것) ⓑ**주문서(주문/결제) 헤더 마이 버튼** = btnMy href 빈 값 렌더(실측·ec_orderform/header.html은 SFTP 읽기 잠김) → orderform.html에서 로드 후 배선(로그인→login·마이→myshop — 정상 href는 무접촉·후주입 재배선 2회). 하네스 합격·실물은 대표 재검.
- **#268(주소록 마감 — 대표 실물)**: ①고정/해제 버튼 열 제거(카페24 fix_url이 실효 무반응 — 대표 지시. '-' 잔재 동반 소멸) ②유의사항 고정 안내 문구 삭제 ③3번 문구 = "기본 배송지는 1개만 저장되며, 기본 배송지 변경은 [수정]에서 '기본 배송지' 체크를 해주시면 됩니다."(3파일 공통) ④체크박스 = appearance:none 라운드 9px·체크 시 연두+흰 ✓(SVG data URI). 하네스 합격.
- **#267-b(회원ID 릴레이 — 진단 캡처로 확정)**: 대표 로그인 캡처 실증 = stateLogon `{$member_id}` 빈 값·`{$name}`만 치환·EC 전역 member_id도 없음 → 게임 페이지 단독으론 ID 획득 불가 확정. **릴레이 설계**: ID가 실치환되는 길목(①myshop/index.html 마이 경유 — 리다이렉트 직전 `data-akm-a="{$member_id}"` 저장 ②join_result 아이디 dd — 실렌더 실증)에서 `localStorage['akm_mid_v1']`에 심고, farm·roulette·index 폴백 체인(probe→EC→릴레이)이 읽음. **비로그인 감지 시 릴레이 즉시 삭제**(같은 폰 계정 전환 오염 차단 — 실측: 비로그인이 릴레이 심어도 서버 호출 0·즉시 청소). 진단 v2 = /akm-diag.html(E=릴레이 값. 확인 순서: 로그인→마이 1번→재방문). ⚠️myshop/index의 `{$member_id}` 실치환 여부는 대표 E 캡처로 최종 판정(안 되면 join_result 경로만 유효 — 기존 회원용 추가 길목 필요). 기존 회원 +5 = 코드상 이미 전 회원 최초 1회(join:{id} 멱등 — 가입일 무관). 휴대폰 인증 = 카페24 관리자 본인인증 설정(대표 몫·안내함).
- **🚨#267(게임 계정 분리 — 대표 실물이 잡은 중대 결함)**: 신규 계정에 "출석 이미 완료·가입 +5 미지급" → **DB 실증: mall_members = 테스트 1행뿐 = 실회원의 farm→서버 배선이 0회**. 원인 = probe `{$member_id}`(속성 치환)가 실로그인에서 비어 login=true·id='' → **전 회원이 ':guest' 로컬 키 공유**(그동안의 "가입+5·출석" 체감도 전부 로컬 데모 폴백이었음). 조치: ⓐ회원ID 폴백 = `EC_FRONT_EXTERNAL_SCRIPT_VARIABLE_DATA.member_id`(카페24 앱 전역 — **비로그인 실측으로 전역 실존 확인**) farm·roulette·index 3곳 ⓑ로그인인데 id 미확인 = ':pending' 전용 버킷(guest 공유 원천 차단) ⓒ**/akm-diag.html 진단 페이지**(A=stateLogon member_id·B=name·D=EC 전역 — 대표 로그인 캡처 1장으로 실치환값 확정 → 확정 후 진단 파일 제거). 주소록 추가 결함(대표): #265 카드 CSS(!important)가 카페24 숨김 클래스(displaynone)를 눌러 해제·고정 동시 노출+빈상태 노출 → 복원 CSS + [해제] confirm. ⚠️404 간헐 증상 = **root 관리자 미리보기 세션 만료 시 실서비스 스킨5로 떨어져 skin6 전용 페이지(farm 등) 404** — 코드 아님·정식 전환 시 소멸(검증 접속은 /skin-skin6/ 권장). 휴대폰 인증 = 카페24 관리자 설정 영역(대표 확정 대기).
- **#266(가입 완료 페이지 — 대표 실물)**: ①[메인으로 이동] 404 = `{$redirect_function}` 치환 의존 → **href='/'로 교체**(어느 도메인·스킨에서든 그 스킨의 메인 — akkome.com 전환 후에도 구조적으로 안전. 대표 지적 로직) ②가입 혜택 콜아웃 신설(💧물방울 5개 — 귤나무 첫 방문 시 자동 지급 #256 재사용·서버 호출 0 + [물방울 받으러 가기]=/farm.html) ③v5 라임 카드 디자인. ⚠️join_result는 가입 세션 없이는 즉시 이탈(직접 접근 검증 불가) — 하네스 검증(href '/'·콜아웃·넘침 0)·실물은 대표 다음 가입 시 재검.
- **#265(귤나무 게이트·주소록 스킨링 — 대표 실물)**: ①비로그인이 마이에서 귤나무(물방울 타일·키우러 가기) 클릭 시 404(root 미리보기에서 상대경로 farm.html 실패) → `akmGoFarm()` 게이트: 비로그인 = `/member/login.html?returnUrl=/farm.html`(실측: returnUrl 정상)·로그인 = farm.html. 홈 진입점은 무수정(farm 자체 #258 게이트 담당) ②**배송 주소록 3파일 v5 스킨링**(myshop/addr/list·register·modify — CSS 오버레이만): 8열 PC 표 → 카드(grid — nth-child 배치·긴 주소 keep-all 줄바꿈·가로 넘침 0 하네스 실측)·폼 = 라벨 위 세로(입력 48px)·배송지등록 전폭 진연두 56px. ⚠️실렌더는 로그인 게이트 — 대표 재검.
- **#264(홈 진입점 2건 — 대표 지시)**: ①리뷰보기 카테고리 아이콘 = 스크롤은 되는데 **#227 위임 모달(document capture)이 요소 핸들러보다 먼저 실행**돼 준비중이 같이 뜨던 것 → 그 버튼만 `removeAttribute('data-modal')` 1줄(기존 핸들러 무접촉) ②헤더 "제주에서 우리집까지" = 준비중 → **/myshop/addr/list.html**(배송 주소록 — 비로그인은 카페24 표준 로그인 게이트·returnUrl로 복귀). 실측: 모달 안 뜸+스크롤 정상·주소록 로그인 게이트 정상·타 준비중 모달/상세 오버레이 무회귀·에러 0.
- **#263(수량 무리로드·체크 보존 — 대표 실물 2건)**: 실측 = 카페24 +/−(Basket.addQuantityShortcut)가 **클릭마다 서버 커밋+페이지 리로드**(내비 2회) → 리로드 때 자동 전체체크(#245)가 선택 해제를 덮어씀. 교정 = ①+/−·[변경]·직접입력 **capture 가로채기**(화면 수량·행 금액·#261 바 즉시 갱신 — 리로드 0) ②900ms 디바운스 후 **숨김 iframe에서 Basket.modifyQuantity() 조용한 커밋**(값 대조 검증·실패 시 리로드 폴백) ③체크 상태 sessionStorage 보존(30분) — 저장맵 있으면 자동 전체체크 대신 복원(새 행만 기본 체크) ④주문 클릭 시 미커밋 수량 선반영. 🚨함정: rowKey에서 숫자 전부 벗기면 **5kg/3kg처럼 숫자만 다른 옵션 키 충돌**(실측) — "(N개)"만 제거. 실검증: 리로드 0·해제 유지·서버 합 4 정합·리로드 후 복원 전부 합격.
- **#262(주문내역 옵션 겹침 — 대표 실물)**: 옵션 줄(p.optionGroup)이 원 스킨 CSS의 음수 마진 탈출로 썸네일 위에 겹침 → list.html #262 블록 = **prdBox 2열 grid 고정 + optionGroup 탈출 전면 무효화**(12.5px·줄간격 1.55·keep-all). 하네스 실측(겹침 원흉 재현+오버라이드) 비겹침 합격. ⚠️실카드는 로그인 게이트라 대표 재검.
- 다음 묶음 제안(대표 확정 대기): 게임 실물 쿠폰 자동 발급용 `mall.read/write_promotion`·적립금 `mileage` — 지금 안 넣음(승인제). 넣을 거면 다음 재동의에 합치면 재동의 횟수 절약.

### 🚀 8/7 오전 세션 종료 인계 (컨텍스트 만석)
**운용**: 폴링 OFF(대표 지시 8/7 10:30 — 직접 오더 체제. "지시함 확인" 입력 시에만 poll-light 조회). **버전: 회사 v5.9.214 / app.js v=353 / skin6 재검 캐시 ?v=72.**

### 📋 8/7 낮 병행 세션 종결 기록 (타이머 라벨·디자인 추출 — 이 세션 종료)
- ✅ **v5.9.215**: 타이머 라벨 3건 정리(대표 직접 지시) — 알림톡·문자 "켜도 dry-run만" → "🚀 실발송 가동 중" / 상품 스냅샷 "권한 필요" 제거 / cafe24_sync 한글 라벨은 214 기반영(브라우저 캐시 문제였음).
- ✅ **실측 디자인용 HTML 3종 = 바탕화면**(`디자인용_①주문내역/②회원정보수정/③주문서.html`) — 스킨 뼈대+카페24 CSS 인라인+샘플 실데이터(김제주·하우스감귤·29,900원)+수정금지 주석. 생성 스크립트 소실 대비 없음(1회성). **용도**: 대표가 클로드 디자인에 투입 → 결과물을 받는 클코가 실스킨 이식+Playwright 실물 대조. **주의**: ①②는 구조 1:1 이식 가능 / **③주문서는 캡처 방식으로 선회**(카페24 모듈이라 뼈대 재현 불가·색/버튼/로고만 `form_onetouch_user.css` `#userStyle #mCafe24Order`로) — 디자인 결과물의 "텍스트 내용"은 샘플이니 스타일만 취할 것.
- 🔍 **실측 발견(후속 필요)**: 전환 후 **메인 화면에 신규 SALE 5종(91·92·93·94·100) 미배정** — 메인엔 기존(품절 표시) 32·35·39·42·44·69만 노출 → 첫 화면이 "품절 몰"로 보임. 카테고리 배정과 메인 진열 배정은 별개. 대표에게 전환 세션 지시문 전달함("메인 진열에 신규 5종 배정·품절 기존 제외").
- ℹ️ 대표 문답 확정: akkome.com = 카페24 연결 대표 도메인 실측 확인(스킨 전환해도 주소 불변) · /skin-skin6/은 미리보기 전용 · ?v=N은 캐시 꼬리표.

### 📝 8/7 밤 4차 — #259 후기 물방울 실작성 검증 (대표 확정 — v5.9.222)
- **추보(v5.9.223)**: mall_game_config가 2회 데모값(10·50)으로 되돌아간 원인 = 게임 운영 카드 저장 시 화면 미전송 필드가 코드 기본값으로 리셋되는 구조 → **기본값 자체를 정책값(water_cost 1·목표 100)으로 교정** — 이후 화면 저장해도 유지. DB 재교정 + wallet goal 100 실측.
- **대표 확정**: 후기 지급 = **실제 후기를 써야** 지급(버튼만 누르면 안 됨). 사진 무관·주문 1건당 1회 유지·성공 시 "물방울 1개를 받았어요" 문구.
- **시공**: review-claim이 **카페24 상품후기 게시판(board 4) 실대조** — 지급 수 < min(주문 수, 실후기 수). 미작성 시 "후기를 먼저 작성해 주세요 🍊 작성 후 이 버튼을 누르면 물방울 1개를 드려요!". scope에 `mall.read_community` 추가.
- 🔴 **대표 재동의 1회 필요(3권한 일괄)**: 개발자센터 앱 권한에 **상점(store) 읽기·쓰기 + 게시판(community) 읽기** 추가 → [데이터관리] 카페24 카드 재동의. **재동의 전엔 후기 검증이 자동 생략**(주문 상한만 — 안전 폴백)·mains API도 계속 403.
- ⚠️함정 기록: 인라인 node -e의 한글·따옴표 이스케이프 실패로 version.js 미갱신 채 배포 1회(보정 커밋 dd9273f — **버전·CHANGELOG 갱신은 파일 도구로**).

### 🔒 8/7 밤 3차 — #258 비로그인 적립 차단·문구 마감 (대표 실물 지적) ← 최신
- **비로그인 적립 구멍(대표 발견)**: 게스트 로컬 모드가 출석 +1·물주기까지 허용 → **비로그인 = 전면 차단 + 로그인 유도**: farm 진입 시 노랑 CTA 배너("🍊 로그인하고 물방울 모으기 — 가입 혜택 5개!")·물주기 버튼 "로그인하고 물 주기"·출석/물주기 클릭 = 토스트 후 로그인 이동(returnUrl). 실측: 클릭해도 적립 0.
- **프로필 안내 문구**: "나만의 캐릭터와 닉네임을" → **"나만의 캐릭터와 귤나무를 키울 수 있어요"**(닉네임 기능 제거 정합 — 대표 지정 문구).
- **홈 [후기 더보기] 준비중 팝업 제거** → **네이버 스토어 실후기 링크**(새 탭 — "후기 13만 건 더보기 →" — 13.6만 리뷰 신뢰 자산 활용).
- 실측: 3건 전부 합격·에러 0.

### 🧽 8/7 밤 2차 — #257 구버전·데모 잔재 전수 정리 (대표 지적 적중) ← 최신
- **대표 통찰이 맞았음**: "구버전 잔재가 오류 원인 아니냐" → 전수 스캔 결과 **고객 노출 잔재 발견·전부 제거**: 홈/캘린더 하단 "(프로토타입 — 실제 서비스 아님)" · 상세 하단 "실서비스 연결 전 프로토타입…" · 알림/주문내역 오버레이 "🧪 예시 화면…데모입니다" 배지+데모 목록(비로그인 = 로그인 안내형으로 교체) · 물방울 위젯 "곧 열려요"(준비중 모달) → farm 실링크("물방울 모으러 가기 →").
- **삭제 판단 기준(대표 답변 기록)**: ❌지우면 안 됨 = 구 스킨 skin5(**현 실서비스**)·기존 상품 23종(주문 이력+복귀 안전핀)·백업들 / ✅정리 대상 = 데모 수치·문구·구 번호(완료). 스킨 정식 전환 후 2~4주 안정되면 구 스킨·기존 상품 정리를 대표 확정으로.
- 실측: 데모/프로토 문구 노출 0·비로그인 알림/주문내역 안내형·구매 사슬 무회귀(3.8초)·에러 0. 잔여 "프로토타입" 표기는 코드 주석·로컬 백업 사본뿐(고객 무노출).

### 🎮 8/7 밤 — #256 게임 결선 완료 (대표 (a)안 승인 — 서버 지갑 가동) ← 최신
- **대표 확정(8/7)**: (a)안 승인 · 룰렛 확률 = 게임 운영 카드 세팅(물1 50%·물3 8%·쿠폰5% 28%·쿠폰10% 10%·업글 3.5%·박스 0.5%) · 후기 = 사진·중복 무관 **주문 1건당 1회 상한**.
- **서버(v5.9.220~221)**: mall-api **공개 라우트 /pub/* 6종**(wallet·join-bonus·attend·review-claim·tree-water·spin) — 게이트 = DB 스위치 `mall_pub`(현재 ON·무토큰·CORS 화이트리스트 akkome.cafe24.com/akkome.com·레이트리밋 120/분). **신뢰 = 카페24 실주문 대사**(orders/count·최근 180일·N10~N40): 룰렛 티켓 = 주문 수−사용 수 / 후기 = 주문 수 상한. 지급 전부 idem(재클릭 무해). 실물 당첨(쿠폰·박스·업글) = reward_grants pending + **텔레그램**(수동 발급 — 쿠폰 스코프 없음). 수확(물 100) 시 텔레그램. 룰렛 1일1회 UNIQUE 제거(주문당 1회 정책).
- 🚨 **함정 2건(재발 방지)**: ⓐv5.9.220 기동 실패 — `const cafe24`(8350행)가 mall 마운트(5629행)보다 뒤 = TDZ ReferenceError → **지연 참조로 교정(221)**. Render는 기동 실패 시 구버전 유지(무중단이었지만 "배포 완료" 메시지만 믿으면 오판 — 신코드 응답 실측 필수) ⓑ`mall_game_config.water_cost`가 데모값 10 → 물주기 "잔액 부족" — **1로 교정**(+level_thresholds [10,50,100] — 수확 100 정합).
- **스킨 배선**: farm·roulette·마이 — 로그인 회원 = 서버 지갑(잔액·티켓·출석·가입+5·후기+1·물주기·서버 추첨 → 프론트는 연출만). 비로그인/서버 실패 = 로컬 데모 폴백. 실측: **API 8케이스 전부 합격**(가입+5→재클릭 replayed·출석+1·티켓 403·후기 403·물주기 6→5·지갑 정합·CORS) + 비로그인 무회귀 + 룰렛/farm 렌더 에러 0.
- 대표 재검(로그인 실기기): 가입 +5 토스트 → 출석 → **실주문 있는 계정으로 룰렛**(티켓 = 주문 수) → 후기 +1 → 폰/PC 잔액 동일. 실물 당첨 나오면 텔레그램 수신 확인.

### 🌱 8/7 저녁 2차 — #254·#255 구매 판정 경량화·장바구니 정밀 마감·귤나무 실사화 ← 최신
- **#254 구매 이동 실패(대표 실기기) 교정**: 담김 판정 fetch가 basket.html 통짜(150KB)라 모바일 회선에서 판정 지연→이동 실패. **초경량 카운트 프로브 신설**(`/order/akm-count.html` — `Layout_OrderBasketcount {$basket_count}` 치환·161ms 실측 — ⚠️{$count} 아님) + 판정 윈도우 8.4초 + **재시도 전 실카운트 재확인(중복 담김 방지)**. 실측: 사슬 3연속 성공·**3.6초로 단축**.
- **장바구니 정밀 마감**: 수량 스테퍼 실컨테이너 = `.quantity > div > span.ec-base-qty`(실DOM — .quantity 아님·up/down은 텍스트형 앵커) → [−][수량][+] 48px 한 줄 재배치·요약(.sumPrice = **컨테이너**라 v6 21px이 통째 적용됐던 것) 14.5px 카드화. ⚠️카페24 스킨 파일 **서버 캐시 ~1분** — 업로드 직후 실측은 구버전일 수 있음(재확인 필수).
- **#255 귤나무 실사화(대표 확정)**: 데모(1,250·62%) 폐기 — **0 시작·상태 = localStorage(회원ID별 키·farm↔마이 단일 소스)** · **가입 혜택 물 5개 1회**(로그인 회원·farm 진입 시 자동+토스트) · **출석 +1 실지급**(하루 1회) · 퀘스트 = 출석/행운 룰렛("구매 1회마다 1번·꽝 없음" 안내)/사진 후기(+1 — "확인 후 지급")·**상품 구경 삭제** · "프로토타입" 문구 제거 · 단계 뱃지(씨앗→새싹→열매→수확) given 파생. farm 회원 probe = 치환 모듈 standalone 렌더(실증). 실측: 0→출석 +1→물 주기 1%→마이 타일·나무 카드 동기 전부 합격.
- 🔴 **결선 잔여(서버 필요 — 임시 한계 명시)**: 기기 간 물방울 동기·**구매 1회당 룰렛권 실지급·후기 확인 후 지급** = mall API+인증 구조(#236) 확정 후 시공. 현 localStorage는 기기별.

### ❎ 8/7 저녁 — 자사몰 고객 취소 체계 확정 (대표 관리자 설정 — 몰 공통) ← 최신
- **주문내역에 취소 버튼이 안 뜨던 원인** = 스킨 아님 — 카페24 [주문 설정 > 주문 후 설정 > 구매자 취소/교환/반품 신청 설정]의 **신청버튼 노출 범위가 전부 미체크**였음(이 몰은 결제 즉시 배송준비중이라 사실상 전 주문 비노출).
- **대표 확정 설정(8/7)**: 취소신청 노출 = 배송준비중·배송보류·배송대기 / 반품신청 = 배송완료(30일·배송완료일 기준) / 교환신청 = 미사용 / **자동환불 = 배송보류·배송대기만**(배송준비중은 제외 — 포장 후 송장 입력 전 자동환불 사고 방지, 대표가 [취소 관리]에서 승인). 몰 공통 설정이라 현 스킨·리뉴얼 모두 적용.
- 운영 수칙: 아침 발주·포장 전 [주문 > 취소 관리] 확인 → 승인. 후속 제안(미시공): 자사몰 취소신청 텔레그램 즉시 알림.
- 참고: skin6 주문내역의 취소 버튼 빨간 강조(#252)는 준비 완료 — 설정 저장 후 실카드 노출은 대표 확인.

### 🧹 8/7 오후 9차 — #253 상세 바 단일 동선·장바구니 잔상 제거 ← 최신
- **상세 하단 바 [🛒] 제거(대표 확정 — 동선 단일화)**: 구매하기가 옵션 확인→장바구니까지 담당하므로 담기 버튼 중복 제거 → 바 = [💬 문의][구매하기](flex라 자동 확대 — 실측 307px). 홈 카드 🛒는 유지.
- **장바구니 노란 잔상**: 상품행 안 카페24 개별 [주문하기] 노랑 버튼(44px)이 합계 요약에 깔려 비어져 보이던 것 — 실DOM 특정 후 숨김(전체 주문 = 하단 고정 바 담당). 실측: 잔상 소멸·담김·사슬·홈 무회귀 합격.

### 🍋 8/7 오후 8차 — 청귤 옵션 클릭 불가 근본 교정 (대표 실물 재보고) ← 최신
- **원인 실증**: 상세 하단 고정 구매 바(pd-bar 69px)가 스크롤 영역(pdBody) **위에 겹치는 구조** — 옵션 목록이 짧은 상품(청귤 4행)은 2~4행이 바에 깔려 터치가 흡수됨(elementFromPoint 실측: 4행 중 1행만 클릭 가능). ⚠️**검증 함정 기록**: 기존 사슬 검증이 항상 "첫 행"만 탭해 전 상품 통과로 보였음 — 옵션 행 검증은 **마지막 행 포함** 필수.
- **교정**: `#pdBody{ height: calc(100% - 78px) }` — 스크롤 영역이 바 위에서 끝나 어떤 행도 바에 깔리지 않음. 실측: 청귤 4행 전부 겹침 0·탭 선택 성공·마지막 행으로 구매→장바구니 담김 / 감귤(10행) 마지막 행 무회귀 / 에러 0.
- **이름 정책(대표 확정)**: 현행 유지 — 로그인 시 가입 실명 표시·프로필 관리는 캐릭터만(이름 변경 버튼 추가 취소).

### 👤 8/7 오후 7차 — #252 이름 단일화·타일 2열·취소 버튼 강조 ← 최신
- **이름 기기별 불일치("아꼼"/"아꼼이") 해소**: 원인 = 닉네임이 localStorage(기기 로컬) 저장 — 폰/PC가 서로 다른 값. **이름 = 카페24 회원 실명 고정**(서버 값 — 모든 기기 동일)으로 전환: 프로필 관리에서 닉네임 편집·중복검사 제거(캐릭터 선택만 저장), 로드도 avatar만 로컬 사용. ⚠️로그인 실확인 = 대표 재검.
- **마이 타일 2열**: 포인트 제거 후 3열 그리드에 빈 칸 → `1fr 1fr` 교정(실측 175px×2).
- **주문내역 취소 버튼**: 카페24 표준 버튼(주문취소=입금전 즉시·취소신청=결제 후, 교환/반품 포함)이 **마크업에 기존재**(상태별 자동 노출 치환 — history_list_item.html) → 강조 시공: 버튼 줄 플렉스 정돈+취소 계열 빨간톤(.akm-cancel-btn — 텍스트 기준 클래스 부여·onclick 무접촉). ⚠️실카드 노출 여부 = 카페24가 주문 상태로 결정(취소 가능 상태에서만) — 대표 실주문 재검.
- **미리보기 링크 확정 답변**: /skin-skin6 = 작업 확인 링크(대표와 동일 화면) · 최종 전환 시 akkome.com 주소 그대로 skin6 적용(관리자 [디자인 관리] 대표 지정 1클릭 — GO 시 사전 전수 점검 지원).

### 🛒 8/7 오후 6차 — 빈 장바구니 이동 결함 근본 교정 (대표 실물) ← 최신
- **증상**: 구매하기 → 옵션 선택 → 장바구니 이동됐는데 비어 있음. **원인**: #250 신호 감지식의 "3초 무신호 = 성공 간주" 폴백 — 실측 결과 카페24 담기가 **성공해도 무신호**(confirm/alert 없음 설정)이고 실패도 무alert일 수 있어, 실패를 성공으로 오판하고 이동 가능한 구조였음.
- **교정**: 성공 판정 = **장바구니 실개수 fetch 대조**(basket.html의 "전체 N 개" 파싱 — 실증). 담기 전 N0 → 담기 → 폴링 → **N+1 확인된 경우에만 이동**(빈 바구니 이동 구조적 차단). 실패 시 change부터 3회 재시도 → 최종 실패 = 안내 토스트+상품 페이지 이동. 실측: 구매 사슬 3연속 성공·**4~5초로 단축**(실개수 확인이 고정 대기보다 빠름)·상세 🛒·홈 무회귀 합격.
- ⚠️ **대표 환경 미규명 1건**: 대표 폰 스크린샷이 전부 akkome.cafe24.com **루트 URL에서 v5 렌더** — 실측로는 루트(모바일 UA) = 아모르2·미리보기 쿠키 전이도 없음 → **관리자 미리보기 세션으로 접속 중인 것으로 추정**(대표 확인 필요: 폰 접속 경로가 관리자 미리보기인지 /skin-skin6/인지). 실패 재현 환경이 그 세션이었을 가능성.

### 🚀 8/7 오후 5차 — #251 실서비스 준비 (마이 실사화·게임 ON·로그아웃 교정·장바구니 시안 마감) ← 최신
- 🚨 **로그아웃 무반응·이름 "<span class=" 오염 — 같은 뿌리 교정(최우선)**: probe(#akmMemberSrc)가 `{$name}`·`{$action_logout}`을 **속성값**으로 받아 치환값에 따옴표·마크업이 섞이면 속성이 깨졌음 → **텍스트 노드 방식으로 개편**(`<i data-akm-nm>`·`<i data-akm-lo>` — textContent 파싱·태그 문자 무효화·localStorage 오염 저장분 무시). 로그아웃 실행 = 경로형이면 이동·JS형이면 실행·실패 시 표준 `/exec/front/Member/logout/` 폴백. ⚠️로그인 상태 실검증은 불가(대표 재검 — 마이 이름 정상 표시·로그아웃 실동작).
- **마이 실사화**: 예시 배지 삭제 · 타일 = 💧물방울(farm 이동·"곧 적립 시작!")+🎟쿠폰(→/myshop/coupon/coupon.html) 2개 — **포인트 제거(대표: 물방울로 대체 예정)** · 배송지 관리 → /myshop/addr/list.html · 데모 수치(단골 12회·대기 2) 제거. 실측 합격(span 누출 0·onclick 서버 프리픽스 재작성 확인).
- **게임 ON**: 홈 배너 2·카테고리 2·홈 탭바 2·akm_frame(서브 탭바) 2 = 전부 실링크(roulette.html·farm.html — 준비중 모달 해제). 페이지 실렌더 확인. ※게임은 데모 로직(물방울 실적립 = 결선·인증 구조 확정 후).
- **장바구니 시안 마감(스크린샷 실측 사이클)**: 서브헤더("담긴 상품은 7일 동안 보관돼요")·아꼼이 안내 배너("주문 전에 수량과 옵션…")·"장바구니 상품"/"일반상품(1)" 헤더 숨김·"-0원" 표기 숨김(실DOM ul.price>li>span.txtSecondary — leaf 필터 함정)·옵션변경 연연두 pill·**하단 고정 바 주문하기 진연두 56px**(#orderFixArea). 스크린샷 대조 합격·에러 0. 수량 [변경] 버튼은 카페24 반영 로직이라 유지(시안과 유일한 의도적 차이).
- 대표 재검: ①로그인 상태 마이(이름·로그아웃) ②쿠폰·배송지 실화면 ③폰 실기기 장바구니·게임.

### ⚡ 8/7 오후 4차 — #250 구매 동선·마이 일원화·화면 정돈 (대표 실물 지적 3차) ← 최신
- **"필수 옵션" 간헐 실패 원인 확정·교정**: 연동 자체는 정상(같은 사슬 재현 성공) — iframe 카페24 스킨 JS 초기화 전 change가 무시되는 **타이밍 결함**(모바일 회선에서 재현↑). 교정 = 고정 대기(500+1600ms) → **신호 감지식**(담김 confirm=성공 즉시 진행 / "옵션" alert=자동 재시도 3회 / 3초 무신호=성공 폴백) + buy 이동 700→150ms. **실측: c92 구매 사슬 3연속 성공(6초 내외·전부 iframe 로드 시간)·에러 0**.
- **상세 🛒 = 구매하기와 동일 동선**(대표 확정): 상세 안 🛒는 옵션 행 선택 재사용 — 선택 시 레이어 없이 즉시 담김(뱃지 +1·화면 유지), 미선택 시 옵션 탭 안내. 홈 카드 🛒만 종전 레이어 유지. 실측 합격.
- 🚨 **특가 카드 구 번호 발견·교정**: 홈 특가 `data-c24-cart="40"` 정적 하드코딩(#243 당시) — 오픈 전환 때 매핑표만 교체하고 누락 → **92 교정**(정적값 전수 검색 1건뿐 확인).
- **마이 일원화**: 카페24 마이(myshop/index.html) 진입 시 v5 아꼼이 마이(#v/my)로 replace. ⚠️비로그인은 logincheck가 선행해 로그인 페이지로 감(정상 게이트) — **로그인 상태 실렌더는 대표 재검**.
- **장바구니 정돈(대표)**: 국내/해외배송 탭·해외장바구니 이동·견적서출력 = **비사용 숨김**(국내 전용 몰 — 카페24 필수 아님·CSS 계층만) / [기본배송] 합계·주문정보 = 흰 카드화 / 전체선택·선택삭제 44px. 실측 합격.
- **로그인 정돈(대표)**: 🚨 **SNS 실로그인 오류 실측** — 네이버 "서비스 설정 오류"·Apple "invalid redirect" = **카페24 관리자센터 [회원 > SNS 로그인 설정] 미완(스킨 무관 — 아모르2도 동일했을 것)** → 오류 노출 방지로 네이버·애플 등 임시 숨김(카카오만 전폭 58px "카카오로 시작하기" — 실동작은 대표 확인). 3분할 카드 겹침 교정. **SNS 재노출 = 관리자 설정 완료 후 login.html #250 블록에서 숨김 해제**.

### 🎨 8/7 오후 3차 — 리뉴얼 디자인 v6 이식 (주문내역·장바구니·로그인 — 대표 클로드 디자인 핸드오프) ← 최신
- **핸드오프** = `_참고자료\디자인리뉴얼\Jeju Akkome Design Polish\handoff\`(README = 작업 지시서·완성 코드 2종·로그인 시안·이미지 3종). 원칙 그대로: 카페24 모듈·치환코드·JS 로직 무접촉 — 각 파일 맨 아래 v6 오버레이 블록만 추가.
- **시공**: ①이미지 3종(akkomi-wink/sad·btn_kakao) → `/sde_design/skin6/assets2/` 업로드, 참조 = **레이아웃 로고와 동일한 pre+`/assets2/` JS 방식**(미리보기·실서비스 겸용) ②주문내역(myshop/order/list.html)·장바구니(order/basket.html) = 핸드오프 완성 블록 append(경로만 교체) ③**로그인(member/login.html) = 시안 기준 직접 작성**(인사 블록·라벨 위 배치·비번 표시/숨기기 토글·58px 진연두 로그인·카카오 58px 노랑+아이콘·유틸 3분할 카드·가입 배너 — 전부 JS 장식+CSS·try/catch 가드).
- **[확인필요] 해소(README 지시)**: 장바구니 상품행 컨테이너 = **`.xans-order-list`** 실DOM 확정(.cart-product 아님) — 카드 룩 셀렉터 교정 후 적용 실측(흰 카드·radius16·shadow).
- **실측 합격(전부 실클릭)**: 로그인 = 히어로 아꼼이 실이미지·토글 password↔text 실동작·로그인 버튼 58px 진연두·카카오 아이콘+문구·에러 0 / 장바구니(담김) = 체크 28px·상품명 16.5px·수량 스테퍼 48px(.quantity 적중)·주문 버튼 58px·에러 0 / 빈 장바구니 = 슬픔 아꼼이+쇼핑하러 가기 / 주문내역 비로그인 = 로그인 리다이렉트(returnUrl 정상) / 홈 무회귀(map 20·에러 0).
- 대표 재검 항목: **주문내역 실카드**(로그인 필요 — 상태 배지 색·4단계 막대·주문일자 표시는 코드 시공분, 실렌더는 대표 로그인 재검·#238과 동일) + 폰 실기기 룩 확인. 로컬 정본 = skin6-work-final에 3파일 동기 완료.

### ✂️ 8/7 오후 2차 — 상품명 축약·재동의 (대표 실물 지적) ← 최신
- **상품명 겹침 해소**: 신규 19종의 네이버식 긴 이름이 아모르2 목록 카드(1줄 상정)에서 두 줄 포개짐 → **기존 명명 스타일로 축약 19/19**(91 제주 미니밤호박·92 제주 감귤·93 제주 청귤 풋귤·94 제주 황금향·97 제주 과일 대용량·100 혼합 과일 선물세트·109 제주 홍귤 산하홍 등 — `scripts` 보존 fix-names.js). 실서비스 스킨 무접촉 유지. 저장 검증 19/19 + 목록(23)·메인 실렌더 확인 + **v5 원터치 구매 사슬 무회귀 재합격**(이름 변경은 번호·옵션 매칭 무영향 실증).
- **메인 진열 종결(대표 확정)**: 판매중 상품이 위로 정렬돼 신규 5종 메인 노출 — **기존 6종 제거 불요·현상 유지**. 실측: 메인에 신규 5종 실렌더.
- ⚠️ **store 스코프 재동의 재요청 1건**: 대표 재동의가 v5.9.219 배포 반영 전 시점이라 구 권한으로 동의됨 — mains GET 여전히 403. 급하지 않음(메인 해결됨) — 다음에 [데이터관리] 카페24 카드 재동의 1회만 다시 하면 store API 개통(메인 자동 관리 기반).
- 측정 함정: 아모르2 상품 링크는 `product_no=` 쿼리가 아니라 SEO 경로(`/product/이름/번호/`) — 셀렉터는 `/product/`로 잡을 것(직전 메인 미노출 판정도 이 셀렉터 함정이 섞였음).

### 🎨 8/7 오후 — 오픈 전환 마감 5종 (대표 실물 지적 반영 — v5.9.218) ← 최신
- **①내부 문구 노출 제거(최우선)**: 목록 카드의 "네이버 정본 이관(B안)…" = summary_description 잔존 → **기존 상대 상품 요약문 이식 19종**(97 대용량·109 산하홍은 상대 요약 부재 → 빈값). 실측: 전 화면 내부 문구 0·92 요약 "여름에도 맛볼 수 있는 고당도 하우스감귤!" 실렌더.
- **②목록 대표 이미지 이식 19/19**: 네이버 스냅샷 img → base64 → **`POST products/{no}/images` {shop_no,request:{image_upload_type:'A',detail_image:<base64>}}**(실측 확정 스키마 — 이 한 콜로 업로드+대표 지정+리사이즈 완료·응답 = `image.detail_image` akkome.com 자체 호스팅 경로). ⚠️함정 2: PUT products에 base64 직접은 422 "extension" 거부 / 러너 결과가 error로 보여도 실등록 성공(파서가 image.detail_image 미인식 — 표시 문제일 뿐). 목록 실렌더 20/20 확인.
- **③상세 텍스트 간격 개선**: bulk-detail 조립에 v5 렌더러(.pd-txt) 규칙 스코프 스타일 동봉(.akm-det — 16px·줄간격 1.8·문단 여백·keep-all·중앙정렬) → 19/19 재이관. 실측 computed line-height 28.8px 적용. 상세 이미지 rendered 0으로 보이는 건 **아모르2 lazyload placeholder**(스크롤 후 로드 — 정상).
- **④메인 진열**: 신규 SALE 5종(91·92·93·94·100) **main:[3] 등록 완료**(products PUT의 main 필드 — write_product로 가능 실측). 🔴 **기존 품절 6종(32·35·39·42·44·69) 제거는 불가** — PUT main:[]·null 전부 no-op(추가만 가능·제거 안 됨 실측), 정석 mains API는 **mall.write_store 스코프 부족(403)**. 기존 6종이 메인 자리를 차지해 **신규 5종이 아직 메인에 안 보임** → 대표 선택: (a)관리자 [메인 진열 관리]에서 기존 6종 제거(1분 클릭) (b)앱 scope에 read_store·write_store 추가 재동의 → 클코가 API 제거.
- **⑤도구**: 러너 raw 가드에 mains 허용(v5.9.217)·bulk-image 액션 신설(217→218 스키마 교정). 배포 롤링 중 플래그 소비→결과 유실 사례 1회(재실행으로 해소 — 배포 직후 러너 실행은 재확인 필수).

### 🚀 8/7 오픈 전환 완료 (대표 "상품 전환 GO" — 신규 세트 실판매 개시) ← 최신
- **절차 실행**: ②신규 91~109 진열·판매 ON(19/19) → ①기존 실판매 4종(43 밤호박·42 청귤·40 하우스감귤·23 하우스황금향) 판매안함 — 전환 전 전수 상태 보존 = `_참고자료\카페24스킨백업\scripts\c24-prestate-open-2026-08-07.json`(되돌림 = c24-batch-toggle.js rollback) → ③skin6 index `AKM_C24MAP` 신규 교체(**20항목** = 신규 19 + 체험 7673020911→47 유지·대용량 97 매핑 신설) 업로드 → ④전수 재검.
- 🚨 **전환 중 발견·교정 2건**: ⓐ**신규 상품 분류 미배정 → 스토어프론트 404**(진열 T여도 분류 없으면 404 실측) — 기존 상대 상품의 분류 이식(23·29·30·42 그룹, 분류 배정 = `POST categories/{no}/products`(공식 문서 확인) — 러너 가드 확장 v5.9.216) ⓑ**신규 19종이 과세(A)로 등록돼 있었음** — 기존 전 상품 면세(B) 실측 대조 후 **전원 B 교정**. 검증 19/19 일치.
- **품절 정본 동기**: 전환 직후 신규 전부 판매 ON = 네이버 품절 14종이 실서비스 구매 가능 상태(실재고 위험) → **동기화 파이프라인 fix 1회 실행**으로 네이버 정본 정렬(품절 14종 selling F·SALE 5종(91,92,93,94,100) T 유지 — applied 전량 확인).
- **최종 상태(API 전수 실측)**: 기존 4종 TF · 신규 SALE 5종 TT · 신규 품절 14종 TF · 전원 면세 B — 네이버 정본과 1:1.
- **재검 합격**: 신규 19종 스토어프론트 렌더+판매가 전수 19/19 · **원터치 구매 사슬 실측**(v5 상세 옵션 클릭→구매하기→**확인 레이어 없이 자동 담기**(#249 정확 일치 성립)→장바구니 실도달·pageerror 0) · 아모르2 실서비스 스모크(홈 정상·기존 43 구매 버튼 0·신규 106 품절 표시·신규 92 구매 가능 26,800원).
- ⚠️ **검증 함정 재확인**: 같은 URL에 hash만 바꾼 goto는 리로드 없음(라우터 로드 시점 처리) — 쿼리 변경으로 실로드 강제 / v5 상세 오버레이는 데이터 비동기 로드 후 열림 — waitForFunction(.pd-dim.show) 필수 / 네이버 품절 상품은 v5에서 구매하기 대신 시즌 대기 렌더(검증은 SALE 상품으로).
- **후속 권고(대표/똑똑이)**: ①타이머 `cafe24_sync` ON — 전환 후엔 매일 품절·가격 정본 동기가 실판매 정합의 핵심(dry부터 또는 바로 fix) ②기존 4종 진열(display) 정리 여부 ③구 스킨(아모르2) 목록에 신규·기존 중복 노출 정리 여부.

### ✅ 8/7 오전 후속 — 동기화 파이프라인 시공 완료 (v5.9.214)
- **#246 판정2 시공 완료**: 수집기 `cafe24_sync`(naver_auto_collect — **기본 OFF**·05:10 앵커, 04:30 스냅샷 직후) = 네이버 스냅샷(정본) ↔ 카페24 신규 세트 기본가·품절 대조. **등록식 실검산: 기본가 = 네이버 할인가 + min(옵션 증감액) — 실등록가 19/19 일치**(옵션 정본 = 7/30 STORE_DATA). 매핑+minAdd = DB `cafe24_sync_map`(시드 완료·보존본 `_참고자료\카페24스킨백업\scripts\c24-sync-map-246.json`).
- **가드 4종 전부 실측 검증**(결함 주입 테스트 — minAdd 조작 후 원복): ①기본 dry(`cafe24_sync_mode`='fix'일 때만 실보정) — dry에서 무쓰기 확인 ②±50% 초과 = blocked 분류·fix에서도 무쓰기 확인(c105 주입 99500 차단) ③차이·보정 시 텔레그램 보고(quiet 옵션 — 러너 테스트는 기본 억제) ④품절 동기는 display=T만(오픈 전 세트 무조작·statusType SALE/OUTOFSTOCK 기준). **fix 실PUT 경로도 실측**: c91 주입 드리프트 15800 → fix가 스스로 14800 복원(카페24 API 재조회 확인). ⚠️ 카페24 GET 가격 응답에 **단기 캐시(~1분) 실측** — 보정 직후 재조회는 구값 가능(일 1회 운용엔 무영향·PUT은 절대값이라 무해).
- **수동 실행** = 러너 `{action:'sync', mode:'dry'|'fix', telegram:'yes'(선택)}` → 결과 `cafe24_product_result`(요약+`cafe24_sync_last` 상세). 러너 폴러 **60초→10초 단축**(대표 지시 8/7).
- **가동 대기(대표/똑똑이 GO)**: ①타이머 `cafe24_sync` ON(dry — 차이 감지 시 텔레그램만) ②`cafe24_sync_mode`='fix' 투입(자동 보정 활성). 오픈 전환과 무관하게 ON 가능 — 전환 후에도 매핑 무수정(신규 번호 기준이라 그대로 유효).

**🎯 핵심 현황 — B안 신규 세트 = 네이버 100% 완전체·오픈 전환만 남음**
- 카페24 신규 상품 **91~109(19종·전부 진열/판매 안함)**: 가격(전수 일치 검산)+옵션 108종(추가금 정합·천혜향 14/14 표본검증)+**상세페이지 19/19 이관 완료**(bulk-detail — CDN+no-referrer). 매핑표 = `_참고자료\카페24스킨백업\scripts\c24-map-new-246.json`.
- **오픈 전환 절차(대표 "전환 GO" 시 실행)**: ①기존 겹침 상품 판매안함(⚠️실서비스 아모르2에서 팔리는 상품 꺼짐 — 시점 대표 결정) ②신규 91~109 진열·판매 ON(raw 액션으로 display/selling T PUT) ③skin6 index의 `AKM_C24MAP`을 신규 번호로 교체(매핑표 JSON) ④전환 직후 재검(화면 정합·원터치 구매 — #249 옵션 텍스트가 신규 표기와 동일 조립이라 확인 레이어 자동 소멸).
- **카페24 API 도구(재사용)**: 러너 `cafe24_product_request`(10초 폴러 — v5.9.214에서 60초→10초) — verify / bulk-create / bulk-detail / **sync(동기화 1회)** / raw{method,path,body — products 한정·DELETE 금지} / delete-test. 결과 = `cafe24_product_result`. 실행 스크립트 = scratchpad `c24-raw.js`(새 세션은 재작성: 플래그 INSERT→15초 폴링 회수). **스키마(실측 확정)**: 상품 POST `{shop_no:1,request:{product_name,price,supply_price,display,selling}}` / 옵션 POST `{...request:{has_option:'T',option_type:'T',option_list_type:'S',options:[{option_name,option_value:[{option_text}]}]}}` / 추가금 `PUT variants/{code} {request:{additional_amount}}` / 상세 `PUT products/{no} {request:{description}}`.
- **#249(완료)**: v5 상세 옵션 행 클릭 선택+[구매하기] 실진행(정확 일치=자동 담기→장바구니 / 불일치=확인 레이어). 담기·구매 공용 = index 내 `akmC24Flow`.

**남은 단계(우선순위순)**
1. ✅ **오픈 전환 완료**(8/7 대표 GO — 위 "8/7 오픈 전환 완료" 절 참조. 원터치 구매 실측 합격).
2. ✅ **동기화 파이프라인 시공 완료**(8/7 오전 후속 — 위 절 참조): 가동 스위치(타이머 ON·fix 모드)만 대표/똑똑이 GO 대기.
3. #236 4단계 게임 결선 — 인증 구조 (a)공개 라우트+서버 대사(권고)/(b)브릿지 대표 확정 대기.
4. 대표 재검 대기: #238(주문내역 실카드 로그인 재검)·#242(순회 결함 0 재검)·#232(종결 정리).
5. 백로그: 리뷰 대체 경로 조사(원천 봉쇄 — (b)) · 가입 폼 최종 재검 · #241/#245 시공분 실물 재검.

**도구·함정 요약(새 세션 필독)**: 업로드 = PowerShell로 `scripts\upload.js`(가드 /sde_design/skin6/) · npm --no-save는 **한 명령에 묶어**(playwright ssh2-sftp-client) · 미리보기 URL 슬래시 필수·hash-only 이동은 리로드 없음 · **카페24 미리보기는 href+JS 라우트 리터럴 서버 재작성**(프리픽스 금지·에셋 /assets2/는 비대상) · 모바일 titleArea mt120px/사이드바 592px는 layout.html에서 숨김 처리됨 · basket 전체 체크 = 재시도식.

### 🌅 8/6 아침 인계 요점 (밤샘 종결 — 05:30)
- **#234 완료**(⑨ 포함 전 항목): **리뷰 수집기 복구 실패 확정** — 8/6 수집 37분 소요·리뷰 20/20 실패 6일 연속. 원인 실측 = **로컬 IP 첫 1콜도 429+HTML 에러** → 네이버가 brand.naver.com 리뷰 엔드포인트를 요청 지문 기반 원천 봉쇄(백오프 해결 불가 층위). 화면 무해(폴백 — 직전분 유지). 선택지 (a)갱신 시도 OFF (b)대체 경로 조사 (c)유지 — 대표/똑똑이 결정 대기.
- **#242 밤샘 순회**: 심야 재순회 3회 연속 **결함 0** 유지. 대표 아침 재검 = **?v=32**(탭 5종 통일·게임 탭 모달·로그인 여정·주문내역 폼·프로필 게이트). 제안 4건 = #242 응답 참조.
- ✅ **#243**(아침 GO): ①마이 [로그아웃](probe `{$action_logout}` 확장·로그인 시만 표시) ②**실장바구니 사슬 완성** — 실상품 28종 크롤→**매핑 19종**(`AKM_C24MAP` — 하우스감귤=40·밤호박=43·청귤=42·황금향=23…·보류 1·확인 2건) · 담기=카페24 상품 페이지 경유(**옵션 자동 매핑 배제** — 오담기 방지) · 특가 카드 통일 · 장바구니=실페이지(order/basket.html **v5 스킨링**·데모 오버레이 은퇴) · **헤더 뱃지 실값 "1" 실측** · 주문하기→로그인 게이트(표준). ⚠️교훈: **카페24 미리보기는 JS 리터럴의 페이지 라우트도 재작성**(/product/ 등 — 프리픽스 붙이면 이중. 에셋 /assets2/는 비대상).
- ✅ **#244**: 가입 agreement+join v5 스킨링(CSS만·검증 JS 보존 — 입력 73·셀렉트 19 전수 표시·에러 0). 약관 "샘플" 문구 = 관리자센터 등록(대표).
- 재검 캐시 **?v=47** · 순회 결함 0 재확정(장바구니 실페이지 기대값 갱신 포함).
- ✅ **#245(대표 터미널 직접, 8/6 오전 — 회사 v5.9.206·207)**: ⓐ보류 [오늘/내일 발송으로 안내] **직원 개방**(manual-send adminOnly 해제 — 문의 관리 전 직원 공용 원칙) ⓑ**이력 체크바** — 행 체크+전체선택 → 보류 다건 일괄 발송(보류 아닌 건 자동 건너뜀·멱등 재사용) + **선택 삭제**(취소 주문 정리 — `deleted_at` soft-delete·audit·조회/집계 제외·재수집 재등장 없음). 실클릭 검증: 체크 100행·바 카운트·confirm 문구·보류 판정("보류 1건·99건 건너뜀") 정확. **확인 답변: 수집 후 취소된 주문의 보류 행은 자동으로 안 사라짐**(취소 마킹은 수집 시점 한정 #141) — 체크 삭제로 정리.
- ✅ **지시함 #245**(담기 이후 동선 — 8/6 오전): ①**배경 담기 신동선** — 상세 [🛒] → v5 옵션 레이어(카페24 실옵션) → **숨김 iframe으로 카페24 표준 담기 로직 그대로 실행**(페이로드 재조립 0 — 오담기 방지) → 토스트+뱃지 즉시 +1·화면 이탈 0 ②basket: 로드 시 `#product_select_all` 트리거로 **전체 기본 체크**(+체크박스 v5 가시화 — "체크 안 돼 주문 불가" 해소)·합계 카드·CTA 위계 ③기본 상세 Samplemall = **detail_layout에 기본 헤더 2벌 잔존**이 원인 → #240-③ 동일 처리. 재검 ?v=51. ⑨ 추보 재기록(완료 지시 응답은 폴링 시야에서 빠짐 — 추보는 활성 지시에 기록할 것).
- ✅ **#247·#248(8/6~7 — 회사 v5.9.210~212)**: #247=리뷰 갱신 OFF(`REVIEW_FETCH_OFF`)+표기 정직화(`reviews_fresh_at` — 리뷰 성공 이력 0 발견·"리뷰 7.30 기준" 동적 표기)·B안 대상 확정(애매 5종 미포함·개인결제 무시). #248=①basket 실폰 재마감(**공백 3중 원인**: header 잔여+사이드바 592px+**모바일 titleArea mt:120px** — layout 공통 교정 / 체크박스 appearance:none→네이티브+연두 / 자동 전체체크 재시도화) ②딜레이=카페24 응답 요동(0.9~7초 실측)·이동 토스트 ③**B안 19종 등록 완료** — 재승인 후 raw 실험으로 스키마 3종 확정(상품 `{shop_no,request:{...}}`·옵션 `has_option/option_type:T/option_list_type:S`·추가금 variants PUT), **카페24 신규 91~109·옵션 108·추가금 85·에러 0·전부 진열/판매 안함**. 옵션 정본=7/30 STORE_DATA(일일 스냅샷엔 옵션 없음)·base=최저 옵션가 양수화(결제가 불변). **신규 매핑표 = `scripts/c24-map-new-246.json`**. 다음 = 미리보기 정합 전수 테스트 → 오픈 전환(대표 확정) → 동기화 파이프라인.
- ✅ **#249(대표 터미널, 8/7)**: 상세 옵션 행 클릭 선택(연두 ✓·단일)+[구매하기] 실진행 — 정확 일치 시 자동 담기·불일치 시 확인 레이어(참고 문구)→담기→장바구니 이동. 옵션 data-text=신규 세트 표기(n1 · n2)라 **전환 시 원터치 구매로 자동 업그레이드**. 터치 전 사슬 실측 결함 0.
- ✅ **상세 이관(8/7 — v5.9.213)**: bulk-detail 러너로 네이버 상세 스냅샷 → 신규 19종 description **19/19 성공**(블록 24~52·CDN+no-referrer). **신규 세트 = 네이버 100% 완전체(가격·옵션·상세)** — 남은 것 = 오픈 전환 GO(①기존 판매안함 ②신규 진열 ③매핑 교체 ④재검).
- 미종결 지시함: #232(설계 승인 후 시공 — #236으로 진행됨·정리 대기) #236(4단계 게임 결선 = 인증 구조 확정 대기) #238(주문내역 실카드 = 대표 로그인 재검 대기) #242(대표 재검 후 종결) #246(정합 전수 테스트·오픈 전환·동기화 잔여).

### 🌙 야간 진행 현황 (8/5 밤 — #234 큐 + 신규 #235~#238)
- ✅ **#234 큐 ①~⑧ 전부 완료**(⑨ 04:30 수집기 판정만 잔여 — check-revnote 스크립트로 naver_product_snapshot 최신행 reviews·note 판정) / ✅ **#235**(/guide 교정 2건 — v5.9.205) / ✅ **#237**(캘린더 상품 연결 결함 2곳 교정: 맛보러가기 `#c/citrus` 하드코딩·구매하러가기 홈행 → `#p/{no}` 직결, 칩 전수 5/5 실검증. 장바구니 판정: v5 데모·주문하기=준비중 모달(실주문 유입 0 실측)·예시 배지 추가)
- 🏗️ **#236(#232 승인 — 4단계 시공)**: ✅1단계 회원 연동(probe `#akmMemberSrc`+`window.AKM_MEMBER`·헤더↔마이 단일 소스 — 비로그인+미설정=시각 무변경. 카페24 실측: 비로그인=statelogoff 블록만 렌더) ✅2단계 주문내역(로그인→/myshop 이동)·알림종(로그인=정직 빈 상태) ✅3단계 챗 배선(`CHAT_OPEN=false` 단일 스위치 — true 한 줄로 입력창 disabled 해제+실API. OFF=호출 0 실증) ✅**4단계 게임 이식**(v5-preview roulette/farm → skin6 라이브·직접 진입 정상·**홈 진입점=준비중 모달 잠금 유지**. v5 게임=순수 데모(mall API 참조 0) — **결선은 🔴인증 구조 대표 확정 대기**: 공개 skin에 MALL_API_TOKEN 노출 불가 → (a)공개 라우트+서버 실주문 대사 검증(권고) vs (b)세션 브릿지. 확정 전 착수 금지)
- ✅ **#238 시공분**(주문내역): **(A) 카페24 myshop 스킨링 채택·시공 완료** — /myshop/order/list.html v5 카드(치환·모듈 원형 보존·CSS만. (B) 서버 API는 인증 부재 PII 위험 — 탈락). [재주문] 직행은 표준 액션 부재 — 후속 판단 대상. 알림 실이벤트화는 4단계와 병합. 로그인 실렌더 = 대표 확인 항목.
- ✅ **#239**(로그인 진입점): 마이·주문내역·알림 3화면에 로그인 CTA(returnUrl 동적 — 실측: 카페24 로그인이 스킨 경로 유지·returnUrl 필드 정확 주입).
- ✅ **#240**(대표 실로그인 검증 후속): ①아꼼이 깨짐 = **무슬래시 URL(`/skin-skin6`) base 어긋남** 재현·교정(index head 보정 스크립트 — 무슬래시 케이스만 에셋·캘린더 링크 절대화) ②로그인 v5 스킨링(member/login.html — CSS만) ③**layout/basic/layout.html에 v5 미니 헤더**(Samplemall 헤더 숨김·아꼼이 로고 JS 세팅) — myshop·member·board 일괄 적용. ⚠️교훈: **카페24 미리보기는 절대경로 href를 서버측 재작성**(JS 중복 보정 금지 — 이중 프리픽스 남). 약관 문안 = 관리자센터 등록(대표 몫). 재검 캐시 = ?v=23.
- ✅ **#241**(대표 실측 결함 4건): ①프로필 로그인 게이트(비로그인=저장 프로필 무시+편집 차단·로그인=회원명 기본·키 회원ID별 `akkome_profile_v1:{id}`) ②TALK FOUC = CSS 선차단 ③오버레이 body 스크롤 잠금(show 클래스 감시식 — open/close 무수정) ④myshop 폼 v5 마감.
- ✅ **#242 1차**(밤샘 자율 순회 체제 — 대표 GO): **akm_frame 탭바 = 홈 정본 5탭 통일**(홈·나침반·귤나무·주문내역·마이 + 게임 탭 준비중 간이 모달) · FOUC 선차단 전 레이어(4페이지+레이아웃 3종) · 손님 순회(오버레이 5종 전수·FOUC 20샘플·첫방문 사슬) **결함 0**. 제안 4건 별도 보고(상세 담기 부재·뱃지 무반응·카테고리 상품 미배정·가입 폼 스킨). 재검 캐시 ?v=32.
- 보류(대표 결정 대기): 클릭 4건(MALL_API env·챗 GO(=CHAT_OPEN true 1줄)·게임 GO·scope) / 서브 합격 판정 / 카탈로그 이원 통합 / 카카오 봇 배포 / 대표 디자인 전환
- 🛠️ 야간 교훈: **npm --no-save 연쇄 함정**(개별 설치가 앞선 --no-save 패키지 제거 — playwright+ssh2-sftp-client 반드시 한 명령에) · 미리보기 검증 URL은 **`/skin-skin6/` 슬래시 필수**(없으면 base가 루트 → 에셋 404 거짓 결함) · **hash-only 이동은 리로드 없음**(#p/ 라우팅은 로드 시점 처리 — 전수 검증은 실로드 강제) · 업로드 스크립트는 PowerShell로 실행(Bash는 한글 경로 오류)

### 🏗️ 카페24 skin6 작업 체계 (홈 합격 상태 — 절대 회귀 금지)
- **미리보기 = https://akkome.cafe24.com/skin-skin6** (skin_no=11·skin_code=skin6 — URL은 skin_code 기준. 캐시 회피는 ?v=N 증가). 실서비스 아모르2 무접촉 원칙(현재까지 오염 0 — 매 시공 확인).
- **파이프라인**: 정본 = akkome-website 리포 **v5-preview 브랜치**(로컬 플랫폼본은 구판 — 사용 금지) → `transform-v5.js` 변환(치환 카운트 검증) → SFTP 업로드. **핵심 스크립트·최종 작업본 전부 `_참고자료\카페24스킨백업\scripts\` 보존**(스크래치 소실 대비 — transform-v5/calendar·upload/backup/tree·poll-light/respond/append-report/get-instruction·스냅샷·skin6-work-final 30개).
- **SFTP**: akkome.ftp.cafe24.com:3822(SFTP)·계정 akkome·비번 .env(CAFE24_FTP_*). 업로드 가드 = /sde_design/skin6/ 한정.
- **변환 내용(transform-v5.js — 홈)**: 게임 링크→준비중 모달(data-modal)·캘린더→실페이지·물방울 "곧 열려요"(실수치 미연동)·에셋 assets2/(CDN 캐시 키)·no-referrer·**실드 스크립트**(앱 오버레이 `[class^="app-smart-popup"]`+캠페인+TALK **상시 display:none** — 모바일 전용 딤이 터치 흡수하던 실폰 결함의 근본, #230)·data-modal 위임 핸들러·장바구니 뱃지 실연동(치환코드 모듈 hidden 패턴 — #232 실증).
- **검증 표준(무회귀 4종 — #230 확립)**: 12항목 스모크 + 아이콘 정본 diff 0 + **터치 시퀀스**(touchscreen.tap — 좌표 클릭은 거짓 합격 이력) + 실서비스 무변경. 스크립트: verify-*·final-smoke·touch-test(scripts 폴더).
- 교훈: PowerShell `>`/Set-Content는 바이너리·한글 오염(git archive/tar·Write 도구 사용) · 검증은 데스크톱+모바일 양 조건 · CDN 캐시는 ?v= 우회 · "HTTP 200"이 아니라 naturalWidth 렌더 실측.

### 📌 주요 라이브 상태
- **카카오 스킬 서버**: 톡톡봇 `/kakao-skill` 라이브(무언 대기 — 톡톡 완전 동일 라임·#228 확정). 봇 배포·블록 생성 = 대표 확정 대기. 스킬 URL = `https://jeju-akkomene-talktalk-bot.onrender.com/kakao-skill`.
- **발사 관찰(#214)**: 기준선 10:09(switch-off 40·hold 7) → 저녁까지 전건 sent·실패 0·보류 0 소진. 스냅샷 = scripts\snapshot2-214.js. 저녁 보고에 알리고 잔여(aligo-remain.js request→result)·60분 텔레그램 실물(대표 확인 항목) 포함.
- **소급(#216·#217)**: 14/14 sent·발주확인 자동(러너 kakao_backfill_request — 1회성 종료). 라벨 「소급 발송」(#221·backfill 플래그).
- **리뷰 스냅샷**: 수집기 429 5일 연속 → v5.9.203 교정(백오프 15/30/60×부분 성공) — **내일 04:30 수집분으로 판정**(naver_product_snapshot.reviews not null 여부·note).
- 지시함 스크립트: scripts\poll-light.js 등 — DB 접속 문자열 내장(.env DATABASE_URL 동일). 폴링 3분 체제(#197) 유지.

## ★★★ 8/4 밤~8/5 세션 인수인계 (지시 #200~#210 전건 완료 — v5.9.198 라이브)

**현재 버전: v5.9.198 / app.js 캐시 v=348** (커밋: `1abdef4` v5.9.197 → `2c5c2c4` v5.9.198 → `2eac703` CLAUDE.md 무회귀 원칙). KAKAO_NOTIFY=off·실발송 0 유지.

### 처리 요약 (#200~#210)
- **#200 채널 응답 시간 실측**: 톡톡 30초의 정체 = **의도적 지연이 아니라 메시지 버퍼링 타이머**(톡톡봇 server.js:224~227, 마지막 메시지 기준 리셋). AI 생성 순수 시간 = 답변 9.3초/SKIP 4초(→ 톡톡 체감 ≈40초). 🔴 **회사프로그램 `qnaGenerate`에는 즉답(키워드) 단계가 없음** — 즉답은 톡톡봇의 "가격 문의" 하나뿐. 교정은 **대표 확정 대기**: (a)현행유지 (b)자사몰 챗만 즉답 (c)3채널 공통 — 공용 함수라 (c)는 가동 2채널이 바뀜.
- **#201→#202 자동답변률 오판·정정 (🚨 자책)**: #201에서 "자동답변률 20~30%"로 보고 → **틀렸음**. 분모를 메시지 단위로 잡아 **쿨다운 30분 후속 인사말("감사합니다!" 등 116건)을 실패로 센 것**. 실제는 **대화(손님) 단위 85~91%** — 대표 관찰이 맞았음. 🔴 **교훈: `answered=false` ≠ 실패. message_logs 1행 = 메시지 1개(대화 아님).**
- **#201·#203 카카오 챗봇 조사 (조사만·미착수)**: 채널 웹훅은 added/blocked만 — **손님 메시지 수신 불가**. 경로 = **(B) 챗봇+스킬(무료·권고)** vs (A) 상담톡(딜러사·유료). 알리고는 발신 전용. **비즈니스 채널 인증·발신프로필은 알림톡으로 이미 확보** — 챗봇만 별도 개설. 콜백 방식 대표 수용(5초 내 대기문구 → 30초 버퍼+AI → 콜백 발송, 유효 5분). 🔴 **선0 = 대표가 채널 관리자센터 `1:1 채팅 > 채팅설정`에 「챗봇 채팅」 영역 보이는지 확인**(있으면 정식 챗봇 보유). 미해결: 카카오에서 "무응답"이 가능한지(쿨다운·버퍼링 재현의 관건) — 붙여봐야 앎. 대기문구 2안 권고("안녕하세요 고객님, 제주아꼼이네입니다 🍊 문의 확인 중이에요!…").
- **#204 (v5.9.197)**: ⓐ**요약 줄 선물하기 미분리** — 행 표시는 재조회 실번호 `*` 패턴, 요약은 `status='gift-masked'`(실DB 0건)를 봐서 갈라짐 → 요약도 `_telCache` 재사용으로 통일 ⓑ**"모두 표시됨 (60/30건)"** — DB는 30/30 정상, **프론트 경합**(겹친 응답이 각각 `notifyLogShown +=`) → 요청 순번 가드 + append 여부로 확정. 수정 전 코드로 60/30 재현→수정 후 30/30.
- **#205~#207 "주문 기록 밖" 규명 (조사만)**: 전수 **10건**(8/1~8/3). 선물하기 가설 **기각**(7건이 정상 번호 일반 주문). 7/28 주문 2건=과도기, **7/30·31·8/3 주문 5건=수집 중 누락**(#177 "과도기" 판정 불충분 인정). #177 교정(15분 되감기) 이후 발생 0. **선물하기 발송 대상 = 주문자 우선**(ordererTel→수취인 폴백)이나 선물하기는 **주문자 번호조차 안 옴**(수집 시 빈값 → 나중에 마스킹으로만 재조회됨). 발송안내 문면은 주문안내 전제 표현 0건 — 단독으로 자연스러움(수정 불요).
- **#208 (v5.9.198)**: 🚨 **빈 번호 발송 차단**(대표 확정) — 기존 가드는 마스킹만 걸러 **빈값('')은 알리고까지 통과**했음(#207 발견, 선물하기 6건 전부 이 경로로 switch-off 기록). 주문안내·발송안내 양쪽 동일 지점 차단. **상태 구분: `gift-masked`(네이버가 가림·조치불요) / `no-tel`(신설, 애초에 안 옴·[재조회] 구제 가능 → receiver_masked='***'로 남겨 기존 화면 로직이 경고+버튼 표시)**. 정상 번호 무영향 8케이스 검증. **[재조회] 버튼 = 결함 아님** — 표시 조건 3개(선물하기 아님·재조회 실패·수집 시 ***) 만족 건이 현재 0이라 안 보이는 게 정상.
- **#209·#210 (세션 재시작×2)**: **무회귀 원칙 CLAUDE.md 최상단 등재**(`2eac703`). **Playwright 로컬 설치 완료**(--no-save·Chromium 헤드리스) → **#204-2 실클릭 소급 증명**: 필터 3연속 전환(경합 유발) 후 「모두 표시됨 (33/33건)」·행 수 일치·콘솔 에러 0.

### 🔴 대표 확정·확인 대기 (다음 세션 인계)
1. **알림톡 발사 GO**(KAKAO_NOTIFY=on) — 게이트 전부 통과 상태.
2. **v5.9.198 Render 배포 성공** — 서버 코드만이라 공개 확인 수단 없음(→ 4번 제안).
3. **요약 줄 실물**: `***` 건이 생기는 날 `⚠️ 수동 연락 0 · 🎁 선물하기 N`으로 갈리는지(8/5 현재 대상 0건이라 미표시가 정상).
4. **공개 버전 엔드포인트 신설 여부**(`GET /api/public/version` — 제안만, 미시공).
5. **유선번호(02-…) 분류**: 현재 「🎁 선물하기」로 뜸(#199부터) — `no-tel`로 재분류할지(1줄 조정, 임의 수정 안 함).
6. **#200 즉답 선택지 (a)/(b)/(c)** + **자사몰 챗 재개 여부**(#196 보류 유지 중 — 재개 시 대기 표시(타이핑) 시공 대기).
7. **카카오 선0 확인**(채널 관리자센터 「챗봇 채팅」 영역) + 콜백 권한 절차(고객센터) + 대기문구 선택.
8. **카페24 전환 A안** — 착수 예정(똑똑이 지시 대기).

### 🔭 관측 중
- **"주문 기록 밖(발송만 감지)"**: **8/5~8/8 4일 관측**. 기준선 = 8/4까지 누적 10건·이후 0건. 자동 알림 없음 — **똑똑이가 물으면 집계**(쿼리: `lms_guide_log l LEFT JOIN kakao_notify_log k ON order_key WHERE k.order_key IS NULL`). 4일 0건이면 #177 교정 유효 확정.

### 🛠️ 도구·환경 메모 (새 세션용)
- **Playwright**: `node_modules`에 `--no-save` 설치됨(⚠️ **`npm install` 실행 시 날아갈 수 있음** — 그때 `npm install --no-save playwright` 재실행, Chromium 바이너리는 `%LOCALAPPDATA%\ms-playwright`에 잔존). **실클릭 요령**: 로그인 우회 = localStorage `jwt_token`(+`jwt_user` JSON) — 'token' 아님 / 「알림 발송 이력」은 **서브탭이 아니라 [🏷️ 판매현황·가격] 탭 안의 섹션** / 로컬 서버는 `JWT_SECRET=아무값` + `global.setInterval=()=>({unref(){},ref(){}})`(스케줄러 차단 — 실DB 오염 방지) + PORT 지정.
- **지시함 스크립트**: 세션 스크래치는 세션별로 바뀜 — 새 세션은 poll-light.js(cc_instructions에서 '대기·진행중' 전건+본문, 첫 줄 verdict)·respond.js(UPDATE response/status='완료'/responded_at) 재작성. 컬럼: id, content, status, source, response, created_at, responded_at.
- **로컬 한계**: 네이버 릴레이 env 없음 → 재조회 성공 경로 로컬 검증 불가(폴백 경로만 가능). `/tmp`는 git bash에서 `C:\tmp`가 아닌 경우 있음 — 스크래치 디렉토리 사용.

## ★ 8/1 세션 인수인계 (지시 #149~#167 전건 완료 — v5.9.173 라이브)

- **자사몰 v5**: 상세페이지 원본 완전 재현(#149~151·155 — 이미지·텍스트 블록 전량, 지도 위젯 제거, 형광펜·밑줄·배경색·크기 위계, 이미지 병렬 93그룹) + **디자인 2라운드**(#166 — 카테고리 풀스크린 #c/·알림/장바구니/주문내역/마이/프로필 오버레이 #v/, 데모 배지 3화면, akkomi 헤더). v5-preview a56ee89. 복귀 = revert 한 방·로컬 *_pre166_backup. 04:30 일일 스냅샷이 상세 블록판 자동 갱신.
- **알림톡**: 승인 4종 검증 종결 — 이모지 원문 온전(#153 API 원문 실측)·버튼 게이트 통과(#157)·대표 개인폰 5종 테스트 발송 전부 sent(#161, 러너 aligo_test_send_request·화이트리스트 2번호). 청귤 4종 발송 안내문 등록(#164·165 최종본)·/guide URL 링크화(v5.9.173). 뱃지 단계형(#160). **발사(KAKAO_NOTIFY=on)는 여전히 대표 GO 대기.**
- **판매현황 탭**: 예약발송 토글+달력 픽커(akm-date)·행 일괄 저장·알림톡 상태 뱃지·안내문 펼치기(#152·156·158·160, app.js v=334).
- **미완·후속 (다음 깨우기 때 처리)**:
  ① **#163 잔여**: list.html 리다이렉트가 카테고리(?cate_no=)까지 튕기는 부작용 → 조건부 스크립트 교체안 게시함 — 대표 스킨 교체 회신 오면 재실측(파라미터無→메인 / cate_no→목록 정상 / 알림톡 버튼 경로) 후 #162-1 종결.
  ② ✅ **#138 종결 (2026-08-04 실측)**: 대표가 알리고 관리자 페이지에서 수동 삭제 완료 → selftest 실측 결과 **잔존 템플릿 4장뿐**(UJ_9084 주문완료_일반·UJ_9085 주문완료_예약·UJ_9086 회원가입환영·UJ_9087 발송안내2 — 전부 status=A/insp=APR). 구본 UJ_9082·반려 이미지형 UJ_9135~9138 **전량 소멸 확인**. 삭제 러너(aligo_template_del_request)는 더 이상 쓸 일 없음(코드는 존치).
  ③ 대표 실물 확인 대기: 알림톡 5종 폰 검수 항목(#161 응답)·대화방 채팅 입력(#158)·akkome-test 상세/2라운드 화면.
- 도구: 테스트 발송 러너 sendTestOne(화이트리스트 01066874031·01065594031)·selftest full 모드(문안 원문)·product_detail_request(상세 재수집).

## ★ 현재 상태 (2026-07-27 세션 종료 시점) — 문의 3채널 자동답변 완전 가동

- **배포**: v5.9.122까지 라이브(app.js v=311, curl 실확인). 7/27 하루 배포분 = v5.9.97~122. 상세는 CHANGELOG.md.
- **문의 3채널 자동답변 완전 가동** (전부 시나리오 기반 생성 — 철칙: 재료에 없는 사실 생성 금지·부족하면 SKIP·꼬리 문구 서버 강제):
  - **톡톡**(실시간 웹훅, 별도 리포) · **상품문의**(수집 3분·`qna_auto_post` ON·공개 게시) · **고객문의**(수집 3분·`inquiry_auto_post` ON·1:1 비공개, 반품·교환·환불 클레임은 항상 직원 처리). 쓰기는 `naverPostQnaAnswer()`/`naverPostInquiryAnswer()` 2개 함수만(PII 차단 내장). 중계서버 ALLOW 쓰기 2줄 반영 완료(RELAY_VERSION 2026-07-27.3, 대표 SSH 완료). 시나리오 53건, 공통 전환분 위험 점검·문구 수정 완료(43번 되묻기형 포함).
  - 답변 옆 **재료 시나리오 번호 링크** 3채널 완비(클릭→편집 화면). 톡톡봇 scenario_nos 기록 커밋 00aa21f push됨(가격 즉답 가로채기 수정 da1f184 포함).
- **글샘·마루 문의 DB 연결**(v5.9.105 — 읽기 전용·마스킹본): 문의 요약/분석을 보고서로 생성. ⚠️ **3연타 재테스트 미완**(1번만 검증됨). 마루 도구 JSON 태그 오염은 **평문 폴백**(v5.9.110)으로 해결 — 전 요원 모델 claude-sonnet-5 통일(Render env도 수정됨).
- **자동 로그아웃 수정·검증 종결**: 폴링이 이용으로 카운트되던 버그 → X-User-Active 헤더(실사용자 행동만 연장, v5.9.112). 계정별 예외 `users.idle_hours`(발주컴퓨터 12시간). 실검증 전 항목 합격·대표 현장 확인.
- **알림 관제 완전체**: 상황별 분리(0건 무알림/답변완료 확인바람/직접 처리 필요)·미처리 리마인더 30분·야간 모드 21:30~07:30 전 계열 누적·아침 종합 브리핑 07:30(0건 줄 생략·OFF 제외)·취소반품 알림은 **반품·교환만**(취소는 수집만). 타이머: 문의·상품문의 3분(하한 5→3분 완화)·반품 10분·주문 60분.
- **문의 관리 UI 개편**: 탭 순서 판매현황→시나리오→💬톡톡 문의(서브탭: 자동답변/미매칭·무응답+엑셀)→상품문의→고객문의. 전 탭 표 규격 통일(질문·답변 60자 접기·고정 열폭·상품명 말줄임·모바일은 min-width 가로 스크롤). 저장/게시 라임 토스트 피드백.
- ✅ **7/28 07:30 첫 종합 브리핑 성공**(대표 텔레그램 실물 확인) — 야간 모드→아침 브리핑 사이클 자연 검증 완료. 알림 회사폰 연동도 완료(대표 직접, 7/28).
- ✅ **톡톡봇 구 페이지 은퇴 공식 종결(7/28)**: 대표가 Render(톡톡봇)에 `PRODUCTS_PAGE=off`·`UNMATCHED_PAGE=off` 설정·재배포 → 검증 4항목 전부 합격(/products·/unmatched HTTP 410+이전 안내, /health 정상, 판매현황·가격 탭 실물 정상, 톡톡 실전 답변 확인). **scenarios.js 폴백(47KB) 정리는 백로그로 이관 — 폴백 전략 설계 선행 필요**(DB 장애 시 봇 응답 대안 결정 후 착수).
- **다음 작업 후보**: ①알리고 문자 AI상담사 ②주문 알림 확장(보류 — 대표 필요시 지시) ③scenarios.js 폴백 정리(백로그, 상기 참조).

> **★ 현재 상태 (7/29 지시 #90~#99 처리 완료 — v5.9.139 라이브)**: 🚀 **알림톡 4장(A·B·D·E) 검수 신청 완료(2026-07-29, 대표 GO #98)** — tpl_code UJ_9084(A)·9085(B)·9086(D)·**9087(E 3버튼: AC+택배 배송조회+맛있게 드시는 법·보관법, #99)**. 구본 UJ_9082(E 1버튼)는 미사용 예정. 예상 결과 7/31~8/5(공식 영업일 2일·알리고 안내 4~5일). **폴링 턴마다 심사 상태 추적 — 승인/반려 시 지시함 즉시 기록.** 상세·시행착오(tpl_type=AD 필수·linkM/linkP 매핑·심사중 수정 불가) = `docs/알림톡_템플릿_문안.md` 심사 신청 이력.
> **가동 인프라 전부 완비**: 알리고 릴레이 경유(#95 — IP 등록·릴레이 2026-07-29.1 갱신 완료·재테스트 전 항목 통과, 잔여 포인트 SMS 11,096·LMS 3,599) · 발주확인 자동(#92 — confirm ALLOW 반영·dry-run 합격) · 발송 안내 E 우선→SMS 대체(#93·94) · 공개 페이지 `/guide?p=상품id`·`/track?n=송장번호` 라이브. C 폐기 유지. 실발송 0·KAKAO_NOTIFY=off.
> **승인 후 절차(대표)**: ①Render env에 tpl_code 4종 투입(ALIGO_TPL_CODE=UJ_9084·_RESERVE=9085·_WELCOME=9086·_GUIDE=9087) ②`ALIGO_TEST=Y` 리허설 → `KAKAO_NOTIFY=on`·타이머 ON(전부 대표 GO로만). **잔여 대표 확인**: 카페24 scope 재동의 묶음 · D 마이페이지 '가입 혜택' 실존 확인 · A/B 발송 문면 JSON 일원화 정비(#93 응답 참고). 상세 = 지시함 #92~#99 응답.
>
> **★★ 8/1~8/4 세션 인수인계 (지시 #149~#178 전건 완료 — v5.9.182 / app.js v=340 라이브)**
>
> **운영 체제 변경 (#167)**: 상시 폴링 중단 → **호출 대기 체제**. 대표가 "지시함 확인" 입력할 때만 `poll-light.js` 실행·처리·응답 기록. 지시 없는 동안 폴링·출력 금지.
>
> **① 알림톡 발사선 — 게이트 통과분 (아직 KAKAO_NOTIFY=off·실발송 0)**
> - **#171 발송일 8시 컷오프**(v5.9.176, 🚨 발사 게이트 결함이었음): 주문 ~07:59=**당일 발송**("오늘 X요일")·09:00~=익일. 08:00~08:59 주문은 **자동 보류(hold-0809)** + 이력 화면 [오늘/내일 발송으로 안내] 수기 버튼(멱등·재조회 방식). `computeShipping(orderAt, holidaySet, noticeByDate, {forceDay})`.
> - **#173 발송안내 skip 구멍 교정**: 안내문 미등록이어도 알림톡은 전 건 발송(안내문 의존은 SMS 대체 문면만).
> - **#177 조사 결과 결함 2건**(v5.9.179·180): ⓐ**감지 누락** — 네이버 변경분이 뒤늦게 노출되면 체크포인트가 지나가 영구 누락(8/3 11:24 결제 1건 실측) → 주문·발송 두 수집기 체크포인트 **15분 되감기**(중복은 order_key UNIQUE·seen 필터로 제거) ⓑ**수신번호 "***"** — 주문자 연락처 없는 건은 발송 대상·기록 모두 공백 → **수취인 연락처(shippingAddress.tel1/tel2) 폴백**(발송 8곳+기록). 실발송 시 번호 공백 미발송을 막은 건.
> - **#176 [알림 발송 이력] 통합**(v5.9.177·178): 2화면 → **주문 단위 한 줄**(일시|품목|수신|주문안내|발송안내|발주확인|문면). 조회는 order_key **FULL OUTER JOIN**(원본 테이블 무변경). 칸 상태 ✅/🔁문자대체/❌실패+[수동 재발송]/⏸보류+[오늘][내일]/— 미도래/dry-run.
> - **#177 수신 풀번호**: DB는 마스킹만 저장(원칙 유지)·**표시 시점 네이버 재조회**(admin 전용·배치 100·5분 캐시·실패 시 마스킹 폴백).
> - 🔴 **발사(KAKAO_NOTIFY=on)는 대표 GO 대기.** 절차: env TPL 4종은 코드 기본값 투입 완료 → `ALIGO_TEST=Y` 리허설 → `KAKAO_NOTIFY=on`.
>
> **② 상품 상세 재현 (#149~#151·#155 — v5-preview)**: 상세 이미지 **캡 2중 제거**(전 20종 588장)·**텍스트 블록 원순서 보존**(277블록·XSS sanitize+태그 밸런싱)·네이버 지도 위젯 통째 제거(이미지+텍스트+링크 묶음·과잘림 금지)·스타일 재현(형광펜/밑줄/배경색/`se-fs-fsNN` 폰트 위계·imageStrip 병렬 93그룹). 리뷰는 상세정보처럼 **인페이지 접이식**(리뷰 더보기)·리뷰수 클릭 시 해당 섹션 스크롤.
> - 🔴 **리뷰 전량(13.6만 건) 수집은 불가 확정** — API 20건/페이지 → 상품당 6,800콜·429 차단·용량. 대체 = **상품별 최근 20건 일일 갱신**(v5.9.175, 04:30 스냅샷 동반).
>
> **③ 문의관리 UI (#178 — v5.9.181·182)**: ⓐ발송 휴무일 목록 → **월 달력**(날짜 클릭 지정/해제·시작→종료 클릭으로 기간·토요일 상시불가 표시·등록 현황은 접이식 요약표로 계승) ⓑ수정 이력 **조회식 전환**(기본 비표시·기간 선택) ⓒ발송 이력 **날짜 조회 + 연락처 뒷자리·품목명 검색**.
> - 🚨 **자책 기록 2건 (같은 실수 반복 금지)**: ⓐ내 패치 스크립트가 `oldBindEnd > oldBindStart` 조건으로 삽입 위치를 잡았는데 두 앵커의 실제 순서가 반대여서 **버튼 5종 이벤트 바인딩이 통째로 조용히 누락**(마크업·함수는 정상, 클릭만 무반응) → v5.9.182에서 실행 검증된 `setupBotProductsTab` 블록에 복구. **패치 스크립트는 삽입 후 `grep -c`로 실삽입을 반드시 확인할 것.** ⓑ어제 하네스 검증이 함수를 **직접 호출**해 통과시켰던 탓에 "버튼 클릭 → 함수" 경로가 미검증으로 남았음. **UI 검증은 반드시 실제 마크업에 클릭을 날려서 할 것.** ⓒ서버 파라미터명 불일치(`end_date` vs `end`)·재렌더 후 **detached DOM 노드 클릭**으로 거짓 합격이 났던 사례도 동일 세션에서 발생 — 재조회(selector 재질의) 후 클릭.
>
> **③-2 지시 #179 (v5.9.184 — 8/4 오후)**: #178 실물 검수에서 **조회가 실제로 안 먹는 결함 2건**이 드러나 교정. ⓐ**날짜 필터 통째 무시** — 정규식 백슬래시 소실(`/^d{4}-d{2}-d{2}$/`, 정상은 `\d`)로 `useRange`가 항상 false → 날짜 조건이 SQL에서 빠지고 "최근 100/120건" 반환(라우트 3곳: 판매현황·시나리오 수정이력, 발송이력) ⓑ**검색어 조회 HTTP 500** — SQL 파라미터 `$1`·`$2`의 `$` 소실로 정수 리터럴이 박힘(`LIKE 1`) → `operator does not exist: character varying ~~ integer`. 추가분 = 빠른 기간 버튼 [오늘][최근 7일][최근 30일]·검색창 "연락처로 검색"(풀번호 앞3+뒷4 대조·뒷자리 부분검색 유지)·휴무일 달력 확대(560→780px 중앙정렬·셀 62px, 디자인 가이드 토큰 준수·구 연두 #8CC63E 잔재 정리)·표시 상한 300건 경고·서브탭 콘솔 TypeError 가드.
> - 🚨 **자책 기록 3건째 (같은 뿌리 — 패치 스크립트 이스케이프 소실)**: JS 문자열로 코드를 삽입할 때 `\d`→`d`, `$${}`→`${}` 가 조용히 소실됨. **삽입 후 `grep -c`로 개수만 세는 걸로는 못 잡는다**(문자열은 들어갔으나 내용이 손상). 정규식·SQL placeholder를 포함한 패치는 **삽입 후 반드시 그 값으로 1회 실행**해 판정할 것(`node -e "정규식.test(실제값)"` 한 줄이면 잡혔음).
> - ✅ **검증 방식 교정(어제 자책 ⓑ 해소)**: 이번엔 (a) 수정 전 코드로 `git stash` 되돌려 **결함 실재 재현** (b) 로컬 실서버 기동(**setInterval 무력화로 스케줄러 차단 — 실DB 오염 방지**) 후 **API 15케이스를 실DB 쿼리와 숫자 대조** (c) **실제 브라우저 마우스 클릭**(Playwright) 3경로 교차. 하네스 직접 호출 방식은 폐기.
> - ⚠️ **측정 함정 2개 기록**: ①`new Date(DB date).toISOString()`은 KST 날짜를 UTC로 되돌려 **하루 밀려 보임**(8/1 → "7-31") — 날짜 대조는 DB 쪽에서 `(created_at + interval '9 hours')::date`로 통일할 것 ②연속 클릭(▶◀오늘) 후 한 번에 측정하면 **제자리로 보여 "버튼 무반응" 오판** — 한 동작씩 재질의 후 판정.
>
> **③-3 지시 #180·#181 (v5.9.185 — 8/4 오후)**: ⓐ**발송이력 페이징**(상한 300 폐지 → 100건씩 [더보기]·`total` 분리로 요약 줄 고정·타이브레이커·필터 SQL 승격). 최근 7일 실측 **779건** — 기존 상한에서 479건이 잘리고 있었음 ⓑ달력 안내 문구 danger 토큰 강조 ⓒ**수정이력 탭 혼입 교정** — 원인은 프론트 2건(**시기별 상품지식 전용 라우트 부재** → 그 탭에서 시나리오 이력이 조회됨 / **탭 전환 시 이력 미초기화** → 이전 탭 결과 잔류). 서버는 원래도 `target_type`으로 분리 중이었음. `season-knowledge-logs` 신설·대상 매핑표·초기화·제목 명시·대상 없는 탭 카드 숨김. 교차 오염 id 0건 ⓓ빠른기간 재조회 — 코드는 정상이었고 **오늘·최근7일이 실제 0건이라 화면이 같아 보인 것**이 원인 → **조회 요약 줄 신설**(건수·기간·대상 항상 표시)·0건 문구에 기간 명시·**요청 순번 가드**(실서버 지연 시 늦은 응답이 최신 화면 덮는 것 차단).
> - 🚀 **알림톡 실발송 리허설 완료 (8/4, 지시 #180-B)**: 화이트리스트 2번호(01066874031·01065594031) × 승인 4종 = **8건 전량 `sent`**(알리고 code=0). 문면은 실주문 품목 + `computeShipping` 당일 실계산. 버튼 링크 4종 전부 HTTP 200. **실물 카톡 수신은 대표 확인 대기**(보고 규약 #113).
>   - 🔴 **`ALIGO_TEST=Y`를 켜면 안 되는 이유(중요)**: 알리고 공식 testMode는 **과금·실발송 없이 성공 응답만** 반환(kakao-notify.js:129·178) → 폰에 안 옴. 폰 수신 검수가 목적이면 **켜지 말고** 러너 `sendTestOne`(화이트리스트 하드 가드 내장·KAKAO_NOTIFY 무관)으로 실발송할 것.
>   - 리허설 발송은 `kakao_notify_log`에 **기록을 남기지 않음** → 실운영 통계 오염 0(전후 스냅샷 대조로 확인).
>   - 🔴 **발사 전 잔여 리스크(리허설로 검증 불가)**: ①LMS failover 미발동(전건 성공) ②수신거부·채널차단 고객 처리 ③대량 발송 한도·속도(실운영 하루 200~300건) ④**자동 발송 라인 전체 경로는 여전히 실API 미태움**(리허설은 수동 단건 러너) ⑤hold-0809·수취인 폴백 분기 ⑥알림톡 잔여 포인트(대표 확인).
>
> **③-4 지시 #182 (v5.9.186 — 8/4, 🚨 발사 게이트 결함 4건째)**: 리허설 UJ_9085 문면 지적 → **리허설 한정 현상**으로 판명(테스트 러너가 4종에 같은 vars 공통 주입). 그러나 파고들다 **별개 실운영 결함 발견**: 예약 판정이 `/예약/` **문자열 매칭뿐**이라 네이버 옵션 표기에 '예약'이 빠진 청귤 주문이 일반(UJ_9084)으로 새어 `computeShipping` "오늘/내일 발송"으로 오안내 — **dry-run 이력 13건 실측**(실제 발송일은 8/10). → `isReserveOrder(optText, matched)` 신설: '예약' 문자열 **또는** 품목 `reserve_ship_start`가 미래면 예약, 시작일 경과 후엔 일반 복귀. 적용 2곳(주문 수집기·보류 수기 발송). **일반 문면 무접촉**.
> - 예약 분기 참조: 발송일 소스 = `bot_products.reserve_ship_start`(판매현황 탭 편집·계산 아님). 청귤 4종 전부 2026-08-10 등록. 이력 대조 = 예약형 241건·일반형 497건 전부 정상.
> - 🔴 **대표 확정 대기**: "발송 시작일 경과 후 처리" — 현재는 일반 계산 복귀. 예약 물량이 밀리면 `reserve_ship_start` 갱신 또는 휴무일 안내문구로 운영 대응(자동 연장 로직은 임의로 넣지 않음).
> - 🔴 **알림톡 폰 수신 검수 통과(8/4 대표)**: 리허설 8건 전부 수신·버튼 4종 실동작·이모지/변수 치환 정상. **발사(KAKAO_NOTIFY=on)만 남음** — #180-B 잔여 리스크 5건(LMS failover·수신거부·대량 한도·자동 라인 실API 미검증·포인트) 감안해 대표 판단.
>
> **④ 자사몰 v5 (#166·#168·#170 + 대표 직접 지시)**: 디자인 2차 채택(카테고리 풀스크린·알림·장바구니·주문내역·마이·프로필)·네이버 프레임 벤치마킹(썸네일 숨김·재구매 배지·가격/구매 영역)·알림 2중창 구조 결함 수정(`window.__v5z` 동적 z-stack)·프로필 캐릭터 = 할망/하르방/**아꼼이(기존 캐릭터 실이미지)**/**아깨비(아꼼이 남자 버전 — 현재 👦 임시, 실이미지 교체 필요)**, 감귤·밤호박 제거.
>
> **⑤ 미완·대표 확인 대기 (다음 세션 인계)**
> - ✅ **UJ_9082 수동 삭제 완료 (2026-08-04, 대표 직접)** — selftest 실측으로 확인: 발신프로필 잔존 템플릿은 승인 4장(UJ_9084·9085·9086·9087)뿐, 구본 9082 및 반려 이미지형 9135~9138 전량 소멸. 발사 시 코드 기본값(A=9084·B=9085·D=9086·E=9087)과 알리고 실제 목록이 1:1 일치.
> - 🔴 **아깨비 실캐릭터 이미지** 필요(현재 이모지 임시).
> - 🔴 **대표 실물 확인 3건**: 알림톡 5종 테스트 발송 폰 수신 · 메시지룸 채팅 입력 경로 · akkome-test 화면.
> - 🔴 **발사 GO**(KAKAO_NOTIFY=on) 및 그 전 `ALIGO_TEST=Y` 리허설.
> - 기존 잔여 유지: 카페24 scope 재동의 · 시기표 20종 검수 · 카드 축약명/이미지 호스팅 정책 · scenarios.js 폴백 정리(백로그).
>
> **★ 7/30 새벽 세션 종료 시점 추가분 (#100~#102)**:
> - **#100 이미지형 알림톡 ✅ 등록·검수 신청 완료 (7/30 오전, v5.9.141)**: 게이트 2건 충족(①대표 이미지 제공→`assets/alimtalk_image.jpg` 800×400·50KB·JPEG 합격 ②대표 릴레이 SSH 갱신→2026-07-29.2 실측 확인) → 이미지형 4장 등록·검수 신청 성공 — **UJ_9135(A)·9136(B)·9137(D)·9138(E 4버튼)**. 신설 `relay_health_request` 플래그(읽기 전용 relayHealth — 릴레이 버전 자율 검증)도 v5.9.141에 포함.
> - 🎉 **텍스트형 4장 전원 승인(7/30 18:18 실측)**: UJ_9084·9085·9086·9087 APR(+구본 9082 APR — 미사용). **이미지형 4장(UJ_9135~9138) REQ — 추적 계속.** 가동 선택지 = (a)텍스트형 우선 가동(env 투입→ALIGO_TEST 리허설→KAKAO_NOTIFY on — 대표 GO로만) (b)이미지형 대기 — 대표 판단 대기.
> - **심사 결과 확정 (7/31 11:38 실측)**: 텍스트형 5장(UJ_9082·9084~9087) **APR 승인 유지** · 🚨 **이미지형 4장(UJ_9135~9138) 전원 반려(REJ)**. 반려 사유는 API 목록 미포함 — **알리고 관리자 페이지에서 대표 확인 필요**. #100 전략(이미지형 본판)은 보류 — 선택지 (a)기승인 텍스트형 4장으로 즉시 가동(env ALIGO_TPL_CODE=UJ_9084·_RESERVE=9085·_WELCOME=9086·_GUIDE=9087→ALIGO_TEST 리허설→KAKAO_NOTIFY on, 대표 GO로만) (b)반려 사유 확인 후 이미지형 수정·재신청(반려 상태는 수정 가능). 지시함 #100 응답에 동일 기록.
> - **#101·#102 자사몰 index_v4 완료**: `제주아꼼이네 플랫폼\index_v4.html` (1.2MB 단일 파일·v3 보존). **시안 원본 = `플랫폼\assets\시안_홈_2026-07-29.png`(864×1536)**, 크롭 자산 13종 = 같은 assets 폴더(akkomi_profile·akkomi_drop(투명화)·ocean_banner.jpg·ic_* 10종 — 전부 v4에 base64 임베드). 빌드 스크립트 = 세션 스크래치 build-v4b.js(v3→v4 풀빌드 방식). 대표 피드백 반영: 배경 흰색/회백·감귤체험 탭(체험 섹션 스크롤)·타이포 통일·하단 탭 홈/쇼핑/내 나무/주문내역/마이.
> **★ 7/31 오후 진행분 (#136~#140 완료)**: ①**#136·#139·#140 — 클로드 디자인 폴리시 전면 채택**(v5-preview 314f900): 산출물(Jeju Akkome Design Polish) 디자인 100% + 현행 데이터·배선 주입. 특가 일자 로테이션 실구현·스토리 GIF(2.5MB 최적화 — gifsicle -O옵션은 이 GIF 애니를 깨뜨림·scale만 사용)·룰렛/캘린더 풀 리스킨(로직·20종 데이터 생존)·farm 신설. 복귀=revert 314f900 e6ebcd8. 로컬 백업 *_pre139_backup ②**#137 — 승인 알림톡 가동 준비**(v5.9.154): TPL 기본값 투입(A=9084·B=9085·D=9086·E=9087)·주문 문면 승인 템플릿 일원화·예약 분기·타이머 kakao_notify ON(dry-run — KAKAO_NOTIFY=off 유지). dry-run 10건+ 축적 시 문면 샘플 보고(폴링 추적) ③**#138 — 9082·이미지형 정리**(v5.9.155): 삭제 러너(화이트리스트)·구본 문안 docs 백업. 🔴 삭제 실행은 릴레이 2026-07-31.1 SSH 갱신 후(template/del ALLOW — 갱신 시 플래그 재발동) ④이미지형 4장 반려 확정(브랜딩 이미지 유형 불가) — 재도전 안 함.
> **★ 7/30 오전 추가분 (#103~#105 완료 — v5.9.142 라이브)**:
> - **#103**: v4 검색바→AI상담 입구("궁금한 건 언제나 물어보십셔!!"+챗 시트 프로토타입·시나리오 DB 5번째 채널 전제) + 상품 API 조사 — 공식 경로 확정(POST /v1/products/search·GET /v2/products/*)·릴레이 ALLOW 2종 추가·`naver_query_request` 실행기(v5.9.142). **⚠️ 릴레이 SSH 대기(RELAY_VERSION 2026-07-30.1)** — 반영 후 공식 API 교차 검증·detailContent 2차.
> - **#104+#105 — `플랫폼\index_v5.html` 실데이터판 완성**(v4 보존): 자사 브랜드스토어(brand.naver.com/jejuakkome) 공개 페이지 실측 스냅샷 = **전 상품 20종**(실사진 URL·실가격+옵션범위·할인율·리뷰수·평점·재고·옵션 전체) + 스토어 지표(관심고객 190,013·평점 4.76) + 베스트 실리뷰 12건 → `플랫폼\assets\data\store_snapshot_2026-07-30.json`. 상품 그룹핑(대표 카드→옵션 시트)·품절 15종 하단 "시즌 준비 중"+대기 신청 버튼(#105)·신선도 배지·먹는법 탭(shipping_guide 재사용)·후기 실리뷰. **리뷰 공식 API는 부재 확정**(포럼 #1909) — 공개 페이지 STORE_PICK로 대체. v4에도 pick 3종+상품 4카드 실사진 반영.
> - **#107 완료 (v5.9.143~144)**: 릴레이 2026-07-30.1 반영 확인 · **공식 상품 API = GW.AUTHN(앱에 '상품' 권한 없음 — 🔴 대표: 커머스API센터에서 권한 추가 필요)** · 상세 2차 = 공개 상세 이미지 5종×10장 → v5 "상세정보" 탭 · naver_product_no 23건 투입(+판매현황 탭 표시) · v5 개편(품절→캘린더 이주·캘린더 재설계 시기표 20종·후기 3건+더보기·캘린더 요약) · **일일 스냅샷 수집기 `product_snapshot`(기본 OFF·04:30 앵커·GET /api/public/store-snapshot)** — 권한 개통 후 ON.
> - ✅ **상품 API 권한 개통 (7/30 12:05 대표)**: 공식 API 6콜 전부 성공·스냅샷과 교차 검증 20/20 일치·**일일 스냅샷 타이머 ON 가동**(매일 04:30·첫 수집 12:08 성공 20건·/api/public/store-snapshot 정상). #113(오보 사안)도 종결 — 원인=정규식 간격 14자 미매칭(발송 0건), v5.9.148 수정·재테스트 12:04 result='sent'(실물은 대표 확인 대기)·**보고 규약 신설(운영 규칙 참조)**.
> - **대표 확인 필요 목록(잔여)**: ①시기표 20종 검수(추정 초안) ②황금향 매핑 확인 ③카드 축약명·이미지 호스팅·리뷰 수집 정책·카페24 read_product scope ④📮 재테스트 알림 실물 수신 확인(#113 합격 기준).
> - **#109~#111 완료 (v5.9.146 + 봇 5a25d7e)**: 청귤 시드 정정(달달 삭제·산도 본질·💡 장점 포인트 규약) · **"정직한 사실+현재의 장점" 전 채널 공통 지침**(qna·inquiry·봇 3곳 — 장점도 재료만·역검증 SKIP 확인) · 요청형 문의 보강(복창·완료 단정 금지 3곳 + **요청형 직원 인계 알림 폴러**(message_logs 3분·야간 보류 큐) — 실측 합격·검증 시뮬 총 7케이스 전부 합격.
> - **#108 완료 (v5.9.145) — 시기별 상품 지식 실가동**: `product_season_knowledge` 시드 12구간(대표 원문) → `seasonScenariosToday()`가 오늘 구간을 "[오늘 시기]" 가상 시나리오로 3채널 주입(상품문의·고객문의·**톡톡은 /api/scenarios 응답 포함 — 봇 무수정**). 화면 = 판매현황 탭 "📅 시기별 상품 지식"(직원 편집). 재현 4케이스 합격("포슬한가요"→촉촉 전환기 교정·회귀 무변화). 재현 도구 = `qna_sim_request` 플래그(생성만).
> **★ v5 온라인 미리보기 (#121 — 2026-07-30 가동)**: **https://akkome-test.onrender.com** = 자사몰 v5 프로토타입 (직원 공유·폰 실기기 확인용). 소스 = `akkome-website` 리포 **v5-preview 브랜치**(orphan — dev·리뉴얼 원본 무접촉). 구성: `/`(=index_v5 사본)·products_v5·calendar·my·roulette + assets/data 스냅샷.
> **미리보기 갱신 절차**: 플랫폼 폴더에서 v5 파일 수정 후 → akkome-website 리포에서 `git worktree add tmpwt v5-preview` → 변경 파일 복사(index_v5.html은 index.html로도)·커밋·push → `git worktree remove tmpwt` (Render 자동 재배포 ~1분). ⚠️ 공개 사이트 — PII·시크릿 0 점검 후 push(리뷰는 마스킹형만).
> **운영 규칙 요약**: 지시함 폴링(경량 poll-light v2 — 판별 내장·대기 전건 조회) · 3층 체제(#71) · 서브 전문가 역할 부여(#82) · 품질 그물망(#83 — 함정5·체크리스트·교차 검수·애매하면 대표 확인) · 알림 정책(#84 — 지시함 평시 침묵, 긴급만 🚨 `naverCfgSet('urgent_call_request', {reason})`) · 검수 신청·실발송·스위치 ON은 대표 GO로만.
> **🔴 보고 규약 (#113 대표 확정 — 상시)**: 알림·발송류 검증 보고에서 **"대표 폰 수신"·"실물 도착" 표현 금지** — 코드/DB/API 성공 응답(예: 텔레그램 result='sent')까지만 주장하고, 실물 수신 확인은 항상 "대표 확인 대기"로 표기한다. 간접 신호(큐 비움 등)를 발송 증거로 단정하지 말 것 (7/30 #111 오보 사례 — 정규식 미매칭으로 발송 0건이었는데 '실측 증거'로 보고).
> **속도 규칙 2종 (#90 대표 확정 — 상시)**:
> - **[규칙 A — 서브에이전트 최소주의]** 기본은 메인 단독. 서브 기동은 ①독립 큰 덩어리 2개+ 병렬 이득 명확 ②완성 단계 교차 검수 필요 시에만. **예상 30분 미만 작업은 무조건 메인 단독.** 전문가 역할·품질 그물망은 메인 단독 시에도 동일 적용(퀄 유지, 기동 비용만 제거).
> - **[규칙 B — 폴링 간격 상한]** 지시 없을 때도 대기 간격 **5분 이내** 유지(20분 완화 금지 — 똑똑이 지시 감지 지연 방지). **지시 응답·대기 모드 선언 직전에는 반드시 재폴링**(스테일 판단 금지 — 7/29 #87~89 놓침 원인). poll-light v2: '대기·진행중' 전건 조회 + 첫 줄 verdict 내장(LIMIT 잘림·눈판독 결함 수정).

## ★★ 2026-07-29 진행분 (지시 #80~#85 — 아래 7/28 인수인계에 이어서)

- **운영 규칙 3건 추가 접수·상시 적용**: #81 구조·틀 집중(디자인은 추후 전문 디자인) / **#82 서브에이전트 전문가 역할(페르소나) 부여 의무** / **#83 품질 그물망**(착수 전 함정 5·마감 체크리스트·교차 검수(만든 자가 검사 금지)·애매하면 "대표 확인 필요"로 분리). 보고에 "에이전트 운용"+"품질 그물망 3줄" 필수.
- **#80 재개 작업**: Playwright MCP 등록 완료(✔ Connected — **세션 재시작 후부터 도구 사용 가능**, 스크린샷 자가 검수용) / 리뉴얼 리포 git에서 이미지 6건 복구(로고·체험장4·추천pick — 플랫폼 폴더 images\, 원본 무수정. **아꼼이 캐릭터 단독 파일은 git에 없음** — 로고가 겸함) / **index_v3.html 최종**: 로고·체험장 실사진 base64 임베드(738KB 단일 파일)·릴스 4→7편(청귤 카드 = 시즌 배지+상품 링크+대기 신청 버튼).
- **v5.9.133 (#84)**: 지시함 텔레그램 알림 **기본 침묵**('ccbox' 스위치, CS폰 소음 방지) + 긴급 호출 **"🚨 똑똑확인요청"** 신설 — 발동 = `naverCfgSet('urgent_call_request', {reason:'...'})` DB 플래그(60초 폴러가 발송 후 제거, 야간 무관). 테스트 1건 발송 확인. ⚠️ 대표 확인 대기 2건: 텔레그램 장애 시 긴급 호출 유실 트레이드오프·ccbox ON 시 야간 발송 여부.
- **v5.9.134 (#85)**: 자사몰 **게임 백엔드 선행 시공** — 테이블 5종(mall_members·points_ledger append-only·roulette_spins 1일1회 DB UNIQUE·tree_state·reward_grants) + `mall-api.js` → `/api/mall/*` 13종(**env MALL_API=on + MALL_API_TOKEN 없으면 전 라우트 503 — 현재 완전 잠금 실측**) + 데이터관리 "🎮 게임 운영" 카드(확률·한도 화면 편집·대표 전용). QA 교차 검수 결함 8건 수정. ⚠️ 대표 확정 대기: 룰렛 확률·원가(현 기대지급 ~22.7물방울/스핀)·한도 실값·회원 인증 방식·가동 env 투입.
- **다음 후보**: ①알림톡 템플릿 검수 신청(#75 보류 해제 시) ②발송 안내 dry-run 실검증(타이머 lms_guide ON) ③자사몰 프론트-백엔드 연결(프로토타입→/api/mall 실호출) ④카페24 scope 재동의(mall.write_promotion·mileage).

## ★★ 2026-07-28 세션 인수인계 (지시 #79 금일 종료 — 내일 대표 GO 사인 후 재개)

> **운영 체제**: 지시함(AGENT OFFICE) 폴링 파이프라인 가동 중. 똑똑이(전략·지시)→클코 메인(총괄·분할·검수·취합)→서브에이전트(제작 실무·병렬) 3층 구조(지시 #71). 보고는 지시함 단일 창구 + "에이전트 운용" 1줄 필수. **민감 작업(DB 마이그레이션·배포/push·기존 가동 코드 인접·시크릿)은 메인 직접**, 서브 결과물은 메인 검수 없이 머지 금지.
> **지시함 응답 방식**: `respond-instruction.js` 부재 → `cc_instructions` 직접 UPDATE(세션 스크래치 스크립트). 경량 폴링은 id·상태만 조회 후 '대기' 건만 본문 조회.
> **모델**: 7/28부터 Opus 5 (Fable 한도 이슈 — 대표 지시 #78).

### 오늘 배포분 (v5.9.128~132, 전부 additive·실발송 0건)
- **v5.9.128 (#68)**: 알림톡 자동화 기반 — bot_products.notify_message(품목별 짧은 안내문)·shipping-schedule.js(발송일 계산)·kakao-notify.js(알림톡 뼈대)·kakao_notify_log·season_waitlist(시즌 대기 신청)·텔레그램 kakaosend 알림. 전부 스위치 OFF
- **v5.9.129 (#69)**: 발송일 규칙 대표 확정 — **토요일만 발송 불가**(일 발송 가능)·발송일=주문 다음날(토·휴무일이면 밀기)·"오전 발송" 표현. 공휴일 하드코딩 → `shipping_holidays` 테이블+화면 관리 승격(2026 공휴일 19일 시드)
- **v5.9.130 (#73)**: 휴무일 **기간 등록**(최대 31일·연속일 한 줄 묶음 표시) + **안내 문구**(넣으면 그 기간 주문의 발송안내가 문구로 대체 — 명절 마감은 계산이 아니라 화면 운영으로 흡수)
- **v5.9.131 (#74·#75)**: bot_products.**shipping_guide**(LMS 장문 안내문)+판매현황 탭 편집 열·시드 19품목·`{{내일요일}}`/`{{모레요일}}` 요일 자동 치환·sendLms 뼈대. 알림톡 템플릿 2종 규격 정리 → `docs/알림톡_템플릿_문안.md`. ⚠️ **템플릿 실등록·검수 신청은 보류(#75 — 다음 주 대표 GO 사인)**
- **v5.9.132 (#76)**: 발송 안내 트리거 — 타이머 `lms_guide`(**기본 OFF**·30분, 네이버 DISPATCHED 감지→shipping_guide LMS)·`lms_guide_log`(order_key UNIQUE 중복 방지)·판매현황 탭 하단 "📨 발송 안내 이력" 섹션+수동 [발송 안내 보내기](대표 전용). 🔴 **알리고 문자 호스트 오류 발견·수정**(apisms.aligo.in → **apis.aligo.in**), testmode_yn 지원(env ALIGO_TEST=Y)

### 🔴 실발송 가동 전 대표 손작업 (순서)
① 알리고 발신프로필 등록(웹 — 채널 검색용 ID+관리자 카카오톡 인증번호→senderkey) ② 알림톡 템플릿 등록·검수(4~5일 — **#75 보류 해제 후**) ③ 알리고 포인트 충전(알림톡 6.5원+대체문자 8.4~25.9원) ④ Render env 투입: `ALIGO_API_KEY`·`ALIGO_USER_ID`·`ALIGO_SENDER_KEY`·`ALIGO_SENDER`·`ALIGO_TPL_CODE`·`ALIGO_TPL_CODE_RESERVE`·`KAKAO_NOTIFY=on` ⑤ 타이머 ON
- 검증 순서 제안: 타이머 `lms_guide` ON(그대로 dry-run) → 이력에 문면 쌓임 → 문안 검수 → `ALIGO_TEST=Y` 리허설 → `KAKAO_NOTIFY=on`

### 자사몰 신축 프로토타입 (`C:\Users\전승범\OneDrive\문서\제주아꼼이네 플랫폼\` — git 리포 아님·로컬 보존)
- `home.html`(v1 배민 노랑톤) / `home_v2.html`(v2 회사프로그램 인디고 스킨) / **`index_v3.html`(최신 — 배민 뼈대+기존 리뉴얼 자산 7종+신규 기능 자리 5종)** / `index.html`·`products.html`·`product.html`·`calendar.html`·`my.html`·`roulette.html`(룰렛 실동작) / `전환_아키텍처_설계서.md` / `기존자산_분석.md`
- **아키텍처 권장안 = (c) 하이브리드**: 몰=상품·결제·회원 / 게임·물방울·귤나무=독립 웹앱(Render) / 보상은 카페24 Admin API로 쿠폰·적립금. (b) 독립 전면은 **카페24에 일반 앱용 주문 생성 API 부재로 구조적 불가**(공식 확인). 신규 API 12종·테이블 5종 목록은 설계서 §2-1
- ⚠️ 기존 리뉴얼 폴더(`★제주아꼼이네 자사몰 홈페이지 리뉴얼`) **무수정 유지** — 발견 2건 미조치: ①깨진 이미지(로고·아꼼이·체험장 4장)는 git HEAD에 살아있어 `git checkout -- images/` 한 줄로 복구 가능(대표 지시 대기) ②미사용 릴스 3편(비바람·트렁크SNS·청귤풋귤 62MB) 즉시 가용

### 내일 재개 시 첫 작업 후보
① 자사몰 3차(index_v3 기준 나머지 페이지에 리뉴얼 자산 이식·기존 리뉴얼 이미지 복구 여부 결정) ② 알림톡 템플릿 검수 신청(#75 보류 해제 시) ③ 발송 안내 dry-run 실검증(타이머 ON→문면 검수) ④ 실서비스 API 12종 설계·시공 착수 ⑤ scenarios.js 폴백 정리(백로그)
- **폴더 정리**(별도 코워크 세션): 낱개 자료 → `_참고자료/`(gitignore됨) 이동·README 기록. .gitignore에 `_참고자료/`·`NCP인증키/` 추가 커밋(a92381c). 코드·docs·디자인 폴더는 무이동.
- 백업 체계(7/26 완성): ①배포 시 강제 ②일일 07:00 스케줄러 ③로테이션 B정책 ④복구 절차서 `docs/DB복구절차.md`. ⚠️ .cmd 한글 주석 금지(CP949). 시즌 시한부 시나리오 38·44(청귤 특가)는 특가 종료 또는 9/15에 갱신/OFF 필요.
- ✅ 대표 확인 잔여 2건 모두 완료(7/28): ①Render 워크스페이스 플랜 = Hobby·**PITR 복구창 3일 확정**(Recovery 화면 실측, `docs/DB복구절차.md` 기재) ②OneDrive/Render **2단계 인증 설정 완료**(대표 직접).

## ★ 진행 중: 문의시나리오 DB 통합 (2026-07-25, v5.9.55)

- **[B] 타이머의 목적 재정의(대표)**: 송장변환·중간발주는 수기 버튼 유지. 타이머는 ①고객문의 자동답변 ②정산 아침 자동수집용. 진행 순서 확정: ①문의시나리오 DB 통합 → ②정산 수집(아침 9:30~10시 + 오후 7~8시, 화면에서 변경 가능) → ③네이버 상품문의 수집+자동등록(쓰기 — 중계서버 허용목록 추가 필요). 문의 수집 주기 30분.
- **①단계 완료(v5.9.55 배포됨)**: `inquiry_scenarios` 테이블 + 톡톡봇 시나리오 **47건 이관**(46+영업시간외, 바이트 일치 검증) + [문의 관리] 화면(직원 편집 가능, 삭제·전체스위치 대표 전용) + 봇 조회 API `GET /api/scenarios`(env `SCENARIO_API_TOKEN` Bearer, 미설정 시 503 잠김).
- **톡톡봇**(`C:\Users\전승범\OneDrive\문서\★제주아꼼이네 톡톡봇`, 별도 리포): `scenario-store.js`(5분 캐시·폴백) 커밋됨, **push는 대표가 직접**(마지막 1회). `SCENARIO_SOURCE=code`(기본)→검증 후 `db` 스위치. 비교 스크립트 `scripts/compare-scenarios.js` 47/47 일치 확인됨.
- **대표 실행 절차**: `docs/문의시나리오_전환절차.md` (토큰 생성→Render env 2곳→봇 push→스위치→체크리스트).
- 설계/계획: `docs/superpowers/specs/2026-07-25-inquiry-scenario-db-design.md`, `docs/superpowers/plans/2026-07-25-inquiry-scenario-db.md`. 진행 원장: `.superpowers/sdd/progress.md`.
- ⚠️ 영업시간외 문구(0번)는 현재 봇이 미사용(코드에 import만 존재) — 화면 수정해도 봇 영향 없음, 재활성화는 별도 작업.
- **품목별금액→판매현황 자동 연동 (v5.9.57 배포됨)**: pricing 저장(POST/PUT) 시 새 품목명이 bot_products에 **'준비중'(봇 미노출)·판매가 빈칸**으로 자동 등록(`syncPricingToBotProducts`, ON CONFLICT DO NOTHING — 삭제 품목 부활 금지). 판매가는 수기 세팅 후 [판매중] 전환 시 노출. 결제가·판매가는 이름만 공유(금액 복사 금지). 탭2 수동 [품목 추가] 버튼 제거(유입 일원화, POST API·복구 로직은 존치). 백필 19개 준비중 등록됨.
- **판매현황·가격 탭 (v5.9.56 배포됨)**: `{{가격표}}`/`{{판매현황}}` 원본 = `bot_products`(회사프로그램과 **같은 DB**, 36건) → [문의 관리] 탭2에서 관리(저장 → 봇 1분 캐시 반영, 봇 무수정). ⚠️ **스마트스토어 판매가 — '품목별 금액'(거래처 결제가)과 절대 통합 금지.** 톡톡봇 정리 커밋(시드 하드코딩 제거·deleted_at 필터·PRODUCTS_PAGE 게이트) push 완료. ✅ **구 /products·/unmatched 페이지 은퇴 종결(7/28)** — Render `PRODUCTS_PAGE=off`·`UNMATCHED_PAGE=off` 적용·410 실측 확인. 시나리오는 **db 스위치 완료·실응답 정상**(대표 확인).
- **에이전트 운용 원칙(대표 지시)**: 총괄(설계·판단·실서비스 섬세 작업)은 메인 모델 직접, 단순 시공만 Haiku 위임+Sonnet 검수, Task별 분류 선보고.

---

## 0. 프로젝트 개요

- **회사프로그램**: Node.js/Express + PostgreSQL, **Render** 배포 (서비스명 jeju-acom-company).
- **저장소**: `bumiletter-bit/jeju-acom-company` (GitHub, **PUBLIC**, main 브랜치 push → Render 자동배포).
  - ⚠️ PUBLIC이므로 **네이버 시크릿·토큰을 코드/커밋에 절대 넣지 말 것.**
- **로컬 작업 경로**: `C:\Users\전승범\OneDrive\문서\★제주아꼼이네 회사프로그램`
- **목표**: 네이버 스마트스토어 주문/정산을 API로 자동 수집해 회사프로그램(송장변환·정산·중간발주)에 연동. 이후 쿠팡·카페24(자사몰)까지 확대.

### 배포 방법
```
npm run deploy
```
- 동작: DB 백업 → git tag → git push (main). push되면 Render가 자동 재배포(~2분).
- ⚠️ **DB 백업이 오래 걸림(3분+ 가능)** → Bash 타임아웃 **9분(540000ms)** 이상으로 실행할 것.
- 배포 전 반드시: `node --check server.js`, `node --check public/app.js`, `version.js`의 VERSION 상향, `public/index.html`의 `app.js?v=NNN` 캐시버전 상향, `CHANGELOG.md` 갱신.
- **현재 버전: v5.9.186 / app.js 캐시 v=343** (2026-08-04 오전 배포·Render 반영 실측 — 배포 전 `version.js`·`index.html` 실값 재확인, 병행 세션이 올렸을 수 있음. 실제로 8/4에 v5.9.183을 병행 세션이 선점한 사례 있음).
- ⚠️ **병행 세션 주의**: 같은 작업폴더에서 세션 2개가 동시에 커밋하면 배포 준비 `git add`에 상대 세션의 미커밋 변경이 쓸려 들어감(2026-07-26 실사례 — 무해 확인 후 병합 배포). **핵심 로직 작업 2개가 겹칠 땐 반드시 따로 배포**(대표 확정 원칙). 배포 직전 `git log`·`git status`로 상대 세션 커밋 확인.
- git commit 메시지 끝에 Co-Authored-By / Claude-Session 푸터 붙이기(기존 커밋 참고).

---

## 1. 네이버 API 연동 구조 (IP 화이트리스트 우회)

네이버 커머스API는 **사전 등록된 개별 고정 IP에서만** 호출을 허용. Render의 아웃바운드 IP는 유동(랜덤) → 직접 호출 불가.
→ **고정 공인 IP를 가진 NCP 중계서버를 프록시로 경유**한다.

```
[Render 회사프로그램]  →  [NCP 중계서버 akkome-relay]  →  [네이버 커머스API]
   (naver-relay.js)        101.79.16.213 : 4000            api.commerce.naver.com
   Bearer 자체토큰만        네이버 시크릿 보관             (등록된 IP = NCP 공인IP)
```

- **네이버 client_id / client_secret 은 중계서버 .env 에만 존재.** Render/코드/GitHub에는 없음.
- Render는 중계서버 URL + 자체 Bearer 토큰(RELAY_AUTH_TOKEN)만 앎.

### 1-1. 중계서버 (NCP, akkome-relay)
- **공인 IP: `101.79.16.213`**, **포트: `4000`**
- 코드: 리포 `relay/` 폴더 (`relay/server.js`, `relay/install.sh`, `relay/설치가이드.md`).
- 현재 **HTTPS**로 동작 (자체서명 인증서). `cert.pem`/`key.pem` 있으면 HTTPS, 없으면 HTTP 폴백.
- `relay/server.js` 핵심:
  - `RELAY_VERSION` 상수를 `/health`에 노출 (install.sh 재실행으로 최신 반영됐는지 확인용). 현재 `'2026-07-24.2'`.
  - `/health` (인증 불필요, `?token=1`이면 토큰발급까지 시험) — token_test:"success" 나오면 인증 OK.
  - `/naver` (POST, 자체 Bearer 필요) — `{method, path, query?, body?}` 받아 네이버로 중계.
  - **허용목록(ALLOW)** = **읽기(GET) 전체 허용**: `/external/v1/pay-settle/`, `/external/v1/pay-order/seller/`, `/external/v1/pay-user/`, `/external/v1/seller/` + 상세조회 POST `/external/v1/pay-order/seller/product-orders/query`. **쓰기(발송처리·답변등록 등)는 전면 차단.** → 새 '조회' 기능 추가 시 **install.sh 재실행 불필요**(이미 다 열려있음). 쓰기는 나중에 명시적으로 추가.
  - 텔레그램 직접 호출 안 함(대표 설계). 오류는 journalctl 로그 + HTTP 응답으로만. 알림은 회사프로그램(텔레그램 시크릿 보유처)이 담당.

### 1-2. 중계서버 .env 구조 (`/opt/akkome-relay/.env`)
```
PORT=4000
NAVER_CLIENT_ID=(네이버 실제 client_id)
NAVER_CLIENT_SECRET=(네이버 실제 client_secret)
NAVER_TYPE=SELF
RELAY_AUTH_TOKEN=(openssl rand -hex 32 로 재생성한 값 — 설치스크립트 출력값 아님)
NAVER_API_BASE=https://api.commerce.naver.com
(TELEGRAM_* 는 비움)
```
- 실제 값 입력 완료됨(대표 직접).

### 1-3. Render 환경변수 (회사프로그램)
```
NAVER_RELAY_URL   = https://101.79.16.213:4000   (HTTPS 전환됨)
NAVER_RELAY_TOKEN = (중계서버 RELAY_AUTH_TOKEN 과 동일한 값)
NAVER_RELAY_CA    = (중계서버 자체서명 cert.pem 내용 — 인증서 고정용)
```
- ⚠️ HTTPS 전환·CA값 반영 여부는 대표 실환경 기준으로 재확인 필요할 수 있음.

### 1-4. 인증 방식 (bcrypt 전자서명) — 중계서버가 처리
- 토큰 발급: `POST https://api.commerce.naver.com/external/v1/oauth2/token`
  - Content-Type: `application/x-www-form-urlencoded` (**JSON 금지**)
  - 파라미터: `client_id`, `timestamp`(밀리초), `grant_type=client_credentials`, `client_secret_sign`, `type=SELF`
  - **서명** = `bcrypt(`${client_id}_${timestamp}`, salt=client_secret)` → base64 인코딩
  - 응답 `expires_in`(초, 보통 10800=3시간)만큼 토큰 캐싱(만료 1분 전 재발급).

### 1-5. 회사프로그램 측 클라이언트 코드
- `naver-relay.js` (리포 루트): Node 내장 http/https로 중계서버 호출. `callNaver({method,path,query,body}, notify)`, `relayHealth()`, `configured()`. 자체서명 인증서 고정(NAVER_RELAY_CA 있으면 `opt.ca`, 없으면 `rejectUnauthorized:false`).
- `server.js`에서 `const naverRelay = require('./naver-relay.js')`.
- 연결테스트: `GET /api/agent-office/naver/test` (adminOnly) — 데이터관리 화면 `#naver-connect-card` 버튼.

---

## 2. 인프라 현황 (NCP·Render·보안)

### 2-1. NCP ACG (방화벽) 인바운드 규칙
- TCP **22** ← 대표 회선 IP만(myIp). **⚠️ 대표 회선이 유동 IP** → 인터넷 회선이 바뀌면 NCP 콘솔에서 myIp 재설정해야 SSH 접속 됨.
- TCP **4000** ← `74.220.52.0/24`
- TCP **4000** ← `74.220.60.0/24` (Render 아웃바운드 대역)
- 3389(RDP) 규칙은 삭제함.

### 2-2. SSH 접속 (🔴 NCP 특성 — AWS와 다름)
- **NCP의 .pem 은 SSH 로그인 키가 아니라 root 비밀번호 복호화용.**
- 접속 순서: ①ACG 22 열림 확인 ②NCP 콘솔 '관리자 비밀번호 확인'에 .pem 첨부해 root 비밀번호 꺼냄 ③PowerShell `ssh root@101.79.16.213` + 그 비밀번호.
- **🔴 PasswordAuthentication 끄면 서버 잠김 → 절대 끄지 말 것.** (일반 SSH키 등록 후에만 키전용 전환 가능)
- 무차별대입 방어는 **fail2ban**으로(설치가이드 STEP5). 유동 IP라 ACG로 22를 특정 IP에 영구 고정 불가.

### 2-3. 서버 작업 방식
- **클로드는 코드/명령만 작성. NCP 서버 명령 실행은 대표가 PowerShell/SSH에서 직접.**
- 명령 줄 때: 복붙 가능한 한 덩어리 + 한국어 설명 + `여기에_입력` 자리표시 + 검증 명령 함께.

### 2-4. 미완료(인프라)
- 밀린 OS 업데이트(보안 포함) 미적용(재부팅 부담으로 보류).
- unattended-upgrades(자동 보안업뎃)은 ON.

---

## 3. 송장변환 자동 불러오기 진행상황 (4단계 [A]) — **작동 중, 실테스트 확인 단계**

### 3-1. 목적/흐름
네이버 **배송준비** 주문을 API로 가져와, 기존 송장변환의 스마트스토어 데이터로 **주입**한다. **변환 로직(convertDataSmart)은 절대 손대지 않음.** 수동 엑셀 업로드도 그대로 유지.

- **화면**: 송장변환 → 스마트스토어 카드 → **[🛰️ 네이버 배송준비 전체 불러오기]** 버튼 + 옆에 **조회 기간(일) 입력칸**(기본 40, 최대 180).
  - 마크업: `public/index.html` 의 `#invoice-auto-smart`, `#invoice-auto-smart-days`, `#invoice-auto-smart-msg` (~1625행).
  - 프론트 로직: `public/app.js` `loadNaver()` (~5637행). 결과를 전역 `invoiceDataSmart` 에 넣고 `updateInvoiceMergeBtn()`+`showInvoiceMergedPreview()` 호출 → 대표가 [통합 변환 및 다운로드] 누르면 기존 로직이 처리.

- **백엔드**: `GET /api/agent-office/naver/invoice-orders?days=N` (adminOnly, server.js ~5024행) → `naverFetchInvoiceOrders(days)` (server.js ~4950행).

### 3-2. `naverFetchInvoiceOrders(days)` 동작
- 네이버 조건형 조회 `GET /external/v1/pay-order/seller/product-orders` 호출.
  - query: `from`, `to`(ISO+09:00), `rangeType=PAYED_DATETIME`, `productOrderStatuses=PAYED`, `placeOrderStatusType=OK`, `pageSize=300`, `page` 순회.
- **days일을 24시간씩 나눠 순회**(네이버 1회 조회 최대 24시간 제약). 각 날짜마다 페이지 순회(최대 20p).
- 응답 항목 파싱(중첩 방어): `const c = it.content||it; const po = c.productOrder||it.productOrder||c; const od = c.order||it.order||po.order||{}; const sa = po.shippingAddress||{};`
- 스마트스토어 컬럼으로 매핑해서 rows 리턴. `convertDataSmart`가 읽는 키:
  - `구매자명`=od.ordererName, `구매자연락처`=od.ordererTel, `수취인명`=sa.name, **`옵션정보`=po.productOption**, `수량`=po.quantity, `수취인연락처1`=sa.tel1, `수취인연락처2`=sa.tel2, `통합배송지`=sa.baseAddress+detailedAddress, `배송메세지`=po.shippingMemo.
- 진단용 `sample`(개인정보 마스킹, `naverMaskPII`)·`sample_option` 반환 → 화면 🔧 진단에 표시.

### 3-3. 🔴 해결한 문제들 (이 세션)
1. **미매칭(빈값)** (v5.9.49): 주입 `옵션정보`를 상품명+옵션으로 넣었던 게 원인. **수동 엑셀은 옵션(productOption)만 매칭에 씀.** 대표 실엑셀(`스마트스토어_전체주문발주발송관리_20260725_1027.xlsx`)로 확인 — 8열 옵션정보 = "아꼼이네 상품선택: 1. 미니밤호박 특품최상급 / 상품 및 과수: 특품 5kg(10~20개)" 같은 옵션 문자열, 20열 상품명은 변환기가 안 씀. → `옵션정보=productOption`으로 수정. **대표 확인: 옵션 매칭 정상.** + content 래퍼 방어 추가.
2. **데이터 누락 1299건→183건** (v5.9.50): 최근 **3일**만 조회하던 게 원인.
   - **🔴 네이버 제약(공식문서·개발자포럼 확인)**: 네이버엔 **'현재 배송준비 상태 전체'를 주는 API가 없음**(상태 기반 목록조회 불가). **결제일 기준으로만 조회, 1회 최대 24시간, 최대 180일.**
   - → days 기본값을 **40일**로 확대(청귤 예약주문 성수기 = 결제일 한 달 이내분 커버). 화면에서 조절 가능(상한 180). 부족하면 일수 올리면 됨.
3. **중복방지 완전 제거** (v5.9.50, 대표 지시): 예전엔 "이미 올린 것 제외"(`naver_invoice_uploaded` 테이블)를 썼으나 **제거**. 이유: **중간발주 연동상 매번 '배송준비 전체'를 그대로 넘겨야 함**(거래처에 현재까지 들어온 물량 계속 알려야 하므로). 신규주문 발주확인은 대표가 수기. (테이블 정의만 잔존·무해)
4. **429 호출 제한** (v5.9.51): 40일 = 40회+ 연속 호출이 네이버 **초당 rate limit(429)**에 걸림 → `naverFetchInvoiceOrders` 안에 ①호출 사이 **350ms 간격** ②429 시 **지수 백오프 재시도**(1s→2s→4s→8s→16s, 최대 5회, `callWithRetry`) ③재시도 끝 실패 시에만 텔레그램 1회(스팸 방지) ④`naverFriendlyError`에 429 안내 추가. **⚠️ [B] 타이머 만들 때도 같은 간격/재시도 패턴 필수.**
5. **부분취소 수량** (v5.9.54): 4박스 주문→2박스 취소 시 배송준비는 2인데 4로 나옴(김혜경 사례). 네이버 `quantity`=**최초 주문 수량**(취소 미반영). 공식 구조체 수량 3필드: quantity·initialQuantity·**remainQuantity(남은 수량)**. → `수량 = remainQuantity 있으면 그것, 없으면 quantity`, remainQuantity=0이면 주문 제외. 결과 메시지에 "부분취소 수량 반영 N건" 표시. 중간발주도 같은 rows라 자동 연동. **⚠️ [B] 타이머·향후 수량 쓰는 모든 곳에서 remainQuantity 우선 규칙 유지.**
6. **하루 300건 잘림 (1298→999)** (v5.9.52): 기간을 180일로 늘려도 999건 동일 → 기간 문제 아님. 원인 추정: 응답 **totalPages 필드명이 예상과 달라 1페이지로 오판** → 주문 몰린 날(청귤 예약 오픈일)은 하루 300건(pageSize)에서 잘림. → 페이지 순회를 **"300건 만재면 다음 페이지 계속"**(짧게 오면 그 날 끝, 상한 50p) 방식으로 교체 — 응답 필드명에 무관하게 동작. 🔧 진단에 `page_info`(응답 상위 필드, PII 없음) 노출. **⚠️ 대표 확인: 취소·반품은 배송준비에 안 걸림 — 상태 정의 차이로 혼동하지 말 것. v5.9.52 재테스트(1298 근처 나오는지) 대기 중.**

### 3-4. ⚠️ 지금 확인 대기 중인 것
- 대표가 **[🛰️ 네이버 배송준비 전체 불러오기]** 눌러서 **건수가 배송준비 총건수(1299 근처)와 맞는지** 확인. (v5.9.50에서 429 떴고 → v5.9.51에서 간격+재시도로 대응 배포함. **v5.9.51 재테스트 결과 대기 중**)
- 40일 × 350ms 간격이라 **1~2분 소요**. 만약 화면이 "가져오는 중..."에서 **2~3분+ 멈춰** 있으면(Render/브라우저 타임아웃 위험) → **백그라운드 수집 방식**(뒤에서 모으고 진행률 표시, DB에 스냅샷 저장)으로 전환해야 함. 이게 [B]·중간발주 연동에도 유리.
- 발주확인·발송처리는 **대표 수기** (자동화 금지).

### 3-4-b. 중간발주 API 직결 (v5.9.53) — 완료·테스트 대기
- 송장변환 > **중간발주 탭**: 비번(4031) 엑셀 업로드 **제거** → **[📦 중간발주 시작하기]** 버튼이 같은 API(`/api/agent-office/naver/invoice-orders?days=N`)로 배송준비 전체를 불러와 `qtyRowsMain`에 주입 → 기존 `recomputeQtyAggregate()`(matchProduct로 옵션정보 파싱)·거래처 필터·품목추가·이미지 저장 **무수정** 동작. 조회 기간 입력 `#invoice-qty-days`(기본40·최대180). 문구 "현재 스마트스토어 배송준비로 중간발주를 진행합니다".
- app.js: `handleQtyUpload`/`setupQtyArea` 삭제, `setupQtyStart()`(~5765행) 추가, `resetInvoiceQty` 정리. `parseInvoiceRows` 등 헬퍼는 미사용 정의로 존치. 서버 `/api/invoice/decrypt` 존치.
- ⚠️ 불러오기 API는 **관리자 전용**(기존 정책) — 직원 계정은 중간발주 시작하기 사용 불가(필요 시 직원 허용 여부 대표에게 문의).

### 3-5. 정산 조회 (3단계) — 완료
- `GET /api/agent-office/naver/settlements?from&to` (adminOnly). 일별정산 `GET /external/v1/pay-settle/settle/daily` (필수: startDate·endDate·pageNumber·pageSize). 데이터관리 화면에서 조회. **대표 확인: 정산내역 정상.**
- 🔴 **네이버 정산금 ≠ 우리(세미) 거래처결제가 정산.** 체계 완전히 다름, 대조·병합 불가. 네이버 정산은 별도 영역으로만.

---

## 4. 다음 할 일

### 4-0. ✅ [B] 자동수집 타이머 — 완료 (v5.9.60 배포, 2026-07-25)
- 60초 틱 스케줄러(한 틱 1개 실행·몰림 방지), 수집기: 정산(하루1회 09:30 앵커·스냅샷+텔레그램)/주문(신규 결제 건수 알림)/취소·반품(**claimType/claimStatus로 판정** — lastChangedType 아님!·알림만)/문의(**pay-user/inquiries 파라미터 문서 미확정 — '지원 예정' 스텁**, 켜면 실패 표시). 전부 기본 OFF·설정은 naver_auto_collect(DB)·[데이터관리] 타이머 카드에서 관리.
- last-changed-statuses는 more.moreFrom/moreSequence 페이지네이션 필수(1회 상한 ~300건).
- **대표 확정: 송장변환 직전 취소 재확인 넣지 않음**(취소·반품은 배송준비 무관 — 취소는 PAYED 자동 이탈). 안전장치 = [자동 불러오기]가 항상 실행 시점 신규 조회. 타이머 수집분은 현황·통계용(변환 재사용 금지).
- 문의시나리오 DB 통합·판매현황 탭 이전·품목별금액 연동은 CLAUDE.md 상단 "★ 진행 중" 절 참조 (전부 완료·배포됨).

### 4-1. [B] 타이머 자동수집 (스마트스토어) — 다음 작업
- **전부 기본 OFF**, 화면에서 on/off + 주기(분) 변경 가능(**코드에 주기 하드코딩 금지**). `naver_auto_collect` 테이블 사용(이미 생성·시드됨: settlement 1440, order 60, claim 90, inquiry 30, 전부 OFF).
- 주기(제안): 정산 하루1번(1440분), 주문 60분, 문의 30분.
- **취소·반품 = 알림만**(대표가 (B)안 선택): 취소/반품 요청 들어오면 텔레그램 알림만. 자동처리 X.
- **🔴 "송장변환 직전 취소·반품 재확인" 기능은 넣지 말 것**(대표 지시로 제거됨): 반품은 배송준비에 없고, 취소는 PAYED에서 자동 이탈해 이미 필터로 빠짐. 취소는 대표 수기 확인.
- 네이버 **초당(token bucket) rate limit** 대비 실행 시각 분산(stagger). 일일한도 아님.
- 실패 시 텔레그램 알림(회사프로그램의 기존 텔레그램 사용).
- 변경 감지엔 `GET /external/v1/pay-order/seller/product-orders/last-changed-statuses`(PII 없음) 활용 가능.

### 4-2. 🛒 쿠팡 송장변환 연동 — ✅ 완료·검증 통과 (2026-07-26, v5.9.70 태그로 배포)
- **대표 검증 완료**: 키 입력(NCP) → 연결 3줄 초록불 → 자동 불러오기(3일 조회·발주서 1건) → 통합변환 → **수기 Wing 다운로드 변환과 완전 동일 대조 확인**. 취소 재확인·부분취소 공식 탑재.
- 지시문 `지시문_쿠팡_송장변환_연동.md` v2 기준. 스펙 `docs/superpowers/specs/2026-07-26-coupang-invoice-design.md`(공식 문서 확정 — v5 ordersheets/INSTRUCT·v6 returnRequests/cancelType=CANCEL·HMAC CEA·**수량=shippingCount−(hold+cancel)**).
- 중계서버 `/coupang`(조회 2종만·vendorId 강제·RELAY_VERSION 2026-07-26.1 — **install.sh 재실행 필요**), 회사프로그램 `callCoupang`+수집기+연결테스트+취소재확인, [쿠팡 자동 불러오기] 버튼. 변환 로직·네이버 코드 무수정.
- 대표 절차: `docs/쿠팡_키입력_절차.md` (SSH 키 입력 → 연결테스트 → 실주문 검증 체크리스트 §⑥ — 부분취소·보내는사람연락처 대조 포함). 키 만료 2027-01-21, 재발급 일정 2027-01-07 자동 등록.

### 4-3. 🏠 카페24(자사몰) 송장변환 연동 — ✅ 완료·검증 통과 (2026-07-26, v5.9.73) — **3채널 완성**
- **3채널 완성** (스마트스토어·쿠팡·자사몰). 스펙 `docs/superpowers/specs/2026-07-26-cafe24-invoice-design.md` (공식 문서 전 스펙 확정 — 추측 없음).
- 구조: **IP 제한 없음 → Render 직접 호출**(중계서버 불필요). 신규 모듈 `cafe24.js` + server.js 라우트 4종(`/api/cafe24/auth-url`·`/api/cafe24/callback`(공개, state 검증)·`/api/agent-office/cafe24/test`·`/api/agent-office/cafe24/invoice-orders?days=1~90`).
- OAuth: Authorization Code, mall `akkome`, Client ID `mMdlm3cHGZwkVaem7wGDIB`(공개값, 코드 상수), scope `mall.read_order`. **Secret = Render env `CAFE24_CLIENT_SECRET`**(대표 직접 입력, 열람 금지). 토큰 AES-256-GCM 암호화 저장(`agent_office_config` 'cafe24_tokens', 키=sha256(Secret)) — Secret 변경 시 재승인 1회. access 2h/refresh 2주, 만료 60초 전 자동 갱신(**동시 갱신 락** — 갱신 시 refresh도 교체되므로 필수). 갱신 실패 → 텔레그램 1회 + "재승인 필요".
- 취소 방어: akkome 몰은 **결제 즉시 N20(배송준비중) 자동 진입**(발주확인 없음) → **품목 단위 `items[].order_status==='N20'`만 채택**(취소신청 품목은 C코드로 변경돼 자연 제외) + **수량 = quantity − claim_quantity**(0 이하 제외). 반품 로직 없음.
- **대표 검증 결과 (2026-07-26)**: ①②Secret 입력·[동의]·연결 테스트 🟢 ③**9건 자동 불러오기→변환→수기 대조 완전 동일 — 통과**(옵션 표기 포함, 매핑 조정 불필요) ④취소 제외 — 몰 구조상 취소신청 시 배송준비중에서 즉시 이탈 → 실행 시점 조회로 충분, 탑재된 품목 필터+수량 차감은 **이중 안전벨트로 유지** ⑤토큰 자동 갱신은 오후 4:23 이후 재불러오기로 확인 예정(유일한 잔여 확인 — 실패 시 [연결 테스트]에 "재승인 필요" 표시됨).

### 4-4. (보류) 입금예정 캘린더
- 대표가 별도로 작업 중이라 클로드는 손대지 않음.

---

## 5. 작업 규칙 (대표 지시 — 반드시 준수)

- **변환 로직(convertDataSmart) 절대 수정 금지.** 자동 불러오기는 invoiceDataSmart에 주입만.
- **추측 금지 — 스펙은 공식 문서에서 확인.** 엔드포인트 probe(찔러보기) 금지.
  - apicenter 직접 fetch 차단됨 → **r.jina.ai 프록시**로 읽음(`https://r.jina.ai/https://apicenter.commerce.naver.com/docs/...`). SPA라 본문 안 나오면 WebSearch로 개발자포럼(github.com/commerce-api-naver/commerce-api/discussions) 확인.
- **결과물은 클로드가 직접 검증 후 보고.** 애매한 건 그대로 복명복창.
- **부작용 사전 고지**: 수정이 다른 문제를 유발할 수 있으면 먼저 대표에게 알리고 진행. 아니면 바로 실행.
- **쓰기(발송처리 등) 자동화 금지** — 현재 전부 읽기 전용. 발주확인·발송처리는 대표 수기.
- 물리삭제 금지·soft-delete, audit_logs 기록(출처 naver-api).
- 시크릿·토큰을 코드/커밋에 넣지 말 것(PUBLIC 저장소).

---

## 6. 핵심 파일 위치 요약

| 항목 | 위치 |
|---|---|
| 중계서버 코드 | `relay/server.js`, `relay/install.sh`, `relay/설치가이드.md` |
| 회사프로그램 중계 클라이언트 | `naver-relay.js` (루트) |
| 자동 불러오기 백엔드 | `server.js` `naverFetchInvoiceOrders()` ~4950행, 엔드포인트 ~5024행 |
| 자동 불러오기 프론트 | `public/app.js` `loadNaver()` ~5637행 |
| 자동 불러오기 UI 마크업 | `public/index.html` ~1616~1632행 (스마트스토어 카드) |
| 변환 로직(수정금지) | `public/app.js` `convertDataSmart()` ~5514행, `matchProduct()` ~5340행 |
| 연결테스트/정산 | `server.js` `/api/agent-office/naver/test`, `/api/agent-office/naver/settlements` |
| 자동수집 테이블 | `naver_auto_collect` (server.js initDB, 시드 완료) |
| 최초 설계 인수인계 | `NCP인증키/네이버커머스API_연동_인수인계서.md` |
| 🎨 디자인 가이드 (**디자인 작업 전 필독**) | `디자인/디자인_가이드.md` — 인디고 테마 확정·토큰·공통 컴포넌트·노랑 리테마 금지 |
| 버전 | `version.js` (현재 v5.9.50), `public/index.html` app.js 캐시(현재 v=258) |
