// 지율 (법무팀장 · 노무) — 지시 #44: 역할 부여 (0원 — 지침서 주입 전까지 모델 호출 없음)
// 절대 규칙: [노무 지침서]만 근거로 답변. 지침서가 아직 주입되지 않았으므로 모든 질문에
// "지침서 대기 중 — 범위 밖" 정직 응답 후 정지 (일반 지식 추측 금지). 휴가는 결재 시스템 안내.
module.exports = {
    live: true, // 배정은 받되, 지침서 주입 전엔 정직 정지 응답만 (모델 호출 없음)
    steps: ['노무 지침서 확인 중...'],
    stepDelayMs: 1200,
    async result({ params = {} }) {
        const q = String(params.order_content || '').slice(0, 200);
        return {
            summary: '지침서 대기 중 — 범위 밖 (정직 안내)',
            lines: [
                '노무·휴가·근태 답변은 [노무 지침서]만 근거로 합니다 (일반 지식 추측 금지 — 지시 #44)',
                '지침서가 아직 등록되지 않아 답변드릴 수 없습니다 — 노무사 확인이 필요합니다',
                '휴가 신청·승인은 기존 결재 시스템(휴가신청서)을 이용해주세요',
                '대표님이 노무사 지침서 파일을 확인해 주시면 즉시 자문을 시작합니다',
            ],
            report: {
                type: 'jiyul_labor',
                pending_guide: true,
                question: q,
                note: '지침서 주입은 대표 파일 확인 시점 별도 (지시 #44 — 로드맵 v5.4) · 답변 형식: ①결론 ②근거(지침서 항목) ③실무 처리 순서 ④주의점',
            },
        };
    },
};
